# Intent Lineage Corpus v0

Story 6.2 AC4 — NFR-Aud-14 100% intent_lineage coverage falsifiability artifact.

Generated by `crates/maos-eval/fixtures/intent-lineage-corpus-v0/`.

## Class breakdown (50 scenarios total)

| Class | Count |
|---|---|
| `lineage_chain_uninterrupted` | 15 |
| `lineage_union_via_distillate` | 15 |
| `lineage_broken_spirit_auto_strips_field` | 10 |
| `lineage_continuity_across_retract` | 10 |

## Per-scenario assertions

- **`lineage_chain_uninterrupted`** — assert lineage carries originating intent unchanged across N=1..5 hops.
- **`lineage_union_via_distillate`** — assert `DistillationReceipt::intent_lineage == [a, b]` for two source intents.
- **`lineage_broken_spirit_auto_strips_field`** — assert `EIntentLineageBroken` REJECTS empty-lineage SpiritAuto cross-Spirit emissions.
- **`lineage_continuity_across_retract`** — assert `IacBusAdapter::retract` copies the original frame's lineage into the emitted Retract frame.

## Runner

`crates/maos-kernel-core/tests/intent_lineage_corpus_v0.rs` loads this directory
and asserts per-scenario outcome. The discipline.yml gate
`nfr-aud-14-intent-lineage-corpus` runs the runner on every PR touching
`crates/maos-kernel-core/src/iac/`.
