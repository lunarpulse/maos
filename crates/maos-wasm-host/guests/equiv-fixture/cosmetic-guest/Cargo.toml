[workspace]

[package]
name = "equiv-cosmetic-spirit"
version = "0.1.0"
edition = "2021"
publish = false
description = "Story 11.1b cross-form equivalence gate — WASM cosmetic-delay fixture (invariants preserved)"

[dependencies]
wit-bindgen = "0.44"
equiv-fixture-logic = { path = "../logic" }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
panic = "abort"
strip = true
