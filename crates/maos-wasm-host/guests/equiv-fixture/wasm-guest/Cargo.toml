[workspace]

[package]
name = "equiv-identity-spirit"
version = "0.1.0"
edition = "2021"
publish = false
description = "Story 11.1b cross-form equivalence gate — WASM identity fixture (echo)"

[dependencies]
wit-bindgen = "0.44"
equiv-fixture-logic = { path = "../logic" }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
panic = "abort"
strip = true
