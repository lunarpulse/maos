//! maos-registry-server — thin binary entry-point.
//!
//! Wired in Task 13 (MAOS_ONE_SHOT=registry-server).
fn main() {
    eprintln!("maos-registry-server: not yet wired — Story 5.5d Task 13");
    std::process::exit(1);
}
