# CI Baselines

This directory records the resolved SHA and gate results of every founding-sprint and epic baseline CI run.

## Rule

Any gate that was **green** in a baseline file must stay green in all subsequent PRs. If a gate goes from green to red, the failing PR cannot merge until the regression is explained and fixed.

## Baselines

### v0.1-α

- **File:** `v0.1-alpha.json`
- **Commit:** TBD (populated after first green `discipline.yml` run on `main`)
- **Tag:** `v0.1-alpha-ci-baseline`

Generated by Story 0.1 after the first all-green CI run. Every subsequent epic's first CI run is compared against this baseline.

## Format

```json
{
  "commit_sha": "abc123...",
  "workflow_run_id": 123456789,
  "gate_results": {
    "ac1": "ok",
    "ac2": "ok",
    "ac3": { "alarm": false, "hardfail": false },
    "ac4": "ok",
    "ac5": "ok"
  },
  "timestamp_utc": "2026-05-11T00:00:00Z",
  "runner_image_sha": "sha256:..."
}
```

## Branch Protection

PRs cannot merge unless the `aggregate` job in `.github/workflows/discipline.yml` is green. This rule must be configured manually in the repository's branch-protection settings.
