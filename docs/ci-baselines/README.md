# CI Baselines

This directory records the resolved SHA and gate results of every founding-sprint and epic baseline CI run.

## Rule

Any gate that was **green** in a baseline file must stay green in all subsequent PRs. If a gate goes from green to red, the failing PR cannot merge until the regression is explained and fixed.

## Baselines

### v0.1-α

- **File:** `v0.1-alpha.json`
- **Commit:** TBD (populated after first green `discipline.yml` run on `main`)
- **Tag:** `v0.1-alpha-ci-baseline`

Generated by Story 0.1 after the first all-green CI run. Every subsequent epic's first CI run is compared against this baseline.

## Format

```json
{
  "commit_sha": "abc123...",
  "workflow_run_id": 123456789,
  "gate_results": {
    "ac1": "ok",
    "ac2": "ok",
    "ac3": { "alarm": false, "hardfail": false },
    "ac4": "ok",
    "ac5": "ok"
  },
  "timestamp_utc": "2026-05-11T00:00:00Z",
  "runner_image_sha": "sha256:..."
}
```

## Branch Protection

PRs cannot merge unless the `aggregate` job in `.github/workflows/discipline.yml` is green. This rule must be configured manually in the repository's branch-protection settings.

## Dogfood checklist for invariant-touching PRs

When a PR touches any `docs/invariants/I*.md` file, the author must produce all three
artifacts required by the `invariant-lock` gate (Story 0.1 AC5 / ADR-037):

1. **Cadence touch:** Confirm that the `enforcement_cadence` block in the touched
   `I*.md` is modified (additive only — new rows allowed, regressions rejected).
2. **Corpus delta:** Confirm that `tests/coverage-matrix.yaml` is touched in the
   same diff (adding or updating rows for the affected invariants/NFRs).
3. **≥2 maintainer reviewers:** Explicitly request review from two maintainers in
   the PR description. The merge is blocked until both approvals are granted
   (operator-side ceremony, not enforceable by xtask logic).

## Founding-sprint baseline gates

The v0.1-alpha `discipline.yml` runs the following gates:

| Gate | Story | Status |
|------|-------|--------|
| reproducible-build | 0.1 | required |
| check-unsafe | 0.1 | required |
| check-empty-kernel | 0.2 | required |
| check-loom | 0.2 | required |
| check-service-boundary | 0.2 | required |
| kloc-check | 0.1 | required |
| abi-diff | 0.1 | required |
| invariant-lock | 0.1 | required |
| check-corpus | 0.3 | required |
| check-judge-config | 0.3 | required |
| coverage-matrix | 0.3 | required |
| corpus-staleness | 0.3 | required |
| corpus-rebaseline | 0.3 | scheduled, not per-commit |

**Any green gate going red is a merge-block.**
