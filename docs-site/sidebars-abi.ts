import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const sidebarsAbi: SidebarsConfig = {
  abi: [
    "index",
    "lifecycle",
    "ctx",
    "compliance",
    "identity",
    "cancellation",
    "gateway",
    "deprecation",
    "constants",
  ],
};

export default sidebarsAbi;
