# ABI-extension proposal ratification manifest (ADR-045 §4 / F6).
#
# Every abi-diff-detected ABI change must be covered by a ratified entry
# here, or the `xtask check-abi-ratification` gate fails.
# Base case: no ABI changes + no entries = PASS (∅ ⊆ anything).

# Story 9.3b — FR62/FR64 kernel re-pin, the FIRST ratified AbiExtensionProposal.
# Dogfoods ADR-045 §8: the re-pin is recorded through the governance model it introduces.
[[ratification]]
proposal_id = "9.3b-FR62-FR64-kernel-repin"
summary = "FR62 GovernanceEvent (28) + FR64 CostAttribution (29) FrameKind discriminants + cost emission at inference/mod.rs + SR-3 forget-cascade extension"
adr_ref = "ADR-045,ADR-046"
status = "ratified"
covered_changes = [
  "GovernanceEvent",
  "CostAttribution",
  "CostAttributionPayload",
  "PrincipalRef",
  "AttributionSource",
  "AttributionConfidence",
  "CostDimension",
  "ProviderPricingConfig",
  "ProviderPricingEntry",
  "SchemaRegistryEntry",
  "GovernanceEventPayload",
  "GovernanceEventKind",
  "VetterKeyPayload",
  "SchemaLifecyclePayload",
  "RatificationStatus",
  "AbiExtensionProposal",
]
ratified_at = "2026-06-14"

# Story 9.4b AC-6 — model-provenance manifest field + FR62 governance journaling.
# Purely-additive ABI surface (D2: ratification-only, zero kernel-core re-pin).
# Records the schema 2→3 bump and the new model-provenance governance stream.
[[ratification]]
proposal_id = "9.4b-AC6-model-provenance"
summary = "MANIFEST_SCHEMA_VERSION 2→3 + [model_provenance] section (ModelProvenanceSection) + FR62 ModelProvenance governance stream (ModelProvenancePayload) + catalogued ProvenanceError; admission gate validate_model_provenance (D6/D7)"
adr_ref = "ADR-045"
status = "ratified"
covered_changes = [
  "MANIFEST_SCHEMA_VERSION",
  "ModelProvenanceSection",
  "ModelProvenancePayload",
  "ModelProvenance",
  "MODEL_PROVENANCE_SCHEMA_ID",
  "ProvenanceError",
  "EModelProvenanceMissing",
  "EModelProvenanceStale",
  "ModelProvenancePolicy",
  "ModelProvenanceRecord",
  "validate_model_provenance",
  "GovernanceEventKind",
  "GovernanceEventPayload",
]
ratified_at = "2026-06-15"
