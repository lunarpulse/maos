# maos-kernel-core/src — SINGLE SOURCE OF TRUTH for the pinned line count.
#
# Established by Story 8.16 (Epic 8 retro §A4): per-story "byte-identical / +N"
# claims were locally true but never SUMMED — story records cited 16263 while the
# only enforced literal (maos-a2a-tcp t11_t12_chaos_absence.rs) and the true HEAD
# count were 21128. This file is the ONE pinned value; both the
# `check-kernel-baseline` xtask gate AND the maos-a2a-tcp zero-kernel guard read
# it, so a multi-story phase can never again drift unsummed.
#
# HISTORY (authorized deltas only):
#   15505  Story 8.4  — last pre-live-runtime pin (founder-loop wedge, zero kernel KLOC)
#   16263  Story 8.11 — live runtime spine (first authorized byte-break since 8.4; budget seam)
#   ~16992 Story 8.12 — live CliWrapper subprocess bridge (+729, charter-amended kernel delta)
#   21197  Story 9.1  — ship maosctl audit subcommands (+69, charter-amended kernel delta).
#          FLAG-Winston: authorized 2026-06-12 to support audit-read-side composition
#          wiring (shared SecurityManagerAdapter/CapabilityRegistryAdapter owner and
#          transparency-log query paths). The delta is additive; no public symbols removed.
#   21276  Story 9.2  — GDPR Article 17 erasure spine (+79, charter-amended kernel delta).
#          FLAG-Winston: authorized Decision C — distillate redaction-marker frame +
#          conditional body-scrub on forget; additive only; no public symbols removed.
#   21336  Story 9.2 review (2026-06-13) — GDPR erasure review patches (+60,
#          charter-amended kernel delta). P29 durable legal-hold store wiring
#          (place/release/consult in forget_with_reason), P3 content-filtered
#          distillate scrub, P7 atomic frame-id capture, P2 case-insensitive
#          hold, REGISTERED_ERASURE_BACKENDS const + release_legal_hold. All
#          additive; no public symbols removed.
# To change this number you MUST be landing an AUTHORIZED kernel delta (charter
# amendment + FLAG-Winston), and you update it HERE — nowhere else.
#   21472  Story 9.3b — FR62 GovernanceEvent + FR64 CostAttribution kernel delta
#          (+136, charter-amended kernel delta, jointly re-pinned).
#          FLAG-Winston: authorized per ADR-045/ADR-046. FR64 cost emission at
#          inference/mod.rs (+68), SR-3 forget-cascade extension in memory/mod.rs
#          (+70), AC6 erasure schema-version stamp in memory/mod.rs. All additive;
#          no public symbols removed. Re-pin recorded as the first ratified
#          AbiExtensionProposal (ADR-045 §8 dogfooding).
#   21894  Story 9.4b AC-5/AC-8 — region-pinning (TL-anchored, Option A) +
#          provider_history bound (+422, charter-amended kernel delta).
#          FLAG-Winston: authorized 2026-06-15 (re-ratification R2 + tripwire
#          authorization). Production delta ~+237 lands ~21709 — UNDER the
#          ratified HARD STOP of 21720; the remaining ~+185 is merge-blocking
#          TEST code the line-count gate includes (R-RG1/R-RG2/R-RG3 in
#          memory/write_entry_point.rs, AC-8 provider_history_tests in
#          security/mod.rs, RegionSection tests in security/operator_config.rs).
#          SHAPE VERIFIED Option A: RegionSection config + WriteEntryPoint
#          region chokepoint + memory-adapter wiring + bounded ProviderHistory;
#          NO per-row crypto / Option-B limb. CryptoProvider untouched; region
#          HKDF derivation lives in maos-audit (not kernel-core). All additive;
#          no public symbols removed.
#   22226  Story 9.6 — baseline reconciliation at multi-Spirit scheduler cutover
#          (+332 observed at HEAD before this story's maos-bin-only driver loop;
#          no maos-kernel-core files changed by Story 9.6). FLAG-Winston carry-forward:
#          visibility blocker resolved by using public scheduler.load()/returned pid
#          and existing public pick_next_spirit_from_slice/HookDispatcher surfaces;
#          allocate_pid remains pub(crate), so no new kernel ABI was exposed.
#   22227  Story 9.6 review closure — added `#[i9_exempt]` to `ProviderHistory`
#          (`crates/maos-kernel-core/src/security/mod.rs`) to resolve a pre-existing
#          I9 / P3 violation inherited from Story 9.4b AC-8. The bounded
#          last-provider tracker is ephemeral switch-detection state, not learned
#          state; +1 line. FLAG-Winston: this is an invariant-lock exemption, not a
#          functional kernel delta.
#   22266  Epic 10 §A3 security prep — validate_namespace_write de-stub (+39,
#          charter-amended kernel delta). FLAG-Winston: authorized for
#          sec-redteam SR-1 (Story 9.3b). De-stubs validate_namespace_write
#          in memory/mod.rs: adds principal_write_enforcement RwLock<HashSet>,
#          with_principal_write_enforcement() builder, authorize_principal_writes()
#          method, and enforcement check in write(). PrincipalWriteUnauthorized
#          error variant added in maos-domain (not counted here). All additive;
#          no public symbols removed.
#   22269  Epic 9 CI-gate closure — check-serde-error-handling propagation in
#          maos-kernel-core: inference/mod.rs cost-attribution TL row now
#          best-effort skips on the near-infallible serde failure (was .expect);
#          lifecycle/upgrade.rs post-success TL payload keeps its graceful
#          fallback via a match (was .unwrap_or_else). +3 lines, additive, no
#          public ABI change.
#   22300  Story 10.4a — Loom-lite collective tier kernel de-stub (+31,
#          charter-amended kernel delta, FIRST authorized kernel delta in
#          Epic 10). FLAG-Winston: authorized 2026-06-22 (party-mode
#          preflight ratified). Added CollectiveMemoryPort injection seam:
#          +1 field (Option<Arc<dyn CollectiveMemoryPort>>), +1 builder
#          method (with_collective_port), +3 delegating match arms in
#          write/read/scan (MemoryTier::Collective → port.{write,read,scan}).
#          The `:709` error variant STAYS (port-absent return). Dependency
#          arrow: maos-loom-lite → maos-domain (trait) ← maos-kernel-core;
#          adapter injected at daemon composition root. ADR-006 ref.
#          22488  Story 10.4a review rework (2026-06-22, glm-5.2) — AC1
#          collective-tier mediation completion (+188, charter-amended kernel
#          delta). FLAG-Winston: authorized under Decision A (party-mode
#          ratified 2026-06-22) — the cap-gated I1/I2 mediation that the
#          de-stub required to not be inert. Adds: `capabilities` field +
#          `with_capabilities` builder; cap-gated `collective_write/read/scan`
#          (verify_and_audit = I2 + scope-check = I1); `reject_principal`
#          guard (Decision D — Principal partitioned out of the Collective
#          tier) in the 3 trait arms; MemoryError::Collective variant
#          (maos-domain); Intent::Loom{Read,Write,Scan} (cap_policy) +
#          scope_to_intent arms. All additive; no public symbols removed.
#   22574  Story 10.4a review patches (2026-06-23, patch group A) — wire
#          I1/I2 into the LIVE dispatch path + LoomWrite TTL + typed
#          Collective error (+86, charter-amended kernel delta).
#          FLAG-Winston: authorized under the Story 10.4a Decision-A rework
#          ratification — the prior rework left the cap-gated
#          collective_write/read/scan with ZERO callers and the three trait
#          arms calling the port DIRECTLY with no capability check; this delta
#          closes that gap so AC1's "capability check BEFORE the port call"
#          holds on the live path. P1: the three MemoryTier::Collective trait
#          arms now DENY fail-closed when `self.capabilities` is wired (the
#          MemoryManagerPort ABI carries no token/posture, so the unmediated
#          trait path CANNOT mediate — callers MUST use the cap-gated
#          collective_write/read/scan); only the legacy/test path (no caps
#          injected) falls through to the direct port delegation. P8:
#          collective_write asserts a LoomWrite token's remaining TTL ≤ 60s
#          before the port call (belt-and-braces over the issuance-side cap).
#          P12: a collective_port_error helper maps CollectivePortError → the
#          typed MemoryError::Collective{kind,reason} discriminant. The
#          CollectiveErrorKind enum lives in maos-domain (not counted here).
#          ADR-006 / AC1 ref. All additive; no public symbols removed.
#   22726  Story 10.5 AC3 — Windows binary sandbox body (windows.rs) + mod.rs
#          Windows drop/classify_exit paths (+152, AUTHORIZED kernel-core delta).
#          FLAG-Winston: AC3 is the single AC in Story 10.5 permitted to touch
#          kernel-core (per story preflight flag #4). Initial implementation
#          added the Windows sandbox body; code review immediately found stub
#          token usage and post-spawn Job Object assignment.
#   22929  Story 10.5 code-review patches — Windows sandbox enforcement hardening
#          (+203, AUTHORIZED AC3 kernel-core delta). FLAG-Winston: closes review
#          findings by running children through CreateProcessAsUserW with the
#          restricted low-integrity token, creating the process suspended,
#          assigning the Job Object before ResumeThread, enabling kill-on-close,
#          and using Job Object CPU rate control instead of a fixed lifetime
#          CPU-time budget. All additive; no public symbols removed.
#   22955  Story 10.5 AC3 windows.rs compile-correctness fix (+26, AUTHORIZED AC3
#          kernel-core delta). FLAG-Winston: the 22929 pin was recorded for a
#          windows.rs that DID NOT COMPILE — Epic-10 retro §A2 re-review finding R1
#          (missing `Win32_System_JobObjects` feature + `HANDLE(x as isize)`, which
#          is invalid under windows 0.58 where `HANDLE` wraps `*mut c_void`, not
#          `isize`). This delta makes the AC3 Windows binary-sandbox body actually
#          build on x86_64-pc-windows-msvc (compile-proven via an isolated
#          cross-target check of windows.rs + stubbed super:: types), and replaces
#          the inert working-set memory hint with a real commit cap (retro finding
#          H1: `JOB_OBJECT_LIMIT_PROCESS_MEMORY` set via `SetInformationJobObject`,
#          re-asserting `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`). Net +26 confined to
#          windows.rs (+34/-8). All additive; no public symbols removed. Windows
#          RUNTIME (R4/R5) still requires a CI windows-latest runner — compile is
#          proven from this Linux host, runtime is not.
#   ── Story 10.5 R3 DISCLOSURE (Epic-10 retro §A2 re-review; 2026-06-27) ──
#   The Story 10.5 squashed commit (0d2b18e) also drew the kernel budget in SIX
#   files OUTSIDE the authorized windows.rs / sandbox surface, via a
#   behavior-preserving `cargo fmt` line-wrap reflow that the 22726 and 22929
#   entries above attributed WHOLLY to windows.rs. This note discloses that
#   churn — the exact "silent budget draw" the D7 tripwire exists to surface
#   (re-review finding R3). Affected files, net +25 lines (71 insertions /
#   46 deletions), reflow ONLY, no functional change:
#     memory/mod.rs, memory/principal.rs, memory/read_entry_point.rs,
#     memory/write_entry_point.rs, security/mod.rs, security/operator_config.rs
#   (e.g. operator_config.rs:323 — one assert_eq! split across four lines).
#   RETAINED, not reverted: there is NO `cargo fmt --check` gate, so a revert is
#   non-durable (the next `cargo fmt` reintroduces it), and these lines are
#   ALREADY inside the pinned count below — so this disclosure changes the
#   NARRATIVE, not the number (pin stays 22955; gate remains green). FLAG-Winston:
#   authorized as a behavior-preserving after-the-fact disclosure. Hardening
#   follow-up (separate, may surface drift elsewhere): add a workspace
#   `cargo fmt --check` gate so future fmt churn cannot draw the budget silently.
#   22964  Story 10.5 R4-bundle hardening in windows.rs (+9, AUTHORIZED AC3
#          kernel-core delta). FLAG-Winston: two correctness fixes the §A2
#          re-review folded into the R4 close (next_actions LOW hardening):
#          (1) `spawn_sandboxed` now closes the restricted token via
#          `.map_err(|e| { CloseHandle(restricted_token); e })` if
#          `create_limited_job` fails — previously the token handle leaked on a
#          Job-setup error (windows.rs error path). (2) the per-process commit
#          cap is skipped when `memory_max_mb == 0` (`.filter(|mb| *mb > 0)`) —
#          a zero cap is rejected by SetInformationJobObject (ERROR_INVALID_PARAMETER).
#          Confined to windows.rs; behavior-preserving on the success path. The
#          R4 enforcement negative controls themselves live in
#          tests/sandbox_enforcement_windows.rs (NOT counted — tests/, not src/).
#          All additive; no public symbols removed. Windows compile/runtime still
#          CI-only (windows-latest); not verifiable from this Linux host.
#   23023  Story 11.2a — charter-amended kernel delta: bounded FLAG-Winston
#          CrossRegionReadmit provenance arm (+59, incl. test LOC).
#          FLAG-Winston: authorized 2026-07-02 (party-mode preflight, unanimous).
#          Added WriteEntryPoint::CrossRegionReadmit { source_region, source_log_ref }
#          variant + SourceLogRef struct + one source_region() match arm in
#          write_entry_point.rs. enforce_region body = 0-LOC (generic over
#          source_region()). r_rg_cross_region_readmit_enforced test +
#          r_rg3 exhaustiveness array updated. Functional src delta ~10 LOC;
#          remainder is doc comments + test code the line-count gate includes.
#          All additive; no public symbols removed. ADR-049 ref.
#   23040  Story 11.4a (F2, FLAG-Winston) — bounded org-level deny layer
#          (+17, AUTHORIZED kernel-core delta; party-mode preflight ratified
#          2026-07-05, unanimous Winston·Murat·John·Amelia+Mary). Added
#          OperatorPolicyConfig.per_capability_deny (HashSet<String>) + one
#          deny-wins arm in PolicyTable::evaluate placed BEFORE the grant
#          checks (Cedar forbid-beats-permit). The kernel keeps the ceiling
#          (I1) — subtract-only; an out-of-kernel PolicyDecisionPort adapter
#          (maos-pdp) populates the set at the composition root. The deny is
#          observable via the existing Err(CapError::PolicyDenied) issue path
#          (CATCH-B); rich forbid-rule attribution is 11.4c's SIEM slice.
#          Confined to cap_policy/mod.rs (field + arm + doc). F2 unit tests
#          live in tests/policy_per_capability_deny.rs (NOT counted — tests/,
#          not src/). All additive; no public symbols removed. ADR-050 ref.
#   23081  Story 11.4a code-review patches — bounded follow-up to the same
#          FLAG-Winston surface. Added stable PDP action-key lookup (replacing
#          Rust Discriminant Debug keying), subject-scoped
#          OperatorPolicyConfig.per_spirit_capability_deny, and schedule
#          watchdog denial logging for CATCH-B observability. Still confined to
#          the policy/mediation observability surface; no PDP engine dependency
#          enters kernel-core; all additive, no public symbols removed.
#   23082  Story 12.3 review-closure reconciliation — carry-forward of a
#          pre-existing, behavior-preserving `cargo fmt` reflow committed by the
#          Story 12.2 commit (0c733d33) that was never re-pinned, leaving `main`
#          RED on check-kernel-baseline at Story 12.3's baseline. Net +1 line,
#          reflow ONLY, ZERO functional / ABI change (Story 12.3 itself adds ZERO
#          kernel-core delta — the receipt courier + observer are entirely
#          out-of-kernel in maos-cohort / maos-a2a-core). Affected files
#          (`crates/maos-kernel-core/src/`), reflow only:
#            capability/cap_policy/mod.rs — closure + method-chain line-wrap
#              (`is_some_and` closure collapsed; `per_capability_approval.get`
#              collapsed), net -4;
#            memory/write_entry_point.rs — two `assert!(cond, "msg")` test lines
#              split across four lines each by rustfmt, net +6.
#          Same class as the Story 10.5 R3 disclosure above (silent fmt budget
#          draw, no `cargo fmt --check` gate). FLAG-Winston: authorized by
#          Lunarpulse 2026-07-11 as a behavior-preserving reconciliation so the
#          green-at-HEAD prerequisite holds; no public symbols removed.
#   23141  Story 12.5 code-review FLAG-Winston — PostSwapMonitor halt-set-delta
#          fix + proven-red tests. The review found a latent kernel defect the
#          12.5 drain-per-hop migration mechanism activates: initiate_swap
#          snapshotted the PRE-drain pending halt-ids, so after step-3's I14 gate
#          drained a pending halt the PostSwapMonitor saw it "missing" and
#          auto-reverted the (valid) swap ~1s later — a silent safety-guarantee
#          falsifier on the Epic-12 closer. Fix (hot_swap/coordinator.rs):
#          snapshot the post-drain SURVIVOR set (only halts that must persist).
#          Extracted the pure `detect_halt_set_loss` helper (post_swap_monitor.rs)
#          + 4 in-src #[cfg(test)] proven-red tests (RED: a pre-drain snapshot
#          false-flags a drained halt; GREEN: a survivor snapshot does not;
#          genuine loss still caught). Net +59 = fix logic (~+8 src) + the in-src
#          proven-red tests (the private helper cannot be reached from tests/).
#          No public symbols removed; no new kernel dependency. FLAG-Winston:
#          authorized by the Story-12.5 review team consensus
#          (Winston/Murat/Vex/John/Amelia, per-spec + long-term correctness) 2026-07-13.
#   23147  Story 12.6 user-authorized formatter reconciliation — `cargo fmt`
#          normalized pre-existing layout drift in
#          hot_swap/post_swap_monitor.rs and
#          tests/hot_swap_halt_continuity_corpus_integration.rs, net +6 physical
#          source lines. No behavior, public API, or dependency change.
#          FLAG-Winston: Lunarpulse authorized the formatter run and this
#          baseline reconciliation on 2026-07-13.
#   23202  J1 Tier-2 live-agent bridge — real-CLI stdin deadlock fix (+55,
#          AUTHORIZED kernel-core delta). FLAG-Winston: Lunarpulse authorized
#          2026-07-15 ("approving proper fix"). The J1 Tier-2 signed-live-run
#          bridge spawns a REAL worker CLI (`codex exec`) that reads
#          stdin-until-EOF for its prompt; `spawn_and_bridge` held the child's
#          stdin open as a control channel, so the worker BLOCKED reading stdin
#          while the bridge BLOCKED reading the worker's output — a mutual
#          deadlock. The hermetic fixture never read stdin, so this stayed
#          invisible through T1–T5 (the "PROVEN" runs were fixture-only). Fix
#          (lifecycle/cli_wrapper/runtime.rs, spawn_and_bridge): when
#          `control_channel` is `Signals` (worker is NOT driven via stdin), drop
#          the `ChildStdin` right after spawn so the child receives EOF and
#          proceeds on its argv prompt. `StdinCommands` (the J1 bench's
#          `write_stdin_line` path) keeps stdin open — unaffected. Net +55 = ~9
#          functional (the match arm) + ~46 the in-src #[cfg(test)] regression
#          (signals_control_closes_worker_stdin_so_a_stdin_reader_proceeds — a
#          real `cat`-then-`echo` that can only complete if it received EOF; the
#          private spawn path cannot be exercised from tests/). All additive; no
#          public symbols removed.
#   23228  Story 13.5d — production Spirit collective route (+26, AUTHORIZED
#          kernel-core delta). FLAG-Winston: authorized on the epic-13 lineage.
#          ROW ADDED RETROACTIVELY 2026-07-25 by the Story 13.5h grant review:
#          the 23202 → 23228 re-pin landed with the code but its HISTORY row was
#          never written, leaving the pin one entry ahead of its own audit trail.
#          The number below was already correct; only this record was missing.
#   23234  Story 13.5h — Shared-tier principal partition (+6, AUTHORIZED
#          kernel-core delta). FLAG-Winston: Lunarpulse authorized 2026-07-25 on
#          a UNANIMOUS 4/4 panel (Winston · Murat · John · Amelia,
#          `variant=B grant=approve extraction=waive headroom=accept`).
#          Closed a PROVEN GDPR Art.15/17 hole: `MemoryTier::Shared` accepted
#          `MemoryNamespace::Principal` at all three trait arms (memory/mod.rs
#          :743/:803/:857) while `SharedMemoryStore` has NO delete at any
#          visibility and `subject_access_query` reads only `principal_index` —
#          so subject-scoped PII written there was neither erasable nor visible.
#          The control that claimed otherwise was measured to be a NULL CONTROL:
#          it planted its canary under `Coordination`, so it passed identically
#          with and without principal PII in the tier.
#          SHAPE (Variant B, measured — Variant A's separate mirror cost +26
#          physical / +20 tokei and BREACHED the then-17900 kloc ceiling, and
#          was rejected 4/4 on ARCHITECTURE not cost): generalized
#          `reject_principal_collective` into
#          `reject_principal_outside_private(tier, namespace)`, stated as a
#          negation of the allowlist so a FUTURE MemoryTier is
#          principal-rejecting by default; hoisted ONE call above each of the
#          three `match tier` blocks; deleted the three in-arm collective calls;
#          updated the three cap-gated call sites so they cannot become an
#          alternate bypass. Additive; no public symbols removed; no enum
#          widened.
#          LANDED AND RE-MEASURED 2026-07-25 on the FORMATTED tree (13.5d :475 —
#          the estimate is not the authorization): +6 physical, 23228 → 23234,
#          exactly as authorized; +4 tokei, 17890 → 17894.
#          BINDING CONDITION DISCHARGED BEFORE THIS NUMBER MOVED: the rewritten
#          discriminating control was falsified PER LEG, not once. Deleting the
#          hoisted predicate above the write / read / scan `match tier` block
#          each made `multi_backend_erasure_partition_invariant` RED at its OWN
#          assertion (write / read / scan respectively), proving all three legs
#          independently load-bearing; restoring all three returned the suite to
#          `running 1 test` / `1 passed`. The PRE-13.5h control would NOT have
#          redded on guard deletion — it never wrote a principal namespace at
#          Shared. `xtask/fkcs-baseline.toml` src_lines = 23081 is the FROZEN tag
#          and is NOT touched. Authorization detail:
#          the Shared-tier principal-partition implementation artifact
#   23280  Story 13.5i — private-tier filesystem residue (+46, AUTHORIZED
#          kernel-core delta). FLAG-Winston: Lunarpulse authorized 2026-07-26
#          on the operator-ratified Shape A4 measured at preflight.
#          Closed the production Art.17 defect in memory/private.rs:
#          one-shot uninstall constructs an empty cache, so map-only
#          `forget_principal` never enumerated any filesystem value and signed
#          Removed { count: 0 } while persistent principal bytes survived.
#          The prior `"private"` backend discharge was measured as NULL
#          CONTROL #24: it wrote sub-threshold Text that never reached disk, so
#          its filesystem scan passed whether private erasure worked or not.
#          SHAPE (A4, measured): authoritative namespace decode walk,
#          fail-closed read_dir handling, pid-level symlink containment, and a
#          distinct (pid/namespace, key) set for exact-once counting. Shape B
#          was rejected because serde field reordering would silently stop
#          prefix matching; the +12 A1/B1 variants were rejected as fail-open
#          and fs_root-escaping; out-of-kernel Shape C violated the port
#          contract and sealed a false zero; index-driven Shape D treated the
#          index claim as more authoritative than filesystem reality.
#          LANDED AND RE-MEASURED 2026-07-26 on the FORMATTED tree: +46
#          physical, 23234 → 23280; +30 tokei, 17894 → 17924. The estimate is
#          not the authorization. The 18248 ceiling is unchanged (324 spare);
#          no hot_swap extraction is owed for this compliance repair.
#          BINDING CONDITION DISCHARGED BEFORE THIS NUMBER MOVED: serialized
#          per-limb falsification produced M1 restart residue RED (3/3) while
#          13 pre-existing private tests stayed GREEN; M2 spill RED/Markdown
#          GREEN; M3 Markdown RED/spill GREEN; M4 proof-count RED/file-absence
#          GREEN; M5 symlink-containment RED/ordinary-erasure GREEN; M6
#          unreadable-directory RED/happy-path GREEN; M7 bystander-retention
#          RED/target-erasure GREEN; M8 exact-once RED/absence GREEN. Restoring
#          every limb returned the seven restart controls GREEN.
#          `xtask/fkcs-baseline.toml` src_lines = 23081 is the FROZEN tag and
#          is NOT touched. All additive; no public symbols removed.
#   23318  Story 13.5i code review — private-tier erasure hardening (+38
#          physical / +17 tokei on top of the A4 landing; 23280 → 23318,
#          17924 → 17941). FLAG-Winston: Lunarpulse authorized 2026-07-27 on
#          the review's three decision findings, as a bounded correctness
#          repair to an already-authorized surface.
#          Closed three residue/attestation holes the A4 landing left open in
#          memory/private.rs: (1) a symlink at the NAMESPACE level was
#          followed by read_dir, its external files counted into `erased`,
#          and then only the link unlinked by remove_dir_all — a signed
#          Removed { count: n } over bytes still on disk, this story's own
#          defect one level down (pid-level containment did not cover it;
#          measured on rustc 1.96.0: read_dir(symlink)=Ok, remove_dir_all
#          (symlink)=Ok, target survives); (2) `file_stem()` as the key
#          identity double-counted the empty key, whose spill is named
#          `.bin` and stems to ".bin" while the cache side holds ""; (3)
#          every namespace child was attested as a logical key, so a
#          hand-created sub-directory counted as one entry while its
#          recursively-deleted contents counted as none, and an editor
#          backup inflated the receipt. The private tier's Markdown area is
#          deliberately operator-editable, so all three are reachable.
#          SHAPE: fail closed on a namespace entry that decodes to the
#          target principal but is not a real directory (Trap 4 preserved —
#          the two Err-expecting tests stay green); count only regular files
#          whose name carries a `file_ext_for_kind` extension, recovering
#          the key by stripping that extension rather than `file_stem()`;
#          propagate a late `read_dir` iterator error in the pid reap
#          instead of reading Some(Err(_)) as "not empty". The decoder
#          round-trips through `file_ext_for_kind` rather than a literal
#          extension array — 11 of the 17 tokei lines — because a duplicated
#          literal is Shape B's rejected drift failure mode on the file-name
#          axis. The measured cost is 3.8x the estimate quoted when the
#          repair was authorized; the estimate is not the authorization.
#          The 18248 ceiling is unchanged (307 spare) and no hot_swap
#          extraction is owed for this compliance repair.
#          BINDING CONDITION DISCHARGED BEFORE THIS NUMBER MOVED: serialized
#          per-limb falsification, every mutant run showing `Compiling
#          maos-kernel-core`, source restored after each — M8 AS SPECIFIED
#          (count per file, which the A4 landing had no CI-executed detector
#          for) RED on forget_counts_inline_only_entries_that_never_spill,
#          9 others GREEN; M9 (drop the namespace type check) RED on
#          forget_does_not_follow_namespace_directory_symlink, 9 GREEN;
#          M10 (identity back to a non-stripping shape) RED on the logical-key,
#          exact-once and inline-only limbs, 7 GREEN; M11 (drop the
#          regular-file guard) RED on forget_counts_logical_keys_not_
#          filesystem_nodes, 9 GREEN. Restored: 10/10 GREEN, and
#          maos-bin erasure_uninstall_13_5b 13/13 GREEN including the
#          Trap-4 leg private_forget_reports_filesystem_removal_failure.
#          `xtask/fkcs-baseline.toml` src_lines = 23081 is the FROZEN tag and
#          is NOT touched. All additive; no public symbols removed.
#   23401  Story 13.5j — the private tier's READ surface is made consistent
#          with its durable state and contained to its own directory
#          (+83 physical / +34 tokei; 23318 → 23401). AUTHORIZED 2026-07-27,
#          FLAG-Winston, at the MEASURED number after six shapes were probed
#          and proven defective at HEAD. Story 13.5i made `forget_principal`
#          authoritative about the filesystem; `write`, `read` and `scan` were
#          left behind and disagreed with it in six ways, all confirmed by
#          running code before a line was written: (D1) `write` never unlinked
#          a superseded spill, so `k.json` and `k.bin` coexisted and a cold
#          read returned the value the Spirit had already overwritten
#          (measured warm=Blob / cold=Json); (D2) `scan` unioned the in-memory
#          cache with the filesystem without a shared logical-key identity and
#          returned `["digest:aaa", "digest:aaa"]` for one key, which reaches
#          `working_memory_digest_refs` in a SIGNED `decision.*` frame and
#          halves the effective scan cap; (D3) `scan` recovered keys with
#          `file_stem()`, so the empty key was invisible to it while
#          `forget_principal` counted it — the two disagreed about what a
#          logical key IS; (D4) one hand-made directory in the deliberately
#          operator-editable Markdown area failed the WHOLE namespace scan
#          with `IsADirectory`, which the production digest provider swallows
#          into empty refs; (D5) `scan`'s `is_dir()` and (D6) `read`'s
#          `exists()` both traverse symlinks, so a link planted in a Spirit's
#          own area read entries from outside it (I5) — the containment
#          13.5i closed on the erase path, still open on the read path.
#          SEVERITY CORRECTED, NOT INFLATED: the 13.5i residual filed this as
#          a "live data-integrity and data-exposure bug". It is LATENT at
#          HEAD — no shipped writer produces the trigger (the sole production
#          private writer, `halt/resolver.rs:170`, writes unique
#          `halt_context::` keys, and nothing writes the `digest:` keys the
#          sole production scanner reads). The surface is nonetheless fully
#          wired: `KernelCtx::memory()` hands the adapter to Spirit-hosting
#          code, so both halves are one Spirit write away.
#          The 18248 ceiling is unchanged (measured 17975, 273 spare); no
#          hot_swap extraction is owed for this correctness repair.
#          BINDING CONDITION DISCHARGED BEFORE THIS NUMBER MOVED: serialized
#          per-limb falsification, source restored byte-identical after each —
#          M1 (write does not unlink) RED on both `write_unlinks_*`, 8 GREEN;
#          M2 (scan drops the cache-identity skip) RED on the one-entry, the
#          read-cardinality and the signed-frame limbs, 7 GREEN; M3 (`scan`
#          back to `file_stem()`) RED on the empty-key limb, 9 GREEN; M4 (drop
#          the regular-file guard) RED on the junk-directory limb, 9 GREEN;
#          M5 (`scan` back to `is_dir()`) RED on the namespace-symlink limb,
#          9 GREEN; M6 (`read` back to `exists()`) RED on the spill-symlink
#          limb, 9 GREEN. Restored: 10/10 GREEN.
#          NO CONTROL IS CLAIMED for the in-memory loop's `break` → `continue`
#          change: `limit` caps the filesystem pass first, so a complete
#          versus truncated `cached` set is not observable today. It is kept
#          because the comment above it would otherwise be false.
#          `xtask/fkcs-baseline.toml` src_lines = 23081 is the FROZEN tag and
#          is NOT touched. All additive; no public symbols removed.
# HISTORY: Post-review hardening (2026-08-02): 23596 -> 23517 physical lines
#          (+116 physical lines from the 23401 Story 13.5j pin). The approved
#          spec-13-6c-ci-blockers-2 design now uses Unix directory-relative
#          O_NOFOLLOW traversal, parent/file/directory fsync, serialized
#          hard-link-backed rollback transactions, exclusive temporary files,
#          fail-closed equal-mtime dedupe and hostile PID handling, and
#          prefix/limit-isolated scans. Behavioral contracts moved from private
#          source-unit tests to public-adapter integration tests: 15 Story
#          13.5j, 10 Story 13.5i, and the full 558-test kernel suite pass. The
#          logical count is 18171, 77 below the unchanged 18248 ceiling; no
#          public symbols removed.
# HISTORY: Story 13.6c code review round 2 (2026-08-03): 23517 -> 23679
#          physical lines (+162). (1) Private-store security hardening: BH-1
#          hard-link containment (fstat nlink==1 on the spill read path),
#          EH-1 crash durability (fsync the root's parent on first creation),
#          BH-4 containment (require an absolute spill root). (2) TI-1/2/3
#          test-depth fault injection: a cfg(test/debug_assertions) module
#          (spill_test_faults.rs) + thin consultation points prove the
#          temp-fsync rollback, io_lock serialization, and no-follow open.
#          The fault module is kloc-excluded (check_kloc.rs `-e`) per the
#          gate's 'production code only' intent — a measurement-scope
#          refinement ratified by party-mode (Winston 2026-08-03), NOT a
#          ceiling increase. Logical count 18210, 38 below the unchanged
#          18248 ceiling; the pub mod is cfg-gated and absent from release.
# HISTORY: Epic 5 review-finding closure (2026-08-13): 23679 -> 24472
#          physical lines (+793). The closure baseline commit already carried
#          52 unpinned lines; the user-directed "apply every patch" closure
#          added the remaining net +741 across scheduler, hot-swap, lifecycle,
#          revocation, supervision, and T3 sandbox production surfaces.
#          FLAG-Winston: authorized as the bounded repair of the reopened
#          Stories 5.1/5.2/5.4/5.5a findings. The delta removes placeholders
#          and false proofs, preserves ABI discriminants and the single-SCB
#          ownership invariant, adds typed fail-closed errors and executable
#          negative controls, and removes no public symbols.
# HISTORY: Story 15-1 AC2 (2026-09-08): 24472 -> 24474 physical lines (+2).
#          The I9 repair: the `af788c3e` misattachment moved (net 0, doc +
#          attribute returned from T3ImageVerificationConfig to their owner
#          SecurityManagerAdapter) plus two single-line `#[i9_exempt]`
#          attributes on ScbRuntimeSnapshot (scheduler/control_block.rs) and
#          VerifiedImageLock (security/sandbox/t3/image_lock.rs), each with
#          reason <= 63 chars so check-fmt keeps the line at one. D-F standing
#          permission ("15-1 +2"); the paired kloc row moves 18933 -> 18935 in
#          the same commit or the two instruments disagree again.
# HISTORY: Story 16-3 code-review closure (2026-09-16): 24474 -> 24477
#          physical lines (+3), operator-authorized with all review patches.
#          DRIVER: TaskAssignmentRecord.capability_token now represents absence
#          as Option<TokenId>; CrashDetector preserves None as SQL NULL and an
#          empty in_flight_tokens array instead of fabricating TokenId([0; 16]).
#          Changed files: scheduler/control_block.rs and
#          supervision/crash_detector.rs. No new public API or transition.
# HISTORY: Story 16-5 (2026-09-17): 24477 -> 24591 physical lines (+114).
#          FLAG-Winston operator-authorized the audit-truth repair and re-pin.
#          DRIVER: fail-closed audit delivery, per-principal forget serialization,
#          append-safe lifecycle journaling, durable halt-resolution ordering,
#          honest task-orphan rows, and repaired T3 audit paths.
#          Changed files: capability/mod.rs, halt/{mod,resolver}.rs, inference/mod.rs,
#          journal/mod.rs, memory/mod.rs, security/{mod,sandbox/t3/{cap_audit_bridge,
#          quarantine}}.rs, and supervision/crash_detector.rs. No kernel file was added or removed.
# HISTORY: Story 16-5 review round (2026-09-17): 24591 -> 24628 physical lines
#          (+37), operator-approved patch batch (code review, 30 items).
#          DRIVER: resolve() serialization mutex + contract docs (AC5(a)
#          duplicate-side-effect window), release_legal_hold serializer guard,
#          hold-over-erased-principal honest status, T0 DropWithAudit disposal
#          record. Changed files: capability/mod.rs, halt/resolver.rs,
#          lifecycle/cli_wrapper/runtime.rs, memory/mod.rs, revocation/applier.rs,
#          scheduler/scheduler_loop.rs, supervision/crash_detector.rs. No kernel
#          file was added or removed.
src_lines = 24628

# ── Story 16-0 — PINNED FILE SET + PER-FILE CONTENT HASH ─────────────────────
#
# HISTORY: Story 16-0 (Epic 16, drift D2 / Epic-15 retro action A5), 2026-09-13,
#          measured at 323ab597: 98 files (97 `.rs` + `security/sandbox/t3-image.lock`),
#          set_hash 5575b00ad923a2dc95ca16fe82774b9ae04c7b2aec0e494f458c7d69e49bb275.
#          DRIVER: a line count alone is too weak. Story 15-4's seccomp commit
#          (843d5365, 8+/8-) edited `security/sandbox/linux.rs` at a CONSTANT
#          24474 and `check-kernel-baseline` said PASSED on both sides. The
#          per-file map reds that edit and NAMES the file.
#
# ZERO kernel-Δ: this block PINS the tree, it does not change it. `src_lines`
# above is unmoved at 24474 and keeps its `.rs`-only denominator; the hashed set
# is every file under the root, any extension (D-16-0-F) — `t3-image.lock` is the
# signed T3 trust anchor and must not sit unpinned inside the pinned tree.
#
# The set is read from the FILESYSTEM, not `git ls-files`: the compiler builds
# what is on disk, so a `.gitignore`d `mod backdoor;` would compile into the
# kernel and never appear in the index (D-16-0-F / round-table F-A).
#
# RE-PIN with `cargo run -p xtask -- check-kernel-baseline --emit-pin` and paste
# the output here, recording the measured value, the driver and the story key in
# the SAME commit. There is no in-place rewriter and NO hold, waiver or
# known-mismatch constant — `xtask/fkcs-baseline.toml`'s admission hash has been
# held RED by exactly such a constant since Story 13.4 (D-16-0-G).
#
# `set_hash` is DERIVED from the sorted map as a headline and is never the
# authoritative value; the per-file digests are. Keys carry no `src_lines`
# prefix and land strictly AFTER the assignment above, because three hand-rolled
# line scanners take the FIRST `src_lines`-prefixed line and one of them
# `.expect()`s (D-16-0-C).

[kernel_src]
root = "crates/maos-kernel-core/src"
set_hash = "66ee5370de033e2a09b1570fd6525a6d75bdb8af2fca142664a1f826cb08a6ca"

[kernel_src.files]
"api.rs" = "253cb0c7f80ea784da2587aaec3956a77ae6d888e094ff205bd610bd4f3e057e"
"bin/assert-revocation-p99-floor.rs" = "dab2d5ac79c9c5f9b67eedc33350c640d4909715d5eb78aaa0187c2bf7e48432"
"capability/cap_audit/mod.rs" = "9f253336da1d618808984fef11941929a6af927ef66120d84b43e122ff70decc"
"capability/cap_audit/writer_task.rs" = "7c90023579458fdb4a3357990a46cb0520d018196b1ffa0f2ed65fa5e7e84ef2"
"capability/cap_policy/decision.rs" = "be8b2e5a0b08821f55f019ab85b0a91b953dd60f3a8d3f994bd59e087b409e41"
"capability/cap_policy/mod.rs" = "3ff76e22c2493fca4ef8b61ec37c9ce51aab36ae9fb29fe37f3ae9a590fd51fc"
"capability/mod.rs" = "ea09b10768bc9cc99821ed0ba6bcb109fa0618cab08526930167c4e24edc067b"
"capability/working_memory/mod.rs" = "151f1ea71b3b05e575b4771736a65aaaba0503e3a33a20d949d8e5382829892a"
"capability/working_memory/orchestrator.rs" = "c6e102b0965fac7e813fe303be4ea989001fd646dd97cf22a9ab08c1dc0fdc7f"
"capability/working_memory/policy_runtime.rs" = "199858803e69787c8f981c18f4fa22201c8ad0f47d18638623606462e2e374b5"
"compliance/mod.rs" = "46782179f13d4796d484317ab9eabad57d0b2e1d3c52a0eeb6a69834d788732a"
"halt/mod.rs" = "ea1fb831c6d278c900821286fc127665039681b2e1166417a43d7bafd45c92bc"
"halt/output_markers.rs" = "a320e7614f6b129e21853a5f98be10b8deeeb9f826d48cac829caf8cf236d893"
"halt/resolver.rs" = "3756181d4a06e121fdd3b63818b45d20205e6acb9b43cc18fb5ad82c93ac6b1f"
"halt/termination.rs" = "13f32e1f6dcca190621a5a47bff8fca181b63d79d10b20e9c6264080722f7a70"
"hot_swap/archive.rs" = "a6e4ea60193e182b2dad8edeb793d3de366ef95b0be30e8c85756af78aeb7ddb"
"hot_swap/coordinator.rs" = "a481b1d671cc25a091714f05962bbedf246da2ff9480b46b36949a8fd02ca959"
"hot_swap/migrator.rs" = "1c72d4b472310fcc36f80521d25ab3da72691fc99b79c1b258bdd75ab5a53b88"
"hot_swap/mod.rs" = "d378c979b9338914bdb50d507ddc7826c0f5852a4364e4736fde3a7d9e35e5a6"
"hot_swap/post_swap_monitor.rs" = "33bf93474c5b9d83012b5b4c9f032e13ff57dfa28c4b4cf03bbb80aa02e42b10"
"hot_swap/precheck.rs" = "4a447575f94942c43e2784b5b4a6f38610a45ebf511333bc9df7a575446f740f"
"hot_swap/saga.rs" = "886994c9ab553372c85df2caf8de395c0b712b888656c030674babf12f8efc77"
"hot_swap/state_codec.rs" = "8d1c62a7486582eb329b208bd4dca5280ac62d1462c89cdfb1d8691aaa3861dd"
"iac.rs" = "18c0b4e562376fd5b6170fac0544bb3465c4fc452b46c702a8bc1c070d7a08bf"
"inference/mod.rs" = "35e7a2d5f0f65827c4a25a9837cd41b77e689c62dbb4a0ae9567ba022d04fe7d"
"inference/router.rs" = "4cd2744273861514bf0989eb43f723380a5caf0801454078bd10a1953a94e8b1"
"io/mod.rs" = "1c4c5202c33d39de7dd61d36607015dd07dac01922bd82ac6fd40300a8e72bf6"
"isolation/mod.rs" = "a7f618dd75f7dd4115589dc7e9cd742e3f0f4c07d30d6f95b1cc494b5947813c"
"isolation/runner.rs" = "3b4890261b2116d91efe15a9e0369ad8e1544b056a5286826ab7ee87ead593ff"
"journal/mod.rs" = "c0e8306b72ed4349ebfde22d4501f5467e354a840fd473e8aee1c03037a47e77"
"lib.rs" = "a4c3708400196b1c97ac614ead31645d77cc5124bfbd30f4b13139bf4c7f9100"
"lifecycle/cli_wrapper/admission.rs" = "921310672eabc0d40cadfe30ce6de1234ebde779896c9e3f0ea0ca022724e911"
"lifecycle/cli_wrapper/lifecycle.rs" = "4a3d2e2f631c47195321fc48410efcbc218d69a979195fbc8f741a09ace09599"
"lifecycle/cli_wrapper/mod.rs" = "c78a43ba26d796315af0ee07f2ce76991c7b0f1785298d49c08ffbdf17c0addc"
"lifecycle/cli_wrapper/runtime.rs" = "36755713f73b6877a3b65f396e53198f28980698eaa65a5ac3822e20e11a0b97"
"lifecycle/mod.rs" = "b4d995c3b6887d905790611f4145f1ea8eb349e0dc73a923d13e56abcd09d335"
"lifecycle/upgrade.rs" = "443465480afedca6948e3fcfc0f39504a1409849074411e56e0a50f0f1f42cf7"
"mcp/mod.rs" = "65689d1501791c3fdd2f413666d7fc0ab0562136bd78e3903eb2972a87b5c233"
"memory/for_spirit.rs" = "6d58364f78f6026a8fe203f161964eb724bd2fa82f43146c3c3d8b605e1ce95a"
"memory/mod.rs" = "2b36b724ab77164c2c425e09c34d85070ecc39f6cf87e582ce62fef4439d73c9"
"memory/principal.rs" = "c99431b9fd3fbe40b758e22dd54091fae4def4b8700717c9767d7ebc67bd9aac"
"memory/private.rs" = "9b149278ed9128b4edb38601e5f0d0b88948283e10d4fd693aeee50024f40e5c"
"memory/read_entry_point.rs" = "0528ce87453ef4a10b4bbe4a360f557769be412fc091df4734495482d551dda1"
"memory/self_telemetry.rs" = "c562094354fe64784e49de7e37754badce6637802f12c17ce3e6d2873e2adcef"
"memory/shared.rs" = "1df5abe33a685cfb940bc191fe96bc8e0ac6f5c38893ac8792d944504df99d84"
"memory/spill_test_faults.rs" = "4c385fa2ea48d9439f81950600342d8ef3d46b0f3da5f1d6aa6fc399b14b4fcd"
"memory/write_entry_point.rs" = "e43e8c2957d0042a13b75e4972c7417b72388316b1c56091f6b417f06509f224"
"orchestrator/buffer.rs" = "8f3f73b3ce363ca6678774dc83acdb83c9910a04a13487df8d0220ea66ce8f4d"
"orchestrator/echo_gateway.rs" = "7c5195d3df9e20909d52bf5e04d829377433313667842964de51c1625083ae92"
"orchestrator/gateway_dispatcher.rs" = "d01ea8018515c42bcb205e23577783cd2f010401339f261eefea88f1071a037a"
"orchestrator/mod.rs" = "89c7ac62c660f6cb6cee4379571842fdded2264dc6df38ebecda8bf5df60287e"
"orchestrator/registry.rs" = "d7788571a2a4bc17f3d5835a5764e266e5042036b8447eb4db4296ec11d6b7a4"
"revocation/applier.rs" = "656ed2329758353aeb2de17c0d64d07afce80075f0bccd0fd387dce641ed537a"
"revocation/mod.rs" = "272ba459ed2095beff6d7497c62ab0fa47bbe3d2ffb0b47ba2045c406a49998e"
"revocation/parser.rs" = "f907b4fe7cac52a1e2d1e8496a1b83fee820d4b3779529419215dea6e2dcdca7"
"revocation/poller.rs" = "ad449a7a206cb42d160afb44c05f17bfbfe39cf4815d6be7996034dfbb51a960"
"revocation/rules.rs" = "726b27e55d5fff3e1cbd9ca2c1cf38494dfd272fbe586c1a0ed869c760366f44"
"revocation/version_match.rs" = "b225bac84b6e2529cab18b4dadbc271f0c0ff487eaca4f11e23a49ea0ccf5046"
"scheduler/control_block.rs" = "d0b216677842b73b788df8e969cc5ec42e4cdd8c94b11f5917c9e1a5b01bcf6a"
"scheduler/hook_dispatch.rs" = "6f26d6b147425f8001e1c5d377cf88676329f7cd6402e1e5940e51580ac4d985"
"scheduler/idle_watchdog.rs" = "07901b89eab3911e6cc23df9d383ba6fee5547c43bbc028fe6eedcacadeffdc7"
"scheduler/kernel_ctx.rs" = "7baf6a7e15a5ffb62312a02aa19d58b24c8500f277bcc724b2065f4c732963ef"
"scheduler/mod.rs" = "fa89437053eedadef4662a1f538ad275caa62df69a9ddbbd4dc0a9976a377cf5"
"scheduler/resource_ceiling.rs" = "becc322aa3700d8f08c10ac443aff2ea80995926a2cc647ddbad49aecfa6b06d"
"scheduler/schedule_watchdog.rs" = "b50009c32df76c8f99095584a68c1de65d6a4831f84a97061672303dccca70fc"
"scheduler/scheduler_loop.rs" = "6e1ba841af1d4420d74150360939ca39deed3d5b53ae86b0caf16023e3762478"
"scheduler/verb_resolver.rs" = "114fe959e9f7b4126464b63ad51f57a374646c95dbb72c0f3d8c19dbc363ab16"
"security/approval.rs" = "d9847e9ffbd4ef4b5f21410a7a1c24e0dbb3f57bb98eeee20ae6b2f27e670ea3"
"security/crypto.rs" = "201a1034b043a8001ecac32c424645d2db2a58cd0bfbba5a5a1092d960397a4e"
"security/drift.rs" = "875b2c652bc10ca1aa2f4dc1f4b6bd410e5c91ef37750b98fceb6058fb926328"
"security/manifest.rs" = "d2e02fe45059446d55437e9d9e03893c96945741ec7d9ca4b13882d2bc5a4e85"
"security/mod.rs" = "c5e014d38ff053bee721218e94843628882da6bb8242c8008185aaf21fd1f4f1"
"security/operator_config.rs" = "9f1087f82c72b84cf880952310710306cc4f8feb5e44406891a916e9a763e236"
"security/posture.rs" = "59f09376bb92d00419b26435135f72b2bdc05c8c550c78b85a53ee2339c85fac"
"security/sandbox/linux.rs" = "8964be98eb51c34dd4a7ae75d2d60ab90d8e8d6897205f8787f0ae0563ff29f2"
"security/sandbox/macos.rs" = "e2bcfda0b12bd4d738e0d8f1c4795c26a95cb828cd8a73eec02edc5e9014c2af"
"security/sandbox/mod.rs" = "1732708673ce3294434b5db9919683008a42e24cada8e8af3795032792b711a1"
"security/sandbox/t3-image.lock" = "028858c187bcfe77bde8e996866da0fcba3388cc6d81a96c2a2588cb5d94ac64"
"security/sandbox/t3/argv.rs" = "18badaa3d8141e86fb87b852577d1720319a16b765e2dbdc177facad913c5ae0"
"security/sandbox/t3/cap_audit_bridge.rs" = "c4f77a015d1b21541f82ce35a6a49c66e8e3a0228191de1a68eac330d2093035"
"security/sandbox/t3/child.rs" = "b020bc87c673f83e0d49831f564dadbda94d901f68299803cd41c88670d733d6"
"security/sandbox/t3/image_lock.rs" = "1ae4cc7db0290d62de482d50bf7e4cc97c672e85a32b06ffdae37823a5461b09"
"security/sandbox/t3/image_verify.rs" = "2418b6e509a547aecb192ff6e83a12423935599bb9ab5c978b50827d320ee523"
"security/sandbox/t3/mod.rs" = "f8e44ec0e9bb15005c800ffff127913a13b3f706f7d0224f568142239b1e625e"
"security/sandbox/t3/quarantine.rs" = "e1df8535cd5371c83718084c6bde3b9a80647fe8dfebd18b932de3b6b6e118b6"
"security/sandbox/t3/runtime_detect.rs" = "91cba03e8a2a1f7ee03945100f3198bb36601d7008b2fc848f53f10466aeb974"
"security/sandbox/t3/spawn.rs" = "31658654ff7f6e824dceafc68be06bd3758b6efe1b1097517250d84f28653e95"
"security/sandbox/unsupported.rs" = "124e2d5baa3a64757cde10dcf7014bd7a77a77312c3ab76609140d060e515894"
"security/sandbox/windows.rs" = "5f8ff06185984f9f5eb18a457b6f4329d0baf2ced8662db20ea70f5b7f5f2935"
"supervision/cold_restart.rs" = "2977944d24dc54c28dba02c1d3395fc16225f45a3994b0a53f1a27aca20149b7"
"supervision/crash_detector.rs" = "b2b927bdaa515133f81d098ae3cb36e9da7a33ea7b3534ba5f505045a908daa2"
"supervision/disposition.rs" = "5d8220cafba3e6afb757e3cb9acf23788d458319e093a858b10ed41596640b93"
"supervision/mod.rs" = "71f3704a2073987a249b601111d03bd7815e1822c8d0cad377234dc0e1ecf518"
"supervision/progress_watchdog.rs" = "97dbead77b4a9d9546f24767f5f580e4bd8d29a8a1eef742dd5f203365be4097"
"supervision/silent_failure_detector.rs" = "fb9e4c0e881c08964c02f5372ddb664e294828b670d91f2ef078670f609ccec6"
"supervision/test_double.rs" = "3162ed1ee1c2835f8a08496aff056d41acf247949c9076cb4fd9cb8a3cec8d5c"
"supervision/watchdog_common.rs" = "a6f98ea2e799ea8d7b0fbcc9e02ac154625ab9b4e8e95d4508bd173dcde6e23c"
"telemetry/mod.rs" = "48c1b6401e8806d83ef8899d5341b1d7be64ac91261c42f16578ec720963c70c"
