# Per-crate KLOC budgets — production Rust code only.
# Excludes: target/, tests/, benches/, examples/, fuzz/, spirits/, *.md, doctests.
# Alarms and hard-fails are independent: a per-crate ceiling breach fails the build
# even if the aggregate alarm has not fired.
#
# Ceilings are reviewed at each epic retrospective.
#
# -----------------------------------------------------------------------------
# Epic 5 retro §A4 Debt 3 — in-progress decomposition of `maos-kernel-core`
# -----------------------------------------------------------------------------
# `maos-kernel-core` measures ~21,370 LOC at HEAD (post-Story-5.5d), 3.56× over
# the 6,000 ceiling below. Per §A4 decision (2026-05-24): DECOMPOSE the crate
# across Epic 6/7 stories. DO NOT raise the ceiling — amending the ceiling to
# fit current size would invert ADR-038's purpose (the ceiling is the
# architectural discipline, not a measurement of current state).
#
# Phased extraction plan:
#   Phase 1 — Story 6.5 (gateway sub-modules)
#     extract `maos-iac`            (~3,350 LOC: TL + log_recall + bus routing)
#     extract `maos-manifest`       (~2,500 LOC: TOML parsing + section schemas)
#   Phase 2 — Story 6.1 prep (full IAC bus + DRR + retract primitive)
#     CORRECTED 2026-05-25: Original boundary at `capability/` directory level
#     created circular dependencies via `working_memory/orchestrator.rs` (depends
#     on halt, iac, journal, security) and `cap_audit/writer_task.rs` (depends on
#     iac::TL). New boundary extracts ONLY the pure capability surface:
#       cap_tokens, cap_quota, cap_audit (types + channel only),
#       working_memory (types + store only).
#     Extracted LOC: ~1,271 (was ~2,400). Status: DONE.
#   Phase 3 — Story 7.2 (full registry publish/install/yank + air-gapped import)
#     residual security/* fold      (~1,000 LOC moves into maos-manifest or
#                                    maos-sandbox depending on classifier)
#   Phase 4 — Story 7.x (TBD; scheduling depends on remaining size)
#     extract `maos-scheduler`      (~1,961 LOC)
#     extract `maos-memory`         (~1,659 LOC)
#     extract `maos-hot-swap`       (~1,317 LOC)
#     extract `maos-supervision`    (~569 LOC)
#
# At HEAD post-Phase-4 the residual `maos-kernel-core` is ~5,400 LOC (halt +
# inference + telemetry + revocation + journal + lifecycle + orchestrator +
# small glue modules + composition-root forwarders). Under the 6,000 ceiling.
#
# **Interim posture (Epics 6/7 in flight before Phase 4 completes):**
# the kloc gate stays strict. Stories that ADD to `maos-kernel-core` MUST first
# extract a candidate module netting to ≤0 added LOC. The gate's continuing
# failure on every CI run keeps the breach visible until decomposition lands.
# -----------------------------------------------------------------------------
# Epic 10 retrospective ceiling review (2026-06-26)
# -----------------------------------------------------------------------------
# The Epic-10 branch (12 commits: 10.1a..10.5 + retro) is the first time the
# WHOLE epic ran the blocking `kloc-check` aggregate together. Eight crates +
# the aggregate breached the prior ceilings. Per the established epic-retro
# process (cf. the maos-a2a-core 3050→3144→3200 surfaced bumps), ceilings are
# reviewed and set to the TIGHT measured residual — NOT round headroom.
#
#   crate            prev    measured  new ceiling   driver
#   maos-kernel-core 17000   17687     17750         Story 10.5 AC3 (Windows
#                                                     sandbox: WindowsChild
#                                                     extraction) — the ONLY
#                                                     authorized kernel delta
#                                                     this epic, FLAG-Winston
#                                                     22574→22929. Phase-4
#                                                     decomposition (scheduler/
#                                                     memory/hot-swap/supervision)
#                                                     REMAINS the standing debt
#                                                     — this bump fits the
#                                                     authorized delta, it does
#                                                     NOT retire the plan.
#   xtask            15000   22389     22500         +7,873 LOC of v1.0/v1.5 gate
#                                                     infrastructure (red-team,
#                                                     RTO, skill-conformance,
#                                                     third-party-trial gates +
#                                                     proven-red suites). Tooling,
#                                                     not shipped kernel.
#   maos-cli         2000    3689      3750          skill-queue + audit
#                                                     subcommands (Epic 9/10).
#   maos-domain      7000    8045      8100          collective-tier ports +
#                                                     i9 invariants growth.
#   maos-iac         5500    5736      5800          transparency-log + bus.
#   maos-manifest    4000    4012      4050          schema-version surface.
#   maos-bench       1500    1512      1550          J4/J6 harness rebuild.
#   _aggregate       90000   102171    103000        sum of the above.
#
# FLAG-Winston: ratify the kernel-core 17000→17750 bump in the Epic-10 retro
# (authorized AC3 delta) — the others are tooling/CLI/domain growth.
# -----------------------------------------------------------------------------

maos-kernel-core = 17750
maos-capability = 2000
maos-cap-registry = 3000
maos-corpus-gen = 3000
maos-wire = 2000
maos-journal = 2000
maos-spirit-abi = 3000
maos-spirit-sdk = 3000
xtask = 22500          # Epic-10 retro 2026-06-26: +7,873 LOC v1.0/v1.5 gate infra (tooling, not kernel)
# Story 6.5 — Phase 1 extraction
maos-iac = 5800        # Epic-10 retro 2026-06-26: 5500→5800 (TL + bus growth; measured 5,736)
maos-manifest = 4050   # Epic-10 retro 2026-06-26: 4000→4050 (schema-version surface; measured 4,012)

# Story 1a.1 — 13 new crate stubs
maos-domain = 8100     # Epic-10 retro 2026-06-26: 7000→8100 (collective-tier ports + i9; measured 8,045)
maos-spirit-hello = 1000
maos-providers = 2000
maos-mcp = 1100
maos-acp = 1000
maos-a2a = 1500
# Story 8.6 — extraction of maos-a2a-core (transport-agnostic ADR-012 substrate)
# from maos-a2a; resolves the maos-a2a 1500 overage by moving the bulk out.
# Ceiling recorded post-extraction (actual count in the Dev Agent Record).
#
# Story 8.8 — 3000 → 3050 (team consensus 2026-06-07: Winston + Murat + security
# red-team, under the most-spec-faithful + long-term-correct criterion). AC6's
# premise ("crate was 2644") was STALE: HEAD was 2942 (8.9 grew it), leaving 58
# lines headroom. The fail-closed G7 closure (shared `consent_decision` seam + two
# unconditional enforcement gates + response interpretation + error mapping)
# necessarily exceeds that. Per consensus, the band-fallback toggle was DELETED
# first (Option 2), then re-measured: actual = 3034. The ceiling is set to the
# tight measured residual (3050, ~16-line margin) — NOT round headroom for code
# we might add later. A submodule split was considered but does not reduce crate
# kloc (tokei sums all src under the crate). Ratified bump to 3050 for the A2A
# cross-Host security core.
#
# Story 8.13.1 — 3050 → 3144 (SURFACED per AC6, NOT a silent bump). Option A
# (receiver-side ConsentRupture emission) was ratified UNANIMOUSLY by party-mode
# (Winston + Murat + Amelia + John, 2026-06-08) as the only home where genuine
# `maos run` production code emits the cross-Host rupture (sender-side has no
# production caller at HEAD). The emission seam (additive `rupture_sink` field +
# `install_rupture_sink` + `emit_consent_rupture` building the typed
# `FramePayload::ConsentRupture`) plus its red-first unit + negative-control tests
# necessarily exceed the prior tight residual: measured 3041 → 3144. AC6 clause 2
# explicitly anticipated this ("if Option A's emission would push maos-a2a-core
# over a per-crate ceiling, that is a scope signal — surface it, do not bump
# silently"). Per the 8.8 precedent: set to the tight measured residual (3144),
# NOT round headroom. FLAG-Winston: ratify this surfaced bump in 8.13.1 review.
maos-a2a-core = 3200
# Story 8.6 — maos-a2a-tcp: live cross-Host TCP/mTLS transport (the second
# A2ATransport impl). Socket + rustls + codec wiring + verifier.
maos-a2a-tcp = 1500
# Story 8.13 — generic HTTP mobile-push adapter, intentionally small and
# dependency-light (maos-director-surface + maos-domain + ureq only).
maos-notify-push = 250
maos-persistence = 2000
maos-secrets = 1000
maos-compliance = 2000
maos-control = 1500
maos-cli = 3750        # Epic-10 retro 2026-06-26: 2000→3750 (skill-queue + audit subcommands; measured 3,689)
maos-bin = 9000
maos-bench = 1550      # Epic-10 retro 2026-06-26: 1500→1550 (J4/J6 harness rebuild; measured 1,512)

_aggregate_alarm = 16000
_aggregate_hardfail = 103000  # Epic-10 retro 2026-06-26: 90000→103000 (measured aggregate 102,171)

# --- Isolation contract (Story 9.5, D2 — Hard Guardrail #2) ---
# docs-site/** is a non-Cargo Docusaurus project (Node/TypeScript). It is NOT a
# workspace member and must contribute ZERO Rust source. This is ENFORCED — not
# relied on by incidence — by `assert_docs_site_zero_rust()` in xtask's kloc gate
# (xtask/src/kloc_check.rs), which walks docs-site/ and hard-fails on any `.rs`
# file. tokei's `--types Rust` flag provides the structural exclusion; the xtask
# assertion is the explicit gate the spec mandates.
_docs_site_isolation = "ENFORCED by xtask kloc assert_docs_site_zero_rust() — docs-site must contribute 0 Rust LOC"

[in_progress_decomposition]
phase_1 = { target = "maos-iac + maos-manifest", status = "done", epic = "6.5", notes = "maos-iac: 13 files, 4834 code lines; maos-manifest: 1 file, ~3800 code lines (extracted from security/manifest.rs)" }
phase_2 = { target = "maos-capability", status = "done", epic = "6.1", notes = "CORRECTED 2026-05-25: extracted ~1,271 LOC of pure surface; cross-cutting orchestration remains in kernel-core" }
phase_3 = { target = "security/* residual", status = "pending", epic = "7.2", notes = "type-only refactor; no new port trait — re-homes ~1,000 LOC into maos-manifest + maos-capability per existing classifier" }
phase_4 = { target = "maos-scheduler + maos-memory + maos-hot-swap + maos-supervision", status = "pending", epic = "7.x", notes = "ADR-041 (Epic 6 retro §A3 2026-05-28) — trait-boundary refactor via SpiritSchedulerPort + MemoryManagerPort + HotSwapPort (NEW in this ADR PR) + SupervisionPort (consumes existing SpiritSchedulerPort). Story 7.5a STABILITY.md draft DOES NOT publish until this phase closes." }
