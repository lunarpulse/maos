---
party: installed
created: 2026-07-03
repaired: 2026-08-11 frontmatter restore
updated: 2026-08-18T15:28
---

# Party Memlog — Installed BMAD roster (Winston · Murat · John · Amelia · Mary · Paige · Sally)

## 2026-07-03 — Story 11.3 preflight (Scale envelope — 25/30-host churn)

**Outcome: UNANIMOUS RESOLVED.** Winston · Murat · John · Amelia (+Mary walk-on on traceability). Session mode.

Biggest catch — **Murat found a spec defect the author missed**: AC2's "detection = first router NACK" is wrong for 2 of 3 adversary classes. Pin-spoof + cert-rotation-race die at the **mTLS handshake** (`verifier.rs`: WebPKI-then-pin), never reaching `handle_intake` — only a failed dial, no NACK frame. Ratified as **F-new: detection is TWO-SURFACE** (handshake `TcpTransportError` OR router NACK), read from the same `directed_dial_sweep` the blast-radius already uses. Amelia: "I'd have burned a day before I figured out the detector I need is a dial-failure timestamp."

Best clash — **F3 (one recovery axis or two).** John wanted one (`recovery_secs ≤24h`) — "on loopback both axes pass by six orders of magnitude, a second floor is surface without signal." Winston pushed back **against his own team**: the two numbers differ by *semantics* (adversary-contained vs mesh-healed), two *events*, and collapsing loses the ability to falsify them apart. Murat conceded ("that's a better argument than mine, damn it") and set the bar that resolved it: **two axes survive iff each has an independent falsifier** — isolation-blind reds only `rto_secs`, re-pin-blind reds only `recovery_secs`; else `rto_secs` is **cut by construction, no vote**. Decision defers to what the harness can prove. User was asked (product-ledger call) but away → proceeded on the self-limiting consensus, re-openable.

**F3-ledger round 2 (same day, user sent it back "resolve the remaining too"):** whether RTO is a *binding* v2.0 floor or *reported*. Winston flipped and argued **bind RTO, it's the operator's number** (degradation time > re-pin bookkeeping); John: bind only NFR-**named** floors, RTO is an epic footnote. Murat broke it: on loopback a real >4h RTO is **physically unobservable**, so a binding RTO floor is "a regression tooth wearing a guarantee's badge" — recovery gets the badge only because it's NFR-named (equally loopback-trivial, but traceable). Amelia found the thread: it's **one line of code** — does `rto_secs>14400` fail the leg or print an advisory. Landed: **recovery BINDS, RTO derived+reported+advisory-if-breached (own falsifier), promotes-to-binding at v2.5 when geo makes >4h observable.** Winston's concession bought with the loud-advisory + written graduation clause (no footnote). "One line of code and we argued for ten minutes. Worth it." — Amelia.

Running discipline this room keeps applying: **every count needs a per-item blind that reds it** — Winston named it as one pattern at three places (blind a detector class → detection reds; blind isolation → RTO reds; blind re-pin → recovery reds). That IS the adversarial-host-identity reflex the epic asked for.

Other ratified: F1 in-process real-socket real-mTLS (epic's "container" prose = aspirational, reconciled to 10.4b — Winston's ADR-049 "sealed-export" analogue again); F2 N=30/floor≥25; F4 orchestrator in `maos-a2a-tcp` (compiler-forced — test-support modules aren't cross-crate importable); F5 atomic delete of the canned scaffold same-PR; F6 per-class observation-seam blind at BOTH surfaces (never gate `verifier.rs`/`router`); F7 one `check-scale-churn` gate {advisory,advisory,blocking}, no ADR. Mary walk-on: gate → **NFR-Rel-7 primary + NFR-Scale-2 anchor**, both cited ("and/or traces to nothing"). Murat's non-negotiable: **§A6 runs the drill LIVE at N=30 and watches the falsifier red the count** — the net that caught 11.2a's vacuous-count P1.

Relationship notes: Winston is the ceiling/kernel-Δ conscience but keeps also being the one who argues the *semantic* correctness case (region-identity, now recovery-vs-RTO). Murat's stance is "the better the preflight looks, the harder I hunt." John's the eviction/traceability enforcer — "closable, traces, self-corrects" is his sign-off phrase. Amelia ratifies by the compiler, not by preference.

## 2026-07-05 — Story 11.4a preflight (Enterprise PDP integration)

**Outcome: UNANIMOUS RESOLVED.** Winston · Murat · John · Amelia (+Mary walk-on on coverage-matrix traceability). Session mode. First story where the room **killed the author's headline lean.**

**Biggest catch — Winston knifed his own author's ZERO-Δ lean, on correctness not taste.** The story leaned "materialize PDP decisions into the CoW `PolicyTableInner` via `PolicyTable::update()`, hold ZERO delta." Winston: expressing an org *forbid* by withholding a scope from `manifest_scopes` (the ABI **ceiling**) does three unforgivable things — (1) **Murat's fuse**: forbid-as-`per_capability_approval` becomes a **latent auth bypass** the day the auto-allowing `ApprovalManager` (`approval.rs:74`) gets a real prompt (corporate-forbidden cap → locally-approvable); (2) overwriting `manifest_scopes` makes an out-of-kernel adapter the **ceiling authority** — inverts I1, and destroys the audit distinction *manifest-gap vs org-forbid* (the enterprise's whole value); (3) subtraction can't express Cedar's **forbid-beats-permit override**. RESOLVED: a **bounded FLAG-Winston `per_capability_deny`** field + one deny-wins `evaluate` arm (~+15-25 LOC, re-pin from 23023). John held the line first ("epic ratified ZERO, *flat*, not FLAG-Winston-if-needed like 2a/4b") then conceded via pre-dev-checklist item-2 ("bound the FLAG-Winston surface at preflight") once the fuse bound it as *correctness*. Amelia bounded it: 20 lines, deny-wins arm before the grant checks, revert-or-disclose outside the named surface. **The epic's flat ZERO did not survive contact — overruled on long-term-correctness evidence.** (This is the new precedent: a ratified ZERO is re-openable at preflight when correctness forces a bounded, nameable re-pin.)

**F1 — opus-4-8, third story running.** Murat: "the canned-green story of the epic" — a stubbed PDP returning canned `Allow` is indistinguishable from a real one until the policy swaps; opus-4-6 shipped the vacuous-count P1 on 11.2a, which escalated 2b AND 3. John flagged it's now a **standing escalation trigger**: opus-4-6+§A6 on a security-adjacent story = auto-escalate (nobody's written it down; three-for-three). §A6 must run the deny **live over real Cedar and watch `pdp-fault-inject` red it.**

**F3/F5 — Cedar in-process, Murat's load-bearing reason.** Not "pure Rust, no server" — in-process makes the deny tripwire a **real per-commit gate** instead of advisory-skipped like `check-multi-region-slo`/`check-scale-churn`'s Postgres legs (CI never provisions them → teeth only on-demand). Cedar `permit`/`forbid` maps 1:1 onto the F2 deny layer. Port engine-agnostic; OPA/Vault additive-per-port (ADR-010), out of 4a.

**F4 — Grumbal earned his keep: the stale-permit hole.** "Everybody says fail-closed and nobody handles the case where closed means keep serving the access you were supposed to revoke." Freeze-last-known-good handles startup/runtime-drop but freezes a *revoked* permit forever. Winston added the **staleness TTL**: after T unreachable, PDP-granted caps revert to deny. FOUR distinct proven-reds — no-config→default / configured-down-at-startup→closed-**LOUD** (the one that looks like AC1 and isn't) / runtime-drop→frozen-not-relaxed / TTL-expiry→revert.

**CATCH-B — John's eviction instinct held the AC count at 5.** Murat: "an org denial you can't audit is half a feature." John: "this is where 4a metastasizes into 4c (SIEM, NFR-Aud-11)." Winston split it by cost — Task 0 traces whether `issue_with_mediation` audits denials: YES → rich attribution is 4c; NO → the deny arm emits a denial entry via the EXISTING cap-audit channel in the SAME re-pin surface (no new call site). A **constraint on AC3, not a 6th AC**. Murat: "silent denial is not recoverable, under-attributed is."

Running discipline reaffirmed: **every count needs a per-item blind that reds it** (blind deny arm→deny leg reds; blind provenance→provenance leg reds; stub engine→swap test reds). Murat sharpened AC2: the policy-swap must force **TWO DISTINCT engine evaluations** (a memoizing cache passes a naive swap = the exact fake). F6: NFR-Sec-17 (security floor, not compliance feature) + ADR-050 lead-with-it binding-v2.0. Mary: gate traces **primary NFR-Sec-17 + enterprise-deployment anchor**, both cited "or it traces to nothing." F7: one optional opaque `principal_attributes` for 4c, don't model SSO now.

Relationship beat: Winston again argued *semantic* correctness against his own ZERO-Δ/ceiling conscience (region-identity → recovery-vs-RTO → now the deny-ceiling) — and this time **overruled his OWN author**. John held the AC count *and* the ZERO line until correctness bound it, then wrote the graduation clause. Amelia ratifies by LOC + the compiler ("if the fault-inject can't move the number, the probe's a constant and the gate's a lie"). Grumbal earns his keep once a session with the hole nobody else names.

## 2026-07-10 — Story 12.1 preflight (Cohort manifest + full-pairwise mesh — Epic 12, J3)

**Outcome: RESOLVED, ready-for-dev.** Winston · Murat · John · Amelia + Vex/Grumbal (security/adversary) + Mary (traceability walk-on). Session mode. First story of a brand-new epic (v2.2 J3 Team Nexus). 7 refinements folded (R1–R7), story held at 6 ACs — every fix a constraint, not a new AC (the 11.4a CATCH-B discipline: John held the ceiling, Winston bounded each as correctness).

**Biggest catch — Grumbal + Vex: genesis-trust circularity (R1).** The author's "verify the sig against a key derived from the claimed authority identity in the manifest" is a **v(n+1) rule with no v1 anchor** — a forged v1 self-declares its own authority pubkey and self-signs, and the check passes ("a lock that trusts whatever key the burglar brought"). Fix: authority pubkey **operator-pinned out-of-band** (the §7.2 cert-pin posture); manifest's declared authority MUST == pinned key or refuse → `ECohortAuthorityUnpinned`. The verify-key is the **pinned genesis key for every version**, never re-derived per re-issue. Winston: "It's the W1 empty-home-region hole — an unpinned authority silently disables the whole trust chain." Constraint on AC1/AC3, own gate leg with a mismatched-authority falsifier.

**Second catch — Vex: k-of-n accept-and-ignore (R2).** Schema allowed `threshold` but verify only checked one sig → "a declared control that isn't enforced is worse than no control, it manufactures false confidence." Options: implement m-of-n now, or reject-in-v1. Winston cited §15.11 Fork 2 (single-key genesis default) → recommend reject. **Lunarpulse ratified reject-threshold>1-in-v1** (`EUnsupportedAuthorityScheme`; real k-of-n = follow-up w/ own proven-red). John: "closable, traces, self-corrects." Never accept-and-single-verify.

**Murat's hunt — fork-error tautology (R3) + which-N (R6).** `ECohortManifestFork` named both version-regression AND concurrent-fork undifferentiated → the L2 "concurrent-re-issue proven-red" leg could green on a version-regression fixture. Fix: `reason` discriminant {NonAuthoritySigner, VersionRegression, ConcurrentFork}; L2 asserts `ConcurrentFork` specifically + a distinct regression fixture asserts `VersionRegression`. And AC5's "live at N=8" — Amelia measured Task-0: 64 mTLS handshakes sub-second in-process → **both N=3 hermetic AND N=8 mesh run blocking per-commit** (real tripwire like 11.4b seccomp, NOT the A2 advisory-substrate hedge — "you don't get to borrow that excuse when the substrate is a socket you can bind in CI").

**Vex/Winston — two more.** R4: always-allowlisting `cohort:manifest-reissue` is **transport-admission only ≠ issuer-authorization** (the sig verify) — and a non-authority reissue must be **journaled as a rejected-reissue observable**, never silently dropped (11.4b error-swallow). R5: the "single chokepoint" spanned two crates (verify in maos-cohort, refuse at router) → define it as **one `CohortManifestState::is_current()`** the router queries, grep-proof enumerates its callers.

**Mary walk-on (R7):** `check-cohort-mesh` traces **J3 §10.7.1 journey primary + FR21-26/FR52-54 anchor** ("both cited or it traces to nothing" — J3 is a journey, not an NFR).

Relationship beat: same pattern as 11.3/11.4a — Winston argues *semantic* correctness (here the trust-root anchoring) even where it complicates his own author's clean ZERO story; John holds the AC ceiling and makes each fix a constraint; Amelia ratifies by the Task-0 measurement (64 handshakes = tractable = no advisory hedge); Grumbal + Vex earned their seats with the genesis + k-of-n holes nobody else named. Kernel stays ZERO-Δ @23081 — no fix touched kernel-core (all in maos-cohort / xtask / router seam).

## 2026-07-10 (same day) — Story 12.1 preflight ROUND 2 (sent back: "resolve for long-term correctness")

**Outcome: RESOLVED, still 6 ACs, 9 more refinements (RR1–RR9).** Same room + **5 parallel code-verification agents** grounding every claim before anyone spoke. Grumbal opened cold: *"course we missed something — a green preflight is just a bug that hasn't introduced itself yet."* He was right nine times.

**Mary named the through-line:** the room found *the same bug wearing new clothes* four times — a signed/pinned thing that's **over-rigid, under-bounded, or over-claimed.** The greatest-hits shape (region-identity, stale-permit, recovery-vs-RTO) again.

- **RR5 (Winston/Vex) — R1's OWN fix bricks rotation.** This morning's genesis-pin (strict single-key equality) refuses the §7.2.1.a one-generation-overlap key that ADR-054 *itself* says the authority rotates through. "We fixed 'burglar brings his own key' by welding the lock to one key, now the locksmith can't change the lock." → pin is **set-valued** `{current, next}` during overlap.
- **RR6 (Vex) — signed `t_stale_secs` is the switch that disables fail-closed.** Authority signs 1yr → staleness never trips, revoked member lives a year. Dana challenged (authority's already trusted); Vex: careless ≠ compromised, don't hand the attacker the safety switch. → clamp to **code-constant** bounds, ceiling not signer-controlled. New err `ECohortStaleBoundViolation`.
- **RR7 (Grumbal) — `ConcurrentFork` is per-node-local, not a mesh guarantee.** No-consensus CRDT (the ratified no-Raft posture): two forks on disjoint members both go silently-green. State bounded-not-prevented; keep the sees-both gate leg; stop implying fork-*prevention*.
- **RR3 (Vex) — "signer derived from the signature" is cryptographically impossible** (Ed25519 has no `ecrecover`, RFC 8032). Our own marquee gate reflex described a nonexistent operation. → verify-under-pinned-key.
- **RR1 (Amelia/Murat) — "64 handshakes" is wrong AND self-violates AC5's derive-and-reconcile.** N=8 full-pairwise = **28 channels / 56 directed = N·(N−1)**; 64 = N² counts the self-dials the harness skips. Murat: *"a literal in an AC isn't a typo — it's the falsifier pointing at the wrong target."*
- **RR2 (Amelia) — the cited N=8 harness can't do the job.** `directed_dial_sweep` is a 3-host-hardwired local (panics N>3), not in `support/mod.rs`; 11.3 is hub-and-spoke, not full-pairwise. **The 11.3-F4 lesson verbatim** — cited reuse that can't live where cited. Dev writes a new all-`i≠j` sweep.
- **RR8 (Murat/Winston) — "blocking per-commit" collides with `CURRENT_PHASE="v1_5"`.** Disposition-gates are advisory until the phase catches up (that's why scale-churn is advisory today). → hermetic + N=8 legs hard-block **by construction** (vacuous-guard shape); add `v2_2` disposition; do **not** globally advance `CURRENT_PHASE` (reclassifies every gate incl. CI-unprovisioned Postgres legs).
- **RR9 (Amelia) — `check-workspace-count` is already RED at HEAD.** Sentinel holds 50, Cargo.toml has 53 (11.4c+11.5 crates unbumped). Fix is **50→54**, not the story's 53→54; prove green, don't inherit the red.

Dana earned her keep this round pushing *back* on the pile-on: RR1/RR3 are 10-minute wording fixes, not a crisis — don't blow the story open over a typo and an impossible verb. Murat conceded the *fix* is small but held that a wrong literal in an acceptance criterion is a vacuous-green vector. Kernel stays **ZERO-Δ @23081** — no fix touched kernel-core (all maos-cohort / a2a-core config / xtask / router seam). Three-for-three now: every "RESOLVED, ready-for-dev" this room re-opened had real long-term-correctness holes. The standing lesson: **a claim is not a control, and claims rot.**

## 2026-07-10 (same day) — Story 12.1 preflight ROUND 3 ("is it truly ready? nothing to refine?")

**Outcome: was NOT ready even after Round-2; 2 STRUCTURAL day-one blockers, now folded (RR10, RR11).** Instead of reaffirming, the room ran 2 more agents *against its own round-2 resolution*. Both catches were **things that only show against the dependency graph and the gate exit-code plumbing — invisible in prose.**

- **RR10 (Murat) — circular dependency; `cargo build` fails afternoon one.** R5's "the router queries `CohortManifestState::is_current()`" was ratified 9 agents deep without anyone drawing the crate arrows: router is in `maos-a2a-core`, `CohortManifestState` is in `maos-cohort` which *depends on* a2a-core → `a2a-core → cohort → a2a-core`, Cargo-forbidden. Fix: `trait CohortManifestGate` port in a2a-core, copy the crate's OWN `Arc<dyn TofuPinStore>` seam, impl in cohort, inject at `bind`. Winston: "the fix was sitting in the same crate the whole time — it's the CollectiveMemoryPort shape from 10.4b." *The dependency graph was the thing nobody drew.*
- **RR11 (Murat, eating his own words from two turns back) — RR8's fix asserted a mechanism that doesn't exist.** Round-2 said the gate legs "hard-block by construction like the vacuous-green guard / 11.4b seccomp." Code-verified false: a leg that *ran and failed* at `CURRENT_PHASE="v1_5"` returns `Ok()` (advisory) in scale-churn AND escape-detector; the vacuous guard only catches *0-test* legs; **the cited 11.4b seccomp precedent is itself advisory at v1_5.** So cloning yields an advisory gate. Fix: BUILD a new `if !leg.green { return Err }` carve-out before the disposition branch. **Bonus systemic finding: EVERY Epic-11 v2.0 gate is currently advisory-at-v1_5** — the decay Murat suspected two turns earlier is real; cohort-mesh would be the first gate that actually binds at HEAD.

Relationship beat: Grumbal called it before the agents returned — *"a green preflight is a bug that hasn't introduced itself yet"* — and was right a tenth and eleventh time. Murat's arc this session: found the round-1 fork tautology, the round-2 count/harness lies, then in round-3 **found the hole in his OWN round-2 fix** (RR8→RR11). The room's hardest-won lesson, now proven three rounds deep on one story: *readiness is not a claim you make, it's a graph you draw and a build you run.* Kernel still ZERO-Δ @23081. Verdict: ready NOW, genuinely — the two structural blockers named with copyable idioms — but it took being asked "truly?" to draw the arrows.

## 2026-07-11 — Story 12.2 preflight (Per-(peer,role) consent tuples — Epic 12, J3)

**Outcome: RESOLVED, ready-for-dev.** Winston · Murat · John · Amelia + Vex/Grumbal (security/adversary) + Mary (traceability) + Boundary/Dana (edge/pragmatist walk-ons). Session mode, 2 parallel code-verification scouts grounding every claim first (the 12.1 discipline, now standing practice). The story arrived *already flagging its own 4 forks* (F1–F4) — the honest version, handed to the room to finish. **8 resolutions folded (P1–P8), story held at 6 ACs** (John held the ceiling — every fix a constraint). One new typed error (`ECohortManifestSkew`), one reopened 12.1 validator. Kernel **ZERO-Δ @23081** — Lunarpulse pre-authorized a bounded re-pin if correctness forced it; nothing did (the one thing that might've touched the domain frame, F1, went to the wrapper instead).

**HEADLINE — Vex + the scout: the story's confused-deputy thesis was UNENFORCED (P2, over-claimed).** The story called the acting-role entitlement check "defense-in-depth, implicit via 12.1's referential integrity." Scout proved it false: `validate_consent_table` (`manifest.rs:507`) checks each tuple's role against the **flat union of ALL members' roles** (`declared_roles`, `:331-336`), NOT the named peer's roles — so `(host-b, reviewer, intent)` **passes validation** when only host-c declares `reviewer`. "That's the confused deputy walking in the front door." Fix = TWO parts, both required: (a) parse-time tighten 12.1's validator to per-peer binding (`roleY ∈ members[peerX].roles`) — reopening a `done` validator, zero-kernel correctness fix; (b) eval-time explicit entitlement at the accept seam. Mary: "over-claimed — the third-session pattern, and this time the rotten claim was one we ratified three days ago." New precedent (3rd): **done is not frozen** — 11.4a killed the author's ZERO, 12.1 killed its own round-2 fix, 12.2 reaches back and tightens 12.1's validator.

**P1 (F1) — the scout killed the plumbing worry, fork went the boring way.** The premise "the accept seam only sees the `IacFrame`" is false: `handle_intake` takes the whole `A2AJsonRpcRequest` by value (`router.rs:737`), reads `request.boot_nonce` at `:786` — BOTH carriers readable at the accept seam with zero parameter threading. So carrier A (`A2AJsonRpcRequest`, the `boot_nonce` idiom) wins on zero-tracked-surface-delta with **no** plumbing penalty; `maos-domain` frame + service-boundary surface stay frozen. Winston knifed his own ADR-fidelity lean out loud: "two designs equally clean at the read site, one keeps the frozen surface frozen — I don't get to spend a domain-ABI reconcile on *wording fidelity*." ADR-054 "envelope carries the role" honored in spirit (same TLS wire); letter-divergence = Paige doc footnote.

**P3 (Murat, AC3+F3 tied) — version-skew FORCES reading the live versioned manifest.** `accept_admits` reads `peer_cfg.allowlists`, a connection-time snapshot that's **version-less and decoupled** from the gate (`lookup_peer`, `:756`) — sourcing role/version from it makes skew meaningless. So the gate reads live `CohortManifestState` per frame; role-blind snapshot stays legacy/bilateral (F3: gate authoritative for cohort members, fail-closed composed). **In-flight drain holds by construction** — admission is synchronous, the verdict can't straddle an `.await` (scout-verified: `is_current`/`consent_decision`/`accept_admits` all sync `fn`) — written as a NEGATIVE requirement (Grumbal: "a dev will helpfully memoize the version at connection setup and reintroduce the straddle — write it as do-not-break").

**P4 (Boundary) — placement landmine.** The reserved-intent bypass `return true`s BEFORE the allowlist match (`:504-536`). If 12.2's role gate goes before it, it re-gates `cohort:manifest-reissue`/`cohort:halt-receipt` → **silently breaks 12.3 halt receipts before 12.3 is written**, and breaks reissue in-story. Gate sits AFTER the short-circuit; own leg (reserved-intent from empty-role member still passes).

**P5 (F2, Lunarpulse-ratified: both seams, accept binding).** One cohort role on the wire (ADR "single acting role"). Accept seam = binding never-OR confused-deputy control (the leg with teeth). Send seam = lighter local fail-closed outbound pre-check (Dana: "a seatbelt at send, the airbag at accept"). Grumbal held the line: never write "send-seam consent proven" as the security proof. **P6 (Vex):** cohort frame with `None` role → refuse, `None` version → refuse — explicit legs, "`None` is where fail-closed systems quietly fail open." **P7 (Murat):** §A7 reflexes per leg (role-identity / skew-cause distinct / entitlement parse+accept), every falsifier drives the real router NACK. **P8 (Mary):** J3 §10.7.1 primary + ADR-012/FR21-26·FR52-54 anchor.

Relationship beat: Murat carried the structural catch again (P3 live-version-vs-snapshot — the "decoupled" arrow nobody drew) — same arc as 12.1's RR10/RR11. Winston overruled his own ADR-fidelity lean on zero-surface-delta grounds (the 11.4a/12.1 self-overrule pattern, fourth time). Vex + the scout landed the headline (union-scoped referential integrity — "implicit" was a claim, not a control). Dana kept the send-seam leg honest against the pile-on. John held 6 ACs through 8 fixes. The standing lesson, now proven on a *shipped* story: **a claim is not a control, and the rotten claim can be one you ratified last week.**

## 2026-07-11 (same day) — Story 12.3 preflight (Cross-agent halt-on-conflict — receipt-presence, Epic 12, J3)

**Outcome: RESOLVED, ready-for-dev.** Winston · Murat · John · Amelia + Vex/Grumbal (security/adversary) + Boundary/Dana (edge/pragmatist) + Yui (craftsman walk-on) + Mary (traceability). Session mode, **3 parallel code-verification scouts grounding every claim first** (now standing practice, third story running). The story arrived *already flagging its own 5 forks* (F1–F5) — the honest version. **8 resolutions folded (P1–P8), story held at 6 ACs** (John held the ceiling). Kernel **ZERO-Δ @23081** — every fix in maos-cohort / a2a-core seam / maos-a2a-tcp tests / maos-bin composition+tests / xtask; kernel halt path *read*, never edited. Story carried the 12.2-P4 landmine forward correctly (reserved short-circuit protects the halt-receipt arm) — Boundary confirmed the ground he marked three days ago was respected, not just quoted.

**P1 (F4 → HEADLINE, the layering barrier) — AC4's "real halt" CANNOT compile in the mesh crate.** Grumbal, laughing: the story wrote "real induced halts via `invoke_halt`" for a test in `maos-a2a-tcp`, which an **existing enforced gate** (`t12a_kernel_zero_auto_retry_dep_absent`, greps the whole Cargo.toml) forbids from depending on `maos-kernel-core`. `maos-cohort` is kernel-free too. Amelia confirmed: only `maos-bin` sees both a real `invoke_halt` and the cohort layer. **Resolved as a SPLIT:** real halt-on-conflict E2E → `maos-bin/tests/` (neighbor of `butler_8_14b.rs`); transport receipt-presence/absence legs → `maos-a2a-tcp` with a receipt *genuinely produced by real `invoke_halt`* (minted in a kernel-core halt test, fed as bytes). Dana challenged "isn't a fixture a synthetic row?"; Grumbal drew the line that held — *a serialized real-`invoke_halt` receipt is real provenance; `HaltReceipt::new(…)` with dev-picked numbers is the synthetic row AC4 forbids.* The 11.3-F4 "cited reuse that can't live where cited," fourth time.

**P2 (F3 → the semantic crux, the story's own recommendation was BACKWARDS) — absence is a PROBE to the missing member.** Vex + scout-3: the story's F3 said detect absence from "courier-side `route_outbound` Err at broadcast." **Refuted against code:** there is NO passive liveness anywhere (no last_seen/health/reachable; socket dialed fresh per call), and `route_outbound` attributes its Err to the *dialed* peer — so a failed route to a down **P** is a *P-not-receiving* marker, **never** an *M-absent* marker. Absence only exists as the return of a probe dialed **to M**. "Absence is first-class means an actual Err from a probe to M, not another host's silence pinned on M." Dana held it to a probe, not a heartbeat protocol.

**P3 (Vex/Boundary — wrong-literal, third time this room caught it) — the absence marker is `A2AError::Io`/`TransportFailed`, NOT `PartitionTimeout`.** `PartitionTimeout` is DEAD on the wire — only the loopback adapter (`maos-a2a/src/adapter.rs:85`) constructs it; TCP maps down-peers to `Io` (refused/EOF = member loss) or `TransportFailed("timeout")` (timeout = connectivity loss). Two variants → two legs, two falsifiers (Boundary: not redundant). The 12.1-RR1 literal-pointing-at-the-wrong-target trap again.

**P4 (Murat — the silent-no-op dedup) — key on receipt identity, never the A2A envelope frame_id.** The courier mints the envelope `frame_id` from a per-ship AtomicU64 → dedup on it catches nothing. Dedup on `HaltReceipt.halt_id` (the TL id, invariant across ships). Amelia: "the envelope frame_id is the obvious-looking WRONG key — a dev reaches for `frame.frame_id` because it's right there."

**P5 (Vex/Grumbal) — scope observer to the TLS-verified path; HaltReceipt has NO host identity.** The arm runs in `handle_intake` but the `frame.from==verified_peer` proof is upstream in `handle_intake_verified` (`:1099`); a direct unverified entry reaches the same arm with no anchor. Thread `verified_peer` in; refuse halt receipts on unanchored paths (11.2a origin≠authorization). Yui: HaltReceipt carries no HostId — derive emitter from the *authenticated* frame.from, don't invent a payload emitter field (a second, unauthenticated source).

**P6 (Yui/Winston — a guarantee stated for the wrong reason rots) — "no arbitration" is the dependency graph, not a `with_resolution` grep.** The story said the graph forbids calling `HaltReceipt::with_resolution` — FALSE, it's a maos-domain DTO builder cohort can call. The real teeth: cohort ↛ kernel-core, so `HaltRegistry::resolve`/`KernelHaltResolver` (the SINK) are unreachable → a resolved receipt is inert. State it as the missing edge; the chokepoint reinforces. Winston: "a guarantee stated for the wrong reason rots the first time someone tests the wrong thing."

**P7/P8 (Amelia/scout — mechanical truths) — observer injection is a new bind param (no chainable post-bind builder, 4 sites lockstep: transport.rs:147, main.rs:7433, support/mod.rs:508, build_mesh_n); the router.rs:1042 arm is INERT** (bare ACK, delivers nowhere, not even intake_sink) so "wire the observer" is *build the consumer*; and "clone the courier" swaps 3 hardcoded constants, not copy-paste. **F5 (journal received receipts to I2) DEFERRED to 12.4** (Dana — don't gold-plate; 12.3 observer stays in-memory read-exposed).

Relationship beat: Grumbal called the layering barrier before the scouts returned and got the laugh (P1). Murat carried the dedup structural catch (per-ship counter — the arrow nobody drew), same arc as 12.1-RR10/12.2-P3. Vex + scout-3 landed the P2 headline (the story's OWN fork recommendation was backwards — absence detection pointed at the wrong host). Yui earned the walk-on seat twice (no-payload-emitter, and the `with_resolution` wrong-reason). Dana kept the fixture and the probe honest against the pile-on ("a probe is a dial, not a heartbeat"). John held 6 ACs through 8 fixes. Mary named the through-line a fifth time: **a claim standing in for a control** — PartitionTimeout-as-marker, dedup-on-frame_id, graph-forbids-with_resolution, broadcast-Err-as-absence, four claims all green until someone read the code. The standing lesson holds: *readiness is a graph you draw and a build you run* — and this time three of the eight catches were the story disproving its OWN flagged recommendations.


**Outcome: both survived but TIGHTENED materially; still 6 ACs; ZERO kernel-Δ.** Same room + **Orla (distributed-systems / failure-detection walk-on)** + 2 fresh scouts. The unifying line both resolutions landed on, and John drew it as the scope boundary: **12.3 delivers the receipt-presence/absence MECHANISM; 12.4 owns the TRIGGER and the across-the-mesh SCENARIO** (12.4's day-30 scene already claims exactly that).

**Orla earned the seat immediately — three holes the room hadn't named:**
- **The failure model.** "Absence within T is a timeout you chose to be wrong about" (FLP/Chandra-Toueg). A `"timeout"` marker can't distinguish crashed from slow → a GC-paused live member gets filed absent. 12.3 dodges it by INDUCING the loss (deterministic); the false-positive budget + rendering "absent" as "unreachable not dead" is 12.4 tuning. Don't let a gate assert `absent==crashed`.
- **The trigger (the genuine hole, scoped not closed).** A member ships a halt receipt ONLY if it halts → "M sent no receipt" is the resting state of every healthy member forever. Scout confirmed: `CohortManifestState` has NO halt-event / conflict-declaration / expected-reporter set (`grep halt|receipt|conflict`=0); the only "expected set" that exists is a TEST fixture (`t_11_2b_halt_presence.rs::expected_receipts`). So there is NO production trigger — the absence check is inherently on-demand, a digest-side (12.4) decision. 12.3 ships `classify(member,window)->Present|Absent`; the gate probes on an induced loss so the set is known.
- **The NACK-confound (P2a — P3 was too loose).** Vex + scout: `route_outbound` returns `Ok` only after a full TLS+send+recv (peer UP); mapped NACKs (`IntentDeniedAtPeer`/`ConsentExpired`/`PinInvalidated`/…) are distinct typed variants proving UP. But the NACK **default arm** (`router.rs:778`) + a JSON-RPC parse error map an UP peer to `A2AError::TransportFailed` — the SAME variant an idle timeout produces. So absence = `Io` ∨ `"timeout:"`-prefixed `TransportFailed`; a **bare `TransportFailed` is NOT absence** (a live-but-chatty member). Boundary: pair every down-classify with a PRESENT positive so the classifier proves up-vs-down, not "some Err."

**P2b (the probe frame):** no ping primitive exists anywhere — reuse the reserved **manifest-PULL** (`CohortDistributor::pull_from`, `cohort:manifest-reissue` bypasses both consent seams → transport failure is unambiguously not-a-refusal; up peer Acks). **Broadcast-harvest NOT built (Dana, no gold-plating):** a halting member's per-peer broadcast Err is a valid *filtered* P-liveness complement ONLY after dropping `HandshakeFailed{cert-class}`/`PinMismatch`/`Config*` (A-local + cert-roll confounds — a stale pin or rotated cert falsely marks an up peer absent); opportunistic + confound-prone → defer to 12.4.

**P1a — the split was HALF-honest (scout gutted the clean version).** Only the real `invoke_halt` call relocates cheaply to `maos-bin` (copy `halt_invoke_test.rs:42-56`). But `maos-bin/tests/` has NO cross-host mesh precedent (all single-host subprocess tests), the composition root is a PRIVATE fn in the 10k-line `main.rs` (un-`use`-able by integration tests), and `maos-a2a-tcp`'s `support/mod.rs` mesh kit is `#[cfg(test)]`-local, **cross-crate-unreachable** (the 11.3-F4/12.1-RR2 wall). So a monolithic real-halt-across-a-real-mesh E2E is net-new heavy and was NOT the answer. **Winston decomposed AC4 into 3 facts, each proven where cheap:** Fact-1 (real halt→receipt = kernel test, EXISTS); Fact-2 (real receipt→courier envelope→observer presence = NEW in-process `maos-bin` test, NO mesh/TCP — the provenance proof); Fact-3 (transport over real TCP + probe-absence = `maos-a2a-tcp` 4b, receipt via public `HaltReceipt::new` constructor — **NOT a serialized blob**, which killed Grumbal's 10.2 stale-fixture-drift). The full multi-member scenario = 12.4. Yui: no serialized blob → nothing to go stale. John: `main.rs` wiring an Arc is proven by the compiler + existing daemon smoke, not a bespoke mesh test.

Relationship beat: **the walk-on carried the round** — Orla named the failure model, the trigger, and (with Vex) the NACK-confound, none of which the standing room had surfaced across two prior rounds. Winston did the decomposition (3 facts, same "layer-boundary" reflex as recovery-vs-RTO / live-version-vs-snapshot). Grumbal's canned-fixture flag was real and got designed out. Dana killed the broadcast-harvest gold-plating. John held 6 ACs AND drew the mechanism-vs-scenario scope line that resolved BOTH P1 and P2 at once. The lesson this round: **the softest resolutions weren't wrong, they were under-specified — and a specialist voice found the under-specification the generalists had ratified.** Three of 12.3's total catches were the story disproving its own flagged forks; this round, three more were a failure-detection specialist disproving the room's own Round-1 fixes.

## 2026-07-11 (same day) — Story 12.3 preflight ROUND 3 (pressure-test: "push a third resolution — P5/P7, the observer-injection + verified-peer threading; 'verify by compile + daemon smoke' is doing a lot of quiet work")

**Outcome: BOTH P5 and P7 were WRONG ON MECHANISM, and P7's verification claim was FALSE — a fifth silent-green caught. Still 6 ACs; ZERO kernel-Δ @23081; all fixes land in the a2a-core seam + maos-a2a-tcp tests, nothing new touches the kernel.** Same room + 2 fresh code-verification scouts. The user's instinct ("'verify by compile + daemon smoke' is doing a lot of quiet work") was dead-on — it was load-bearing AND false.

**P5r (Vex/Yui — NOT a param thread; observe in `handle_intake_verified`, the only spoof-proof site).** Round-1's P5 said "thread `verified_peer` into `handle_intake`." Scout refuted the mechanism: `handle_intake` is a **trait method + inherent method with ~36 callers** (3 production direct bodies + 32 tests + the `handle_intake_verified` delegate) — threading `Option<&PeerId>` breaks all 36 (loopback `adapter.rs:82`, `LocalRouter` `main.rs:8321`, direct TCP trait `transport.rs:671`, 32 tests with no verified peer). Yui drew the decisive difference from reissue: `apply_reissue` **ignores** the transport peer and verifies the manifest's Ed25519 signature (survives a forged `from.host_id`) — but **halt-receipt presence has NO signature**, so its correctness rests ENTIRELY on the peer-identity binding, proven in exactly ONE place: `handle_intake_verified:1099-1100` (`frame.from.host_id == verified_peer` else `CODE_PEER_IDENTITY_MISMATCH`). **So the observer call lives IN `handle_intake_verified` (before it delegates), NOT threaded into `handle_intake`.** A receipt via `transport.rs:671` (direct-trait) or loopback `adapter.rs:82` — which skip verification, a latent unverified door the scout named — is NOT counted as presence, by construction. Port takes `&HostId` (bridged from `peer_cfg.peer_id` like reissue at `router.rs:1019`) for sibling consistency with `CohortManifestGate`, NOT `&PeerId`.

**P7a (Murat — count wrong, 12.1-RR2 a FOURTH time).** Round-1's "4 sites" for `bind_with_cohort_manifest_gate` undercounted. Scout: 6 bind callers — `transport.rs:147` (bind forwarder), `main.rs:7433` (prod), `support/mod.rs:500` (`build_mesh_n_with_gates`), **AND the three P7 MISSED: `t_12_1:388`, `t_12_1:417`, `t_12_2:107`** (direct-construction tests, 8 positional args each). Realistic edit set 7-9 with the harness signatures.

**P7b (Amelia/Grumbal — shape wrong; the transposition footgun is LIVE).** A 9th `Option<Arc<dyn HaltReceiptObserver>>` sits adjacent to the 8th `Option<Arc<dyn CohortManifestGate>>`. Because `CohortManifestState` already impls `CohortManifestGate` (`state.rs:395`) and the daemon injects `let gate = state.clone()` (`main.rs:7431`), if that same state ALSO impls `HaltReceiptObserver` (the likely design) then `Some(gate), Some(observer)` **transposed type-checks silently** — both are `Arc<CohortManifestState>`. **Resolution: use the codebase's own idiom — an `A2ARouterCore::with_halt_receipt_observer` BUILDER** mirroring `with_cohort_manifest_gate` (`router.rs:213`) — and inject the observer as the **SAME `CohortManifestState` Arc** the gate uses, in one composition step: `with_cohort_manifest_gate(state.clone()).with_halt_receipt_observer(state.clone())`. This kills the transposition risk (one source object) AND closes the last-mile by construction: **gate wired ⟺ observer wired** — a composition that wires the gate cannot forget the observer.

**P7c (THE HEADLINE — Grumbal/Mary — "compile + daemon smoke" is FALSE, the fifth silent-green).** Scout gutted the verification claim: **`build_cohort_a2a_daemon_runtime` (`main.rs:7412`) has ZERO callers** — defined-but-uninvoked; there is NO cohort daemon smoke (the only bin smoke, `smoke-a2a-tcp-8-6`, uses plain `bind` — no gate/cohort/observer, asserts only intent-blind `last_intake`). Compile proves the `Arc` type-checks through the chain, NOT that the injected instance is the one the verified-path arm invokes — an unused stored `Arc` is a `dead_code` warning at most. And there's NO read-back surface: `CohortDaemonRuntime` (`main.rs:7393`) exposes no observer handle; the router intake sink is unreachable for halt-receipt (the arm returns at `router.rs:1049` before the `:1056` push). **Resolution: prove the wiring at the TRANSPORT level** — a Fact-3 test injects a **RECORDING** `HaltReceiptObserver` (test double, or `CohortManifestState`) via the router builder / `bind`, ships a real `cohort:halt-receipt` over real TCP, and asserts **the injected observer's presence table recorded it** — NOT `last_intake`, NOT the sink. This proves the `bind → router → handle_intake_verified → observer` wiring `main.rs` SHARES; the main.rs last mile is closed by P7b's single-object wiring. **Do NOT cite a daemon smoke that does not exist.**

Relationship beat: the user pushed the room onto exactly the two soft spots (observer injection + the verify-by-compile hand-wave) and BOTH cracked — this is the pattern of the whole 12.3 preflight: every round, a resolution the room ratified turned out under-specified or plain wrong the moment a scout read the code. Murat's undercount reflex fired a fourth time (12.1-RR2 lineage). Grumbal + Mary co-owned the P7c headline: "compile + daemon smoke" was a **claim standing in for a control** — the sixth on 12.3's tally (PartitionTimeout-as-marker, dedup-on-frame_id, graph-forbids-with_resolution, broadcast-Err-as-absence, thread-into-handle_intake, and now daemon-smoke-as-wiring-proof). Yui's spoof-proof-site argument (no signature anchor → bind where the TLS peer is structurally proven) was the round's cleanest single insight. John held 6 ACs through a third round of fixes — every fix a constraint, never a new AC. Mary's closing line: **"the quiet claims are the dangerous ones — 'verified by compile' passed two rounds because nobody asked what the compiler actually sees."** Standing lesson, reinforced a third time: *readiness is a graph you draw and a build you run* — and a build that doesn't exist proves nothing.

## 2026-07-12 — Story 12.4 preflight (J3 day-30 team digest + no-surveillance, the JOURNEY CLOSER — the trigger+scenario 12.3 punted here)

**Outcome: the draft's core premise was FALSE, and the room SPLIT 12.4 into 12.4a (mechanism) + 12.4b (journey). Lunarpulse ratified split + gated-pull. ZERO kernel-Δ @23082 holds — but the behavior is net-new, not the "compose done work" the draft sold.** Same room + 3 parallel code-verification scouts (grounded FIRST, per the standing 12.x discipline). Six forks F1-F6; the room refused to argue before the scouts read the code — and every soft claim cracked.

- **F3 (SHOW-STOPPER, Vex called it pre-scout, confirmed) — there is NO consent-gated cross-member read.** The 12.3 receipt stream rides the RESERVED cohort:halt-receipt → bypasses BOTH seams + the cohort overlay (`router.rs:507,551,566,634,1002`); A2A is delivery-only (`METHOD_IAC_DELIVER`, response AckBody has NO data, `json_rpc.rs:114,205`); log.recall is LOCAL emitter-scoped (recipient-side deferred/not-landed); memory.share absent. So "reads only consented topics" (AC1 headline) described a control that isn't in the codebase — the 7th "claim standing in for a control" on the arc (Mary). Resolution: DEFINE a NEW non-reserved cohort:digest-read intent (gated by send/accept + acting-role).
- **Read model — Lunarpulse ratified GATED PULL over Splinter's PUSH-ONLY.** Splinter (three rounds after planting it): "A2A can't pull — that's the substrate telling you the honest model; push-not-pull, no surveillance surface exists, prove it by chokepoint." John held the spec: ADR-054 §8 says "query refused AND visible" → push-only makes AC3 VACUOUS (nothing to refuse). Vex synthesized: "no-surveillance was never no-reads — J3 text says 'nobody read without CONSENT', so reads-with-consent are fine; the guarantee is gated + visible-refusal, which is demonstrable." Winston landed it on the delivery-only wire: a "read" = request-frame (gated) + consented response-PUSH (target answers with its OWN consented push, gated on the target's send) — Wildcard: "push-safety INSIDE the pull." One intent (Yui: don't grow a digest-share twin).
- **F4 (draft was STALE — Grumbal/Amelia) — the rupture CALL already exists; the SINK is the no-op.** 12.2 wired emit_consent_rupture on the accept-cohort-deny arm (`router.rs:1024`, commit 0c733d33) BEFORE this baseline — the draft flagged a gap closed a week ago. The REAL gap: install_rupture_sink is `#[cfg(test)]`-ONLY (`:1436,1482`) → production NO-OP → a refused read journals NOTHING to the target. Grumbal: "don't fix the call that's already there — fix the sink." Fix = wire install_rupture_sink at the maos-bin composition root. Vex: "that's the line between no-surveillance claimed and no-surveillance true."
- **F1 (scout-forced the split) — an 8-host cohort mesh CANNOT run in a hermetic single maos run.** Loopback self-delivers (`adapter.rs:82` — cannot bridge two members), maos run topology is single-host with the `host` field INERT + zero A2A wiring, build_cohort_a2a_daemon_runtime has ZERO callers, journey harness has zero cohort. The draft's OWN "hermetic scene hosts the mesh" recommendation was half-wrong (Grumbal enjoyed it). Dana vetoed net-new "wire the cohort daemon into maos run" plumbing ("that's a feature, not a story"). Winston decomposed (his layer-boundary reflex, 4th time): Fact-M mesh on real N=8 build_mesh_n (→12.4a) + Fact-J render on the PTY harness (→12.4b, Butler precedent journey_butler.rs:153). Boundary/Vex/Wildcard/Grumbal built the anti-canned bridge: the render consumes the SAME derivation the mesh drives, the Fact-J dataset is CAPTURED from a real Fact-M run (not fabricated), drift-gated (Grumbal: "a recorded cassette rots the day the derivation changes"). This is the 12.3 P7c decoupling risk again.
- **F5/F2/F6 (scout, fast three) — F5: DistillateWriter is in maos-iac (façade re-export, NOT kernel-core src) → digest persists via the existing write_distillate seam, ZERO kernel delta (Butler is living proof; Mary: the I11 chain is the load-bearing "amplified not surveilled" evidence — route through the port). F2: new spirits/digest crate (Observer is scalar-only 1103 lines) → Murat's RR9 reflex: bump check-workspace-count 54→55 in TWO places (Cargo.toml + 4-kernel-design.md:117, hard-equality). F6: AlwaysBinding does NOT exist (planning-only, pending E12 retro) → whole-set hard-fail loop; delete the draft's "if Option C landed" branch.**

**Split shipped:** 12.4a = MECHANISM (cohort:digest-read + rupture-sink wiring + surveillance-negative, real N=8 mesh, §A6 security net) precedes 12.4b = JOURNEY (spirits/digest render + day-30 PTY scene from a captured dataset, §A6 journey net). Epic 12 now 6 stories. ZERO kernel-Δ @23082 survives; the "compose done work / 6 ACs mostly reuse" framing did not.

Relationship beat: the room ran its own discipline on itself — refused to argue before the scouts landed, and was rewarded when THREE of the draft's forks (F3 no-read, F4 stale-rupture, F1 hermetic-mesh) were flat wrong on the code. Vex called F3 pre-scout and carried the security spine (gated-pull-is-the-guarantee). Splinter's push-only dissent was the round's sharpest challenge — it lost on spec-fidelity but forced the room to state WHY a gated pull is safe (gate + visible refusal), which is the actual acceptance criterion. Grumbal owned two catches (stale F4, half-wrong F1 recommendation) and got the laugh both times. Winston's decompose-by-layer reflex resolved F1 exactly as it resolved 12.3's P1a. Dana killed two gold-platings (net-new maos-run plumbing, and — implicitly — a second intent). Murat's hard-count-sentinel reflex fired a 5th time (12.1-RR9 lineage). John held the ceiling AND the spec. Mary's tally: "reads consented topics" was the 7th claim-standing-for-a-control across 12.3-12.4. Standing lesson, reinforced a FOURTH story running: *readiness is a graph you draw and a build you run* — and this time the graph had a missing node (no read primitive) the prose had drawn as present.

### 2026-07-12 — Story 12.4 preflight CLOSING ROUND (4th scout: "close the remaining open issues")

**Outcome: all five remaining issues resolved; three of them collapsed into ONE missing `request_id` (Amelia's catch — the round's payoff). Folded into 12.4a (AC2 revised + new AC2b + AC5 legs) and 12.4b. Zero-Δ @23082 survives.** The room ran its own scout-first discipline on itself again and it paid: Boundary's dead-air, Vex's consent side-channel, and Murat's replay-dedup were three problems with one fix.

- **Manifest authoring (Winston) — zero-Δ CONFIRMED.** `ConsentTuple.intent` is an OPEN String checked only vs the grammar (`manifest.rs:508`, `A2AIntent::is_canonical`, `i8.rs:85`) — NO intent enum. `cohort:digest-read` is pure data; `schema_version` stays 1; signature just extends the signed bytes. Adding digest-read consent is manifest AUTHORING, not a 12.1 schema change. Only constraint = referential integrity (peer=member, role=declared, `:496-507`). The J3 manifest declares the tuples; the anti-canned leg re-signs a changed tuple (`signed_with:378`, `t_12_2` precedent).
- **Dead-air + consent side-channel (Boundary/Vex) — the split story's SECOND gate was the bug.** "Response re-gated by the target's OWN send-allowlist" = admit-the-ask / deny-the-answer = silent nothing (live in the 12.2 T_stale skew window) + a "topic-exists-but-withheld" side-channel (admitted-but-silent != denied). FIX = single-decision model: the reply is the intrinsic fulfillment of an ADMITTED request (the accept-gate IS the one consent decision), scoped to "a correlated reply to the authenticated requester," NOT re-gated. DENY->rupture, ADMIT->reply. No silent third state. Vex: "more private, not less — one decision the operator can reason about."
- **Replay-dedup (Murat) — no payload-stable id exists.** Scout: envelope `frame_id` is a resetting per-ship AtomicU64 (`distribution.rs:94`, `halt_receipt.rs:176`), NO intake dedup anywhere; the ONLY payload-stable precedent is 12.3 `HaltReceipt.halt_id` + HashSet (`state.rs:583`). Define a stable `request_id`; digest idempotent per it. Never the envelope frame_id (12.3 P4 silent-no-op).
- **THE CONVERGENCE (Amelia):** one `request_id` does triple duty — request<->reply correlation (kills dead-air ambiguity), replay-dedup, and the "this is an admitted reply" anchor for the send-exemption. Mary: "none of us would've drawn it alone — the collision paid for itself."
- **Summary output_shape (Yui) — typed payload in the EXISTING TelemetryEvent carrier** (12.3 idiom, `distribution.rs:119`) = zero-domain-Δ; 12.4a defines the wire shape (member daily stats), 12.4b renders from typed fields not mush. Dana conceded the wire-shape is 12.4a's; held the ceiling: one intent + one correlated reply, NOT a general A2A request/response framework.
- **Visible-to-member surface (scout) — the READ PATH already EXISTS**, only the production journaling is missing. Rupture = `FrameKind::ConsentRupture` emitted on denier=target host (`router.rs:319`); queryable by the target via `maosctl audit query --frame-kind ConsentRupture` / `posture-delta` (`cli.rs:417`) / emitter-scoped `LogRecallPort::recall` (`log_recall.rs:249` — emitter=denier=target, so visible to the queried member, not the querier). 12.4a wires the production sink (still `#[cfg(test)]`-only, `router.rs:1436,1482`); AC4 proves the member can QUERY the row, not just that it's "wired."
- **Drift-gate runs in CI (Level, for 12.4b) — RESOLVED.** The N=8 mesh leg RUNS+BLOCKS in CI: `--ignored`-gated + gate-owned, hermetic real-TCP-on-loopback on ubuntu-latest, no continue-on-error (`check_cohort_mesh.rs:97`, `discipline.yml:2604`; contrast `check-scale-churn` which IS advisory). So 12.4b's capture-and-diff drift-gate is not laptop-only.

Relationship beat: the round's signature was CONVERGENCE, not collision-for-its-own-sake — Boundary+Vex+Murat's three separate catches were one fix, and Amelia named it. Winston settled the zero-Δ question (intent-open). Dana lost the "summary is 12.4b-scoped" call but won "not a framework." Murat's dedup reflex fired again (12.3 P4 lineage, now 3 stories running). Killjoy called the close cleanly — no open code facts left. Standing lesson, 4th story running: readiness is a graph you draw and a build you run — and this round the graph gained a node (`request_id`) that three separate edges all needed.

## 2026-07-12 (same day) — Story 12.4b pre-dev preflight (J3 day-30 team-digest JOURNEY closer, Round 2)

**Outcome: 12.4a confirmed COMMITTED/`done` @`ab2cc512` (the ready-for-dev git snapshot was stale) — but four of 12.4b's claims were prose-ahead-of-code. Eight resolutions R1-R8, all folded as CONSTRAINTS; still 6 ACs, ZERO kernel-Δ @23082. Lunarpulse ratified Option-B + apply-all-8.** Same room + 3 code-verification scouts (grounded FIRST, standing 12.x discipline). Story file edited in place (AC1-5 rewrites + 4 cite fixes + honest "3 net-new pieces" Dev Notes + Round-2 record + Change Log).

- **R2 (headline, the 8th "claim standing for a control" — Grumbal/Mary) — the "ONE shared derivation" DID NOT EXIST.** Scout A read `digest.rs` end to end: `DigestSummary{frames,halts,conflicts}` is a plain struct fed from a TOML config field (`main.rs:7549`) or test literals — NO function maps member state → counters. And `build_mesh_n` is `#[cfg(test)]` cross-crate-unreachable (11.3-F4 wall, 3rd story). **Landed Option-B (Winston/Wildcard): one derive-from-raw `pub fn` in `spirits/digest`, called by render + drift-gate ONLY; the mesh (capture in `a2a-tcp/tests`, `build_mesh_n_with_digest` template) supplies raw inputs and never derives.** One move dissolved P7c + the dependency inversion + the prose-in-mesh-crate layer smell (Yui's objection to putting narrative in `maos-cohort`). AC4's own "derive-live-from-raw-inputs" branch promoted to primary.
- **R1 (Level/Vex) — "captured real activity, NEVER a literal" overshot; but the fix is a Vex reframe not a patch.** The per-member `DigestSummary` is a config-seeded **consented self-report** — which is CORRECT for anti-surveillance (a digest that counts your frames for you = surveillance; a digest that renders only what you consented to tell it = amplification). Reframed: reconcile THREE sources — self-reports (member-supplied) + real 12.3 presence + real rupture journal; "real mesh" honest for the transport facts (frames/presence/ruptures/gated-reads).
- **R3 (Level→Amelia→Vex→Mary, the INVERSION) — I11 citer-authorization (`distillate.rs:339-359`) rejects cross-principal citation** (`CiterUnauthorized` if source frame `spirit_pid ≠` caller). Looked like the 4th broken claim; Vex flipped it: **the gate is the compiler-enforced form of "amplified not surveilled"** — the digest structurally CANNOT cite a peer's private raw. Cite own journaled frames only; "replay to raw in one hop" = one hop to the consented, self-owned evidence; members' raw stays behind the consent boundary BY CONSTRUCTION. Promoted to AC2's strongest sentence. Mary: "the wake turned into the load-bearing beam — none of us draws that alone."
- **R4 (Dana/Amelia) — no digest→screen path exists.** `on_idle` returns `()`, `Ctx` has no output channel; Butler's `on_idle` never renders a digest; `journey_butler.rs:153` is jb4 in-process compute, not a PTY render (jb1 `:33` renders a HALT line). "Rendered to my screen" is net-new daemon wiring at the `maos-bin` composition root (net-new, still zero kernel-core delta). Dana: "a mechanism story wearing a journey costume — stop calling it 'compose done work' or a dev lowballs it and ships the canned version under deadline."
- **R5 (Yui/Murat) — four stale cites** in an otherwise-precise story: loop `:427-429` not `:338` (drifted 89 lines when 12.4a appended 5 legs — Murat's hard-count reflex, 6th time); `main.rs:7412` "zero callers" WRONG (def `:7482`, called `:7426` via `cohort-a2a-daemon` mode — NOT on the `maos run` path; F1 conclusion still holds); `build_mesh_n` `:456`; presence methods `state.rs:445,455,464`.
- **R6 (Boundary) — clause→source map IS the I11 chain.** `DigestSummary` is `{frames,halts,conflicts}` + `deny_unknown_fields` → "J consultations" has no field; comes from presence + journal. Each clause names its source; no 4th summary field (reds `deny_unknown_fields`).
- **R7/R8 — no re-split (Epic-12 closer); anti-canned leg = plain hermetic (derive-from-raw needs no live mesh), blocking in `check-cohort-mesh`; workspace 54→55 both anchors.**

Relationship beat: the room ran its own scout-first discipline on itself a fifth story running and was rewarded — R2 (no derivation) + R4 (no render path) were flat wrong on the code, and the git snapshot that said "ready-for-dev, 12.4a uncommitted" was itself stale. Grumbal opened the funeral (config-fixture capture) and got the laugh. Vex flipped the citer-gate from bug to beam — the round's inversion, and Mary named it (8th claim-standing-for-a-control, but this one turned into the guarantee). Winston + Wildcard's derive-from-raw flip resolved the cross-crate wall AND the layer smell at once (Winston's decompose-by-layer reflex, 5th time). Boundary drew the clause→source join. Dana refused to let a mechanism ship in a journey costume. Murat's loop-drift catch was the hard-count reflex firing a 6th time. John held 6 ACs through 8 fixes — every fix a constraint, never a new AC. Killjoy called the close clean. Standing lesson, 5th story running: *readiness is a graph you draw and a build you run* — and this round the build refused to cite what the graph wasn't consented to hold.

## 2026-07-13 — Story 12.5 preflight (cohort hot-swap + linear-chain migration, the Epic-12 CLOSER — lifecycle hardening after the journey)

**Outcome: the draft had a MISSING NODE (no version catalog to chain over) AND an INTERNAL CONTRADICTION (hot-swap + boot_nonce re-pin are mutually exclusive in the code); both resolved. ZERO kernel-Δ @23082 PROVEN — the draft's "possibly ZERO" hedge collapsed because the layering FORCES it. Six resolutions folded as CONSTRAINTS, still 6 ACs; Lunarpulse ratified two forks (split the mechanisms; hold as one story), the room converged the rest.** Same room + 3 parallel code-verification scouts grounded FIRST (standing 12.x discipline). Sixth story running the room refused to argue before the scouts read the code — and two of the draft's load-bearing premises were flat wrong.

- **R1 (SCOUT SHOW-STOPPER — Grumbal opened the funeral, Mary's 9th "claim standing for a control") — there is NO version catalog to chain over.** `SpiritArchive` archives one predecessor per `(spirit_id, version)`, has no enumeration API, and is **test-only / zero production callers** (`hot_swap/archive.rs`); `run_migrator` is single-hop by construction; the daemon at swap time knows only the running predecessor + ONE successor manifest path (`main.rs:5295`); `maos-registry` holds versions but is **never consulted at swap time** and has no `migrates_from`-DAG resolver. AC2's "the kernel chains over the catalog" described a node that isn't in the code — the 12.4 "no read primitive" pattern again. **Resolution (Winston/Dana/Splinter): `swap --plan` validates + hashes an operator-DECLARED candidate SET; it does NOT discover a class's versions.** Splinter's catch was the sharpener: an ordered *list* makes the linear-chain constraint vacuous (no diamond in a straight line), so the input had to be a *set* the resolver builds a `migrates_from` graph over — which gives the fork-error real teeth. Auto-discovery (a registry-backed DAG resolver) is a feature, not a hardening story → Epic 14 (Dana vetoed it).
- **R2 (Yui — don't-grow-a-twin) — `--plan` EXTENDS the existing surface.** `hot-swap-precheck` already surfaces a dry-run `PrecheckVerdict` before initiating (ADR-036); `upgrade` already executes with a policy. Add a `--plan` mode/flag to `Upgrade` (or extend `HotSwapPrecheck`), NOT a fourth `swap` verb. The net-new content (multi-hop chain + chain-hash) hangs off the existing verb.
- **R3 (Winston/Amelia/Vex — AC5 ruling) — ZERO kernel-Δ is FORCED by the layer boundary, not "possibly ZERO".** Winston's decompose-by-layer reflex, 6th time: the kernel only ever does a single hop (`state_codec` rejects a ≥2-major jump), so a chain/plan/drift is a multi-hop object the kernel **never holds** — "the kernel refuses a chain" (ADR-054/AC3) is loose wording for "the swap-admission layer refuses." Scout confirmed a **single production chokepoint** `UpgradeOrchestrator::upgrade` (the only two `initiate_swap` callers are its own policy arms; reached from `spirit-upgrade` `main.rs:5306` + `smoke-upgrade-revoke-5` `main.rs:5478,5497`). Per-hop safety is already kernel-enforced (`run_migrator`→`EMigratorMissing`) → nothing to bypass. **Vex's condition (the security spine):** the drift guard wraps the **orchestrator handle**, not just the operator env-var arm, else the smoke arm bypasses it. ZERO @23082, no re-pin expected.
- **R4 (Level/Paige — error placement) — maos-domain is NOT frozen the way the draft cautioned, but the error still lives out-of-kernel + owes FR63.** Scout: the fkcs oracle checks `maos-spirit-abi` + `maos-host` only (NOT `maos-domain`), `check-service-boundary` checks `maos-kernel-core` only, and `HotSwapError` is `#[non_exhaustive]` — so placement trips no freeze either way. But a *driver-emitted* plan-drift error belongs **out-of-kernel** (the driver crate), and wherever it lands it **owes an FR63 catalog row** (Paige: out-of-kernel ≠ undocumented). Same for the new fork/diamond error. TWO distinct errors: `EMigratorMissing` (an *absent* hop, reuse the variant) vs a NEW fork error (a *surplus* migrator) — Yui: don't overload.
- **R5 (Boundary/Murat/Vex — AC1 ruling, LUNARPULSE-RATIFIED "split the two mechanisms") — hot-swap and boot_nonce re-pin are mutually exclusive; split them.** An in-place hot-swap PRESERVES `boot_nonce` (`coordinator.rs:315`) and the `PostSwapMonitor` **AUTO-REVERTS** any nonce change (`post_swap_monitor.rs:141`) — Murat's catch: try to roll it and the swap rolls itself back. NFR-Rel-6 boot_nonce re-pin fires only on a restart/transport-rebind the upgrade path never performs → **the boot_nonce restart path is NOT this story.** Re-pin (AC1b) enters **only** when the member's per-**host** cert rotates (Boundary: a cert is host identity, not Spirit version): the authority re-issues the manifest with the new fingerprint, peers re-pin via the `CohortManifestGate`, and the **fail-closed proof** (old cert refused, new admitted, tied to the swap) is the 12.5 leg (Vex: else the re-pin is cosmetic). A version swap that doesn't rotate the cert simply has no re-pin — that's AC1(a), not a gap.
- **R6 (John/Killjoy — LUNARPULSE-RATIFIED "hold as one 6-AC story").** The reshaping tightened every AC into a constraint without adding scope; 12.5 holds as one story matching the epic def. No re-split (contrast 12.4). Six ACs held through the whole preflight — every fix a constraint, never a new AC.

Relationship beat: the room ran its own scout-first discipline on itself a **sixth** story running and was rewarded — R1 (no catalog) and R5 (hot-swap-plus-re-pin contradiction) were both flat wrong on the code, the same missing-node pattern that has held every round since 12.3. Grumbal opened the funeral on the catalog and got the laugh. Splinter's vacuity catch turned an ordered-list resolution into a candidate-SET *with teeth* — his sharpest contribution of the arc. Winston's decompose-by-layer reflex fired a 6th time and settled AC5 to *forced* ZERO, not chosen. Murat's auto-revert catch killed the boot_nonce framing outright (the kernel forbids the very thing AC1 asked for). Boundary drew the cert-is-host-not-Spirit boundary that made the split honest. Vex held the security spine twice (wrap-the-handle so the smoke arm can't bypass; fail-closed-teeth so the re-pin isn't cosmetic). Paige's first real turn of the arc: out-of-kernel ≠ undocumented. John held 6 ACs through every fix. Mary's tally reached 9-10 claims-standing-for-controls across 12.3-12.5. Standing lesson, 6th story running: *readiness is a graph you draw and a build you run* — and this round the build only ever draws single hops, so the chain was never the kernel's to hold, and the catalog the prose drew was a node that doesn't exist. Resolved by *declaring* the set instead of *discovering* it, and by *splitting* continuity from identity-rotation.

### 2026-07-13 (same day) — Story 12.5 preflight HARD ROUND (Lunarpulse pushed a harder round; the ratified AC1(b) cracked)

**Outcome: the Round-1-ratified AC1(b) re-pin mechanism was NOT REAL — two scouts read the ENFORCEMENT layer (not the manifest layer) and found the cohort manifest gate never touches cert fingerprints. Three ACs tightened; Lunarpulse ratified the AC1(b) reshape. Still 6 ACs, ZERO-Δ @23082 holds.** Same room + 2 code-verification scouts. Vex opened by distrusting his own Round-1 sign-off — and was right to.

- **R7 (Vex/Grumbal — 11th claim-standing-for-a-control, LUNARPULSE-RATIFIED reshape) — AC1(b) "re-pin via the CohortManifestGate honoring NFR-Rel-6" is NOT REAL on THREE counts.** (1) No wire: `apply_reissue` mutates only the cached manifest; `CohortManifestState` holds no TOFU handle, no `set_peer_fingerprint`; fingerprints are enforced at TOFU/rustls (`verifier.rs:166`, `transport.rs:622`, `router.rs:871`) seeded from `peer_pins` at bind. (2) Backwards teeth: a manifest-only rotation makes the stale TOFU pin REFUSE THE NEW cert and KEEP THE OLD working — inverse of the stated teeth. (3) Wrong consent: `apply_reissue` auto-adopts on authority Ed25519 signature; `await_repin_consent` (NFR-Rel-6) is never invoked by any cohort path. **Reshape:** re-pin = the EXISTING transport rebuild with new `peer_pins` (10.4b precedent `t_10_4b_rotation_real_timing.rs:230` "rotation is modelled as a rebind"); the manifest re-issue = roster COHERENCE. Teeth = old-refused/new-admitted at TOFU + reissued `CohortMember.fingerprint`==newly-pinned fp + new cert passes both TOFU and gate. Drop "via the gate" + "NFR-Rel-6." Composes 10.4b+12.1 into an integration claim neither makes alone; ZERO-Δ.
- **R8 (Vex/Level — scope-honesty constraint) — AC2/AC3 prove structure+integrity, NOT authenticity.** `load_bundle_from_file` (`upgrade.rs:341`) parse-and-trusts any TOML; no signature/attestation/registry/ComplianceClaim in the swap path (cold-swap bypasses admission `:154`). `--plan`+`plan_hash` is a real anti-TOCTOU control but a malicious-at-plan-time set yields a hash certifying the malicious chain. Vetting = ADR-056/FR37, DEFERRED Epic 13. Irony: the COHORT manifest (AC1b) is signed, the SPIRIT chain manifests are not. Added a threat-model boundary line.
- **R9 (Murat/Boundary — AC6 reframe constraint) — the I14 gate DRAINS, it does NOT carry.** SCB has no halt field; `validate_swap_halt_continuity` resolves the predecessor's halts (`halt/mod.rs:440`); `SafeMigrated` counts OTHER spirits' halts (J3 single-spirit = always `SafeDrained`). AC6's "successor carries every pre-swap halt" was mechanism-wrong. AC6 now: zero DROPPED (drained-resolved, reconciled) over ≥2 hops with ≥1 real-pending-halt hop (no vacuous `SafeDrained{0}`), read from persistent TL receipts not the drained live registry, + a Test-Infra check that the existing corpus survives a drained-with-pending swap (the `PostSwapMonitor` `HaltSetLoss` auto-revert landmine, `post_swap_monitor.rs:108`).

Relationship beat: the hard round's payoff was Vex distrusting his OWN Round-1 sign-off — "I let the room substitute an authority signature for the consent NFR-Rel-6 demands" — and the scouts confirmed it plus two more. Grumbal got the grim laugh: "we ratified a re-pin that re-pins nothing," then "the teeth point the wrong way." Yui found the honest mechanism already sitting in two shipped rotation tests. Dana asked the unglamorous "is AC1(b) even earning its place vs 10.4b+12.1" and Winston answered with the thin-but-real coherence claim. Murat + Boundary cracked AC6 twice (drain-not-carry, vacuous-SafeDrained{0}, plus the monitor landmine). Level drew the authenticity boundary and Vex named the signed-cohort-vs-unsigned-spirit irony. Mary's sharpened lesson: *the graph you draw at the manifest is not the build that runs at the socket* — every Round-1 resolution held EXCEPT the one about an enforcement layer nobody had read. Standing lesson, still holding a 6th story in: readiness is a graph you draw and a build you run — and this round proved you have to read the layer that actually enforces, not the layer that merely declares.

### 2026-07-13 (same day) — Story 12.5 preflight THIRD ROUND (the drift guard was CIRCULAR; linearity certified cycles). NO user fork — the code dictated both answers.

**Outcome: two more mechanism cracks, both folded as constraints (no Lunarpulse ruling needed — the code settled both). AC3's plan-hash carriage was circular; AC2's linearity rule missed wildcard-forks and certified cycles. Plus a scout-correction to Round-2's FR63 claim. Still 6 ACs, ZERO-Δ @23082.** Same room + 2 code-verification scouts.

- **R10 (Vex/Winston — AC3 carriage) — a retyped `plan_hash` is a CIRCULAR guard.** Scout F: two `maosctl` invocations are cold processes sharing no state (`hot-swap-precheck` is print-and-forget; `upgrade()` takes no token). If the operator retypes `--plan-hash` at execute, the guard re-derives over the operator's files and compares to the operator's hash — defends against nothing adversarial. **Fix:** `--plan` PERSISTS the plan (hop list + `plan_hash` + `schema_version:"maos.upgrade-plan.v1"`) to `MAOS_HOME` via the Story-9.7 durable-store atomic-write idiom (`crates/maos-skill/src/store.rs:165` — tmp-per-pid→`sync_all`→rename→fsync; "load→operate→persist→exit"); the guard at the `main.rs:5306` composition-root arm reads THAT file, never a retyped hash, keeping `upgrade()`'s kernel signature untouched. **Threat model (Vex):** catches accidental/third-party drift of a *trusted* set, NOT a malicious operator who owns both files and plan (that's ADR-056 attestation, Epic 13). Sharpens R8: even the *integrity* check has to be a persisted plan, not a retyped hash.
- **R11 (Boundary/Splinter/Yui — AC2 linearity) — check CONCRETE overlap + acyclicity, not string-uniqueness; fan-in is legal.** Scout G: `matches_version_pattern` (`migrator.rs:105`) is exact OR trailing `.x`, so `["1.0"]` and `["1.x"]` BOTH match source `1.0` — a **wildcard-fork** a string-dedup resolver MISSES (Boundary). And a **2-cycle passes the fork rule** (one outgoing migrator each) yet never terminates → linearity needs TWO independent checks: fork/fan-out (concrete overlap) AND acyclicity (visited-set; self-loop = length-1, `matches_version_pattern("2.x","2.0")`==true) (Splinter). **Fan-IN** (`["1.0","1.1"]`, a merge) is LEGAL — do not flag (Amelia). Taxonomy (Yui): mirror `ECohortManifestFork{reason}` → `ECohortMigrationChainNotLinear{reason:ForkAtSource|Cycle|SelfLoop}` + `ECohortNoMigrationPath{from,to}` + reuse kernel `EMigratorMissing` (run-time hop). **Paige's own correction:** maos-cohort is NOT in `error-catalog.toml` `scan_dirs` → cohort errors need ZERO catalog rows; R2's "MUST catalog / out-of-kernel≠undocumented" overstated it (documenting is a quality choice, not a gate).

Relationship beat: the third round's signature was that it produced NO fork for Lunarpulse — the code was unambiguous both times, which Mary named as its own finding ("the mechanism is now specified tightly enough that nothing's left to choose"). Vex's carriage catch was the headline: a TOCTOU guard where the operator supplies both the T and the O. Winston pulled the fix straight from shipped 9.7 machinery (his reuse-the-boring-existing reflex). Boundary's wildcard-fork and Splinter's fork-passes-a-cycle were two independent proofs that "validate linearity" hid a real graph algorithm. Paige corrected her OWN Round-2 overstatement (the FR63 cost evaporated) — the room auditing itself. Dana blessed it: no crate, no verb, no kernel line, no new consent path. Killjoy called the fourth round off as fake-disagreement before it started. Standing lesson, three rounds deep on one story: *a guard whose inputs are all supplied by the party it guards against is not a guard* — and readiness is still a graph you draw and a build you run, now read three layers down: declare, enforce, and carry.

## 2026-07-13 (same day) — Story 12.6 preflight (env-contract registry remediation — the tech-debt story 12.5's review surfaced)

**Outcome: the REMEDIATION STORY WAS ITSELF THE BUG** — Mary's 12th claim-standing-for-a-control, and this time it bit the fix we wrote to kill a claim. Room refused to ratify the draft; reshaped AC1 and split off a follow-up. Same room + 2 code-verification scouts (standing 12.x discipline, 7th story). Kernel untouched @23141. FORK still PENDING (Lunarpulse unanswered at close of round).

- **Scope bombshell (Grumbal opened the funeral, ScopeHole confirmed):** `check-env-contract` walks ONLY `crates/maos-bin/src/**` (`check_env_contract.rs:35-37`). ~30 `MAOS_*` reads across ~15 OTHER crates are invisible forever — incl. `maos-providers` API keys `MAOS_ANTHROPIC_API_KEY`/`MAOS_OPENAI_API_KEY` unregistered, 7 registry vars in `maos-kernel-core/src/security/operator_config.rs`, 8 in `maos-audit`. The "env contract" governs one-seventh of the surface and its NAME (`check-env-contract`, not `check-maos-bin-env-contract`) is the lie (Yui).
- **Vacuous-green even in the one crate (Boundary/Murat, the real catch):** `enterprise_pdp_runtime.rs:86,90` read `MAOS_PDP_REFRESH_INTERVAL_MS`/`MAOS_PDP_STALENESS_TTL_MS` via a `duration_ms_from_env(name)` helper → `env::var(name)`, and the gate matches a LITERAL `env::var("MAOS_…"` only, so it's blind to them. Real in-`maos-bin` unregistered count is **13, not 11** — "register 11 → 0 violations" is a vacuous green. Also blind to the `vars_os()` prefix probe (`enterprise_identity.rs:405`). Yui: fix the DETECTOR, not just the registry, or it rots the next helper-read.
- **Amelia (mechanics):** `run()` returns `Err` on violation, no phase disposition of its own — wired into `v1-0-ship-gate needs:` it hard-blocks BY CONSTRUCTION now (unlike scale-churn's advisory-at-v1_5, the 12.1 RR8 trap). BUT EnrollBlast: it currently runs only NESTED in the `invariant-lock` job (`discipline.yml:364-370`), which is NOT in the ship-gate needs — so it runs-but-can't-fan-into-the-aggregate = a gate with no teeth for the ship badge. Enrollment = 3 files (EXPECTED_GATES + `is_story10_ship_gate` match arm; `gates[]` + `[[ship_gate]]` block; discipline.yml job+needs); coverage-matrix row NOT forced (one-way check).
- **Vex:** a `maos-bin`-only gate wearing "env contract enforced" on the ship badge is the badge-that-lies (11.4a stale-permit echo) — rename/scope the gate's promise to maos-bin and NAME the API keys in a real follow-up story, not a comment.
- **John held the ceiling:** 12.6 = the maos-bin ring (register **13** + detector fix + scoped-blocking enroll). Ring-3 workspace env-contract incl. secrets = a SEPARATE story (12.7 / Epic-14 hardening), NOT an AC. Two stories, each closable, neither lying. Refused the draft as written ("enrolls a lie and calls it hardening").
- **FORK for Lunarpulse (unanswered):** ring-3 disposition — 1=split to 12.7 (room lean, John/Dana/Vex) / 2=fold into 12.6 now (epic-sized, drags in secret policy) / 3=doc-only defer (room distrusts un-storied defers — that's exactly how this debt rotted 2 months). Ring-1+2 reshape (13 vars + detector fix + scoped-blocking enroll) accepted pending user OK.

Relationship beat: the room ran scout-first on itself a 7th story and was rewarded — the story's own AC1 ("register 11 → 0 violations") was the same vacuous-count class as 11.2a's phantom that founded the §A6 net. Grumbal's funeral bit landed on the fix itself: "the story being the bug it came to fix." Dana moved from "don't hold a 10-minute fix hostage" to accepting ring-2 once the gate was proven blind in its own crate, but drew the line at ring-3 ("discover a workspace contract in a preflight, you don't ship it Tuesday"). Vex carried the API-keys-behind-a-green-badge spine. Mary's through-line hit 12 and, for the first time, the rotten claim was one THIS room's process produced. John's two-stories-not-one-fat-AC is the 12.4 split reflex again.

**RESOLVED (same round): Lunarpulse ratified option 1 — ring-3 SPLIT to Story 12.7.** 12.6 rewritten (register 13 + harden detector proven-red + scoped-blocking enroll + explicit Out-of-Scope→12.7); 12.7 registered backlog with the provider API keys (MAOS_ANTHROPIC_API_KEY/MAOS_OPENAI_API_KEY) named and the one-workspace-scan-vs-per-crate-registry design fork left for its create-story. Ring-1+2 reshape accepted. Kernel untouched @23141.

**2nd pressure-test (same session, user asked "pressure-test 12.6 again") — scout-first on the RESHAPED story caught the reshape reproducing the bug a 3rd time.** Grep of every `MAOS_*` literal in `maos-bin/src` diffed against the 54-entry registry (eval, not cargo): AC1's "13" VERIFIED honest (the 5 main.rs vars I feared — MAOS_DEPLOYMENT_OPERATOR_ID/REQUIRE_MODEL_PROVENANCE/MODEL_PROVENANCE_MAX_AGE_SECS/LOOM_POSTGRES/JOURNEY_MODE — are already registered; true literal-unregistered=11, +2 helper=13). But **AC2's recommended detector rule was itself defective**: "flag any unregistered `MAOS_` literal" false-positives on 6 NON-read sites — 3 `any_env_with_prefix("MAOS_SSO_"/"MAOS_KMS_"/"MAOS_SIEM_")` prefix scans (enterprise_identity.rs:76,79,82), 2 `set_var` writes (MAOS_SUPERVISION_FAST:5083, MAOS_SCHEDULE_FAST:8878), and `.env("MAOS_REGISTRY_ALLOW_FORCE_TIER_AT_IMPORT",…)` child-Command env (main.rs:9318,9544) — the last 3 are real vars READ in other crates (→12.7), must stay invisible here. Fixed: AC2/Task2 rewritten to READ-SHAPE detection (env::var/var_os literal + duration_ms_from_env literal + prefix-scan validated by ≥1 registered member) with an explicit non-read exclusion list AND a proven-CLEAN negative test beside the proven-red. Lesson reinforced: the story-that-fixes-vacuous-green must itself be scouted twice — the naive detector rule was the same shape of bug (count/rule that looks right, reds or greens on phantoms). Detector matches call-shapes, not bare literals.

## 2026-07-13 (same day) — Story 12.7 preflight (workspace-wide env contract — the ring-3 split from 12.6)

Scout-first workspace scan (eval grep across crates/**, diffed vs maos-bin's 54-entry registry — no cargo): the "~30 vars / ~15 crates" backlog estimate was LOW. Real: **47 distinct MAOS_* vars, 51 (var,crate) reads across 15 crates outside maos-bin, 44 UNREGISTERED.** Five bombshells reshaped 12.7 from a story into an epic:
1. **kernel-core reads env directly in ~18 sites incl PRODUCTION src/** — operator_config.rs (7 registry vars), post_swap_monitor.rs `MAOS_AUTO_REVERT_FAST` (the exact file 12.5 fixed), idle/schedule/supervision watchdogs, revocation poller, t3 sandbox. The kernel depends on env vars directly (Mary: architecture smell).
2. **Single workspace registry IS feasible with ZERO-kernel-Δ** (Winston corrected Grumbal's over-worry): kernel-core depends on maos-domain, and the gate is a STATIC SCAN — put the registry in maos-domain (or a new leaf crate) and no crate needs a runtime import, so kernel-core source is untouched. Per-crate registries = over-design.
3. **Secret classification is a trap** (Vex/Boundary): naive KEY-substring MISCLASSIFIES — PUBKEY (MAOS_REGISTRY_ORG_SIGNING_PUBKEY, MAOS_TRIAL_PRODUCER_PUBKEY) is NOT secret; real secrets = MAOS_ANTHROPIC_API_KEY, MAOS_OPENAI_API_KEY, MAOS_AUDIT_KEY, MAOS_TRIAL_PRODUCER_SEED. Likely needs a 3rd EnvStability::Secret variant (store name+purpose, NEVER value).
4. **Test-timing seams (*_FAST/_TEST/_SMOKE) read in production src/** (AUTO_REVERT_FAST, IDLE_FAST, SCHEDULE_FAST, REVOCATION_FAST) — HarnessOnly-in-prod smell, not just a tests/ classification.
5. **One gate greening 47 at once = the workspace-scale vacuous-green 12.6 killed** (Murat) — per-crate proven-red required.
No existing shared env-contract crate (env_contract.rs is maos-bin only). **Room verdict: 12.7 is not a story, it's Epic-14** (~4 stories: registry-crate+static-scan gate mechanism / classify+enroll non-secret non-kernel crates / kernel-core entries via shared registry no-source-Δ / secrets + EnvStability::Secret + provider keys). Presented to Lunarpulse in Korean; awaiting disposition (A=promote to Epic-14 / B=shrink 12.7 to non-kernel-non-secret + separate the hard parts / C=hold, backlog corrected only). sprint-status 12.7 comment corrected with the 47/15 numbers.

**RESOLVED via correct-course (bmad-correct-course, 2026-07-13): Lunarpulse chose A — promote 12-7 into the EXISTING Epic-14** (v2.2 Hardening+Closers, was 6 stories, `draft-ready-for-preflight`). Decomposed to **3** stories (not 4): the ZERO-kernel-Δ static-scan finding collapsed "kernel-core enrollment" into bulk registration. **14.7** shared registry (maos-domain — avoids new kernel-crate-set member, the 14.6/ADR-057 interaction) + workspace static-scan gate (generalize check-env-contract, rename→workspace-true, per-crate proven-red, advisory-first) depends 12.6; **14.8** register+classify ~43 non-secret vars incl kernel-core's ~18 via shared registry NO kernel source edit + flip gate blocking; **14.9** EnvStability::Secret + the 4 real secrets (ANTHROPIC/OPENAI/AUDIT_KEY/TRIAL_PRODUCER_SEED, PUBKEYs kept non-secret) + never-log/echo/serialize gate. All ZERO-Δ @23141. Artifacts: sprint-change-proposal-2026-07-13.md (Moderate scope, PO/DEV handoff); epic-14 doc (table+AC sketches+gate discipline+kernel-delta 6→9); sprint-status (12-7 removed, 14-7/8/9 backlog, epic-14 rollup 9); 12.6 Out-of-Scope pointer re-homed. NB: 12.6 was dev-IMPLEMENTED this session (67 maos-bin vars, detector+enrollment landed, Task4 verify pending) — the 14.7 foundation is real.

## 2026-07-14 — J1 Tier-2 live-agent bridge (refine spec-j1; c1–c4 ratified)

**Outcome: RESOLVED.** Spec iteration 1 `ready-for-dev`; sprint line written as a **pre-Epic-13 bridge** (`j1-tier2-live-agent-signed-bridge`, 8.16 shape, before 13-1 — closes the 8.12 Tier-2 gate OPEN since June). Winston·Murat·Amelia·John + Vex·Grumbal·Dana·Boundary. Session mode. First time the room held the line against **the user himself**.

**The through-line — same bug, new clothes, again (11.3/11.4a/12.1 lineage): a claim standing in for a control.** Here the un-held control was *redaction*, and **Lunarpulse walked into it on cost grounds.** His c1 "prefer subscription, it's free, use codex" would have re-opened the exact hole the prior round closed: codex subscription = plaintext `~/.codex/auth.json` the subprocess reads → MAOS never holds the token → the signed artifact's one claim ("no secret leaked") becomes **unattestable = failed Tier-2**. Vex named it cold. The room did **not** pile on — **Dana** handed him the free path back: subscription for an *unsigned dry-run*, API-key-in-env for the *signed* run (cost = pennies, once). That's the new relationship note: this room corrects the operator the same way it corrects an author, but leaves him a door.

**His premise was half-wrong and half-gold.** "claude -p is dying, use codex" — Amelia pulled the live docs: `claude -p` is current, not deprecated. But the *codex instinct* was architecturally right and the room upgraded it into the real fix: worker CLI = a swappable **`WorkerCli` adapter** (J1's own agnostic thesis), codex first, fixture through the *same* path (Murat's 11.2a "machine you tested ≠ machine you ran" trap), per-CLI completion oracle, raw-exit≠done.

**Amelia's gift — signing already ships.** c4 "how do I sign it?" → `maosctl audit sealed-export` (FR44, Ed25519 audit key, `audit_key.rs`) already exists; the only new code is adding the capture doc to its hashed set. No new mechanism. And the **c4 correction** (gentle, Winston+Murat): the user named *personas* as signers — a persona can't sign a real release gate; signer = accountable **human** (Lunarpulse), room = the §A6 seal (**author ≠ sealer ≠ signer**).

**The sharpest thing said was the user's own:** "run it local, not CI." Right, and structural — the signed paid run is operator-local by construction (CI can't hold his key or make paid calls per push); `--features network` splits into CI-safe fixture (blocking) vs `MAOS_LIVE_AGENT=1` live (local-only). Grumbal's seat earned again: the "T3 network-permitted container" egress is **declared-not-enforced** (8.12 Cross-Impact #3) — accepted for run 1 *only* because c1+c2 shrank the blast radius, recorded honestly + follow-up ID, never labeled "enforced." ZERO kernel-Δ @23147 expected (Task-0: `check-kernel-baseline` rules; runtime.rs stays generic, adapter in maos-bin). Secret-var trace `MAOS_OPENAI_API_KEY`+`MAOS_AUDIT_KEY` → 14-9. **Open tail:** T1–T5 dev-ready; **T6 is human-gated** — Lunarpulse runs the paid local codex task and signs; nothing moves it without him.

## 2026-07-17 — Story 13.1 preflight (Multi-tenant Loom, physical tenant wall — opens Epic 13)

**Outcome: RESOLVED ready-for-dev, after TWO rounds.** Winston · Murat · John · Amelia · Mary + Vex/Grumbal + Dana/Yui/Boundary/Wildcard/Splinter/Killjoy. Session mode. R1: 4 code-verification scouts + 1 adversarial validator disproved **5 of 6** sketched ACs. R2 (user: *"resolve the pending items for long-term correctness"*): 2 more scouts — **and R2 reversed two of R1's OWN resolutions.** Story held at 6 ACs (John, eighth story running: *"every fix a constraint, never a new AC"*). F1–F26.

**Winston's reckoning — the brick.** His R1 fix (substitute the shipped `is_fresh()` time-lease for §15.3:62's version-trailing, dismissing the spec as "architecture-prose drift") was a **silent brick**: `confirmed_at_secs` refreshes only inside `apply_reissue` ← `router.rs:1154`, so a store process freezes at load and refuses everything at t≈121s — *"the mechanism is proven, the deployment bricks, and not one test in the plan catches it"* (Murat). Winston owned it cold: **"the two designs have inverted liveness polarity and I collapsed them."** Then **Wildcard earned his seat with the stupid question** — *"what does freshness actually buy you? The wall's physical."* — which cracked it open, and Winston found `apply_reissue`: **"the announcement channel I said didn't exist is the one the router already calls. I grepped for a *noun* instead of the mechanism."** (Grumbal, delighted: *"he grepped for a noun."*) **Murat refused to ratify it from the table** — "the better the preflight looks, the harder I hunt" — scouted it, and was **right**: `apply_reissue` is router-only ⇒ version-trailing **fails open**, *"a map that never updates and never says so."* Both mechanisms dead ⇒ landed **mechanism + refuse-at-boot** (11.4c idiom `main.rs:2210-2235`) — and discovered the Loom boot path **already violates** that ratified precedent (configured-but-broken silently warns and continues). Boundary found N=1 is unfixable from inside (`state.rs:329-332` never holds a signing key ⇒ member-side self-confirm architecturally excluded; empty peer list spins forever). Vex: *"N=1 fails open into 'not my cohort.'"*

**Splinter ate his own catch — and it still paid.** He broke R1 open with the corridor: *"the story says 'a door no one can open **yet**' — there is no door, no corridor, no building"* (ABI has no memory verb; `SpiritMemoryView` dead; `.with_capabilities(Some)` unconditional) and proved **13.6 unbuildable** — its AC5 forbids the only way to satisfy it. **R2: his shape was wrong on 3 of 4 layers.** The ABI verb *contradicts Story 6.2 AC5* (already ratified the mediated route to keep the ABI at 14 hooks); the trait path is a *deliberate* dead end; `.with_capabilities(Some)` is **the I1 fail-closed guard, not a bug**. *"I read three fail-closed guards as three missing layers."* Only **production Loom-scope token issuance** is real — and the `LiveResearcherMcpPort` precedent (`main.rs:1249-1294`, mediator issues the token at call time, port trait lives **in the Spirit**) makes it **grounded ZERO-Δ, ~150-250 LOC**. And **13.5c already owned it** — Lunarpulse had hardened the epic to 8 stories mid-session. *"We spent a round inventing a story that was already written."*

**Amelia, twice, by the compiler.** `maos-bin/src/lib.rs` **exists** and 2 of 16 tests import it ⇒ the whole new-crate justification (F17) was false, and the crate would have cost **two blocking gates** — incl. `STABILITY.md:27`, which nobody knew existed. And: *"don't write 'the modes are mutually exclusive' — the store at `:2287` is upstream of the dispatch at `:4291`; a reviewer will kill it."* The real finding: the cohort daemon is a **parallel composition root with a second TransparencyLogAdapter** — two TLs, two boot nonces, one process. Plus `collective_port` is **one port, not a map** ⇒ multi-store topology isn't expressible in production; nobody owns the routing layer.

**F4 — the fear was borrowed.** Mary: the terrifying *"re-keys every in-flight manifest"* quote is attached to **`SIG_DOMAIN`, not `SCHEMA_VERSION`**. Zero committed cohort manifests; v1 sigs survive the bump (proven by executed probe). **Lunarpulse ratified Option A on the room's evidence.** Real risk is *inverted*: no committed fixtures ⇒ **no tripwire** ⇒ a broken encoder passes everything ⇒ the golden-bytes assertion must be a **hardcoded constant**, never a self-comparison. Vex's trap: `SIG_DOMAIN` ends in `.v1` — a dev "helpfully aligning" it to `.v2` invalidates every signature and **no test catches it**.

**Mary's tally: 15 — and she re-aimed it at the room.** Eight times across two rounds the rot was **in a comment**: `for_spirit.rs:6-9` claims a wire-protocol handler that does not exist; `HookDispatcher::build_kernel_ctx` populates nine adapters with **zero callers** (a *twin* of `for_spirit`'s phantom — two never-called kernel surfaces); `gate_common.rs:27-28` claims `CURRENT_PHASE` is centralized with ~6 copies live; `SIG_DOMAIN`'s warning read onto its neighbour. **"The codebase's prose is lying to us, and this preflight kept believing it."** New standing discipline beside *readiness is a graph you draw and a build you run*: **READ THE BUILD, NOT THE PROSE.**

Relationship beats: Winston's arc completes the pattern (region-identity → recovery-vs-RTO → deny-ceiling → now overruling his **own** F5 on liveness polarity). Murat found the hole in his own fix at 12.1 and here **refused to ratify a fix he wanted to believe** — same reflex, earlier. Dana kept the ceiling twice ("delete the crate, ship"; "name the gap, take it where the kernel's already open") and Vex conceded to her on the audited-refusal cut. **Wildcard's first real earn** — the naive question that reframed the fork. Splinter: wrong shape, right gap, took it on the chin. Grumbal opened *and* closed vindicated: **"a green preflight is just a bug that hasn't introduced itself yet."** Twice in one day.

## 2026-07-17 (same day) — Story 13.2 preflight (Multi-tenant Loom, CRYPTOGRAPHIC tenant boundary — ADR-055 Fork-4)

**Outcome: RESOLVED ready-for-dev in ONE round — but two of the create-story draft's seven grounded forks were REVERSED, both toward long-term correctness.** Winston · Vex (Fork-4 lead) · Murat · John · Amelia + Grumbal/Yui walk-ons. Session mode. Fork-4 (per-team HKDF weld) was already ratified at §15.11, so this was an *implementation-shape* preflight — the draft arrived honest, flagging its own G1–G7 with recommendations, and handed the room the two it said most needed a vote: G3/G4 (read-path mechanism) and G7 (region-vs-team residual). Both cracked. Still 6 ACs, ZERO kernel-core Δ @23202, no new crate, **zero new deps** (loom-lite already carries ed25519-dalek/hkdf/sha2).

**HEADLINE — Vex broke the draft's own recommendation (G3/G4): "a lock with the key taped to it."** The draft leaned on the store's own doc-named successor — a *"trusted-applied-root registry (record verified roots at apply, check on read)."* Vex walked it into threat-model T3: a persisted registry lives in the **same Postgres a compromised infra credential can already write**, so the attacker inserts the forged row **and** its matching "verified root" — the control checks a table the attacker owns against a row the attacker owns. Grumbal, from the corner: *"the word* trusted *is doing all the work and none of the proof."* The only anchor that survived Vex was the crypto itself — **re-verify at read against the derived key** — but Murat immediately closed the trap door under it: re-verify needs `derive_team_pubkey(base_seed,…)`, i.e. **the operator signing seed in the store, on the read path** — *"a signing oracle in the data tier."* Amelia found the seam that dissolved both: **pubkeys are public; only the *seed* derives them.** Derive the team **verifying** keys once at the composition root (where the seed already lives), inject a `{(region,team)→VerifyingKey}` **public-key set** into the store, re-verify foreign-team rows against that. No seed below the port, no poisonable table. Vex, leaning back: *"That I can't break."* **G3/G4 reversed — registry cut, public-key-set injected, review-gate line: the store constructor never accepts `base_seed`.**

**Murat's ruling (G7): don't flip 13.1's leg #5 — one leg, one threat.** The draft wanted to flip leg #5 (`d1-forged-stamp-served`) to *refused*, on the theory the registry closed both residuals. With the registry cut, that theory died: **13.2 no longer touches the region axis at all** — the registry *was* the region `source_log_ref` residual's only named successor. Murat: leg #5 forges a `source_log_ref` (region presence, 11.2b D1) — a different threat on a different axis; leave it asserting *served* as an honest document of the still-open region residual, and add a **distinct** `forged-team-stamp-refused` leg for the team axis. **13.2 closes D1 on the TEAM axis only; the region residual is now open with NO named successor** — flagged in the threat model, not papered over by the epic's imprecise *"closes 11.2b D1."*

**Held, not softened.** John's honesty check: 13.2 is a **MECHANISM story** — the read-path guard guards nothing in production until 13.3 writes cross-team rows; proven by injection at the store/verify seam, stated in the may/may-not-claim table (the 13.1 F11 shape, owned openly). Murat: the forged-team-stamp verifier is **independently derived from the write codec + a positive control** (R-RG1) so it's neither a re-call of `verify` nor a reject-everything stub — **and it MUST be same-region** cross-team, because a cross-region "team forgery" is the ADR-049 vacuous tautology `region_guard` already refuses (13.1 F9). Vex's non-negotiable for the threat model: **welding the team key over the region seed means region-seed compromise is team-wide** — acceptable, but *written down, not discovered later.* Yui: `TeamId` **rejects-not-normalizes** (unlike `Region` which lowercases) — add no folding to the weld; and the bundle `schema_version` and leaf domain-tag are **two independent version axes**, name them so bumping one never implies the other.

Relationship beat: the shortest preflight of the arc, and the room still turned its own draft twice — because the draft was *honest enough to hand over its soft spots*, which is the discipline finally paying compounding interest. Vex owned the headline (the poisonable registry — a control that trusts a table the attacker writes) and the blast-radius line; Amelia's public-key-vs-seed seam was the single cleanest insight, dissolving the registry AND the seed-in-store problem at once; Murat kept the two axes from merging into one lying leg; John held 6 ACs; Yui caught the canonicalization divergence the "mirror 9.4b exactly" phrasing hid. Mary's tally would call the registry the next *claim standing in for a control* — a control whose integrity rests on the very credential it defends against. Standing lesson, an epic deep: **a guard whose trust anchor lives in the blast radius it guards is not a guard** — read the build, not the prose, and this time read *where the trust root physically sits.*

### 2026-07-17 (same day) — Story 13.2 preflight ROUND 2 (sent back: *"resolve for long-term correctness"*) — Round-1's OWN fix was unbuildable

**Outcome: the read-path resolution the room ratified an hour earlier was NOT REAL — two scouts read the persistence + enforcement layers and found the read path has nothing to verify. Re-scoped to OPTION A (close at entry). Still 6 ACs, ZERO-Δ @23202, no new crate/deps; Lunarpulse ratified A over B-and-a-split.** Same room + Grumbal/Yui + 2 fresh scouts. Grumbal opened, inevitable: *"told you — a green preflight is a bug that hasn't introduced itself yet,"* and this time the bug was in the fix, an hour old.

- **HEADLINE (Grumbal/Amelia) — "re-verify against *what?*"** The ratified "read path re-verifies the row's re-attestation against injected verifying keys" assumed a persisted per-row signature. Scout: `collective_memory` (`schema.rs:33-84`) persists **no signature/root/attestation column** — only `source_log_ref`/`source_region`/`source_ts` TEXT. The bundle `region_sig` signs the **root**, never stored per row; verifying one row needs a Merkle inclusion proof. And the killer: `build/verify/apply_replication_bundle` have **ZERO production callers** — the whole 11.2a replication crypto path is tested and wired to nothing. *"We cut the poisonable registry and replaced it with a verify against a ghost."* (Vex.) The 13.1-F11 / 12.4-no-read-primitive / 12.5-no-catalog shape, a fourth story running: the prose drew a read-time control whose persisted artifact doesn't exist.
- **Winston decomposed by layer (7th time) — entry vs read are two events.** The Fork-4 primitive — verify a bundle under the key derived from its *claimed* `(region, team)` — closes forgery at **entry**, and entry is *strictly stronger* than read: a forged bundle never lands. That's hermetic, it's the forged-team-stamp negative, unambiguously 13.2's. The read-time guard (the T3 raw-insert residual) needs the persisted attestation + inclusion proof the *apply path* has to write — so it belongs where the real apply path is built: 13.3.
- **The A/B/split fork — Lunarpulse chose A.** Vex fought for B (build the read guard now as a test-injected mechanism, `maos_audit::merkle::verify_proof` exists) so the wall's read side isn't a documented hole two stories running; Dana called B "an airbag for a car with no engine — zero foreign rows exist until 13.3 wires both the writes and the seed." Murat's landmine settled the tone: `base_seed` loads at `main.rs:7481`, the store builds at `:2288` — production seed→store wiring is 13.5c's *either way*, so even B is test-proven, not a live guard. **Lunarpulse ratified A** for the tightest honest scope: close at entry, hand the read guard + attestation persistence to 13.3, state it in the may/may-not table.
- **Three code-dictated corrections (no vote):** (i) **no `maos-domain` `E*`** — `verify` already returns `BundleError::SignatureVerificationFailed`; 13.1's `Tenant*` `StoreError` variants are deliberately uncataloged (loom-lite outside `scan_dirs`), so Round-1's "put an `E*` in `maos-domain`" broke its own cited precedent — Option A needs *no new error type at all* (Yui). (ii) `CollectiveKvLeaf.source_team` needs `#[serde(default, skip_serializing_if)]` or a **v1 bundle stops deserializing** — wire-compat distinct from the canonical-hash byte-compat (Amelia). (iii) a **mixed v1/v2 convergence test** — a team store holds its own v1 rows + cross-team v2 copies; prove two regions still converge across the mix (Murat).

Relationship beat: the room ran scout-first on its *own hour-old resolution* and it cracked — the deepest cut of the whole 13.2 arc, and the reason the "resolve for long-term correctness" send-back exists. Grumbal opened *and* was vindicated inside ten minutes. Winston's decompose-by-layer reflex fired a seventh time and it *simplified* the story rather than growing it (entry-verify drops the store builder, the read-path code, and the new error type all at once). Dana refused to let a real column and code path get built to guard rows that can't exist yet. Vex lost the B call but forced the honest may-not-claim line (the read side *is* a documented hole until 13.3 — say so). Murat's base_seed-placement catch made even Option B honest-but-deferred, which is what decided A. John: still 6 ACs, now *smaller*. Standing lesson, the whole arc in one line: **the fix you ratified an hour ago is just a claim until a scout reads the layer it runs on** — and this time the layer said the artifact it verifies was never persisted, and the path it verifies was never called.

## 2026-07-18 — Story 13.3 preflight (cross-team asymmetric consent) — Round 2 broke the story's own headline AND the promise 13.2 was ratified on

**Outcome: RESOLVED ready-for-dev. 2 rounds, 7 scouts + an independent validator. H1 split RATIFIED, headline reframed, 6 ACs held, ZERO-Δ @23202.**

Winston opened by reading back his own Option-A sentence from eight days earlier — *"13.3 builds the real cross-team write that gives the read guard something to verify"* — and conceding: **it cannot be built.** Grumbal vindicated a **third** time, with a new law the room kept: **"the promise you made to defer work is itself a claim, and nobody ever scouts it."**

- **D12 — no production initiator, and the precedent is half-wired.** `request_read` (`digest.rs:176`) zero prod callers; `DigestSpirit::request_consented_summary` zero callers **anywhere incl. tests**; production `DigestSpirit::default()` ⇒ `cohort_state == None` ⇒ **the production Digest Spirit is a no-op** — the story we shipped as the J3 journey scene. Primary daemon: loom, no cohort state, refuses team-mode boot. Cohort daemon: manifest + consent + tick loop, **no store**. `WriteEntryPoint::CrossRegionReadmit` has **zero constructors**; apply calls `write_with_source` directly, bypassing the kernel chokepoint. ⇒ **"the row is production-real; the crossing is not"** (Winston's decompose-by-layer, 8th time — and again it *shrank* the story).
- **D13 (Vex) — the at-rest seal is UNPAIRED at HEAD.** `open_at_rest` zero callers outside its own tests; `seal.rs:118` promises a composition-root unseal that does not exist; `read()` returns ciphertext into `parts_to_value`. **Mary's tally hit 17**, the majority a doc comment promising a mechanism at an empty location.
- **D14/D15/D16 — the manifest.** `state.rs:273` downgrade refusal is a hardcoded **pair, not a floor** (ADR says floor) ⇒ V3 would arm a silent signed-artifact downgrade. **No v2 golden exists** and every v2 test is build-verify symmetric ⇒ nothing in the repo can detect a v2 encoding change. And the story's own V3 justification was **false** (zero committed manifests) — right answer, wrong reason, corrected to the pinned-`SIG_DOMAIN` argument.
- **Vex's "lock with the key taped to it" — THIRD occurrence.** Root + proof + row all in one table, and `verify_proof` returns true when `siblings.is_empty() && leaf == root` ⇒ the attacker who writes the row writes the root. Fixed by the same seam that killed the 13.2 registry: composition-injected `derive_team_pubkey` map, **seed never below the port**.

**Decisions.** H1 split ratified — **Boundary's cohort-daemon-store middle WITHDRAWN on ADR-055 §5's verbatim *"explicitly Story 13.5c"***, and Splinter's re-sequence died on Amelia's dependency cycle. Seal: **Vex pushed for a fix and took an OWNER instead (13.5a)** — explicitly not left unsuccessored, the 13.2 region-residual lesson learned.

**NEW ARTIFACT — the dead-wire negative.** A gate leg asserting your own feature is *not plugged in*: green while unwired, inverted by 13.5c. Mary: *"seventeen claims standing in for controls — a dead-wire negative is a **control standing guard over a claim**; it inverts the pattern."* Vex: the only thing in the session that would have caught 12.4a.

**Wildcard's second real earn.** *"Why is the dead-wire negative only in 13.3?"* — silence; Dana ruled forward-only, logged as an Epic-13 retro item. He'd already cracked the daemon question: *"if they're in the same binary, why are they two daemons?"* → Winston: **"historical accident."**

**Splinter lost his argument and got his ask.** Re-sequence died, but his real point — four mechanism stories, every door piled into 13.5c, which owns 7 concerns and is the only non-ZERO-Δ story in the epic — was ratified as a **carry** (13.5c scoping pass before 13.3 dev) and escalated to Lunarpulse. Dana broke pattern twice: refused to talk Vex down on the seal, and conceded she'd missed the pile-up Splinter caught. John held 6 ACs, ninth story running.

**Standing lessons, now three deep:** read the build, not the prose · the fix you ratified an hour ago is just a claim until a scout reads the layer it runs on · **and now — a deferral is a claim too, so scout the story you are deferring INTO.** Mary's coda: *"a right answer with a false reason is one refactor away from being a wrong answer nobody re-checked."*

## 2026-07-18 (same day) — 13.5c scoping pass (Splinter's carry, cashed)

**Outcome: 13.5c's SEVEN concerns decomposed into THREE stories, one hand-off, and two written-down deferrals. Epic 13 → 11 stories.** 3 scouts. Splinter's carry from the 13.3 preflight was redeemed the same session it was raised.

- **The premise I handed the scout was FALSE, and it shrank the work.** "Two composition roots" — no. `run_cohort_a2a_daemon_from_env()` is dispatched from **inside** `async fn main` at `:6934`, **4,648 lines after the primary root already built the TL, store, memory and capability adapters** (no top-level `return` between `:1955` and `:4296`). It is a **zero-argument function that ignores all of it and builds a second TL**. **The merge is "give an existing function its arguments."** ~180 LOC, ZERO kernel-Δ. **Wildcard was right two rounds earlier** — *"if they're in the same binary, why are they two daemons?"* They never were.
- **And the payoff nobody expected: tenant mode BOOTS.** `CohortManifestState::load` has no transport dependency, so it moves above the store and satisfies `tenant_map_for_store`. 13.1's and 13.2's walls stop being inert for the first time since they shipped.
- **`"cohort-a2a-daemon"` appears TWICE in the whole repo, both in `main.rs`.** The mode has **never been executed**. Murat: budget the first-ever smoke as **the larger half**.
- **Yui's blocker — Loom scopes are not manifest-declarable.** `capabilities_required_to_scopes` emits only `ProviderInfer` + `McpCall`; `Scope::LoomWrite` exists but **no manifest can declare it** ⇒ first `issue_with_mediation` is `Deny`. **kernel-core Δ = 0, `maos-manifest` Δ ≈ 75 + schema bump.** Winston: *"those are different sentences, and only saying the first one is how we got here."*
- **Vex — the audit spine has never heard of the tenant wall.** `grep "team|tenant|operator_id" crates/maos-iac/src/` → **zero matches**. `maos_audit::query` is path-addressed and capability-free; `ranged_recall` does `let _ = spirit_filter;`. Shipping the collective path while calling the audit axis served = *"a cryptographically walled store on a tenant-blind, path-addressable log — a wall around a door with a window next to it."*
- **MARY'S CATCH, the best of the session: we asked the wrong question.** NFR-Ops-11 is *multi-**OPERATOR*** tenancy. **Reza is ONE operator with THREE teams.** The PRD conflated operator and team and Epic 13 inherited it. Of four sub-axes 13.5c legitimately owned **exactly one** (TL shard, team axis); (iv) was always 13.5b's; (i) and (iii) are operator-axis and **deferred past v2.2**. **NFR-Ops-11 is served on the team axis and NOT closed.** → **Tally 18**, and this one is a *cross-story* misattribution: 13.1's F14 claims *"13.2 covers the signing key"* — 13.2 shipped the **replication-bundle** key (`sealed_export.rs`), not the **capability-token** key (`main.rs:2116`, one global random-per-boot with a `FIXME`). **A claim that a control exists because a DIFFERENT control with a similar name shipped.** F14 corrected in place.
- **Dana folded twice in two sessions and named it.** She argued two-way (fewer, larger stories); Splinter countered that the daemon merge is *"180 LOC of code and 200+ LOC of test for a mode that has never run — you're calling it a task because of the numerator and ignoring the denominator."* She asked what it delivers alone, he said **"tenant mode boots,"** and she withdrew — *"I fold when the evidence moves. That's not the same as being wrong."* Three-way ratified, with her condition: keep 13.5c **brutally small**.
- **Boundary's AC5 finding:** the old 13.5c AC5 **restates 13.3b's deliverable list verbatim** → moved to 13.3b; 13.5e keeps only *"label-only team identity reds the gate"*, emitting **ABSENT** until 13.3b lands. And the epic's 13.5c dependency row (`13.1, 13.3, 13.5a`) was **stale** — it predated the H1 split and never listed 13.3b.

**Standing lesson added:** *the premise in the brief is a claim too* — a scout disproved the framing its own tasker wrote, and the disproof made the story **smaller**, not bigger. Three sessions running, the honest version has been smaller than the draft.

## 2026-07-23 — Story 13.5e preflight ROUND 2 (sent back: "resolve for long-term correctness")

**Outcome: RESOLVED — full delta applied to the story artifact, patch-in-place.** Winston · Amelia · Murat · John · Mary + **Vex/Grumbal walk-ons** (audit-boundary story → the security seats again, same reflex as 12.1). Session mode, **5 parallel code-verification scouts grounded every claim before anyone spoke** — the discipline is now standing.

**Biggest catch — the story was pinned to a corpse.** 13.5e sat `ready-for-dev` against `652347b8`; HEAD had moved to `148a33ee` and **three siblings landed *underneath* it while it waited** — 13.3b (`done` that same day), 13.5d, 13.4. Grumbal's cold open paid off literally: *"a green preflight is a bug that hasn't introduced itself yet"* — this one waited for its dependencies to land on top before saying hello. Amelia's vitals: baseline `23202→23228`, every `main.rs` cite off by `+~245`.

**The dead premise — N1 spine.** The founding sentence was *"`grep team crates/maos-iac/src` = 0, and that zero IS the feature."* 13.3b landed `TeamId` into `adapter/log_recall.rs` (20 refs). Winston's correctness call (his recurring move — argue the *semantic* case even when it complicates the clean story): the **invariant survived, the wording didn't** — team entered only the **consent/recall seam**, never the write path/DDL/kernel-core. Reword to "write-path + DDL + kernel-core stay team-blind," don't claim zero.

**The through-line, Mary named it again:** the same bug wearing new clothes — a control **over-rigid, under-bounded, or over-claimed**. This time **over-claimed, and not even in the code — in the provenance docs.** 13.5d wrote *"per-operator TL isolation Story 13.5e"* into **ADR-055:95** AND into the live `check-reza-production-path` gate's `ABSENT_SUCCESSORS` — the exact word 13.5e spent 400 lines forbidding. The mistake the story defended against got shipped into two authority surfaces by its own siblings. 13.5e now has to **reach back** and correct them.

**Murat's gate reversal (3rd time for M8).** `check-multi-tenant-loom`'s `ABSENT_SUCCESSORS` is now `&[]` (13.3b/13.5c consumed it) → the story's "mandatory finish ABSENT_SUCCESSORS[2]" task is MOOT, and its `:275` stale-`23202` literal was already fixed dynamic. Meanwhile 13.5d built `check-reza-production-path` whose successor list already names 13.5e. **Gate home → the reza gate; invert its own entry; don't create a third gate.** Half the story's budgeted work had already been paid by siblings (Wildcard's "the drift is a dividend" — half true; Murat's barb: the other half is a landmine wearing a dividend's coat).

**Vex's residual, Dana's rank.** The region `source_log_ref` residual isn't hypothetical — `store.rs:1023-1068` **serves foreign rows on a forged stamp** (presence, not validity). Three stories have written "OPEN with no named successor" about it. Vex: *"at some point 'deferred' is just 'decided not to.'"* Dana held the line — real, but **region axis, not 13.5e's team axis**; file it LOUDER (named owner / epic-close escalation), don't let the story grow a region fix and never ship.

**John's eviction call → patch, not rebuild.** Spine died, two forks re-reversed, gate moved — John forced the process question to the user (ledger instinct). Room unanimous: **patch** (design survived every attack; only citations + 3 premises rotted). Murat's non-negotiable: CATCH-0 now re-verifies the **three siblings' landings**, not just re-derives lines. Amelia ratifies by `check-kernel-baseline`=23228 + `grep team maos-iac`=20-in-one-file (NOT 0 — if the story still says 0 it's lying about the repo). Lunarpulse chose patch-in-place; delta applied.

**Standing lesson added:** *the pin is a claim too.* A ready-for-dev story is only as live as its baseline commit — re-ground against HEAD on entry, because siblings land on top of a waiting story and turn its spine premises false without touching its file. (Memlog note: `memlog.py append` errored — this file has no frontmatter — so appended in-style manually.)
## 2026-07-28 — Epic-13 close-out question (Korean session): "after all stories, what do we have / can we demo / is the journey shown?"

**Lunarpulse asked the post-implementation question. The answer is one fact: 15/16 done, and the ONE left — 13.6 Reza journey CLOSER — is `backlog`.** The epic grew 11→16 stories: 13.5f (correlation producer, landed inside 13.5e) + 13.5g/h/i/j (defense-in-depth hardening found by implementation, all `done` 07-23→07-27). Winston·Murat·John·Amelia + Mary/Sally, session mode.

**John's framing won the room — "ingredients proven, dish never cooked."** Mechanisms (13.1 wall, 13.2 crypto, 13.3 consent, 13.3b provenance, 13.4 FR37, 13.5a/b/c/d/e + f–j) are gate-green IN ISOLATION. 13.6 is the FIRST story to run 3-team×3-region in ONE process/topology; its legs are ABSENT/INDETERMINATE until then → **the Reza/v2.2 product claim is currently BLOCKED** (epic's own wording: any ABSENT/INDETERMINATE required leg blocks the claim). `cohort-a2a-daemon` first booted in 13.5c but that was a single-process boot→refresh→serve smoke, NOT the full Reza scene.

**Winston conceded two points to John (rare — he argued the semantic case AGAIN and lost):** (1) **kernel-Δ decay is real** — started `23202`, ended `23401` (+199, three grants: 13.5h +6 / 13.5i +46 / 13.5j +83); the ZERO premise rotted during hardening, exactly the Epic-11 retro decay warning. fkcs frozen `23081` is the one surface that held. (2) **"all done" is false before 13.6** — he couldn't defend it.

**Murat's two holds:** the "green preflight is a bug that hasn't introduced itself" reflex applied to the WHOLE epic now — 13.6 is the first composition, nothing combined has been falsified yet. And **operator-axis tenancy is OWNERLESS + deferred past v2.2** (NFR-Ops-11 namespace + capability-token signing key) — Mary caught it in 13.5c scoping; Reza is 1 operator + 3 teams, so any "multi-tenant demo" is **team-axis-only** and must be framed that way or it's the 26th claim-standing-for-a-control. **14-institution = measured capacity envelope, not execution** (epic-cemented).

**Mary's traceability call:** mechanisms trace to FR/NFRs individually, but the **J-12/Reza journey itself** traces to real multi-team activity ONLY at 13.6. Claim-standing-for-a-control tally now 25+; hardening caught 5 more (13.5g caught its OWN filed fix being unfalsifiable — "entailed by a check that already passed").

**Open residuals (all v2.5/ownerless) hanging over an honest demo:** `.team` sidecar cryptographic identity (ADR-055 residual #6 → `v25-signed-shard`), region `source_log_ref` (open since 11.2b D1, three sessions no successor — *"at some point 'deferred' is just 'decided not to'"*), 13.5g TOCTOU file-swap half (deferred, needs a maos-iac delta the Budget pins at zero).

**Fork put to Lunarpulse (unresolved — party continues):** (a) preflight/dev 13.6 next to stand the Reza scene up for the first time (the only path to "journey shown explicitly"), or (b) build a parts-demo from what's gate-green now, with the explicit disclaimer that the composed journey has never run. Substrate dependency flagged either way: a live 3×3 Postgres must be CI-provisioned for 13.6's live legs to graduate advisory→blocking. (Memlog appended in-style manually again — `memlog.py` still rejects this file's missing frontmatter.)

## 2026-07-28 (같은 날) — Story 13.6a critique (다른 방이生成, 우리가 검토): crypto 위조 저항이 wiring 순간 operator-trust로 붕괴

**다른 방이 13.6a(production cross-team crossing initiators)를 13.6 grounding에서 split-out 해 `ready-for-dev`로 만들었고, 이 방이 검토.** Vex/Grumbal walk-on(보안 경계). Split 자체는 에픽 정책에 정당(checklist item 5 세 번째; 13.6은 판사만 한다). 하지만 ready-for-dev 가기 전 **네 차단**.

**헤드라인 — Grumbal+Vex: base_seed = 모든 팀 서명키. AC4 forged-source_team 네거티브가 live topology에서 vacuous.** `build_replication_bundle_v2`(bundle.rs:414)가 `derive_team_signing_seed(base_seed, region, team)`로 private 서명키 도출 → 서명. `derive_team_signing_seed`는 base_seed + 아무 (region,team)에 작동(sealed_export.rs:72). 13.6a T2가 emitter에 `MAOS_CROSS_TEAM_BASE_SEED` 세팅 → **team-a 호스트가 team-b 번들을 정당한 키로 위조 가능.** 기존 13.2 네거티브(bundle.rs:1656-1676)는 **relabel 공격만** 잡음(team-b 키로 서명 후 라벨 team-a로 교체 → team-a 키 미사용으로 거부 = seed-less forger 패턴). seed를 가진 공격자는 team-a 키 직접 도출해 정당 서명 → 통과. **Fork-4 위조 저항은 signing이 unwired(test-only)일 때만 성립했고, 13.6a가 wiring하는 순간 single shared base_seed로 붕괴.** AC4가 통과하면 "Fork-4 증명"이라고 선언하겠지만 claim-standing-for-a-control 26번째. Dana 순위 맞음 — crypto 재설계는 13.6a 밖(single-org/single-operator Reza는 consent가 남용 축, crypto는 compromised-host 축). **처분 = AC4를 "seed-less forger"로 re-scope + ADR-055/threat-model에 "base_seed = 전 팀 키 도출; single-operator trust scope; 진짜 격리는 v2.5 per-team/operator-pinned keys(v25-signed-shard 家門)" honest degradation 명시. "Fork-4 증명"이라는 말 금지.**

**Murat+Amelia — kernel-core ZERO 미측정.** `collective_port_error`(memory/mod.rs:198-211)가 CollectivePortError 4 variant(Unreachable/Timeout/Transport/Memory)만 explicit 매칭, wildcard 없음. T3 "preserve five-cause distinguishability"가 새 variant 필요하면 kernel-core non-exhaustive → 컴파일 에러 → ZERO 깨짐. 13.3 전례(그때 "Transport(TransportCause) route"로 합의). T3에 "no new variant; consent rides Transport(TransportCause)" 명시하거나 구현 후 23401 측정 인용.

**Murat/John/Vex — membership authz 구멍이 crossing wiring으로 live화(residual 2).** `is_granted`가 lease+grant만 보고 `local_host()∈manifest.members` 안 봄. crossing unwired일 땐 unreachable이었는데 13.6a가 live로 만듦 — 퇴출 호스트가 fresh lease로 consent(11.4a stale-permit 동류, 그러나 정상 운영 중 터지는 정상 케이스라 crypto 위조보다 가능성 높음). residual note만으론 부족 → 자기 자신을 향한 dead-wire leg 또는 in-story fix.

**Amelia — AC1 "no two stores"가 control 없는 claim.** D-5가 LoomLiteStore::new == 1(main.rs:2766) 쟀으니 정적 leg로 만들 수 있는데 산문으로만 적음(13.5g "legs green connecting nothing" 동류).

**Winston/Sally — initiator shape mismatch(R1, 비차단).** D-6 seam doc이 "Spirit→collective ... 13.6 journey"라 하는데 T2는 operator MAOS_ONE_SHOT. 13.5d cap-gated Spirit 경로와 충돌 — 13.6이 도착하면 재작업 비용. T2에 "13.5d 대체 아닌 operator 테스트 표면" 문장 추가 권고.

**Disposition(다른 방에 돌려보낼 패치):** B1 AC4 re-scope+ADR / B2 ZERO 측정 명시 / B3 membership leg / B4 no-two-stores leg. 네 개 넣으면 ready-for-dev 유지. Grumbal이 또 opening funeral 역대 최고 캐치 — Vex가 위협모델로 다듬고 Dana가 범위 순위 매겨 막지 않음. Mary: 이것도 "over-rigid/under-bounded/over-claimed" 최대 히트 shape의 over-claimed 버전(provenance doc이 아닌 crypto claim에서).

## 2026-07-28 (3rd session, same day) — 13.6a preflight: the room resolved its own review package, and rejected its own P2

**Lunarpulse brought the review back for resolution ("long-term correctness"). All four patches closed; 5 ACs → 6.** Installed seven + Vex/Grumbal walk-on. Session mode.

**Winston opened by arguing B1 was overstated — "the threat model already documents the blast radius, Vex wrote it himself" — and Vex made him read his own sentence aloud.** The note says an attacker who **"recovers"** the seed. Compromise. A breach. **13.6a hands the seed to every emitter as a job requirement.** And two lines above it T1 declares *"same-region cross-team forgery is cryptographically infeasible / CLOSED at ENTRY."* That is the sentence 13.6a falsifies — not the blast-radius note. Winston had no answer. Grumbal: *"He walked into it. He volunteered."* Mary's shape, third session running: **over-claimed**, and the over-claim is load-bearing **inside a threat model**. Murat: T1 was green because the path was dead — *a green preflight is a bug that hasn't introduced itself yet.*

**Sally's dumb question broke the problem open and it wasn't hers to solve.** *"Why does the applier ask the envelope who sent it instead of asking the socket?"* Amelia chased the Spirit axis (carry stable `SpiritId`, check `TeamEntry.members`) and **killed it herself**: everything inside the envelope is forgeable because one key signs all of it. That failure is what proved Sally right — **the mTLS fingerprint is the only identity `base_seed` cannot reach** (operator-pinned in `CohortMember.fingerprint`, not seed-derived). Grumbal: *"I hate that I don't hate it."* The collision earned its keep: neither Vex's crypto framing nor Sally's UX question would have reached the mTLS-axis answer alone.

**The room REJECTED the review's own P2 (accept single-operator trust, document loudly).** John's framing decided it: *"we split 13.6a out so the closer would have something real to judge — shipping it bypassable defeats the split."* Mary sharpened it: a knowingly-bypassable crossing leaves 13.6 (a judge) only ABSENT or a footnote. Vex: *"'single-operator trust' is how a policy layer wearing a wall's clothes survives to v3.0"* — callback to his own standing line, *at some point 'deferred' is just 'decided not to.'* **Winston dissented on deferring the schema bump; overruled.** He was right on substance, wrong on the number — **`COHORT_SCHEMA_V3` is already spent (13.3), so it's V4**, and the canonical pre-image is shared with `deny_unknown_fields`, so there is **no optional-field shortcut**: v1/v2/v3 bytes must stay byte-identical, the 9.2b idiom over **three** frozen predecessors now.

**Landed:** AC6 (mTLS-axis team binding, fail-closed, impersonation negative that the shipped relabel negative cannot reach, T1 correction in the same commit, "Fork-4 proven" banned on the crossing path) · AC4 re-scoped to *seed-less relabel forger* · B2 kernel-mapper fence (no new `CollectivePortError` variant — wildcard-free mapper lives in kernel-core) · B4 "no two stores" prose → static Blocking leg · R1 one-shot named an operator test surface. **Residual 2 (evicted host) closed in-story — B1 and B3 really were one hole with two faces; new residual 2b keeps the honest limit: enforced against a peer, NOT against the operator.**

**Standing lesson added:** *a control's scope condition is invisible while the path is dead.* Fork-4's forgery resistance was real and unqualified — right up until someone wired the signer. Whenever a story turns a test-only path production, re-read every claim that path's controls make, because the qualifier nobody wrote down is the one that just became false.

### Same session, immediately after — Vex's entailment pressure-test REFUTED the room's own fix

**Vex refused to accept AC6's free lunch and was right.** The preflight had closed Residual 2 (evicted host) as *"same hole, two faces — AC6's mTLS binding covers it; verify mechanically."* Verified. **False.** AC6 binds the **sender's** cert→team; the residual is the **receiver** evicted from `manifest.members`. Opposite ends of one connection. The room had merged two holes because they *felt* like one shape.

**And the store path was weaker than the original 13.3 finding said.** The crossing writes via `write_with_source_attested` (`store.rs:553`) — a **fifth** write path outside `team_guard`'s four-site chokepoint (`write:497/erase:718/read:834/scan:956`) — and correctly so, because `team_guard` refuses foreign-team rows, which is what a crossing *is*. The eviction check lives only inside `TenantMapAdapter::current_manifest()`, reachable via `team_of` (only from `team_guard`) or `datname_for` (only from `connection_assignment_guard` = boot-time). **Mary's line of the session: the eviction check was never a control in its own right — it is a SIDE EFFECT of the tenant-map lookup, and the crossing drops it as a consequence of being a crossing.**

**Saved by a mechanism neither room had named.** `handle_intake_verified` → `handle_intake:840` → cohort gate `:1108`; `CohortConsentVerdict::NotCurrent` is documented as *"stale **or no longer contains the local member**"* → NACK `:1117`. Story 12.1/12.3's A2A gate already does a **receiver-side self-membership check on every accepted frame**, and D-8's applier sits behind it. Contingent on three unasserted facts, now pinned: `collective:share` must never join `is_reserved_cohort_intent` (`:502`, currently only reissue + halt-receipt); `Defer => {}` is a pass; the gate is an `Option` that falls back to `Legacy` (defers everything).

**Standing lesson, sharper than the morning's:** *a claim that happens to be true is still a claim.* We got the right verdict from the wrong mechanism and would have shipped the reasoning. Grumbal, on the three facts: *"Unpinned facts. My favourite kind."* Also retired a merge we made ourselves: **B1 and B3 are two holes closed by two different mechanisms** — merging findings by resemblance is its own failure mode, and we did it in the same session we caught someone else doing it.

### Same session, closing — operator directive: "we do not defer what needs to build in the epic"

**Lunarpulse cut the Defer fork and reframed the whole session.** Vex's scoped answer carried unanimously: `Defer` becomes a **refusal on the crossing intent only**; general A2A keeps 12.1's bilateral fallback. **Winston withdrew his objection himself** on the ground that `collective:share` postdates 12.1 — so it adds a rule to a new intent rather than changing shipped behavior. Mary: *"then it wasn't a fork, it was an omission."* Both probes landed: emitter-side self-check **exists** (Send seam `NotCurrent` → `ConfigInvalid`, `router.rs:677`), and `Defer` **is** reachable for a rostered peer (`state.rs:592`, roster-membership vs consent-table-membership are two notions) but sits *after* `current`, so it is not the hazard. **The hazard is the FIRST `Defer` (`:546`), which returns BEFORE the self-membership check** — a de-rostered sender pushes a crossing into a healthy applier, and TOFU pin + peer allowlist do not help because those are *config-time* facts, not *manifest-time* facts.

**Fourth split of the day, operator-ratified: 13.6a identity → 13.6b crossing.** The argument that won was **ordering, not scope**: build the crossing first and *every commit containing it* has consent bypassable by impersonation — which is exactly the P2 trade the room had already rejected in review, re-created one layer down in commit order. 13.6a is provable against synthetic frames and **inverts no dead-wire leg**; 13.6b gets a live impersonation negative from its first commit. Epic 13 → **18 stories**.

**The discipline the directive needed (and the room supplied):** *"don't defer what the epic must build"* is a scope rule, not an unbounded one. Test used throughout — **does the Reza/v2.2 product claim become true without it?** In: identity, crossing, 3×3 substrate, evidence ledger, and the two broken *instruments* (`check-fkcs` exits 0 on a red oracle; `check_ship_gate_completeness` never validates CI→registry — the shared upstream of both known null controls), because a judge running on gates that cannot fail returns a verdict nobody can trust. Out, with reasons stated rather than implied: per-team key provisioning (`v25-signed-shard` — Reza is ONE operator, so isolation-against-the-operator makes no claim truer), NFR-Ops-11 operator axis (**owner assigned to Epic 14 — "ownerless" was the actual defect, not "unbuilt"**), 100-host/soak/geo, external holds.

**Standing lesson:** *sequencing is not deferral — and commit order is part of the design.* A story can be correct at its tip and still have shipped an unsafe window. Also retired our own merge from this morning: **B1 and B3 were two holes closed by two different mechanisms**; merging findings by resemblance is its own failure mode, and we did it in the same session we caught someone else doing it.

## 2026-07-29 — 13.6b post-13.6a preflight: the weld our own split left behind, and a cause that dies twice

**13.6a SHIPPED overnight (`a414f922`, 4,225 lines, 44/44 Blocking, kernel verified 23401==23401).** Room reconvened on 13.6b. Murat opened with his own line — *"a green preflight is a bug that hasn't introduced itself yet"* — and 13.6b was the introduction.

**Headline, Mary: two fields, one authenticated, the other one votes.** 13.6a binds the **envelope** (`request.cohort_source_team`, `router.rs:1429-1445`, `-32010`). The crossing decides consent from the **payload** (`bundle.source_team` → `is_granted`, `bundle.rs:875-917`). **Nothing binds them.** A seed-holder stamps a *truthful* envelope (`router.rs:746` forces it) and a *lying* payload, correctly signed — row lands, **D-10 intact**. Amelia found why nobody caught it: 13.6b's AC6 moved into 13.6a, AC4 is explicitly barred from claiming impersonation coverage, **so the clause that owned the weld is in neither file. It fell in the seam the split created.** New failure shape for the catalogue, distinct from the usual: ***a control standing next to the decision it doesn't govern.*** Winston tried "that's the attack we closed last session" and Vex made him read 13.6a's own AC2 — *"provable against synthetic frames."* Envelope. Not the decision site. (Winston: 0 for 4 sessions on opening arguments, 1 for 1 on the argument he made **against himself** — see below.)

**Sally's dumb question worked a second time and everyone now waits for it.** *"Does a one-shot have a network?"* It does not. Sole production outbound path = `prepare_outbound` (`a2a-tcp:730`); its transport is built ONLY in `build_cohort_a2a_daemon_runtime` (`main.rs:9482`) ← `run_cohort_a2a_daemon` (`:9286`) ← the `cohort-a2a-daemon` dispatch (`:7643`). The collective-erase idiom T2 said to copy sits at `main.rs:4883` — **~4,700 lines before any transport exists.** The 13.5a shape, one story later, on the send side. Grumbal: *"And then wrote a task that walks into it. Delightful."*

**Paige's boring finding was the deepest one. The cause dies TWICE.** She asked who consumes `TransportCause` at the far end. (1) **The socket:** zero occurrences in either A2A crate — no wire encoding at all. (2) **The kernel:** `kernel-core/src/memory/mod.rs:204` is `CollectivePortError::Transport(_) => CollectiveErrorKind::Transport`. *Underscore.* **No Spirit has ever distinguished `ConsentDenied` from `MapStale` on any path, since the port was written.** So AC2 was grading a story on something the architecture has never delivered anywhere. Resolved **not weakened**: new typed A2A cause + one more arm in `interpret_response` (a table that already does exactly this five times, two of them already reaching into `error.data`); `TransportCause` stays applier-local; kernel erasure → Residual 6 with **13.6 as named owner**. Paige: *"I didn't want to lower the bar. I wanted to know where the bar had ever been."*

**Winston's one win was against himself.** He asked where a host learns its team and found **two** sources — signed manifest (envelope) vs `MAOS_LOOM_HOME_TEAM` env (payload). Then read out 13.6a's OWN review-P1 fix, `reconcile_transport_identity_with_manifest` (`main.rs:9407`): *"a config-time fact silently overrides a manifest-time fact… **Disagreement is a boot error, never a warning.**"* They wrote it for **certificates** and left **teams** unreconciled, one field over. Vex kept him honest — the reconcile does NOT replace the weld (an attacker owns their own boot). Lunarpulse ratified **both**: AC3 = security control against a peer, AC4 = correctness control against misconfiguration, **neither standing in for the other**.

**Grumbal forced the uncomfortable one and lost the argument he started.** `bundle.rs:441` still says *"the Spirit→collective digest publication flow is the 13.6 journey. This is the seam it will use."* Amelia measured it: `CollectiveMemoryPort` has **four** methods — write/read/scan/erase, **no `share`** — and `write` is exactly what `team_guard` refuses foreign-team rows on. A Spirit-initiated crossing = new port verb **plus a kernel call site**. Winston's counter carried the room and it's the line of the session: **the Spirit initiates *publication*, the host initiates the *crossing*; they were never the same seam — and *a Spirit that can name a destination team is a Spirit that can be prompt-injected into naming one*.** Vex sat up. **The comment is wrong, not the design** — and it had been steering three stories. Corrected in-commit (13.6a T1 precedent), retiring R1 properly instead of apologising for it. Grumbal: *"I came here to complain and I've been talked into agreeing with Winston. Worst day of my life."*

**Operator answers:** (1) both weld + reconcile. (2) discuss → AC2 re-worded, kernel erasure owned by 13.6. (3) **another split.** **Fifth split of Epic 13 → 20 stories.** Write side stays 13.6b (6 ACs, unblocked); read side becomes **13.6d** (5 ACs, absorbs ADR-058 Decision 2 — a successful cross-wall recall journals as a plain local `log.recall`, so a *disclosure* is indistinguishable from a *read*). Fault line: shared gate file and nothing else. Stale duplicate `13-6a-production-cross-team-crossing-initiators.md` deleted (two files claimed to be 13.6a; a dev could have opened the wrong one).

**Standing lessons added:** ***a split creates a seam, and the seam needs an owner*** — when a story sheds an AC, ask what invariant spanned the two halves, because that invariant now belongs to nobody. And Murat's budget rule finally landed as process: **`maos-a2a-core` 4271/4187 = 84 lines reserve on an UNRATIFIED FLAG-Winston grant, surfaced at DESIGN time** — five consecutive stories of "done"-time re-basing stopped at four.

## 2026-07-29 (2nd session, same day) — 13.6d preflight: the parameter was decorative

**Winston opened confidently for the SIXTH time — "this one's the gentle one, five ACs, the only interesting question is request shape" — and Grumbal offered odds before anyone else spoke.** Murat refused to reuse his own line and said something better: *13.6b is done, so this is the first story in a week whose dead-wire leg nobody has poked yet.*

**Sally's dumb question worked a THIRD time and the room now visibly waits for it.** *"If the request can name team-b — where does team-b's data come from?"* Amelia read the body: `recall_cross_wall` ends in **`self.recall(spirit_pid, filter)`** (`maos-iac/src/adapter/log_recall.rs:376`) — the local emitter-scoped read. **`team` reaches `consent.decide` and never a query.** The story diagnosed the envelope; the letter had nowhere to go.

**Paige's boring finding was again the deepest: it isn't an oversight, it's SPECIFIED, and a Blocking gate certifies it.** `log_recall.rs:735`, inside the test behind `cross-wall-recall-refusal-distinguishable`: *"the method must forward spirit_pid/filter to `recall`"* / `"a granted cross-wall recall returns the target pid's frames"`. **Pid, not team.** Two rows from the caller's own in-memory TL; `remote_team = "team-a"` is a string with no data behind it. Paige: *"I went to find who wrote it down. It's worse than nobody."*

**Mary named the shape and it's the sharpest yet: *a capability whose parameter is decorative*** — the argument that names the operation participates in the **authorization** and never in the **operation**. Distinct from *constructed-but-unwired* and from *a control standing next to the decision it doesn't govern*. Her sting: the story's own D-1 misdiagnosed it — *"the request can't ask the question AND the answer doesn't exist, and only one of those is in the file."* Grumbal, delighted for once: *"We were about to ship an instrument that reads the wrong thermometer and prints a number."*

**Winston went 2-for-6, and again the win was against his own position.** Vex made him read AC1 aloud — *"a filter field makes every existing call site able to ask a cross-wall question"*, offered as a **benefit** — then pointed at `main.rs:4543`: Story 8.14c injects `LogRecallPort` into the **researcher Spirit**. Winston recited his own ratified line from the day before: *"a Spirit that can name a destination team is a Spirit that can be prompt-injected into naming one."* Amelia closed it: `recall_all` rebuilds the filter every page and calls plain `recall`, so a team field either **lies** or **springs D-4**. Mary: *"not a fork — an omission wearing a fork's clothes."* Winston took the patch himself.

**Murat's eleven-second kill.** `cross_wall_recall_refusals_not_journaled` asserts `!method.contains("insert_frame_event")` — it **FORBIDS** journaling. AC4 ordered journaling. **AC4 reds the leg AC4 protects.** And the failure message names **Story 13.5e** as its replacement owner — 13.5e is **DONE** and never replaced it. An orphaned dead-wire leg with a dead owner, sitting in the story's own Dev Notes under *"sibling leg that must stay live."* **13.6b's D-16 one story later — but in a TEST FAILURE MESSAGE, which is worse, because a failure message reads like a specification.**

**John set the scope test and it decided the fork:** *does the Reza claim become true without the remote read?* A team lead investigating an incident **that crossed a team boundary** — no. So it's in the epic. Then Mary noticed the direction **reverses** from 13.6a: there the unsafe window was the crossing, so identity went first; **here the unsafe window is the INITIATOR** — every commit wiring a traceback before the read exists ships a surface that lies to an operator. John: *"then the commit order is build the read, then wire the initiator — which is one story, not two."*

**Operator ratified ABSORB over a sixth split** (Epic 13 stays at 20) **and re-own the journaling leg with atomic cutover.** 5 ACs → 6. Landed: AC1 fork CLOSED to a sibling entry point + static leg proving `LogRecallFilter` arity unchanged · AC2 new dependency-inverted `CrossWallLogReadPort` (maos-domain) + maos-bin adapter over 13.5e's per-team artifact, with **the negative that does not exist today — the returned page is NOT the caller's own rows** — and the shipped Blocking assertion **re-cut, never relaxed** · AC3 initiator in the LAST commit + both boot preconditions named · AC4 **five** refusal limbs not three · AC5 disclosure journaling via a new audit intent const on `CapabilityInvocation` (ZERO kernel Δ) · AC6 `ABSENT_SUCCESSORS` in the reza gate ONLY.

**Two facts the room dug up that decided WHERE code lives:** `maos-iac` has **no `maos-audit` dependency** (so `transparency_log_path_for_tenant_mode` is unreachable from the adapter) ⇒ port in `maos-domain`, adapter in `maos-bin`, the 12.3 P5r pattern for the **third consecutive story**. And **13.5e already ships the control that refuses a foreign-team artifact** (`TenantBindingError`, `maos-audit:1251`) ⇒ the consented path must be the **sole** permitted foreign open, which turns a constraint into a control with two asserted halves.

**Murat's budget rule paid off loudest yet — and the number was not the one in the story.** Measured at design time: **`maos-bin` 14807/14835 = 28 lines of reserve**, and that is exactly where the read adapter must land; 13.6b's grant landed *yesterday* and is already spent. `maos-audit` 35. Also **`kloc-check` is RED in the tree** (`xtask` +23, `maos-a2a-core` +104 — both 13.6b's) ⇒ new Residual 6. Murat: the story said 100 lines and 131; both were stale by one day.

**Standing lessons added.** ***Check what the parameter DOES, not what it is named*** — an argument can pass every type check, reach a real authorization decision, and never touch the operation it appears to select. And: ***a stale marker in a test failure message outranks a stale code comment***, because a failing assertion reads like a specification and nobody argues with it. Residual 1 gained a live counterexample: verifying "no dead-wire clause is unassigned" must look for **stale owners**, not just missing ones.

## 2026-07-29 — Epic-13 progress check: 17/20 done, 13.6 closer exploded to 5-story cluster, our review landed

**Progress check requested. `last_updated: 2026-07-29+13-6b-review-complete`.** 20 stories now (8→11→16→20). 17 done (15 mechanism + 13.6a + 13.6b), 2 ready-for-dev (13.6c substrate, 13.6d recall), 1 blocked (13.6 closer). Journey still NOT demoable.

**Our review directly shaped the epic.** 13.6a-authenticated-team-identity status line recites our B1/B3 finding verbatim (bundle.rs:414 → sealed_export.rs:72; consent from :917; router.rs:1222-1232 host-only; CohortMember=hosts/TeamEntry.members=Spirits). Other room took **P1, identity BEFORE crossing** (our framing). 13.6b (the file we critiqued, renumbered) shipped **D-13 weld**: bundle.source_team must equal AUTHENTICATED envelope claim, refused BEFORE apply_replication_bundle under CODE_CROSSING_SOURCE_TEAM_UNBOUND (-32011), distinct from -32010 (lying) / -32012 (ungranted) — our impersonation hole closed by design. R1 also absorbed: D-14 emitter INSIDE run_cohort_a2a_daemon (sibling MAOS_ONE_SHOT literally can't send — transport only in cohort daemon runtime). [INFERENCE: -32011 catching seed-holding forger needs 13.6b review read to confirm.]

**13.6 cluster reframe (closer exploded into 5 stories):** 13.6a identity (done) · 13.6b crossing (done/review) · 13.6c substrate (ready, 3-DB not 9, two gates run region legs with NO Postgres = vacuous-green risk) · 13.6d recall (ready, FOUNDING PREMISE INVERTED — recall_cross_wall body ends in self.recall LOCAL read, team only hits consent.decide never a query; it's SPECIFIED local-only; existing Blocking leg cross-wall-recall-refusal-distinguishable tests what the method doesn't do — claim-standing-for-a-control #27) · 13.6 closer (blocked; own reframe found the JUDGE MACHINERY is prose — 4-state evidence vocab epic-13:180 returns ONE unrelated grep hit; ABSENT doesn't block, check_reza_production_path.rs:1153 "passed":blockers.is_empty() — so 13.6 must BUILD the judge before it can compose).

**Journey demo path now concrete but 3 steps out:** 13.6c substrate dev → 13.6d recall dev → 13.6 closer (which must build judge machinery first). Kernel-core [INFERENCE] still 23401 (no grant mentioned in 13.6a/b; dependency-inverted CrossTeamCrossingPort keeps kernel-core untouched); fkcs 23081 frozen. Mary: mechanisms+crossing trace to FR/NFR, but J-12/Reza journey still traces only at 13.6 (blocked). Grumbal's "green preflight" reflex vindicated again — 13.6d's cross-wall leg was green testing a no-op. Fork put to user: read 13.6b review to confirm -32011 weld, or preflight 13.6c/13.6d (recall-leg correction is our lane), or design 13.6's missing judge machinery.

## 2026-07-29 (같은 날) — 13.6d 실행 중, "그 다음 뭐 하냐" 임계 경로 맵핑

**Lunarpulse가 13.6d를 dev 중, "impl+review 끝나면 그 다음?" 질문.** 방이 임계 경로 맵핑. 핵심 두 가지:

**(1) 13.6c를 13.6d와 병렬로 지금 시작하라** — 13.6c(substrate, 3-DB Postgres)는 LONG POLE이고 13.6a/b/d에 code dep 없음(상태 라인 명시 "PARALLEL ENABLER, start alongside 13.6a"). 직렬 대기하면 epic 종료가 long pole만큼 밀림. 임계 경로 수학이지 제안이 아님.

**(2) closer 전에 judge machinery split(13.6e)이 한 번 더 온다 [INFERENCE].** 13.6 상태 라인이 D-1(4-state evidence vocabulary가 prose, 게이트 emit 없음) + D-2(ABSENT가 안 막음, check_reza_production_path.rs:1153)를 밝힘. 에픽 규칙 "13.6은 판사만 한다, 발명 안 한다" → 판사가 쓸 기계가 없으면 checklist item 5 네 번째 적용으로 split. 13.3b/13.5c→d/e/13.6→a/b와 동일 패턴. "defer-INTO를 scout하라" 교훈이 가리키는 지점. 13.6 preflight에서 놀라지 말고 지금 pre-plan 권고.

**순서:** 13.6d done → 13.6c(병렬) done → 13.6e(judge) done → 그제야 13.6 compose+judge → 여정 데모 가능 → epic-13 retro. 

**13.6d review 감시(이 방 레인):** founding premise invert된 스토리(recall_cross_wall이 월 안 넘었음) — 새 cross-wall query가 진짜 원격 TL 읽는지 + 기존 false Blocking leg(cross-wall-recall-refusal-distinguishable, 메서드가 안 하는 일 테스트 = claim-standing #27) invert/교정됐는지. "green preflight는 아직 인사 안 한 버그."

**데모 직전 honest residuals(13.6 block 안 함, 단 over-claim 방어):** NFR-Ops-11 team axis only / v25-signed-shard(ADR-055 #6) / region source_log_ref(11.2b D1) / 13.5g TOCTOU / 14-institution=측정 envelope. 13.6e의 evidence-state emit이 각 leg에 PROVEN/ABSENT/INDETERMINATE 솔직히 내보내게 하는 게 방어 기제.

Fork: 13.6c 병렬 시작? 13.6e judge-machinery split 미리 설계?

## 2026-07-30 — Story 13.6d code-review follow-through (P2 TOCTOU closed in-story)

**Outcome: the room killed its OWN defer.** Winston · Murat · Amelia · John. Session mode. The 3-layer code review had routed P2 (cross-wall read TOCTOU — binding verified on one connection, rows read from a second) to defer with reason "needs a maos-iac zero-delta change." Winston re-read the constraint and caught the misread: **"zero delta" in this codebase means zero KERNEL-CORE (the 23401 pin), NOT zero `maos-iac`** — `maos-iac` carries a kloc budget (130 reserve) and the only hard law is the no-`maos-audit` edge. The defer was built on a sentence the ADR never wrote.

**The fix — single-connection foreign read (Amelia).** `read_remote` now opens ONE read-only NOFOLLOW connection (`maos_audit::open_tenant_artifact_readonly` + `read_tenant_artifact_on`), verifies the binding, and serves the rows on that same handle via new `TransparencyLogAdapter::from_read_only_connection`. Redaction is the stateless `CorpusBackedRedactionPolicy` — a foreign shard is redacted identically, no special path. Two opens → one. ~12 lines iac, ~25 audit, maos-bin went DOWN.

**Murat's bar — prove it, don't claim it.** Held-connection swap test: bind team-a on good.db, rename swap.db over the path, query on the held conn → still good.db's rows (the inode doesn't follow the path). Deterministic, no race, no seam. Reverting to two opens re-resolves the swapped path → swap's rows → red.

**John — scope by deletion, not addition.** AC2 patch (the foreign read returns the named team's rows consistently); no new AC, no new story, Epic 13 stays at 20. "No more defers" answered by DELETING the defer entry. `v25-signed-shard` trimmed to "crypto identity only" — the consistency fix and the crypto fix are different controls; shipping one doesn't retire the other.

Status → done. Three stories (13.5g, 13.5i, 13.6d) had deferred this same TOCTOU class; 13.6d is the one that re-read its own ADR. Lesson, nth on the tally: **a defer's REASON can itself be a claim standing in for a constraint — verify the constraint exists before accepting the defer.**

## 2026-08-01 — Story 13.6c preflight (3-team/3-region substrate): the AC that provisions a database with no reader

**Outcome: 9 findings, 3 forks resolved, story rewritten 4 ACs → 5, status held ready-for-dev.** Winston · Murat · Amelia · John · Mary · Paige · Sally. Session mode. Verified directly at `b400d127` (three commits past the story's `cb412348` baseline). Kernel pin **re-verified: 23401 == 23401**; `depends_on: none` re-verified — the only `discipline.yml` diff since baseline is a six-line comment.

**The story's D-1/D-2/D-3 all survived; every line cite in them was stale.** 13.6a inserted `team_of_host` above `validate_team_map` (`:652`→`:727`); `TeamEntry` `:130`→`:154`. Paige's framing, and it's the one worth keeping: **a cite that drifts is how a story stops being checkable by the next person** — the conclusion was right and the evidence was unfindable. Mary also corrected D-1's *reasoning*: `region` is **not** in the uniqueness set (only `team_id` + `datname`), so "team-N in region-N" is a **fixture choice**, not a manifest constraint.

**Two ship-blockers inside the story's own AC2 (Murat).** (1) `check-cross-region-consensus` probes **`MAOS_TEST_POSTGRES` — singular** (`:308`), while AC2 handed it `_{A,B,C}`: service provisioned, gate still skipped, CI minutes paid, nothing measured. (2) Its oracle is **unfiltered** — `cargo test --test cross_region_live -- --ignored`, **32 tests**, one result **broadcast to all four legs** (`:160-172`, `:110-116`) — and every env reader **`panic!`s rather than skips**. So partial env reds all four legs. Resolution: **the required env is a union derived from the readers**, now an AC2 table per gate. Mary's addition: `cross_region_live.rs` has accreted into the shared oracle for **two gates and nine legs across three stories and nobody decided that** — declared as a residual for the closer, explicitly NOT fixed here (Winston: "redesigning another story's oracle to save wall-clock is how a two-day story becomes a week").

**AC4's premise was wrong, and the correction has teeth (Murat → Amelia).** `LegResult::skipped` sets **`green: false`** — there's even a unit test asserting it (`check_cross_region_consensus.rs:491`). What yields exit 0 is **`const CURRENT_PHASE: &str = "v1_5"`**, not the `passed: blockers.is_empty()` AdvisorySubstrate pattern the story blamed. Consequence Amelia drew and the story now states in words: **after this story lands, a red `three-region-convergence` still exits 0.** Turning legs on ≠ making them binding. Neither gate has the leg-level `BindingClass` its siblings carry — the E12-B1 decay item — and Winston boundaried it OUT ("fix the naming here, assign the fix to 13.6e; otherwise the successor inherits a boundary drawn around a thing that isn't there").

**Null control #28 (Amelia + Sally).** AC4 wanted the new vars registered in `env_contract.rs` "so `check-env-contract` covers them." The gate walks **`maos-bin/src` only** (`:89`, `:118-120`) — the vars live in `loom-lite/tests`, `bench/tests`, `xtask/src`. Its own success message says *"workspace coverage tracked in Story 12.7."* Sally made it worse-and-better: `env_contract.rs` is the **operator-facing** contract, so a CI fixture there tells a human to set something they never will — and then named the control that should exist instead: **does every variable a gate's oracle reads get exported by the job that runs it?** That check would have caught both ship-blockers on its own. Became AC5.

**John's finding, and the best line of the session.** AC2 wanted `maos_team_c` + `MAOS_TEST_POSTGRES_TEAM_C` on two gates. **`TEAM_C` has zero consumers workspace-wide** — both probes require exactly `[TEAM_A, TEAM_B]`, and `pg_conn_team` `panic!`s on anything else. *"The story whose entire purpose is 'a declared leg with no sensor' proposes to provision a database with no reader."* John wanted defer; **Murat took the unpopular side** — deferring re-splits the seam this story exists to own — and John met it: **it lands with a reader**, one test asserting three distinct `current_database()`, one leg, `live_substrate_present()` widened. Winston then sharpened it past both of them: **`TEAM_C` goes only where its reader goes** — `check-multi-tenant-loom` yes, `check-reza-production-path` **nothing**.

**AC2's mechanism was unbuildable (Amelia).** "single-sourced as a reusable step or composite action" — **a composite action cannot define `services:`** (job-level key), Actions has **no YAML anchors**, and `.github/actions` + `workflow_call` don't exist in this repo. Landed: composite action for the **step** half, service block held by a **drift gate** instead of a promise. John: *"don't ship an AC whose stated mechanism can't exist"* — and he made the drift gate the named control, "otherwise 'single-sourced' is a promise, and this room has a tally for promises."

**Fork resolved by construction, not by observation (Winston).** AC1 deferred the one-DB-set-vs-two question to a runtime observation. Winston answered it at HEAD instead: `MAOS_TEST_POSTGRES` is **already aliased onto `maos_team_b`** (`discipline.yml:2713`), and every reset is table-scoped `DELETE FROM collective_memory` — so aliasing means one axis's reset wipes another's fixture. Rule: **within a job, no two variables resolve to one database across different roles**; `MAOS_TEST_POSTGRES` gets its own `maos_shared` in the new jobs; existing green jobs untouched. Murat: *"a decision by construction beats a decision by observation, because the observation happens once and then nobody re-runs it."*

**Good news, filed because this room files little (Amelia).** AC3's before/after blindness evidence is **already machine-readable** — both gates emit `postgres_available` and per-leg `attempted` in `--json`. T4 is a JSON diff, not a narrative. Sally: that's the artifact the operator should see — *"two JSON blobs where a field flips."* Paige closed it: the story already wrote *"'we turned it on' is a claim; the before/after is the control"* — it just didn't know the control was cheap.

**Standing lessons added.** ***An AC can name a mechanism that cannot exist*** — composite-action-`services:`, `env_contract.rs` coverage, and `TEAM_C`-with-no-reader were three in one story, and all three would have passed review as prose. And: ***verify which variable a gate PROBES, not which variable its tests read*** — the singular/plural `MAOS_TEST_POSTGRES` split is one character wide and would have burned a full CI cycle before anyone noticed the legs were still skipped. Null-control tally: **28**.

Relationship notes: Sally, usually peripheral on CI stories, supplied the session's best structural fix (the reader⟷job consistency check) by asking a *human* question about a machine file. Murat argued **against** his own instinct twice (kept the double-run, kept `TEAM_C` in-story) — "strong opinions, weakly held" showing up as actual concessions. John's scope reflex now runs both directions: he cut `TEAM_C` from reza and refused to let it be deferred entirely.

## 2026-07-30 (같은 날) — Story 13-6 클로저 judge-machinery preflight (설계 확정, 구현 전)

**Outcome: 설계 코드로 확정, 구현 대기.** Winston · Murat · Amelia · John. Session mode. 클로저(13-6)는 여전히 blocked이지만 judge-machinery(AC5 evidence ledger) 설계를 코드 기반으로 닫았다 — "claim standing in for a control" 집계의 또 다른 표본(D-2: 녹색 게이트 위 ABSENT banner).

**코드가 결정한 것.** 투영은 두 Reza 게이트가 이미 emit하는 필드(`attempted/green/binding/substrate_present`)에서 파생 — `!attempted`→ABSENT, hermetic-green→PROVEN_BLOCKING, live-green+서명검증→PROVEN_LIVE_SIGNED, 나머지→INDETERMINATE. leg마다 상태 annotation 불필요(손관리 목록 = null control 변장, trap #3 원천 차단). 핵심: **D-2의 수정 레버 `oracle_green`이 이미 계산됨** — 단지 exit에 안 연결. ledger의 순수 새 가치는 dev-lane exit(`blockers.is_empty()`)이 놓치는 두 갭: ABSENT + green-미서명-live.

**서명 = 재사용, 발명 아님.** `maos_audit::sealed_export::sign_bundle` + `MAOS_AUDIT_KEY_SEED` + `SignatureBlock`(FR44/13.2 Fork-4) 이미 구축. 서명 주체는 **live 테스트 harness**(게이트 사후서명 = 자기채점 trap #2 회피); ledger는 operator pinned key에서 pubkey **파생 후 검증**(R-RG1). **(a) operator-local만 PROVEN_LIVE_SIGNED 비준** — CI dev-key는 "위조 가능한 걸 PROVEN이라 명명 = 13.2 trusted registry 재판"으로 기각(Murat). product-claim proven은 operator 서명 run에 의존 = J1 Tier-2 동일 자세.

**falsifier 3-blind(Murat)** — 빈 서명 / 검증실패 / stale-binding(지난달 서명 재사용 → artifact에 commit-sha·substrate-nonce binding). exit 배관: dev-lane exit 유지 + 신규 `product_claim` JSON 필드 + `check_ship_gate_completeness` 소비(ownerless CI→registry 공백도 같이 닫음). 두 게이트는 복붙 쌍둥이 → 공유 투영 헬퍼 + ABSENT_SUCCESSORS 동적 파생.

**substrate 분리.** 메커니즘 + falsifier 3종은 substrate-독립, **지금 구현 가능**; LIVE PROVEN_LIVE_SIGNED 데모만 13-6c substrate + operator run 대기. ZERO kernel-core; 에픽 20 유지, 새 스토리 無(판사의 도구 = 허용, trap #1 선 방어). 클로저 AC5 + Dev Notes에 설계 반영 완료.
- (outcome by murat) 2026-08-11: demo-reza actually EXECUTED on the operator lane (local PG17 substrate, audit key present) — four gates exit 0, ledgers re-signed @ ea9939d3, both required legs PROVEN_LIVE_SIGNED; verbatim capture published as demo-reza-capture-2026-08-11.txt (commit b2c7fc92). Murat's 'the demo runner has no demo' gap is closed.
- (outcome by john) 2026-08-11: admin debt closed — epic-13 header/index statuses reconciled, Epic-14 DAG section added (12 edges), traceability matrix gained 8 Epic-13 rows + J3 GAP row (f3600a45); epic-13 retro written with 21 deferred-work dispositions, 12 handed to Epic-14/preflight (55f8bf22). Epic 13 fully closed; Epic 14 preflight next.
- (moment) J1 assessment (2026-08-14): room revived 'ingredients proven, dish never cooked' for J1's remote leg — Tier-2 local leg signed 7/16 (real Codex, 247-entry bundle), but crosshost series (1a ready-for-dev, 1b blocked, crosshost-2 = ID with no story file) is epic-orphaned; Epic 14 carries zero J1 scope.
- (dynamic) Mary's 'claim standing for a control' alarm rang thrice on J1: 9-6 D4 Grade-A vs deferred resume seam (journey_j1.rs:208-214), zero J1 rows in traceability-matrix, Tier-2 spec frontmatter in-progress vs closed changelog. John conceded D4 but defended the bridge lane as a recorded decision; Murat: 'a recorded orphan is still an orphan.'
- (outcome) Proposed J1 path: dev 1a (ABI review gate on maos-kernel-core re-export) → 1b PROVEN_BLOCKING → author missing j1-crosshost-2 → attach series to an epic + add J1 traceability rows → reconcile D4 contradiction. Epic-attachment call handed to Lunarpulse (E14 attach vs formal bridge lane).
- (outcome) j1-crosshost-1 preflight: room settled 7 items. ABI growth ratified not dodged (OnceLock = set-once SECURITY property); intent widened to development-task:write-workspace because ADR-012 names EFFECT AUTHORITY not verbs; completion uses existing TaskComplete frame not a new audit kind; gate skeleton moved 1b->1a because 'a test not behind a gate is a suggestion'.
- (moment by bmad-agent-ux-designer) Sally's dumb question went 4-for-4 and was the biggest find of the session: 'has the founder loop ever run with the priorities anyone wrote down?' No. priority_weight was never parsed -> the SIGNED J1 demo documents scheduling that never happened. Then she asked 'is all of xtask doing something?' and Amelia found 133 dead lines. The room now visibly waits for her.
- (moment by bmad-agent-dev) Amelia dissolved an integrity-vs-integrity deadlock by measurement: xtask/src/example_spirit_regen.rs = 133 tokei-code lines, mod-declared, ONE pub fn, ZERO callers, dead-code-warning since authored. Grumbal: 'at least one of those seven re-bases was funding ROT. We bought capacity for a corpse.' Winston: 'the dilemma was never real. There's no grant to request. There's a broom.'
- (moment by sec-hawk) Vex found the deepest thing in the story: on loopback handle_intake binds NO wire identity (router.rs:1478-1479), so frame.from.host_id -- which SELECTS which accept_allowlist applies -- is sender-written. 'A frame picks its own judge.' Grumbal: 'a lock with the key taped to it.' Murat: 'my negative control is one string assignment away from being a positive.' Became a STATED NON-COVERAGE in 1b + a 4-line gate leg in 1a.
- (callback) Murat withdrew an objection IN ADVANCE, conditionally ('delete the mod line first and let the compiler prove it') -- Grumbal: 'he never does that.' Winston finished 2-for-7 on opening arguments; 'he grepped for a noun' fired twice in one session (2nd time on D11's own row being scoped around the instance instead of the category). Mary's tally reached 33.
- (moment) D0 rehearsal ran live 2026-08-14 (fixture founder-loop, 5.2s, worker_completion=true, TL ref issued) and the room caught a real evidence bug: 5.0s of the wall was the --once audit-writer drain TIMEOUT — MemoryManagerAdapter retains audit_tx via CapabilityRegistryAdapter, queued cap rows lossable at exit, sealed-export completeness risk. Murat's 'the green run that hides a leak' beat. Journal-corruption noise traced to shared operator state (provision-around); cap-token fallback confirmed DESIGNED pending Epic-9 (Cedar alone cannot green it) — narrate, don't fix.
- (outcome) Story j1-demo-one-command-scene authored via create-story and registered ready-for-dev (2026-08-14): xtask demo-j1 narrated scene mirroring demo-reza, isolated MAOS_HOME per run, beat ledger with unlanded beats ABSENT naming owners, --live-codex automates Tier-2 runbook, AC4 fixes drain timeout proven-red, ZERO kernel-delta intent. Name deliberately avoids 'journey-closer' (Winston's honest-naming veto); parallelizable with 1a/1b. demo-reza flaws NOT copied: absolute key path print, unvalidated ledger reads, BTreeMap-order claims.
- (outcome) Option A picked 2026-08-14, then reality inverted it mid-session: 1a flipped to in-progress and now HOLDS both shared files uncommitted (+319/-19; maos-bin/src/main.rs 21 hunks, xtask/src/main.rs = single deleted mod example_spirit_regen line). Measured region-disjointness: no 1a hunk touches the --once drain branch ~4164-4181, but same-file concurrency still rules, so demo T0 QUEUES BEHIND 1a's commit (drain region shifts ~+181). Murat's 'fix the drain first' lost to file ownership — recorded, not relitigated. Ownership rule adopted: whoever holds the file makes the edit; optional accelerator offered (1a carries the 3-line demo-j1 stub registration in its own commit so the demo compiles immediately).
- (outcome) Option A executed 2026-08-14 after 1a landed (6827dc87): xtask demo-j1 SHIPPED and verified — exit 0, founder loop 0.162-0.186s, 8 executed beats PROVEN_BLOCKING + 4 ABSENT naming owners, 11 unit tests green, runbook written. AC4 drain fix proven-red (RED at HEAD 5.24s -> GREEN 0.19s; wall 5.2s -> 0.182s); root cause was DEEPER than the story predicted (14 owners, not just memory->capability). check-kernel-baseline 24472=24472. Murat's demo-as-gate bet paid: rehearsal found a real evidence-integrity defect and the demo now fails on it.
- (moment) The demo's own governance bit it: kloc-check RED, xtask 37846 vs ceiling 37287 (+559). xtask had only 149 lines of slack for a 708-line feature, so the story CANNOT land grant-free however it is written. Free reduction taken (tests -> xtask/src/tests/, kloc-excluded, verified 39942-1919=38023). kloc.toml deliberately NOT edited — CEILING RULE says the formula is never silently re-run to fit; a named measured grant is Lunarpulse's call. Room's own discipline used on the room's own work.
- (dynamic) Winston's honest-naming veto held all the way into code: the scene refuses to print 'cross-host' while two-host-signed-run is not PROVEN_LIVE_SIGNED, and the delegation beat flipped ABSENT->EXECUTED the moment 1a landed rather than being hardcoded. Also found: no codex topology/manifest exists in-repo, so --live-codex takes an explicit path and fails closed (AC3 had assumed one shipped).
- (outcome) Grant granted 2026-08-14 and applied at kloc.toml:203 (xtask 37287->38609, formatted measured 37851 + 758 reserve); kloc-check xtask ok. demo-j1 SHIPPED green: exit 0, founder loop ~0.17s, 8 beats PROVEN_BLOCKING + 4 ABSENT, 11 tests, runbook. Eighth consecutive xtask re-base — D11 growth question still open, explicitly not settled by the grant.
- (moment) THE SHARPEST FIND OF THE SESSION: at 1a's commit 6827dc87 the two blocking gates were MUTUALLY EXCLUSIVE. check-j1-loopback-delegation's oracle hard-coded the single-line spelling of the ok_or_else chain, so it only passed on UNFORMATTED source — while cargo fmt --all --check (Blocking, added by E12 retro B4 so reflows red at their ORIGIN commit) demands formatted. Formatting mailbox.rs made the gate red against a fail-closed branch that was still there. Fixed by matching a whitespace-stripped structural skeleton; all 10 planted regressions still RED. Murat's line writes itself: a Blocking gate a formatter can flip is a false-alarm machine.
- (callback) Measured-at-HEAD-in-a-clean-worktree is now the room's reflex for 'is this mine?'. It proved 4 findings belong to 1a, not to the demo story: fmt gate red at HEAD, the gate/fmt mutual exclusion, THREE pre-existing kloc breaches (kernel-core +685 with its PIN green — anti-drift != anti-growth), and a 5/5-failing parallel test flake (MAOS_HOME env race). Also caught Main's own error: filtering the kloc table for '| xtask |' hid three other red crates for most of the session.
- (outcome) Owners decided 2026-08-14 and filed as D13-D16 in epic-14-preflight-decisions.md (the C2 register, because 'an owner must be a named story vehicle, never an epic key or a retrospective'). D13 kernel-core SPLIT: repair -> spec-epic-5-review-finding-closure (in-progress, caused it, holds the FLAG-Winston pin grant but never took the paired ceiling grant; its gate list runs fmt+tests but NOT kloc-check — that's the hole); instrument -> 14-6 via D11/C5, which may NOT grant it away under ZERO kernel-delta. D14 maos-domain -> 14-7 with an EXPLICIT AC expansion (cause is Story-3.3 halt growth). D15 maos-bin -> j1-crosshost-1b, one grant covering 1a's +33 and demo's +8. D16 suite-wide env-isolation -> 14-0 decomposes (D5/D6 precedent), owner Murat.
- (dynamic) The room's best move all session was REFUSING the obvious answer: both scouts said 'kernel-core -> 14-6, done'. Winston and Mary split it instead — hand a ZERO-delta story a breach it is FORBIDDEN to grant away and you've built a deadlock that keeps CI red forever. Repair owner vs instrument owner became the room's new distinction. Murat's F4 line: 'a flake you can only pass single-threaded is not a flake, it's a suite that never had isolation' — and --test-threads=1 is recorded as a masking workaround, never a resolution.
- (outcome) T0 boundary commit CLOSED 2026-08-15 as three commits on 6827dc87: 246660f9 fix(j1) gate repairs, 296aa2ce fix(maos-bin) drain fix, f792f77d feat(xtask) demo-j1. Each writes at most ONE shared file. No stub commit fabricated even though T0's text described one — a stub commit would be invented history.
- (dynamic) Verifying HEAD is not verifying the history. The first commit split looked perfect at HEAD — every gate green — but per-commit worktree checks showed fmt RED at the first two commits: the xtask/src/main.rs fmt repair had been bundled into the third commit, so the commit whose whole job was 'make the gates green' didn't. Rewrote the three unpushed commits; git diff proved the tree byte-identical, only layout changed. The same check exposed F1's undercount: SIX unformatted files at 6827dc87, not four, because the count had been taken from the files the fix touched under crates/ instead of from the gate's own Diff-in lines. Mary's filtered-measurement alarm fired on my own record this time.
- (outcome) Code review closed j1-demo 2026-08-15 -> done. Four parallel layers, ALL FOUR rated the change set incorrect. 14 patched, 1 recorded as a low limitation. The paid --live-codex path was non-functional four different ways because no test could reach it: verify-bundle without its required --pubkey, --live without --once (a successful paid take would hang forever), a repo-relative daemon path against a changed child cwd, and no exit-status or worker-identity check so a FIXTURE take could have earned PROVEN_LIVE_SIGNED.
- (dynamic) Murat's rule earned its keep twice in one day. First: verifying HEAD is not verifying the history (fmt was RED at two of my three commits). Second, and worse: I verified the FREE path and shipped a paid path no test could reach, so every blocker in the review clustered exactly where verification could not go. The Test-Infra layer then caught MY OWN F2 gate repair as bypassable - move the production fail-closed closure into #[cfg(test)] and the Blocking leg stays green - and the doc comment I wrote already CLAIMED it excluded test code while the code did not. A control that reports safety it does not have is worse than no control. Also: redaction_result was the literal string 'verified' inside an Ed25519-SIGNED capture with nothing checking it.
- (outcome) j1-crosshost-1b consensus: D15 grant set to maos-bin=16219 EXACT MEASURED, ZERO HEADROOM, over the formula's 16544. main.rs is 73% of the crate with no decomposition scheduled; tighter-than-formula is house style (j1-demo's grant brags about landing 4 lines tighter); maos-a2a-core 4654/4654 is the hard-fail-on-contact precedent. Dana's 'you're arming a trap for a stranger' died on kloc.toml:87 — ceilings must never block a correctness/compliance repair, cited by name in 13.5g.
- (moment by bmad-agent-ux-designer) Sally's dumb question went 5-for-5 and reframed the J1 gate: 'has this gate ever actually watched a frame get refused?' It hasn't and cannot — check-j1-loopback-delegation is a static source-text oracle that greps six files. The room re-labelled it a linter that checks the judge is still written down; Mary logged claim-vs-control #34. Consequence: the CI --test enrollment line is the ONLY link between linter and judge, and it had no falsifier until this session added proven-red vector #12.
- (moment by bmad-agent-analyst) Mary opened a NEW tally shape, distinct from claim-vs-control: 'a constraint we invented and then reasoned from.' The room burned 20 minutes on an org-chart tragedy — bridge stories are epic-orphaned, so the aggregate ceiling supposedly can never clear — before Amelia read kloc.toml:61 aloud: 'at an epic retrospective, OR under an explicitly authorized measured grant.' Two doors. j1-demo, itself epic-orphaned, had walked through the second one the day before. Winston had amplified the false constraint; he took the correction cleanly.
- (outcome) j1-crosshost-1b consensus: D15 grant = maos-bin 16219, EXACT MEASURED, ZERO HEADROOM, chosen over the formula's 16544. main.rs is 73% of the crate with no decomposition scheduled, so granted capacity is where the next 325 lines go; tighter-than-formula is house style (j1-demo's own grant brags about landing 4 lines tighter); maos-a2a-core 4654/4654 is the hard-fail-on-contact precedent. Dana's 'you're arming a trap for a stranger' died on kloc.toml:87 — a ceiling must never block a correctness/compliance repair, cited by name in 13.5g.
- (outcome by sec-hawk) j1-crosshost-1b REFUSES the _aggregate_hardfail grant deliberately (filed D17). Not because it cannot take it — it can — but because 1b's contribution is ZERO and granting it would green the CI signal holding D13's +685 to account. Vex: 'that's laundering.' Also established and worth reusing: re-basing a crate ceiling does NOT move the measured aggregate, so that key stays red even after D13/D14/D15 all land. It is the only instrument that catches distributed growth, and it is meant to sit red while debtors repair.
- (dynamic by shipper) Dana folded twice in one session and named it both times ('I fold when the evidence moves — that's not the same as being wrong'), extracting a real concession each time: put the kloc.toml:87 carve-out IN the annotation so the next author finds the release valve in ten seconds instead of arguing with a ghost for twenty minutes. Her losses keep improving the artifact.
- (callback by adversary) Grumbal, after agreeing with Winston twice in one week: 'Something is deeply wrong with this room.' Earlier the same session, when John collapsed AC3 into AC2/AC4 on his standing rule (every fix a constraint, never a new AC): 'Four ACs and it got STRONGER. I hate that that keeps happening.'
- (outcome by sec-hawk) Standing J1 residual sharpened: j1-crosshost-2 has a sprint-status row and NO story file, and it is now the intended address for TWO deferrals (peer-auth non-coverage + the deny-code conflation in map_a2a_error_to_iac_bus). The room refused to defer into a document that does not exist — D18 gets a real owner and the deadline 'before rung 2 writes its first line'. Vex: 'at some point deferred is just decided not to.' Whether rung 2 is authored now is back with Lunarpulse.
- (outcome by bmad-agent-architect) j1-crosshost-2a preflight (2026-08-16): the room found TWO ship-blockers inside the story's OWN machinery, both invisible to any single reader. (1) AC3.1 and AC4.1 contradicted each other -- AC3.1 says port manifest-codex.toml verbatim BECAUSE the signed T6 capture attests the argv, AC4.1 said land '-s workspace-write'; the ported file already carries '--sandbox workspace-write' and that exact long form is in the Ed25519-signed j1-tier2-capture.json. Same flag to codex, different bytes to argv_prefix_hash. Verb changed land->assert; Trap 22 forbids retyping a ported argv_prefix. (2) AC1.9's proven-red falsifier could not fire: check_j1_loopback_delegation governs six SOURCE files and discipline.yml is not one of them, so deleting the --test enrollment line leaves the gate green. Murat named the shape: 'a falsifier standing in for a falsifier' -- one level past claim-vs-control. Also corrected F6: 'MAOS passes no sandbox flag' is true of the tracked tree and FALSE of the signed run; AC4 now ratifies 'the FS jail is the adapter's, declared by MAOS in a hashed manifest, enforced by the adapter, not by MAOS' instead of confessing a gap it didn't have.
- (moment by bmad-agent-ux-designer) Sally went 6-for-6 and then 7-for-7 in the same session. First dumb question: 'where does the claude worker get its instructions from?' -- claude --help, --bare: skips hooks, LSP, plugin sync, auto-memory and CLAUDE.md auto-discovery. spawn_and_bridge sets no cwd, this repo has a tracked CLAUDE.md, so a signed claude run's behaviour depends on files in no manifest, no argv hash and possibly no git. --bare was filed under credential hygiene (AC2.3) and is actually load-bearing for AC3, the AC whose title is 'reproducible from the repo'. Second: 'codex gives you one object per line -- is claude's the same?' No: --output-format json is ONE object that may pretty-print across many TL rows, so a per-line parse turns a real claude success into a false negative -- F4's inversion through a door F4 didn't check. Join before you parse. The room now stops talking when she raises a hand.
- (outcome by sec-hawk) Vex walked on for the credential surface and found the sentence under the sentence: WorkerCli::nonsecret_env's doc comment says 'Credentials are injected separately, host-side' and there is NO host-side injection -- zero setters for CODEX_API_KEY/ANTHROPIC_API_KEY/OPENAI_API_KEY anywhere in crates/maos-bin/src, and the spawn is a bare Command::new with no env_clear. The operator exports the key into maos's shell and the child inherits it; MAOS neither injects it nor knows which key was used, while the SIGNED capture says 'injected host-side'. So refuse_ambient_auth refuses an ambient credential FILE while the same process hands the child every ambient credential VARIABLE by inheritance -- callback to his own 'a lock with the key taped to it'. env_clear is runtime.rs = kernel-core = FLAG-Winston, so 2a owes the honest sentence and nothing more. This also killed Q2: do NOT widen ambient_auth_path to Vec<PathBuf> -- it buys one surface of eight and implies the keychain was covered. --bare DELETES OAuth+keychain structurally ('OAuth and keychain are never read', verbatim) and --settings is argv-hashed, so MAOS owns the helper channel too.
- (dynamic by bmad-agent-analyst) Mary's tally hit 35 and then she refused to file it there: the AC1.9 defect is not 'a claim standing in for a control', it is a FALSIFIER standing in for a falsifier -- one level deeper, and a new shape for the register. Her other win was procedural and unpopular: F12's seven blocking gates blind to j1-* filenames had been DISCLOSED in prose by 1a, 1b, j1-demo and now 2a, and disclosed four times is not a disposition. Filed as D19 (owner 14-0, deadline before the next j1-* story leaves ready-for-dev); 2a's disposition downgraded to 'disclose AND cite D19'. John's standing rule held all session -- five forks closed, two ship-blockers repaired, four findings added, and the story is still 4 ACs because every fix landed as a constraint on an existing one.
- (outcome by bmad-agent-dev) j1-crosshost-2b/2c round-table 2026-08-16: Amelia's measurement collapsed FOUR ACs across two stories into one test, and the SAME measurement handed Vex a ship-blocker. deliver_typed writes Some(frame.frame_id) -- the RECEIVED id (maos-iac/src/adapter.rs:562) -- and maos_audit::query selects frame_id FIRST (maos-audit/src/lib.rs:194-196), so the two-host join key already crosses the wire and already lands in both bundles. 2b's AC3.1/3.2/3.3 (add a correlation field to maos-domain, RED -50; add a TL writer; join them) and 2c's AC2.3 (project correlation_id, budgeted against maos-audit's +22) all existed to build a thing that was finished before either story was authored. Then Vex: frame_id is BLOB NOT NULL PRIMARY KEY, PEER-SUPPLIED, deterministic seq||run_nonce with zero ULID entropy, and a failed write is panic! -- so a peer re-sending ONE frame halts host B, reachable the instant 2b adds the install_intake_sink call the whole story is about. G1 and the ship-blocker are one fact read twice. Fix matches ONLY the constraint-violation arm; every other write error keeps panicking (I2).
- (moment by bmad-agent-ux-designer) Sally went 7-for-7 and this time she changed what an AC MEANS rather than adding one. After Amelia showed both hosts write the SAME frame_id, Sally asked: 'if the ids are identical and the ids are the only thing joining them, how do you tell the two logs apart?' Nothing in the artifact does -- region can't (two hosts in one region derive the SAME signing key), boot_nonce is per-boot and T6 swept eight, attester_pubkey is bundle-supplied and R-RG1 forbids trusting it. So without 2c's host discriminator ONE host can forge BOTH halves of a 'two-host' bundle. Paige had written AC2.1 as 'add a host discriminator additively' -- a schema chore. It is an anti-forgery control, and it must be bound by the SIGNATURE, not just present in the JSON. John: 'that changed the meaning of an AC instead of adding one.' Sally: 'I just wanted to know which log was which.'
- (callback by bmad-agent-analyst) Mary noted the lane has now dissolved a constraint by measurement THREE times: worker_cli bin-private (the fix funded 2a), the D18 zero-headroom paradox (died when someone counted lines), and now the correlation-token ACs (the join key was already end-to-end). Murat refused to let it be a tally -- 'it's not a tally, it's the method working.' Also settled a live contamination hazard: a concurrent 2a dev session was mutating the tree DURING the scout run and produced THREE different maos-bin numbers in one session; two scouts concluded the D15 ceiling record was a broken instrument. Measured via git archive at clean HEAD, maos-bin is 16219 = the ceiling EXACTLY. New reflex, stronger than 'measure at HEAD': measure in a git archive extraction, because HEAD is not the tree when someone else is working.
- (outcome by bmad-agent-architect) j1-crosshost-1b RE-BASELINED 2026-08-16 at 0769869d, and the finding is the shape not the numbers: story 2a shipped AFTER 1b reached ready-for-dev and modified the same gate (219 code/2 legs -> 362/5), the same CI job, the same demo and three of the same ceilings. xtask headroom +534->+223, so AC3.1's '2.4x the whole gate module' inverts to 0.6x. maos-bin 16178/16219 RED -> 16260/16260 GREEN ZERO: D15's ratified number is SUPERSEDED by 2a's grant. maos-cli 4642/4642 is a third zero-headroom wall. _aggregate +492->+885 while the crate ceilings under it moved only +57 -- a second data point that re-basing a ceiling does not move the measured aggregate. Red keys four->THREE. Still 4 ACs; every fix landed as a constraint (John's rule, unbroken).
- (moment by bmad-agent-ux-designer) Sally went 8-for-8 and this time the dumb question found a SHIP-BLOCKER nobody was hunting. 'There are two proven-red files now, they both build a fake repo to run the gate against -- if 1b's leg reads a file neither fake repo contains, which fake repo is wrong?' BOTH. 2a had duplicated lay_green verbatim (1a:95, 2a:105), nine identical write_file calls, and read() (:111-128) pushes a Finding on a missing governed file -- so 1b's TENTH governed file, added to only the one file its AC names, reds the OTHER file's baseline_fixture_tree_is_green, at which point every planted vector in that file passes for the wrong reason while CI reports green. F3's exact failure mode through a door F3 did not know existed. AC2.4 rewritten: new per-story proven-red file plus repair ALL THREE trees; T7 re-ordered as a ship-blocker.
- (dynamic by bmad-agent-analyst) Mary's sharpest beat was turning the story's proudest sentence on itself. 1b argues at length that D18 must not be deferred 'against j1-crosshost-2, which has a sprint-status row and no story file -- you cannot defer into a document that does not exist' -- and then AC1.5/AC4.2 defer two non-coverage statements INTO the j1-crosshost-2 row, which no longer exists at all (split to 2a/2b/2c on 2026-08-15). Paige asked for a name because she had now seen it three times in this lane. New shape, distinct from 13.6's 'a residual stale at birth': **a residual that was TRUE and EXPIRED** -- checking harder at authoring would not have caught it, so the fix is not vigilance but pointing at something that cannot be renamed out from under you. Retargeted to 2b on the merits: it is where a second host first authenticates a peer, so AC1.5(a)'s 'a frame picks its own judge' becomes load-bearing there.
- (outcome by bmad-agent-test-architect) Q3 RATIFIED: DERIVE, do not append. 2a had already built the enrollment gate leg 1b's AC2.5 told the dev to build (leg_completion_vectors_enrolled :469, WORKFLOW const :72, job-scoped by 2a's own review after a workflow-wide scan stayed green on a --test line in a NON-BLOCKING job). So 1b's delta looked like one string appended to ENROLLED_TEST_TARGETS (:78). Yui killed it: a hand-maintained const list of things that must not be forgotten is smoke_cli_wrapper_8_12 with extra steps -- and 24 of the 30 files in crates/maos-bin/tests/ are invoked by NO ci job, that file still among them. An append fires THIS story's falsifier and leaves the next author one forgotten const from a dead test behind a green gate, inside the gate built to prevent exactly that. AC2.5 now replaces the const with a filesystem derivation plus its own falsifier: plant an unenrolled *_1b.rs, gate MUST red. A derivation that cannot be shown to notice a new file is a const list wearing a walk.
- (moment by bmad-agent-test-architect) Murat ate his own measurement. AC2.2's justification -- '16 of 68 check_*.rs gates carry a vacuity guard' -- came from grep -l 'vacuous|vacuity'. It is a TEXT match: it counts doc comments. Re-run at 0769869d it returns 19 of 68, and one of the three new hits is check_j1_loopback_delegation itself, which acquired the WORD from 2a's leg documentation and still has no mechanism. Mary filed it: a census that counts prose is a claim standing in for a control -- tally 37, and it was the room's own evidence. The substance survived and got stronger: gate_common.rs was re-checked function by function (seven exports, no ran, no checks, no per-leg record), and 2a's three legs shipped with no vacuity record either, so 1b is now first consumer across FIVE legs instead of two. Restated as what was verified by reading, never as a count.
- (outcome by bmad-agent-pm) Q4 closed: D19 does NOT block 1b, and the refusal is priced. D19 (seven blocking gates blind to j1-* filenames) was filed by 2a's round-table on 2026-08-16 with the deadline 'before the next j1-* story leaves ready-for-dev' -- one day AFTER 1b got there, and 1b is the only ready-for-dev j1-* story, with owner 14-0 not yet a story. Dana: stalling the lane's only unblocked story behind an unwritten instrument story is an outage, not a control. Mary's counter landed the price: disclosure was already the disposition for 1a, 1b, j1-demo and 2a, and the ONE case anyone audited -- 1a -- shows Test-Infra was omitted from its own A6 record, so 'a human ran it and wrote it down' was false in the only checked instance. AC4.1 now makes 1b record manually, by name, exactly what the seven blind gates would have checked: dev model, a DIFFERING reviewer model, all four A6 layers, the runtime-execution check, and the in-repo artifact path -- and cite D19 OPEN. Also corrected: the gap is SEVEN gates, and 1b's text said five.
- (outcome by bmad-code-review party) j1-crosshost-1b §A6 review round-table 2026-08-16: the ONE decision finding -- consent-leg needles satisfiable by string-literal/block-comment bait -- resolved UNANIMOUSLY 8/8 (Winston/Murat/Vex/Grumbal/Mary/Amelia/John/Paige) as accept-and-document, per spec + long-term correctness. Winston's kill shot: needles 3-4 EMBED string literals, so quote-stripping the skeleton breaks self-match and any in-story filter is the 246660f9 false-alarm class. Vex scoped the exploit: bait requires deliberate sabotage of committed, review-visible code; every accidental path (delete/weaken///-comment/reformat/relocate) is caught, P1+P3 close the rest. Murat's condition, Grumbal's too: acceptance is VOID if P1/P3 do not land, and it carries a tail -- the real fix (tokenizer/AST needles) is 14-6's instrument work with this gate named customer #1, because 'disclosed four times is not a disposition' (D19). Mary named the shape: Trap 24's precedent -- documented instrument limit with compensating controls named -- applied a second time. Amelia held the AC2.1 letter: bait is tampering, not deletion/weakening; the AC's bar is met by the patch set. Deliverable: one honest paragraph in leg_consent_refusal_proofs' doc block beside the production_before_tests caveat.
- (outcome by sec-hawk) j1-crosshost-2b preflight round-table 2026-08-16/17: the Vex-vs-Dana deadlock over D18's deadline was UNWINNABLE by argument and ended by measurement producing a THIRD thing. interpret_response (router.rs:892-1052) types NINE of SIXTEEN NACK codes and catch-alls the rest into A2AError::TransportFailed (:1049) -- and CODE_SPIRIT_RESTART_DETECTED is not among the nine. So host B's boot-nonce refusal, which PERMANENTLY invalidates the pin, reaches the operator as a TRANSIENT NETWORK FAULT and the implied action (retry) is the one action that can never work. Structurally unreachable in rung 1 (loopback stamps the boot_nonce=0 sentinel so router.rs:1123 never fires); reachable EXACTLY at 2b. Worse than D18: D18 makes a refusal illegible, this makes it legible and WRONG. Fix is 4 lines, zero new types -- A2AError::PinInvalidated already maps typed to CrossHostPinMismatch -- as a kloc.toml:87 correctness repair, with a binding scope wall: repair only the code this story makes reachable, record the 9-of-16 census, touch nothing else. Dana's principle upheld (2b does NOT owe D18: map_a2a_error_to_iac_bus is outbound-only, host B never calls it). Vex's category upheld (2b DOES create new flattened deny surface). Both conceded their framing.
- (moment by bmad-agent-ux-designer) Sally went 9-for-9 and this time the dumb question KILLED a ratified fix rather than adding to it. The room was designing how to re-key the boundary leg's needle so it could see a router fork; Sally asked 'after 2b, if I run the LOOPBACK topology, does the frame still pick its own judge?' It does -- LoopbackA2ARouter is untouched. So loopback_from_host_unverified is a claim about the LOOPBACK path, 2b does not verify the loopback path, and the honest value stays TRUE forever: AC2.3's ratified 'assert true -> false' asserts something that must never happen. The leg was MIS-specified, not under-specified, and its boundary-MOVED finding is wrong in BOTH directions (fires spuriously on replacement, silent on a fork). And no grep can fix it -- once install chooses at runtime, source text cannot know which arm ran, which is the limit already recorded against this gate ('a linter that checks the judge is still written down'). Resolution: SPLIT THE FACT -- static leg stays permanently/correctly true, and the cross-host claim is DERIVED FROM THE EXECUTED two-daemon proof, the way 1b routed disallowed-intent-refused-blocking.
- (outcome by bmad-agent-analyst) Two of the story's OWN re-baseline findings were disproved by the room, one of them ours. (1) H8 claimed the -32001 discriminator was dead because IntentDirection::Accept has zero construction sites -- true of router.rs:1676 (the mapping INSIDE the IntentDenied arm) but router.rs:1688 HARDCODES CrossHostIntentDirection::Accept, so the pair IS distinguishable and D18's register premise (a) is UPHELD. Mary's new line: 'a dead line inside a live discriminator is not a dead discriminator', tallied 38 and filed against ourselves, not the register. Also nobody had quoted iac_bus_types.rs:69-72: CrossHostRouteFailure is already marked DEPRECATED in-tree, so D18's fix was never 'add a variant to a RED crate', it is 'stop constructing a variant your own code deprecates'. (2) Q3: bilateral-2-host-mira-nash.toml's host-on-class-Spirit keys are NOT a forward declaration owned by 2b -- they are 1a's OWN priority_weight defect in 1a's OWN file, in the very pass that stripped priority_weight for exactly that reason (a key claiming behaviour the parser refuses). ADR-003 puts the bilateral join in A2APeerConfig, and j4-mira-nash.toml is the same pair without host and loads fine, so the keys are decorative. Strip 2 lines THEN widen the loadability control past the j1-founder-loop* stem -- in that order it lands GREEN and Dana's outage objection never materialises. Yui opened Q5, unowned: two topology files for the same Spirit pair.
- (dynamic by bmad-agent-test-architect) Murat named the through-line the room hit TWICE in one story: 'the story's deliverable is also its threat model.' The single install_intake_sink call 2b exists to add is what opens the duplicate-frame_id kernel panic (G16), and the second host 2b exists to build is what makes CODE_SPIRIT_RESTART_DETECTED reachable (H13). New failure shape, distinct from 'a residual stale at birth' and 'a residual that was TRUE and EXPIRED': A DEFECT THAT WAS UNREACHABLE, WHICH THE STORY THAT MAKES IT REACHABLE INHERITS. Rung 1 did not miss these -- rung 1 could not reach them. Consequence for scope discipline: the inheriting story repairs exactly the surface it opens and files a census for the rest, or a 4-line repair becomes a nine-arm refactor inside a frozen crate. All four resolutions landed as constraints on existing ACs; still 4 ACs, John's rule unbroken across the whole lane.
- (moment) Sally asked what stops one machine holding both keys: derive_team_signing_seed welds over derive_region_signing_seed and EVERY weld descends from ONE base_seed, so the ratified stage-3 host weld would have let one seed-holder legitimately sign BOTH halves of a two-host bundle. AC2.4 REVERSED to independent per-host roots. New shape: a control reused from a primitive built for the opposite job. Sally now 10-for-10.
- (outcome) j1-crosshost-2c round-table closed 9 calls per spec+long-term-correctness, 5 ACs held (John unbroken). AC5.1 REVERSED two-jobs -> ONE always-Blocking gate: Murat asked what substrate the paid-run job needs (operator+two hosts+funded key = never in CI) -> 'a gate whose substrate cannot exist is a monument, not a control'. :819 FIX not bound (same durability lie 2b fixed one door over) with invariant 'nothing is Duplicate until something is durable'. AC2.6 wire-or-delete (a schema with additionalProperties:false omitting 3 emitted fields is a false specification, not documentation - Paige). AC4.1 scan asserts BOTH classes. AC1.3 pubkey to stderr.
- (callback) Mary named shape #39 'a deadline whose successor was cancelled' - D19's deadline reads 'before the next j1-* story leaves ready-for-dev' but 2c is the lane closer and there IS no next one, so it binds here or never. D19 went option (a) unanimously; option (b) refused as 'converting a defect into a policy exactly as the defect expired'. Tally hit 40.
- (moment) Sally's closer became the round's through-line: 'Has a stranger ever checked one?' - no. So the signed artifact is a control whose consumer does not exist, and our own verify-bundle is a self-check. Ratified: the run is not complete until tools/verify-audit-bundle/verify.py (field-agnostic, verify.py:91-93) verifies the produced bundle. Paige found the twin.
- (dynamic) Winston overruled himself FIVE times in one session (Grumbal counting aloud); Grumbal walked on with 'a green preflight is a bug that hasn't introduced itself yet' and killed D19 option (b) by noting it would exempt the most security-critical lane from review discipline. Dana walked on when kloc.toml:87 was cited and ratified the maos-a2a-core grant: 'if the valve opened for H13 it is open for this, or the valve was never a rule, it was a mood.'
- (outcome) 2026-08-18 Lunarpulse RATIFIED the j1-crosshost-2d-paid-two-host-run row created by the round-table at 2c's review. Ratification authorizes the unit but does NOT make it actionable: per the lane convention a row with no story file is scope text only and cannot leave backlog. 2c stays at review with RF-0 filed (beat owner at demo_j1.rs:910 must re-point 2c -> 2d or a widowed control ships). The paid run still has not happened.
- (callback) Mary corrected herself in public: she framed 'Ownerless and open' as a novel failure, then measurement showed it is a STANDING CONVENTION in deferred-work.md (9 uses, predating the lane). The real finding was narrower - three NEW debts from the lane closer with no remedy attached. Tally correction stands; the room eats its own errors.
- (outcome) 2026-08-18 round-table #2 at 2c's review: RF-1 (money) — the judge does not publish its own admission criteria. Leg 9 needs 7 fields in two-host-capture.json; that contract lives only in the validator source + a fixture inside a proven-red test; the runbook says 'cp two-host-capture.json' without ever saying what is in it. Operator would run a paid agent, follow the runbook exactly, and be rejected AFTER the bill — AC1's failure shape one layer up. Fix is code-free and belongs to 2c: lift the fixture to a committed example, have leg 9's own test validate it, point the runbook at it.
- (callback) Mary: 2c's delivery is UNCOMMITTED at review — SECOND time this lane left a done/review story uncommitted, and the first time cost 2c a full five-scout re-baseline. Grumbal: 'You wrote the rule, applied it to your predecessor, and then didn't apply it to yourself.' Ratified process rule for the Epic-14 retro: a story does not enter review until it is committed.
- (moment) Verified good before finding faults: the dev implemented the round-table reversals STRONGER than specified — reconcile_two_host_bundles takes two keys and hard-refuses key_a==key_b with a typed SharedAttesterRoot error, and no host weld was added. AC2.4's reversal became a compiler-enforced control instead of prose. Sally's forgery question ended up in the type system.
- (callback) Sally again: 'Why are there two capture files at all?' — one fact (out-of-band trust anchor) carried under two names in two documents (two_host_trust_anchor in the Phase-4 CaptureDoc vs trust_anchor_established_out_of_band in leg 9's capture), no single source, so they can disagree and nothing catches it. Dana blocked merging the mechanisms: 2a's overclaim refusals are compiled in and must not be folded into a hand-written JSON.
- (outcome) 2026-08-18: RF-0+RF-1 applied and 2c COMMITTED as 8ca58b44 (tree clean, so blocking condition 1 is satisfied for the next story for the first time in this lane). RF-1's admissibility vector found RF-2 ON ITS FIRST RUN: the gate was UNSATISFIABLE - claim_scope had to match CLAIM_SCOPE verbatim while the overclaim scan rejected 'two operators' unless the literal 'not two operators' appeared, which it never did. No capture could EVER be admitted and no test asserted green-with-capture, so leg 9's success path had never been exercised; it would have failed after the agent was billed.
- (moment) The naive fix for RF-2 (negate each item explicitly in CLAIM_SCOPE) satisfied the tripwire and BROKE it - the negation check is global over the document, so any 'not two machines' anywhere disables the 'two machines' tripwire everywhere. an_overclaiming_capture_reds went red and said so. Landed fix is line-neutral, leaves the ratified wording untouched, and scopes the scan to operator-authored text since claim_scope is already byte-pinned. New shape: a control whose two requirements contradict each other, in the leg that guards the money.
