# Party Memlog — Installed BMAD roster (Winston · Murat · John · Amelia · Mary · Paige · Sally)

## 2026-07-03 — Story 11.3 preflight (Scale envelope — 25/30-host churn)

**Outcome: UNANIMOUS RESOLVED.** Winston · Murat · John · Amelia (+Mary walk-on on traceability). Session mode.

Biggest catch — **Murat found a spec defect the author missed**: AC2's "detection = first router NACK" is wrong for 2 of 3 adversary classes. Pin-spoof + cert-rotation-race die at the **mTLS handshake** (`verifier.rs`: WebPKI-then-pin), never reaching `handle_intake` — only a failed dial, no NACK frame. Ratified as **F-new: detection is TWO-SURFACE** (handshake `TcpTransportError` OR router NACK), read from the same `directed_dial_sweep` the blast-radius already uses. Amelia: "I'd have burned a day before I figured out the detector I need is a dial-failure timestamp."

Best clash — **F3 (one recovery axis or two).** John wanted one (`recovery_secs ≤24h`) — "on loopback both axes pass by six orders of magnitude, a second floor is surface without signal." Winston pushed back **against his own team**: the two numbers differ by *semantics* (adversary-contained vs mesh-healed), two *events*, and collapsing loses the ability to falsify them apart. Murat conceded ("that's a better argument than mine, damn it") and set the bar that resolved it: **two axes survive iff each has an independent falsifier** — isolation-blind reds only `rto_secs`, re-pin-blind reds only `recovery_secs`; else `rto_secs` is **cut by construction, no vote**. Decision defers to what the harness can prove. User was asked (product-ledger call) but away → proceeded on the self-limiting consensus, re-openable.

**F3-ledger round 2 (same day, user sent it back "resolve the remaining too"):** whether RTO is a *binding* v2.0 floor or *reported*. Winston flipped and argued **bind RTO, it's the operator's number** (degradation time > re-pin bookkeeping); John: bind only NFR-**named** floors, RTO is an epic footnote. Murat broke it: on loopback a real >4h RTO is **physically unobservable**, so a binding RTO floor is "a regression tooth wearing a guarantee's badge" — recovery gets the badge only because it's NFR-named (equally loopback-trivial, but traceable). Amelia found the thread: it's **one line of code** — does `rto_secs>14400` fail the leg or print an advisory. Landed: **recovery BINDS, RTO derived+reported+advisory-if-breached (own falsifier), promotes-to-binding at v2.5 when geo makes >4h observable.** Winston's concession bought with the loud-advisory + written graduation clause (no footnote). "One line of code and we argued for ten minutes. Worth it." — Amelia.

Running discipline this room keeps applying: **every count needs a per-item blind that reds it** — Winston named it as one pattern at three places (blind a detector class → detection reds; blind isolation → RTO reds; blind re-pin → recovery reds). That IS the adversarial-host-identity reflex the epic asked for.

Other ratified: F1 in-process real-socket real-mTLS (epic's "container" prose = aspirational, reconciled to 10.4b — Winston's ADR-049 "sealed-export" analogue again); F2 N=30/floor≥25; F4 orchestrator in `maos-a2a-tcp` (compiler-forced — test-support modules aren't cross-crate importable); F5 atomic delete of the canned scaffold same-PR; F6 per-class observation-seam blind at BOTH surfaces (never gate `verifier.rs`/`router`); F7 one `check-scale-churn` gate {advisory,advisory,blocking}, no ADR. Mary walk-on: gate → **NFR-Rel-7 primary + NFR-Scale-2 anchor**, both cited ("and/or traces to nothing"). Murat's non-negotiable: **§A6 runs the drill LIVE at N=30 and watches the falsifier red the count** — the net that caught 11.2a's vacuous-count P1.

Relationship notes: Winston is the ceiling/kernel-Δ conscience but keeps also being the one who argues the *semantic* correctness case (region-identity, now recovery-vs-RTO). Murat's stance is "the better the preflight looks, the harder I hunt." John's the eviction/traceability enforcer — "closable, traces, self-corrects" is his sign-off phrase. Amelia ratifies by the compiler, not by preference.

## 2026-07-05 — Story 11.4a preflight (Enterprise PDP integration)

**Outcome: UNANIMOUS RESOLVED.** Winston · Murat · John · Amelia (+Mary walk-on on coverage-matrix traceability). Session mode. First story where the room **killed the author's headline lean.**

**Biggest catch — Winston knifed his own author's ZERO-Δ lean, on correctness not taste.** The story leaned "materialize PDP decisions into the CoW `PolicyTableInner` via `PolicyTable::update()`, hold ZERO delta." Winston: expressing an org *forbid* by withholding a scope from `manifest_scopes` (the ABI **ceiling**) does three unforgivable things — (1) **Murat's fuse**: forbid-as-`per_capability_approval` becomes a **latent auth bypass** the day the auto-allowing `ApprovalManager` (`approval.rs:74`) gets a real prompt (corporate-forbidden cap → locally-approvable); (2) overwriting `manifest_scopes` makes an out-of-kernel adapter the **ceiling authority** — inverts I1, and destroys the audit distinction *manifest-gap vs org-forbid* (the enterprise's whole value); (3) subtraction can't express Cedar's **forbid-beats-permit override**. RESOLVED: a **bounded FLAG-Winston `per_capability_deny`** field + one deny-wins `evaluate` arm (~+15-25 LOC, re-pin from 23023). John held the line first ("epic ratified ZERO, *flat*, not FLAG-Winston-if-needed like 2a/4b") then conceded via pre-dev-checklist item-2 ("bound the FLAG-Winston surface at preflight") once the fuse bound it as *correctness*. Amelia bounded it: 20 lines, deny-wins arm before the grant checks, revert-or-disclose outside the named surface. **The epic's flat ZERO did not survive contact — overruled on long-term-correctness evidence.** (This is the new precedent: a ratified ZERO is re-openable at preflight when correctness forces a bounded, nameable re-pin.)

**F1 — opus-4-8, third story running.** Murat: "the canned-green story of the epic" — a stubbed PDP returning canned `Allow` is indistinguishable from a real one until the policy swaps; opus-4-6 shipped the vacuous-count P1 on 11.2a, which escalated 2b AND 3. John flagged it's now a **standing escalation trigger**: opus-4-6+§A6 on a security-adjacent story = auto-escalate (nobody's written it down; three-for-three). §A6 must run the deny **live over real Cedar and watch `pdp-fault-inject` red it.**

**F3/F5 — Cedar in-process, Murat's load-bearing reason.** Not "pure Rust, no server" — in-process makes the deny tripwire a **real per-commit gate** instead of advisory-skipped like `check-multi-region-slo`/`check-scale-churn`'s Postgres legs (CI never provisions them → teeth only on-demand). Cedar `permit`/`forbid` maps 1:1 onto the F2 deny layer. Port engine-agnostic; OPA/Vault additive-per-port (ADR-010), out of 4a.

**F4 — Grumbal earned his keep: the stale-permit hole.** "Everybody says fail-closed and nobody handles the case where closed means keep serving the access you were supposed to revoke." Freeze-last-known-good handles startup/runtime-drop but freezes a *revoked* permit forever. Winston added the **staleness TTL**: after T unreachable, PDP-granted caps revert to deny. FOUR distinct proven-reds — no-config→default / configured-down-at-startup→closed-**LOUD** (the one that looks like AC1 and isn't) / runtime-drop→frozen-not-relaxed / TTL-expiry→revert.

**CATCH-B — John's eviction instinct held the AC count at 5.** Murat: "an org denial you can't audit is half a feature." John: "this is where 4a metastasizes into 4c (SIEM, NFR-Aud-11)." Winston split it by cost — Task 0 traces whether `issue_with_mediation` audits denials: YES → rich attribution is 4c; NO → the deny arm emits a denial entry via the EXISTING cap-audit channel in the SAME re-pin surface (no new call site). A **constraint on AC3, not a 6th AC**. Murat: "silent denial is not recoverable, under-attributed is."

Running discipline reaffirmed: **every count needs a per-item blind that reds it** (blind deny arm→deny leg reds; blind provenance→provenance leg reds; stub engine→swap test reds). Murat sharpened AC2: the policy-swap must force **TWO DISTINCT engine evaluations** (a memoizing cache passes a naive swap = the exact fake). F6: NFR-Sec-17 (security floor, not compliance feature) + ADR-050 lead-with-it binding-v2.0. Mary: gate traces **primary NFR-Sec-17 + enterprise-deployment anchor**, both cited "or it traces to nothing." F7: one optional opaque `principal_attributes` for 4c, don't model SSO now.

Relationship beat: Winston again argued *semantic* correctness against his own ZERO-Δ/ceiling conscience (region-identity → recovery-vs-RTO → now the deny-ceiling) — and this time **overruled his OWN author**. John held the AC count *and* the ZERO line until correctness bound it, then wrote the graduation clause. Amelia ratifies by LOC + the compiler ("if the fault-inject can't move the number, the probe's a constant and the gate's a lie"). Grumbal earns his keep once a session with the hole nobody else names.
