---
baseline_commit: 652347b8
---

# Story 13.3: Cross-team asymmetric consent + the governed cross-team row

Status: **ready-for-dev** — party-mode preflighted 2026-07-18 (Winston · Murat · John · Amelia · Mary + Vex/Grumbal/Dana/Yui/Boundary/Wildcard/Splinter/Killjoy, Lunarpulse sign-off), **7 scouts across two rounds**. Round 1: 4 scouts + an independent validator disproved 11 epic-sketch premises (D1–D11) and found 5 critical regressions in the draft itself. Round 2 (*"resolve for long-term correctness"*): 3 more scouts **broke the story's own headline claim** — there is **no production initiator for a cross-team crossing at HEAD, and the 12.4a precedent it would copy is itself half-wired** (D12). **H1 RATIFIED (split → 13.3b)**; **headline reframed to the sentence the code supports: *the row is production-real; the crossing is not***; the seal gap and the V3 downgrade hole given named owners. Still **6 ACs**, ZERO kernel-core Δ @23202 (pin == actual verified), no new crate.

<!-- Epic 13 — Reza Single-Org Cross-Team Cortex (v2.2). THIRD story of Epic 13. Depends on 13.2 (crypto boundary + source_team, done @652347b8) and Epic 12 (12.2 consent tuples). 13.5b (collective erasure) consumes this story's persisted source_team; 13.5c (production routing + refusal audit) consumes its taxonomy; 13.6 judges the composition. -->
<!-- Model/review: frontier-class dev allowlist {opus-4-8, gpt-5.5, glm-5.2, equiv} (E11 retro A1 / E12-B3). §A6 full-layer net (Blind + Edge + Acceptance + Test-Infra + runtime) NON-DEGRADABLE per E12-B6 — 13.3 is the story where cross-team rows FIRST EXIST in production, so a claimed-but-unenforced consent check IS a cross-team data leak. -->
<!-- Kernel baseline 23202 at HEAD 652347b8 — pin == actual VERIFIED (`find crates/maos-kernel-core/src -name '*.rs' -exec cat {} + | wc -l` = 23202). ZERO-Δ is a per-story RESULT to re-verify at CATCH-0, not an epic premise. -->
<!-- Grounded by 4 code-verification scouts (2026-07-18): (1) the cross-team write path post-13.2, (2) Epic-12 consent machinery + manifest schema, (3) I11 provenance + log.recall, (4) the gate/tests/CI/baseline/catalog surfaces. Every premise below was checked against code at HEAD before being kept; the disproofs are recorded verbatim in "What the scouts disproved". -->

## What this story is, in one paragraph

13.1 built the **physical** wall (database-per-team, presence-only `team_guard`). 13.2 built the **cryptographic** wall at **entry** (per-team HKDF weld; a forged same-region cross-team bundle is refused at `verify`, so it never lands). Both are **mechanism stories proven at a seam** — and at HEAD the wall they built is *inert in every bootable configuration*: `MAOS_LOOM_HOME_TEAM` is a hard boot failure (D11), and the whole replication path has zero production callers (D9).

**13.3 separates two objects the epic sketch conflated: the ROW and the CROSSING.**

- **The row is production-real in 13.3.** `source_team` genuinely persisted through `write_with_source` (closing D8 — today `apply` *discards* it), per-row attestation persisted, the read path verifying it, the clobber hazard closed, the refusal taxonomy widened. All of that lands on `store.rs`'s **actual** `read`/`scan`/`write` — the production path, not a harness — and goes live the instant team mode becomes bootable. Neither 13.1 nor 13.2 can say that.
- **The crossing is NOT production-real, and 13.3 says so.** Initiation, transport, and the consent decision need a daemon holding both a manifest and a store; **no such daemon exists** (D12), and joining them is assigned verbatim to 13.5c by ADR-055 §5. 13.3 builds the crossing **mechanism**, proves it at the verify/apply seam, and ships a **dead-wire negative that stays red until 13.5c wires it**.

It does **not** build multi-hop distillation provenance or cross-wall raw traceback (H1 → **13.3b**), collective erasure (13.5b), or multi-store routing / refusal audit / TL isolation (13.5c).

## Story

As a **MAOS operator running Reza's 400-person fintech as one governed Cortex (teams `security`, `support`, `data` — normally sharing a region)**,
I want **a cross-team share to be a real, consented, re-attested write into the other team's database — authorized by an ordered-pair `(from_team → to_team, intent)` grant in the signed org manifest so that A sharing with B never implies B may share with A, landing a row whose `source_team` and attestation are cryptographically bound and persisted, and refused with a caller-legible typed outcome that names the responsible authority when consent, freshness, or attestation fails**,
so that **cross-team collaboration exists as a governed production path rather than a test-only seam — and the three things that would otherwise silently defeat it (an unconsented reverse share, a forged foreign-team row already resident in the store, and a refusal that reaches the caller as an opaque `Transport(reason)` string) are each closed by construction and proven red.**

---

## What the scouts disproved (read this before touching the epic sketch)

The epic's AC sketch (`epic-13-reza-cortex-v2-2.md:67-73`) was written against `§15.3`, which was itself a **draft response to a rubric finding**, not a design validated against code. Eleven load-bearing premises are false at HEAD:

| # | Epic/§15.3 premise | Code at HEAD `652347b8` | Consequence |
|---|---|---|---|
| **D1** | *"a cross-team distillate carries its flattened I11 chain inside the re-attested crossing bundle"* | `CollectiveKvLeaf` (`replication/leaf.rs:61-78`) and `CrossRegionReplicationBundle` (`replication/bundle.rs:66-80`) have **no `source_log_ref`, no `distillation_depth`, no `intent_lineage`**. There is no wire representation. | Requires a **leaf v3** with the same byte-compat obligation 13.2 carried for v2. → **13.3b** (H1) |
| **D2** | *"ordinary traceback dereferences within the consumer team's own database"* | `flatten_source_log_ref` (`maos-iac/src/adapter/distillate.rs:196-253`) walks **`self.transparency_log`** — one local SQLite TL. No store selector, no team/region parameter, no remote fetch. Unresolvable ref → `SourceFrameNotFound`. | Cross-wall flattening **does not exist**. → **13.3b** |
| **D3** | *(implicit)* cross-team citation is possible | `distillate.rs:330-359` (Story 8.10 AC2b) rejects `CiterUnauthorized` whenever `e.spirit_pid != spirit_pid`, **over the flattened refs**, with the comment *"so a digest-of-digest cannot launder a cross-principal raw frame through an intermediate hop."* | 13.3's provenance ask is **exactly what this check was built to prevent**. Needs ratification, never incidental relaxation. → **13.3b** (H1) |
| **D4** | *"an ADR-012-consented `log.recall`"* | ADR-012 (`docs/adr/ADR-012-*.md:12`) binds **cross-Host frame admission** on `(peer-identity, intent-class)` — write-side. It says nothing about reads, recall, or teams. `LogRecallPort` (`maos-domain/src/ports/log_recall.rs:14-28`) is **2 methods, `spirit_pid`-only**; `recall` hard-pins `spirit_pid: Some(spirit_pid)` (`log_recall.rs:282`, emitter-scope); the consent check is **commented out** (`log_recall.rs:378-384`; `:386` calls it *"a no-op pass-through at v0.3-β"*); `LogRecallError` has **no refusal variant**. | This **invents new read-consent semantics**. Needs an ADR + a port signature change. → **13.3b** |
| **D5** | *"per ADR-014/018"* | **`ADR-018` does not exist** in `docs/adr/` (37 files; index jumps 012→014). **`ADR-013` (the `log.recall` authority cited by `prd/domain-specific-requirements.md:9`) does not exist either.** Both live only as planning-artifact prose. | Half the citation resolves to nothing. Port or drop. → **13.3b** |
| **D6** | *"per-(peer,role) consent tuples extended to the team axis"* expresses asymmetry | `ConsentTuple { peer, role, intent }` (`maos-cohort/src/manifest.rs:136-145`) names **ONE endpoint**. Direction comes from *which table* it sits in (`send`/`accept`, `consent.rs:43,71`), not from an ordered pair. `send_admits(peer=host-b, …)` grants **every** member the right to send to host-b. | Adding a `team` field yields *"anyone may send to team-b"*, **not** *"team-a may send to team-b"*. Asymmetry needs a **second endpoint field** — this is AC2's whole design problem. |
| **D7** | *"`source_team` provenance for the forget-cascade"* | `CollectiveMemoryPort` (`maos-domain/src/ports/collective_memory.rs:60-97`) has **3 methods, no erase**. Kernel `forget`/`forget_with_reason` never reaches `MemoryTier::Collective`. The gate names `13.5b collective GDPR erase/legal-hold cascade` as an absent successor. | **There is no cascade to feed.** 13.3 may claim `source_team` is *persisted and bound*, never that a cascade consumes it. |
| **D8** | `source_team` is written by the crossing | The column exists (`schema.rs:61`), the leaf field exists (`leaf.rs:77`), the bundle field exists (`bundle.rs:78`), the **read** exists (`store.rs:612-618`) — but `write_with_source` (`store.rs:281-290`) **has no `source_team` parameter**, and its INSERT (`store.rs:313-327`) omits the column from both the column list and the `DO UPDATE SET`. `apply_verified_replication_bundle` (`bundle.rs:427-438`) **silently discards `leaf.source_team`**. | **This is the concrete hole 13.3 closes** (AC1). |
| **D9** | the replication path is live | `build`/`verify`/`apply_replication_bundle` have **ZERO production callers** — re-verified exhaustively at HEAD. Every call site is under `#[cfg(test)]` (boundary `bundle.rs:532`) or in `tests/`. | 13.3 **wires up genuinely dead code**. Every "it already works" intuition is false. |
| **D10** | `maos-tenant-map` is a crate (`epic-13:53`) | **No such crate.** 13.1 rejected it (F21). Trait: `maos-loom-lite/src/tenant.rs:22-26`. Impl: `maos-bin/src/tenant_map.rs:105`. Errors are `StoreError::Tenant*` (`store.rs:79-84`), **not** `ETenant*` (zero hits repo-wide). | Use the real names. |
| **D11** | tenant mode boots | `main.rs:2286` calls `tenant_map_for_store(&home_team, **None**)` → `Err(SourceUnavailable)` for any non-empty `home_team`. **Setting `MAOS_LOOM_HOME_TEAM` today is an unconditional boot failure**, by deliberate 13.1 design (13.5c owns refresh wiring). | 13.3's "production path" is proven at the **crossing seam with an injected consent port**, exactly as 13.1/13.2 proved theirs. Do **not** try to make the daemon boot in tenant mode — that is 13.5c. |

### Round 2 — three more scouts, and they broke the story's own headline

| # | Round-1 premise (this story's, not the epic's) | Code at HEAD | Consequence |
|---|---|---|---|
| **D12** | *"13.3 builds the production cross-team write path"* — the promise **13.2 Option A was ratified on** | **There is no production initiator, and the 12.4a precedent is itself half-wired.** `CohortDigestDistributor::request_read` (`maos-cohort/src/digest.rs:176`) has zero production callers; its only non-test caller `DigestSpirit::request_consented_summary` (`spirits/digest/src/lib.rs:71`) has **zero callers anywhere, tests included**; production loads `DigestSpirit::default()` (`main.rs:3266,4143`) so `cohort_state == None` and `refresh_consented_summaries` returns at `lib.rs:85` — **the production Digest Spirit is a no-op**. Primary daemon holds the loom, no cohort state, refuses team-mode boot (`main.rs:2286`). Cohort daemon holds manifest + lease + both consent seams + a real tick loop (`main.rs:8095-8118`) but **no store — `CohortDaemonFileConfig` has no connection string** (`main.rs:7921-7932`). `WriteEntryPoint::CrossRegionReadmit` has **zero constructors**, and `apply_verified_replication_bundle` calls `write_with_source` directly (`bundle.rs:428`), bypassing the kernel chokepoint entirely. | **The headline claim is false.** Reframed: the **row** is production-real, the **crossing** is not. Joining the two daemons is ADR-055 §5's verbatim assignment to **13.5c**. |
| **D13** | *"the read path verifies attestation"* | **The at-rest seal is UNPAIRED at HEAD — a live defect in shipped 11.4c work.** `store.rs:303` seals every write; `open_at_rest` (`maos-secrets/src/lib.rs:126`) has **zero callers outside its own crate's tests**. `seal.rs:118` claims the unseal *"lives at the composition root"*; `main.rs:2302` wires the hook **in** and never wires one **out**. So `read()` returns ciphertext into `parts_to_value` → `StoreError::Serialization` under any configured posture. The seal is also **non-deterministic** (`random_data_key()` + `random_nonce()`), and apply re-seals: `db.value_data = seal(vtp(ptv(leaf.value_data)))` ≠ `leaf.value_data`. | **Re-hashing `value_data` at read is impossible** under a seal, and unstable without one. → Yui's `canonical_hash` design (AC3). Gap **owned by 13.5a**. |
| **D14** | *(unnoticed)* the manifest downgrade refusal is a floor | **It is a hardcoded PAIR.** `state.rs:273`: `cached.schema_version == COHORT_SCHEMA_V2 && candidate.schema_version == COHORT_SCHEMA_V1`. Add V3 and **cached=V3 / candidate=V2 with a higher revision is silently ACCEPTED** — the cross-team consent block evaporates from the enforced manifest; cached=V3 / candidate=V1 is also accepted, `teams` → `None`, and `tenant_map.rs:85` then returns `Stale` on every call (tenant wall → fail-closed outage). ADR-055 §2 says **floor** (*"any v1 reissue is rejected … even when its manifest revision is higher"*); the code says pair. Two more: `tenant_map.rs:85` and `:168` are `!= COHORT_SCHEMA_V2` and would reject a valid V3. | **A signed-artifact downgrade hole that V3 would arm.** Fixed as floors in a **separate provable change before V3 exists** (Task 1). |
| **D15** | *(unnoticed)* v2's canonical bytes are protected | **Nothing in the repo can detect a v2 encoding change.** There is no v2 golden. Every v2 test — `both_schemas_round_trip_and_verify_with_distinct_domains` (`:1419`), `v2_canonicalization_sorts_teams_and_members_without_mutation` (`:1464`), `every_v2_team_field_is_signature_bound_and_domains_do_not_cross` (`:1545`) — is **build-verify symmetric** or relative-invariant, so field order, a dropped length prefix, or a changed sort key are all invisible. | **Freeze a v2 golden FIRST**, before anything touches `to_canonical_bytes` — true regardless of V3. |
| **D16** | *this story's own V3 justification*: *"extending V2 would invalidate every already-signed v2 manifest"* | **FALSE — there are zero.** `grep -rln "reserved_intents\|cohort_id" --include="*.toml"` returns nothing; every cohort manifest in the suite is built in Rust and serialized in-memory. 13.1's F4 finding survives v2. | **Right answer, wrong reason.** V3 still wins on the correct argument: `SIG_DOMAIN_V2` is pinned, and silently changing which bytes a pinned domain covers **while the domain string stays constant** is the one mutation a signature scheme cannot express. |

**Two additional hazards the scouts surfaced that are not in any sketch:**

- **H-CLOBBER (ship-blocker class).** `collective_memory`'s UNIQUE key is `(spirit_pid, namespace_kind, namespace_detail, key)` — **`source_team` is deliberately not in it** (`schema.rs:63`). The `ON CONFLICT … DO UPDATE` LWW clause resolves on `(source_ts, source_region)` only (`store.rs:317-327`). So a cross-team copy landing in team B's database with the same `(pid, ns, key)` as a team-B **first-party** row will **overwrite it** whenever its `source_ts` is newer. Cross-team sharing as designed can silently clobber the destination team's own data. AC1 must resolve this explicitly (H4).
- **`ranged_recall` is the path of least resistance and it is a trap.** `maos-audit::log_composition::ranged_recall` (`:104`) is path-addressed, capability-free, and returns everything when `spirit_filter = None`. `spirits/researcher` pins it out **at compile time** by carrying no `maos-audit` dependency (`researcher/tests/recall_walker.rs:9-10`). Reaching for it to implement cross-team reads would silently demolish that boundary. Forbidden here and in 13.3b.

---

## Design forks for preflight (H-numbered; grounded recommendations the ACs assume)

### Preflight resolution record (party-mode 2026-07-18 — Winston · Murat · John · Amelia · Mary + Vex · Grumbal · Dana · Yui · Boundary · Wildcard · Splinter · Killjoy; Lunarpulse sign-off)

Discipline: E12-B6 *"a claim standing in for a control"*, non-degradable on 13.1–13.3. **Mary's running tally reached 17** — the majority of them a doc comment promising a mechanism at a location that is empty (`for_spirit.rs`, `build_kernel_ctx`, `gate_common.rs`, now `seal.rs:118`). Standing rule reaffirmed: **read the build, not the prose.**

- **DECISION 1 — H1 RATIFIED (split stands).** Boundary proposed a middle (give the cohort daemon a store → reach 12.4a-parity, "production answers, tests ask") and **withdrew it on the spec**: ADR-055 §5 says *"Joining that source to production team-store routing is **explicitly Story 13.5c**"* — the middle **is** 13.5c's scope, word for word. Splinter's re-sequence (move 13.5c forward) died on Amelia's cycle: 13.5c depends on 13.1 + 13.3 + 13.5a. Murat's closer: 12.4a's own initiator is dead code, so the middle would build 13.5c's foundation badly, in the wrong story, to reach parity with a precedent that doesn't work.
- **DECISION 1b — SPLINTER'S CARRY (not 13.3's; escalated to Lunarpulse).** *"Four mechanism stories and every door piled into 13.5c — reading me an ADR that says the door is in 13.5c doesn't answer the risk, it **is** the risk."* 13.5c currently owns 7 distinct concerns (multi-store routing, Spirit mediation, refresh wiring, refusal audit, TL isolation, cross-team correlation, the unscoped daemon-merge) **and is the only story in Epic 13 with no ZERO-Δ claim.** Ratified ask: **13.5c gets a scoping pass before 13.3 development starts.**
- **DECISION 2 — headline reframed + dead-wire negative.** Winston conceded his own 13.2 sentence: *"the two objects are the row and the crossing, and we've been calling them one thing."* 13.3 ships on **"the row is production-real; the crossing is not."** Murat bound it to evidence: row legs `PROVEN_BLOCKING`/`PROVEN_LIVE_SIGNED`; crossing legs proven at the seam with honest states; **plus a dead-wire negative that proves the crossing is unwired and flips green only at 13.5c** (the 13.5a AC3 idiom). Mary: *"seventeen claims standing in for controls — a dead-wire negative is a **control standing guard over a claim**; it inverts the pattern."* Vex: it is the only thing in the session that would have caught 12.4a. **Wildcard's question — "why is this only in 13.3?" — went unanswered; Dana ruled forward-only (don't reopen shipped stories) and it is logged as an Epic-13 retro item.**
- **DECISION 3 — the seal: scope + owner, not a fix here.** Vex pushed to fix it and **took an owner instead**. Three parts: (i) AC3 states its validity posture out loud in the may-not-claim table — read-time verification is defined for the plaintext posture, inert under a configured seal pending the unseal; (ii) the unpaired-unseal gap is filed with **13.5a named as owner** (ADR-051 owns at-rest; 13.5a AC2 is the first story that exercises it end-to-end) — explicitly **not** left unsuccessored, the 13.2 region-residual lesson; (iii) Yui's `canonical_hash` design makes the attestation **seal-independent by construction**, so this is a scoping statement, not a dependency.
- **Settled without a vote (4):** (a) persist the leaf's **`canonical_hash`** (32 B) rather than re-hashing `value_data` at read; (b) **the root's *signature* is the control**, not the proof — root+proof+row all live in the same table, and `verify_proof` returns `true` when `siblings.is_empty() && leaf == root` (`merkle.rs:192`), so an attacker who writes the row sets `root := leaf_hash, proof := empty` and walks through (**Vex: "the registry again — a lock with the key taped to it," third occurrence**); fix is the shipped `derive_team_pubkey` (`sealed_export.rs:88`) with a composition-injected `HashMap<(Region,TeamId),[u8;32]>`, **seed never below the port** (G4); (c) the three hardcoded-`COHORT_SCHEMA_V2` comparisons become **floors in a separate provable change before V3 exists**; (d) **freeze the v2 golden first** — nothing else may touch `to_canonical_bytes` until it exists.
- **Scope after preflight: still 6 ACs** — the reframe *shrinks* AC1's claim and adds one gate leg, not an AC. John, ninth story running: *"every fix a constraint, never a new AC."*

---

**H1 — SCOPE SPLIT. ✅ RATIFIED 2026-07-18.** The epic's six ACs are **two stories**:
- **13.3 (this file)** — *the governed cross-team row*: production cross-team write, asymmetric consent, per-row attestation + read-time team-axis verification, widened taxonomy, gate, ZERO-Δ. **6 ACs, all grounded in code that exists.**
- **13.3b (new)** — *provenance crosses the wall*: leaf **v3** carrying the flattened I11 chain, the **D3 citer-auth cross-team ratification**, consented cross-wall `log.recall` with first-class refusal, and authoring **ADR-058** (+ porting ADR-013/ADR-018 into `docs/adr/`, D5). **~5 ACs.**

*Why split rather than compress:* (i) 13.3's retained half is already 6 ACs before any provenance work; (ii) AC3/AC4 have **no ratified §15 home** — the architecture's own rubric review (`review-rubric-reconcile.md:62`) records *"NO §15 HOME — the flagged question, answer: no, nowhere … This is the single most important reconciliation failure"* and offers the exact unresolved fork (*"Either the provenance read path crosses the guard with re-attestation (needs design + a gate clause), or the flattened refs are unresolvable cross-team and the Reza 4:55 PM scene does not reproduce"*); (iii) D3 requires reversing a shipped security control — an ADR-grade decision, not a story task; (iv) the epic's own pre-dev checklist item 5 mandates exactly this (*"If grounding reveals another independently shippable mechanism, split it rather than hiding implementation"*). **13.6 depends on 13.3b, so 13.3b must land before the journey closer — the split defers nothing off the epic.**
*If H1 is rejected:* the AC set must be **re-cut**, not crammed — and 13.3 then owns an ADR-grade reversal of Story 8.10's citer-auth control.

**H2 — where cross-team consent is decided and enforced.** The A2A router's cohort-consent chokepoint is **grep-pinned on two counts** (`maos-a2a-core/tests/cohort_consent_chokepoint_12_2.rs:12-26` — `consent_decision(` == 1, `cohort_consent_decision(` == 3), and 13.1 established that **`maos-loom-lite` and `maos-cohort` gain no dependency edge** (ADR-055 Consequences). **Recommend: a new `CrossTeamConsentPort` trait in `maos-loom-lite` (mirroring `TenantMapPort`, `tenant.rs:22-26`), implemented in `maos-bin` over `CohortManifestState`, injected at composition — consent is decided at the *crossing/apply* seam, not in the A2A router.** Rationale: the router governs *frame delivery*; this governs *row landing*. Different seam ⇒ no chokepoint violation, no new crate edge, and the check sits where the row actually lands (spoof-proof site — the 12.3-P5r idiom). **Reject** adding a second `consent_decision` call in `router.rs` (reds the chokepoint test) and **reject** a direct loom-lite→cohort dependency.

**H3 — how asymmetry is expressed in the manifest (D6 is the constraint).** `ConsentTuple` cannot carry it. **Recommend: a new `[[cross_team_consent]]` table of ordered triples `{from_team: TeamId, to_team: TeamId, intent: String}` under a new `COHORT_SCHEMA_V3`**, copying the shipped V1/V2 dispatch idiom verbatim (`manifest.rs:51-58`, `:191-254`): V1 and **V2 canonical bodies both stay byte-frozen**, V3 signs under `SIG_DOMAIN_V3` and appends the cross-team block after the teams block. Asymmetry is then **structural** — `(A,B,share)` is a distinct row from `(B,A,share)`, so the reverse share is refused by *absence*, which is the strongest possible form. Validation mirrors `validate_team_map`/`validate_consent_table` (`manifest.rs:551-648`): both teams must exist in `[[teams]]`, `from_team != to_team`, `intent` canonical (`A2AIntent::is_canonical`), duplicates rejected.
*Settled at preflight — V3, but the story's original reason was FALSE (D16).* "Extending V2 would invalidate every already-signed v2 manifest" is wrong: **there are zero committed cohort manifests** (verified; 13.1's F4 finding survives v2). **The correct argument, which holds at zero deployment:** `SIG_DOMAIN_V2` is pinned — *"never change without an ADR"* (`manifest.rs:55-56`) — and silently changing **which bytes a pinned domain covers, while the domain string stays constant**, is the one mutation a signature scheme cannot express. The `[u64; 2]`→`[u64; 3]` arity change is also a free compile-time forcing function that extend-V2 forfeits. *(Mary's note: a right answer with a false reason is one refactor away from being a wrong answer nobody re-checked — so the reason is recorded, not just the verdict.)* **Recommend V3.** Note `#[serde(deny_unknown_fields)]` is on every manifest struct and the **schema↔teams biconditional** (`manifest.rs:296-310`) must gain its V3 arm exactly (V3 ⟺ teams present; cross-team block optional-but-empty-is-not-`None`, decided at preflight).

**H4 — cross-team row identity vs the destination team's own rows (H-CLOBBER).** Three candidate closures: **(a)** add `source_team` to the UNIQUE key — changes the physical constraint, re-keys existing rows, breaks 11.2a convergence for same-team cross-region copies (**reject**); **(b)** namespace cross-team copies into a distinct `MemoryNamespace` detail so `(pid, ns_detail, key)` cannot collide with a first-party row (**recommend** — no constraint change, no re-keying, and it makes "this row came from elsewhere" visible in the namespace, consistent with `reserved_namespaces.rs` discipline); **(c)** extend the LWW `DO UPDATE` predicate to refuse when `collective_memory.source_team IS DISTINCT FROM EXCLUDED.source_team` (**recommend as belt-and-braces with (b)** — a one-clause fail-closed guard that turns a clobber into a no-op). Whatever is chosen, **a planted same-key cross-team clobber must be a proven-red test**, or this ships as a silent cross-team data-loss bug.

**H5 — what the read-time verification actually verifies (the 13.2 hand-off).** 13.2 deferred *"per-row attestation persistence (`source_team` + `merkle_root` + `region_sig` + inclusion path), the read-path verify-with-inclusion, and production seed→store wiring"* to 13.3. But **G4 still binds: `base_seed` must never cross below `CollectiveMemoryPort`** (a signing oracle in the data tier), and Round 2 proved the store cannot hold it. **Recommend: persist `merkle_root` + `region_sig` + the leaf's inclusion path as additive nullable columns; the store's read path verifies *inclusion* (`maos_audit::erasure::merkle::verify_proof`, `merkle.rs:183`) against the persisted root, and verifies the *root's signature* against a composition-injected set of derived team **verifying keys** (public only — never the seed).** This is the Round-1 shape Round 2 correctly rejected *at 13.2* (nothing persisted to verify, zero prod callers) — both objections are **resolved by this story**, because AC1 makes the rows real and persists the attestation. **⚠ Scope the read-time check to the TEAM axis only** — the region-axis `source_log_ref` presence residual stays open with no named successor (G7), and `d1-forged-stamp-served-boundary` (`tenant_wall_live.rs:264-305`) must keep asserting *served* (H6).
*Note the dedup caveat:* `kv_merkle_root` → `build_tree` **sorts and dedups** (`merkle.rs:64-65`), so an inclusion proof proves set-membership, not multiplicity or position. Say so; do not overclaim.

**H6 — leg #5 must NOT flip.** `d1-forged-stamp-served-boundary` forges a **`source_log_ref`** (region axis, 11.2b D1) via `write_with_source`. G7 ratified leaving it asserting *served* as an honest document of the still-open region residual. AC3's read-time verification is **team-axis only**, so leg #5 survives unchanged. **If an implementation makes leg #5 red, the design has silently widened to the region axis — stop and raise it, do not update the assertion.**

**H7 — the `WriteEntryPoint` question (13.2 named this 13.3's FLAG-Winston).** `WriteEntryPoint` (`maos-kernel-core/src/memory/write_entry_point.rs:41-72`) is a 4-variant non-`#[non_exhaustive]` kernel enum with **exactly one match site** (`:80`) and **one production construction site** (`memory/mod.rs:684`, `DirectWrite`). Adding `CrossTeamReadmit` is compile-forced at one arm (11.2a precedent: +59 lines). **Recommend: do NOT add a variant — ZERO kernel-Δ.** Rationale: the loom-lite replication apply path **does not go through the kernel at all** (`write_with_source` is a direct store method below the port), exactly as the v1 cross-region apply already does; `CrossRegionReadmit` remains reserved for a *kernel-mediated* path that still does not exist. **The honesty obligation:** `write_entry_point.rs:35` says *"Every store-write path is exactly one of these variants"* — 13.3 must state in ADR-055 that this enumerates **kernel-mediated** writes, and that the cross-team crossing is out-of-kernel replication. **Do not "fix" the kernel doc comment — `check-kernel-baseline` is exact line equality, so even a comment edit is a delta.** Raise to Winston; do not absorb silently either way.

**H8 — where the widened taxonomy lives (and where it must not). ⚠ A NEW `CollectivePortError` VARIANT IS A KERNEL DELTA — this fork exists to avoid blowing AC6.** ADR-055 §3 reserves the taxonomy for 13.3; the gate names it `ABSENT_SUCCESSORS[0]`. The lossy seam is real: `TenantMapError::TeamUnknown { team_id }` is **structured** (`tenant.rs:11-20`) but collapses into `StoreError::TenantMapStale(String)` / `TenantConnectionMismatch(String)` (`store.rs:79-84`), stringifying the team identity away; `CollectivePortError` (`collective_memory.rs:23-39`) then flattens further to `Transport(String)`.
- **⚠ THE TRAP (verified at HEAD):** `CollectivePortError` is matched **wildcard-free in kernel-core** — `maos-kernel-core/src/memory/mod.rs:189-197`, four arms (`Unreachable`/`Timeout`/`Transport(_)`/`Memory(_)`) — and the enum is **NOT `#[non_exhaustive]`**. **Adding a fifth variant is `E0004` in `maos-kernel-core`, so fixing it moves a kernel-core line and AC6's ZERO-Δ @23202 is dead.** Adding `#[non_exhaustive]` is *not* an escape — it breaks the same wildcard-free match immediately. (And `write_entry_point.rs:37`'s R-RG3 rule — *"No wildcard match arm may ever be written against this enum"* — is the house posture: a `_` arm is not an acceptable patch either.)
- **Recommend (i): structure the three `Tenant*` `StoreError` variants** (`{ team_id, .. }`) and add cross-team consent/attestation refusals alongside. `StoreError` lives in `maos-loom-lite` — no kernel reach.
- **Recommend (ii): DO NOT add a `CollectivePortError` variant. Change `Transport(String)` → `Transport(TransportCause)`** where `TransportCause: Display + Debug` is a `#[non_exhaustive]` enum in `maos-domain` carrying the structured refusal. **The kernel-core arm is the tuple pattern `Transport(_)` — it matches unchanged, so no kernel line moves.** The caller gets machine-readable causes; `e.to_string()` at `mod.rs:199` still renders. Verify with `cargo check -p maos-kernel-core` **before** writing the code (see Task 0 / H8-verify).
- **If preflight insists on a fifth variant:** that is a **FLAG-Winston kernel re-pin** — HISTORY entry, new `src_lines`, and the duplicated literal at `check_multi_tenant_loom.rs:275` updated. It is then **not** a ZERO-Δ story. Decide this at preflight, not mid-implementation.
- **No `maos-domain` `E*`**: loom-lite is outside `error-catalog.toml scan_dirs` (`xtask/error-catalog.toml:21-38`) and 13.1/13.2 both deliberately stayed out — adding a cataloged `E*` breaks that precedent for no gain. Amend 13.1's *"consciously lossy at the frozen collective port"* posture **in ADR-055**, don't contradict it silently.

**H9 — third team database?** The gate's substrate oracle is a hardcoded 2-element array (`check_multi_tenant_loom.rs:56`) and `LiveTenantMap::new` is fixed 2-arity (`tenant_wall_live.rs:87-102`). Asymmetric consent is fully provable with **two** teams (A→B granted, B→A absent). **Recommend: stay at two datnames for 13.3**; the 3-team substrate is 13.6's (prior art exists — `check_multi_region_slo.rs:148-150` uses `MAOS_TEST_POSTGRES_{A,B,C}`). Adding a third DB is 4 mechanical edits if preflight wants it.

---

## Acceptance Criteria

1. **`source_team` is persisted on the production write path — closing D8. THE ROW IS PRODUCTION-REAL; THE CROSSING IS NOT (D12).** A cross-team crossing is: build a v2 bundle under the **source** team's derived key → verify under the key derived from the **claimed** `(region, team)` (13.2, unchanged) → apply into the **destination** team's store, landing rows whose `source_team` is the **verified** source team.
   - **⚠ THE CLAIM THIS AC MAY MAKE, EXACTLY.** The *row* work lands on `store.rs`'s real `write`/`read`/`scan` and is live the moment team mode boots. The *crossing* — initiation, transport, consent decision — is a **mechanism proven at the verify/apply seam**, because no daemon holds both a manifest and a store (D12) and joining them is ADR-055 §5's verbatim assignment to **13.5c**. Do not write "production cross-team write path" anywhere; write **"the row is production-real; the crossing is not."**
   - **`write_with_source` gains a `source_team: Option<&TeamId>` parameter**; the INSERT column list **and** the `ON CONFLICT … DO UPDATE SET` clause (`store.rs:313-327`) both carry it. `apply_verified_replication_bundle` (`bundle.rs:427-438`) passes `leaf.source_team` instead of dropping it. A round-trip test proves `read_all_rows_from` returns the same `TeamId` that `verify` proved — **derived from the write codec, not re-read from the label**.
   - **Destination enforcement (currently absent):** `apply_verified_replication_bundle` canonicalizes `dest_region` and then **uses it only for a tracing log** (`bundle.rs:368-371`) — nothing checks the bundle against the destination store. Apply must **refuse** when the bundle's claimed `source_team` equals the destination `home_team` (a self-crossing is not a crossing) and when the destination store's `home_team` is not the grant's `to_team`. Fail closed, typed.
   - **⚠ BLAST RADIUS — `write_with_source` has 33 call sites across TWO crates.** `cross_region_live.rs` (**27**), `store.rs` (**2** — the `write` wrapper `:254` + the definition), `bundle.rs:428`, `tenant_wall_live.rs:284` (**this is leg #5 — it must keep asserting *served***, H6), `maos-bench/tests/t_11_2b_cross_region_slo.rs:129` (a **different crate**; `maos-bench/Cargo.toml:27` dev-deps loom-lite), and the assertion string in `read_path_chokepoint.rs`. **Strongly prefer a `WriteSource { ts, region, log_ref, team }` struct parameter, or a new `write_with_source_team(…)` that `write_with_source` delegates to** — a bare positional add is 33 mechanical edits, 27 of them in one file, with leg #5 among them. **`read_path_chokepoint.rs:105` looks the function up by the literal prefix `"pub async fn write_with_source("`** — a parameter add survives that, a **rename does not**; if you rename, update `:105` in the same commit.
   - **Cross-module access:** `apply_verified_replication_bundle` lives in `replication/bundle.rs`, and `LoomLiteStore.config` is **private** (`store.rs:54`). Use the existing `pub fn config(&self) -> &StoreConfig` at **`store.rs:656`** for the destination `home_team` check.
   - **⚠ H4 / H-CLOBBER is a ship-blocker:** the UNIQUE key omits `source_team` (`schema.rs:63`) and LWW resolves on `(source_ts, source_region)` only, so a cross-team copy can **overwrite a destination-team first-party row** with the same `(pid, namespace, key)`. Implement H4(b)+(c); a **planted same-key clobber must be proven red**.
   - **May NOT claim:** that a forget-cascade consumes `source_team` (D7 — no cascade exists; 13.5b), or that the daemon boots in tenant mode (D11 — 13.5c).

2. **Asymmetric cross-team consent: an ordered-pair grant in the signed org manifest, enforced at the crossing seam — B→A is refused when only A→B is granted.** D6 is the constraint: `ConsentTuple` names one endpoint and **cannot** express this.
   - **Manifest (H3):** `[[cross_team_consent]]` = ordered triples `{from_team, to_team, intent}` under **`COHORT_SCHEMA_V3`** / `SIG_DOMAIN_V3`, copying the V1/V2 dispatch idiom verbatim (`manifest.rs:51-58,191-254`). **V1 *and* V2 canonical bodies stay byte-frozen** — extend `v1_canonical_body_matches_frozen_golden` with a **v2 golden** before adding V3, or v2 byte-compat is unprovable afterwards. Validation mirrors `validate_team_map` (`manifest.rs:551-618`): both teams declared in `[[teams]]`, `from_team != to_team`, canonical intent, no duplicates — each a distinct typed refusal. The schema↔teams biconditional (`:296-310`) gains its V3 arm exactly.
   - **⚠ TWO SILENT FAILURES — neither is a compile error (this is the story's highest-severity trap after H4):**
     **(1) `manifest.rs:235` guards the teams block with `if self.schema_version == COHORT_SCHEMA_V2`.** A V3 manifest carries teams, but under `==` the **teams block is omitted from the canonical signed body** — the signature verifies while team→region→`datname` placement is **unsigned and substitutable**, defeating 13.1's entire manifest-authoritative premise. It **must** become `matches!(self.schema_version, COHORT_SCHEMA_V2 | COHORT_SCHEMA_V3)`.
     **(2) `to_canonical_bytes`'s domain dispatch has a `_ => b"maos.cohort-manifest.unsupported"` fallback (`manifest.rs:196`).** A missing V3 arm yields a **wrong signature domain, not a compile error.**
     **Prove both:** a V3 golden whose canonical body is strictly longer than its V2 counterpart, **plus** a negative that mutates a V3 `[[teams]]` `datname` and asserts `verify` reds. Extend `every_v2_team_field_is_signature_bound_and_domains_do_not_cross` (`:1596`, already in the aggregator).
   - **⚠ `SUPPORTED_COHORT_SCHEMAS` is a fixed-size `[u64; 2]`** (`manifest.rs:53`) → becomes `[u64; 3]`. Consumers: `lib.rs:65` (re-export), `manifest.rs:285`, `:288` (populates `ECohortSchemaUnsupported { supported }`), `:875` (a test asserting equality with the const).
   - **Enforcement (H2):** a `CrossTeamConsentPort` in `maos-loom-lite` (mirroring `TenantMapPort`, `tenant.rs:22-26`), implemented in `maos-bin` over `CohortManifestState`, injected at composition. **No new `maos-loom-lite`→`maos-cohort` edge; no second `consent_decision` call in `router.rs`** (`cohort_consent_chokepoint_12_2.rs:16-22` is grep-pinned).
   - **Gate ⟺ observer by construction (12.3-P7b idiom):** the consent decision is taken at the **same** function that lands the row, so a crossing cannot be applied without a decision having been reached. The **asymmetric negative is the headline**: with only `(A→B, share)` granted, an A→B crossing lands and a B→A crossing is **refused by absence of a grant** — proven on **real cross-team writes over live Postgres**, not a hermetic stub.
   - **Freshness composes:** the decision requires a fresh, locally verified manifest (`state.rs:378-386` lease); a stale lease refuses fail-closed and is distinguishable from *no grant* (AC4).

3. **Per-row attestation is persisted and the read path verifies it on the TEAM axis — closing 13.2's read-time hand-off (H5).** 13.2 left the read with nothing to verify; AC1 makes the rows real, so AC3 gives the read a verifiable artifact.
   - **Additive nullable columns** (`schema.rs:76-97` `DO $$ … ADD COLUMN IF NOT EXISTS` idiom): **`leaf_canonical_hash` (BYTEA 32)**, `merkle_root`, `region_sig`, `bundle_schema_version`, and the leaf's inclusion path — written by the apply path alongside `source_team`. First-party local rows leave them **NULL** and are unaffected (the G2 discipline: do not re-root every store).
   - **⚠ PERSIST `canonical_hash`; DO NOT RE-HASH `value_data` AT READ (settled item (a); D13).** The at-rest seal is **unpaired at HEAD** and **non-deterministic**, and apply re-seals (`db.value_data = seal(vtp(ptv(leaf.value_data)))` ≠ `leaf.value_data`), so reconstructing the canonical leaf from a DB row is impossible under a seal and unstable without one. Persist `CollectiveKvLeaf::canonical_hash()` (`leaf.rs:135`) at apply time; the read path verifies `verify_proof(persisted_root, persisted_leaf_hash, persisted_proof)` and separately binds the hash to the row by re-deriving only the **exactly-round-tripping** fields (region, ts, pid, namespace, key, kind, team). `value_data` is covered **transitively by the signature**, never by a read-time re-hash. Persist `bundle_schema_version` too, or `build_team_sign_payload` cannot be reconstructed.
   - **⚠ THE SIGNATURE IS THE CONTROL — the proof is convenience (settled item (b)).** Root, proof, and row all live in the **same table**, and `verify_proof` returns `true` when `siblings.is_empty() && leaf == expected_root` (`merkle.rs:192-194`) — so an attacker who can write the row sets `root := leaf_hash`, `proof := empty`, and passes. **This is the 13.2 trusted-applied-root registry in a Merkle hat — a lock with the key taped to it, third occurrence.** The actual control is verifying `region_sig` against a **composition-injected `HashMap<(Region, TeamId), [u8;32]>`** built from the shipped `derive_team_pubkey` (`sealed_export.rs:88`). Public keys only.
   - **⚠ SEAL POSTURE, STATED OUT LOUD (Decision 3).** Read-time attestation verification is defined for the **plaintext (default) posture**. Under a configured at-rest seal the collective read path is **already broken at HEAD** (D13) — `read()` returns ciphertext into `parts_to_value`. The `canonical_hash` design keeps *this AC* seal-independent, but the surrounding read path is not. **The unpaired-unseal gap is filed with 13.5a as named owner** (ADR-051 owns at-rest; 13.5a AC2 is the first story to exercise it end-to-end). It is **not** left unsuccessored — that is how the 13.2 region residual became furniture.
   - **Dedup is NOT a problem (scout-disproved).** Two distinct rows can never collide: the canonical form includes `spirit_pid`/`namespace_*`/`key`, which is exactly the table's UNIQUE key. Duplicate leaves share one hash and one proof and verify correctly. `verify_proof` never reads `leaf_index`, so dedup index drift is inert. Rebuilding the tree at apply is one line — `bundle.leaves` is in scope at `bundle.rs:373`.
   - **⚠ THIS CANNOT LIVE IN `team_guard` — verified at HEAD.** `fn team_guard(&self, spirit_pid: u32) -> Result<(), StoreError>` (`store.rs:505`) is **synchronous**, takes **only `spirit_pid`**, and runs **pre-query**: at `read` it fires at `:359`, *before* `get_client_with_timeout()` at `:360`. It has no row, no client, no `source_team`. Attestation verification is **post-query, per row** — the two sites where `region_guard` is already called (`store.rs:380` in `read`, `:449` in `scan`).
   - **Add a NEW `attestation_guard`** (`fn attestation_guard(&self, source_team: Option<&TeamId>, root: Option<&[u8]>, sig: Option<&[u8]>, proof: …) -> bool`) at those two post-query sites. **Do not overload `team_guard`** (wrong signature, wrong phase) and **do not overload `region_guard`** (its count is pinned at 2 and it is the region axis — H6). For a row with a non-NULL `source_team` it verifies (i) Merkle **inclusion** of the row's canonical leaf against the persisted root (`maos_audit::erasure::merkle::verify_proof`, `merkle.rs:183`) and (ii) the **root's signature** under the team verifying key. **A foreign-team row without valid attestation is refused, never served.**
   - **⚠ The SELECT column lists must be extended.** `read`'s query fetches only `value_kind, value_data, source_region, source_log_ref` (`store.rs:365-367`) — **no `source_team`**; `scan` is the same. Add `source_team`, `merkle_root`, `region_sig`, and the inclusion path to **both**, and shift the positional `row.get(N)` indices accordingly.
   - **⚠ `base_seed` never crosses below `CollectiveMemoryPort` (G4 still binds).** The composition root derives **public verifying keys** via the shipped `derive_team_pubkey(base_seed, region, team)` (`maos-audit/src/sealed_export.rs:88` — do **not** hand-roll it from `derive_team_signing_seed`) and injects that set; the store holds no seed. Deriving keys ≠ holding the root.
   - **⚠ Chokepoint arithmetic (`read_path_chokepoint.rs:71-113`) is source-text counting** over three `include_str!`'d files (`store.rs`, `adapter.rs`, **and `maos-domain/src/ports/collective_memory.rs`** — the latter two are edited by AC4): `self.team_guard(` **exactly 3** in `store.rs`, **exactly 0** in `write_with_source`/`pool`; `self.region_guard(` **exactly 2**. Leave all three counts alone. **`attestation_guard` is counted by nothing — add `count_code_occurrences(STORE_SRC, "self.attestation_guard(") == 2` to `read_path_chokepoint.rs` in the same commit, or the new chokepoint ships unpinned.** Note only `team_guard_is_exactly_the_three_spirit_entry_points` is a gate leg (`check_multi_tenant_loom.rs:135-144`); the other four tests red the build without reddening the gate.
   - **⚠ TEAM AXIS ONLY (H6).** `d1-forged-stamp-served-boundary` (`tenant_wall_live.rs:264-305`) forges a `source_log_ref` (region axis) and **must keep asserting *served***. If it reds, the design widened to the region axis — stop.
   - **May NOT claim:** that inclusion proves multiplicity or ordering (`build_tree` sorts+dedups, `merkle.rs:64-65`), or — per **ADR-049 §7, verbatim binding** — that the bundle establishes **authorization** provenance. It establishes **origin** provenance. *"No claim anywhere — AC, gate, or doc — may assert the bundle establishes authorization provenance."* A crossing whose consent decision is absent is an **observable state**, never folded into a clean applied total.

4. **The caller-facing tenant/consent error taxonomy is widened and distinguishable — the item ADR-055 §3 reserved for this story and the gate names as `ABSENT_SUCCESSORS[0]` (H8).** Today `TenantMapError::TeamUnknown { team_id }` is structured but collapses into `StoreError::TenantMapStale(String)` and then `CollectivePortError::Transport(String)` — the team identity is stringified away and a caller cannot tell *why* it was refused.
   - The three `Tenant*` `StoreError` variants (`store.rs:79-84`) become **structured** (carrying `team_id` where known); cross-team **consent-denied** and **attestation-invalid** refusals join them.
   - **⚠ `store_error_to_port_error` (`adapter.rs:148-162`) is wildcard-free over `StoreError`** — restructuring the `Tenant*` variants is a **pattern break**, not just a construction break. Full touch list (~26 sites): `adapter.rs:156-158`; `store.rs:477,479,483,485,510,516,519,521,526`; in-file tests `store.rs:779,790,794,803,814,875`; and **`tenant_wall_live.rs:203,218,226,248,260`** — four of those five are inside gate leg `two-datname-physical-absence`, and `:226` **destructures** `TenantConnectionMismatch(reason)`. Update all of them in one commit.
   - **The port-side cause is carried inside the existing `Transport` variant (H8) — NOT as a new `CollectivePortError` variant**, which would be a kernel delta (`memory/mod.rs:189-197` is wildcard-free, enum is not `#[non_exhaustive]`). Change `Transport(String)` → `Transport(TransportCause)`; the kernel's `Transport(_)` tuple pattern matches unchanged. The caller distinguishes **`consent-denied` / `map-stale` / `attestation-invalid` / `unmapped-spirit` / `connection-mismatch`** without string-matching. **Five causes, five distinguishable outcomes, proven by a matrix test** (the 13.4 four-cause-distinguishability idiom, applied here).
   - **No cataloged `maos-domain` `E*`** (loom-lite is outside `error-catalog.toml` `scan_dirs`, `:21-38`; 13.1/13.2 precedent). Amend 13.1's *"consciously lossy at the frozen port"* posture **in ADR-055**, don't contradict it silently.
   - **May NOT claim** an **audited** refusal or per-operator TL isolation — 13.5c owns those. A typed, caller-visible refusal is this story's scope; an unaudited refusal is a **named** intermediate gap.

5. **`check-multi-tenant-loom` gains the cross-team legs; the asymmetric negative and the clobber negative are proven on REAL cross-team writes.** Reuse the gate's `BindingClass`/`LegResult`/non-vacuity model (`check_multi_tenant_loom.rs:26-105`; each backing test is one `#[test]`, `"running 1 test"` + `"1 passed"`). Remove `ABSENT_SUCCESSORS[0]` (`:20`).
   - **(a) `cross-team-crossing-lands-with-bound-source-team`** — `AdvisorySubstrate` (live 2-datname): a consented A→B crossing applies into B's database; the landed row's `source_team` is the one `verify` proved, **read back through the store**, verifier independently derived from the write codec.
   - **(b) `asymmetric-consent-reverse-share-refused`** — `AdvisorySubstrate` (live): with only `(A→B)` granted, B→A is refused and **zero rows land in A**. The headline negative; it must run on real cross-team writes, not hermetic stubs.
   - **(c) `cross-team-clobber-refused`** — `AdvisorySubstrate` (live): a cross-team copy colliding on `(pid, namespace, key)` with a destination first-party row **does not overwrite it** (H4). Planted collision, proven red.
   - **(d) `foreign-team-row-without-attestation-refused-at-read`** — `AdvisorySubstrate` (live): a row with `source_team` set but invalid/absent attestation is **refused, never served** (AC3, team axis).
   - **(e) `tenant-refusal-taxonomy-distinguishable`** — hermetic `Blocking`: the five refusal causes are distinguishable at the port without string-matching (AC4).
   - **(f) `crossing-not-production-wired` — THE DEAD-WIRE NEGATIVE (Decision 2). ⚠ TWO SEPARATELY-NAMED CLAUSES, EACH WITH ITS OWN INVERTER — do not write it as one composite assertion.** Hermetic `Blocking`. *(Corrected 2026-07-18 after the 13.5c/13.5d story generation: the epic's "13.5d inverts leg (f)" was **false** — 13.5d's route is `DirectWrite` and never touches `apply_replication_bundle` — and a single composite leg would have been red by 13.5c while this story's own may-not-claim table asserted it stayed green.)*
     - **(f-i) `no-production-crossing-initiator`** — no non-test caller reaches `build_replication_bundle`/`apply_replication_bundle`. **Inverter: UNASSIGNED at 2026-07-18.** Neither 13.5c (daemon merge, no crossing) nor 13.5d (`DirectWrite` Spirit route) wires this. Name the gap in the leg's failure text rather than pretending a successor exists — the 13.2 unsuccessored-residual lesson.
     - **(f-ii) `tenant-mode-unbootable`** — the primary daemon's `tenant_map_for_store` second argument is still `None` (`main.rs:2286`). **Inverter: 13.5c**, which makes `MAOS_LOOM_HOME_TEAM` boot. Name 13.5c in this clause's failure text.
     - **Do NOT add a `CohortDaemonFileConfig`-carries-no-store clause.** 13.5c's merge runs the other direction (the primary root already built the store; the cohort function receives the live `Arc`s), so that assertion would stay true forever and prove nothing.
     - The 13.5a AC3 idiom (*"a constructed-but-unwired thing must red"*) applied to a story's own deferral — green while unwired, red the moment the named inverter lands.
   - **Leave leg #5 unchanged (H6).** Update `tests/coverage-matrix.yaml` ADR-055 `notes` (`:58-82`) and roll `valid_until`. **No new gate ⇒ no `check_ship_gate_completeness` change** (it is gate-name-granular only). Extend the existing `discipline.yml` job (`:2666-2704`); stay at two datnames (H9).
   - **The gate is `blocking` at v2_2** (`gate-registry.toml:301`) and the gate self-asserts that (`check_multi_tenant_loom.rs:119-127`) — this epic's target phase. The AdvisorySubstrate legs (a)–(d) are advisory only for the *development lane*; their evidence state still gates the Reza claim.
   - **Housekeeping the scouts found:** the gate doc-comment `:6` still says *"the **two** live Postgres legs"* (there are three since 13.2); `23202` is duplicated as a literal at `:275` in violation of the *"no second literal"* invariant asserted at `kernel-core-baseline.toml:1-8`; **no xtask test covers this gate** — `xtask/tests/loom_integration.rs` **exists but tests a different gate** (`check-loom`, NFR-Test-9), so do not mistake it for coverage; and `discipline.yml:2672`'s `timeout-minutes: 15` may need a bump for 5 more legs, 4 of them live. Fix the first two; name the rest.

6. **ZERO kernel-core Δ @ 23202, verified — and H7 (`WriteEntryPoint`) is resolved explicitly, not absorbed.** No line under `crates/maos-kernel-core/src/**` moves; `check-kernel-baseline` reads **23202** (pin == actual, re-confirmed at CATCH-0).
   - **`WriteEntryPoint` untouched (H7 recommendation).** The cross-team apply is **out-of-kernel replication below the port**, exactly as the v1 cross-region apply already is; `CrossRegionReadmit` stays reserved for a kernel-mediated path that does not exist. **ADR-055 must state that `write_entry_point.rs:35`'s *"every store-write path"* enumerates kernel-mediated writes** — and must **not** edit that kernel comment, because `check-kernel-baseline` is exact line equality (`check_kernel_baseline.rs:94-108`: every `.rs` line, comments and tests included). If preflight instead rules that a `CrossTeamReadmit` variant is required, that is a **FLAG-Winston re-pin** with a HISTORY entry, a new `src_lines`, the `check_multi_tenant_loom.rs:275` literal updated, and proof of no unrelated churn — **never a silent absorption, never a parallel out-of-kernel owner built to preserve the ZERO claim.**
   - **⚠ AC4 is the live threat to this AC.** `maos-domain` is not kernel-core, so *most* of the taxonomy work is free — but a **new `CollectivePortError` variant is NOT**, because `maos-kernel-core/src/memory/mod.rs:189-197` matches it wildcard-free and the enum is not `#[non_exhaustive]`. Take the `Transport(TransportCause)` route (H8), which leaves the kernel's tuple pattern untouched, and **prove it with `cargo check -p maos-kernel-core` before writing AC4** — not after.
   - **ZERO new crates** (`CrossTeamConsentPort` is a trait in loom-lite + an impl in maos-bin, the `TenantMapPort` precedent); **no new `maos-loom-lite`→`maos-cohort` edge** (`check-dependency-closure` only inspects kernel-core + domain, so it will **not** catch a bad loom-lite edge — verify with `cargo tree` by hand).

---

## Tasks / Subtasks

- [ ] **Task 0 — CATCH-0 verify-or-prove-zero (do FIRST, before any code).** (AC: 6)
  - [ ] Re-confirm at dev HEAD: `write_with_source` still lacks a `source_team` parameter and its INSERT still omits the column (D8); `build`/`verify`/`apply_replication_bundle` still have **zero production callers** (D9); `team_guard` still presence-only with **exactly 3** call sites; `WriteEntryPoint` still 4 variants / one match site; `ConsentTuple` still single-endpoint (D6); no `cross_team` anything in `maos-cohort`. If any changed, re-scope.
  - [ ] `cargo run -p xtask -- check-kernel-baseline` reads **23202** and equals actual. Record in Dev Agent Record.
  - [ ] **Prove ZERO kernel-core Δ is achievable before writing code.** If any pressure pushes a line into `maos-kernel-core/src` (a `WriteEntryPoint` variant, a doc-comment "fix"), STOP → FLAG-Winston (H7), do not absorb.
  - [ ] **H8-verify — the ABI gates are a NULL CONTROL here; do not rely on them.** Both hard-code a single manifest: `xtask/src/abi_diff.rs:8` and `check_abi_ratification.rs:126` point at **`crates/maos-spirit-abi/Cargo.toml` only**. Neither inspects `maos-domain`, so both report green while an AC4 change blows AC6. **The real detector is `cargo check -p maos-kernel-core`**, which reds on the wildcard-free match at `memory/mod.rs:189-197`. Run it against the planned AC4 shape **before writing AC4 code**; record the result.
- [ ] **Task 1 — THREE SUB-STEPS IN THIS ORDER. Murat was rigid about the ordering; do not reorder.** (AC: 2)
  - [ ] **1a — FREEZE THE V2 GOLDEN FIRST.** Mirror `v1_canonical_body_matches_frozen_golden` (`manifest.rs:1408-1416`: assert `bytes.len()` **and** the hex `canonical_hash`) over `tenant_manifest_body()`. **Do this before anything touches `to_canonical_bytes`** — D15: every existing v2 test is build-verify symmetric or relative-invariant, so *nothing in the repo can currently detect a v2 encoding change at all*. Without this golden you have no oracle and you will never know. **True and required regardless of whether V3 lands.**
  - [ ] **1b — CONVERT THE THREE HARDCODED-V2 COMPARISONS TO FLOORS, in a SEPARATE COMMIT with its own proven-red (D14).** `state.rs:273` → `candidate.schema_version < cached.manifest.schema_version`; `tenant_map.rs:85` and `:168` → `< COHORT_SCHEMA_V2` (rename `SchemaV2Required` accordingly). These are **correct standalone** and are a latent signed-artifact downgrade hole today — ADR-055 §2 says floor, the code says pair. Separate commit so the test proves the fix, not the feature. **Do not bundle with 1c.**
  - [ ] **1c — then add `COHORT_SCHEMA_V3`** (mechanical once 1a/1b land).
  - [ ] Add `COHORT_SCHEMA_V3` / `SIG_DOMAIN_V3`; widen `SUPPORTED_COHORT_SCHEMAS` to `[u64; 3]` (+ its 4 consumers); the `cross_team_consent` block appended after teams; the V3 arm of the schema↔shape biconditional (`:296-310`). Copy the V1/V2 dispatch shape verbatim; `#[serde(deny_unknown_fields)]` everywhere.
  - [ ] **Fix the two silent failures FIRST:** `:235`'s teams-block guard `== COHORT_SCHEMA_V2` → `matches!(…, V2 | V3)`, and add the V3 arm to the domain dispatch whose `_ =>` fallback (`:196`) would otherwise sign under `"maos.cohort-manifest.unsupported"`. Both are silent, neither is a compile error.
  - [ ] Validation mirroring `validate_team_map`: both teams declared, `from_team != to_team`, canonical intent, no duplicates — each a **distinct typed refusal**. Extend the `tenant_manifest_option_a_plus_gate_matrix` aggregator (`manifest.rs:1587-1599`) rather than adding an un-gated top-level `#[test]`. **Sub-tests needing V3 arms:** `schema_and_team_shape_are_exact_biconditionals` (`:1591`), `both_schemas_round_trip_and_verify_with_distinct_domains` (`:1590` — now *three* schemas), `every_v2_team_field_is_signature_bound_and_domains_do_not_cross` (`:1596`), `deny_unknown_fields_applies_to_both_schemas` (`:1597`), `reject_unknown_schema_before_strict_field_validation` (`:1592`).
- [ ] **Task 2 — `CrossTeamConsentPort` + composition wiring (H2).** (AC: 2)
  - [ ] Trait in `maos-loom-lite` mirroring `TenantMapPort` (`tenant.rs:22-26`); impl in `maos-bin` over `CohortManifestState`; injected at composition. **No loom-lite→cohort edge** (verified absent in `maos-loom-lite/Cargo.toml` at HEAD — re-verify with `cargo tree`); **no new `router.rs` consent call** — `cohort_consent_chokepoint_12_2.rs:12-26` pins **two** counts: `cohort_manifest_gate.consent_decision(` **== 1** *and* `cohort_consent_decision(` **== 3** (one definition + outbound + inbound).
  - [ ] Freshness composes with the shipped lease (`state.rs:378-386`); stale is fail-closed and **distinguishable from no-grant** (AC4).
- [ ] **Task 3 — the production cross-team write path (D8/D9 closure).** (AC: 1)
  - [ ] `write_with_source` gains `source_team`; INSERT column list **and** `DO UPDATE SET` carry it; `apply_verified_replication_bundle` stops discarding `leaf.source_team`.
  - [ ] Destination enforcement: refuse self-crossing and destination/`to_team` mismatch, typed and fail-closed. `dest_region` stops being tracing-only.
  - [ ] **H4/H-CLOBBER:** implement (b) distinct namespace detail for cross-team copies **and** (c) the fail-closed LWW predicate. **Planted same-key clobber proven red.**
  - [ ] Gate ⟺ observer: consent is decided in the same function that lands the row.
- [ ] **Task 4 — per-row attestation + team-axis read verification (H5).** (AC: 3)
  - [ ] Additive nullable **`leaf_canonical_hash`** / `merkle_root` / `region_sig` / `bundle_schema_version` / inclusion-path columns (`schema.rs:76-97` idiom); first-party rows stay NULL.
  - [ ] Read-path verify: `verify_proof(persisted_root, persisted_leaf_hash, persisted_proof)` + **root-signature check against a composition-injected `HashMap<(Region,TeamId),[u8;32]>`** from `derive_team_pubkey`. **Never re-hash `value_data` (D13); the signature is the control, not the proof. `base_seed` never below the port (G4).**
  - [ ] State the **plaintext-posture scoping** in the may-not-claim table and file the unpaired-unseal gap with **13.5a** as named owner (D13 / Decision 3).
  - [ ] **Widen inside `team_guard` — do not add a call site** (`read_path_chokepoint.rs:84-113` counts source text: 3 and 0).
  - [ ] **Verify leg #5 still asserts *served*** (H6). If it reds, STOP.
- [ ] **Task 5 — widened taxonomy (H8).** (AC: 4)
  - [ ] Structured `Tenant*` `StoreError` variants + consent/attestation refusals; ONE additive `CollectivePortError` variant carrying a structured cause.
  - [ ] Five-cause distinguishability matrix test. **No `maos-domain` `E*`**; amend ADR-055 §3's lossy-port posture explicitly.
- [ ] **Task 6 — gate legs + docs.** (AC: 5, 6)
  - [ ] Legs (a)–(e) **plus (f) the dead-wire negative** (Decision 2 — green while unwired, inverted by 13.5c, 13.5c named in its failure text); remove `ABSENT_SUCCESSORS[0]`; **leave leg #5**; fix the `:6` "two live legs" doc and the `:275` duplicated `23202` literal; name the missing xtask self-test as a known gap.
  - [ ] `coverage-matrix.yaml` ADR-055 `notes` + `valid_until`; extend the existing `discipline.yml` job; **no new gate**.
  - [ ] ADR-055 update: cross-team crossing semantics, H7's kernel-mediated-writes clarification, the AC4 posture amendment, and the carried gaps (13.3b provenance, 13.5b erasure, 13.5c routing/audit; region residual still open, no successor).

---

## Dev Notes

### The one-paragraph version
This story turns two mechanism stories into a working capability. Everything hard about it is **honesty about what does not exist**: the replication path has **zero production callers**, `source_team` has **no write path**, consent has **no ordered pair**, and the destination store is **never checked against the bundle**. The traps are (1) assuming `ConsentTuple` can express asymmetry — it names one endpoint, so a `team` field yields *"anyone may send to team-b"* (D6); (2) letting a cross-team copy **clobber** a destination first-party row, because `source_team` is not in the UNIQUE key and LWW resolves on `(source_ts, source_region)` (H4 — a silent data-loss bug); (3) adding a `team_guard` call site and reddening the source-text chokepoint count (`3` and `0`); (4) flipping leg #5, which documents the **region** residual on a different axis (H6); (5) putting `base_seed` below the port to make read-time verification convenient (G4 — a signing oracle in the data tier); (6) reaching for `ranged_recall` for anything cross-team (unscoped, capability-free, compile-pinned-out by `spirits/researcher`); and (7) "fixing" the `write_entry_point.rs:35` comment — `check-kernel-baseline` is exact line equality, so a comment edit is a kernel delta. **If you find yourself touching `maos-kernel-core/src`, adding a `WriteEntryPoint` variant, adding a second `router.rs` consent call, or creating a `maos-loom-lite`→`maos-cohort` dependency, stop — the design has gone wrong.**

### Files being modified — current state, change, and what must be preserved

| File | State today | This story | Must not break |
|---|---|---|---|
| `crates/maos-cohort/src/manifest.rs` (1599 L) | `COHORT_SCHEMA_V1/V2` + `SIG_DOMAIN_V1/V2` `:51-58`; `TeamEntry :124-132`; `ConsentTuple/ConsentMatrix :136-156`; `to_canonical_bytes :191-254`; biconditional `:296-310`; `validate_team_map :551-618`; matrix aggregator `:1587-1599` | +`COHORT_SCHEMA_V3`/`SIG_DOMAIN_V3`, `[[cross_team_consent]]` ordered triples, V3 biconditional arm, validation, aggregator extension (H3) | **v1 AND v2 canonical bodies byte-frozen** (freeze the v2 golden *before* V3); `deny_unknown_fields` on every struct; `TeamId` `Ord`-based canonical sorting `:238`; the pinned-authority verify path `:419-437` |
| `crates/maos-loom-lite/src/tenant.rs` (27 L) | `TenantMapPort` 3 methods `:22-26`; `TenantMapError` 4 variants `:11-20` | +`CrossTeamConsentPort` trait alongside (H2) | the 3-method `TenantMapPort` shape; **no `maos-cohort` dependency edge** |
| `crates/maos-loom-lite/src/store.rs` (878 L) | `write_with_source :281-344` (**no `source_team`**, INSERT `:313-327` omits it); `team_guard :505-532` **sync, pid-only, pre-query**, 3 sites (`:249,359,407`); `region_guard :575-584` **post-query**, 2 sites (`:380,449`); `read`'s SELECT `:365-367` (**4 cols, no `source_team`**); `StoreError :63-85`; `config() :656`; `read_all_rows_from :591` | +`source_team` on the write path (AC1, prefer a `WriteSource` struct); H4 clobber closure; **new post-query `attestation_guard`** at `:380`/`:449` (AC3, **not** inside `team_guard`); extend `read`/`scan` SELECT columns; structured `StoreError` (AC4) | **the 3/0 and 2 source-text counts**; `write_with_source`'s deliberately-unguarded replication role; the at-rest seal path `:300-312`; `read_all_rows_from`'s hard error on a non-canonical stored tag `:612-618` |
| `crates/maos-loom-lite/tests/cross_region_live.rs` (~1800 L) | **27 `write_with_source` call sites** | mechanical update for the new write signature | the 11.2a cross-region convergence assertions — this file is 11.2a's live proof, do not weaken it while re-signaturing |
| `crates/maos-bench/tests/t_11_2b_cross_region_slo.rs` | 1 `write_with_source` site `:129`; dev-deps loom-lite (`maos-bench/Cargo.toml:27`) | mechanical update | **a second crate compiles against this signature** — it is easy to miss and reds the workspace build |
| `crates/maos-loom-lite/src/adapter.rs` | `store_error_to_port_error :148-162` **wildcard-free over `StoreError`** (`:156-158` are the `Tenant*` arms) | pattern updates for structured `Tenant*` + the new refusals (AC4) | the `spawn_blocking` sync-port-over-async-store crossing; **`read_path_chokepoint.rs` `include_str!`s this file** and asserts it never mentions `read_all_rows_from` |
| `crates/maos-loom-lite/src/replication/bundle.rs` (905 L) | `CrossRegionReplicationBundle :66-80` (has `source_team`); v1/v2 dispatch `:270-307`; `apply_verified_replication_bundle :353` **drops `leaf.source_team`** `:427-438`, `dest_region` tracing-only `:368-371`; `apply_replication_bundle :456` verifies first `:462` | pass `source_team` through; destination enforcement; consent decision at the crossing (AC1/AC2) | **verify-before-apply** (the 13.2 review patch); v1 byte-exact region path; `build`/`verify` payload symmetry; `#[serde(default)]` v1 wire-compat |
| `crates/maos-loom-lite/src/replication/leaf.rs` (612 L) | `CollectiveKvLeaf :61-78` (`source_team :77`); `KV_LEAF_DOMAIN(_V2) :45,52`; dispatch `:97-132`; `kv_merkle_root :171`; `compute_kv_payload_oracle :155` | **likely untouched** — leaf **v3** (I11 chain) is **13.3b**, not here (H1) | the frozen v1 golden `22fe8237…`; `None` → byte-identical v1; length-prefix rules |
| `crates/maos-loom-lite/src/schema.rs` | `collective_memory` DDL `:33-72`; UNIQUE `:63` (**no `source_team`**); additive `DO $$` `:76-97`; `source_team TEXT :61`; I11 CHECK `:69-71` (**vacuous** — nothing writes `kind='pattern'`) | +nullable `merkle_root`/`region_sig`/inclusion-path via the additive idiom | the UNIQUE key (H4 rejects changing it); the additive-DDL idempotence; existing column semantics |
| `crates/maos-domain/src/ports/collective_memory.rs` | `CollectivePortError` 4 variants `:23-39`, **NOT `#[non_exhaustive]`**, matched **wildcard-free in kernel-core** (`maos-kernel-core/src/memory/mod.rs:189-197`); port 3 methods `:60-97` | `Transport(String)` → `Transport(TransportCause)` **only** (H8) — the kernel's `Transport(_)` tuple pattern survives | **NEVER add a variant** — it is `E0004` in kernel-core and blows AC6's ZERO-Δ; the **3-method port ABI**; the zero-async contract; no `E*`; **`read_path_chokepoint.rs` `include_str!`s this file** |
| `crates/maos-kernel-core/src/memory/mod.rs` | `collective_port_error :189-197`, 4 arms, no wildcard | **MUST NOT CHANGE** — this is the AC6 tripwire | if this file needs an edit, the AC4 design is wrong (or it is a FLAG-Winston re-pin, decided at preflight) |
| `crates/maos-bin/src/tenant_map.rs` + `main.rs` | `TenantMapAdapter :105`; store built `main.rs:2274-2335` with `tenant_map_for_store(&home_team, None)` `:2286` → **boot refusal** (D11) | +`CrossTeamConsentPort` impl + injection | **do not try to make tenant mode boot** (13.5c owns refresh wiring); the `None`-source boot refusal is deliberate |
| `crates/maos-loom-lite/tests/read_path_chokepoint.rs` | static counts `:71-113` (`team_guard == 3`, `region_guard == 2`, `0` in `write_with_source`/`pool`) | **ideally unchanged** | if a count must change, update assertion + implementation in ONE commit and justify — never retrofit the test |
| `crates/maos-loom-lite/tests/tenant_wall_live.rs` (443 L) | `two_datname…matrix :155-262`; **`d1_forged_stamp_is_still_served_boundary :264-305` asserts SERVED**; `per_team_merkle_independence_mixed_v1_v2 :314-443` (synthesizes the v2 row by **raw SQL** because *"13.2 has no production path that writes source_team (that is 13.3)"* `:352-353`) | +live cross-team legs (a)–(d); **replace the raw-SQL synthesis with the real production path** | **leg #5 must keep asserting served** (H6); `#[ignore]` + `MAOS_TEST_POSTGRES_TEAM_A/_B` + `static PG_LOCK :18-22` |
| `xtask/src/check_multi_tenant_loom.rs` (352 L) | 10 legs; `ABSENT_SUCCESSORS :17-23`; substrate oracle `:56` (2-element); v2_2-blocking self-assert `:119-127`; **stale `:6` "two live legs"**; **duplicated `23202` `:275`** | +5 legs; remove `ABSENT_SUCCESSORS[0]`; fix `:6` and `:275` | the `BindingClass`/`LegResult`/non-vacuity model `:26-105`; leg #5; the registry self-assert |
| `docs/adr/ADR-055-multi-tenant-loom.md` | §3 *"Story 13.3 may widen the caller-facing taxonomy"*; §4 13.3 hand-off; Consequences carried gaps | crossing semantics, H7 clarification, AC4 posture amendment, updated carried gaps | the carried-gaps honesty (13.5b/13.5c open; **region residual open with no successor**) |
| `tests/coverage-matrix.yaml` + `.github/workflows/discipline.yml` | ADR-055 `:58-82`, `valid_until: '2027-07-17'`; job `:2666-2704` | update `notes` + `valid_until`; extend the existing job step | the 5-point enrollment; **no new gate**; `ABSENT_SUCCESSORS` and the matrix prose are duplicated with **no mechanical cross-check** — update BOTH |

### Idioms to copy verbatim (do not reinvent)
- **Versioned schema/domain dispatch under a frozen predecessor:** `manifest.rs:51-58,191-254` (V1/V2 → copy for V3) — the house pattern, also used by leaf v1/v2.
- **Port trait in loom-lite + adapter in maos-bin, no crate edge:** `tenant.rs:22-26` + `maos-bin/src/tenant_map.rs:105` (the `CrossTeamConsentPort` template).
- **Additive DDL migration:** `schema.rs:76-97` (`DO $$ … ADD COLUMN IF NOT EXISTS`).
- **Derive-verify-key-from-claimed-identity:** `bundle.rs:270-307` + `sealed_export.rs:72,88`; anti-tautology proof shape `bundle.rs:781-842` (independent verifier from the write codec **+ a positive control**).
- **Aggregator gate leg:** `manifest.rs:1587-1599` / `tenant_map_13_1.rs:288-293` — add assertions to the matrix so the `--exact` single-test invocation still emits `"running 1 test"`/`"1 passed"`. A new top-level `#[test]` not called from the matrix is **un-gated**.
- **Live-leg gating:** `#[ignore]` + panic-if-env-unset + `static PG_LOCK` (`tenant_wall_live.rs:18-32`).
- **Merkle inclusion:** `maos_audit::erasure::merkle::{prove_inclusion :119, verify_proof :183}` — note `build_tree` sorts+dedups `:64-65`.

### Traps (each has already bitten this codebase, or is proven live at HEAD)
1. **Asymmetry via a `team` field on `ConsentTuple`.** One endpoint ⇒ *"anyone may send to team-b"* (D6). Ordered pair or nothing.
2. **Cross-team clobber (H4).** `source_team` is not in the UNIQUE key; LWW resolves on `(source_ts, source_region)`. A cross-team copy **overwrites** a same-key destination first-party row. Silent data loss. Prove it red.
3. **Putting attestation verification in `team_guard`.** It is **sync, `spirit_pid`-only, and pre-query** (`store.rs:505`, fires at `:359` *before* the client exists at `:360`). There is no row to verify. Use a **new post-query `attestation_guard`** at `region_guard`'s two sites — and extend the `read`/`scan` SELECT lists, which today do not even fetch `source_team` (`store.rs:365-367`).
3b. **Adding a `team_guard`/`region_guard` call site.** `read_path_chokepoint.rs` counts **source text** across three `include_str!`'d files: exactly 3 `team_guard`, exactly 2 `region_guard`, exactly 0 in `write_with_source`/`pool`. Pin the new `attestation_guard` at 2 in the same commit.
3c. **A new `CollectivePortError` variant.** Wildcard-free kernel-core match (`memory/mod.rs:189-197`), enum not `#[non_exhaustive]` ⇒ `E0004` in kernel-core ⇒ **AC6 dead**. And the ABI gates will *not* catch it — they only inspect `maos-spirit-abi` (`abi_diff.rs:8`). `cargo check -p maos-kernel-core` is the real detector.
3d. **Leaving `manifest.rs:235`'s teams-block guard as `== COHORT_SCHEMA_V2` when adding V3.** The V3 canonical body then omits teams entirely — placement becomes **unsigned and substitutable** while the signature still verifies. Silent. Not a compile error.
4. **Flipping leg #5.** It forges a `source_log_ref` — the **region** axis (11.2b D1), a different threat. G7 ratified leaving it *served*. Team-axis only (H6).
5. **`base_seed` below the port.** A signing oracle in the data tier (Vex, 13.2 Round 1). Inject **public** verifying keys only (G4).
6. **`ranged_recall` for anything cross-team.** Path-addressed, capability-free, returns everything when `spirit_filter = None`. `spirits/researcher` pins it out at compile time. Forbidden.
7. **Editing a kernel comment.** `check-kernel-baseline` counts **every line of every `.rs`** under `maos-kernel-core/src`, comments and `#[cfg(test)]` included, with **exact equality**. Even `cargo fmt` reflow reds it.
8. **Trusting the epic's file paths.** Everything is under `src/replication/`, not `src/`. `maos-tenant-map` does not exist. `ETenant*` does not exist (D10).
9. **Assuming the replication path works because tests pass.** **Zero production callers** (D9). Test-green ≠ wired.
10. **A skipped live leg is *unmeasured*, not green** (`check_cross_region_consensus.rs:127`). Provision the substrate.
11. **Claiming authorization provenance.** ADR-049 §7 forbids it in any AC, gate, or doc. Origin ≠ authorization; a consent-absent crossing is an observable state, never folded into a clean total.
12. **Writing "production cross-team write path" anywhere (D12).** It is the sentence the room struck. The row is production-real; the crossing is not. A dead-wire negative guards this claim — if you find yourself wanting to delete leg (f), the design has drifted into 13.5c.
13. **Re-hashing `value_data` at read (D13).** The at-rest seal is unpaired *and* non-deterministic, and apply re-seals. Persist `canonical_hash`; let the signature cover the payload.
14. **Trusting `verify_proof` alone.** `siblings.is_empty() && leaf == root` ⇒ `true` (`merkle.rs:192`), and the attacker who writes the row writes the root. **The signature over the root is the control.** Third time this room has killed this exact shape.
15. **Adding `COHORT_SCHEMA_V3` before the floors are fixed (D14).** `state.rs:273` is a hardcoded *pair*. Add V3 first and a V2 manifest with a higher revision is silently accepted, taking the cross-team consent block with it.
16. **Touching `to_canonical_bytes` before the v2 golden exists (D15).** There is no oracle. You will not know.

### What this story may and may not claim
| May claim | May NOT claim |
|---|---|
| **The ROW is production-real** — `source_team` + attestation persisted on `store.rs`'s actual `write`/`read`/`scan`, live the instant team mode boots | **That the CROSSING is production-real** (D12) — no daemon holds both a manifest and a store; initiation/transport/consent are a mechanism proven at the verify/apply seam. Never write *"production cross-team write path"* |
| That the crossing mechanism is **proven at the verify/apply seam**, and that a **dead-wire negative proves it is unwired** | That 13.3 reaches even **12.4a-parity** — 12.4a's own initiator (`request_read` ← `request_consented_summary`) is **dead code**; the production Digest Spirit is a no-op (D12) |
| Read-time attestation verification **in the plaintext posture** | That it holds under a **configured at-rest seal** — the collective read path is already broken at HEAD (D13); gap owned by **13.5a** |
| A consented cross-team crossing **lands a real row** in the destination team's database when driven at the seam | That the daemon **boots in tenant mode** (D11 — `main.rs:2286` refuses by design; 13.5c) |
| `source_team` is **persisted and cryptographically bound** to what `verify` proved | That a **forget-cascade** consumes it (D7 — `CollectiveMemoryPort` has no erase; 13.5b) |
| **Asymmetric** consent: A→B granted does not admit B→A, refused by **absence of an ordered-pair grant** | That per-(peer,role) tuples already expressed this (D6 — they name one endpoint) |
| A foreign-team row without valid attestation is **refused at read** (team axis) | That the **region** `source_log_ref` residual is closed (open, **no named successor**); that leg #5 changed |
| Merkle **inclusion** against the persisted root | **Multiplicity or ordering** (`build_tree` sorts+dedups); **authorization** provenance (ADR-049 §7) |
| Five refusal causes **distinguishable at the port** without string-matching | An **audited** refusal, or per-operator TL isolation (13.5c) |
| ZERO kernel-core Δ @23202; `WriteEntryPoint` untouched | That the kernel enumerates this write path — it enumerates **kernel-mediated** writes (H7, stated in ADR-055, kernel comment **unedited**) |
| Cross-team rows exist, so a read has something to verify (closing 13.2's hand-off) | **Multi-hop distillation provenance** or **cross-wall raw traceback** (D1–D5 → **13.3b**) |

### Testing standards
Live legs `#[ignore]`-gated + `MAOS_TEST_POSTGRES_TEAM_A/_B`-driven + panic-if-unset; hermetic legs plain `cargo test`; each gate leg is exactly one `--exact` test carrying the vacuity guard. **The asymmetric-consent negative, the clobber negative, and the unattested-read refusal must be proven red through the REAL store verdict over live Postgres** — a hermetic stub cannot prove a cross-team write path that did not exist before this story. Verifiers are **independently derived from the write codec** with a **positive control** (never a re-call of the function under test, never a reject-everything stub). §A7 reflexes per leg: derive-and-reconcile on applied/refused counts; same-region (not cross-region) crossings, since cross-region is already refused by `region_guard` and proves nothing new (F9); v2 canonical golden frozen **before** V3 lands.

## Project Structure Notes
- **New:** `CrossTeamConsentPort` (trait in `maos-loom-lite/src/tenant.rs`, impl in `maos-bin/src/tenant_map.rs` or a sibling); `COHORT_SCHEMA_V3` + `[[cross_team_consent]]`; nullable attestation columns; 5 gate legs. **No new crate, no new dependency edge.**
- **Modified:** `manifest.rs`, `tenant.rs`, `store.rs`, `replication/bundle.rs`, `schema.rs`, `collective_memory.rs` (one variant), `tenant_map.rs`/`main.rs`, `tenant_wall_live.rs`, `check_multi_tenant_loom.rs`, `discipline.yml`, `coverage-matrix.yaml`, `ADR-055`.
- **Frozen:** `crates/maos-kernel-core/**` (esp. `memory/write_entry_point.rs` — including its comments); the `CollectiveMemoryPort` **method set**; the exact 9.4b/13.2 derivations; v1 **and** v2 manifest canonical bodies; the v1 leaf golden; `maos-cohort/src/pin.rs` (a different trust root — do not cross-wire with HKDF-derived team keys).
- **Deliberately NOT in this story (owner named):** multi-hop distillation provenance + cross-wall `log.recall` + ADR-058/013/018 → **13.3b** (H1); collective erase/legal-hold → **13.5b**; multi-store routing, refusal **audit**, per-operator TL isolation, tenant-mode boot → **13.5c**; 3-team substrate + journey → **13.6**. The **region** `source_log_ref` presence residual remains **OPEN with no named successor**.
- **Variance from the epic sketch (each grounded above):** epic AC3 (I11 chain in the bundle) and AC4 (consented cross-wall `log.recall`) are **removed to 13.3b** — no wire representation exists, no ratified §15 home exists, ADR-018/013 do not exist, and AC3 requires reversing Story 8.10's citer-auth control (D1–D5, H1). Epic AC1's forget-cascade rationale is **downgraded to persistence-only** (D7). Epic AC2's *"tuples extended to the team axis"* becomes an **ordered-pair grant under a new schema version**, because tuples cannot express asymmetry (D6, H3). **Three items the sketch omitted are added:** per-row attestation + team-axis read verification (13.2's explicit hand-off, H5), the widened taxonomy (ADR-055 §3 + `ABSENT_SUCCESSORS[0]`, H8), and the cross-team clobber closure (H4 — a live data-loss hazard no artifact had noticed).

## References
- [Source: `_bmad-output/planning-artifacts/epics/epic-13-reza-cortex-v2-2.md`] — 13.3 AC sketch (:67-73), story table (:36), gate discipline (:118-124), kernel-delta budget (:126-127), pre-dev checklist item 5 (:141)
- [Source: `architecture-maos-minimal-opus/15-full-spectrum-v2-2.md#15.3`] — ADR-055 decision (:49-53); row ownership + `source_team` (:64); **multi-hop provenance paragraph (:66)** — the text D1–D5 disprove; gate sketch (:70); §15.11 ratification (:158-177)
- [Source: `…/architecture/architecture-maos-2026-07-06/reviews/review-rubric-reconcile.md:61-62`] — **"Cross-team asymmetric consent envelopes — IMPLICIT ONLY"** (F-6, `:61`) and **"NO §15 HOME … the single most important reconciliation failure"** (F-1 HIGH, `:62`). The primary evidence for H1.
- [Source: `docs/adr/ADR-055-multi-tenant-loom.md`] — §3 *"Story 13.3 may widen the caller-facing taxonomy"*; §4 the 13.2→13.3 hand-off; Consequences carried gaps
- [Source: `docs/adr/ADR-049-cross-region-collective-memory-consensus.md:89-91,121`] — **origin ≠ authorization provenance; "No claim anywhere — AC, gate, or doc — may assert the bundle establishes authorization provenance"** (binding on AC1/AC3)
- [Source: `docs/adr/ADR-012-typed-intent-a2a-consent.md:12,16`] — cross-**Host** frame admission on `(peer, intent-class)`; silent on reads/recall/teams (D4)
- [Source: `docs/adr/ADR-014-distillation-audit-chain.md:12`] — I11 definition (13.3b's basis; ADR-018 **absent**, D5)
- [Source: `_bmad-output/implementation-artifacts/13-2-multi-tenant-loom-cryptographic-tenant-boundary.md`] — Option-A entry closure; **the explicit 13.3 hand-off** (:73, Completion Notes); G2/G4/G6/G7; the may/may-not-claim discipline
- [Source: `_bmad-output/implementation-artifacts/13-1-multi-tenant-loom-physical-tenant-wall.md`] — F9 (same-region vs cross-region tautology), F18 (typed-lossy-at-port), F21 (no new crate)
- [Source: `_bmad-output/implementation-artifacts/epic-12-retro-2026-07-13.md`] — B6 §A6 non-degradable on 13.1–13.3; *"a claim standing in for a control"*
- Code — **cross-team write:** `store.rs:63-85,242-264,281-344,313-327,353-407,505-532,575-584,591-618`; `replication/bundle.rs:42-55,66-80,127-152,192-258,270-307,353-371,427-438,456-464,532`; `replication/leaf.rs:45-52,61-78,97-132,155-177`; `schema.rs:33-72,76-97`
- Code — **consent/manifest:** `maos-cohort/src/manifest.rs:51-58,124-156,168-186,191-254,296-310,419-437,464-530,551-648,1587-1599`; `consent.rs:10-116`; `state.rs:378-395,495-560`; `maos-a2a-core/src/router.rs:445-463,502-524,660-687,1107-1147`; `cohort.rs:34-91,111-138`; `tests/cohort_consent_chokepoint_12_2.rs:16-22`
- Code — **provenance/recall (13.3b evidence):** `maos-iac/src/adapter/distillate.rs:196-253,256-288,318-328,330-359,448-470`; `maos-domain/src/ports/log_recall.rs:14-28`; `maos-iac/src/adapter/log_recall.rs:254-400`; `maos-audit/src/log_composition.rs:104`; `spirits/digest/src/lib.rs:219,336-342,391-434`; `maos-domain/src/invariants/i11.rs:30-39`
- Code — **ports/kernel/keys:** `maos-domain/src/ports/collective_memory.rs:23-39,60-97`; `maos-kernel-core/src/memory/write_entry_point.rs:35-88`; `memory/mod.rs:684`; `maos-domain/src/team.rs:9-33`; `maos-loom-lite/src/tenant.rs:11-26`; `maos-bin/src/tenant_map.rs:73-160`; `main.rs:2274-2335,7481-7487`; `maos-audit/src/sealed_export.rs:15-19,27-41,51-60,72-88`; `maos-audit/src/erasure/merkle.rs:62-65,119,183`
- Code — **gate/CI/baseline:** `xtask/src/check_multi_tenant_loom.rs:1-24,26-105,119-127,130-280`; `check_kernel_baseline.rs:30-31,65,94-108`; `xtask/kernel-core-baseline.toml` (`src_lines = 23202`, HISTORY); `xtask/error-catalog.toml:21-38`; `xtask/gate-registry.toml:300-301`; `tests/coverage-matrix.yaml:58-82`; `.github/workflows/discipline.yml:2666-2704,2881`; `crates/maos-loom-lite/tests/read_path_chokepoint.rs:71-113`; `tenant_wall_live.rs:18-32,155-262,264-305,314-443`

## Dev Agent Record

### Agent Model Used

_(frontier-class allowlist per E11 retro A1 / E12-B3 — record the exact model at dev start)_

### Debug Log References

_(record CATCH-0 results here: baseline pin/actual, the six D-premise re-confirmations, ABI gate result)_

### Completion Notes List

### File List

### Change Log

| Date | Change |
|---|---|
| 2026-07-18 | Story 13.3 created (create-story analysis, 4 code-verification scouts @652347b8). Eleven epic-sketch premises disproved (D1–D11); two unnoticed hazards surfaced (cross-team clobber, `ranged_recall`). Scope re-cut to the governed cross-team row; multi-hop provenance + cross-wall recall recommended out to **13.3b** (H1). 6 ACs, ZERO kernel-core Δ @23202 (pin == actual verified), no new crate. Forks H1–H9 open for preflight. |
| 2026-07-18 | **Party-mode preflight — 3 Round-2 scouts broke the story's own headline.** **D12:** no production initiator exists, and the 12.4a precedent 13.3 would copy is itself half-wired (`request_read` and `request_consented_summary` both dead; production `DigestSpirit` is a no-op) → **headline reframed to "the row is production-real; the crossing is not."** **D13:** the at-rest seal is **unpaired at HEAD** (`open_at_rest` has no callers; `seal.rs:118`'s promised composition-root unseal does not exist) → `canonical_hash` design + plaintext-posture scoping + **13.5a named owner**. **D14:** `state.rs:273`'s downgrade refusal is a hardcoded **pair, not a floor** — V3 would arm a silent signed-artifact downgrade → floors land in a separate provable commit **before** V3. **D15:** no v2 golden exists and every v2 test is build-verify symmetric → **freeze it first, before anything touches `to_canonical_bytes`.** **D16:** the story's own V3 justification was false (zero committed manifests); V3 kept on the correct argument (pinned `SIG_DOMAIN_V2`). Decisions: **H1 split RATIFIED** (ADR-055 §5 assigns the wiring to 13.5c verbatim; Boundary's cohort-daemon-store middle withdrawn on that text; Splinter's re-sequence died on the 13.5c dependency cycle) · **dead-wire negative added as gate leg (f)**, green while unwired, inverted by 13.5c · **signature-over-root is the control, not the proof** (root+proof+row share one table — the 13.2 registry shape, third occurrence). Still 6 ACs. Escalated out: 13.5c scoping pass (Splinter's carry). Retro item: dead-wire negatives on every mechanism story. |
| 2026-07-18 | Independent checklist validation (fresh context, 45+ citations spot-checked) found **5 CRITICAL regressions the draft missed**, all since folded in: **(C1)** a new `CollectivePortError` variant is `E0004` in kernel-core (`memory/mod.rs:189-197` wildcard-free, enum not `#[non_exhaustive]`) — AC4 as drafted would have blown AC6's ZERO-Δ; redesigned to `Transport(TransportCause)`. **(C2)** AC3's "verify inside `team_guard`" is structurally impossible (sync, pid-only, pre-query at `:359` before the client at `:360`; SELECT doesn't fetch `source_team`) — replaced with a post-query `attestation_guard` + SELECT extension. **(C3)** `write_with_source` has **33 call sites across 2 crates** (27 in `cross_region_live.rs`, 1 in `maos-bench`) — blast radius + `WriteSource` struct recommendation added. **(C4)** Task 0's ABI check was a **null control** (both gates hard-code `maos-spirit-abi`) — replaced with `cargo check -p maos-kernel-core`. **(C5)** `manifest.rs:235`'s `== COHORT_SCHEMA_V2` teams guard and `:196`'s `_ =>` domain fallback make a V3 manifest **silently unsign the teams block** — both now explicit, with a proving negative. Plus 8 enhancements (`SUPPORTED_COHORT_SCHEMAS` arity, ~26 `StoreError` touch points, `adapter.rs` wildcard-free match, 5 aggregator sub-tests, dual chokepoint counts, `gate-registry` v2_2-blocking, `derive_team_pubkey` reuse) and 6 citation corrections. |

## Status

**ready-for-dev.** H1 **RATIFIED** (split → 13.3b) on ADR-055 §5's verbatim assignment of the wiring to 13.5c; headline reframed to *"the row is production-real; the crossing is not"* (D12); the seal gap (D13 → **13.5a**) and the V3 downgrade hole (D14 → Task 1b, floors before V3) given owners and ordering. H2–H9 carry grounded, room-confirmed recommendations the ACs assume. 6 ACs, ZERO kernel-core Δ @23202.

**One item escalated OUT of this story and still open (Decision 1b — Splinter's carry):** **13.5c needs a scoping pass before 13.3 development starts.** It currently owns 7 distinct concerns and is the only story in Epic 13 with no ZERO-Δ claim; three consecutive mechanism stories have deferred every production wiring into it. That is an Epic-13 sequencing decision for Lunarpulse, not a 13.3 blocker.

**Logged for the Epic-13 retro (Wildcard's unanswered question, Dana's forward-only ruling):** every mechanism-story gate carries a **dead-wire negative** for whatever it does not wire. Vex's read: it would have caught 12.4a's dead initiator eight months early.
