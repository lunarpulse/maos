# Epic 15 Retrospective — Foundations (W0), closed green 2026-09-12

> **Convened as a REFINEMENT session**, not a write-up (operator directive 2026-09-06, confidence rule 9, binding). The session's primary deliverable is the Epic-16 OLD→NEW edit table in §6, filed against the tree at HEAD `843d5365` **before Epic 16's first story is created**. Rows, not prose.
>
> **Measured, not read.** Every figure below was produced by running the thing, at HEAD `843d5365` on 2026-09-12. Where a number came from a document rather than a command, it says so.

---

## 1. The close — all eight exit legs green

Epic 15's hermetic exit block, executed:

| Leg | Result |
|---|---|
| `cargo run -p xtask -- check-empty-kernel` | ✅ PASSED (0 violations) |
| `cargo run -p xtask -- check-service-boundary` | ✅ PASSED |
| `cargo run -p xtask -- kloc-check` | ✅ PASSED (aggregate 158000) |
| `cargo run -p xtask -- check-env-contract` | ✅ PASSED |
| `cargo run -p xtask -- check-j1-loopback-delegation` | ✅ PASSED |
| `cargo test --workspace --no-fail-fast` | ✅ **exit 0 — 4231 passed / 0 failed / 119 ignored, 508 binaries** |
| `cargo run -p xtask -- check-exit-commands` (new, 15-3) | ✅ PASSED — 7 epics / 33 tokens resolved / 4 owed while Epic 15 was open; **6 / 26 / 4 after this retro flipped `epic-15` to `done`**, because 15-3's roster is filtered by sprint status and a closed epic's block leaves it. Same 4 owed tokens either way |
| `cargo run -p xtask -- release-dry-run` (new, 15-4) | ✅ PASSED |

The epic opened against **eight** red controls — five gates, four workspace test failures and one Blocking job that could not pass by construction. Mid-epic at `2fd492c0` the workspace suite was still exit 101 with 4 failures. It is now exit 0.

**Delivery:** 6/6 stories `done`. Kernel pin 24472 → **24474** (+2, 15-1 AC2 — the only authorized delta). Five ADRs minted (060–064). Decision register 23 rows, **12 open** (15 at Epic-14 close).

⚠ **One thing the exit block does not contain, by design:** the signed tag. It was moved to the operator lane as `ops-first-signed-tag` under R2 rule 2 and is still `backlog`. `git tag -l` returns `frozen-kernel-v2.0` and nothing else.

---

## 2. Epic-14 action follow-through

| # | Action | Vehicle named | Measured status 2026-09-12 |
|---|---|---|---|
| A1 | Aggregate green + cut `v0.1.0-alpha.1` | `15-1`, `15-3` | 🟡 **half** — green ✅; tag ❌, re-homed to `ops-first-signed-tag` |
| A2 | One `CURRENT_PHASE` | `15-2` | ✅ done — **by `15-3`, not `15-2`**; the retro named the wrong vehicle |
| A3 | Land the model-pin lane (W0-3) | "hotfix rows" | ❌ **not started**, and silently moved **W0 → W1+W2** unfiled |
| A4 | Daemon RPC + applied sandbox | `recovery-w1-*` | ⏳ not due — Epic 16/17 |

**And none of the four was ever written into `sprint-status.yaml`.** That section holds 18 rows — six each for Epics 11, 12, 13 — and zero for 14. They lived only in the retro markdown, which no machine reads.

That is the **third confirmed appearance of one defect**: Story 14-0 found the decision register's charter *"died on measurement — no machine reads it, so the binding rule is false as written."* Epic 13's retro found tracking erring pessimistic from the same single source. This retro found the same shape a third time, in the output of the very retro that was supposed to carry the lesson. A3's wave slip is the direct cost.

**Closed this session.** See §5 A1 — the gate shipped here, proven red on `RetroWithoutRows(14)`, and Epic 14's A1–A4 are backfilled as queryable rows.

---

## 3. What the epic actually produced — six for six

Every story disproved its own epic section before writing a line. This is the epic's real product; the green gates are a consequence.

| Story | Model | Premises measurement killed |
|---|---|---|
| **15-1** | `zai/glm-5.3` | "4 red gates + 1 test red" was **eight**; AC1's J1 fix was *unimplementable and self-contradictory in the same sentence*; AC1's CI citations were false **in kind**, not in line; AC3's "9/8 split" was 18/1 and its two halves annihilated each other |
| **15-2** | `openai-codex/gpt-5.6-sol` | The epic's own AC2 was **arithmetically vacuous** — spendable to 25% of its own grant; a ratified architecture clause forbade exactly what the story does; the `maos-cohort` row reserved against a grant that did not exist |
| **15-3** | `opus-5` | AC4's justification **inverted** — the gate was live and retiring it dropped two controls; the phase duplication was 16 constants + 12 function copies + a repo variable, not 8; AC3's tokeniser clause was a **null control in its own text** |
| **15-4** | `openai-codex/gpt-5.6-sol` | The release **dies twice** and the AC named one; `maos-*` never matched `maosctl-*`, excluding the story's headline addition; AC4 unimplementable as written; the aggregate was red at HEAD and nobody had filed it |
| **15-5** | `zai/glm-5.3` | AC1's numbering premise false; **"this ◆ story has no mechanical control — `ls` passes on five empty files"**; three of ADR-060's four ratified premises unbuildable at HEAD |
| **15-6** | `openai-codex/gpt-5.6-sol` | **AC1's command already exits 0 at HEAD** — every guarantee it named was a green that proved nothing; the recorder never wrote `response.text`, so record→replay was silently empty; the story **reds 15-5's own gate, twice**, and 15-5 was `done` |

All six models are inside the frontier-class allowlist. The §A6 review net was non-degradable on the two ◆ stories and ran on all six.

---

## 4. The recurring finding — *the null control*

Three independent instruments, one epic, the same shape: **a test that asserts a value it set itself.**

| Instrument | Story | What the review found |
|---|---|---|
| `_aggregate_alarm` | 15-2 | No proven-red control **anywhere**; `alarm_fires…` built a `Report` by hand and asserted a field it set, so the parser never ran. A workspace test had been standing in for the control *on a timer* — scheduled to fail after 22% of the authorized draw |
| D11 `xtask` ceiling ratchet | 15-4 | *"Never reads the tree it claims to ratchet"* — synthetic YAML, hard-coded numbers, four green tests over a gate that ratcheted nothing. All four review layers converged on it |
| `require_absent` anti-rot anchors | 15-5 | `read_to_string(...).unwrap_or_default()` made all four anchors pass **silently** when the anchor file was deleted or renamed |

**Also found, and it is the one that should worry us most:** Story 15-4 landed a **line-neutral (8+/8−)** `#[cfg(target_arch)]` gate on seven syscall constants in `crates/maos-kernel-core/src/security/sandbox/linux.rs` — inside a story whose frontmatter swore *"ZERO kernel-Δ … touches no kernel-core file."* `check-kernel-baseline` reported `PASSED (24474, pinned 24474)` throughout, because the pin is a raw `.rs` line **count**. A kernel-core security-surface change was caught by a reviewer reading the diff, not by any gate. The edit is necessary and stays — a probe crate proved `cargo check --target aarch64-unknown-linux-gnu` emits 7× `error[E0425]` without it — and the operator amended the declaration rather than reverting. The gate gap is E15-A5.

**And a fourth shape, found by this session's own verification runs:** Story 15-5's review patched *"three identical zero-information `## run @untracked` appends"* in `intent-lineage-coverage-report.md` down to one. Running the workspace suite today re-appended two more in a single pass — the patch cleaned the **artifact**, the **producer** still appends on every run. A control applied to the output rather than the cause regenerates the defect on the next execution. Not filed as its own action (it is cosmetic and uncharged); recorded because it is the same reasoning error as the null controls above, seen from the repair side rather than the test side.

**Review volume:** 15-4 alone closed 20 patch findings, 1 decision, 0 deferred, 4 dismissed across six layers including non-author runtime re-execution. 15-6 closed 11 patch + 3 decisions. 15-3 closed 15 patches. Zero stories shipped with an open decision.

---

## 5. Actions (6) — all written as queryable rows in `sprint-status.yaml`

| # | Action | Owner | Status |
|---|---|---|---|
| **A1** | A retrospective's actions must reach the tracker, not just its document | Amelia + Murat | ✅ **done — shipped in this session** |
| **A2** | Land the model-pin currency lane, or file its re-homing with a named owner and wave (E14-A3 slipped W0→W1+W2 unfiled) | John + Winston | open |
| **A3** | Cut the first signed tag: `ops-provisioning-secrets-and-accounts` → `ops-first-signed-tag`. Oldest unclosed promise in the lane; the remaining half of E14-A1 | Lunarpulse | open |
| **A4** | Close register row **D11** or re-point it at `21-4` with a **queryable** deadline (15-4 filed its closure here) | Winston + Murat | open |
| **A5** | Give `check-kernel-baseline` a file set and content hash **before 16-5's re-pin** | Winston | open |
| **A6** | Carry the null-control question into every Epic-16 review net: *does this test read the tree, or only what it set itself?* | Amelia + Murat + Dana | open |

**A1's control, shipped here** — `xtask/tests/retro_action_items.rs`, kloc-free (`xtask` is at 43797/43797 zero headroom; `xtask/tests/` is excluded from the measurement, the same placement and reasoning as `d11_xtask_ceiling_ratchet.rs`). Nine synthetic vectors exercise the decision procedure; two tree-bound tests apply it to the real `sprint-status.yaml` and the real retro corpus, with denominator assertions on both, so it cannot become the fixture-only null control it exists to replace.

Rules enforced: every `epic-N-retrospective: done` from Epic 11 forward owes ≥1 row (the floor is a named constant with its reason — epics 0–10 predate the convention and must not red); every row carries a non-empty `action` and `owner`; every `status` is one of `{open, in-progress, done}`; every row's epic has a retrospective document to have come from.

**Proven red before the backfill:** `RetroWithoutRows(14)`. Green after.

**A4's deadline note, so we do not repeat D19:** D19 was closed as **UNQUERYABLE** because its deadline — *"before the next `j1-*` story leaves ready-for-dev"* — was self-voiding; there was no next `j1-*` story. A4's replacement deadline must be derivable from the tree.

---

## 5b. Post-session rulings — 2026-09-12, same day, operator-ratified

Five decisions taken after the retrospective's first pass, when the operator asked for the three real drifts to be
assigned rather than noted. **A correction comes first, because it changes what the operator does.**

### ⚠ Correction — A3 as first filed was not actionable

I filed *"cut the first signed tag"* as an open action. Measured afterwards, it is blocked three deep:

- `check_release_precondition.rs:58` — *"discipline runs only after the commit reaches main"*. The tag's `sign-and-publish`
  refuses a SHA that has not reached `main`.
- `main`'s `release.yml` is `on: push: tags: ['v*']` **only**. 15-4 added `workflow_dispatch` **on `recovery-lane`**, and GitHub
  exposes a dispatch trigger only for workflows present on the **default** branch — so the pre-tag Darwin rehearsal
  `tag-procedure.md` mandates **cannot be run today either**.

Every node except provisioning is downstream of the operator's merge decision, which is theirs to take and is deliberately
not scheduled here.

### The three real drifts — assigned

| Drift | Home | Owner | Disposition |
|---|---|---|---|
| **D1 residue** — model-pin lane slipped W0 → "W1+W2" | `model-pin-currency-gate-and-retired-pin-sweep` | Winston (wave) + Murat (gate) | **Wave decided: W1, parallel with Epic 16** — it touches no `main.rs` line, so it does not contend for the write lock Epic 16 holds. "W1+W2" was an ambiguity, not a schedule |
| **D2** — kernel pin blind to line-neutral edits | **NEW `16-0-kernel-pin-content-hash`** — opens Epic 16, **blocks 16-5** | Winston + Murat | Promoted out of the 20-3/W5 lane: W5 was too late for a pin 16-5 moves in W1. **Not** an AC inside 16-5 — a story must not build the control that audits its own edit (13.6e). 4 ACs drafted; AC2's falsifier is 15-4's **actual** seccomp commit, not a fixture |
| **D3** — sprint key outlives its scope | `check-epic-close-coherence` (a 6th mechanical check) | Murat | Carried as A6's sibling; the key/scope mismatch is checkable once a story file's H1 is the declared scope |

### Load management — rule 10, and three moves

**Honest re-count of my own §4 number.** The "34 / 55 mentions" was a raw grep; most are dependency citations or items
already inside those stories' charters. By hand: **4 genuinely new** filings to 20-3 (+1 sibling row), **5** to 21-4.
Smaller than I said — **but worse in kind**: two of 21-4's five (`spawn_and_invoke`'s EPIPE, the `_2c.rs` J1 blind spot)
have nothing to do with *one instrument and the env registry*. They landed there because 21-4 had become the default
bucket. That is the mechanism, not the volume.

| Move | What |
|---|---|
| **Rule 10 — route by charter, never by bucket** | Added to the binding Confidence-rules block of **all six** recovery-lane epics (16–21). A deferral names one of three destinations and never a default one: **(a)** fix at origin — the eased ceiling rule removes the budget reason this was declined; **(b)** the story that already touches that file, asked **first** not last; **(c)** a theme story only on charter match |
| **20-3 split** | → `20-3a-gate-honesty` (correctness of existing gates: EvidenceState, honest-N, ledger loud-fail, `BindingClass` on 42 names) + `20-3b-gate-retirement-and-coverage-generator` (spike, generated matrix at `mode: error`, **10 retirements × 6 enrolment sites**, xtask net −4.5k…−5.2k). Different risk profiles, different review surfaces, and 20-3b is what **funds** xtask. 20-3a before 20-3b |
| **21-4 AC3 → 21-3 AC6** | `MAOS_REGION_HOME` boot reconciliation (D4a) is a **production boot refusal** — a runtime behaviour change, and the only AC there that could break a running deployment — inside an instrument story. 21-3 owns cohort identity and already declares those crates. Same wave; ownership only. The AC3 slot is kept, never deleted, so the D4a citation resolves |
| **2 off-charter re-routes** | `spawn_and_invoke` EPIPE → **18-1** (already cites `stdio.rs:24-37`) · `_2c.rs` J1 blind spot → **19-4** (Epic 19 owns J1 and `demo-j1`) |

**Why this matters:** Epic 14 closed early because *"of Epic 14's 11 remaining rows, 9 were gates, registries, ledgers or
ceilings … priced at 4–6 weeks with no operator-visible behavior at the end."* Both 20-3 and 21-4 sit at the **end** of
this lane (W5, W6), so every deferral compounds into the two stories immediately before ship.

### RECOVERY-LANE CEILING RULE — eased, with the boundary the operator drew

> *"I approve ease the constraints on the budgets of kloc until we complete the future epics. We seal it after all
> developed. Be aware only important and necessary increments allowed, no slops or uncontrolled addition to the kloc."*

Half of this was **already built**: 15-4's `d11_xtask_ceiling_ratchet.rs` governs on **cause, not count** (cite an OPEN
story) and **freezes once every numbered epic is `done`** — which *is* "we seal it after all developed", derived from the
tree rather than promised for a date. The change generalises that from `xtask` to every crate.

| | Before | After |
|---|---|---|
| Who may raise | epic retro / authorized grant / operator-ratified re-base story until 21-4 | **Any story**, if ① code exists and was `cargo fmt --all`-measured **before** the ask ② the raise cites an **OPEN** sprint key ③ measured figure + driver in the same commit |
| Formula | `measured + max(100, 2%)` mandatory | **Default, not a cap** |
| Expiry | (d) dies when 21-4 lands | **Seals when every numbered `epic-N` is `done`** |
| `maos-kernel-core` | zero headroom, per-line FLAG-Winston | ⚠ **UNCHANGED** |
| Kernel pin | anti-drift instrument | ⚠ **UNCHANGED** |

**"No slops" is the evidence requirement, not the ceiling value.** What is eased is *who may raise and when*. What is
unchanged is that the code exists first, is measured formatted, cites an open story, and records its driver — and that a
reservation for planned growth stays forbidden (a grant is a global consumed by whichever story measures first).

**The boundary, in the operator's own terms.** `kloc.toml`'s own rule block says the two are different mechanisms:
*"the PIN is anti-DRIFT (it never forbade growth, only UNRECORDED growth); the CEILINGS are anti-GROWTH."* Budgets were
eased. Easing the pin would silently widen the drift `16-0` exists to close.

**Landed in three places, and it is enforced rather than prose** — which the old rule explicitly was not
(*"No xtask code parses or enforces the policy comments in this block"*):

1. `xtask/kloc.toml` — RECOVERY-LANE CEILING RULE block
2. architecture §15.5 clause **2(e)** — amended in the same commit (rule 9)
3. `xtask/tests/recovery_lane_ceiling_rule.rs` — kloc-free, **10/10**: every refusal proven, the kernel-core boundary
   asserted against the real `kloc.toml`, and a tree-bound test that **fails the day the lane seals** so the seal cannot
   be missed

### Tag chain — resolved as four rows, not one

```
ops-provisioning-secrets-and-accounts   ← STARTS NOW (only parallelizable node; the long pole is
                                           external accounts — tap repo, AUR, Docker Hub, cosign —
                                           not the secrets)
ops-darwin-rehearsal                    ← ALSO BEFORE THE MERGE (amended, see below)
        ↓
[operator decides recovery-lane is ready → merge to main]   ← not scheduled here
        ↓
ops-first-signed-tag   ← blocking predicate now written into the row, so it stops reading as forgotten
```

⚠ **Amended later the same session — the rehearsal came off the critical path.** As first written this row
said *"cannot run before the merge (`workflow_dispatch` is not on `main`)"*. That was true of the mechanism
D-15-4-F named, and the mechanism was the problem: **a dispatch trigger is exposed only for workflows on the
default branch, so the rehearsal could not run until after the very merge it exists to de-risk.**

`release.yml` gained `pull_request: branches: [main]` with a **label gate** on its `build` job
(`github.event_name != 'pull_request' || contains(…, 'release-rehearsal')`). The workflow was **already safe by
construction** — `sign-and-publish` carries `if: github.event_name == 'push'`, so neither a dispatch nor a PR can
publish; 15-4 built that guard for `workflow_dispatch` and it covers `pull_request` for free.

**D-15-4-F's substance is untouched** — macOS bills ~10×, stays out of the discipline matrix, and stays off
unlabelled PRs, so ordinary pull requests pay nothing. Only the *route* to the first build changed, and it now
happens one merge earlier at the same cost: one operator-triggered run.

**The gate is pinned, not assumed** — `xtask/tests/release_workflow_15_4.rs`, three falsifiers each proven red
before restore: (1) delete the `if` → every PR into `main` runs a three-target build including `macos-latest`;
(2) change the label name → a gate that matches any label is no gate; (3) delete the `pull_request` trigger →
the pre-merge path silently disappears. Without (1) in particular the regression is a **silent PR tax**, not a
broken build, which is why it needed a control rather than a comment.

**20-2 AC1/AC3 keep their no-secret proof** (the `.deb` builds in `release-dry-run` against the dev-signed `./dist`) and
must not be re-blocked on provisioning; only AC2's production-key swap is gated on it.

---

## 6. Epic 16 refinement — OLD→NEW edits, filed (rule 9)

`epic-16-one-daemon-one-door-j0-w1.md` was pinned `@9f920180`, **before all six Epic-15 commits**. Re-derived at HEAD `843d5365`; **45 anchor groups moved**; edits applied to the epic file in this commit.

### 6.1 What moved, and what held

| File | Drift | Verdict |
|---|---|---|
| `crates/maos-bin/src/main.rs` | 14120 → 14347 lines | ❌ **all 25 citations stale** |
| `crates/maos-cli/src/subcommands.rs` | +9 uniformly | ❌ all stale (18 `MAOS_ONE_SHOT` sites, each exactly +9) |
| `crates/maos-cli/src/cli.rs` | +2 | ❌ all stale |
| `.github/workflows/discipline.yml` | 3887 → 4008 | ❌ all stale (+35 … +121) |
| `maos-kernel-core`, `maos-control`, `maos-shell`, `maos-a2a-core`, `maos-domain`, `maos-journey-test`, `maos-spirit-hello` | — | ✅ **hold exactly** — Epic 15 did not touch them |

### 6.2 Mechanical re-pin (applied)

| Anchor | OLD @9f920180 | NEW @843d5365 |
|---|---|---|
| hand-parser arms `init\|audit\|traceback\|shell` | `main.rs:1679,1691,1729,1730` | **deleted** → `verbs::dispatch` (`verbs.rs:254`) at `:1335`/`:1677`, arms `:1702`/`:1710`/`:1746` |
| unknown → daemon fall-through | `main.rs:1738` | **deleted** → `Outcome::Unknown` `:1348`/`:1688` |
| `run_init` call | `:1699` | `:1708` |
| `run_audit_query` | `:1691-1721` | `VerbName::Audit :1710` → call `:1744` |
| `run_shell` call | `:3249` / `:3281-3283` | **`:3378`** |
| `MAOS_OPERATOR_BEARER_TOKEN` | `:2669-2687` | `:2706-2724` |
| `OperatorHttpServer::bind` | `:2792` | `:2829` |
| `FIXME(secrets)` | `:3079-3080` | `:3116` |
| butler `LoadedSpiritKind` | `:447` | `:455` |
| continuous serving loop | `:4020-4031` | `:4172-4183` |
| `LifecycleEvent::Uninstall` / cascade | `:5152` / `:5160` | `:5349` / `:5359` |
| synthetic halt seed | `:5568-5586` | `:5765-5783` |
| SCB task-assignment writers | `:6399,6452` | `:6596,6649` |
| `revocations-import` arm | `:6607-6643` | `:6804-6840` |
| unknown-`MAOS_ONE_SHOT` message | `:7993` | **replaced** — rendered from `verbs::MAOS_ONE_SHOT_MODES` (`verbs.rs:146`); diagnostic `:5162-5165` |
| `TcpA2ATransport` pin note | `:9805-9815` | `:10037` |
| `PostureArgs` / `PostureChoice` | `cli.rs:676-682` / `:685-691` | `:678-684` / `:687-693` |
| `HaltOp` / Resolve / `ResolutionKindChoice` | `:700-710` / `:711-727` / `:731-738` | `:702-712` / `:713-729` / `:733-740` |
| `PauseArgs` / `SpiritOp::Inspect` | `:769-772` / `:853-860` | `:771-774` / `:855-862` |
| `MAOS_ONE_SHOT` spawn sites | `subcommands.rs:557,1164,1461,1480,…,1842` | **all +9** — `:566,1173,1470,1489,…,1851` |
| inspect dispatch / hand-rolled GET | `subcommands.rs:1881-1885` / `:1938-1960` | `:1890-1894` / `:1947-1969` |
| `maosctl_smoke.sh` / `v01_evaluator_path.sh` | `discipline.yml:630` / `:665` | `:665` / `:700` |
| `onb_nfr2_timing.sh` | `:1069` | `:1104` |
| `nfr-rel-1` / `nfr-rel-2` jobs | `:1161-1172` / `:1175-1186` | `:1196-1207` / `:1210-1221` |
| `MAOS_ONE_SHOT` smoke jobs | `:1541-2267` | `:1576-2302` |
| aggregate `needs` (nfr-rel) | `:3571-3572` | `:3687-3688` |
| kernel pin | `kernel-core-baseline.toml:472` = **24472** | `:481` = **24474** |
| kernel-core kloc row | `kloc.toml:195` | `:199` |

### 6.3 Premise-level changes — ⚠ R4, these change what gets **built**

| # | Finding |
|---|---|
| **R4-1** | **16-4's stated red is already closed.** Epic 16 funds 16-4 on *"unknown verb boots the daemon."* Story 15-3 deleted that parser; `verbs::dispatch` returns `Outcome::Unknown` and its own doctrine reads *"an unknown verb exits instead of booting the daemon"* (`verbs.rs:249-253`). The remaining real red is narrower: **no product-removal verb exists at all.** |
| **R4-2** | **`uninstall` is occupied, by a different destructive semantic.** `verbs.rs:193` registers it as a `MAOS_ONE_SHOT` mode running the *Spirit erasure cascade* (`run_uninstall_cascade`, `main.rs:5349`/`:5359`, terminal receipt + explicit `process::exit`), with `maosctl uninstall <spirit>` (FR65) on top and `erasure_uninstall_13_5b.rs:69,767,782` pinning it. **Operator ruling 2026-09-12: the existing semantics do not move.** 16-4's verb becomes **`maos purge`**; a synonym is 16-4's to choose, `uninstall` is not, and disambiguation by argument arity is refused — a typo must not change what is erased. Exit-block line 7 amended. |
| **R4-3** | **16-5's kernel funding cites a reservation, not a grant.** "+5–25 from the D-F +250" — 15-2 §8 ruled D-F a *standing permission*, and `kloc.toml:199` holds `maos-kernel-core = 18935` at **zero headroom deliberately, per D13(a)**. 15-1 already exercised it for +2. 16-5 needs its own operator-authorized, formatted-measured grant taken **after** the code exists. |
| **R4-4** | **The re-pin target was wrong twice.** `kernel-core-baseline.toml:472` `24472 → N`: the value is **24474** and lives at **`:481`**; `:472` is 15-1's HISTORY comment. |
| **R4-5** | **The doorbell Epic 16 never names.** Any new file under `crates/maos-bin/src` (16-1's surface client, 16-4's purge module) must bump `SCANNED_SOURCE_FILES` in `cohort_daemon_smoke_13_5c.rs:869` in the same commit. It is **19** at HEAD (15-3 moved 17→18, 15-6 18→19). |

**Also corrected:** `MAOS_INFERENCE_MODE` is no longer red — 15-6 shipped it (`main.rs:3182`, `crates/maos-bin/src/inference_mode.rs`), so exit line 1's only remaining red is `control.json`. `--replay-llm` was **not** a no-op (`"--replay-llm" => live = false` in an order-sensitive loop), so its retirement is a behaviour removal. `check-exit-commands` is 15-3 **AC5**, not AC3.

### 6.4 Budget, re-measured at HEAD

`kloc-check` is now **PASSED (aggregate 158000)**, not the `EXIT=1` Epic 16 records. Measured src-only headroom:

| Crate | Headroom | Epic-16 ask | Verdict |
|---|---|---|---|
| `maos-bin` | **+2390** | +480–820 | ✅ |
| `maos-cli` | **+1577** | +400–700 | ✅ |
| `maos-control` | **+988** (803/1791) | +400–600 | ✅ — the file's *"697 headroom, 803/1500, `no change` row"* is stale **twice**: 15-2 re-based 1500→1791 and its §6 row 10 marked `no change` FALSE |
| `maos-shell` | **+193** (307/500) | ~+130–180 | ⚠ **the binding crate for the epic** |
| `maos-a2a-core` | **+130** | +20–40 | ⚠ tight |
| `maos-secrets` / `maos-journey-test` / `maos-domain` / `maos-providers` | +839 / +604 / +497 / +748 | small | ✅ |
| `maos-kernel-core` | **ZERO** | +5–25 (16-5) | ⚠ R4-3 |
| `xtask` | **ZERO** (43797/43797) | net 0 | ⚠ now a **hard wall** — `d11_xtask_ceiling_ratchet.rs` reds any raise citing no OPEN sprint story |

### 6.5 Rule-1 planning leg, re-run

The amended exit block was re-dry-parsed at HEAD: **`bash -n` CLEAN**, and `cargo run -p xtask -- check-exit-commands` **PASSES** with the renamed verb correctly attributed —

```
owed: `purge` (epic-16) → 16-4-maos-uninstall-and-keyring [backlog], claimed in epic-16 AC text
```

15-3's own gate validated this refinement's edit. That is the check working in the direction it was built for.

---

## 7. Lessons (three, each with a mechanical home)

1. **An action that is not queryable is not an action.** Epic 14's four commitments lived in prose; A3 slipped a whole wave unnoticed. *Home:* `xtask/tests/retro_action_items.rs` (E15-A1, shipped here).
2. **A test that asserts a value it set itself is a claim, not a control.** Three instruments failed this way in one epic; a fourth gap — a line-neutral kernel-core edit under a line-count pin — was caught only by a human reading. *Home:* E15-A5 (file set + content hash) and E15-A6 (named §A6 question).
3. **A story key outlives its scope.** `15-4-release-repair-and-first-signed-tag` cuts no tag — correctly, by ratified design — but the key still advertises one, and the epic-14 retro mis-attributed A2 to `15-2` instead of `15-3`. When scope moves, the key moves with it or carries the re-scope. *Home:* the same sweep `check-epic-close-coherence` already performs; raised at Epic-16 close.

---

## 8. Readiness for Epic 16

| Dimension | State |
|---|---|
| Testing & quality | ✅ workspace suite exit 0, 4231/0/119; all 7 xtask exit gates green |
| Deployment | ⚠ **no release tag exists** — `ops-first-signed-tag` backlog, blocked on `ops-provisioning-secrets-and-accounts` (E15-A3) |
| Branch | ⚠ `recovery-lane` is **7 commits ahead of `main`** (`main` @ `03ee0ad8`). Same drift shape as E11-A3 / E13-C4 — noted, not yet an action; the lane is mid-flight |
| Technical health | ✅ kernel pin stable at 24474 · ⚠ `maos-kernel-core` and `xtask` at zero headroom by design |
| Blockers into Epic 16 | E15-A5 (pin content hash) should land **before 16-5**; `maos-shell` headroom (+193) is the epic's binding constraint |
| Epic update required | ✅ **done in this session** — §6, applied to `epic-16-one-daemon-one-door-j0-w1.md` |

**Epic 16 is clear to open.** Its foundations (15-2 allowances, 15-5's ADR-062/ADR-064, 15-6's selector, 15-1's env registrations) are all landed and verified at HEAD. No blocking dependency is unmet.

---

*Facilitated by Amelia. Participants: Lunarpulse (Project Lead), Alice (PO), Charlie (Senior Dev), Dana (QA), Elena (Junior Dev), Winston (Architect), Murat (Test Architect), John (PM).*
