---
baseline_commit: e17a1056
---

# Story 12.4b: Team digest + J3 day-30 scene (journey closer)

Status: done

<!-- Epic 12 — J3 Marcus Team Nexus (v2.2). SPLIT from the original 12.4 (party-mode preflight 2026-07-12): 12.4a = no-surveillance MECHANISM; 12.4b = the J3 day-30 JOURNEY scene (this story, the Epic-12 journey closer). DEPENDS ON 12.4a (consumes cohort:digest-read + a captured mesh dataset) + 12.1/12.2/12.3. -->
<!-- Model/review: frontier-class dev allowlist. §A6 full-layer net NON-DEGRADABLE — this is the JOURNEY half of J3; a canned/scripted digest makes the journey a lie (11.2a/10.2/12.3 lesson). -->

## Story

As **Marcus at the day-30 10:00 standup**,
I want **a Digest Spirit that composes the consented per-member summaries it obtained via 12.4a's `cohort:digest-read` plus the 12.3 receipt-presence stream into a narrative team digest — "Overnight, N agents ran. F IAC frames exchanged. K agents halted, 0 acted invisibly. …" — rendered to my screen and reproducible end-to-end on the journey-acceptance harness**,
so that **I read the team's day in 60 seconds instead of reading logs, every sentence replays to raw evidence (I11), a halt-on-conflict surfaces as prose, and the last UNSERVED PRD journey (J3 §10.7.1) is demonstrable — the substrate's "amplified, not surveilled" promise made watchable.**

**This story is the DEMONSTRATION; 12.4a is the CONTROL.** The preflight (2026-07-12) proved two things that shape it: (1) **an 8-host cohort mesh cannot run in the hermetic single-`maos run` journey harness** — the loopback adapter self-delivers (`crates/maos-a2a/src/adapter.rs:82`), `maos run` topology is single-host with the `host` field inert and no A2A wiring, `build_cohort_a2a_daemon_runtime` has zero callers, and `crates/maos-journey-test` has zero cohort capability. So the J3 scene renders on the PTY harness from a dataset **captured from a real 12.4a N=8 mesh run** — not a live mesh, not a fabricated cassette. (2) The narrative digest is a §9.5 distillate written through the **existing** `DistillationPort::write_distillate` seam (`crates/maos-iac/src/adapter/distillate.rs`), so I11 traceability is real and the kernel stays frozen. This story does **NOT** re-build the consent-read primitive or the rupture sink (12.4a) or the halt-receipt mechanism (12.3). It does **NOT** touch the kernel or `maos-domain`.

## Acceptance Criteria

1. **Digest Spirit (`spirits/digest/`) composes consented reads + the receipt-presence stream into a narrative.** A NEW `rust-inproc` reference Spirit implementing `trait Spirit` (`crates/maos-spirit-abi/src/lifecycle.rs:233`, `on_idle` compute hook, `#[spirit]` proc-macro) with the §6.5 Observer read-only posture (`passive-observer`/`cautious`, no arbitration). It obtains each member's **typed consented summary payload** (`DigestSummary{frames,halts,conflicts}` defined in 12.4a — `request_id`-tagged) via **12.4a's `cohort:digest-read`** (gated — it can only read what its tuples permit; **idempotent per `request_id`** so a replayed summary does not double-count — dedup is real, `received_digest_summaries: HashMap<(String,String),DigestSummary>` `state.rs:91`, enforced in `observe_reply` `:884`), reads the 12.3 receipt-presence stream (`CohortManifestState::{present_receipt_count,is_receipt_present,absence_of}`, `crates/maos-cohort/src/state.rs:445,455,464`), and derives the narrative from the typed fields (not free-form prose). **The single narrative-derivation `pub fn` lives HERE, in `spirits/digest` (Option-B, R2)** — a derive-from-raw-inputs function called by the journey render (AC3) + the drift-gate (AC4/AC5), the only two callers; the mesh capture never calls it. New workspace crate → **bump `check-workspace-count` 54→55 in BOTH `Cargo.toml` members AND `4-kernel-design.md:117`** (hard-equality gate, `check_workspace_count.rs:57`; current count IS 54, `Cargo.toml:57`). Observer is a scalar-only watchdog (1103 lines, no cohort, no `DistillationPort` access) — a NEW crate, not an extension (F2).

2. **Narrative digest = an I11 distillate, DERIVED-and-RECONCILED not scripted, every clause traced to its source.** Render "Overnight, N agents ran. F IAC frames exchanged. K agents halted, 0 acted invisibly. J cross-agent consultations resolved… 1 architectural conflict surfaced for review" — the narrative is a **reconciliation across THREE real sources, with a clause→source map that IS the I11 chain** (R6): **frames** → sum over the consented per-member `DigestSummary.frames` (12.4a self-reports); **halts** → the 12.3 receipt-**presence** stream (not the self-report); **the architectural conflict** → a real halt-on-conflict receipt; **consultations/refusals** → the consent-rupture journal. NO clause is free-form, and NO clause needs a 4th `DigestSummary` field — the payload is fixed `{frames,halts,conflicts}` with `#[serde(deny_unknown_fields)]` (`crates/maos-cohort/src/digest.rs:77`); extra narrative facts come from presence + journal, never a new summary field (R6). Written as a distillate via the **existing** `DistillationPort::write_distillate` (impl `DistillateWriter` in the **`maos-iac`** crate `crates/maos-iac/src/adapter/distillate.rs:292` — re-exported via the kernel façade `kernel-core/src/iac.rs`, NOT kernel-core src; injected at the composition root like `main.rs:2029`; Butler persist precedent `spirits/butler/src/lib.rs:781`) carrying `source_log_ref` + `distillation_depth` (I11). **I11 citer-authorization is the compiler-enforced form of "amplified not surveilled" (R3):** `write_distillate` returns `CiterUnauthorized` if any `source_log_ref` frame's `spirit_pid ≠` the caller's (`distillate.rs:339-359`) — so the digest can cite **only its OWN journaled ingestion frames** (its `request_id`-tagged consented reads + presence reads), NEVER a peer's private raw. "Replay to raw in one hop" therefore means **one hop to the consented, self-owned evidence the digest is entitled to hold**; the members' private raw stays behind the consent boundary — which is the guarantee, not a gap. **ZERO kernel/domain delta** — the write path already ships (Story 4.4). The renderer **reuses the Butler persist pattern** (compose struct → `DigestPayload::Json` → `DistillationRequest::new` → `write_distillate`, `spirits/butler/src/lib.rs:287,675,781`) but authors a NEW cohort/narrative compose path — Butler's `morning_digest` reads a single local audit_db and is single-agent-shaped, so only the persist tail reuses.

3. **J3 day-30 scene reproducible E2E on the 8.15 PTY harness (Fact-J), including a halt-on-conflict beat.** A NEW `crates/maos-journey-test/tests/journey_j3.rs` follows the **Grade-A PTY pattern** (`journey_butler.rs:33` = jb1, the real PTY-render precedent): `JourneyWorld::builder()…build()` → `Pty::spawn("<maos> run <digest manifest> --once", &world)` → `pty.wait()` → assert `pty.screen().contains("…IAC frames exchanged…")` on the real `vt100` render (`crates/maos-journey-test/src/lib.rs:428,492,579`). **The digest→screen render is NET-NEW daemon-side wiring, NOT an existing path (R4):** `Spirit::on_idle` returns `()` and `Ctx` carries no output channel (`crates/maos-spirit-abi/src/lifecycle.rs:248`, `ctx.rs:58`); Butler's `on_idle` never renders a digest — the only things reaching the PTY today are a Debug-dump of `HookOutcome` and a hard-coded Butler **halt** line (`main.rs:3757`); `journey_butler.rs:153` is jb4, an **in-process** `morning_digest` compute, not a screen render. So the narrative-render plumbing is authored NEW at the `maos-bin` composition root (net-new code, still **zero kernel-core delta**), combining the jb1 PTY-render precedent with the jb4 compute. Single-host render (the harness has no mesh — F1), driven by the **captured dataset** (AC4). The scene surfaces a **halt-on-conflict** event through the digest (the "1 architectural conflict surfaced" beat, sourced from a real 12.3 halt receipt). Assert on all three harness surfaces (vt100 screen, `MockMcp::writes`, `maos_audit::query`).

4. **The bridge — captured-not-canned, ONE derivation over raw inputs (the anti-silent-green line; Option-B, R2).** There is NO reusable digest-derivation function in 12.4a (scout-confirmed: `DigestSummary` is a plain data struct fed from config/test values, `digest.rs:77`), so the "one shared derivation" is **authored NEW in `spirits/digest` as a `pub fn` over raw inputs** and is the SINGLE derivation in the tree — called by **exactly two callers: the journey render (AC3) and the drift-gate**, never a private copy (kills the 12.3 P7c decoupling risk). **The mesh produces raw inputs only, it never derives (R2).** The Fact-J dataset is **captured from a real 12.4a N=8 `build_mesh_n_with_digest` mesh run** (real `request_id`-tagged consented `DigestSummary` self-reports + real 12.3 receipt-presence + real consent-rupture journal), NOT a hand-authored cassette (10.2 / 12.3 blob trap) — but note honestly (R1): the per-member `DigestSummary` counters are **consented member self-reports** (config-seeded in-test), which is the *correct* anti-surveillance model — the transport-level facts (frames-exchanged, presence, ruptures, `request_id`-gated reads) are the real captured mesh activity. **The capture lives in `crates/maos-a2a-tcp/tests/` (the `t_12_4a_digest_read.rs` / `build_mesh_n_with_digest` template), NOT the journey crate** — `build_mesh_n` is `#[cfg(test)]` cross-crate-unreachable (the 11.3-F4 / 12.1-RR2 wall); the raw dataset ships as a fixture the journey test reads. **Drift-gate = derive-live-from-captured-raw-inputs (primary):** Fact-J and the gate both call `spirits/digest`'s one derivation on the captured **raw bytes** (not a pre-rendered string), so a derivation change re-renders and reds. **The drift-gate RUNS and BLOCKS in CI** (scout-confirmed): the derive-from-raw leg is a **plain hermetic test** (no live mesh needed → no `--ignored`), and the mesh-regen variant, if used, is `--ignored`-gated + gate-owned — both hermetic on `ubuntu-latest`, no `continue-on-error` (`check_cohort_mesh.rs:97`, `discipline.yml:2604`; contrast `check-scale-churn` which IS advisory). A hard-coded render line or a stale cassette reds.

5. **`check-cohort-mesh` journey legs — day-30 scene green + anti-canned.** New always-binding legs (whole-set hard-fail loop, `check_cohort_mesh.rs:427-429` — the loop drifted ~89 lines when 12.4a appended 5 legs; `CURRENT_PHASE="v1_5"` `:8`; no `AlwaysBinding`): (a) **day-30 scene** — `journey_j3` renders the narrative on the PTY at N=8-shaped captured data, halt-on-conflict beat present; (b) **anti-canned** — a **blinded change to ONE captured raw input byte** (a summary count / a presence entry), fed through `spirits/digest`'s one derivation, **moves the rendered vt100 line** through the gate's own comparator (the `assert_ne!` cold-start idiom, `journey_j1.rs:202`); a static digest reds. Because the derivation is derive-from-raw (AC4 Option-B), the anti-canned leg is a **plain hermetic test — no live mesh, no `--ignored`** — and still lands in `check-cohort-mesh` (blocking, `ubuntu-latest`). Each new leg = one `--exact` test; `run_leg` requires `"running 1 test"` + `"1 passed"` (anti-vacuous, `:16`). Update `tests/coverage-matrix.yaml` FR21 (`:258`) — cite J3 §10.7.1 journey + §9.5/I11 traceability. §A7 reflexes named.

6. **ZERO kernel-Δ @23082; digest is Spirit-side.** No `crates/maos-kernel-core/src/**` edits; `maos-domain` frozen (narrative content is Spirit-side, `functional-requirements.md:14`; distillation types already exist, `maos-domain/src/distillation.rs`). `check-kernel-baseline` (23082), `check-empty-kernel`, `check-service-boundary` (NO reconcile), `check-dependency-closure` (`spirits/digest` outside the kernel+domain closure), `check-workspace-count` (55, both anchors) green. **§A6 JOURNEY net NON-DEGRADABLE.** §A7 anti-canned reflex: a blinded mesh-captured input moves the digest through the comparator; a scripted render reds.

## Preflight resolution record (party-mode 2026-07-12, Lunarpulse-ratified split)

Same cast + 3 code-verification scouts as 12.4a. This story is the journey half of the split; 12.4a is the mechanism half. Decisions carried here:

- **F1 — the scene splits by capability (scout-forced).** The journey harness (PTY/vt100 single `maos run`) has NO mesh; the mesh (`build_mesh_n`) has NO PTY. So Fact-J (render, this story) runs hermetic single-host on the harness (Butler precedent, `journey_butler.rs:153`); Fact-M (real N=8 mesh activity + the consent mechanism) lives in 12.4a on `check-cohort-mesh`. Net-new `maos run` cohort plumbing rejected (Dana).
- **The bridge (Boundary/Vex/Wildcard/Grumbal) — captured-not-canned.** To stop Fact-J from rendering a canned string decoupled from the mesh, the dataset is CAPTURED from a real 12.4a mesh run (Wildcard: "a recording of the real thing, like the 12.3 real-constructor receipt"), the render shares the mesh's derivation function, and the capture is drift-gated (Grumbal: "a recorded cassette rots the day the derivation changes").
- **F2 — new `spirits/digest/` crate** (Observer is scalar-only, 1103 lines, no cohort — wrong base). Bump `check-workspace-count` 54→55 in TWO places (Murat — the RR9 hard-count-nobody-updates lineage).
- **F5 (scout-confirmed) — distillate write is Spirit-side, ZERO kernel delta.** `DistillateWriter` is in `maos-iac` (re-exported via the kernel façade, not kernel-core src); the digest calls the existing `DistillationPort::write_distillate` seam; Butler is the living proof (zero kernel KLOC). The I11 traceability is REQUIRED (Mary — it's the load-bearing part of "amplified not surveilled"), so route through the port, don't shortcut.
- **F6 — `AlwaysBinding` does not exist**; new legs use the whole-set hard-fail loop.

### Closing round (2026-07-12, 4th scout) — items touching 12.4b
- **Drift-gate RUNS in CI (Level's concern) — RESOLVED.** The N=8 mesh leg is `--ignored`-gated + gate-owned, hermetic real-TCP-on-loopback on `ubuntu-latest`, blocking (no `continue-on-error`; `check_cohort_mesh.rs:97`, `discipline.yml:2604`). So the capture-and-diff drift-gate is not laptop-only — it fires in CI (AC4).
- **Summary shape (Yui) — RESOLVED in 12.4a.** The `cohort:digest-read` reply carries a **typed** summary payload (member daily stats, `request_id`-tagged) serialized into the existing `TelemetryEvent` carrier — so 12.4b derives from typed fields, not mush (AC1). The `request_id` idempotency (12.4a) means a replayed summary can't double-count in the digest.

### Round 2 — pre-dev preflight (party-mode 2026-07-12, same room + 3 code-verification scouts, grounded FIRST)
**Premise correction:** 12.4a is **committed and `done`** at HEAD `ab2cc512` (the ready-for-dev snapshot was stale). Its primitives are real: `cohort:digest-read` gated (`cohort.rs:17`), `DigestSummary`+`request_id` dedup (`state.rs:91,884`), production rupture sink wired non-test (`main.rs:7514`, fail-closed `router.rs:277`). But four story claims were prose-ahead-of-code; all resolved to constraints, still **6 ACs, ZERO kernel-Δ @23082**:

- **R1 — "captured real activity / NEVER a literal" overshot.** The per-member `DigestSummary{frames,halts,conflicts}` is a **config-seeded consented self-report** (`digest.rs:77`, `main.rs:7549`), not counted-from-execution — which is *correct* for the anti-surveillance model (a digest that counts for you = surveillance). Reframed AC2/AC4: the digest **reconciles THREE sources** — consented self-reports (member-supplied) + real 12.3 presence + real rupture journal; "real mesh" is honest for the transport facts.
- **R2 (headline — the 8th "claim standing for a control") — the "ONE shared derivation" did NOT exist**, and `build_mesh_n` is `#[cfg(test)]` cross-crate-unreachable (11.3-F4 wall). **Landed Option-B (Lunarpulse-ratified): one derive-from-raw `pub fn` in `spirits/digest`, called by render + drift-gate ONLY; the mesh (capture in `a2a-tcp/tests`) supplies raw inputs and never derives.** Kills P7c + the dependency inversion + the prose-in-mesh-crate smell in one move (Winston/Wildcard).
- **R3 — I11 citer-authorization (`distillate.rs:339-359`) blocks cross-principal citation** → the digest can cite only its OWN frames. Reframed (Vex/Mary): this gate is the **compiler-enforced form of "amplified not surveilled"** — the members' raw stays behind the consent boundary by construction. AC2 promotes it as the strength, not a gap.
- **R4 — no digest→screen path exists** (`on_idle` returns `()`, `Ctx` has no output channel; Butler never renders a digest; `journey_butler.rs:153` is jb4 in-process compute). AC3 render is **net-new daemon wiring** at the `maos-bin` composition root (net-new, still zero kernel-core delta) — not "extend Butler."
- **R5 — four stale/wrong cites fixed:** loop `:427-429` (not `:338`); `main.rs:7412` "zero callers" wrong (def `:7482`, called `:7426` via `cohort-a2a-daemon` mode, not on the `maos run` path); `build_mesh_n` `:456`; presence methods `state.rs:445,455,464`; `#[spirit]` `:13` is a re-export.
- **R6 — `DigestSummary` is `{frames,halts,conflicts}` + `deny_unknown_fields`;** "consultations" has no field → comes from presence + journal. AC2 carries the clause→source map (which IS the I11 chain); no 4th field.
- **R7/R8 — no re-split** (Epic-12 closer); Dev Notes now state honestly this builds 3 net-new pieces (derivation lib + daemon render + capture/fixture/gate), not "compose done work." Workspace-count **54→55** confirmed (both anchors, hard-equality). Anti-canned leg = plain hermetic (derive-from-raw needs no live mesh), blocking in `check-cohort-mesh`.

**Through-line (Mary):** R2 was the 8th claim-standing-for-a-control across 12.1–12.4 — but the collision inverted R3: the citer-auth gate, which read as the 4th broken claim, is the load-bearing form of the story's own headline guarantee. *Readiness is a graph you draw and a build you run* — this round the build refused to cite what the graph wasn't consented to hold.

**Post-ratification verification — Option-B reachability (scout, same day): BUILDABLE, no cycle, no gate red.** Concrete edge map confirmed against the actual manifests: `maos-bin → spirits/digest` mirrors the 8 existing `maos-bin → spirits/*` path-deps (`crates/maos-bin/Cargo.toml:69-76`, Butler at `:75`); `maos-journey-test → spirits/digest` mirrors the existing `→ butler` regular dep (`crates/maos-journey-test/Cargo.toml:14`, the jb4 in-process-derivation precedent); the anti-inversion invariant HOLDS — `maos-a2a-tcp` has ZERO spirit deps and needs none (the capture reaches `DigestSummary`+rupture rows via its existing `maos-cohort`/`maos-iac` dev-deps `a2a-tcp/Cargo.toml:38,42`). `maos-cohort` is a shared inward leaf (`↛ maos-a2a-tcp`, `↛ spirits/*`) so no cycle forms; the cohort read/presence API (`state.rs:445,455,464,738,745`) is all `pub` inherent (Spirit-callable outside tests). `check-dependency-closure` scans only `{maos-kernel-core, maos-domain}` (`check_dependency_closure.rs:65`) — a new leaf spirit is definitionally outside the closure and stays GREEN (Butler is the closure-clean precedent; `maos-iac` edge is OPTIONAL since Butler reaches `write_distillate` via `maos-spirit-sdk`). **NEW carry-forward flag (non-blocking):** `AbsenceKind` (`halt_receipt.rs:100`) derives no serde — if the raw fixture carries absence markers, add `#[derive(Serialize,Deserialize)]` (1-line additive, closure-clean) or project it to a string capture-side. Workspace 54→55 confirmed (Option-B adds exactly one crate).

## Tasks / Subtasks

- [x] **Task 0 — Preflight verify + confirm 12.4a landed.** (AC: 6)
  - [x] Confirm 12.4a merged: `cohort:digest-read` exists + is non-reserved + gated; the production rupture sink is wired. 12.4b consumes both.
  - [x] Confirm the distillate write path is Spirit-side (F5): `DistillationPort::write_distillate` (`crates/maos-iac/src/adapter/distillate.rs:292`), injected at composition root; ZERO kernel/domain delta. If a kernel seam surfaces → STOP, FLAG-Winston.
  - [x] Record §A5 tier + pre-book §A6 NON-DEGRADABLE (journey). Record `dev_model_used`.
- [x] **Task 1 — Digest Spirit crate.** (AC: 1, 2)
  - [x] NEW `spirits/digest/{Cargo.toml, manifest.toml, src/lib.rs}` — `trait Spirit`/`#[spirit]`/`on_idle`, Observer read-only posture, output_shape = narrative-digest fields, `forms=["rust-inproc"]`, `trust_tier="local"`. **Bump `check-workspace-count` 54→55 in root `Cargo.toml` members AND `4-kernel-design.md:117`** (hard-equality, GOTCHA).
  - [x] It requests consented summaries via 12.4a `cohort:digest-read` (gated) + reads the 12.3 presence stream; it authors narrative, halts/arbitrates nothing.
- [x] **Task 2 — The ONE narrative-derivation fn + I11 distillate.** (AC: 2, 4)
  - [x] Author the single `pub fn` in `spirits/digest` that **derives the narrative from RAW inputs** (R2, Option-B) — reconcile the THREE sources with the clause→source map: frames→Σ`DigestSummary.frames`, halts→12.3 presence, conflict→halt-receipt, consultations→consent journal. No literals; no 4th `DigestSummary` field (`deny_unknown_fields`, R6). This fn is the SINGLE derivation — render (Task 3) + drift-gate (Task 5) call it; the mesh (Task 4) never does.
  - [x] Reuse **only** the Butler persist tail (`digest_to_distillation_request` shape `spirits/butler/src/lib.rs:781`) — author a NEW cohort compose path (Butler's `morning_digest` is single-agent/local-audit-db-shaped).
  - [x] Persist via `DistillationPort::write_distillate` with `source_log_ref`+`distillation_depth` (I11); inject the real `DistillateWriter` at the composition root (`main.rs:2029` pattern). **`source_log_ref` cites ONLY the Digest Spirit's OWN journaled ingestion frames** (else `CiterUnauthorized`, `distillate.rs:339-359`, R3) — the citer-auth gate IS the "amplified not surveilled" control. Do NOT skip the port (else traceability is a promise, not a control).
  - [x] Surface a halt-on-conflict beat from the real 12.3 receipt/absence data.
- [x] **Task 3 — J3 day-30 scene on the PTY harness (Fact-J) + NET-NEW render wiring.** (AC: 3, 4)
  - [x] **Author the digest→screen render at the `maos-bin` composition root** — net-new (no existing path: `on_idle` returns `()`, `Ctx` has no output channel; only Debug-`HookOutcome` + a hard-coded Butler halt line reach the PTY today, `main.rs:3757`, R4). Zero kernel-core delta.
  - [x] `crates/maos-journey-test/tests/journey_j3.rs` — Grade-A PTY (`journey_butler.rs:33` = jb1 pattern): `maos run <digest manifest> --once` in a `Pty`, assert the narrative on `pty.screen().contains(...)`, plus MCP-write + audit-row oracles. The render calls Task 2's one derivation on the captured raw inputs.
  - [x] Drive from the **captured** dataset (Task 4). Single-host render — no live mesh in the harness (F1).
- [x] **Task 4 — Capture-not-cann the dataset (raw inputs only, in `a2a-tcp/tests`).** (AC: 4)
  - [x] Produce the Fact-J dataset by CAPTURING a real N=8 `build_mesh_n_with_digest` run **inside `crates/maos-a2a-tcp/tests/`** (the `t_12_4a_digest_read.rs:201` template — `build_mesh_n` is `#[cfg(test)]` cross-crate-unreachable, the 11.3-F4 wall, R2). Capture the **raw inputs** (real `request_id`-tagged `DigestSummary` self-reports + real presence + real rupture journal), NOT a pre-rendered string. Ship the raw dataset as a fixture the journey test + gate read. The mesh **never derives** — derivation is Task 2's fn only. **GOTCHA:** `DigestSummary` is serde-ready but `AbsenceKind` (`halt_receipt.rs:100`) is NOT — if the fixture carries absence markers, add `#[derive(Serialize,Deserialize)]` to it (1-line additive, closure-clean) or project to a string capture-side.
- [x] **Task 5 — `check-cohort-mesh` journey legs + coverage.** (AC: 5)
  - [x] Append the day-30-scene + anti-canned legs (whole-set hard-fail loop `:427-429` — NOT `:338`; one `--exact` test each, `run_leg` requires `"1 passed"`; `CURRENT_PHASE` unchanged). Anti-canned = a blinded ONE-byte captured-raw-input change, re-derived through Task 2's fn, moves the vt100 line (`assert_ne!`, `journey_j1.rs:202`) — a **plain hermetic test, no `--ignored`** (derive-from-raw needs no live mesh). Update `coverage-matrix.yaml` FR21 (`:258`); `yaml.safe_load` before/after. Name §A7 reflexes.
- [x] **Task 6 — Zero-Δ verification.** (AC: 6)
  - [x] Green: `check-kernel-baseline` (23082), `check-empty-kernel`, `check-service-boundary` (NO reconcile), `check-dependency-closure`, `check-workspace-count` (55), `check-cohort-mesh`, `cargo test -p maos-journey-test --test journey_j3`, digest-crate tests. Regression: existing journeys (J0/Butler/Researcher/J1/J4) green; 12.1-12.4a legs green. `chmod 644` `100755` files.

### Review Findings

- [x] [Review][Patch] `derive_team_digest` aggregation granularity mismatch (latent correctness) [`spirits/digest/src/lib.rs:265-292`] — `agents_ran` dedups per-member (`:265-269`) but `frames_exchanged` (`:270-273`) and `acted_invisibly` (`:283-292`) aggregate per-`(member,request_id)`; a member appearing under two `request_id`s double-counts frames and double-subtracts the per-member receipt count, undercounting the safety signal `acted_invisibly` (e.g. member with two `halts=1` evidences + 1 receipt → `(1-1)+(1-1)=0` instead of `1`). `self_reported_halts` (`:274-277`) is the dead, unused per-member numerator (emits `unused_variables`; no `#[allow]`) — the half-written intended fix. LATENT: the current fixture has exactly 1 `request_id`/member (host_00..07, distinct IDs) so the render is correct (47 frames, 0 invisible); the `(member,request_id)` dedup key permits the multi-request shape and the generic `pub fn` neither rejects nor correctly aggregates it. Fix: reconcile per-member (group by member, sum halts, subtract the member's receipt count once; use/remove the dead var) OR reject multi-request-per-member as `ConflictingEvidence` (matches existing error style). (edge+blind) **FIXED (review 2026-07-12):** collapsed to a per-member `BTreeMap<&str, &SummaryEvidence>` view — identical-stats replays count once, differing stats across request_ids → `ConflictingEvidence`; removed the dead `self_reported_halts`; added 2 regression tests (`member_read_under_two_request_ids_is_counted_once`, `..._with_conflicting_stats_is_rejected`). 8/8 digest lib tests green.
- [x] [Review][Patch] Anti-canned gate leg derives in-process, never the daemon render [`crates/maos-journey-test/tests/journey_j3.rs:92-114`] — `j3_anti_canned_blinded_raw_input_moves_rendered_line` calls `derive_team_digest` directly via `derive_captured` (`:80-84`), never spawns `maos run`; so it proves the derivation is input-sensitive but CANNOT detect a daemon that hardcodes the narrative literal. The day-30 leg asserts fixed clauses on the real screen (`:46-51`) but those strings ("47 IAC frames exchanged.") are satisfied by a hardcoded render too. The §A7 "a static digest reds" reflex is enforced only against the derivation fn, not the daemon path. Fix: spawn the daemon with the blinded fixture and assert the rendered frame count moves 47→48 (mirrors the existing day-30 PTY leg). (edge+blind) **FIXED (review 2026-07-12):** added a `render_digest_screen` daemon spawn and a second `assert_ne!` so the blinded byte now moves the REAL `maos run` vt100 47→48; the in-process AC5 check (and its status as the 2nd `derive_team_digest` caller) is retained. 2/2 journey_j3 green.
- [x] [Review][Patch] Narrative hardcodes singular/plural; renders "1 consent requests were refused" [`spirits/digest/src/lib.rs:330-335`] — the `format!` always pluralizes "consent requests were refused" and always singularizes "architectural conflict surfaced". The current fixture has `consultations_refused=1` → the day-30 render prints the grammatically-broken "1 consent requests were refused"; `architectural_conflicts` of 0 or ≥2 is likewise wrong. No PTY assertion covers the refused clause (`journey_j3.rs:46-51` omits it), so it ships uncaught. Fix: count-aware singular/plural, or a count-agnostic phrasing. (edge+auditor) **FIXED (review 2026-07-12):** all narrative clauses are now count-aware (agent/agents, frame/frames, consultation/consultations, request was/requests were, conflict/conflicts); the refused clause renders "1 consent request was refused"; all asserted day-30 substrings preserved. 8/8 digest tests green.


## Dev Notes

### Substrate map (exact seams — scout-verified at e17a1056)
| Concern | Path |
|---|---|
| **Spirit ABI/SDK** | `trait Spirit` `crates/maos-spirit-abi/src/lifecycle.rs:233` (`on_idle` `:248` — **returns `()`; `Ctx` `ctx.rs:58` carries NO output channel** → digest→screen is net-new daemon wiring, R4); `#[spirit]` re-export `crates/maos-spirit-sdk/src/lib.rs:13` (proc-macro def `crates/maos-spirit-derive/src/lib.rs:133`) |
| **Digest Spirit base + renderer** | Observer read-only posture `spirits/observer/src/lib.rs:422` (scalar-only 1103 lines, NO cohort, NO `DistillationPort` — new crate not extension); Butler **persist tail reusable, compose path is single-agent-shaped** — `MorningDigest` `spirits/butler/src/lib.rs:287`, `morning_digest()` `:675` (reads ONE local audit_db → cites only own principal), `digest_to_distillation_request()` `:781` (depth hard-coded 1). The narrative-derivation `pub fn` is NEW in `spirits/digest` (R2) |
| **12.4a consent-gated read (dependency, COMMITTED @HEAD `ab2cc512`, done)** | `cohort:digest-read` non-reserved gated intent `crates/maos-a2a-core/src/cohort.rs:17`; NOT in `is_reserved_cohort_intent` → falls into `send_admits` `router.rs:646` + `cohort_consent_decision(Send)` `:663`. Reader-side stored: `state.digest_summary(peer,request_id)` `state.rs:738`. Production rupture sink WIRED non-test: `main.rs:7514`→`bind_with_cohort_wiring_and_digest`, fail-closed `router.rs:277` |
| **12.3 receipt-presence stream (dependency)** | `CohortManifestState::{present_receipt_count `state.rs:445`, is_receipt_present `:455`, absence_of `:464`}`; `HaltPresence`/`AbsenceKind` `halt_receipt.rs:100,110` |
| **I11 distillate write (Spirit-side, ZERO kernel delta — F5)** | `DistillationPort::write_distillate` `crates/maos-domain/src/ports/distillation.rs:11` (fn `:17`, carries `source_log_ref`+`distillation_depth`); impl `DistillateWriter` in the **`maos-iac`** crate `crates/maos-iac/src/adapter/distillate.rs:292` (re-exported via kernel façade `kernel-core/src/iac.rs:13`, NOT kernel-core src — a `distillation.rs:7,15` doc-comment still says "kernel-core", STALE, harmless); **CITER-AUTH GATE `:339-359` — `CiterUnauthorized` if source frame `spirit_pid ≠` caller → digest cites ONLY its own frames (R3)**; injected at `crates/maos-bin/src/main.rs:2029` |
| **Journey harness (8.15 — PTY, NO mesh)** | `crates/maos-journey-test/src/lib.rs` — `JourneyWorld::builder` `:55`, `Pty::spawn` `:428`, `Pty::screen`→vt100 `:492`, `Screen::contains` `:579`, `MockMcp::writes` `:311`, `ReplayProvider::cassette` `:346`; **PTY-render precedent = jb1 `tests/journey_butler.rs:33`** (renders a HALT line); **jb4 `:128` is an in-process `morning_digest` compute, NOT a PTY render** — digest-on-PTY combines the two (R4); anti-static `assert_ne!` `journey_j1.rs:202`. NO cohort/mesh capability (F1) |
| **Why no live mesh in the harness (F1)** | loopback self-delivers `crates/maos-a2a/src/adapter.rs:82`; `maos run` topology single-host, `host` field inert (`crates/maos-bin/src/main.rs:336`, never read); `build_cohort_a2a_daemon_runtime` (def `main.rs:7482`, called `:7426` via `cohort-a2a-daemon` smoke mode `:6401`) is **NOT on the `maos run` path** (R5 — the earlier "zero callers" cite was wrong); mesh only in `build_mesh_n` real-TCP tests (`crates/maos-a2a-tcp/tests/support/mod.rs:456`, `#[cfg(test)]` **cross-crate-unreachable** — capture must live in `a2a-tcp/tests`, R2) |
| **Workspace-count gotcha** | `check_workspace_count.rs:57` hard-equality; declared count anchor `4-kernel-design.md:117` (`<!-- workspace-count-authoritative -->`); current 54 → 55 |
| **Gate** | `xtask/src/check_cohort_mesh.rs` (whole-set hard-fail loop `:427-429` — NOT `:338`, drifted when 12.4a appended 5 legs; `run_leg` anti-vacuous `:16`; `CURRENT_PHASE="v1_5"` `:8`); job `discipline.yml:2593`, step `:2604`, `ubuntu-latest`, no `continue-on-error` (blocking); no `AlwaysBinding` (F6); `coverage-matrix.yaml` FR21 `:258` |
| **Kernel pin** | `xtask/kernel-core-baseline.toml` `src_lines=23082` `:222` (stays) |

### Architecture basis
- **J3 §10.7.1** (`prd/user-journeys.md:203-225`): the day-30 scene beats — climax digest at 10:00 ("3 agents halted, 0 acted invisibly", "1 architectural conflict surfaced"); the I11/I12 traceability paragraph (`:215`); "amplified, not surveilled" resolution (`:217`).
- **ADR-054 §8** (`:71-73`): the digest Spirit is the consumer of consented reads + receipts.
- **§9.5 distillation** (`architecture-maos-minimal-opus/9-memory-knowledge.md:33-61`): the digest is a distillate; I11 `source_log_ref`; Story-4.4 `DistillateWriter` seam (already ships).

### §A7 reflexes per leg
- **anti-canned reflex:** a blinded mesh-captured input moves the rendered vt100 line through the gate's comparator (`assert_ne!`); a scripted/static render reds.
- **captured-not-canned reflex:** the Fact-J dataset is captured from a real 12.4a mesh run + drift-gated; a hand-authored or stale cassette reds (10.2 trap).
- **traceability reflex:** the digest routes through `write_distillate` (I11); a digest that skips the port has no `source_log_ref` — its "replay to raw" is a promise, not a control.

### Prior-story traps
- **Canned journey (11.2a P1, 10.2, 12.3).** The whole story fails if the render is a literal. Derive from captured-real raw inputs; prove a blinded ONE-byte change moves the screen.
- **Decoupled derivation (12.3 P7c).** There is ONE derivation fn (`spirits/digest`), called by render + gate; the mesh supplies raw inputs and never derives — no private copy anywhere (R2).
- **Citer-authorization (R3, NEW).** `write_distillate` rejects cross-principal `source_log_ref` (`distillate.rs:339-359`). Cite ONLY the Digest Spirit's own journaled frames — a peer-frame citation reds with `CiterUnauthorized`. This gate IS the anti-surveillance control; don't fight it, use it.
- **No digest→screen path (R4, NEW).** `on_idle` returns `()`; the render is net-new daemon wiring at the `maos-bin` composition root. Don't assume "extend Butler" gives you a screen — Butler never rendered a digest to a terminal.
- **Cross-crate test wall (R2/11.3-F4).** `build_mesh_n` is `#[cfg(test)]`-local — the capture MUST live in `crates/maos-a2a-tcp/tests/`, not the journey crate.
- **Hard-count sentinel (12.1 RR9).** `check-workspace-count` is hard-equality in TWO files (count IS 54 now) — bump both 54→55 or CI reds.
- **Traceability skip.** Route through `write_distillate` — the I11 chain is the "amplified not surveilled" evidence, not optional.
- **Expect a second adversarial pass.** §A6 net non-degradable (journey).

### Project Structure Notes
- Edits: NEW `spirits/digest/{Cargo.toml,manifest.toml,src/lib.rs}` — **houses the ONE narrative-derivation `pub fn` (R2)** (bump workspace-count 54→55 in `Cargo.toml` + `4-kernel-design.md:117`); NEW render wiring in `crates/maos-bin/src/main.rs` at the composition root (digest→screen, net-new, R4); NEW capture in `crates/maos-a2a-tcp/tests/` (`build_mesh_n_with_digest` template — NOT the journey crate, R2) producing the raw-input fixture; NEW `crates/maos-journey-test/tests/journey_j3.rs` + `cassettes/j3/*` (the captured **raw** dataset) + `spirits/topologies/j3-*.toml` (or a digest manifest); `xtask/src/check_cohort_mesh.rs` (legs at the `:427-429` loop); `tests/coverage-matrix.yaml` (FR21 `:258`).
- **The digest is 3 net-new pieces, not "compose done work" (R7):** (1) the derivation fn, (2) the daemon render, (3) the capture+fixture+journey+drift-gate. Estimate accordingly.
- **No `crates/maos-kernel-core/src/**` edits. No `maos-domain/` edits.** Reuse the existing `write_distillate` seam (cite own frames only, R3). **No re-build of 12.4a's read primitive or rupture sink.**

### References
- [Source: epic-12-j3-team-nexus-v2-2.md — Story 12.4 row/AC sketch (34,69-75); J3 §10.7.1 (`prd/user-journeys.md:203-225`); ADR-054 §8 (`docs/adr/ADR-054-cohort-mesh.md:71-73`); §9.5 (`9-memory-knowledge.md:33-61`)]
- [Source: prior 12.4a (`12-4a-cohort-digest-read-consent-gate-no-surveillance.md`) — the `cohort:digest-read` primitive + production rupture sink this story consumes]
- [Source: prior 12.3 (`12-3-...`) — receipt-presence read surface, P7c decoupling discipline, capture-not-blob]
- [Source (scout-verified e17a1056): crates/maos-spirit-abi/src/lifecycle.rs:233, crates/maos-domain/src/ports/distillation.rs:11 + crates/maos-iac/src/adapter/distillate.rs:292, crates/maos-bin/src/main.rs:{336,2029,7412}, crates/maos-a2a/src/adapter.rs:82, crates/maos-journey-test/src/lib.rs:{55,428,492,579} + tests/journey_butler.rs:{33,153} + journey_j1.rs:202, spirits/{observer/src/lib.rs:422, butler/src/lib.rs:287,675,781}, xtask/src/{check_cohort_mesh.rs:338, check_workspace_count.rs:57}, 4-kernel-design.md:117, tests/coverage-matrix.yaml:258]
- [Source: PREFLIGHT party-mode 2026-07-12 — F1 harness-split, captured-not-canned bridge, F2/F5/F6; split into 12.4a/12.4b; memlog `_bmad-output/party-mode/memories/installed/.memlog.md`]

## Dev Agent Record
### Agent Model Used
Initial implementation: `openai-codex/gpt-5.6-sol`. Completion fix and validation: `openai-codex/gpt-5.6-terra`. Both are frontier-class development models; §A6 journey review remains non-degradable.
### Debug Log References
- `dev_model_used`: `openai-codex/gpt-5.6-sol`; frontier-class development model. §A6 full-layer journey review remains NON-DEGRADABLE.
- Task 0 preflight: verified `cohort:digest-read` is non-reserved and traverses both consent seams; verified the production `CohortRuptureLogSink` injection; verified `DistillateWriter` remains in `maos-iac` and is composition-root injected with zero kernel/domain changes.
- `cargo run -p xtask -- check-cohort-mesh` → PASS, 25/25 hard-fail legs.
- `cargo test --workspace` initially exposed a pre-existing FKCS line-baseline mismatch (`23081` versus the already-authorized kernel baseline `23082`); after reconciling the stale duplicate baseline and its oracle, `cargo test -p xtask --test fkcs_oracle` → 10 passed and `cargo test --workspace` → 3206 passed, 75 ignored.
- Task 1 RED→GREEN: `cargo test -p maos-digest --lib` first failed on missing `DigestSpirit`/`DigestPosture`, then passed 2/2 after implementing the passive-observer Spirit and manifest. `check-workspace-count` passed at 55/55; `cargo test --workspace` passed 3208 tests, 75 ignored.
- Task 2 RED→GREEN: derivation tests first failed on all absent raw-evidence/narrative types; `cargo test -p maos-digest --lib` now passes 5/5, including the real `DistillateWriter` accepting 14 Digest-owned source frames and rejecting a peer-private source with `CiterUnauthorized`. `cargo test --workspace` passed 3211 tests, 75 ignored.
- Task 3 RED→GREEN: `journey_j3` first failed because the captured fixture/render path was absent, then passed 1/1 after the real N=8 capture and daemon integration. `maos run digest --once` renders all five canonical clauses through vt100, issues no hidden MCP writes, journals 14 Digest-owned ingestion rows, and persists a queryable I11 distillate. `cargo test --workspace` passed 3212 tests, 76 ignored.
- Task 4 capture proof: `MAOS_CAPTURE_J3_FIXTURE=... cargo test -p maos-a2a-tcp --test t_12_4a_digest_read t_12_4b_capture_j3_raw_inputs -- --ignored --exact` → 1 passed on a real N=8 mTLS mesh. The fixture contains 8 self-reports totaling 47 frames, seven live correlated request IDs, three real shipped receipt-presence facts, and one target-journaled `ConsentRupture`; AST search confirms the mesh capture never calls the narrative derivation.
- Task 5 gate proof: `check-cohort-mesh` passed 27 independent hard-fail legs (+2 J3 exact tests); anti-vacuous output requires one attempted/one passed. The one-byte `frames: 5→6` blind moves vt100 from 47→48 frames. `yaml.safe_load` passed for reconstructed-before and after coverage matrices; FR21 still gates on `check-cohort-mesh`. AST search finds exactly two qualified derivation callers: daemon render + drift test. `cargo test --workspace` passed 3213 tests, 76 ignored.
- Task 6 RED→GREEN: the focused Digest suite exposed cross-member receipt cancellation (`self_reported_halts - total_receipts`); fixed reconciliation to subtract receipt presence only from the matching member's self-reported halt count. `cargo test -p maos-digest --lib` → 6 passed. All zero-Δ gates passed (`kernel=23082`, `workspace=55`, empty-kernel/service-boundary/dependency-closure clean); `check-cohort-mesh` passed 27/27; the full `maos-journey-test` package passed J0/Butler/Researcher/J1/J3/J4; `cargo fmt --all -- --check` passed; `cargo test --workspace` passed. Every listed file is mode `0644`.
### Completion Notes List
- Task 0 complete: 12.4a control surfaces and the existing Spirit-side I11 distillation seam are present and load-bearing; no kernel/domain seam surfaced.
- Task 1 complete: added the `rust-inproc` Digest Spirit, real `cohort:digest-read` request method, request-id-correlated summary collection, receipt-presence reconciliation, cautious/passive-observer posture, and no halt/arbitration authority.
- Task 2 complete: one derive-from-raw function reconciles typed member summaries, receipt presence, and consent-journal outcomes into narrative counts plus a five-clause source map. The Butler-shaped persist tail emits a depth-1 JSON `DistillationRequest`; the real writer enforces Digest-owned citation.
- Task 3 complete: added Digest class loading and a net-new one-shot render at the composition root. The daemon ingests captured raw evidence into Digest-owned TL frames, calls the single derivation, validates output shape, persists through the real writer, and prints the narrative.
- Task 4 complete: added the explicit ignored capture utility in the owning `a2a-tcp` test crate and committed only its raw JSON output. No pre-rendered narrative exists in the fixture; `AbsenceKind` serialization was unnecessary.
- Task 5 complete: added blocking day-30 PTY and plain-hermetic anti-canned legs, a gate-owned daemon build prerequisite, FR21/J3/§9.5/I11 coverage notes, and explicit §A7 journey/anti-canned reflexes.
- Task 6 complete: fixed per-member halt/receipt reconciliation so an unrelated member's receipt cannot conceal an unreceipted halt, then verified all story gates, journey regressions, formatting, file modes, and the full workspace suite.
### File List
- `_bmad-output/implementation-artifacts/12-4b-team-digest-j3-day-30-scene.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `xtask/fkcs-baseline.toml`
- `xtask/tests/fkcs_oracle.rs`
- `Cargo.lock`
- `Cargo.toml`
- `_bmad-output/planning-artifacts/architecture-maos-minimal-opus/4-kernel-design.md`
- `spirits/digest/Cargo.toml`
- `spirits/digest/manifest.toml`
- `spirits/digest/src/lib.rs`
- `crates/maos-bin/Cargo.toml`
- `crates/maos-bin/src/main.rs`
- `crates/maos-journey-test/Cargo.toml`
- `crates/maos-journey-test/tests/journey_j3.rs`
- `crates/maos-a2a-tcp/tests/t_12_4a_digest_read.rs`
- `crates/maos-journey-test/fixtures/j3/day-30-raw.json`
- `tests/coverage-matrix.yaml`
- `xtask/src/check_cohort_mesh.rs`

## Change Log
- 2026-07-12 — Story 12.4b created from the party-mode split of the original 12.4: the J3 day-30 JOURNEY scene (Digest Spirit + narrative render + PTY scene, consuming a dataset captured from a 12.4a mesh run). The Epic-12 journey closer. ZERO kernel-Δ @23082. Depends on 12.4a. Status: ready-for-dev.
- 2026-07-12 — **Round-2 pre-dev preflight (party-mode + 3 scouts), Lunarpulse-ratified.** 12.4a confirmed committed/`done` @`ab2cc512`. Eight resolutions folded (R1–R8), all constraints — still 6 ACs, ZERO kernel-Δ @23082: R1 self-report-not-surveillance reframing; **R2 Option-B one derive-from-raw fn in `spirits/digest`, mesh supplies raw inputs only, capture in `a2a-tcp/tests`**; R3 citer-auth = compiler-enforced "amplified not surveilled" (cite own frames only); R4 digest→screen render is net-new daemon wiring; R5 four cite fixes; R6 clause→source map / no 4th `DigestSummary` field; R7 honest "3 net-new pieces" Dev Notes; R8 anti-canned = plain hermetic leg. Status: ready-for-dev (unchanged).
- 2026-07-12 — Task 0 preflight completed. Confirmed 12.4a consent/rupture controls and the Spirit-side distillation seam; reconciled the stale FKCS line baseline to the already-authorized kernel baseline 23082; full workspace regression green.
- 2026-07-12 — Task 1 completed. Added the read-only Digest Spirit crate, consented-summary/receipt-presence collection, manifest, and workspace count 55; full workspace regression green.
- 2026-07-12 — Task 2 completed. Added the single raw-input narrative derivation, halt-on-conflict reconciliation, clause→source I11 map, and real-writer persistence/citer-authorization proof; full workspace regression green.
- 2026-07-12 — Task 3 completed. Added the real daemon Digest load/render/persist path and Grade-A PTY J3 scene with screen, no-hidden-MCP-write, ingestion-row, and distillate-row oracles; full workspace regression green.
- 2026-07-12 — Task 4 completed. Captured the raw J3 dataset from a real N=8 mTLS digest/read/receipt/rupture run in `maos-a2a-tcp`; committed no rendered prose and verified the capture has no derivation call.
- 2026-07-12 — Task 5 completed. Extended `check-cohort-mesh` to 27 hard-fail legs with the J3 PTY scene and one-byte anti-canned comparator; updated FR21 coverage and verified exactly two derivation callers.
- 2026-07-12 — Task 6 completed. Corrected cross-member receipt cancellation in Digest visibility reconciliation; all focused tests, 27 cohort-mesh legs, zero-kernel discipline gates, journey regressions, formatting, file modes, and the full workspace regression suite passed.
- 2026-07-12 — Story implementation complete and ready for §A6 review. All six acceptance criteria and Tasks 0–6 are complete; final `cargo test --workspace` regression passed with zero kernel delta at 23082.
