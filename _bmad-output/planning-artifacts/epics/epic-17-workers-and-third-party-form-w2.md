# Epic 17 — Workers and the Third-Party Form (W2, Fork C)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 17 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (20 of 20 applied) and the decisions D-B, D-C, D-F are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`. Evidence for every citation below: `../review-2026-09-04/preflight-r2/epic-17.md` §A (all `@9f920180`).

**Round 4 applied (2026-09-20, confidence rule 9 — the Epic-16 retrospective convened as a REFINEMENT session):** this file was re-preflighted against the tree after all seven Epic-16 stories landed, and its OLD→NEW edits are filed in **§R4** below, **before Epic 17's first story is created**. Re-derived at HEAD `e868ff28` by four scouts. **31 anchors moved, 27 hold, 21 findings recorded as R17-1…R17-21.** `crates/maos-bin/src/main.rs` grew 14347 → **14632** lines and `.github/workflows/discipline.yml` grew +36…+118 across five ranges, so every cite into either file is stale. The kernel pin moved **24474 → 24923** (+451, across FOUR re-pins), which is **323–383 lines past** the `~24540–24600` re-pin target line 27 still orders. ⚠ Premise-level, in order of cost: (R17-1) 17-4 AC1's refusal sites `main.rs:3702`/`:4091` **no longer exist** — 16-6 moved the refusal into `maos_bin::admission::gate_manifest`, so adding Observer is a **6-site** change against three *exhaustive* matches, not the 2-site change the AC describes; (R17-4) 17-1 AC4's pid requirement is **not satisfiable through a podman wrapper unmodified** — 16-3's `supervision.rs` opens a pidfd on the bridge's direct child, which under `spawn_t3` is `podman run`; (R17-13) 17-1 AC1 cites a trust-anchor precedent job that sets **neither** a lock file nor the env var; (R17-18) `examples/example-spirit-ts/manifest.toml:7` declares `forms = ["ts-inproc"]`, a value **no validator accepts**, so 17-3b's exit line 2 is red for two independent reasons; (R17-19/20/21) `maos-kernel-core`, `maos-bin` and `maos-domain` are at **exact-measured ZERO headroom** and this file carries **no Kloc paragraph at all**.

**Wave / duration:** 5–8 weeks (measured from 8-12/5-5a/11-1a/11-1b/11-4b/8-3 +30 %; §G of the scout note) · **Makes true:** FR5 true by form: Workers under a real T3 profile behind a host-side egress proxy (17-1); third-party Spirits isolated by construction in WASM — **only in `--features wasm-host` builds until Hold 2** (the feature is OFF by default as the export-control precondition, `crates/maos-bin/Cargo.toml:30`, `RELEASE-HOLDS.md:29-34`); first-party `rust-inproc` Spirits run unsandboxed in the daemon **by design** (`sprint-change-proposal-2026-09-04-round2.md:28`); FR29 over a maos-bin recall side-channel (not the WIT wire, which imports nothing at HEAD — `wit/spirit.wit:219-232`); FR33 via a TS→WASM toolchain (componentize-js, absent from the repo today — 17-3a spikes it)

**Closes:** S2 (sandbox never applied) for every non-first-party form; FR4/FR5 for Workers (FR47 only as narrowed by ADR-061: the proxy is the Worker's sole egress, it is not the kernel Inference Port — §A #6); FR29 (non-Rust); FR33 (TS); FR55 hooks; the Observer activity corpus (`prd/project-scoping-phased-development.md:145` — NFR-Obs-3 itself is already delivered, `crates/maos-kernel-core/src/telemetry/mod.rs:25-40,121`)

**Hermetic exit command (CI job `epic-17-exit`, no secrets; created by 17-1 AC1, extended by 17-3b AC1; red until green):**

```
maos run spirits/topologies/worker-sandbox.toml --once                      # 1 — T3 probing fixture: open("/host-home/.ssh") → ENOENT/EACCES, connect(127.0.0.1:9) → ECONNREFUSED; both journaled as kind-8 sandbox.block rows
maos audit query --spirit worker --format plain                             # 1b — the two kind-8 rows (network row from the egress proxy's refusal log; filesystem row from the fixture's structured stderr — never from echoed stdout)
cargo build -p maos-bin -p maos-wasm-host --features maos-bin/wasm-host --bin maos --bin maos-wasm-runner && target/debug/maos run examples/example-spirit-ts/manifest.toml --once   # 2 — manifest forms=["wasm-component"] (schema 5), artifact=dist/spirit.wasm built by componentize-js in the same job; the TS Spirit recalls its own frames over the recall side-channel; a filter naming another spirit_pid gets the typed refusal
```

**Exit-block provenance (dry-parsed at `9f920180`; `bash -n` clean):**
- Line 1 — `maos run <path> --once` exists (`crates/maos-bin/src/worker_spawn.rs:44-82` `parse_run_args`, accepts `<manifest> [--live] [--once]`; hand-parser fallthrough `main.rs:1738`); `[[topology.spirits]]` parsing exists (`crates/maos-bin/src/topology.rs:59-65`). `spirits/topologies/worker-sandbox.toml` **created by 17-1 AC1** (`ls spirits/topologies/` = 5 files, none a Worker sandbox); the probing fixture `spirits/worker/src/bin/worker-probe-fixture.rs` **created by 17-1 AC1** (`ls spirits/worker/src/bin/` = `worker-cli-fixture.rs` only, which is "no network, no filesystem" by design, `:18`). Job-step preconditions, all secret-free: podman (`t3-smoke-busybox` precedent, `.github/workflows/discipline.yml:1342-1345`), `MAOS_T3_RUNTIME` (`crates/maos-kernel-core/src/security/sandbox/t3/runtime_detect.rs:2`), a test-key `t3-image.lock` + `MAOS_T3_IMAGE_TRUST_ANCHOR_PUB_HEX` (`t3/image_lock.rs:7,70`; `sandbox_t3_image_verify` precedent `discipline.yml:1324-1336`); the base-image pull is network-for-dependencies, precedented, no secret. **red at HEAD:** the topology file is missing; even with it the Worker is spawned bare (`crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:461`), so no errno is produced and no kind-8 row exists.
- Line 1b — `maos audit query [--spirit <name>] [--format ndjson|plain] [--plain]` exists (`main.rs:1691-1712`). **red at HEAD:** zero kind-8 rows for `worker` — the only producer is `CapAuditEvent::SandboxBlock` (`crates/maos-kernel-core/src/capability/cap_audit/writer_task.rs:107-121`), never reached from a container-internal error.
- Line 2 — every cargo token exists: `[[bin]] maos` (`crates/maos-bin/Cargo.toml:10-12`), feature `wasm-host` (`:30`), `[[bin]] maos-wasm-runner` (`crates/maos-wasm-host/Cargo.toml:10-12`); the feature is compile-time (no `MAOS_FEATURES` env var anywhere — `grep -rn MAOS_FEATURES crates xtask .github docs` empty; the R2 proposal's `examples/ts-spirit.wasm` spelling at `round2.md:53` is superseded by this line). No CI job builds `maos-bin --features wasm-host` today (`grep -nE 'features.*wasm-host' .github/workflows/discipline.yml` = 1 hit, `:2901`, a `cargo tree` probe; `wasm-host-tests` `:2578-2591` builds only `maos-host`/`maos-wasm-host`) — the `epic-17-exit` job step is **created by 17-3b AC1**. `examples/example-spirit-ts/manifest.toml` exists but declares `forms = ["ts-inproc"]`, `manifest_schema_version = 2` (`:5,7`) — **rewritten by 17-3b AC1** to `forms = ["wasm-component"]` + `artifact = "dist/spirit.wasm"` at schema 5; `dist/spirit.wasm` is built by componentize-js in the same job step (`npm ci` precedent `discipline.yml:989-992`; componentize-js absent from the code and toolchain — `git grep -il componentize -- crates spirits sdks examples xtask templates '*.json'` = 0 @9f920180 (the 8 repo-wide hits are all `_bmad-output` planning prose)). **red at HEAD:** `ClassSection` rejects any form ∉ {`rust-inproc`, `subprocess`} (`crates/maos-manifest/src/manifest.rs:385-398`; test `:2589-2593` asserts it), `resolve_launch` is only ever called with `NativeSubprocess` (`worker_spawn.rs:482`), and the WIT world has no recall import (`wit/spirit.wit:219-232`).

**Operator-lane legs (never in the exit):**
- `ops-live-worker-under-17-1-profile` — live `claude`/`codex` Worker under the 17-1 egress-proxy profile: needs a vendor key **and** a signed T3 image containing the CLI (`t3-image.lock` + `MAOS_T3_IMAGE_TRUST_ANCHOR_PUB_HEX`, `t3/image_lock.rs:18,60-70`); acceptance = a runbook transcript of exit lines 1/1b with the real CLI in place of the fixture and the vendor key present only in the proxy's process

**Kernel-Δ (D-F, per-story figures granted by 15-2):** **17-1 is FLAG-Winston, +65–130** (`lifecycle/cli_wrapper/runtime.rs`, `security/sandbox/t3/spawn.rs`, `t3/argv.rs`, `lifecycle/cli_wrapper/admission.rs`, `security/manifest.rs`; re-pin `kernel-core-baseline.toml` 24472 → ~24540–24600 and draw the `maos-kernel-core` kloc grant in the same commit). **17-2 is FLAG-Winston, net ≤+10** (−30..+10: the root layout in `scheduler/resource_ceiling.rs` is deleted, `pids.max` +5, `pids_max` cap +3, `--pids-limit` +2; re-pin in its commit). 17-3a 0 (spike). **17-3b 0 by construction** (D-C side-channel: the kernel bridge `runtime.rs:562-608,687` is untouched; the WIT change is gated by `wit_corpus.rs` + `check-wasm-form-equiv`, not by `abi-diff`, which is `cargo-public-api` over `maos-spirit-abi` and cannot see WIT — `xtask/src/abi_diff.rs:1,8`). 17-4 0 (`dispatcher_arc()` `scheduler/scheduler_loop.rs:162`, `scbs()` `:148`, `TelemetryStreamAdapter::subscribe` `telemetry/mod.rs:121`, `fire_on_telemetry_event`/`fire_on_consolidate` `scheduler/hook_dispatch.rs:314,339` are all `pub`).

**Manifest schema (cross-cutting, one bump):** `MANIFEST_SCHEMA_VERSION` 4 → 5 (`crates/maos-spirit-abi/src/lib.rs:33`) carries all three new fields — `[cli_wrapper].egress` (17-1), `[resources].pids_max` (17-2), `forms = ["wasm-component"]` (17-3b); every section is `#[serde(deny_unknown_fields)]` (`manifest.rs:9,150,163,290`), so each field needs the validator, N-1 compat (a v4 manifest still loads), `manifest_field_coverage` rows (`discipline.yml:632-647`) and an `xtask/abi-ratifications.toml` entry (precedent `9.4b-AC6`, `13.5d`). Landed once, in 17-1 (first story to need it); 17-2/17-3b add fields to the v5 schema without a second bump. `maos-manifest` +150 row granted by 15-2.

**Kloc asks (⚠ R4 — ADDED 2026-09-20; this file had no budget paragraph, which is R17-21):** measured at HEAD `e868ff28` from `xtask/kloc.toml`. Σ Epic-17 ask = **+940…+1920**. ✅ absorbable today: `maos-egress` +300–600 against the booked 780 (`:625` — but see R17-19, the crate does not exist and the row contradicts the file's own "a reservation for planned growth remains forbidden" doctrine), `maos-wasm-host` +200–400 against 520 free (`:655`/`:653`), `maos-registry` +60–150 against 767 free (`:641`/`:639` — ledger `:640` books it to 17-4, this epic spends it in 17-3b AC4, R17-20), `maos-host` +10–30 against 100 free (`:631`), `maos-manifest` +70–140 against 260 free (`:390`/`:388`; 15-2's row is +160 to 4474, not the "+150" line 100 names, R17-17). ❌ **ZERO headroom — each needs its own operator-authorized, formatted-measured raise in the same commit as its driver**: `maos-kernel-core` 19213 (`:247`, exact measured raise, sole `ZERO_HEADROOM_CRATES` member `recovery_lane_ceiling_rule.rs:46`) against 17-1 +65–130 and 17-2 net ≤+10 — **both are FLAG-Winston and both must also move `RATIFIED_AT_EASING` (`:268`, now 19_213 after two Epic-16 bumps 19_053 → 19_121 → 19_213) in the same commit or `kernel_core_ceiling_has_not_moved_under_the_easing` (`:275`) reds**; `maos-bin` 23371 (`:477`, exact) against 17-3b +150–300 and 17-4 +80–150; `maos-domain` 9270 (`:395`, exact) against 17-2's `LifecycleEvent::ResourceCeilingDowngraded` +5–10. Aggregate measured at HEAD: **167789** (`cargo run -p xtask -- kloc-check`, 2026-09-20) — alarm 158608 (`:657`) is **already firing and must not be silenced**; hardfail 170884 (`:658`) leaves **+3095**, which the upper ask fits with no margin for a second overrun.

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree · **10 route by charter, never by bucket** (Epic-15 retrospective 2026-09-12, operator-ratified): a deferral names one of three destinations and never a default one — (a) **fix at origin** when the item is inside the story's own blast radius (the eased CEILING RULE removes the budget reason this was declined); (b) **the story that already touches that file**, which is the first question asked, not the last; (c) **a theme story (20-3a/20-3b, 21-4) only when the item matches that story's charter.** Rationale: Epic 14 closed early because nine of its eleven remaining rows were gates, registries, ledgers and ceilings; the Epic-15 retrospective measured this lane repeating it, with two items filed to `21-4` that had nothing to do with one instrument and the env registry · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `17-1-worker-egress-allowlist-and-scoped-credential` | Worker egress proxy, allowlist and scoped credential ◆ FLAG-Winston | S2 (Workers) · FR4/FR5 (FR47 as narrowed by ADR-061) · kernel-Δ +65–130 · `maos-egress` +300–600 · maos-manifest +40–80 |
| `17-2-worker-cgroups-applied` | Worker cgroups applied ◆ FLAG-Winston | FR6 · kernel-Δ net ≤+10 (−30..+10) · maos-domain +5–10 |
| `17-3a-wasm-recall-and-componentize-spike` | Spike: WASM recall side-channel, WIT versioning, componentize-js and the 17-1 proxy (≤3 days) | rule 5 · kernel-Δ 0 |
| `17-3b-wasm-third-party-form-with-log-recall` | WASM as the third-party form, with log.recall over a recall side-channel ◆ | FR29/FR33/FR5-by-form · kernel-Δ 0 · maos-wasm-host +200–400 · maos-bin +150–300 · maos-manifest +30–60 · maos-registry +60–150 · maos-host +10–30 |
| `17-4-hooks-observer-and-activity-corpus` | Hooks, Observer, and the activity corpus | FR55 · Observer corpus (`project-scoping…md:145`) · kernel-Δ 0 · maos-bin +80–150 |
| `17-5-model-pin-currency-gate` | Model-pin currency gate and the retired-pin sweep (E16-A4, three-times-slipped lane promoted to a named story) | E14-A3 / E15-A2 / E16-A4 · kernel-Δ **0** · xtask +260–480 (zero-headroom crate, measured grant in the same commit) · 7 data-line edits |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 17-1-worker-egress-allowlist-and-scoped-credential — Worker egress proxy, allowlist and scoped credential ◆ FLAG-Winston

*Closes · Δ:* S2 (Workers) · FR4/FR5 (FR47 as narrowed by ADR-061) · kernel-Δ +65–130 (`runtime.rs`, `t3/spawn.rs`, `t3/argv.rs`, `admission.rs`, `security/manifest.rs`), re-pin `kernel-core-baseline.toml` and a `maos-kernel-core` kloc grant in the same commit · out-of-kernel: egress proxy + credential mint +300–600 in a **new leaf crate `maos-egress`** (its own 15-2 row; keeps maos-bin's lane budget clear), maos-bin wiring within §E's +200–400 envelope, maos-manifest +40–80

*Design in force (D-B, ADR-061 authored by 15-5):* the Worker container keeps `--network=none` semantics toward the internet; its only reachable endpoint is a daemon-side egress proxy on host loopback that holds the vendor key, enforces the manifest allowlist, and journals every refusal. The child environment carries a per-spawn, kernel-minted proxy-scoped bearer — never the raw key. The production spawn is routed through a piped `spawn_t3`, so the Worker is actually under T3 for the first time (today it is admitted as T3 at `worker_spawn.rs:596-603` / `admission.rs:188` and spawned bare at `runtime.rs:461`).

- **AC1 (command).** Exit lines 1 and 1b run in the new `epic-17-exit` discipline job (podman installed as in `t3-smoke-busybox`, `discipline.yml:1342-1345`; test-key `t3-image.lock` + trust-anchor env as in `nfr-sec-1-t3-image-signature`, `:1324-1336`). This AC **creates** `spirits/topologies/worker-sandbox.toml` (`[[topology.spirits]]`, `topology.rs:59-65`) and the probing fixture `spirits/worker/src/bin/worker-probe-fixture.rs` (`[[bin]]` next to `worker-cli-fixture`, `spirits/worker/Cargo.toml:11-13`). Under T3 (`--user=65534:65534 --read-only --network=none`, only `/maos/spirit:ro` bind-mounted — `t3/argv.rs:72-78`) the fixture's `open("/host-home/.ssh")` fails with `ENOENT`/`EACCES` per the bind-mount set — and so does its `open` of the host's `<MAOS_HOME>/control.json`, the operator door's bearer file (⚠ Story 16-1 round-table F-D: a bare Worker runs as the operator uid, so mode `0600` does not protect the file from it) and its `connect(127.0.0.1:9)` fails with `ECONNREFUSED`. All three probes become kind-8 `sandbox.block` rows in the TL (two filesystem, one network), each by a **named mechanism**: the network row is written from the egress proxy's refusal log through `CapAuditEvent::SandboxBlock` (`writer_task.rs:107-121`, today the only kind-8 producer) — this is the control; the two filesystem rows are derived from the fixture's structured stderr lines by the same writer and are stated for what they are — a self-report from inside the sandbox that proves the mount set, not a kernel control. Neither row may come from the `CliSubprocessOutput` (kind 21) stdout echo (`runtime.rs:562-566`). A second topology entry with `egress = ["127.0.0.1:<echo>"]` reaches a hermetic echo listener through the proxy (positive control) and writes no kind-8 row. Which sandbox produces which errno is stated in the story: all three are T3 outcomes; under T2 the dial would die with `EPERM` (no `socket`/`connect` in the seccomp allow-list, `security/sandbox/linux.rs:201-330`) and `~/.ssh` would be `EACCES` under Landlock (`linux.rs:26-80`) — T2 is fixture-only for Workers (AC2).
- **AC2.** `[cli_wrapper]` gains `egress = ["host:port", …]` (`maos-manifest` `CliWrapperConfig`, `manifest.rs:3977-4001`; schema 4→5 with an `abi-ratifications.toml` entry and `manifest_field_coverage` rows — the epic-wide bump above). The Worker container keeps `--network=none` semantics toward the internet and reaches ONLY the daemon-side egress proxy (podman host-loopback mapping, ADR-061; `t3/argv.rs:68-81` is a podman/docker command line with no allowlist flag, and rootless podman cannot program nftables — the proxy is the only single mechanism satisfying AC2 and AC3). The proxy enforces the allowlist and journals every refusal as a kind-8 row. `admit_cli_wrapper_journaled` (`admission.rs:188`) refuses a manifest whose `egress` is non-empty when no proxy is configured (the existing `network_requested: bool` guard `ci_default_guard`, `runtime.rs:208-236`, gets its manifest source). T2 Workers cannot dial at all (seccomp allow-list has no `socket`/`connect`, `linux.rs:201-330`) and are stated as fixture-only.
- **AC3.** `spawn_and_bridge` (`runtime.rs:449-468`) calls `env_clear()` then `cmd.envs(nonsecret_env())` (`worker_cli.rs:134,655`; consumed at `worker_spawn.rs:567`) plus `ANTHROPIC_BASE_URL` / codex provider `base_url` pointed at the proxy and a per-spawn, kernel-minted proxy-scoped bearer (ADR-061); the raw vendor key (`ANTHROPIC_API_KEY` / `CODEX_API_KEY`, `worker_cli.rs:668,722`) is read by the proxy only — `credential_env_var()` (`worker_cli.rs:148,668,722`, zero call sites in `crates/*/src`) gets its first consumer there, and the FILENAME-control note at `worker_spawn.rs:541-545` is retired. The pinned test `env_clear_stays_absent_from_the_production_worker_spawn_path` (`crates/maos-cli/tests/credential_posture_2c.rs:299-330`; rationale `:6-12`) is **INVERTED in the same commit** with its rationale rewritten (the paid path now works through the proxy). ⚠ Story 16-1 round-table F-D (2026-09-13, operator-ratified): `env_clear()` also removes every operator-door variable from the Worker — the daemon's own are `MAOS_OPERATOR_BEARER_TOKEN` and `MAOS_OPERATOR_HTTP_BIND` (`crates/maos-bin/src/main.rs:2705-2707`; `MAOS_OPERATOR_HTTP_ENDPOINT` is maosctl's, and may also be exported in the operator's shell) — until this AC lands every Worker inherits the daemon's environment (`runtime.rs:461-469`) and can call the 16-1 door; the probe fixture asserts **no `MAOS_OPERATOR_`-prefixed key** is in its environ (a prefix match, so a variable added later is covered). The environ assertion is made from inside the container (the fixture prints its env keys) because the rootless subuid map (`--user=65534`, `argv.rs:76`) makes `/proc/<host_pid>/environ` (host pid via `inspect_container_host_pid`, `t3/spawn.rs:120-121`) unreadable to the daemon.
- **AC4.** `spawn_t3` (`t3/spawn.rs:54`) is the **single Worker path**: it gains piped stdin/stdout/stderr (`:115-117` today spawns an unpiped `Command`) and returns the child the bridge already expects; `spawn_and_bridge` routes `SandboxTier::T3` specs through it (today bare, `runtime.rs:461`; `spawn_t3`'s only caller is the busybox smoke step `main.rs:7027`). `spawn_sandboxed`'s T3 arm (`security/sandbox/mod.rs:140-148`, `SandboxUnavailable`) is unchanged. `select_worker_cli` runs on the manifest command before any podman wrapping (`worker_spawn.rs:490-495`; `argv_prefix_hash` binds `config.argv_prefix`, `:636-644`, and survives wrapping). ⚠ R7 (Story 16-3, HEAD `af96c907`): 16-3 supervises every Worker ABOVE the spawn primitive — the Worker SCB is bound before `spawn_and_bridge`, and a maos-bin observer opens a `pidfd` on the child `spawn_and_bridge` returns, `waitid(EXITED|NOWAIT)`s it and signals it only through that pidfd; the first Worker of a root is SCB `worker`, so this epic's `maos audit query --spirit worker` resolves — so the `spawn_t3` re-route must keep returning a child whose pid is the process whose death ends the Worker (if podman's client process is that child, the observer watches the client, not the container: rule it here); container teardown owns the crashed Worker's orphaned grandchildren (16-3 §11 row 2), the Worker SCB's journaled `sandbox_tier` (16-3 §11 row 9, no scheduler setter today) and the misleading `permitted == requested` tier-denial message (16-3 §11 row 10, `host_grant.rs:134-138`). The `t3-smoke-busybox` job drops its `continue-on-error` + `|| echo "T3 spawn tests skipped"` (`discipline.yml:1353-1354`).

### 17-2-worker-cgroups-applied — Worker cgroups applied ◆ FLAG-Winston

*Closes · Δ:* FR6 · kernel-Δ net ≤+10 (−30..+10: delete the root layout −40..−60, `pids.max` +5, `pids_max` cap +3, `--pids-limit` +2; re-pin in the commit) · maos-domain +5–10 (`LifecycleEvent::ResourceCeilingDowngraded`) · maos-manifest `[resources].pids_max` (field on the v5 schema landed by 17-1)

*Design in force (§H-3):* one delegation-compatible layout — `linux.rs`'s own-cgroup `<own>/maos.slice/spirit-<id>` (`security/sandbox/linux.rs:374`); the root layout is deleted; T3 Workers are capped by podman flags, documented as the third mechanism. **CI decision:** delegation is provisioned via `sudo` in the job (hosted runners have `sudo`, `t3-smoke-busybox` precedent) AND a second job proves the typed-downgrade branch — both, not one.

- **AC1 (command).** A discipline job `cgroup-ceiling-delegated` delegates a subtree (`sudo mkdir /sys/fs/cgroup/maos && sudo chown -R "$USER" /sys/fs/cgroup/maos && echo '+cpu +memory +pids' | sudo tee /sys/fs/cgroup/cgroup.subtree_control`) and moves the test process into it (`echo $$ > /sys/fs/cgroup/maos/cgroup.procs`, so `find_writable_cgroup_root` — which reads `/proc/self/cgroup`, `linux.rs:357-370` — resolves the delegated subtree); then for a T2 fixture child `cat /sys/fs/cgroup$(cut -d: -f3 /proc/<pid>/cgroup)/{cpu.max,memory.max,pids.max}` shows the manifest caps, and for a T3 Worker `podman inspect` shows `--cpus/--memory/--pids-limit` derived from `[resources]` incl. the new `pids_max` (`t3/argv.rs:79-81`). A second job `cgroup-ceiling-undelegated` (no delegation step) asserts a `LifecycleEvent::ResourceCeilingDowngraded { reason }` journal row (new variant on `crates/maos-domain/src/invariants/i10.rs:75`, maos-domain +5–10; `SpawnError::CgroupUnavailable` already exists, `sandbox/mod.rs:64`) instead of today's `eprintln!` + `noop()` (`scheduler/resource_ceiling.rs:112-120`).
- **AC2.** One layout: `linux.rs`'s own-cgroup `maos.slice/spirit-<id>` (`create_cgroup_dir` `:372-377`, literal `:374`) with `pids.max` written in `apply_cgroup_limits` (`:379-394`, today cpu/memory only). `resource_ceiling.rs`'s root layout (`/sys/fs/cgroup/maos/spirit-<pid>`, `:104`; non-root → `PermissionDenied` → `noop()`, `:105-120`) and `apply_resource_ceiling` (`:76`; doc `:73-74` "NOT called at v0.3-β"; zero callers outside `tests/cgroup_ceiling_smoke.rs`) are **DELETED, not wired**. ⚠ **R8 (Story 16-4 creation 2026-09-16, §11 row 9):** 16-4's `maos purge` **confirmed this independently** — it had provisionally listed `/sys/fs/cgroup/maos/spirit-*` as a fifth MAOS root to remove and **withdrew it**, because no HEAD build can create it and, even where one exists, a cgroup v2 directory is `rmdir`-only (kernel-generated files cannot be unlinked, so `remove_dir_all` gives `EPERM`), needs write on a root-owned parent, and returns `EBUSY` while populated. So **17-2 owns the only surviving cgroup layout and whether a leak is possible under it**; `maos purge` never removes a cgroup directory and mentions one only as pre-17-2 residue. `pids_max` is added to `ResourceCaps`/`ResolvedCaps` (`crates/maos-manifest/src/manifest.rs:105,121`) and to `[resources]` (field on the v5 schema), and T3's `--pids-limit` stops borrowing `fd_max` (`t3/argv.rs:47-57`, the documented DR-5.5a-9 placeholder). The three mechanisms at HEAD (root layout `resource_ceiling.rs:104`, own-cgroup `linux.rs:374`, podman flags `argv.rs:79-81`) become two, and the story documents the podman flags as the T3 mechanism.
- **AC3.** `cgroup_ceiling_smoke` (`crates/maos-kernel-core/tests/cgroup_ceiling_smoke.rs`) loses `#[ignore]` and the `MAOS_CGROUP_TEST` gate (`:5-6,15-20`), targets the own-cgroup layout with `pids_max` in `ResourceCaps` (`:21-25`), and is enrolled in the `cgroup-ceiling-delegated` job (it is in no job today — `grep -n cgroup .github/workflows/discipline.yml` empty).

### 17-3a-wasm-recall-and-componentize-spike — Spike: WASM recall side-channel, WIT versioning, componentize-js and the 17-1 proxy (≤3 days)

*Closes · Δ:* rule 5 · kernel-Δ 0

- **AC1 (command).** A written go/no-go in the story file answers six questions: (1) componentize-js produces a `maos:spirit@1.0.0` component (`wit/spirit.wit:18`; the package is `@1.0.0`, not `@1.0`) from a TS Spirit under `examples/example-spirit-ts` (today `tsc` + `vitest` only, `sdks/spirit-ts/package.json:12-19`); (2) a `log.recall` request op can be added to the WIT additively — the world imports nothing today (`spirit.wit:219-232`), new types need `wit_corpus.rs` rows (`crates/maos-wasm-host/tests/wit_corpus.rs:1-25`) and all 8 tracked guests rebuilt under `check-equiv-fixture-provenance` (`discipline.yml:2907-2910`), with `check-wasm-form-equiv` (advisory at `v1_5`, `xtask/gate-registry.toml:200-201`) green; (3) `frame_bridge` can carry intent/consent/lineage/scope (`crates/maos-wasm-host/src/frame_bridge.rs:9-21,389-431`); **(4)** can `maos-wasm-runner` relay a guest `log.recall` import to a maos-bin recall service over a unix socket with zero kernel lines (stub guest, measured — D-C)?; **(5)** does a `maos:spirit@1.0.0` guest instantiate against the `@1.1.0` world (`runner.rs:17,55` pin `@1.0`)? — if not, "additive" is false and the equiv fixtures are re-issued; **(6)** price 17-1's egress proxy: host-loopback reachability from rootless podman in CI, measured (D-B).
- **AC2.** Spike code lives under `spikes/` (`spikes/story-11-0-wasm-host/` precedent, tracker "HOLD-WINDOW no-merge spike"), never merged to `crates/`.

### 17-3b-wasm-third-party-form-with-log-recall — WASM as the third-party form, with log.recall over a recall side-channel ◆

*Closes · Δ:* FR29/FR33/FR5-by-form · kernel-Δ 0 (side-channel design, D-C) · maos-wasm-host +200–400 · maos-bin +150–300 (launch path + recall service) · maos-manifest +30–60 · maos-registry +60–150 · maos-host +10–30

*Design in force (D-C, §H-4):* `log.recall` rides a maos-bin side-channel service, NOT the kernel bridge: the runner's stdio is the ADR-032 bridge (`runtime.rs:562-608,687`) and stays byte-identical; the recall service listens on a unix socket whose path maos-bin hands to `maos-wasm-runner` (argv/env), and the runner relays the guest's WIT import over it. Kernel-Δ 0 holds by construction; 17-3a AC1 (4) proves it with a stub guest before this story leaves backlog.

- **AC1 (command).** Exit line 2 runs in `epic-17-exit` (this AC adds the `cargo build … --features maos-bin/wasm-host --bin maos --bin maos-wasm-runner` step and the componentize-js build of `examples/example-spirit-ts/dist/spirit.wasm`, `npm ci` precedent `discipline.yml:989-992`): the TS Spirit calls the `maos:spirit/recall@1.1.0` import; `maos-wasm-runner` relays it over the recall socket to a maos-bin service wrapping `LogRecallAdapter` (`crates/maos-iac/src/adapter/log_recall.rs:61`, emitter-side scope `:5`; in-proc today only via `KernelCtx.log_recall`, `scheduler/kernel_ctx.rs:27,85`) under the Spirit's `Scope::LogRecall` grant (`capability/cap_policy/mod.rs:161`); a filter naming another `spirit_pid` returns the typed refusal. `maos run` accepts a `[class]` manifest with `forms = ["wasm-component"]` and an `artifact` path (`examples/example-spirit-ts/manifest.toml` rewritten from `forms = ["ts-inproc"]`, schema 2 → 5), and `resolve_launch(WasmComponent)` (`maos-host/src/lib.rs:48`) gets its first production caller (`worker_spawn.rs:482` today hardcodes `NativeSubprocess`; comment `:472-477`). The runner is launched through `spawn_and_bridge` under the existing T2 path (ADR-031; exercised only by `crates/maos-wasm-host/tests/t2_sandbox_kill.rs:29-39` today) — the story states this, since S2 for the WASM form closes only if the runner is sandboxed.
- **AC2.** WIT `maos:spirit@1.1.0` adds the `recall` import (`log.recall`/`log.fetch`), additive: the WIT gate is `wit_corpus.rs` completeness + `check-wasm-form-equiv` (`discipline.yml:2842-2870`), since `abi-diff` is `cargo-public-api` over `maos-spirit-abi` and cannot see WIT (`xtask/src/abi_diff.rs:8`; `check-abi-ratification` asserts `abi-diff ⊆ ratified`, `check_abi_ratification.rs:3-5`); new WIT types get corpus rows; the 8 fixture guests (`tests/fixtures/wasm/*.wasm`) and the native twin are rebuilt and `check-equiv-fixture-provenance` re-pinned; a `xtask/abi-ratifications.toml` entry covers the Rust-side `LogRecall*` types only.
- **AC3.** `frame_bridge` carries intent, consent, lineage and scope (dropped/defaulted today, `frame_bridge.rs:9-21`; scope lossy via a Debug string, `:389-395,401-402,418-431`).
- **AC4.** `ClassSection.forms` accepts `wasm-component` (`manifest.rs:385-398`; test `class_section_rejects_unknown_form` `:2589-2593` inverted for that value, still refusing `wasm`). Registry admission by form and trust tier per ADR-060 (15-5): `admit_spirit` (`crates/maos-registry/src/admission.rs:179`, tier derivation `:4-40`, `extract_manifest_tier` `:467`) reads `forms` from `SignedPackage.manifest_toml` and refuses any package whose forms include `rust-inproc` with a typed `AdmissionError::FirstPartyFormFromRegistry` (enum `:34`; no forms check exists today — `grep rust-inproc crates/maos-registry/src` empty), while admitting `wasm-component` at ≥ `TrustTier::PublicUntrusted` (`manifest.rs:62`; `maos-registry/src/lib.rs:39`). `SignedPackage` is unchanged (`crates/maos-domain/src/ports/registry.rs:234-248`). ◆ — a trust boundary.
- **AC5.** `check-wasm-form-equiv`: the base and anti-canned legs run ≥1 test each and the equiv tier is unchanged (advisory at `v1_5`, `gate-registry.toml:200-201` — "stays green" is not binding). Both README sites stop saying the TS path is "not a kernel runtime": `sdks/spirit-ts/README.md:15` and `examples/example-spirit-ts/README.md:20-25`.

### 17-4-hooks-observer-and-activity-corpus — Hooks, Observer, and the activity corpus

*Closes · Δ:* FR55 · Observer corpus (`prd/project-scoping-phased-development.md:145`; NFR-Obs-3 is already delivered — `telemetry/mod.rs:25-40`, `subscribe` `:121`) · kernel-Δ 0 · maos-bin +80–150. Sequenced after Epic 16 releases the `main.rs` write lock.

- **AC1 (command).** `maos run spirits/observer/manifest.toml` (`[class] name = "observer"`, `forms = ["rust-inproc"]`, `spirits/observer/manifest.toml:30,35`) alongside Butler prints live scalar/telemetry events. Observer is added to `LoadedSpiritKind` (`crates/maos-bin/src/main.rs:433-442`, 8 variants today) with a constructor arm next to Mira/Nash in `classify_spirit` (`:445-457`; today `None` → error at `:3702`/`:4091`), and `maos-bin` adds the `maos-observer` dependency (absent from `crates/maos-bin/Cargo.toml`). **Observer implements `on_telemetry_event`** — today it implements only `on_idle` (`spirits/observer/src/lib.rs:453`) and `observe_scalar` fed by `with_pending_scalars` (`:508,553`); the SDK default is a no-op (`crates/maos-spirit-abi/src/lifecycle.rs:252`).
- **AC2.** A telemetry pump in `maos-bin` bridges `TelemetryStreamAdapter` (`telemetry/mod.rs:39`; built at `main.rs:1846`; `subscribe` `:121`) to `fire_on_telemetry_event` (first caller; `scheduler/hook_dispatch.rs:314-323`, zero callers outside the file) via `dispatcher_arc()`/`scbs()` (`scheduler_loop.rs:162,148`).
- **AC3.** `fire_on_consolidate` (`hook_dispatch.rs:339-349`, zero callers) gets an idle-cadence caller in `scheduler/idle_watchdog.rs` and Observer implements `on_consolidate`; `fire_on_schedule` already has one (`scheduler/schedule_watchdog.rs:257-260`).
- **AC4.** A 20-scenario activity-stream corpus is authored (none exists today — nearest: `spirits/observer/tests/fixtures/drift-scenarios.json` (4), `structural-scenarios.json` (5); `maos-corpus-gen/src` = ccac, lcas, red_team, secret_redaction) with missed-event ≤2 % and causal-ordering ≥99 % oracles. **Denominator (§H-7):** the corpus emits N `ScalarTapEvent`s through `TelemetryStreamAdapter` (lossy broadcast, `telemetry/mod.rs:32`) and the oracle compares Observer-received events against the emitter's own journaled count; ≤2 % missed and ≥99 % causal order are computed from those two lists. No TL row exists for `scalar.tap` emissions today, so the **emitter side** is the journaled one: the corpus harness writes its emission list (seq, tag, ts) to a file the oracle reads — stated in the story as the denominator.

### 17-5-model-pin-currency-gate — Model-pin currency gate and the retired-pin sweep

*Closes · Δ:* E14-A3 / E15-A2 / **E16-A4 (option A, operator-selected 2026-09-20)** · kernel-Δ **0** (nothing under `crates/maos-kernel-core/src`; `check-kernel-baseline` must still read `24923`) · `xtask` +260–480 — `kloc.toml:320` pins `xtask = 44101` and `xtask` carries **ZERO HEADROOM**, so the measured grant lands in the same commit as the lines, `cargo fmt --all` first, tokei 14.0.0 (the CI pin), exact measured via `cargo run -p xtask -- kloc-check --json` · 7 data-line edits outside any crate.

*Why it is a story and not a tracker row.* This lane slipped three times and each slip had a different cause: **E14-A3** slipped invisibly (Epic 14's retro actions were never written to `sprint-status.yaml` at all, so the W0 → "W1+W2" move had no reader); **E15-A2** resolved the ambiguity — "W1+W2" was never a schedule — and fixed the wave at W1, parallel to Epic 16, on the measured ground that the lane touches no `crates/maos-bin/src/main.rs` line and therefore cannot contend for the write lock; **E16-A4** then found it still at `backlog` after Epic 16 shipped seven stories. Epic 15's control (`xtask/tests/retro_action_items.rs`) enforces that an action row *exists*; no control enforces that it *moves*. A row that is nobody's exit condition is a row that never moves — so the lane becomes a story with a deadline derived from the tree.

*Deadline (derivable, not self-voiding):* **before `18-1` leaves `backlog`.** `18-1`'s AC4 already cites `spirits/butler/manifest.toml:21` and `spirits/researcher/manifest.toml:47` and declares that it depends on this lane; if 18-1 opens first it either re-does the sweep or inherits the rot.

*Scope is the archived lane verbatim (`tracker-notes-archive-2026-09-04.md:705-707`, OD1-ratified), re-measured at HEAD `e868ff28`; ACs are the input package's §W1 AC1.1–1.4 and §W2 AC2.1–2.3.*

- **AC1 (command).** `cargo run -p xtask -- check-model-currency` exits 0 at HEAD-after-this-story and names `file:line` for every retired or unknown model id when one is reintroduced. Neither `xtask/src/check_model_currency.rs` nor `xtask/model-currency.toml` exists today (measured: both absent; zero `model-currency`/`model_currency` hits in `xtask/src/main.rs` and `.github/workflows/discipline.yml`). The gate is **offline-safe** — it opens no socket; its whole world is the local data file. (AC1.1)
- **AC2.** `xtask/model-currency.toml` carries **per-provider `allowed` and `retired` lists, and every `retired` entry carries a `replacement` field**. The replacement is **display-only guidance** printed in the violation message (OD1 ratified) — the gate never rewrites a manifest. (AC1.2)
- **AC3.** Scanned surfaces, exactly: `spirits/*/manifest.toml`, `templates/*/manifest.toml`, `examples/*/manifest.toml`, `xtask/provider-pricing.toml`, and the `main.rs` provider-construction literals (measured at HEAD: `crates/maos-bin/src/main.rs:3483` `"claude-haiku-4-5-20251001"` for `AnthropicProvider::new`, `:3502` `"gpt-4o-mini"` for `OpenAiProvider::new` — **both already current**; they are scanned so they cannot rot silently, not because they are dirty). `.claude/worktrees/**` is agent scratch and is **not** a scanned surface. (AC1.3)
- **AC4.** Five-point CI enrollment mirroring the 12.6 / 14-2a pattern: (1) the subcommand declared in `xtask/src/main.rs`; (2) a job in `.github/workflows/discipline.yml` **and** the `needs:` wiring, or `check-ship-gate-completeness` hard-fails; (3) the flat `gates` list in `xtask/gate-registry.toml` (the `check-exit-commands` row sits at `:168`) plus a `[[ship_gate]]` disposition block (shape at `:314-316`); (4) `EXPECTED_GATES`; (5) a proven-red — reintroduce one retired id into a scanned manifest, observe RED naming that `file:line`, remove it, observe green. (AC1.4)
- **AC5.** The sweep: **exactly seven retired pins**, measured at HEAD `e868ff28`, each to the current set — `spirits/butler/manifest.toml:21` (`anthropic.claude-3-haiku-20240307`), `spirits/researcher/manifest.toml:47` (`anthropic.claude-3-5-sonnet-20241022`), `templates/spirit-rust/manifest.toml:12`, `templates/spirit-ts/manifest.toml:12`, `examples/example-spirit/manifest.toml:12`, `examples/example-spirit-ts/manifest.toml:12` (all four `anthropic.claude-3-haiku-20240307`), and `xtask/provider-pricing.toml:18` (`model = "claude-3-5-sonnet"`, an unversioned id in the synthetic price book). (AC2.1)
- **AC6.** `spirits/hello-spirit/manifest.toml:12` is **already current** (`anthropic.claude-haiku-4-5-20251001`) — do **NOT** re-edit it. An edit there is a review finding, not a sweep. (AC2.2)
- **AC7.** Hermetic test fixtures and fuzz seeds are **inert and NOT swept** — accepted debt, recorded as such. Measured instance: `crates/maos-providers/src/anthropic.rs:284,285,304,324,342,356,382` all sit inside `#[cfg(test)]` (`request_body_has_expected_shape` and siblings) and pin `claude-3-haiku-20240307` as a fixture string, never as a live default. Same for `crates/maos-audit/src/lib.rs:2744` and `crates/maos-manifest/src/manifest.rs:2624`. These paths are **outside AC3's scanned set by construction**, so the gate cannot red on them and the debt is disclosed rather than laundered. (AC2.3)

⚠ **Ordering note.** 17-5 is independent of 17-1/17-2/17-3a/17-3b/17-4 — it touches `xtask` + data files only, no `main.rs` line, no kernel line — so it does **not** queue behind the Epic-16 `main.rs` write-lock release and may run alongside any of them. Its only real contention is the `xtask` zero-headroom ceiling, which it shares with no other Epic-17 story (none of 17-1…17-4 books `xtask` lines).

## §R4 — OLD→NEW re-derivation at HEAD `e868ff28` (Epic-16 retrospective, 2026-09-20)

Filed under confidence rule 9. Every row measured from the tree; nothing inferred. `9f920180` was this file's last full derivation (round 3), and `af96c907` its only partial (17-1 AC4's R7 note).

### R4.1 Mechanical re-pins (apply before the first story is created)

| Anchor | OLD cite | NEW cite @ `e868ff28` |
|---|---|---|
| `maos run` parser fallthrough (prov. line 1) | `main.rs:1738` | `main.rs:1737-1746` (`verbs::dispatch` at `:1639`; `VerbName::Run \| Traceback => {}` at `:1719` falls through **deliberately**) |
| `maos audit query` arg surface (prov. line 1b) | `main.rs:1691-1712` | `main.rs:1672-1706` (`--spirit` `:1683-1688`, `--format` `:1689-1694`, dispatch `:1706`) |
| sole `spawn_t3` caller (17-1 AC4) | `main.rs:7027` | `main.rs:6982` |
| `LoadedSpiritKind` (17-4 AC1) | `main.rs:433-442` | `main.rs:444-453` |
| `classify_spirit` (17-4 AC1) | `main.rs:445-457` | `main.rs:456-468` |
| `TelemetryStreamAdapter` built (17-4 AC2) | `main.rs:1846` | `main.rs:1915` (consumed `:1971`) |
| `dispatcher_arc()` / `scbs()` (17-4 AC2) | `scheduler_loop.rs:162` / `:148` | `:175` / `:152` (moved by 16-6) |
| `parse_run_args` | `worker_spawn.rs:44-82` | `worker_spawn.rs:44-77` |
| `spawn_and_bridge` (17-1 AC3/AC4) | `runtime.rs:449-468` | `runtime.rs:453`, body `:453-550` |
| bare Worker spawn (17-1 AC4) | `runtime.rs:461` | `runtime.rs:475-477` (`cmd.spawn()` `:476`) |
| ADR-032 stdio bridge (17-3b design) | `runtime.rs:562-608,687` | `pump_to_journal` `:571`; `write_stdin_line` `:691` |
| podman argv (17-1 AC2) | `t3/argv.rs:68-81` | `build_runtime_argv` `:27`, argv `:66-86` (`--network=none` `:72`, `--read-only` `:73`, `--user` `:75`, `:ro` bind `:78`; `--cpus/--memory/--pids-limit` `:79-81` HOLD) |
| `select_worker_cli` | `worker_spawn.rs:490-495` | `worker_spawn.rs:546` |
| FILENAME-control note (17-1 AC3) | `worker_spawn.rs:541-545` | `worker_spawn.rs:593-595` |
| `nonsecret_env` consumption | `worker_spawn.rs:567` | `worker_spawn.rs:617` |
| T3 admission of the Worker | `worker_spawn.rs:596-603` | `worker_spawn.rs:628-630` (T3 assert `:635-641`) |
| `argv_prefix_hash` bind | `worker_spawn.rs:636-644` | `worker_spawn.rs:686` (consumed `:711`, `:788`) |
| pinned `env_clear` test (17-1 AC3) | `credential_posture_2c.rs:299-330`, rationale `:6-12` | `:295-340+`, rationale `:6-16` |
| `MANIFEST_SCHEMA_VERSION` (schema para.) | `maos-spirit-abi/src/lib.rs:33` | const at `:114` (doctest literal `:112`); `:33` is only the doc-table row. **Value is still 4** |
| podman install precedent | `discipline.yml:1342-1345` | job `t3-smoke-busybox` `:1434`, install step `:1439-1442` |
| `nfr-sec-1-t3-image-signature` | `discipline.yml:1324-1336` | `:1420-1433` (see R17-13) |
| `manifest_field_coverage` | `discipline.yml:632-647` | job `manifest-field-coverage` `:668-683` (see R17-14) |
| `check-wasm-form-equiv` job | `discipline.yml:2842-2870` | `:2953-3021` |
| `check-equiv-fixture-provenance` | `discipline.yml:2907-2910` | `:3017-3021` |
| `wasm-host-tests` | `discipline.yml:2578-2591` | `:2696-2709` |
| `npm ci` precedent | `discipline.yml:989-992` | `:1025-1028` (job `example-spirit-ts-tests`) |
| `cargo tree` feature probe | `discipline.yml:2901` | `:3012` (see R17-16) |
| `check-wasm-form-equiv` disposition | `gate-registry.toml:200-201` | **`:212-213`** (see R17-15) |
| `Scope::LogRecall` | `cap_policy/mod.rs:161` | `:185` (`LogRecall \| LogFetch`; intent string `:418`) — moved by 16-6 |
| feature `wasm-host` | `maos-bin/Cargo.toml:30` | `:29` (`:30` is now `spirit_test`) |
| world `spirit` imports nothing | `spirit.wit:219-232` | `:219-230` |
| Hold-2 wasm precondition | `RELEASE-HOLDS.md:29-34` | row `:28`, mitigation `:32-38` |
| `apply_resource_ceiling` doc | `resource_ceiling.rs:73-74` | `:74-75` |
| kernel pin | `kernel-core-baseline.toml` 24472, re-pin to `~24540–24600` | **`:505` `src_lines = 24923`**; re-pin target becomes **~24988–25053**; HISTORY `:507`, `:564`; `PINNED_ENTRIES` 98 `:531` |

**Holds exactly (no edit):** `t3/spawn.rs:54` and its unpiped `Command` `:115-117`; `t3/argv.rs:79-81`; `cli_wrapper/admission.rs:188`; `sandbox/mod.rs:140-148`; `t3/image_lock.rs:18,60-70`; `worker_cli.rs:134,148,655,668,722` (and `credential_env_var()` still has **zero** call sites in `crates/**`, tests included); `manifest.rs:3977` `CliWrapperConfig`, `:9,150,163,290`, `:62`, `:385-398`, `:2589-2593`; `maos-registry/src/admission.rs:34,179,467` and `lib.rs:39` (and `grep rust-inproc crates/maos-registry/src` is still empty); `log_recall.rs:5,61`; `kernel_ctx.rs:27,85`; `frame_bridge.rs:9-21,389-395,401-402,418-431`; `abi_diff.rs:8`; `check_abi_ratification.rs:3-5`; `wit/spirit.wit:18`; `wit_corpus.rs:1-25`; 8 fixture guests under `tests/fixtures/wasm/`; `sdks/spirit-ts/package.json:12-19` and both README caveats; `telemetry/mod.rs:25-40,32,39,121`; `hook_dispatch.rs:314-323` and `:339-349` (**both still zero callers repo-wide**); `schedule_watchdog.rs:257-260`; `observer/src/lib.rs:453,508,553`; `observer/manifest.toml:30,35`; drift/structural scenario counts 4 and 5; `spirits/topologies/` = 5, none a Worker sandbox; `spirits/worker/src/bin/` = `worker-cli-fixture.rs` only; **every 17-2 cgroup anchor** (`linux.rs:357-370,372-377,374,379-394` — still cpu/memory only, no `pids.max`; `resource_ceiling.rs:76,104,105-120`; `cgroup_ceiling_smoke.rs:15,17-20,21-25`) and `grep -n cgroup .github/workflows/discipline.yml` = **0 hits**. `git grep -il componentize` over `crates spirits sdks examples xtask templates` = **0**.

### R4.2 Premise-level findings — ⚠ these change what gets **built**

| # | Finding |
|---|---|
| **R17-1** | **17-4 AC1's refusal sites are GONE; the Observer arm is a 6-site change.** The AC says Observer joins `classify_spirit` where "today `None` → error at `:3702`/`:4091`". Neither site exists. 16-6 moved the refusal into `maos_bin::admission::gate_manifest` (`admission.rs:409`), which takes `classify_spirit` as a `&dyn Fn(&str)->bool` predicate and returns `AdmissionRefusal::UnknownClass` (`:221`, Display `:288-293`); all three call sites then `.expect("gate_manifest refused every class classify_spirit does not know")` (`main.rs:4446-4456` topology, `:4877-4882` standalone, `:8603-8607` door). Adding `Observer` requires: the enum `:444-453`, `classify_spirit` `:456-468`, `admission::KNOWN_CLASS_NAMES: [&str; 8]` `:317` → 9 (**an array-length change**), and three **exhaustive** `match kind` blocks — `build_spirit_obj` `:814-830`, topology `:4472-4545`, standalone `:4926-5477` — which `main.rs:807-811` states is a deliberate TYPE guarantee. Re-cite AC1 to all six. |
| **R17-2** | **Three classify/parity tests are 17-4 AC1's own red and are cited nowhere.** `story_16_6_known_class_names_match_classify_spirit` (`main.rs:13323`, iterates `KNOWN_CLASS_NAMES` both ways), `story_9_6_single_driver_guard_classify_round_trips` (`:13493-13520`, hard-coded `all_names`), and the arm-by-arm asserts `:13293-13306`. |
| **R17-3** | **17-4 AC1 does not say which launch arm Observer enters.** The topology arm refuses non-deterministic classes at `main.rs:4540` (`Butler \| Researcher => Err("topology manifests currently accept deterministic class Spirits only")`). "Alongside Butler" is ambiguous: a new variant unhandled there is a **compile error**, and placed in that arm it is a **runtime refusal**. (Verified benign: `observer/manifest.toml:33` declares `manifest_schema_version = 2`, admissible under `MIN_SUPPORTED_MANIFEST_SCHEMA_VERSION = 1` (`maos-spirit-abi/src/lib.rs:135`) and unaffected by the 4→5 bump.) |
| **R17-4** | **17-1 AC4's pid requirement is NOT satisfiable through a podman wrapper unmodified, and `supervision.rs` is missing from its Δ line.** Measured order in `worker_spawn.rs`: `bind` `:697` (SCB at a real pid) → `spawn_and_bridge` `:803` → guard-first `LiveWorker{binding,bridge}` `:809` → `watch(core, live.bridge.child_pid())` `:826` → `finish` `:858`. `watch` opens a pidfd on the bridge's **direct child** (`supervision.rs:1363`, `PidfdExitObservation::watch` `:2140`). Under `spawn_t3` that child is `podman run`; the real pid is re-derived out-of-band by `podman inspect --format {{.State.Pid}}` (`t3/spawn.rs:163-169`) and lives in another pid namespace, which `pidfd_open` cannot address from the host without a `--cgroups`/`--pidfile` handshake. **AC4 must rule explicitly on which pid `watch` receives under T3**, and `crates/maos-bin/src/supervision.rs` must join 17-1's Δ line — it is in neither the kernel-Δ list nor the "proxy wiring" envelope today. `supervision.rs:1689-1692` names this itself. |
| **R17-5** | **17-1 AC4 aims "gains piped stdio" at the wrong function, and omits a type adaptation.** `spawn_and_bridge` **already** pipes all three (`runtime.rs:467-469`); it is `spawn_t3` that is unpiped (`:115-117`). And `spawn_t3` returns `SandboxedContainerChild`, not `std::process::Child`, while `SpawnedBridge` (`runtime.rs:297-301`) stores `Option<Child>` + `Option<ChildStdin>` — so "returns the child the bridge already expects" is false at HEAD. |
| **R17-6** | **17-1 AC3 must delete or invert FOUR legs, not one.** `env_clear_stays_absent_from_the_production_worker_spawn_path` (`credential_posture_2c.rs:295-340`) now asserts: no `env_clear` in `runtime.rs`, no `env_clear` in `worker_cli.rs`, `nonsecret_env` still present, **and** the literal `"inherited host-side"` still present in `worker_cli.rs`. Leg 4 pins a doc sentence 17-1 necessarily falsifies. |
| **R17-7** | **17-1 AC3's probe must widen from the `MAOS_OPERATOR_` prefix to the door's FILE route.** Measured: exactly three `MAOS_OPERATOR_*` names exist (`main.rs:3160,3163`, registered `env_contract.rs:480,485`; `maos-cli/src/door_client.rs:32`), and 16-6's `admission.rs` adds **no** new env surface (it reads only `MAOS_REQUIRE_MODEL_PROVENANCE` `:130`). But `operator_door.rs:63-66` resolves the door from **`MAOS_HOME`** → `control.json` at `0600` (`:37`, `check_custody` `:441-457`), readable by a Worker running as the operator uid. Strip the prefix and the door is still reachable by file. 16-1's record already ruled this (row 25); the AC text has not absorbed it. |
| **R17-8** | **17-1's kind-8 filesystem row is CHEAPER than the AC assumes — the bridge exists and is simply never invoked.** Producers of `CapAuditEvent::SandboxBlock`: `security/mod.rs:512` (`emit_sandbox_block` `:505`), `t3/cap_audit_bridge.rs:20` (`emit_t3_escape_block` `:14`, wired into `t3/mod.rs:55`), `t3/quarantine.rs:47`; writer → `FrameKind::SandboxBlock` `writer_task.rs:115`. `emit_t3_escape_block` has **zero callers in `crates/**`**. So AC1 calls an existing emitter rather than authoring one — cite it. |
| **R17-9** | **The first-Worker-id premise is CLOSED and now load-bearing for exit line 1b.** `supervision.rs:1285-1293` hard-codes `seq == 0 ⇒ "worker"` with the comment naming this epic. `--spirit worker` therefore resolves at HEAD — but `worker-sandbox.toml` must make the probing Worker the **first** binding of its root, or the id is `worker-2`. |
| **R17-10** | **`worker_spawn.rs` gained a supervision preamble 17-1 must thread, not skip.** Between token mint and spawn: `bind_token` `:765`, the stopping/phase pre-spawn check `:772-777`, and the guard-first `LiveWorker` construction `:809` whose comment records that `SpawnedBridge::drop` kills and reaps and that **struct fields drop in declaration order**. Any re-route through `spawn_t3` that reorders those fields inverts the RAII guard. |
| **R17-11** | **The dependency 17-4 AC1 names does not exist under that name.** The AC says "`maos-bin` adds the `maos-observer` dependency". `spirits/observer/Cargo.toml:2` is `name = "observer"`, and every sibling Spirit in `crates/maos-bin/Cargo.toml` is declared **bare** (`orchestrator` `:78`, `architect` `:79`, `reviewer` `:80`, `worker` `:81`, `mira` `:82`, `nash` `:83`, `butler` `:84`, `researcher` `:85`); only the digest crate is prefixed (`:86`). The line to add is `observer = { path = "../../spirits/observer" }`; cite `:78-86` as the precedent block. |
| **R17-12** | **Two header citations are off by a line or straddle a heading; both substances hold.** Feature `wasm-host` is `crates/maos-bin/Cargo.toml:29`, not `:30` (`:30` is now `spirit_test`); OFF-by-default still verified (`default = ["network"]` `:15`, optional dep `:58`). `RELEASE-HOLDS.md:29-34` straddles a blank line and the `## Standing mitigations` heading — Hold 2's row is `:28` and the export precondition paragraph is `:32-38`. |
| **R17-13** | **17-1 AC1 cites a trust-anchor precedent that does not demonstrate it.** `nfr-sec-1-t3-image-signature` at `discipline.yml:1420-1433` is checkout → toolchain → cache → `cargo test -p maos-kernel-core --test sandbox_t3_image_verify --release`. It sets **no** `MAOS_T3_IMAGE_TRUST_ANCHOR_PUB_HEX` (zero hits for that string in the whole workflow) and writes **no** lock file. `epic-17-exit` must **originate** both — unpriced work inside AC1. |
| **R17-14** | **AC2's `manifest_field_coverage` citation points at a job, not at editable rows.** `discipline.yml:668-683` only runs `cargo test -p maos-kernel-core --test manifest_field_coverage --locked`; the field rows live in the `MANIFEST_FIELDS` allowlist inside `maos-kernel-core`. Re-point the AC at the allowlist source. |
| **R17-15** | **`gate-registry.toml:200-201` now names a different gate.** At HEAD `:200-201` is `check-cross-form-equiv` — the relabelled CLI-wrapper distributional gate the file's own comment (`:192-193`) says is **not** the equivalence-binding row. `check-wasm-form-equiv` is `:212-213` (`v1_0=advisory, v1_5=advisory, v2_0=blocking`). The substantive claim (advisory at `v1_5`, so "stays green" is not binding) is TRUE and independently pinned by `check_wasm_form_equiv.rs:370-412`. |
| **R17-16** | **The epic's own `features.*wasm-host` inventory grep no longer reproduces.** Line 22 asserts 1 hit at `:2901`; at HEAD the regex returns **0**. The probe at `:3012` is `cargo tree -p maos-wasm-host -e features … \| grep -q "equiv-fault-inject"`, which never matched the pattern as written. The substance strengthens: repo-wide, no CI job builds `maos-bin --features wasm-host` (only `:442`, `cargo build -p maos-wasm-host --bin maos-wasm-runner`, no feature flag). Rule 8: replace the grep with `grep -nE 'maos-bin/wasm-host\|--features[^\n]*wasm-host' .github/workflows/discipline.yml` = 0 @ `e868ff28`. |
| **R17-17** | **The "maos-manifest +150 row" this file cites is not the row 15-2 landed.** `kloc.toml:388` records `4314 → 4474 (+160)` over a measured 4214, and its ledger `:389` books "Epic 17 … +80–200". Epic 17's combined ask (+70–140) is absorbable, but the figure named in **Dependencies** matches neither the ceiling nor the ledger envelope. ⚠ Whether any of the 260 was spent by Epic 16 is recorded in **no** file — `kloc.toml` stores ceilings only, with no `current` key, so per-crate headroom for the untouched crates is knowable only from a `kloc-check` run. |
| **R17-18** | **17-3b's exit line 2 is red for TWO independent reasons.** `examples/example-spirit-ts/manifest.toml:7` is `forms = ["ts-inproc"]`, a value **no** validator accepts (`manifest.rs:393` allows `rust-inproc\|subprocess` only), so the manifest fails `ClassSection::from_toml_str` **today**. AC4 must state that `:7` is rewritten to `forms = ["wasm-component"]` and that `ts-inproc` is an orphan value to delete, not alias. `epic-20` AC4 depends on the same rewrite. Related: AC4's "tier derivation `admission.rs:4-40`" is imprecise — the executable derivation is `parse_manifest_trust_tier` → `strictest_of` `:190-198` with arms `:198-234`; `parse_manifest_string_array` `:507` is the natural reader for `forms`. |
| **R17-19** | **`maos-egress` is a PHANTOM ROW.** `crates/maos-egress/` does not exist, yet `kloc.toml:625` books 780. Every other non-existent crate in that file was zeroed as a "PHANTOM RETIREMENT" (`:249`, `:251`, `:252`), and the CEILING RULE block forbids "a reservation for planned growth". 17-1's +300–600 fits, but the row invites its own lowering before 17-1 mints the crate. **Also uncited:** minting the crate trips `check-workspace-count` (`xtask/src/check_workspace_count.rs:26-27`, job `discipline.yml:267-278`), green at 55/55 today — 17-1 AC1 must bump the members list **and** the sentinel-anchored declared count in the architecture doc. |
| **R17-20** | **`SCANNED_SOURCE_FILES` is unstated.** `crates/maos-bin/tests/cohort_daemon_smoke_13_5c.rs:901` is `[(&str,&str); 24]` with a `len()` assert `:1006`. 17-3b (recall service) and 17-4 (telemetry pump) each add a `crates/maos-bin/src` module → 25, 26. Also: `kloc.toml:640` books the registry raise to **17-4** while this epic spends it in **17-3b AC4**. |
| **R17-21** | **The schema bump touches more than `abi-ratifications.toml`.** Beyond the entry shape (`abi-ratifications.toml:37-58`, `:60-70`), `check_manifest_schema_version.rs` parses all three constants out of `maos-spirit-abi/src/lib.rs` (`:35`, `:48`), enforces `0 < MIN ≤ CURRENT ≤ MAX`, forbids hardcoded comparisons in `maos-manifest/src/manifest.rs` (`:36`, `:67`), and carries `RATIFIED_POST_V1_SCHEMA_SECTIONS` (`:37`), which already lists `cli_wrapper`. Three literals move on a 4→5 bump (`lib.rs:112`, `:114`, and the doc row `:33`). |

**Exit-block verdict (rule 1, re-run 2026-09-20):** `cargo run -p xtask -- check-exit-commands` **PASSES** — 6 epics, 6 blocks, 33 tokens resolved, 4 owed, and **none of the four is Epic 17's**. Heads `maos` (`MAOS_BINARIES` `:152`), `cargo` (`DECLARED_NON_COMMANDS` `:139`) and `target/debug/maos` (stems at `:1085-1088`) all resolve; verbs `run` and `audit` are live `VERBS` rows (`verbs.rs:96`, `:108`). Epic 17's block therefore needs **no** AC1-created verb. ⚠ Caveat in the gate's own source (`:1580-1587`): a binary absent from `target/debug` is treated as absent and its tokens must then be owed by a named open story — `maos-wasm-runner` is currently warned as unbuilt, so 17-3b AC1's build step is what keeps line 2 honest.

## Dependencies

Epic 15 (`15-2-kloc-ceiling-rebase` incl. the maos-manifest +150 row and the per-story kernel figures 17-1 +65–130 / 17-2 net ≤+10; `15-5-decision-adrs-and-provisioning-checklist` for ADR-060 (17-3b AC4) and ADR-061 (17-1); `15-3-single-phase-source-and-exit-command-check` for `check-exit-commands` on the exit block). Epic 16 releases the `main.rs` write lock before 17-1 lands (17-1's proxy wiring, 17-3b's launch path and 17-4's Observer arm all edit `main.rs`; `round2.md:33`). Intra-epic: **17-3a before 17-1** (it prices the proxy) **and before 17-3b**; **17-1 before 17-2** (the Worker process model fixes the cgroup layout); 17-4 independent. Outward: 17-1 before `19-3-real-worker-default-with-effect-oracle`'s **operator leg only** (`epic-19-founder-loop-w4.md:60,75`); nothing here needs a later epic.

**⚠ R4 (2026-09-20) — the `main.rs` write-lock clause above is now HALF wrong and is restated per story.** `crates/maos-bin/src/` holds **23 modules** at HEAD (`admission.rs` 16-6, `supervision.rs` 16-3, `purge.rs` 16-4, `operator_door.rs` 16-1, `shell_host.rs` 16-2, `verbs.rs` 15-3, `worker_spawn.rs`, `topology.rs`, …), but `main.rs` is still **14,632 lines** and still exclusively owns the standalone and topology launch matches, `LoadedSpiritKind`/`classify_spirit`, the `TelemetryStreamAdapter` construction and the `spawn_t3` smoke. Therefore: **17-4** is still a `main.rs` launch-block story (enum + three exhaustive matches + the pump's build site) *plus* a one-line `admission.rs:317` edit — the clause understates it; **17-3b**'s launch path now **bifurcates** — gate/admit is `admission.rs` (a `wasm-component` form must be refused-or-routed at `gate_manifest` gate 5, `admission.rs:409`, whose `AdmissionRefusal::UnknownClass` doc at `:221` already names this epic), construct/start is still `main.rs`'s match, and 17-3b's Δ line names `admission.rs` nowhere; **17-1** does **not** contend for the launch block at all — its contact with `main.rs` is the `spawn_t3` smoke at `:6982` only, while its real blast radius is `worker_spawn.rs` / `worker_cli.rs` / **`supervision.rs`** (R17-4). Replace the blanket clause with: *"Epic 16 releases the `main.rs` launch-block lock before **17-4** lands; 17-1 and 17-3b are launch-block-free, but 17-3b acquires `admission.rs`."*
