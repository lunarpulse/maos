# Epic 20 — Ship It (W5)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 20 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (18 of 18 applied) and the decisions D-D, D-G are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`.

**Wave / duration:** 5–8.5 weeks (measured; 20-3 is 2–4 of it) · **Makes true:** v1.0-rc · a third party publishes and installs a Spirit without the source tree; the release is real; the gates say what they measured

**Closes:** S4, S5; FR1, FR3 (partial), FR35, FR36, FR37, FR59; NFR-Meta-3, NFR-Ops-12; D1, D8, D9, D10, D20

**Hermetic exit command (CI job `epic-20-exit`, ubuntu-latest = linux-amd64, no secrets; the epic's first CI job, red until green; dry-parsed at HEAD @9f920180 — `bash -n` clean, provenance below). `./dist` is 15-4's `release-dry-run` layout — bare per-target binaries + `SHA256SUMS`; no tarball, no `curl`, no tag:**

```
cargo run -p xtask -- release-dry-run                                                                          # 1 — ./dist/{maos,maosctl,maos-registry-server,maos-spirit}-linux-amd64 + SHA256SUMS; the last two binaries join the set in 20-1 AC1
RELEASE_SIGNING_KEY=794959d4c4dc813f968cd95eb4a45c4a02583a7c5211126e7b4583e4776d1c8d cargo run -p xtask -- release-verify --sign --key-env RELEASE_SIGNING_KEY --sha256sums dist/SHA256SUMS --output dist/SHA256SUMS.sig   # 2 — the documented dev seed (release_verify.rs:254); its pubkey is the bundled default (:26-30) the dry-run binaries verify against
for b in maos maosctl maos-registry-server maos-spirit; do ln -sf "$b-linux-amd64" "dist/$b"; done && export PATH="$PWD/dist:$PATH"   # 3 — unsuffixed names for the lines below; --from-local still reads the suffixed file
maosctl install --from-local ./dist --verify-only                                                              # 4 — Ed25519 over SHA256SUMS, then SHA256 of maos-linux-amd64; exit 0
REG_ROOT=$(mktemp -d); maos-registry-server --bind 127.0.0.1:6789 --root "$REG_ROOT" &                          # 5 — the registry on a throwaway root (the installing client's own store is ~/.local/share/maos/registry/)
for i in $(seq 1 50); do (exec 3<>/dev/tcp/127.0.0.1/6789) 2>/dev/null && break; sleep 0.1; done             # 6 — readiness, ≤5 s
MAOS_SPIRIT_SIGNING_KEY=$(od -An -tx1 -N32 /dev/urandom | tr -d ' \n') maos-spirit publish --registry-uri http://127.0.0.1:6789/mcp --tier=public_untrusted --manifest examples/example-spirit-ts/manifest.toml --artifact examples/example-spirit-ts/dist/spirit.wasm --signing-key-env MAOS_SPIRIT_SIGNING_KEY   # 7 — a third-party key; the 17-3b WASM fixture at the third-party tier
maosctl spirit install example-spirit-ts@0.1.0 --registry-uri http://127.0.0.1:6789/mcp                       # 8 — registry.manifest + registry.artifact fetch, public_untrusted admission (signature + ComplianceClaim), persist; summary JSON on stdout
maos init && printf '@example-spirit-ts hello\n' | MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j0/shell-intro.json maos shell   # 9 — the daemon boots from the release binary with the installed Spirit in its load set; the dispatch returns the typed engine-off refusal (WASM engine off in published binaries until Hold 2, 15-5 AC3), never "unknown spirit"
```

**Exit-block provenance (@9f920180):**
- Line 1 — `release-dry-run` created by 15-4 AC1 (`grep -rn release-dry-run xtask .github` empty); red at HEAD: unrecognized subcommand. Layout per `release.yml:17-23,:47-54` (`maos-linux-amd64` naming; `download-artifact@v4` + `merge-multiple` merge into `dist/`, `:67-69`); `maos-registry-server` and `maos-spirit` are added to that set by 20-1 AC1 (today `release.yml:44` builds `-p maos-bin` only).
- Line 2 — `release-verify --sign --key-env … --sha256sums … --output …` exists (`xtask/src/main.rs:737-766`; the same invocation as `release.yml:74`); red at HEAD: `dist/SHA256SUMS` absent (line 1). The seed is the documented development key (`crates/maos-audit/src/release_verify.rs:253-262`, in the test module); `RELEASE_PUBKEY` defaults to its pubkey when `MAOS_RELEASE_PUBKEY` is unset at build time (`:21-24,:26-30,:55-58`), and that same pubkey is the one embedded in `packaging/deb/rules:10`, `maos.rb:20`, `PKGBUILD:36`, `rpm/maos.spec:20` — so the dev-signed `./dist` verifies through `maosctl` (line 4) and through `dpkg-buildpackage` (20-2 AC1) with no secret. Not a production key: `ops-provisioning-secrets-and-accounts` supplies `RELEASE_SIGNING_KEY`/`MAOS_RELEASE_PUBKEY` for tags (`release.yml:75-77`).
- Line 3 — coreutils only; the symlinks are for the runtime PATH; `check-exit-commands` (15-3 AC3) is static and resolves the four bare binary tokens (`maos`, `maosctl`, `maos-registry-server`, `maos-spirit`) against 15-4's artifact set; `--from-local` reads `platform_binary_name()` = `maos-linux-amd64` (`crates/maos-cli/src/subcommands.rs:1216-1222`).
- Line 4 — `maosctl install --from-local <dir> --verify-only` exists (`crates/maos-cli/src/cli.rs:351-358`; `install_from_local` `subcommands.rs:1235-1316` reads `dir/SHA256SUMS`, `dir/SHA256SUMS.sig` (raw 64 bytes), `dir/maos-linux-amd64`; pubkey = bundled `RELEASE_PUBKEY` unless `--release-pubkey`, `:1179-1201`); red at HEAD: no `./dist` (line 1). `maosctl` is a dry-run artifact by 15-4 AC1.
- Line 5 — `maos-registry-server --bind --root` created by 20-1 AC1: `crates/maos-registry/src/bin/server.rs:1-7` prints "not yet wired — Story 5.5d Task 13" and exits 1, no flags; the real server is `MAOS_ONE_SHOT=registry-server maos` at `crates/maos-bin/src/main.rs:7693-7701` (port literal `127.0.0.1:6789`, `LocalFsRegistryStorage::new()` fixed at `~/.local/share/maos/registry/`, `storage.rs:89-96`). Red at HEAD: exit 1. `mktemp` is coreutils.
- Line 6 — bash builtins (`/dev/tcp`); bounded at 5 s; no wall-clock leg.
- Line 7 — `maos-spirit publish --tier --manifest --artifact --registry-uri --signing-key-env` all exist (`crates/maos-spirit-cli/src/bin/maos-spirit.rs:33-58`; `--tier` values `local|org_internal|public_untrusted|public_vetted` `:36`; seed = 64-hex accepted by `signing.rs:90-105`; `od`/`tr` are coreutils). Red at HEAD for three reasons, each owned: the non-stub guard `publish.rs:267-272` (20-1 AC1 deletes it); `examples/example-spirit-ts/manifest.toml:8` is `trust_tier = "local"` → `TierMismatch` (`publish.rs:111-118`; 20-1 AC4 sets `public_untrusted`); `examples/example-spirit-ts/dist/spirit.wasm` and the `forms = ["wasm-component"]` manifest are 17-3b AC1 (the job runs Epic 17's componentize-js step first). `maos-spirit` becomes a dry-run artifact in 20-1 AC1 (a third party publishes without MAOS's tree).
- Line 8 — `maosctl spirit install` created by 20-1 AC1 (`SpiritOp` = `HotSwapPrecheck|Upgrade|Inspect`, `cli.rs:803-870`; remote fetch was removed at v0.5, `subcommands.rs:1209-1210`); red at HEAD: clap "unrecognized subcommand". `example-spirit-ts@0.1.0` = `examples/example-spirit-ts/manifest.toml:2-3` (`name`, `version = "0.1.0"`), not `researcher@1.0` (`spirits/researcher/manifest.toml:29` is `0.5.0`, `:33 forms = ["rust-inproc"]`, refused from any registry by ADR-060 / 17-3b AC4).
- Line 9 — `maos init` exists (`main.rs:1679`); `maos shell` exists (`main.rs:1730-1737`); `MAOS_INFERENCE_MODE` created by 15-6 AC1 (D-D / ADR-064: `replay` requires `MAOS_REPLAY_CASSETTE`); the cassette exists @HEAD (`crates/maos-journey-test/cassettes/j0/shell-intro.json`, the J0 shell cassette Epic 16 also names; `MAOS_REPLAY_CASSETTE` registered `env_contract.rs:255`). The observation is created by 20-1 AC1: today the shell dispatches only the in-proc hello-spirit (`crates/maos-shell/src/lib.rs:164,:176-200`) and no daemon path reads the local registry store (only the registry-server arm does, `main.rs:7694-7697`) → red at HEAD: `maos: expected @<spirit> …`/unknown spirit. Engine-off on published binaries = 15-5 AC3 (`--features wasm-host`, `maos-bin/Cargo.toml:30`).
- Nothing in the block needs a secret, a paid key, a human, wall-clock or the network beyond cargo's registry cache and `127.0.0.1`.

**Operator-lane legs (never in the exit):**
- `ops-provisioning-secrets-and-accounts` — `RELEASE_SIGNING_KEY` + `MAOS_RELEASE_PUBKEY`, tap repo + token, AUR account, `fuzz-ledger`/`rto-ledger` branch creation + push permission (`fuzz-cadence.yml:25-29`, `rpo-rto-cadence.yml:17-18`), **and** `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN` + cosign for `container.yml:40-44,:71-80` (20-2 splits build from push)
- `ops-external-cohort-and-pen-test` — ≥3 external authors recorded through 20-3's honest-N mode; pen-test firm and dates (Hold 1)
- `ops-brew-tap-and-aur-publication` — publish the 20-2-corrected formula/PKGBUILD to the tap repo and AUR
- `ops-rc-signed-tag` (NEW row) — `git tag v1.0.0-rc.1 && git push --tags` after 20-5 lands and the row above records both secrets; acceptance: `release.yml` green on the tag with `prerelease: true`, `.deb` (amd64 + arm64) and the air-gap variant attached, `container.yml` skipped-or-green
- `ops-ga-tag-after-holds` — `v1.0.0` + the LTS clock after RELEASE-HOLDS Holds 1–2 clear
- `ops-live-gemini-leg` (NEW row) — 20-4's shell line under `MAOS_INFERENCE_MODE=record` with a paid Gemini key; records the cassette with `provenance: live-record`; never in CI (`ops-nightly-cassette-rerecord` refreshes it thereafter)

**Kernel-Δ:** ZERO kernel-Δ @24472 (`xtask/kernel-core-baseline.toml:472`); the two seams that could have touched the kernel are placed outside it by name (20-1 AC3 yank policy ≠ `RegistrySection`, `kernel-core/src/security/operator_config.rs:12-26`; 20-4 AC2 pin enforcement at the maos-bin adapter, not `inference/mod.rs`).

**Kloc (asks for the 15-2 table, measured @9f920180 with `cargo run -p xtask -- kloc-check`, exit 1):** maos-bin +100–200 of the lane-wide +3000 (20-1 yank-policy application in `TlYankObserver` +60–120; 20-4 provider registration + pin check +40–80) · maos-cli +250–400 (20-1 `spirit install`; 13.4 precedent `cli.rs` +23 / `subcommands.rs` +104 for one verb, this one fetches + admits) — **note 16-1 books +1100 (raised 2026-09-05; 16-1 books +400–700 of it, 20-1 draws the remainder and re-measures at Epic-20 open) of the same +700 grant** · maos-registry +230–440 (server-bin wiring + `with_root` +50–100, leaf HTTP `IoSubsystemPort` +80–150, yank policy config +50–100, `RegistryOrigin::Remote` +20–40, install helper +30–50) against +300 · maos-spirit-cli +230–400 (client wiring +80–150, `vet issue|revoke` incl. the signed-revocation issuer +150–250) against +300 · maos-providers +300–400 (`gemini.rs`, `anthropic.rs` is 307 lines) against +400 · maos-audit +40–80 (14-e1 +20–60, relocated governance-categories test +20) against +100 · maos-compliance 0 (the issuer lives in maos-spirit-cli; headroom 85) · maos-manifest 0 · maos-mcp 0 · xtask: 20-3 adds +1100–1800 and retires 6406 lines (+130 fold-ins) → **net −4.5k…−5.2k**; 20-2 +0–30 (enrol `check-air-gap`); 20-5 +20–40 (`stability_matrix.rs`) — within "xtask +400 / net ≤0 after retirements" only because `check_epic_6_bridge.rs` (4592) is retired (without it 20-3 nets +300…+1000).

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `20-1-registry-client-install-verb-vetter-and-yank` | Registry client + server binary, `spirit install`, vetter CLI, yank policy | FR35/36/37/59 · kernel-Δ 0 · maos-cli +250–400 · maos-spirit-cli +230–400 · maos-registry +230–440 · maos-bin +60–120 |
| `20-2-deb-airgap-docker-and-formula-corrections` | deb (debhelper), air-gap leg, Docker build/push split, formula corrections | FR1 · NFR-Ops-12 · kernel-Δ 0 · xtask +0–30 · CI/packaging only |
| `20-3-gate-honesty-pass` | Gate honesty pass ◆ (19-gate inventory, honest-N, generator spike, `BindingClass` on the ship-gate rows, ten retirements) | S4 · D1/D8/D9/D10/D20 · NFR-Sec-5/Ops-9/Meta-3 · kernel-Δ 0 · xtask net −4.5k…−5.2k · maos-audit +40–80 |
| `20-4-gemini-driver-and-endpoint-pin` | Gemini driver and endpoint pin enforcement | FR3 (partial) · kernel-Δ 0 · maos-providers +300–400 · maos-bin +40–80 |
| `20-5-v1-0-rc-tag-and-lts-rule` | `v1.0.0-rc.1` readiness and the LTS rule (the tag = `ops-rc-signed-tag`) | NFR-Maint-6 · kernel-Δ 0 · xtask +20–40 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 20-1-registry-client-install-verb-vetter-and-yank — Registry client + server binary, `spirit install`, vetter CLI, yank policy

*Closes · Δ:* FR35/36/37/59 · kernel-Δ 0 · maos-cli +250–400 · maos-spirit-cli +230–400 · maos-registry +230–440 · maos-bin +60–120

- **AC1 (command).** Exit lines 5, 7, 8 and 9 run. (a) **Server binary:** `maos-registry-server` (`crates/maos-registry/src/bin/server.rs:1-7`, exits 1 today; `[[bin]]` declared at `crates/maos-registry/Cargo.toml:36-38`) is wired to `SpiritRegistryServer::new(storage, addr, org_pubkey)` (`src/server.rs:62-74`) + `with_server_signing_key` (`:78-81`) behind clap `--bind <addr>` / `--root <dir>` / `--signing-key <path>`; `--root` needs a new `LocalFsRegistryStorage::with_root(dir)` (`new()` is fixed at `~/.local/share/maos/registry/`, `src/storage.rs:89-96`); the daemon's `MAOS_ONE_SHOT=registry-server` arm (`crates/maos-bin/src/main.rs:7693-7701`, port literal 6789) delegates to the same constructor; `maos-registry-server` **and** `maos-spirit` are added to 15-4's artifact set (`release.yml:44` and `release-dry-run` build `-p maos-bin` only). (b) **Publish reaches the real server:** the stub-only guard at `crates/maos-spirit-cli/src/publish.rs:267-272` is deleted; `McpSpiritRegistryClient` (`crates/maos-registry/src/client.rs:38-50`; daemon wiring at `crates/maos-bin/src/main.rs:2918-2948`) is constructed in `maos-spirit-cli` over `McpClientImpl` + `StreamableHttpTransport` (`crates/maos-mcp/src/transport/streamable_http.rs:19`, takes an `IoSubsystemPort`); the only production HTTP `IoSubsystemPort` is kernel-core's (`crates/maos-kernel-core/src/io/mod.rs`), so a leaf blocking-HTTP implementation lands in `maos-registry` and both CLIs use it (`maos-spirit-cli` already depends on `maos-mcp` + `maos-registry`, `Cargo.toml:31-32`); the server ignores the URL path (`server.rs:150-155`), the client default is `http://127.0.0.1:6789/mcp` (`publish.rs:288`). (c) **`maosctl spirit install <name>@<ver> --registry-uri <uri>`:** new `SpiritOp::Install` (`crates/maos-cli/src/cli.rs:803-870` has `HotSwapPrecheck|Upgrade|Inspect`; top-level `install` means the release binary, `:336-363`; the remote fetch was removed at v0.5, `subcommands.rs:1209-1210`); adds the edge `maos-cli → maos-mcp` (`crates/maos-cli/Cargo.toml:23` is maos-registry only; `maos-registry` re-exports the client but not `McpClient`, `src/lib.rs:38-41`; `check-dependency-closure` re-run), fetches `registry.manifest` + `registry.artifact` (`server.rs:230-260`), runs the admission `import` runs (`admit_spirit` / `admit_spirit_with_attestation`, `subcommands.rs:1073-1083`; `public_untrusted` = signature + ComplianceClaim envelope, `crates/maos-registry/src/admission.rs:196-208`) and persists with `publish_with_origin` (`:1092-1112`) under a new `RegistryOrigin::Remote { uri }`; the summary JSON (`:1116-1129`) is the observation. (d) **Visibility:** the daemon enumerates the local store at boot (today only the registry-server arm reads it, `main.rs:7694-7697`) so exit line 9 lists the installed Spirit; on a published binary the `@example-spirit-ts` dispatch returns the typed engine-off refusal (15-5 AC3) rather than "unknown spirit" (`crates/maos-shell/src/lib.rs:176-200` dispatches hello-spirit only today). kernel-Δ 0.
- **AC2.** `maos-spirit vet issue|revoke` wraps `issue_attestation` (`crates/maos-compliance/src/vetting/attestation.rs:150`; zero **production** callers — test callers at `crates/maos-registry/src/admission.rs:1219` inside `#[cfg(test)]`, `tests/vetting_attestation_gate.rs:176`, `tests/end_to_end_test.rs:229`); revocation reuses `VetterKeyring::push_attestation_revocation` (`vetting/keyring.rs:157`) / `is_revoked` (`:334`) / `verify_attestation_not_revoked` (`:288`) and enrollment `issue_event` (`:117`); what is new is the CLI and a signed-revocation-list **issuer** (placed in `maos-spirit-cli`, not `maos-compliance` — headroom 85, no grant). `maos-spirit validate|inspect` (`maos-spirit.rs:25-28`, exit-1 stubs) are out of scope and say so.
- **AC3.** Yank applies operator policy `warn | quarantine | auto-revoke`: today `TlYankObserver::on_yank` (`crates/maos-bin/src/main.rs:305-339`) journals `FrameKind::SpiritRevoked` (`:316-323`) **and** a `RunningSpiritObservation` via `observe_running_spirit(… registry_yanked: true …)` (`:328-338`, sink `:281-300`) but applies no policy (grep `quarantine|auto.revoke|policy` in `maos-registry/src/yank.rs` and `maos-compliance/src/vetting/terminal.rs`: none). The policy is a `maos_registry::yank::YankPolicy` config read by maos-bin from `MAOS_YANK_POLICY` (registered in `crates/maos-bin/src/env_contract.rs:13` format, `UserFacing`), **not** a `RegistrySection` field (`crates/maos-kernel-core/src/security/operator_config.rs:12-26`, constructor-guarded); `quarantine`/`auto-revoke` act through the existing revocation path (FR13, `yank.rs:7-10` distinction kept: yank alone never terminates). kernel-Δ 0.
- **AC4.** WASM artifacts install through the same path — requires 17-3b AC4 (registry admission accepts `wasm-component` at `TrustTier::PublicUntrusted`, the enum's top value `crates/maos-spirit-abi/src/compliance.rs:202`, and refuses `rust-inproc` from any registry, ADR-060; `crates/maos-registry/src` has zero `wasm` mentions today). The exit's Spirit is `examples/example-spirit-ts` (`forms = ["wasm-component"]`, schema 5, by 17-3b; artifact `examples/example-spirit-ts/dist/spirit.wasm` from 17-3b AC1's componentize-js step, which the `epic-20-exit` job runs first); this story sets its `trust_tier` (`manifest.toml:8`, `"local"` @HEAD) to `"public_untrusted"` because publish's tier check requires manifest == `--tier` (`publish.rs:111-118`) and `maos run` never reads `trust_tier` (only the upgrade path does, `main.rs:136-137`), so Epic 17's exit line 2 is unaffected. Execution of the installed WASM Spirit from a **published** binary is Hold-2-gated (15-5 AC3); the hermetic execution leg of this AC builds `--features maos-bin/wasm-host` (Epic 17 exit line 2's build) and runs the installed manifest from the local store `--once`. Negative: publishing `spirits/researcher` (`rust-inproc`) is refused at install with the 17-3b typed error.

### 20-2-deb-airgap-docker-and-formula-corrections — deb (debhelper), air-gap leg, Docker build/push split, formula corrections

*Closes · Δ:* FR1 · NFR-Ops-12 · kernel-Δ 0 · xtask +0–30 · CI/packaging only

- **AC1 (command).** `release.yml` builds the `.deb` with `dpkg-buildpackage -us -uc -b` over the existing `packaging/deb/{control,rules}` (debhelper, `rules:21-22 dh $@`; `rules:24-41` already verify the Ed25519 signature over `SHA256SUMS` with the embedded `RELEASE_PUBKEY` `:10` and `:49-56` the SHA256 of the binary; `control:5` needs `python3-cryptography`) for amd64 and arm64 (`rules:13-16` map `DEB_HOST_ARCH` to `maos-linux-{amd64,arm64}`); the same step runs in the discipline `release-dry-run` job against the dev-signed `./dist` (exit line 2 — the embedded key **is** the dev default, `release_verify.rs:26-30`), so the `.deb` is proven without a secret; an air-gap matrix leg builds `--no-default-features --features air-gap` (`crates/maos-bin/Cargo.toml:15 default = ["network"]`, `:29 air-gap = []`; compiles at HEAD — `target/debug/maos --version` prints `maos 0.1.0-alpha` from the air-gap main `main.rs:1318-1342`) and re-runs `check-mock-not-in-release` on it (`xtask/src/check_mock_not_in_release.rs:29-31`; `release.yml:46`); `check-air-gap` (`xtask/src/check_air_gap.rs:12-27,:46-60`; `main.rs:767-768,:1342-1347`; needs `nm`, dirty-fixture bite `tests/fixtures/dirty-network-fixture`) is enrolled in `gate-registry.toml`, `discipline.yml` and the coverage matrix (0 hits in all three today); `tests/air-gap-netns-corroborate.sh` (`:29-48`, four `SKIP … exit 0` branches; no workflow runs it — grep `.github xtask/src`: 0) is run **by the air-gap leg** (`unshare`/`strace` installed, root or `CAP_SYS_ADMIN` available on the ubuntu runner) and exits non-zero on SKIP there; the Docker **build** is a discipline job (`Dockerfile:27 RUN cargo build --release -p maos-bin --locked` gains `ARG FEATURES` and a `--features "$FEATURES"` form; no `docker build` exists in `discipline.yml` today) while **push + cosign** stay in `container.yml` (`:33-44,:71-80`, SCAFFOLD) behind `ops-provisioning-secrets-and-accounts`, and `container.yml` is skipped (`if:` on the secrets) rather than failing on a tag.
- **AC2.** `packaging/homebrew/maos.rb` and `packaging/aur/PKGBUILD` reference the real artifact set (no `SCAFFOLD`/`PLACEHOLDER_SHA256`: `maos.rb:7,:25,:32,:36,:42,:47`; `PKGBUILD:6,:27-32`), as does `packaging/rpm/maos.spec:4`; the embedded pubkey at `maos.rb:18-20` / `PKGBUILD:34-36` / `deb/rules:5-10` / `maos.spec:20` (the dev default) is replaced by the production key when `ops-provisioning-secrets-and-accounts` records it, and the formula's `test` block (`maos.rb:76`, `maos --version`) uses a verb the **network** build answers (today only the air-gap main has `--version`, `main.rs:1332`; 15-3's `maos --help/--version` verb table). `maos.rb:5` says it calls `maosctl` but `install` (`:52-73`) never does — corrected either way. Publication is `ops-brew-tap-and-aur-publication`.
- **AC3.** `maos uninstall` (16-4, new top-level verb; today `uninstall` is a `MAOS_ONE_SHOT` lifecycle mode, `main.rs:5148-5170`) removes what the `.deb`/formula installed (`/usr/bin/maos` per `rules:44`); asserted in the deb leg with `dpkg -r`.

### 20-3-gate-honesty-pass — Gate honesty pass ◆

*Closes · Δ:* S4 · D1/D8/D9/D10/D20 · NFR-Sec-5/Ops-9/Meta-3 · kernel-Δ 0 · xtask net −4.5k…−5.2k · maos-audit +40–80

- **AC1 (command).** `cargo run -p xtask -- check-fuzz-floor` (`xtask/src/main.rs:961`) and the other gates of the **19-gate inventory** (measured @9f920180, R2 note §A #25 — the R2 file's "23" had no list) report `EvidenceState::Absent`/`Indeterminate` as non-green: never a silent PASS; exit code per the gate's `BindingClass` (Blocking → 1; Advisory → 0 with the `::warning::` annotation and the state in `--json`). Inventory with disposition — **file-absent advisory PASS (9):** `check-cna-registration` (`check_cna_registration.rs:54-60,:132-134`; retired by 15-3 AC4, untouched here), `check-pentest-gate` (`:85-87`), `check-red-team-gate` (`:133-135`), `check-skill-conformance` (`:141-153`), `check-third-party-trial` (`:208-210`), `check-cross-form-equiv` (`:189-191`; **retired** under AC7, not converted — D10 relabeled it non-binding), `check-fuzz-floor` (`:76-85`), `check-migration-merkle` (`:185-194`), `check-rto-gate` (`:57-72`, `skipped` → `Ok(())`, exits 0 today — no longer); **phase-advisory (6)** — RED or skipped leg → `Ok(())` at `CURRENT_PHASE = "v1_5"` (one source after 15-3): `check-trial-attestation` (`:122,:136-139`), `check-wasm-form-equiv` (`:293`), `check-enterprise-pdp` (`:536`), `check-enterprise-identity` (`:620`), `check-escape-detector` (`:642`), `check-fkcs` (`:349`); **ledger gates (4)** already projecting `EvidenceState` (`gate_common.rs:328-420 EvidenceVerdict::project`, `evidence_ledger.rs:1519-1600 finish_ledger_gate`) but mapping `Absent → Ok(())` off the `MAOS_LEDGER_ENFORCE` lane (`evidence_ledger.rs:131-140,:1528-1530,:1596-1597`): `check-cross-region-consensus`, `check-multi-region-slo`, `check-multi-tenant-loom`, `check-reza-production-path`. **Converted: 17** (13 gain the `EvidenceVerdict::project` seam; the 4 ledger gates stop mapping Absent to green off-lane). Decided explicitly, not converted: `check-j1-two-host-signed-run` (`:1301-1309`, capture absent → PASS with the claim refused — INDETERMINATE-by-design, kept as is) and `coverage-matrix` (`tests/coverage-matrix.yaml:3 mode: warning` → `error` in AC5). `EvidenceState` lives in 7 files today (`gate_common.rs`, `evidence_ledger.rs`, `demo_j1.rs` + the 4 ledger gates), i.e. **4 gates** carry the seam, not 7.
- **AC2.** `check-third-party-trial` gains an honest-N mode: `participants_total < 12` → `preliminary` (today `Err` at `check_third_party_trial.rs:300-303`), advisory under the `blocking-when-present` disposition (`gate-registry.toml:175-177` gains the `preliminary` case; present-and-below-floor still blocks at v2_0), the strata floor dated, so the real cohort file from `ops-external-cohort-and-pen-test` can be committed.
- **AC3.** The ledger collectors fail loudly on a missing `fuzz-ledger`/`rto-ledger` branch (today they warn and no-op, `fuzz-cadence.yml:28-29`; neither branch exists — `git branch -a`); branch creation stays in `ops-provisioning-secrets-and-accounts`. Fuzz runs as chained ≤6 h jobs (today 600 s × 4 workers weekly, `fuzz-cadence.yml:35-38,:77-82`; the collector `:104-120` sums per-chunk records); the floor stays advisory until the ledger spans 90 days (`check_fuzz_floor.rs:13`; `gate-registry.toml:237-247`) and the weekly-cadence floor re-derivation (`fuzz-cadence.yml:12-17`: ~8× unreachable at weekly) is recorded **before** any promotion — the AC says so.
- **AC4 (spike, ≤2 days, rule 5).** A written go/no-go for the generated coverage matrix answers: (1) the requirements-keyed source (`tests/coverage-source.toml`) that carries `phase`/`valid_until`/`notes`/`corpora` for the 198 entries (`tests/coverage-matrix.yaml:15-33`, 1982 lines) — a per-gate `covers` field cannot express them; (2) `covers = [...]` per gate in `gate-registry.toml` (no such field today, grep 0); (3) the oracle: the generator regenerates today's `tests/coverage-matrix.yaml` byte-identically from source + registry before any edit; (4) `invariant_lock.rs:156` (`changed.contains("tests/coverage-matrix.yaml")`) re-pointed at the source — a generated file cannot be the corpus-delta witness; (5) `coverage_matrix.rs:93-120` unchanged; (6) `check-coverage-matrix-completeness` (`check_coverage_matrix_completeness.rs`, 100 lines) subsumed by the generator refusing a `v1.0` entry with empty `gates`. No proc-macro crate. No-go → AC5 is cut and its retirement in AC7 reverts.
- **AC5.** On go: the generator lands per the spike; `tests/coverage-matrix.yaml` is generated and `mode: error`; `invariant-lock` is re-ratified in the concrete sense that its corpus-delta rule reads the source file.
- **AC6.** `BindingClass` (`gate_common.rs:210-222`; `dev_enforced_red_blocks` call site `:229-234`) on every ship-gate row: the 40 `EXPECTED_GATES` (`check_ship_gate_completeness.rs:20-60`) ∪ the 38 `[[ship_gate]]` rows (`grep -c '^\[\[ship_gate\]\]' xtask/gate-registry.toml`) = 42 names; the drift between the two lists (`ccac-n600-ship-gate`, `check-breaking-md`, `check-stability-matrix`, `nfr-rel-3-hsis-95pct` only in the const; `check-fuzz-floor`, `fuzz-build` only in the registry) is closed in the same AC. 15 carry it today (`grep -l BindingClass xtask/src/check_*.rs` = 16 modules, 15 of them ship-gate rows); of the 27 without, 5 are workflow-only jobs (`fuzz-build`, `rto-drill`, `windows-check`, `ccac-n600-ship-gate`, `nfr-rel-3-hsis-95pct`) and 2 structural lints (`check-breaking-md`, `check-stability-matrix`) recorded `Blocking` in the registry without touching a module, 1 is 15-3's (`check-cna-registration`), 1 is retired in AC7 (`check-cross-form-equiv`) → **18 modules** gain the call site. The 33 non-ship gates of the registry's `gates = [...]` list are not touched (D-G). E12-B1 (`sprint-status.yaml:309`, "remaining reach gap … tracked forward") closes here, D20 with it. **14-e1** lands in `crates/maos-audit/src/erasure/regional_teardown.rs:144-158` (`completed: true` unconditional; per 13.5b) under 15-2's maos-audit +100: no signed erasure attestation asserts a completion it did not observe.
- **AC7.** Ten gates retired after invariant-lock review (`gate-registry.toml:2-3`: removing or renaming a gate is a breaking change), each removed from the six enrolment sites — `xtask/src/main.rs` (`mod` `:96`-block, `#[command]` `:949`-block, dispatch `:1360`-block), `check_ship_gate_completeness.rs:20` (`EXPECTED_GATES`), `tests/coverage-matrix.yaml`, `discipline.yml` `aggregate.needs` (`:3510+`, 123 entries), `gate-registry.toml` — plus `xtask/tests/d19_story_file_governance.rs:355-362` where it lists them. Named, with module lines (`wc -l`): (1) `check_epic_6_bridge.rs` **4592** — retired from discipline 2026-06-12 by 8.16/ADR-043 (`discipline.yml:1370-1374`), module + dispatch (`main.rs:73,:1251`) still compiled; (2) `check_cross_form_equiv.rs` **421** — D10 relabeled "NOT the equivalence-binding gate" (`gate-registry.toml` comment above `check-fuzz-floor`), binding row is `check-wasm-form-equiv`; (3) `check_adr_040_accepted.rs` **217** — v0.5 release-block gate, in no workflow (`grep -rl adr-040-accepted .github` empty); (4) `check_literal_reappearance.rs` **158** — registered, in no workflow; (5) `check_governance_categories.rs` **108** — in no workflow; its round-trip assertion becomes a `#[test]` beside `kind_category_to_kinds` (maos-audit +20); (6) `check_coverage_matrix_completeness.rs` **100** — subsumed by AC4/AC5 (reverts on no-go); (7) `check_deprecations_declared.rs` **69** — in no workflow; duplicate of `stability-matrix --check` (`STABILITY.md:44-47`); (8) `check_bare_review_findings.rs` **164** → folded into `check-review-findings-resolved` (+40; both are 7.1.5 migrations of the Epic-6 beacon, `discipline.yml:1372-1373`); (9) `check_dev_model_used_populated.rs` **463** → folded into `check-dev-record-completeness` (+60); (10) `check_epic_close_green.rs` **114** → folded into `check-epic-close-coherence` (+30). `check-cna-registration` (260) is 15-3's and not counted. **Arithmetic:** retired 4592+421+217+158+108+100+69+164+463+114 = **6406**; fold-ins +130; adds from AC1/AC2/AC5/AC6 +1100–1800 (13 conversions +400–650, ledger posture +40–80, honest-N +40–80, rto +10–20, generator +300–500, 18 `BindingClass` sites +270–450, list drift +10) → **xtask net −4476…−5176**. Without (1) the story nets +300…+1000 and fails "net ≤0" — that retirement is load-bearing and is done first.

### 20-4-gemini-driver-and-endpoint-pin — Gemini driver and endpoint pin enforcement

*Closes · Δ:* FR3 (partial) · kernel-Δ 0 · maos-providers +300–400 · maos-bin +40–80

- **AC1 (command).** `cargo test -p maos-providers --test gemini_round_trip_test` (new; mirrors `crates/maos-providers/tests/openai_round_trip_test.rs:1-30`, a `MockTransport` `IoSubsystemPort` over fixture JSON) proves the driver's request/response shape hermetically — `crates/maos-providers/src/gemini.rs` is hand-rolled HTTP like `anthropic.rs` (307 lines) because `gemini-rs` is denied (`xtask/fr47-vendor-sdk-denylist.toml:17`); providers today = anthropic/openai/ollama/fixture_replay/rate_limit (`src/`, 1570 lines), registered with literal endpoints at `crates/maos-bin/src/main.rs:3081-3100` (`default_id` = first registered). Then `MAOS_DEFAULT_PROVIDER=gemini MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/providers/gemini-shell-intro.json maos shell` replies from the cassette (`provenance: seed` until `ops-live-gemini-leg` records it; D-D / ADR-064 selector). **Provider selection = option (a) of the R2 note §K #13:** `MAOS_DEFAULT_PROVIDER` does not exist in code (grep over `crates xtask`: 0) — it is created by the hotfix-lane row `provider-model-env-overrides` (`sprint-status.yaml:224`), now a stated dependency; the manifest `[providers].primary` route (`crates/maos-manifest/src/manifest.rs:2103`) is not used for selection. Live with a key is `ops-live-gemini-leg`.
- **AC2.** `provider_endpoint_pin` (parse-only today: field `crates/maos-manifest/src/manifest.rs:2103`, 64-hex validation `:2159-2171`, no other consumer — `maos-spirit-abi/src/compliance.rs:507` is a test) is enforced at provider construction / `InferencePortAdapter` in `maos-bin` (`main.rs:3081-3100`, `:3136`; endpoints are literals today): `sha256(endpoint_url)` vs the pin; a mismatch is a typed refusal in an out-of-kernel error type (governed by `xtask/fr63-typed-errors.toml`), never routed through `inference/mod.rs`. kernel-Δ 0.
- **AC3.** The Gemini model id is governed by `xtask/model-currency.toml` + `check-model-currency`, both created by the hotfix-lane row `model-pin-currency-gate-and-retired-pin-sweep` (`sprint-status.yaml:223`; `ls xtask/*.toml` = 24 files, none named so) — a stated dependency; until it lands the id is a literal in `gemini.rs` with a `// model-currency` marker. Bedrock/Vertex/Vault are `later-bedrock-vertex-and-kms-backends` (`sprint-status.yaml:294`).

### 20-5-v1-0-rc-tag-and-lts-rule — `v1.0.0-rc.1` readiness and the LTS rule

*Closes · Δ:* NFR-Maint-6 · kernel-Δ 0 · xtask +20–40

- **AC1 (command).** The discipline `release-dry-run` (15-4) at the story's HEAD contains `.deb` (amd64 + arm64) and the air-gap variant beside the three-target binaries (20-2), `cargo run -p xtask -- stability-matrix --check` (`xtask/src/main.rs:595`) is green, and the tree is tag-ready: workspace `version = "1.0.0-rc.1"` (`Cargo.toml:64`, `0.1.0-alpha` today; 15-4 AC5 takes it to `0.1.0-alpha.1`), `Cargo.lock` committed; `STABILITY.md` regenerated — `stability_matrix.rs` gains (i) a `release_channel` row derived from the pre-release suffix of `parse_workspace_version` (`:335-346`: `rc` → "pre-release; LTS clock not started", none → GA), (ii) the LTS placeholder re-pointed from "Epic 10" to `ops-ga-tag-after-holds` (`:350,:367`; `STABILITY.md:59-60`), while `lts_clock_start` (`:352-367`, exact `1.0.0`/`v1.0.0` refs) correctly does not fire on an rc; `release.yml` `softprops/action-gh-release@v2` (`:88-94`) gets `prerelease: ${{ contains(github.ref_name, '-rc') }}` (an rc publishes as a full release today); `container.yml` (`:12-14`, fires on `v*`) is skipped unless its secrets exist (20-2 AC1). The tag itself is cut by `ops-rc-signed-tag` after `ops-provisioning-secrets-and-accounts` records `RELEASE_SIGNING_KEY`/`MAOS_RELEASE_PUBKEY` (`release.yml:75-77`) — not in the exit block (rule 2).
- **AC2.** The LTS clock starts at `v1.0.0`, cut by `ops-ga-tag-after-holds` after RELEASE-HOLDS Holds 1–2 clear; `docs/release/` (today `v1.5-topology-support.md`, `v2.2-capacity-envelope.md`, `windows-deferral.md`; `tag-procedure.md` is 15-4 AC5) gains the rc → GA rule and says so.

## Dependencies

Epic 19 (the J1 demo is what a cohort installs); specifically 15-3 (`check-exit-commands`; `maos --help/--version` verb table — exit line 3's bare tokens and 20-2 AC2's formula test), 15-4 (`release-dry-run` + the artifact layout the exit stages in `./dist`; 20-1 AC1 adds two binaries to that set), 15-5 (ADR-060 forms by tier, ADR-064 replay selector; Hold-2 engine-off rule in AC3), 15-6 (`MAOS_INFERENCE_MODE` + `MAOS_REPLAY_CASSETTE`), 16-1/16-2 (`maos init` mints `control.json`; the shell binds the door — exit line 9), 16-4 (`maos uninstall` — 20-2 AC3), 17-3b (WASM fixture Spirit `examples/example-spirit-ts` + admission by form/tier — exit lines 7–8, 20-1 AC4); hotfix-lane rows `provider-model-env-overrides` (`MAOS_DEFAULT_PROVIDER`) and `model-pin-currency-gate-and-retired-pin-sweep` (`xtask/model-currency.toml`, `check-model-currency`) before 20-4. Order: 20-1 → 20-2 → 20-3 → 20-5; 20-4 parallel. Downstream: Epic 21 depends on 20-2 and 20-3 (`epic-21…md`).
