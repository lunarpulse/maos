# Epic 16 — One Daemon, One Door (J0 for real) (W1)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 16 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (18 of 18 applied) and the decisions D-A (who binds the door, ADR-062) and D-D (one replay selector, ADR-064) are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`. Every citation below is `@9f920180` unless stated.

**Wave / duration:** 3.5–5.5 weeks (measured: scout note §G, 16.5–26 working days serial incl. review-net rounds) · **Makes true:** v0.1 · J0 evaluator — the operator controls a running Spirit and can watch a halt resolve

**Closes:** S1 (control plane never reaches the daemon); FR2, FR9, FR12, FR13, FR15, FR16, FR20 (door), FR24, FR50, FR51, FR58; D3/D5/D7/D12/D18

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green):**

Lines 1–3 are the J0 scene. They are executed by `cargo test -p maos-journey-test --test journey_j0` (PTY harness `crates/maos-journey-test/tests/journey_j0.rs:64` `Pty::spawn`, `wait_for_screen` `:68-72`), which spawns line 1's `maos shell` under the replay selector, types `@hello-spirit refactor src/main.rs to be more idiomatic` into the REPL (hello-spirit answers `[HALT task.acceptance_criterion.ambiguous]`, tag literal `crates/maos-spirit-hello/src/lib.rs:148`, printed at `crates/maos-shell/src/lib.rs:272`) and runs line 3's `maosctl` verbs from the harness against the shell process's door; line 2 of the block **is** that job — the REPL turn is never a line of the block. Lines 5–7 start only after the shell process has exited (D-A: one endpoint per `MAOS_HOME`; a second root binding it fails fast, 16-1 AC2) and are executed by `cargo test -p maos-bin --test one_daemon_one_door_16_1` (16-1 AC1) and `--test maos_uninstall_16_4` (16-4 AC1). `check-exit-commands` (15-3 AC3) resolves only `maos` / `maosctl` / `cargo run -p xtask --` tokens and skips env-var prefixes, `&&`, a trailing `&`, `RUN_PID=$!`, `kill` and `#` comments.

```
maos init && MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j0/shell-intro.json maos shell   # 1 — init mints control.json (16-1 AC2); the shell binds the door and serves the REPL
cargo test -p maos-journey-test --test journey_j0                             # 2 — hermetic J0 scene: types `@hello-spirit refactor src/main.rs to be more idiomatic` → [HALT task.acceptance_criterion.ambiguous], then runs line 3
maosctl halt list --spirit hello-spirit && maosctl halt resolve "<halt_id>" --spirit hello-spirit --kind provided-context --text "idiomatic = clippy-clean, no unwrap"   # 3 — operator form; the harness substitutes "<halt_id>" from `halt list`. REPAIRED by 15-3 F15/T12b: the placeholder is QUOTED. Unquoted, bash read `<halt_id> --spirit` as two REDIRECTIONS — measured against a stub, argv came out `[halt] [resolve] [hello-spirit] [--kind] [provided-context] [--text] […]` with `--spirit` GONE, and stdout was redirected into a file literally named `--spirit`, while `bash -n` stayed clean throughout
maos audit query --spirit hello-spirit                                         # 4 — the EpistemicHalt row and its resolution row
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json maos run spirits/butler/manifest.toml & RUN_PID=$!   # 5 — after the shell has exited: this root binds the door named in control.json
maosctl pause butler && maosctl spirit inspect butler && kill "$RUN_PID"      # 6 — inspect reports `paused` (ScbLifecycleState::Paused via GET /v1/spirits/butler); daemon journal has the pause row
maos uninstall --keep-log                                                      # 7 — MAOS_HOME + XDG data root removed; audit/transparency.sqlite kept; `maos init` afterwards is clean
```

**Exit-block provenance (dry-parsed at `9f920180`; `bash -n` clean):**
- Line 1 — `maos init` exists (`crates/maos-bin/src/main.rs:1679` → `maos_shell::run_init` `crates/maos-shell/src/lib.rs:46`); `maos shell` exists (`main.rs:1730`, `run_shell` called at `:3249`); `MAOS_INFERENCE_MODE` created by 15-6 AC1 (`grep -rn MAOS_INFERENCE_MODE crates xtask docs` empty at HEAD); `MAOS_REPLAY_CASSETTE` exists (`crates/maos-bin/src/cassette_replay.rs`, harness `crates/maos-journey-test/src/lib.rs:118-121`); cassette path exists (`ls crates/maos-journey-test/cassettes/j0/shell-intro.json`). **red:** the selector is unread at HEAD and `maos init` writes no `control.json` (`run_init` writes `config.toml` with `[slots]`/`[retention]` only, `maos-shell/src/lib.rs:60,313`; created by 16-1 AC2).
- Line 2 — `cargo test -p maos-journey-test --test journey_j0` exists (`crates/maos-journey-test/tests/journey_j0.rs`, two tests). **green-but-vacuous at HEAD:** asserts banner strings only (`journey_j0.rs:28,55`); 16-2 AC3 adds the halt / resolution / audit-row asserts, red until 16-1 + 16-2 land.
- Line 3 — `maosctl halt list --spirit` exists (`crates/maos-cli/src/cli.rs:700-710`, `HaltOp::List { --spirit, --limit }`); `maosctl halt resolve <halt_id> --spirit <s> --kind provided-context --text "…"` exists (`cli.rs:711-727`, `ResolutionKindChoice` `:731-738`; `--spirit` required, `--text` required for `provided-context`). **red:** `halt-resolve` spawns a fresh `maos` and seeds a synthetic halt into a new `HaltRegistry` (`main.rs:5568-5586`, `subcommands.rs:1590-1592`), so it never resolves the shell's halt; `halt list` reads the TL, which at HEAD carries a cap-audit row, not an `EpistemicHalt` row (`maos-shell/src/lib.rs:271-282`) — fixed by 16-1 AC3 + 16-2 AC2.
- Line 4 — `maos audit query --spirit` exists (`main.rs:1691-1721` → `maos_shell::run_audit_query` `maos-shell/src/lib.rs:123`). **red:** no halt / resolution rows for hello-spirit at HEAD (16-2 AC1).
- Line 5 — `maos run <manifest>` exists (`crates/maos-bin/src/worker_spawn.rs:44-82`; flags `--live`, `--once`; `--replay-llm` no-op retired by ADR-064); butler is a loadable `rust-inproc` class (`spirits/butler/manifest.toml:10,15`, constructed `main.rs:447`) served until SIGTERM (`main.rs:4020-4031`); cassette exists (`ls crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json`). **red:** the door is env-gated (`MAOS_OPERATOR_HTTP_BIND`, `main.rs:2669-2687`, bind at `:2792`) and GET-only; default bind from `control.json` created by 16-1 AC2.
- Line 6 — `maosctl pause <spirit>` exists (`cli.rs:769-772`); `maosctl spirit inspect <spirit>` exists (`cli.rs:853-860`). **red:** `pause` runs as a one-shot in a fresh process and pauses nothing running (`subcommands.rs:1623`); `spirit inspect` without `--sandbox` prints "requires --sandbox at v0.3-β" and exits **0** (`subcommands.rs:1881-1885`), and no lifecycle-state route exists (`crates/maos-control/src/lib.rs:428` serves only `/sandbox`) — route created by 16-1 AC1, re-target by 16-1 AC3.
- Line 7 — `maos uninstall` does not exist at HEAD: the hand parser's arms are `init|audit|traceback|shell` (`main.rs:1679,1691,1729,1730`) and `Some(_)` falls through to the daemon path (`:1738`). **created by 16-4 AC1**; **red:** unknown verb boots the daemon.

✅ **Line 3 was knowingly broken and is now REPAIRED by Story 15-3 (F15/T12b, 2026-09-07).** bash parsed `<halt_id>` as **two redirections**, not a placeholder: run verbatim against a stub the line created a file named `--spirit` in the working directory, redirected stdout INTO it, and **dropped the `--spirit` flag the command requires** — the file's own contents were `ARGV: [halt] [resolve] [hello-spirit] [--kind] [provided-context] [--text] [idiomatic = clippy-clean, no unwrap]`, with `--spirit` gone and `hello-spirit` slid into the positional slot the halt id should occupy. `bash -n` was clean throughout, which is exactly why rule 1's dry-parse leg blessed it and `epic-15-21-preflight-r2-2026-09-05.md:36` could only catch it in prose (*"does not parse, real shape `--spirit <s> --kind provided-context --text`"*) — filed, never applied. **The repair is to QUOTE the placeholder** (`"<halt_id>"`), which keeps the documented positional shape, makes bash pass it as a literal argument (verified: the real `maosctl` reports `halt resolved <halt_id> (provided_context)`), and creates no stray file. `check-exit-commands` now enforces this as a first-class `PlaceholderHazard` finding: an unquoted `<name>` reds, a quoted one passes, and both directions are pinned in `xtask/tests/check_exit_commands_gate.rs`. 15-3 captured the gate's pre-repair red on this line before repairing it — that transcript is the story's only proof the gate was not green from birth.

**Operator-lane legs (never in the exit):**
- `ops-live-key-j0-leg` (row in `sprint-status.yaml` `# === OPERATOR LANE ===`): runbook transcript of exit lines 1–3 under `MAOS_INFERENCE_MODE=live` with a provider key, plus the NFR-Onb-2 clean-VM 5-minute leg (the repo already demoted that wall-clock to advisory, `tests/integration/onb_nfr2_timing.sh:11-30`); never in CI.

**Kernel-Δ:** ZERO kernel-Δ for 16-1..16-4 — the kernel APIs are already `pub`: `PolicyTable::shift_posture` (`crates/maos-kernel-core/src/capability/cap_policy/mod.rs:233`), `OrchestratorBuffer::enqueue` (`orchestrator/buffer.rs:46`), `SpiritSchedulerAdapter::pause/resume` (`scheduler/scheduler_loop.rs:401/428`, both **`async`**), `resolve_pid` (`:178`), `load` (`:243`), `HaltRegistry::insert_pending_with_metadata/resolve` + `invoke_halt` (`halt/mod.rs:193,243,504`); 16-3's Worker SCB and task record are a maos-bin shim over `load`, not a kernel API. **16-5 is FLAG-Winston** (+5–25 kernel lines per the 15-2 D-F ledger, measured +3–25; re-pin `xtask/kernel-core-baseline.toml:472` 24472→N in its own commit, last in the epic). This epic holds the shared-write lock on `crates/maos-bin/src/main.rs` first (`sprint-change-proposal-2026-09-04-round2.md:33`).

**Kloc asks (15-2 AC1 ledger; `cargo run -p xtask -- kloc-check` @9f920180 EXIT=1, scout note §E):** maos-bin **+480–820** (16-1 +300–500 · 16-3 +40–80 · 16-4 +100–200 · 16-2 wiring ~+20 · 16-5 call sites ~+20; drawn from the lane-wide maos-bin grant) · maos-cli **+400–700** (against 15-2's **+1100**, shared with 20-1's +250–400: 21 verb dispatchers become HTTP calls, one hand-rolled GET is ~40 lines `subcommands.rs:1938-1960`; 9-7 added +911, 13-5b +144) · maos-control **+400–600** (within the existing 697 headroom, 803/1500 — `no change` row) · maos-shell **~+100–150** (16-2 +30–60 + `control.json` minting in `run_init` ~+30 + the `[operator]` read; 15-2 +100 is tight) · maos-secrets **+150–300** (within 839 headroom) · maos-journey-test **+50–120** (16-2 AC3; 15-2's new +400 row) · maos-domain **~+40–90** (16-4 `SecretStore` port +30 · 16-5 `CapError` +3, `IacBusError` +10–20; from 15-2's +300) · maos-providers **+30–60** (injectable key source; within 748 headroom) · maos-a2a-core **+20–40** (after 15-2's +150) · maos-kernel-core **+5–25** (16-5 only, from the D-F +250) · xtask **net 0** (the epic's first CI job is a plain discipline job — `cargo test` — not an xtask gate; 16-3's corpora live in crate `tests/`).

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`; planning-time leg: the provenance list above) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆ (16-1 and 16-5).

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `16-1-daemon-post-surface-and-verb-retarget` | Daemon POST surface and verb re-target ◆ | S1 · kernel-Δ 0 · maos-bin +300–500, maos-control +400–600, maos-cli +400–700, maos-shell ~+30 |
| `16-2-shell-halt-registry-and-j0-scene` | Shell halt registry and the J0 scene | J0 · FR15/FR58 · kernel-Δ 0 · maos-shell +30–60, maos-journey-test +50–120 |
| `16-3-subprocess-crash-to-handle-crash` | Subprocess crash reaches the crash handler | FR12/FR50 · kernel-Δ 0 (`FaultCause::SignaledByKernel` reused; no `CrashCause` variant) · maos-bin +40–80 |
| `16-4-maos-uninstall-and-keyring` | `maos uninstall` and the keyring backend | FR2 · kernel-Δ 0 · maos-secrets +150–300, maos-bin +100–200, maos-providers +30–60, maos-domain +30 |
| `16-5-audit-drop-legal-hold-and-a2a-deny-vocabulary` | Debt slot: audit drop, legal hold, A2A deny vocabulary ◆ FLAG-Winston | D3/D5/D7/D12/D18 · kernel-Δ +5–25 (measured +3–25) · maos-a2a-core +20–40, maos-domain +3–25 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 16-1-daemon-post-surface-and-verb-retarget — Daemon POST surface and verb re-target ◆

*Closes · Δ:* S1 · kernel-Δ 0 · maos-bin +300–500, maos-control +400–600, maos-cli +400–700, maos-shell ~+30. ◆ = §A6 review net non-degradable: this is the "second trust path" the prior preflight §3 and `crates/maos-control/src/lib.rs:68-76` name; the review net is the binding control (frontier allowlist rule).

- **AC1 (command).** With `maos run spirits/butler/manifest.toml` running and the door bound per AC2, `maosctl pause butler` (`crates/maos-cli/src/cli.rs:769-772`) makes `maosctl spirit inspect butler` (no `--sandbox`; today it exits 0 with "requires --sandbox at v0.3-β", `crates/maos-cli/src/subcommands.rs:1881-1885`) report `ScbLifecycleState::Paused` (`crates/maos-kernel-core/src/scheduler/control_block.rs:31`) from a new `GET /v1/spirits/{id}` route (`crates/maos-control/src/lib.rs:428` today serves only `/v1/spirits/{id}/sandbox`; `--sandbox` becomes optional and keeps that report), and the daemon journal carries the pause; `maosctl posture butler --shift cautious` (`cli.rs:676-682`, `PostureChoice` `:685-691`) changes the next capability decision of the running Spirit through `PolicyTable::shift_posture` (`cap_policy/mod.rs:233`) after `resolve_pid("butler")` (`scheduler_loop.rs:178`). The CI job for exit lines 5–6, `cargo test -p maos-bin --test one_daemon_one_door_16_1`, starts the root, waits (bounded) for the endpoint in `control.json` to accept a connection, then issues the verbs — the block carries no `sleep`.
- **AC2 (design — D-A, ADR-062).** The `maos run` / `maos shell` daemon process binds the loopback POST surface (`maos-control`, bearer required): `maos-control` gains POST + a body parser + typed errors (today `handle_connection` `lib.rs:265` reads to `\r\n\r\n` and matches exact request lines `:274-300`; the 4 GET routes are `lib.rs:299,347,392,428`; the READ-ONLY design note `lib.rs:68-76` is superseded by ADR-062 (15-5 AC1); the three `POST → 404` tests `lib.rs:607-611,786,927` are inverted under ADR-062). `maos init` mints the bearer and writes `MAOS_HOME/control.json` `{endpoint, token}` (today `run_init` writes `config.toml` with `[slots]`/`[retention]` only, `crates/maos-shell/src/lib.rs:60`, `default_config_toml` `:313`; the daemon reads the token from env only, `main.rs:2669`, required iff `MAOS_OPERATOR_HTTP_BIND` `:2687`, bind executed at `:2792`). Every composition root (`maos shell` `main.rs:3249`, `maos run` `:4020-4031`, `cohort-a2a-daemon`) binds the endpoint named in `control.json` by default; a second root binding the same endpoint fails fast with a typed `OperatorHttpError::EndpointInUse` (never a silent second daemon, never a fallback port); the daemon records its `pid` and `boot_nonce` in `control.json` at bind so `maosctl` can tell "no daemon" from "stale file". Every `maosctl` verb discovers the daemon from that file; the env overrides `MAOS_OPERATOR_HTTP_ENDPOINT` / `MAOS_OPERATOR_BEARER_TOKEN` are kept (`subcommands.rs:1938-1940`; registered by 15-1 AC5). `maos-cli` gains a hand-rolled HTTP client (one GET exists, `subcommands.rs:1938-1960`) with **no `maos-control` dependency** — it links kernel-core (`crates/maos-control/Cargo.toml:11`) and `maos-cli` may not (`crates/maos-cli/tests/dep_kernel_core_free_test.rs:8-24`); no new workspace crate (`check-workspace-count` is hard equality, `xtask/src/check_workspace_count.rs:57`). Async runtime: `pause/resume` are `async fn` (`scheduler_loop.rs:401/428`) and the server is a sync `std::thread` with no tokio dependency (`lib.rs:222`, `maos-control/Cargo.toml`), so `OperatorHttpServer::bind` takes a `tokio::runtime::Handle` from the root and POST handlers `block_on` the scheduler calls; the daemon-held `Arc`s (`policy` `main.rs:1862`, `halt_registry` `:1985`, `orchestrator_registry` `:1994`, `scheduler` `:2644`, `crash_detector` `:2717`, `capability` with the ring inside `capability/mod.rs:119`) are all in scope at the bind site `:2792-2798`, which already receives `Arc::clone(&scheduler)`.
- **AC3.** The **18** `MAOS_ONE_SHOT` spawn sites in `maos-cli` (`grep -c 'MAOS_ONE_SHOT' crates/maos-cli/src/subcommands.rs` = 21 incl. 3 doc lines; 16 literal `env("MAOS_ONE_SHOT","…")` sites `:557,1480,1488,1510,1536,1552,1592,1623,1641,1668,1686,1715,1741,1756,1793,1842` + `:1461` for `start/stop/unload/uninstall` via `lifecycle_verb` `:28-31` + `:1164` for `run <spirit>` — 21 verb values) are split: state-mutating verbs {pause, resume, posture-shift, halt-resolve, orchestrator-queue, revoke-token, revocations-import, spirit-upgrade, forget, legal-hold-release, governance-admit, start, stop, unload, uninstall, run} go to the surface; TL-only readers {halt-list, revocations-list, orchestrator-status, legal-hold-list, hot-swap-precheck} run in-process in `maosctl` with no child spawn. `MAOS_ONE_SHOT` survives only for `smoke-*`, `acp-server`, `registry-server`, `collective-erase`, `cohort-a2a-daemon` (known-modes message `main.rs:7993`; `discipline.yml:1541-2267`); `backup`/`import` never used it (`dispatch_backup` `subcommands.rs:283`, `dispatch_import` `:942`) and `init` is a `maos` verb (`main.rs:1679`). The synthetic-halt seeding in `halt-resolve` (`main.rs:5568-5586`, "integration tests seed halt IDs via env") is deleted.
- **AC4.** `maosctl revoke-token` mutates the daemon-held `CapTokensShardRing` (`crates/maos-capability/src/cap_tokens/mod.rs:111`, held as `tokens: Arc<CapTokensShardRing>` in `CapabilityRegistryAdapter` `crates/maos-kernel-core/src/capability/mod.rs:119`, built `:154`, mutated by `revoke(token_id)` `:327`); `maosctl revocations import` reports `matched > 0` against a running Spirit (today the one-shot arm `main.rs:6607-6643` counts against a fresh process's ring); `maosctl orchestrator queue` enqueues into the daemon registry (`crates/maos-kernel-core/src/orchestrator/registry.rs:32` `get_or_create` → `OrchestratorBuffer::enqueue` `buffer.rs:46`).
- **AC5.** Proven-red: with the daemon down (no listener at the `control.json` endpoint, or no `control.json`) every re-targeted verb exits non-zero with a typed error, never journal-and-succeed; a stale `control.json` (dead `pid`) and a wrong bearer (401) are separate negatives.
- **AC6.** The re-targeting updates: `crates/maos-bin/tests/one_shot_hello.rs:15` and `erasure_uninstall_13_5b.rs:69,767,782` (the 7 `MAOS_ONE_SHOT` hits in `cohort_daemon_smoke_13_5c.rs` are `cohort-a2a-daemon` and survive); `crates/maos-cli/tests/posture_shift_test.rs` (3 real-chain tests, `:49,69,86`); the nine fake-`maos` contract test files (`grep -l MAOS_BIN_PATH crates/maos-cli/tests/*.rs` = 9: `halt_resolve_test` 8, `orchestrator_queue_test` 7, `pause_resume_test` 6, `revocations_import_test` 5, `revoke_token_test` 4, `spirit_upgrade_test` 4, `legal_hold_cli_13_5b` 1, `uninstall_exit_codes_13_5b` 1, `accessibility_test` partly — ≈36 of the 42 `#[test]` fns in those files, `grep -c '#\[test\]'`) rewritten against a fixture HTTP daemon; CI scripts `tests/integration/v01_evaluator_path.sh` (`discipline.yml:665`) and `tests/integration/maosctl_smoke.sh` (`:630`); non-CI scripts `tests/integration/halt_resolve_smoke.sh`, `orchestrator_queue_smoke.sh`, `maosctl_hot_swap_precheck.sh` retired or updated. `cohort_daemon_smoke_13_5c` `SCANNED_SOURCE_FILES` (`crates/maos-bin/tests/cohort_daemon_smoke_13_5c.rs:863`, 17 == `ls crates/maos-bin/src/*.rs`, equality assert `:937-941`) gains every new maos-bin module.

### 16-2-shell-halt-registry-and-j0-scene — Shell halt registry and the J0 scene

*Closes · Δ:* J0 · FR15/FR58 · kernel-Δ 0 · maos-shell +30–60, maos-journey-test +50–120

- **AC1 (command).** Lines 1–3 of the exit block pass in `journey_j0` on a hosted runner and `maos audit query --spirit hello-spirit` (`main.rs:1691-1721` → `run_audit_query` `crates/maos-shell/src/lib.rs:123`, sqlite at `:128`) shows the halt row and its resolution row; the response-path budget of `tests/integration/onb_nfr2_timing.sh` (`discipline.yml:1069`) stays blocking and the clean-VM 5-minute leg (NFR-Onb-2) is the runbook's operator step under `ops-live-key-j0-leg` (the repo already demoted that wall-clock to advisory, `onb_nfr2_timing.sh:11-30`).
- **AC2.** `maos_shell::run_shell` (today `run_shell(inference, capability: Arc<CapabilityRegistryAdapter>, color_choice, default_provider)`, `crates/maos-shell/src/lib.rs:165-170`; maos-shell already links kernel-core, `crates/maos-shell/Cargo.toml:15`) gains `Arc<HaltRegistry>`, `Arc<TransparencyLogAdapter>` and `Arc<JournalAdapter>`. The `Err(HelloError::Ambiguous{..})` return (`crates/maos-spirit-hello/src/lib.rs:182`; detector `is_ambiguous` `:127-152`; variant `:35`) is turned by the shell into `invoke_halt(tl, journal, registry, payload, pid, "hello-spirit", boot_nonce)` (`crates/maos-kernel-core/src/halt/mod.rs:504-512`), which inserts `HaltState::PendingResolution` (`crates/maos-domain/src/halt.rs:317`) and writes the `FrameKind::EpistemicHalt` row (`halt/mod.rs:523-529`, payload `crates/maos-domain/src/frame.rs:67`) — today the shell prints `[HALT …]` and writes a cap-audit row only (`maos-shell/src/lib.rs:271-282`). One path, one frame kind: hello-spirit is loaded through the scheduler in shell mode so the halt carries its real pid (today admitted "via canonical path" without `scheduler.load`, `main.rs:3233-3246`). The shell process serves the 16-1 door, so `maosctl halt resolve` reaches **this** registry (`HaltRegistry::resolve` `halt/mod.rs:243`; today it spawns a fresh `maos`, `subcommands.rs:1590-1592`).
- **AC3.** `journey_j0` (`crates/maos-journey-test/tests/journey_j0.rs`) drives the REPL through the PTY harness (`Pty::spawn` `:64`, `wait_for_screen` `:68-72`), sets `MAOS_INFERENCE_MODE=replay` on the child beside the harness's `MAOS_REPLAY_CASSETTE` (`crates/maos-journey-test/src/lib.rs:118-121`, ADR-064), types line 2's directive, runs line 3's `maosctl halt list/resolve` against the shell's `control.json` endpoint and asserts the halt, resolution and audit rows through `AuditDb` (today two tests, banner strings only: `j0_init_creates_config` `:28`, `j0_shell_banner_via_pty` `:55`).
- **AC4.** `docs/maos.dev/run-maos.md` (26 lines; no env var or key mention today) documents `MAOS_INFERENCE_MODE={record,replay,live}` + `MAOS_REPLAY_CASSETTE` (D-D / ADR-064) and where the provider key comes from (16-4 AC2).

### 16-3-subprocess-crash-to-handle-crash — Subprocess crash reaches the crash handler

*Closes · Δ:* FR12/FR50 · kernel-Δ 0 — `ExitCause::Signaled{signal}` (`crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:117`) maps to the existing `FaultCause::SignaledByKernel{signal, stderr_tail}` (`crates/maos-domain/src/supervision.rs:99-102`; `CrashCause` is `maos-domain/src/supervision.rs:47`, not kernel — no variant is added) · maos-bin +40–80

- **AC1 (command).** `kill -9` on a fixture Worker (a `sh` fixture at tier T1/none — precedent `crates/maos-kernel-core/tests/cli_wrapper_bridge_8_12.rs:132-136`, `sh -c 'kill -9 $$'` → `ExitCause::Signaled{9}`; no podman/bwrap) yields `task.orphaned` within 2 s; on a 100-SIGKILL corpus ≥99/100, asserted by `nfr-rel-1-crash-detection-2s` (`.github/workflows/discipline.yml:1161-1172`; today `crash_detector_in_process_panic --release` asserts ≥1 receipt, `crates/maos-kernel-core/tests/crash_detector_in_process_panic.rs:119-123`). The corpus runs `--release` with per-kill budgets and asserts counts, not wall-clock per kill; flake policy: one automatic re-run, then red.
- **AC2.** A 50-hang corpus yields `task.stalled` within 60 s for ≥48/50, asserted by `nfr-rel-2-hang-detection-60s` (`discipline.yml:1175-1186`; today `progress_watchdog_smoke` asserts ≥1, `crates/maos-kernel-core/tests/progress_watchdog_smoke.rs:119-122`). Both jobs stay in `aggregate.needs` (`discipline.yml:3571-3572`).
- **AC3.** Workers get an SCB at spawn by loading a maos-bin `WorkerSpirit` shim through `SpiritSchedulerAdapter::load` (`crates/maos-kernel-core/src/scheduler/scheduler_loop.rs:243`), and a `TaskAssignmentRecord` is pushed to `scb.task_assignments_in_flight` (`crates/maos-kernel-core/src/scheduler/control_block.rs:277`; today's only writers are the smoke arm `main.rs:6399,6452`) — because `handle_crash` returns `HandleCrashError::NotLoaded` without an SCB (`crates/maos-kernel-core/src/supervision/crash_detector.rs:95-100`) and emits `task.orphaned` only per record (`:141-150`). `wait_and_finalize` (`runtime.rs:632`, signature `:638-644`; caller `crates/maos-bin/src/worker_spawn.rs:732` inside `run_cli_wrapper_manifest` `:384`, synchronous per `delegation.rs:666`) receives the real pid instead of `0` and feeds `CrashDetector::handle_crash` (sole caller today `scheduler_loop.rs:583`, the `HookOutcome::Panicked` arm) with `CrashCause::Fault(FaultCause::SignaledByKernel{..})`; `on_crash.action` applied with reassign→escalate journaled. The same ledger makes `ProgressWatchdog` (`crates/maos-kernel-core/src/supervision/progress_watchdog.rs:73,109`) see Workers for AC2. kernel-Δ 0: the shim, the record push and the pid all live in maos-bin.

### 16-4-maos-uninstall-and-keyring — `maos uninstall` and the keyring backend

*Closes · Δ:* FR2 · kernel-Δ 0 · maos-secrets +150–300, maos-bin +100–200 (`maos uninstall` verb), maos-providers +30–60, maos-domain +30 (`SecretStore` port, under 15-2)

- **AC1 (command).** `maos uninstall` (new top-level `maos` verb — today the hand parser's arms are `init|audit|traceback|shell`, `main.rs:1679,1691,1729,1730`, `Some(_)` falls through to the daemon `:1738`, and `uninstall` exists only as `maosctl uninstall <spirit>`, one-shot arm `main.rs:5152`) removes `$MAOS_HOME` (`config.toml`, `control.json`, `audit/transparency.sqlite`, `journal/lifecycle.ndjson`, `skills/`, `logs/`; `crates/maos-shell/src/lib.rs:81-90`), the XDG data root `$XDG_DATA_HOME/maos` (`audit/`, `journal/`, `memory/`, `spirit-archives/`, `erasure-proofs/`; `crates/maos-audit/src/lib.rs:865-872,1405-1411,1452,1555-1575,1578-1582` — the archive dir is never under `MAOS_HOME`: `MAOS_ARCHIVE_DIR` else `$XDG_DATA_HOME/maos/spirit-archives`, `:1555-1575`), `$HOME/.maos` when `MAOS_HOME` is unset (`maos-shell/src/lib.rs:299-311`), and the binary only when it was installed under a prefix recorded at `maos init`; the CI leg `cargo test -p maos-bin --test maos_uninstall_16_4` installs to a temp prefix and removes that, never `target/`. `--keep-log` retains `audit/transparency.sqlite`; `maos init` afterwards is clean.
- **AC2.** `maos-secrets` (today one type, `LocalMasterKeyKms` implementing `KeyManagementPort`, `crates/maos-secrets/src/lib.rs:20,33`) gains an OS-keyring backend: `keyring` (not yet a dependency — `grep 'name = "keyring"' Cargo.lock` empty; MIT OR Apache-2.0 within `deny.toml:35-51`) with Linux backend **`linux-native`** (keyutils, no daemon) plus `apple-native`/`windows-native`, and a **`file`** backend selected by `MAOS_SECRETS_BACKEND=file` for CI (hosted runners run no Secret Service D-Bus daemon; `MAOS_SECRETS_BACKEND` has zero readers today); `[bans] multiple-versions = "deny"` (`deny.toml:55`) is checked in the same commit as the lockfile. A `SecretStore` port (maos-domain +~30 under the 15-2 grant; today only `KeyManagementPort`, `crates/maos-domain/src/ports/key_management.rs:21`) is injected into `AnthropicProvider::new` / `OpenAiProvider::new`, which read `MAOS_*_API_KEY` directly today (`crates/maos-providers/src/anthropic.rs:37`, `openai.rs:7`); provider keys are read keyring-first, env fallback journaled as a downgrade; `FIXME(secrets)` at `main.rs:3079-3080` retired.
- **AC3.** `MAOS_SECRETS_BACKEND` registered in `crates/maos-bin/src/env_contract.rs` (the `check_env_contract` scan covers maos-bin `src` only, `xtask/src/check_env_contract.rs:121`); `maosctl uninstall <spirit>` (FR65) keeps its semantics over the 16-1 surface (it shares `lifecycle_verb`, `subcommands.rs:31`; the cascade at `main.rs:5160` needs the live registry).

### 16-5-audit-drop-legal-hold-and-a2a-deny-vocabulary — Debt slot: audit drop, legal hold, A2A deny vocabulary ◆ FLAG-Winston

*Closes · Δ:* D3/D5/D7/D12/D18 · kernel-Δ +5–25 (15-2 D-F ledger; measured +3–25: `capability/mod.rs:349-351` +1–3, memory serialization +10–20 incl. the exemption attribute; D3 measured "+1 kernel-core, +3 maos-domain" for the audit-drop half, `epic-14-preflight-decisions.md:173`) · maos-a2a-core +20–40 · maos-domain +3–25 (`CapError` +3, `IacBusError` +10–20; covered by 15-2)

- **AC1 (command).** A fault-injected audit sink makes `record_invocation` (`crates/maos-kernel-core/src/capability/mod.rs:332-352`; today `cap_audit::record_drop()` at `:349` then `Ok(())` at `:351`) return `Err` and the invocation is refused; new `CapError` variant in `maos-domain` (`crates/maos-domain/src/ports/capability.rs:113`). The two production callers that swallow the result — `crates/maos-shell/src/lib.rs:265,278`, `let _ =` — propagate it (`grep -rn '\.record_invocation(' crates/*/src` finds no other production caller). The verify path at `capability/mod.rs:236-245` returns `()` and is **left as-is**: its `record_drop()` at `:243` stays a counter; re-typing the verify signature is outside D3 and outside the grant.
- **AC2.** The legal-hold check-then-act in `forget_with_reason` (`crates/maos-kernel-core/src/memory/mod.rs:424`; check `:481-485` `is_under_legal_hold`, act `:530-531` `scrub_distillate_body`) is serialized under one lock — an `#[i9_exempt]`-documented field on `MemoryManagerAdapter` (`memory/mod.rs:79`) or a TL-level transaction, since `Mutex` is I9-denylisted (`xtask/i9-denylist.toml:12`); under hold the outcome **stays** `ForgetOutcome::Suspended{hold}` (`:475,:505`; `crates/maos-domain/src/memory.rs:423`, no `MemoryError` variant) and the erasure proof carries `completed:false` — the corpus contract (`crates/maos-audit/src/bin/gen_gdpr_cascade.rs:122-126`, `legal_hold → held`) and `run_uninstall_cascade` (`main.rs:5160`) are unchanged.
- **AC3.** `map_a2a_error_to_iac_bus` (`crates/maos-a2a-core/src/router.rs:1933`) preserves the deny vocabulary: the five "no new `IacBusError` variant" notes at `router.rs:1979-1983, 2011-2013, 2031-2033, 2037-2043, 2048-2051` are retired with typed variants `CrossHostConsentDenied{reason, direction}`, `CrossHostTeamIdentityRefused`, `CrossHostConsentUnclassified{reason}` on `IacBusError` (`crates/maos-domain/src/iac_bus_types.rs:14`; today the only typed deny is `CrossHostIntentDenied` `:36`), replacing the `CrossHostRouteFailure(String)` collapse at `:2015-2019, 2021-2030, 2034-2036, 2044-2047, 2052-2061`; `CrossWallRecallRefusal` (`crates/maos-domain/src/log_recall.rs:291`) variants surface typed (D7). The note at `main.rs:9805-9815` concerns `TcpA2ATransport` pins, not deny vocabulary, and is not touched.
- **AC4.** The kernel re-pin (`xtask/kernel-core-baseline.toml:472` `src_lines = 24472` → N, counted after 15-1's +2) and the measured grant on `xtask/kloc.toml:195` (`maos-kernel-core = 18933`) land in the same commit; 16-5 lands last in the epic.

## Dependencies

Epic 15 (15-2 allowances incl. maos-cli **+1100**, maos-journey-test +400, maos-domain +300 and the lane-wide maos-bin grant; 15-5 ADR-062 (D-A) superseding `crates/maos-control/src/lib.rs:68-76` and ADR-064 (D-D); 15-6 as amended for the shell consumer at `main.rs:3247-3250`; 15-1 AC5 registering `MAOS_OPERATOR_BEARER_TOKEN`/`MAOS_OPERATOR_HTTP_BIND`). 16-1 before 16-2 and 16-4; 16-3 after 16-1; 16-5 last.
