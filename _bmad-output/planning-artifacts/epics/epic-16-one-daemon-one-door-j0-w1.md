# Epic 16 — One Daemon, One Door (J0 for real) (W1)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 16 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (18 of 18 applied) and the decisions D-A (who binds the door, ADR-062) and D-D (one replay selector, ADR-064) are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`. Every citation below was re-derived at HEAD `843d5365` unless stated.

**Round 4 applied (2026-09-12, confidence rule 9 — the Epic-15 retrospective convened as a refinement session):** this file was re-preflighted against the tree after all six Epic-15 stories landed, and its OLD→NEW edits are filed here before Epic 16's first story is created. **45 anchor groups moved.** `crates/maos-bin/src/main.rs` grew 14120 → 14347 lines across four Epic-15 stories, so every one of this file's 25 `main.rs` citations was stale; `maos-cli/src/cli.rs` +2, `maos-cli/src/subcommands.rs` +9 uniformly, `.github/workflows/discipline.yml` +35…+121. Citations into `maos-kernel-core`, `maos-control`, `maos-shell`, `maos-a2a-core`, `maos-domain`, `maos-journey-test` and `maos-spirit-hello` were verified and **hold exactly** — Epic 15 did not touch those files. Five premise-level changes are marked ⚠ **R4** below. Evidence: `implementation-artifacts/epic-15-retro-2026-09-12.md`.

**Round 5 applied to the 16-1 section only (2026-09-13, confidence rule 9 — mid-epic, because Story `16-1-daemon-post-surface-and-verb-retarget`'s creation disproved premises in 5 of its 6 ACs):** re-derived at HEAD `a6ed282c`; every `crates/maos-cli/src/subcommands.rs` cite in this file re-derived again at `f72b557b` (that commit inserted 16 lines at `same_file`, `:278`; each moved line content-verified with `git show`, and three cites that were already wrong at `a6ed282c` — `:1590-1592`, `:1938-1940`, `dispatch_backup :283`/`dispatch_import :942` — corrected to the spawn site, the env reads and the symbols). Five AC2 `main.rs` anchors were wrong **when Round 4 wrote them** — `main.rs` is byte-identical at `843d5365` and `a6ed282c`, so this is not drift — (bind `:2792`→`:2827`, +35; `scheduler`, `crash_detector`, `halt_registry`, `orchestrator_registry` +37), and AC6's doorbell cite was never refreshed. Premise-level changes are marked ⚠ **R5**; the full evidence, 15 decisions and the OLD→NEW table are in `implementation-artifacts/16-1-daemon-post-surface-and-verb-retarget.md` §1–§12. Also found, and owned by that story: `maos init` exits 1 on every clean home (exit line 1 of this block), masked by all init tests pre-creating the directory.

**Round 6 applied to the 16-2 section, the exit-block comments on lines 2 and 4, provenance Lines 2–4, the stories-table 16-2 row, the Kloc asks, the exit-block intro's `[HALT]` cite, one note in 16-4 AC2 and two in 16-5 AC1 (2026-09-14, confidence rule 9 — mid-epic, because Story `16-2-shell-halt-registry-and-j0-scene`'s creation disproved the premises behind all three J0 exit lines):** re-derived at HEAD `bb9f0657` (Story 16-1 landed). 14 of the 16-2 section's 24 line cites were stale and two described code 16-1 deleted. Measured on rebuilt binaries: `maos audit query --spirit hello-spirit` **exits 1** (`Fr4SchemaViolation`) on the first tokenless row, and the halt resolution is journaled to `approval_decision_log`, a table no audit read renders; `maosctl halt list` prints **no `halt_id`**; hello-spirit has **no `impl Spirit`**, is never scheduler-loaded, so the door answers `spirit_not_loaded`; the PTY harness **cannot type**; `j0_shell_banner_via_pty` **passes when the shell fails to start**; and the PRD's J0 (`prd/user-journeys.md:293`) has the evaluator *type a clarification* after which *the Spirit proceeds* — a beat neither this file nor the tree had. Premise-level changes are marked ⚠ **R6**; the evidence, 14 decisions and the OLD→NEW table are in `implementation-artifacts/16-2-shell-halt-registry-and-j0-scene.md` §1–§12. Exit-block COMMANDS are byte-unchanged; only the comments on lines 2 and 4 moved. **The same day's round-table (story §15, operator-ratified: *"approved, route it to 16-3 as a new AC"*) added a NEW 16-3 AC4** (NFR-Rel-11's planned half — no `maos run` root unloads its Spirits at shutdown), a note in 16-4 AC1, and re-worded the 16-5 AC1 re-kind note from optional to decided-with-a-recorded-reason.

**Wave / duration:** 3.5–5.5 weeks (measured: scout note §G, 16.5–26 working days serial incl. review-net rounds) · **Makes true:** v0.1 · J0 evaluator — the operator controls a running Spirit and can watch a halt resolve

**Closes:** S1 (control plane never reaches the daemon); FR2, FR9 (start/pause/resume/unload by 16-1; `load` by 16-6), FR12, FR13, FR15, FR16, FR20 (door), FR24, FR50, FR51 (b), and the door reachability of FR51 (a)(d), FR58; ⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified): FR51(a)'s bounded interrupt of an IN-FLIGHT action and (d)'s bounded fail-safe of in-flight operations have **no mechanism in the tree** — `SpiritSchedulerAdapter::pause` flips SCB state and fires `on_pause`; a running hook keeps running — so they are NOT claimed, and are raised to the **Epic-16 retrospective** as a named refinement item; ⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): FR51(c) (*recalling pending Orchestrator-buffered actions per FR20*) is NOT closed here either — `OrchestratorBuffer::recall_all_pending` (`crates/maos-kernel-core/src/orchestrator/buffer.rs:72`) has no operator route and its only production caller re-enqueues at resume (`scheduler_loop.rs:452-460`) — routed by charter to **19-2** (the FR20 owner; a recall means something only against its safe-point dequeue), 19-2 AC3; D3/D5/D7/D12/D18

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green):**

Lines 1–3 are the J0 scene. They are executed by `cargo test -p maos-journey-test --test journey_j0` (PTY harness `crates/maos-journey-test/tests/journey_j0.rs:64` `Pty::spawn`, `wait_for_screen` `:68-72`), which spawns line 1's `maos shell` under the replay selector, types `@hello-spirit refactor src/main.rs to be more idiomatic` into the REPL (hello-spirit answers `[HALT task.acceptance_criterion.ambiguous]`, tag literal `crates/maos-spirit-hello/src/lib.rs:148`, printed by `maos_shell::run_shell`'s `HelloError::Ambiguous` arm — `crates/maos-shell/src/lib.rs:311` at `bb9f0657`, ⚠ R6 re-shaped by 16-2) and runs line 3's `maosctl` verbs from the harness — `halt resolve` over the shell process's door, `halt list` as an in-process read of the shell's Transparency Log (⚠ R5, Story 16-1 D-16-1-A: a durable TL reader, not a door verb); line 2 of the block **is** that job — the REPL turn is never a line of the block. Lines 5–7 start only after the shell process has exited (D-A: one endpoint per `MAOS_HOME`; a second root binding it fails fast, 16-1 AC2) and are executed by `cargo test -p maos-bin --test one_daemon_one_door_16_1` (16-1 AC1) and `--test maos_uninstall_16_4` (16-4 AC1). `check-exit-commands` (15-3 **AC5** — AC3 in the pre-refinement numbering) resolves only `maos` / `maosctl` / `cargo run -p xtask --` tokens and skips env-var prefixes, `&&`, a trailing `&`, `RUN_PID=$!`, `kill` and `#` comments.

```
maos init && MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j0/shell-intro.json maos shell   # 1 — init mints control.json (16-1 AC2); the shell binds the door and serves the REPL
cargo test -p maos-journey-test --test journey_j0                             # 2 — hermetic J0 scene: types `@hello-spirit refactor src/main.rs to be more idiomatic` → [HALT task.acceptance_criterion.ambiguous], then runs line 3, and waits for the REPL to render the resolution and the Spirit's proceeding turn (⚠ R6, 16-2 AC1)
maosctl halt list --spirit hello-spirit && maosctl halt resolve "<halt_id>" --spirit hello-spirit --kind provided-context --text "idiomatic = clippy-clean, no unwrap"   # 3 — operator form; the harness substitutes "<halt_id>" from `halt list`. REPAIRED by 15-3 F15/T12b: the placeholder is QUOTED. Unquoted, bash read `<halt_id> --spirit` as two REDIRECTIONS — measured against a stub, argv came out `[halt] [resolve] [hello-spirit] [--kind] [provided-context] [--text] […]` with `--spirit` GONE, and stdout was redirected into a file literally named `--spirit`, while `bash -n` stayed clean throughout
maos audit query --spirit hello-spirit                                         # 4 — the epistemic.halt row and the operator.halt-resolve.<op>:completed row (telemetry.event); the resolution's reasoning is in approval_decision_log (⚠ R6, 16-2 D-16-2-I/J)
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json maos run spirits/butler/manifest.toml & RUN_PID=$!   # 5 — after the shell has exited: this root binds the door named in control.json
maosctl pause butler && maosctl spirit inspect butler && kill "$RUN_PID"      # 6 — inspect reports `paused` (ScbLifecycleState::Paused via GET /v1/spirits/butler); daemon journal has the pause row
maos purge --keep-log                                                          # 7 — MAOS_HOME + XDG data root removed; audit/transparency.sqlite kept; `maos init` afterwards is clean. ⚠ R4: the verb is `purge`, NOT `uninstall` — see 16-4
```

**Exit-block provenance (⚠ R4 — re-dry-parsed at `843d5365` on 2026-09-12: `bash -n` CLEAN, and `cargo run -p xtask -- check-exit-commands` PASSES with `purge` correctly OWED to `16-4-maos-uninstall-and-keyring [backlog]`; all reds below re-measured):**
- Line 1 — `maos init` exists (`crates/maos-bin/src/main.rs:1708` → `maos_shell::run_init` `crates/maos-shell/src/lib.rs:46`); `maos shell` exists (`main.rs:1746`, `run_shell` called at `:3283` — ⚠ re-pinned 2026-09-09 by 15-6's preflight; the `:1730`/`:3249` cites were ~34 lines stale); `MAOS_INFERENCE_MODE` created by 15-6 AC1 ⚠ **NO LONGER RED — 15-6 SHIPPED IT**: `MAOS_INFERENCE_MODE` is read at `crates/maos-bin/src/main.rs:3182` through `crates/maos-bin/src/inference_mode.rs`; `MAOS_REPLAY_CASSETTE` exists (`crates/maos-bin/src/cassette_replay.rs`, harness `crates/maos-journey-test/src/lib.rs:118-121`); cassette path exists (`ls crates/maos-journey-test/cassettes/j0/shell-intro.json`). **red:** the selector is unread at HEAD and `maos init` writes no `control.json` (`run_init` writes `config.toml` with `[slots]`/`[retention]` only, `maos-shell/src/lib.rs:60,313`; created by 16-1 AC2).
- Line 2 — `cargo test -p maos-journey-test --test journey_j0` exists (`crates/maos-journey-test/tests/journey_j0.rs`, two tests). **green-but-vacuous at HEAD:** asserts banner strings only (`journey_j0.rs:28,55`); 16-2 AC3 adds the halt / resolution / audit-row asserts, red until 16-1 + 16-2 land. ⚠ **R6 (HEAD `bb9f0657`): worse than vacuous — a NULL CONTROL.** `j0_shell_banner_via_pty` waits for ANY of `["maos shell", "hello-spirit"]`, and `hello-spirit` is also printed by the admission line and the manifest-missing error: run with no `spirits/` directory the child exits 1 and the test reports `ok` (measured). And `Pty` has no input method, so no test can type line 2's directive. Fixed by 16-2 AC6.
- Line 3 — `maosctl halt list --spirit` exists (`crates/maos-cli/src/cli.rs:702-712`, `HaltOp::List { --spirit, --limit }`); `maosctl halt resolve <halt_id> --spirit <s> --kind provided-context --text "…"` exists (`cli.rs:713-729`, `ResolutionKindChoice` `:733-740`; `--spirit` required, `--text` required for `provided-context`). **red:** `halt-resolve` spawns a fresh `maos` and seeds a synthetic halt into a new `HaltRegistry` (`main.rs:5765-5783`, `subcommands.rs:1617`), so it never resolves the shell's halt; `halt list` reads the TL, which at HEAD carries a cap-audit row, not an `EpistemicHalt` row (`maos-shell/src/lib.rs:271-282`) — fixed by 16-1 AC3 + 16-2 AC2. ⚠ **R6 (HEAD `bb9f0657`): 16-1 already moved `halt resolve` to the door (`POST /v1/halts/{id}/resolve`) and deleted the synthetic halt; what stays red is different.** (a) The door answers `spirit_not_loaded` 404 (`OperatorDoor::resolve_halt_command` checks `scheduler.resolve_pid` first) because hello-spirit has no `impl Spirit` and shell mode only admits it at pid 0. (b) `halt list` prints `{frame_id[:8], timestamp_ns, kind, intent}` and **no `halt_id`** (`halt_list_reader`, `crates/maos-cli/src/subcommands.rs:1916`), so the harness has nothing to substitute for `"<halt_id>"`. Fixed by 16-2 AC2 + AC3.
- Line 4 — `maos audit query --spirit` exists (`main.rs:1710 -> `run_audit_query` call :1744` → `maos_shell::run_audit_query` `maos-shell/src/lib.rs:123`). **red:** no halt / resolution rows for hello-spirit at HEAD (16-2 AC1). ⚠ **R6 (HEAD `bb9f0657`): the line EXITS 1 — measured `Fr4SchemaViolation { line: 1, missing_field: "capability_token" }`.** `--spirit` switches the renderer to the FR4 mediation projection, which refuses any tokenless row; the EpistemicHalt row `invoke_halt` writes, the scheduler's `lifecycle.*` rows (kind 7, tokenless) and 16-1's operator completion row (kind 7, tokenless — off 16-1's own ratified D-16-1-E kind) are all tokenless and none is an external call. The resolution is journaled to `approval_decision_log`, which `maos_audit::query` never reads. And `maos audit query` resolves hello-spirit through maos-shell's PRIVATE `resolve_spirit_pid` (`"hello-spirit" => Some(0)`, no boot filter), not the `maos-audit` wildcard 16-1 routed here. Fixed by 16-2 AC5 (FR4 row classifier, completion-row kind, both wildcards retired).
- Line 5 — `maos run <manifest>` exists (`crates/maos-bin/src/worker_spawn.rs:44-82`; flags `--live`, `--once`; `--replay-llm` **retired by 15-6** — ⚠ R4: it was **not** a no-op, `"--replay-llm" => live = false` inside an order-sensitive loop, so its retirement is a behaviour removal, not a cleanup); butler is a loadable `rust-inproc` class (`spirits/butler/manifest.toml:10,15`, constructed `main.rs:455`) served until SIGTERM (`main.rs:4172-4183`); cassette exists (`ls crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json`). **red:** the door is enabled by `MAOS_OPERATOR_BEARER_TOKEN` (`main.rs:2705-2728`, token alone falls back to `127.0.0.1:8787`), bound at `:2827` before the objects half the verbs need exist, and GET-only (⚠ R5, 16-1); default bind from `control.json` created by 16-1 AC2.
- Line 6 — `maosctl pause <spirit>` exists (`cli.rs:771-774`); `maosctl spirit inspect <spirit>` exists (`cli.rs:855-862`). **red:** `pause` runs as a one-shot in a fresh process and pauses nothing running (`subcommands.rs:1648`); `spirit inspect` without `--sandbox` prints "requires --sandbox at v0.3-β" and exits **0** (`subcommands.rs:1890-1894`), and no lifecycle-state route exists (`crates/maos-control/src/lib.rs:428` serves only `/sandbox`) — route created by 16-1 AC1, re-target by 16-1 AC3.
- Line 7 — ⚠ **R4 — THIS BULLET'S STATED RED IS ALREADY CLOSED, AND THE VERB NAME WAS TAKEN.** The hand parser this bullet described (`init|audit|traceback|shell` arms with `Some(_)` falling through to the daemon) **no longer exists**: Story 15-3 replaced it with a single table lookup, `verbs::dispatch` (`crates/maos-bin/src/verbs.rs:254`, `VERBS` `:87`, `VerbName` `:59`), called by **both** `main()`s (`crates/maos-bin/src/main.rs:1335` air-gap, `:1677` network) whose `Outcome::Unknown` arms (`:1348`, `:1688`) exit non-zero and print the table. The gate's own doctrine states it: *"an unknown verb exits instead of booting the daemon"* (`verbs.rs:249-253`). **So the red "unknown verb boots the daemon" is GREEN at HEAD and cannot fund 16-4.** What IS still red is narrower and real: no product-removal verb exists at all. ⚠ **And `uninstall` is occupied**: `verbs.rs:193` registers it as a `MAOS_ONE_SHOT` mode meaning a *Spirit erasure cascade* (`run_uninstall_cascade`, `main.rs:5349` `LifecycleEvent::Uninstall`, `:5359` `if mode == "uninstall"`, terminal receipt + explicit `process::exit`), with `maosctl uninstall <spirit>` (FR65) on top of it and `erasure_uninstall_13_5b.rs:69,767,782` pinning it. **Operator ruling 2026-09-12 (Epic-15 retrospective): the existing `uninstall` semantics do not move.** 16-4's product-removal verb is a DISTINCT token — `maos purge` in this file; 16-4's preflight may choose a different synonym but may NOT be `uninstall`. **created by 16-4 AC1**; **red:** no product-removal verb exists.

✅ **Line 3 was knowingly broken and is now REPAIRED by Story 15-3 (F15/T12b, 2026-09-07).** bash parsed `<halt_id>` as **two redirections**, not a placeholder: run verbatim against a stub the line created a file named `--spirit` in the working directory, redirected stdout INTO it, and **dropped the `--spirit` flag the command requires** — the file's own contents were `ARGV: [halt] [resolve] [hello-spirit] [--kind] [provided-context] [--text] [idiomatic = clippy-clean, no unwrap]`, with `--spirit` gone and `hello-spirit` slid into the positional slot the halt id should occupy. `bash -n` was clean throughout, which is exactly why rule 1's dry-parse leg blessed it and `epic-15-21-preflight-r2-2026-09-05.md:36` could only catch it in prose (*"does not parse, real shape `--spirit <s> --kind provided-context --text`"*) — filed, never applied. **The repair is to QUOTE the placeholder** (`"<halt_id>"`), which keeps the documented positional shape, makes bash pass it as a literal argument (verified: the real `maosctl` reports `halt resolved <halt_id> (provided_context)`), and creates no stray file. `check-exit-commands` now enforces this as a first-class `PlaceholderHazard` finding: an unquoted `<name>` reds, a quoted one passes, and both directions are pinned in `xtask/tests/check_exit_commands_gate.rs`. 15-3 captured the gate's pre-repair red on this line before repairing it — that transcript is the story's only proof the gate was not green from birth.

**Operator-lane legs (never in the exit):**
- `ops-live-key-j0-leg` (row in `sprint-status.yaml` `# === OPERATOR LANE ===`): runbook transcript of exit lines 1–3 under `MAOS_INFERENCE_MODE=live` with a provider key, plus the NFR-Onb-2 clean-VM 5-minute leg (the repo already demoted that wall-clock to advisory, `tests/integration/onb_nfr2_timing.sh:11-30`); never in CI.

**Kernel-Δ:** ZERO kernel-Δ for 16-1..16-4 — the kernel APIs are already `pub`: `PolicyTable::shift_posture` (`crates/maos-kernel-core/src/capability/cap_policy/mod.rs:233`), `OrchestratorBuffer::enqueue` (`orchestrator/buffer.rs:46`), `SpiritSchedulerAdapter::pause/resume` (`scheduler/scheduler_loop.rs:401/428`, both **`async`**), `resolve_pid` (`:178`), `load` (`:243`), `HaltRegistry::insert_pending_with_metadata/resolve` + `invoke_halt` (`halt/mod.rs:193,243,504`); 16-3's Worker SCB and task record are a maos-bin shim over `load`, not a kernel API. **16-5 is FLAG-Winston** (+5–25 kernel lines, measured +3–25; re-pin `xtask/kernel-core-baseline.toml:481` `src_lines = 24474` → N in its own commit, last in the epic). ⚠ **R4 — the funding sentence was wrong and is corrected here.** D-F is **not a +250 pool 16-5 can draw from**; 15-2 §8 ruled it a *standing permission* and `kloc.toml:247` keeps `maos-kernel-core = 18935` at **ZERO HEADROOM deliberately, per D13(a)** — *"at zero headroom every further kernel-core line still costs its own measured FLAG-Winston grant and a HISTORY row."* Story 15-1 AC2 already exercised the permission for +2 (18933 → 18935, pin 24472 → 24474). **16-5 therefore needs its own operator-authorized, formatted-measured grant taken AFTER the code exists, not a citation to a reservation.** ⚠ Second R4 note: Story 15-4 landed a **line-neutral** (8+/8−) cfg-gate in `crates/maos-kernel-core/src/security/sandbox/linux.rs` that `check-kernel-baseline` cannot see, because the pin is a raw `.rs` line COUNT with no file set or content hash (`xtask/src/check_kernel_baseline.rs:30-31,62-63`). 16-5's re-pin commit is the first one after that, and the gap is closed by **`16-0-kernel-pin-content-hash`, which opens this epic and BLOCKS 16-5** (promoted out of the 20-3/W5 lane by the Epic-15 retrospective, 2026-09-12, operator-ratified — W5 was too late for a pin that 16-5 moves in W1). ⚠ **R4 — the doorbell this file never names:** any new file under `crates/maos-bin/src` (16-1's surface client, 16-4's purge module) must bump `SCANNED_SOURCE_FILES` in `crates/maos-bin/tests/cohort_daemon_smoke_13_5c.rs:869` in the same commit — it is **19** at HEAD (15-3 moved it 17 → 18 with `verbs.rs`, 15-6 18 → 19 with `inference_mode.rs`), and the count is asserted against the directory listing. This epic holds the shared-write lock on `crates/maos-bin/src/main.rs` first (`sprint-change-proposal-2026-09-04-round2.md:33`).

**Kloc asks** ⚠ **R4 — re-measured at HEAD `843d5365`, and `kloc-check` is now `PASSED (aggregate=158000)`, not EXIT=1.** Measured src-only headroom against `xtask/kloc.toml` today: `maos-bin` **+2390** · `maos-cli` **+1577** · `maos-control` **+988** (803/1791 — the *"697 headroom, 803/1500, `no change` row"* below is stale **twice**: 15-2 re-based the ceiling 1500 → 1791 and its §6 row 10 marked the `no change` claim FALSE) · `maos-shell` **+193** (307/500 after 15-2's 405 → 500; ⚠ **this is the binding crate for the epic** — the asks below total ~+130–180 against it) · `maos-secrets` **+839** · `maos-journey-test` **+604** · `maos-domain` **+497** · `maos-providers` **+748** · `maos-a2a-core` **+130** (tight) · `maos-kernel-core` **ZERO** · `xtask` **ZERO** (43797/43797). ⚠ The `xtask net 0` line below is no longer a preference but a **hard wall**: `xtask/tests/d11_xtask_ceiling_ratchet.rs` (15-4, kloc-free) reds any `xtask` ceiling raise that cites no OPEN `sprint-status.yaml` story, and freezes the ceiling once every numbered epic is `done`. Original R2 figures follow, unedited except where marked — (15-2 AC1 ledger; scout note §E): maos-bin **+480–820** (16-1 +300–500 · 16-3 +40–80 · 16-4 +100–200 · 16-2 wiring ~+20 (⚠ R6: ≤ +329, 16-2 story §10) · 16-5 call sites ~+20; drawn from the lane-wide maos-bin grant) · maos-cli **+400–700** (against 15-2's **+1100**, shared with 20-1's +250–400: 21 verb dispatchers become HTTP calls, one hand-rolled GET is ~50 lines, `fetch_live_sandbox_report` `subcommands.rs:1955-2005`; 9-7 added +911, 13-5b +144) · maos-control **+400–600** (within the existing 697 headroom, 803/1500 — `no change` row) · maos-shell **~+100–150** (16-2 +30–60 + `control.json` minting in `run_init` ~+30 + the `[operator]` read; 15-2 +100 is tight) · maos-secrets **+150–300** (within 839 headroom) · maos-journey-test **+50–120** (16-2 AC3; 15-2's new +400 row) · maos-domain **~+40–90** (16-4 `SecretStore` port +30 · 16-5 `CapError` +3, `IacBusError` +10–20; from 15-2's +300) · maos-providers **+30–60** (injectable key source; within 748 headroom) · maos-a2a-core **+20–40** (after 15-2's +150) · maos-kernel-core **+5–25** (16-5 only, from the D-F +250) · xtask **+216 MEASURED (16-0 only: 43797 → 44013, 2026-09-13); 16-1..16-5 remain net 0** — the `net 0` claim predated 16-0's promotion into this epic (the epic's first CI job is otherwise a plain discipline job — `cargo test` — not an xtask gate; 16-3's corpora live in crate `tests/`).

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`; planning-time leg: the provenance list above) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree · **10 route by charter, never by bucket** (Epic-15 retrospective 2026-09-12, operator-ratified): a deferral names one of three destinations and never a default one — (a) **fix at origin** when the item is inside the story's own blast radius (the eased CEILING RULE removes the budget reason this was declined); (b) **the story that already touches that file**, which is the first question asked, not the last; (c) **a theme story (20-3a/20-3b, 21-4) only when the item matches that story's charter.** Rationale: Epic 14 closed early because nine of its eleven remaining rows were gates, registries, ledgers and ceilings; the Epic-15 retrospective measured this lane repeating it, with two items filed to `21-4` that had nothing to do with one instrument and the env registry · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆ (16-1 and 16-5).

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `16-0-kernel-pin-content-hash` | Kernel pin gains a file set and a content hash ⚠ R4 — **opens the epic, blocks 16-5** | D2 (Epic-15 retro) · kernel-Δ 0 · xtask **+216 MEASURED** (2026-09-13, after `cargo fmt --all`: 43797 → 44013; forecast was +156…+260 ×1.3, and the FORMATTED measurement — not the forecast — is the ceiling) |
| `16-1-daemon-post-surface-and-verb-retarget` | Daemon POST surface and verb re-target ◆ | S1 · kernel-Δ 0 · maos-control ≤ +559, maos-bin ≤ +891 (net of the deleted one-shot arms), maos-cli ≤ +436 (net), maos-capability ≤ +11 (⚠ R5 (Story 16-1 validation round 2): re-booked for the store lock set, `operation_id` row, butler successor and re-revoke fix), maos-shell +33…+59, maos-domain +78…+130, maos-audit +13…+33 — ×1.3, all inside headroom (story §10; ⚠ R5 (16-1 story, HEAD `a6ed282c`): re-booked) |
| `16-2-shell-halt-registry-and-j0-scene` | Shell halt registry and the J0 scene | J0 · FR15/FR58 · kernel-Δ 0 · ⚠ R6 (16-2 story §10, HEAD `bb9f0657`, ×1.3 upper) · ⚠ MEASURED RE-BOOK (2026-09-15, operator-ruled — re-book, not re-rule or compress): maos-shell ≤ **+253 measured** (320 → 573; ceiling raised 500 → 573; driver: the pending-halt state machine), maos-bin ≤ +329, maos-audit ≤ **+273 measured** (6916 → 7189; ceiling raised 6951 → 7189; driver `crates/maos-audit/src/fr4_classifier.rs` alone is 368 lines, and the writer-shape table's size is set by the tree's tokenless-writer count, which the forecast had estimated from the rows J0 happens to write), maos-cli ≤ +37, maos-control ≤ +65, maos-spirit-hello ≤ +63, maos-journey-test ≤ +104 |
| `16-3-subprocess-crash-to-handle-crash` | Subprocess crash reaches the crash handler | FR12/FR50 · NFR-Rel-11 planned half at root shutdown (⚠ R6, AC4) · kernel-Δ 0 (`FaultCause::SignaledByKernel` reused; no `CrashCause` variant) · maos-bin +40–80, +20–40 for AC4 (estimate) |
| `16-4-maos-uninstall-and-keyring` | `maos purge` (product removal) and the keyring backend ⚠ R4 | FR2 · kernel-Δ 0 · maos-secrets +150–300, maos-bin +100–200, maos-providers +30–60, maos-domain +30 |
| `16-6-maosctl-load-over-the-door` | `maosctl load` over the door ⚠ R5 — (Story 16-1 round-table 2026-09-13, operator-ratified) | FR9 (`load`) · kernel-Δ 0 · maos-bin +250–450 (admission extraction), maos-cli +40–80, maos-control +20–40 (estimates; measured at dev) |
| `16-5-audit-drop-legal-hold-and-a2a-deny-vocabulary` | Debt slot: audit drop, legal hold, A2A deny vocabulary ◆ FLAG-Winston | D3/D5/D7/D12/D18 · kernel-Δ +5–25 (measured +3–25) · maos-a2a-core +20–40, maos-domain +3–25 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 16-0-kernel-pin-content-hash — Kernel pin gains a file set and a content hash

*Closes · Δ:* D2 (Epic-15 retrospective, 2026-09-12) · kernel-Δ **0** · xtask **+216 MEASURED** under the eased CEILING RULE (43797 → 44013, taken after the code existed and after `cargo fmt --all`; the epic's original +60–120 predated the file-set half needing a sorted fail-closed walker, a re-rootable seam, an emit-pin surface and named per-file findings)

⚠ **Why this opens the epic and blocks 16-5.** `check-kernel-baseline` pins a raw `.rs` line COUNT and nothing else
(`xtask/src/check_kernel_baseline.rs:30-31,62-63`). Story 15-4 landed a **line-neutral 8+/8−** `#[cfg(target_arch)]` gate on
seven x86-only syscall constants in `crates/maos-kernel-core/src/security/sandbox/linux.rs` — a kernel-core **security-surface**
change — and the gate reported `PASSED (24474, pinned 24474)` throughout. It was caught by a reviewer reading the diff, not by
any control. **16-5 is the next commit in this project that moves the pin.** Closing the gap after it would mean re-pinning
through the instrument this story exists to replace.

⚠ **Deliberately NOT an AC inside 16-5.** 16-5 is ◆ FLAG-Winston and performs a kernel-core edit; building the control that
audits its own edit is the failure shape Story 13.6e named (*a control whose owner has already shipped*). Different story,
different commit, and 16-0 lands first.

- **AC1 (command).** `cargo run -p xtask -- check-kernel-baseline` fails on a **line-neutral** kernel-core edit. Today it
  compares one integer; after this story it also compares (a) a pinned **file set** — **every file under
  `crates/maos-kernel-core/src`, any extension** (98 at `323ab597`: 97 `.rs` + `security/sandbox/t3-image.lock`, the signed
  T3 trust anchor; `src_lines` keeps its `.rs`-only denominator), read from the **filesystem, never `git ls-files`** — the
  compiler builds what is on disk, so a `.gitignore`d `mod backdoor;` compiles into the kernel yet is invisible to the
  index — so an added or deleted file is named rather than absorbed into a count that happens to balance — and (b) a
  **per-file** content hash over that set, so the failing file is named; an aggregate `set_hash` is derived from the sorted
  map and is **not** authoritative. A single scalar carries no per-file information and structurally cannot name anything.
  `xtask/kernel-core-baseline.toml` gains both beside `src_lines = 24474` (`:481`), **after first comment-repairing its seven
  orphaned prose lines at `:29-35`, which make the file invalid TOML at line 29; the repair is line-count-neutral so `:481`
  is unmoved**. The line count is kept, not replaced, because it is the figure every HISTORY row and every story's
  `kernel_grant` frontmatter cites.
- **AC2 (proven red, against a real commit — not a fixture).** The falsifier is **Story 15-4's actual seccomp commit**
  `843d53657f21…` (parent `830c26854fc1…`), 8 insertions / 8 deletions in the only kernel-core file it touched: with
  the pin taken at its parent, `check-kernel-baseline` must exit non-zero, name `security/sandbox/linux.rs` and report the
  denominator **`changed == 1`, `added == 0`, `removed == 0`** — where at HEAD it exits 0. The SET alone cannot carry this:
  it is **98 files on both sides**. A synthetic edit is not acceptable evidence here — the defect class is precisely *an edit
  a hand-written fixture would not think to make*, and 15-4's commit is the one the tree can prove. **CI precondition:
  `discipline.yml:2089` is a depth-1 checkout and gains `fetch-depth: 0` in the same commit** (precedent `check-fkcs`), and
  the commit is anchored by an annotated tag `kernel-pin-falsifier-15-4` so a squash or rebase merge cannot orphan it. The
  test **fails loud, naming both `fetch-depth: 0` and the tag push — never by skipping**.
- **AC3 (the pin's own HISTORY stays honest, and the gate cannot be silenced).** The new fields carry the same HISTORY
  discipline as `src_lines`: a re-pin records the measured value, the driver and the story key in the same commit, performed
  by `--emit-pin` printing a paste-ready block for a human to commit. **There is no in-place rewriter and no hold, waiver or
  known-mismatch constant — and that promise is proven by BEHAVIOUR, not by name**: an exhaustive line-neutral mutation of
  each of the 98 files in turn must red, denominator asserted first, and `passed` must be derived from the findings. A name
  grep is theatre: renaming the constant defeats it. D11's unreconciled-HISTORY half stays with **21-4**; this story does not
  build the reconciler, and says so.
- **AC4 (no false positive on formatting).** The hash is over **raw bytes**. ⚠ The phrase *"the same normalized bytes the
  count is"* is **struck**: there is no normalization step anywhere in the gate, and implementing it literally would blind
  the hash to exactly the whitespace-only edits it exists to catch. `check-fmt` is not an xtask gate and has a different
  subject, so "cannot disagree" is not a relation between them. The true rationale: *rustfmt is deterministic and idempotent,
  so on a tree held at its fixed point the content hash is a stable function of content; a reflow can only move the hash by
  moving bytes, and `check-fmt` is what holds the tree at that fixed point — the two gates are complementary, not redundant.*
  Proven in a **tempdir copy** (the reflow moves the count 24474 → 24471 and would red three gates in the tracked tree) in
  three measured steps: `H0 == pinned` (which is what proves the hasher is re-rootable), reflow a file with no
  `#[rustfmt::skip]` and assert `H1 != H0` (the positive half — without it the vector passes on a hasher that ignores its
  input), then `rustfmt --edition 2021` and assert `H2 == H0`. The job gains `components: rustfmt`.

### 16-1-daemon-post-surface-and-verb-retarget — Daemon POST surface and verb re-target ◆

*Closes · Δ:* S1 · kernel-Δ 0 · maos-control ≤ +559, maos-bin ≤ +891 (net of the deleted one-shot arms), maos-cli ≤ +436 (net), maos-capability ≤ +11 (⚠ R5 (Story 16-1 validation round 2): re-booked for the store lock set, `operation_id` row, butler successor and re-revoke fix), maos-shell +33…+59, maos-domain +78…+130, maos-audit +13…+33 — ×1.3, all inside headroom (story §10; ⚠ R5 (16-1 story, HEAD `a6ed282c`): re-booked). ◆ = §A6 review net non-degradable: this is the "second trust path" the prior preflight §3 and `crates/maos-control/src/lib.rs:68-76` name; the review net is the binding control (frontier allowlist rule).

- **AC1 (command).** With `maos run spirits/butler/manifest.toml` running and the door bound per AC2, `maosctl pause butler` (`crates/maos-cli/src/cli.rs:771-774`) makes `maosctl spirit inspect butler` (no `--sandbox`; today it exits 0 with "requires --sandbox at v0.3-β", `crates/maos-cli/src/subcommands.rs:1906-1910`) report `ScbLifecycleState::Paused` (`crates/maos-kernel-core/src/scheduler/control_block.rs:31`) from a new `GET /v1/spirits/{id}` route (`crates/maos-control/src/lib.rs:428` today serves only `/v1/spirits/{id}/sandbox`; `--sandbox` becomes optional and keeps that report), and the daemon journal carries the pause; `maosctl posture butler --shift cautious` (`cli.rs:678-684`, `PostureChoice` `:687-693`) changes the next capability decision of the running Spirit through `PolicyTable::shift_posture` (`cap_policy/mod.rs:233`) after `resolve_pid("butler")` (`scheduler_loop.rs:178`). ⚠ R5 (16-1 story, HEAD `a6ed282c`): `maosctl pause|posture|halt|orchestrator butler` exit 2 inside `maosctl` before any spawn (`resolve_spirit_pid` → `maos_audit::resolve_spirit_name` needs a kind-19 row `maos run` never writes; `lifecycle_verb` hardcodes `hello-spirit` for start/stop/unload), five one-shot arms would refuse it again inside the child, and the `pause` arm never calls the scheduler — so the ACs assert the KERNEL TRANSITION, not the journal row; `stop` is refused client-side (no kernel transition is named stop) and `start` on a Running Spirit is a typed 409. The CI job for exit lines 5–6, `cargo test -p maos-bin --test one_daemon_one_door_16_1`, starts the root, waits (bounded) until `GET /v1/spirits/butler` reports `lifecycle_state: Running` (⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): REPLACED "for the endpoint in `control.json` to accept a connection" — the bind precedes `scheduler.start`, `main.rs:5006`, so a connect-ready door can still answer 404), then issues the verbs — the block carries no `sleep`.
- **AC2 (design — D-A, ADR-062).** The `maos run` / `maos shell` daemon process binds the loopback POST surface (`maos-control`, bearer required): `maos-control` gains POST + a body parser + typed errors (today `handle_connection` `lib.rs:265` reads to `\r\n\r\n` and matches exact request lines `:274-300`; the 4 GET routes are `lib.rs:299,347,392,428`; the READ-ONLY design note `lib.rs:68-76` is superseded by ADR-062 (15-5 AC1); the three `POST → 404` tests `lib.rs:607-611,786,927` move to post-auth 405-refusal expectations under ADR-062 — ⚠ R5 (16-1 story, HEAD `a6ed282c`): all THREE become post-auth 405 guards — `:927` guards self-identity, the serving-leaf identity ADR-062 :63 preserves — none narrowed, none inverted into acceptance). `maos init` mints the bearer and writes `MAOS_HOME/control.json` `{"version": 1, "endpoint": "tcp://127.0.0.1:<port>", "token": "<64 hex>"}` (⚠ R5 (Story 16-1 round-table F-I): every reader refuses an unknown `version` or a non-`tcp://` scheme) (today `run_init` writes `config.toml` with `[slots]`/`[retention]` only, `crates/maos-shell/src/lib.rs:60`, `default_config_toml` `:313`; the daemon reads the token from env only, `main.rs:2706`, enabled by the TOKEN, not the bind (`:2705-2728`; the old `:2687` cite is `Arc::clone(&telemetry)`), bind executed at `:2827`; ⚠ R5 (16-1 story, HEAD `a6ed282c`): the real gate is the TOKEN — token alone binds `127.0.0.1:8787` (`:2709-2713`), which is retired; the bind MOVES after `upgrade_orchestrator` (`:3087`) because `hot_swap_coordinator`/`revocation_applier`/`upgrade_orchestrator` do not exist at `:2827`, and it is gated to the three door roots because every `MAOS_ONE_SHOT` child reaches it today). Every composition root (`maos shell` `main.rs:3378`, `maos run` `:4020-4031`, `cohort-a2a-daemon`) binds the endpoint named in `control.json` by default; a second root binding the same endpoint fails fast with a typed `OperatorHttpError::EndpointInUse` (never a silent second daemon, never a fallback port); ⚠ R5 (16-1 story, HEAD `a6ed282c`): REPLACED — `control.json` keeps ONE writer (`maos init`, ADR-062 :52-53); every door root holds a shared `flock` for life on its home directory **and on the directory of every store the durable verbs write** (the directory handles themselves — no lock file is created, ⚠ R5 (Story 16-1 consistency check, HEAD `f72b557b`)) — Transparency Log, memory db, private memory root, Lifecycle Journal, erasure proofs, each resolved by the resolver boot already uses — taken before any store is opened (⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`) and round-table F-C: REPLACED one `MAOS_HOME/daemon.lock` — a lock guards a STORE, so it is keyed to where each store resolves; the memory root, the TL via `MAOS_AUDIT_DB` and the proofs dir all resolve without `MAOS_HOME`), and serves `GET /v1/daemon` → {pid, boot_nonce}; `maosctl` tells "no daemon" from "store in use" by a momentary non-blocking probe of the home lock, without a second writer or a PID race. Every `maosctl` verb discovers the daemon from that file; the env overrides `MAOS_OPERATOR_HTTP_ENDPOINT` / `MAOS_OPERATOR_BEARER_TOKEN` are kept (read at `subcommands.rs:1963,1965`; registered by 15-1 AC5). `maos-cli` gains a hand-rolled HTTP client (one GET exists, `fetch_live_sandbox_report` `subcommands.rs:1955-2005`) with **no `maos-control` dependency** — it links kernel-core (`crates/maos-control/Cargo.toml:11`) and `maos-cli` may not (`crates/maos-cli/tests/dep_kernel_core_free_test.rs:8-24`); no new workspace crate (`check-workspace-count` is hard equality, `xtask/src/check_workspace_count.rs:57`). Async runtime: `pause/resume` are `async fn` (`scheduler_loop.rs:401/428`) and the server is a sync `std::thread` with no tokio dependency (`lib.rs:222`, `maos-control/Cargo.toml`), so ⚠ R5 (16-1 story, HEAD `a6ed282c`): the POST handlers are a SYNC port whose maos-bin implementation holds a `tokio::runtime::Handle` captured in async `main` and spawns each kernel future on it from the server's worker thread — never cancelled; on route-budget expiry the door answers `503 HandlerStillRunning` with an `operation_id` that the command's completion TL row carries (⚠ R5 (Story 16-1 round-table F-F): REPLACED `block_on`) (`OperatorHttpServer::bind` does NOT take a `Handle` — `maos-control` stays tokio-free); the daemon-held `Arc`s (`policy` `main.rs:1899`, `halt_registry` `:2022`, `orchestrator_registry` `:2031`, `scheduler` `:2681`, `crash_detector` `:2754`, `capability` with the ring inside `capability/mod.rs:119`) are in scope at the bind site `:2827-2846` (which already receives `Arc::clone(&scheduler)`); ⚠ R5 (16-1 story, HEAD `a6ed282c`): the handlers are a SYNC `OperatorCommandPort` implemented in a NEW `crates/maos-bin/src/operator_door.rs` holding the tokio `Handle` — `maos-control` stays tokio-free, and `OperatorHttpServer::bind` keeps its signature (18 test call sites + production `main.rs:2829`) behind a new `bind_with_commands`. ⚠ R5 (Story 16-1 round-table F-G/F-H): mutating commands are serialized per Spirit id (a command still queued at its deadline is withdrawn by an atomic `Queued → Withdrawn` CAS ⇒ `503 SpiritBusy`; a started one ⇒ `503 HandlerStillRunning` with its `operation_id` — ⚠ R5 (Story 16-1 fourth read, HEAD `f72b557b`)); every test that spawns a door root isolates `HOME`, enforced in CI by a decoy `control.json` (story D-16-1-Q).
- **AC3.** The **18** `MAOS_ONE_SHOT` spawn sites in `maos-cli` (`grep -c 'MAOS_ONE_SHOT' crates/maos-cli/src/subcommands.rs` = 21 incl. 3 doc lines; 16 literal `env("MAOS_ONE_SHOT","…")` sites `:582,1505,1513,1535,1561,1577,1617,1648,1666,1693,1711,1740,1766,1781,1818,1867` + `:1486` for `start/stop/unload/uninstall` via `lifecycle_verb` `:28-31` + `:1189` for `run <spirit>` — 21 verb values) are split: ⚠ R5 (16-1 story, HEAD `a6ed282c`): live-state verbs {pause, resume, start, unload, posture-shift, halt-resolve, orchestrator-queue, orchestrator-status, revoke-token, revocations-import, revocations-list, spirit-upgrade, hot-swap-precheck} go to the surface; `stop` is refused client-side (no kernel transition is named stop); `run` is not re-targeted; ⚠ R5 (16-1 story, HEAD `a6ed282c`): only {halt-list, legal-hold-list} are durable TL readers and run in-process in `maosctl`; {revocations-list, orchestrator-status, hot-swap-precheck} read IN-MEMORY daemon state and go to the door. {forget, uninstall, legal-hold-release, governance-admit} are DURABLE and go to the door when a daemon is live, else run offline in a `maos` child that takes every lock in the store lock set exclusively (see AC2). `run` (`MAOS_ONE_SHOT=hello-spirit`) is the FR58 evaluator workload and is NOT re-targeted. `MAOS_ONE_SHOT` survives for `smoke-*`, `acp-server`, `registry-server`, `bench-section-13-1`, `collective-erase`, `cohort-a2a-daemon`, `hello-spirit` and the four durable offline modes (`MAOS_ONE_SHOT_MODES` 48 → 32; `verb_table_15_3.rs` re-measured) (known-modes message `main.rs:5162-5165, rendered from `verbs::MAOS_ONE_SHOT_MODES` verbs.rs:146`; `discipline.yml:1576-2302`); `backup`/`import` never used it (`dispatch_backup` `subcommands.rs:308`, `dispatch_import` `:967`) and `init` is a `maos` verb (`main.rs:1677 (`verbs::dispatch`)`). The synthetic-halt seeding in `halt-resolve` (`main.rs:5765-5783`, "integration tests seed halt IDs via env") is deleted.
- **AC4.** `maosctl revoke-token` mutates the daemon-held `CapTokensShardRing` (`crates/maos-capability/src/cap_tokens/mod.rs:111`, held as `tokens: Arc<CapTokensShardRing>` in `CapabilityRegistryAdapter` `crates/maos-kernel-core/src/capability/mod.rs:119`, built `:154`, mutated by `revoke(token_id)` `:327`); `maosctl revocations import` reports `matched > 0` against a running Spirit (today the one-shot arm `main.rs:6804-6840` counts against a fresh process's ring); `maosctl orchestrator queue` enqueues into the daemon registry (`crates/maos-kernel-core/src/orchestrator/registry.rs:32` `get_or_create` → `OrchestratorBuffer::enqueue` `buffer.rs:46`).
- **AC5.** Proven-red: with the daemon down every LIVE-STATE re-targeted verb exits non-zero with a typed error (`NotInitialized` 78 / `DaemonNotRunning` 69 / `StoreInUse` 69 — ⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): REPLACED `DaemonAliveWithoutDoor`, because `flock` cannot tell a door-less daemon from another offline operation), never journal-and-succeed; a wrong bearer (401 ⇒ 77) is a separate negative. ⚠ R5 (Story 16-1 fourth read, HEAD `f72b557b`): a connection that yields no well-formed HTTP response ⇒ `DoorUnresponsive` 69; a store directory that cannot be locked (NFS, permissions) ⇒ `LockUnavailable` 78. ⚠ R5 (16-1 story, HEAD `a6ed282c`): the four DURABLE verbs run offline when no door endpoint is configured (neither the env pair nor `control.json`) or the connect is refused, and their `maos` child acquires the whole store lock set exclusively (500 ms retry); a door that connects — answering, 401, or unresponsive — is never bypassed offline (⚠ R5 (Story 16-1 fourth read, HEAD `f72b557b`)) — making them door-only would break GDPR erasure with no daemon and the 13.5b `0/3/4/5` contract.
- **AC6.** The re-targeting updates: `crates/maos-bin/tests/one_shot_hello.rs:15` and `erasure_uninstall_13_5b.rs:69,767,782` (the 7 `MAOS_ONE_SHOT` hits in `cohort_daemon_smoke_13_5c.rs` are `cohort-a2a-daemon` and survive); `crates/maos-cli/tests/posture_shift_test.rs` (3 real-chain tests, `:49,69,86`); the nine `MAOS_BIN_PATH` test files (⚠ R5 (16-1 story, HEAD `a6ed282c`): only TWO — `legal_hold_cli_13_5b`, `uninstall_exit_codes_13_5b` — write a fake; the other seven run the REAL binary and pin offline success, two of them vacuously) (`grep -l MAOS_BIN_PATH crates/maos-cli/tests/*.rs` = 9: `halt_resolve_test` 8, `orchestrator_queue_test` 7, `pause_resume_test` 6, `revocations_import_test` 5, `revoke_token_test` 4, `spirit_upgrade_test` 4, `legal_hold_cli_13_5b` 1, `uninstall_exit_codes_13_5b` 1, `accessibility_test` partly — ≈36 of the 42 `#[test]` fns in those files, `grep -c '#\[test\]'`) rewritten against a fixture HTTP daemon; CI scripts `tests/integration/v01_evaluator_path.sh` (`discipline.yml:700`) and `tests/integration/maosctl_smoke.sh` (`:665`); non-CI scripts `tests/integration/halt_resolve_smoke.sh`, `orchestrator_queue_smoke.sh`, `maosctl_hot_swap_precheck.sh` retired or updated. `cohort_daemon_smoke_13_5c` `SCANNED_SOURCE_FILES` (`crates/maos-bin/tests/cohort_daemon_smoke_13_5c.rs:869`, 19 == `ls crates/maos-bin/src/*.rs` at `a6ed282c`, equality assert `:954-958`; 16-1's `operator_door.rs` makes it 20) gains every new maos-bin module.

### 16-2-shell-halt-registry-and-j0-scene — Shell halt registry and the J0 scene

*Closes · Δ:* J0 · FR15/FR58 · kernel-Δ 0 · maos-shell +30–60, maos-journey-test +50–120 (⚠ R6: re-booked in the stories table — six crates, not two)

⚠ **R6 (Story 16-2 creation, HEAD `bb9f0657`) — the four ACs below are SUPERSEDED by the story's eight, summarised here; the original text is kept verbatim below this note (not struck through), so its cites stay auditable.** (1) **AC1** keeps its command, and the scene gains the PRD's beat (`prd/user-journeys.md:293`: *"type a clarification, the Spirit proceeds"*): after `maosctl halt resolve`, the REPL renders `halt <id> resolved: provided_context` and the Spirit's proceeding turn replays the j0 cassette. (2) **hello-spirit becomes a scheduler-loaded Spirit in shell mode** — `HelloSpirit` (`impl Spirit`) + an embedded manifest in `maos-spirit-hello`; load → admit at the real pid → start, as `maos run` does; CWD-independent. (3) **The halt** is raised by `invoke_halt` behind a `ShellHost` port (NEW `crates/maos-bin/src/shell_host.rs`, doorbell 20 → 21) with a ULID halt id printed in the REPL; the `shell.halt` cap-audit row is deleted; `maosctl halt list` prints `halt_id`. (4) **Two resolution surfaces, one mechanism** — a clarification typed in the REPL submits `OperatorCommand::ResolveHalt` to the same `OperatorDoor` in-process through ONE extracted `maos_control::submit_and_wait` (the server's withdraw CAS, not a copy; the port is built for every door root, bind or not), and the REPL watches `HaltRegistry` state so a resolution from either surface renders unprompted; **EOF unloads hello-spirit**, so a pending halt gets a `PlannedUnload` receipt (NFR-Rel-11), and `halt list` names each kind-3 row `termination_marker`/`termination_receipt`/`termination_no_pending`/`raised`/`unparseable` (classified in that order — a receipt payload also parses as a halt payload); the shell's teardown drops every `audit_tx` owner before awaiting the writer, proven by no `drain timed out`; halt ids are ULIDs (the kernel's grammar). (5) **Audit reads made true**: both `hello-spirit → pid 0` wildcards retired (maos-shell's private table and the maos-audit branch; the one-shot writes a kind-19 identity row); the FR4 projection classifies non-call kernel rows — kinds that are never calls (`epistemic.halt`, `telemetry.event`, budget 12/13, watchdog 15/16, `spirit.admitted`, `governance.event`, `cost.attribution`) plus an exact writer-shape table of every tokenless kind-7 kernel/bin writer (the eight `lifecycle.*` shapes differ: start/pause/resume/unload carry `spirit_pid`, not `spirit_id`), guarded by an inventory doorbell test — so they never red FR4 while a tokenless CALL still does; hello-spirit's inference request carries its token's pid, not a literal 0; NDJSON stays call-only; plain tables gain an `intent` column; 16-1's completion row is re-kinded to its ratified `TelemetryEvent`, its intent carries `:<outcome>`, and its payload `spirit_id` + `outcome`. (6) **Harness + CI**: `Pty::send_line`; the banner null control repaired and proven red; NEW `j0-scene` job; `maosctl` built in `journey-hermetic-tier-1`. (7) **Honesty**: `onb_nfr2_timing.sh`'s response leg asserts the replayed introduction (it passed on "provider unreachable"); `HelloResponse.transparency_log` names the real file. (8) **AC4's page is `docs-site/docs/run-maos.md`** (the published route), not `docs/maos.dev/run-maos.md`.

- **AC1 (command).** Lines 1–3 of the exit block pass in `journey_j0` on a hosted runner and `maos audit query --spirit hello-spirit` (`main.rs:1710 -> `run_audit_query` call :1744` → `run_audit_query` `crates/maos-shell/src/lib.rs:123`, sqlite at `:128`) shows the halt row and its resolution row; the response-path budget of `tests/integration/onb_nfr2_timing.sh` (`discipline.yml:1104`) stays blocking and the clean-VM 5-minute leg (NFR-Onb-2) is the runbook's operator step under `ops-live-key-j0-leg` (the repo already demoted that wall-clock to advisory, `onb_nfr2_timing.sh:11-30`).
- **AC2.** `maos_shell::run_shell` (today `run_shell(inference, capability: Arc<CapabilityRegistryAdapter>, color_choice, default_provider)`, `crates/maos-shell/src/lib.rs:165-170`; maos-shell already links kernel-core, `crates/maos-shell/Cargo.toml:15`) gains `Arc<HaltRegistry>`, `Arc<TransparencyLogAdapter>` and `Arc<JournalAdapter>`. The `Err(HelloError::Ambiguous{..})` return (`crates/maos-spirit-hello/src/lib.rs:182`; detector `is_ambiguous` `:127-152`; variant `:35`) (⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified): the `hello-spirit → pid 0` wildcard in `maos_audit::resolve_spirit_name`, `crates/maos-audit/src/lib.rs:1624-1665`, matches every pid-0 kernel row; 16-1 routed its retirement HERE because this AC gives hello-spirit a real pid) is turned by the shell into `invoke_halt(tl, journal, registry, payload, pid, "hello-spirit", boot_nonce)` (`crates/maos-kernel-core/src/halt/mod.rs:504-512`), which inserts `HaltState::PendingResolution` (`crates/maos-domain/src/halt.rs:317`) and writes the `FrameKind::EpistemicHalt` row (`halt/mod.rs:523-529`, payload `crates/maos-domain/src/frame.rs:67`) — today the shell prints `[HALT …]` and writes a cap-audit row only (`maos-shell/src/lib.rs:271-282`). One path, one frame kind: hello-spirit is loaded through the scheduler in shell mode so the halt carries its real pid (today admitted "via canonical path" without `scheduler.load`, `main.rs:3362`). The shell process serves the 16-1 door, so `maosctl halt resolve` reaches **this** registry (`HaltRegistry::resolve` `halt/mod.rs:243`; today it spawns a fresh `maos`, `subcommands.rs:1617`).
- **AC3.** `journey_j0` (`crates/maos-journey-test/tests/journey_j0.rs`) drives the REPL through the PTY harness (`Pty::spawn` `:64`, `wait_for_screen` `:68-72`), sets `MAOS_INFERENCE_MODE=replay` on the child beside the harness's `MAOS_REPLAY_CASSETTE` (`crates/maos-journey-test/src/lib.rs:118-121`, ADR-064), types line 2's directive, runs line 3's `maosctl halt list/resolve` against the shell's `control.json` endpoint and asserts the halt, resolution and audit rows through `AuditDb` (today two tests, banner strings only: `j0_init_creates_config` `:28`, `j0_shell_banner_via_pty` `:55`).
- **AC4.** `docs/maos.dev/run-maos.md` (26 lines; no env var or key mention today) documents `MAOS_INFERENCE_MODE={record,replay,live}` + `MAOS_REPLAY_CASSETTE` (D-D / ADR-064) and where the provider key comes from (16-4 AC2).

### 16-3-subprocess-crash-to-handle-crash — Subprocess crash reaches the crash handler

*Closes · Δ:* FR12/FR50 · NFR-Rel-11's planned half at root shutdown (⚠ R6, AC4) · kernel-Δ 0 — `ExitCause::Signaled{signal}` (`crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:117`) maps to the existing `FaultCause::SignaledByKernel{signal, stderr_tail}` (`crates/maos-domain/src/supervision.rs:99-102`; `CrashCause` is `maos-domain/src/supervision.rs:47`, not kernel — no variant is added) · maos-bin +40–80

- **AC1 (command).** `kill -9` on a fixture Worker (a `sh` fixture at tier T1/none — precedent `crates/maos-kernel-core/tests/cli_wrapper_bridge_8_12.rs:132-136`, `sh -c 'kill -9 $$'` → `ExitCause::Signaled{9}`; no podman/bwrap) yields `task.orphaned` within 2 s; on a 100-SIGKILL corpus ≥99/100, asserted by `nfr-rel-1-crash-detection-2s` (`.github/workflows/discipline.yml:1196-1207`; today `crash_detector_in_process_panic --release` asserts ≥1 receipt, `crates/maos-kernel-core/tests/crash_detector_in_process_panic.rs:119-123`). The corpus runs `--release` with per-kill budgets and asserts counts, not wall-clock per kill; flake policy: one automatic re-run, then red.
- **AC2.** A 50-hang corpus yields `task.stalled` within 60 s for ≥48/50, asserted by `nfr-rel-2-hang-detection-60s` (`discipline.yml:1210-1221`; today `progress_watchdog_smoke` asserts ≥1, `crates/maos-kernel-core/tests/progress_watchdog_smoke.rs:119-122`). Both jobs stay in `aggregate.needs` (`discipline.yml:3727-3728`).
- **AC3.** Workers get an SCB at spawn by loading a maos-bin `WorkerSpirit` shim through `SpiritSchedulerAdapter::load` (`crates/maos-kernel-core/src/scheduler/scheduler_loop.rs:243`), and a `TaskAssignmentRecord` is pushed to `scb.task_assignments_in_flight` (`crates/maos-kernel-core/src/scheduler/control_block.rs:277`; today's only writers are the smoke arm `main.rs:6596,6649`) — because `handle_crash` returns `HandleCrashError::NotLoaded` without an SCB (`crates/maos-kernel-core/src/supervision/crash_detector.rs:95-100`) and emits `task.orphaned` only per record (`:141-150`). `wait_and_finalize` (`runtime.rs:632`, signature `:638-644`; caller `crates/maos-bin/src/worker_spawn.rs:732` inside `run_cli_wrapper_manifest` `:384`, synchronous per `delegation.rs:666`) receives the real pid instead of `0` and feeds `CrashDetector::handle_crash` (sole caller today `scheduler_loop.rs:583`, the `HookOutcome::Panicked` arm) with `CrashCause::Fault(FaultCause::SignaledByKernel{..})`; `on_crash.action` applied with reassign→escalate journaled. The same ledger makes `ProgressWatchdog` (`crates/maos-kernel-core/src/supervision/progress_watchdog.rs:73,109`) see Workers for AC2. kernel-Δ 0: the shim, the record push and the pid all live in maos-bin.
- **AC4 (⚠ R6 — Story 16-2 round-table 2026-09-14, operator-ratified *"route it to 16-3 as a new AC"*: NFR-Rel-11's PLANNED half at root shutdown).** NFR-Rel-11 (`prd/non-functional-requirements.md:30`): *"Every Spirit termination, planned or unplanned, produces a halt receipt before process exit."* No `maos run` root unloads its Spirits on the way out — at `bb9f0657` the only `.unload(` call in `crates/maos-bin/src/main.rs` is a smoke arm (`:6119`) — so a SIGTERM'd root (teardown at `:7880`) and both `--once` returns (`:4498`, `:5461`) exit with SCBs loaded and pending halts receipt-less. Every `maos run` shutdown path unloads every loaded SCB (`SpiritSchedulerAdapter::scbs` `scheduler_loop.rs:148`) through `SpiritSchedulerAdapter::unload` (`:472`), which runs `terminate_spirit(…, TerminationKind::PlannedUnload)` (`crates/maos-kernel-core/src/halt/termination.rs:26`) and drains that pid's pending halts into receipts — **AFTER** the door is shut and `drain_started_tasks` has returned (no operator command races an unloading Spirit) and **BEFORE** the watchdog join and audit-writer drain (receipts must reach the Transparency Log); an `on_unload` hook error is journaled and never skips the remaining unloads; ⚠ this teardown's audit drain must actually close the channel — the butler `--once` block (`main.rs:5466-5478`) drops only 7 of its owners and TIMES OUT (probed by Story 16-2's fourth read: `audit writer drain timed out after 5s`, exit 0); repair it at origin so EVERY owner of `audit_tx` is dropped before the writer is awaited — the complete reference is the SIGTERM root's drop block `:7941-7970` (incl. the hidden owners `iac` and `delegation_leg`); the topology `--once` block `:4501-4520` is the shorter proven one — and assert no `drain timed out` on stderr, as `drain_once_audit_writer.rs:32` and `one_daemon_one_door_16_1.rs:481` do; the pids are COLLECTED from `scbs()` and its read guard DROPPED before the first `unload` (which takes `spirits.write()` — iterating under the std `RwLock` read guard deadlocks). The `maos shell` root uses the same order (Story 16-2 D-16-2-C: server shutdown → `drain_started_tasks` → unload → port → audit drain → locks). **Zero-Δ residuals, measured by Story 16-2 validation round 3 and stated, not claimed:** (i) `is_transition_allowed` has no `Loaded → Unloaded` (`crates/maos-kernel-core/src/scheduler/control_block.rs:446-454`), so a Spirit loaded but never started refuses `unload` and gets no receipt; (ii) `unload` runs `check_hook_outcome(…)?` BEFORE `terminate_spirit` (`scheduler_loop.rs:497-508`), so an `on_unload` that panics or exceeds its budget skips that Spirit's receipts — both are kernel bytes with no story charter ⇒ named on the **`epic-16-retrospective`** row beside the cold-swap admission item. ⚠ Butler registers its `belief_variance` halt at `BUTLER_SPIRIT_PID` 0 (`spirits/butler/src/lib.rs:122`; Story 16-1 Trap 19), so `drain_for_spirit(<butler's real pid>)` cannot see it — T0 decides between fixing that pid at origin in `spirits/butler` (kloc-ungoverned) and proving with a Spirit whose halts carry its real pid, and states which. Proven: a root with a pending halt at the Spirit's real pid, SIGTERM ⇒ a `PlannedUnload` receipt row naming that `halt_id` and a `lifecycle.unload` row before exit; Story 16-1's idle SIGTERM budget (≤ 3 s, `one_daemon_one_door_16_1` AC1 step 5) re-measured and still met; tests asserting exact post-shutdown journal or TL row sets are updated for the new `Unload`/receipt rows, never loosened to `contains`. The `maos shell` root does the same for hello-spirit (Story 16-2 D-16-2-D). `cohort-a2a-daemon` is measured at T0 and included if it loads Spirits. kernel-Δ 0 (`unload` and `scbs` are `pub`).

### 16-4-maos-uninstall-and-keyring — `maos purge` (product removal) and the keyring backend

⚠ **R4 (Epic-15 retrospective, 2026-09-12 — operator ruling).** This story's verb was `maos uninstall`. That token is **already occupied** by a different destructive semantic: `verbs.rs:193` registers `uninstall` as a `MAOS_ONE_SHOT` mode running the *Spirit erasure cascade* (`run_uninstall_cascade`), `maosctl uninstall <spirit>` (FR65) sits on it, and `erasure_uninstall_13_5b.rs:69,767,782` pins it. **Ruling: the existing semantics do not move.** The product-removal verb is a distinct token — `maos purge` throughout this file. 16-4's preflight may pick a different synonym, but not `uninstall`, and not disambiguation by argument arity (a typo must not change what is erased). Adding the verb is now a `verbs.rs::VERBS` row + a `VerbName` variant, **not** a hand-parser arm, and `check-exit-commands` (15-3) resolves the exit block's line-7 token against that table.

*Closes · Δ:* FR2 · kernel-Δ 0 · maos-secrets +150–300, maos-bin +100–200 (`maos purge` verb), maos-providers +30–60, maos-domain +30 (`SecretStore` port, under 15-2)

- **AC1 (command).** `maos purge` (new top-level `maos` verb — ⚠ **R4**: it is added as a `crates/maos-bin/src/verbs.rs:87` `VERBS` row plus a `VerbName` variant, because 15-3 deleted the hand parser this AC described; the unknown-verb-boots-the-daemon red it was funded against is **closed** at `verbs.rs:249-253`. `uninstall` is NOT available — it is the Spirit erasure cascade (`MAOS_ONE_SHOT` mode `verbs.rs:193`, `main.rs:5349`/`:5359`, and `maosctl uninstall <spirit>` FR65)) removes `$MAOS_HOME` (`config.toml`, `control.json`, `audit/transparency.sqlite`, `journal/lifecycle.ndjson`, `skills/`, `logs/`; `crates/maos-shell/src/lib.rs:81-90`), the XDG data root `$XDG_DATA_HOME/maos` (`audit/`, `journal/`, `memory/`, `spirit-archives/`, `erasure-proofs/`; `crates/maos-audit/src/lib.rs:865-872,1405-1411,1452,1555-1575,1578-1582` — the archive dir is never under `MAOS_HOME`: `MAOS_ARCHIVE_DIR` else `$XDG_DATA_HOME/maos/spirit-archives`, `:1555-1575`), `$HOME/.maos` when `MAOS_HOME` is unset (`maos-shell/src/lib.rs:299-311`), and the binary only when it was installed under a prefix recorded at `maos init`; the CI leg `cargo test -p maos-bin --test maos_uninstall_16_4` installs to a temp prefix and removes that, never `target/`. `--keep-log` retains `audit/transparency.sqlite`; `maos init` afterwards is clean. ⚠ R6 (Story 16-2 round-table §15 R7): after 16-2 retires the `hello-spirit → pid 0` wildcard, hello-spirit rows in a Transparency Log written BEFORE 16-2 (which carry no identity row, and whose `shell.turn` payloads hold what an evaluator typed) no longer resolve by name and cannot be erased by `maosctl uninstall hello-spirit`; `maos purge` is their documented erasure route, and its docs say so — **and say it is not selective**: purge removes the whole home, and `--keep-log` keeps exactly those legacy rows.
- **AC2.** `maos-secrets` (today one type, `LocalMasterKeyKms` implementing `KeyManagementPort`, `crates/maos-secrets/src/lib.rs:20,33`) gains an OS-keyring backend: `keyring` (not yet a dependency — `grep 'name = "keyring"' Cargo.lock` empty; MIT OR Apache-2.0 within `deny.toml:35-51`) with Linux backend **`linux-native`** (keyutils, no daemon) plus `apple-native`/`windows-native`, and a **`file`** backend selected by `MAOS_SECRETS_BACKEND=file` for CI (hosted runners run no Secret Service D-Bus daemon; `MAOS_SECRETS_BACKEND` has zero readers today); `[bans] multiple-versions = "deny"` (`deny.toml:55`) is checked in the same commit as the lockfile. A `SecretStore` port (maos-domain +~30 under the 15-2 grant; today only `KeyManagementPort`, `crates/maos-domain/src/ports/key_management.rs:21`) is injected into `AnthropicProvider::new` / `OpenAiProvider::new`, which read `MAOS_*_API_KEY` directly today (`crates/maos-providers/src/anthropic.rs:37`, `openai.rs:7`); provider keys are read keyring-first, env fallback journaled as a downgrade; `FIXME(secrets)` at `main.rs:3116` retired. ⚠ R6 (Story 16-2 §11 row 3): the key-source paragraph Story 16-2 writes into `docs-site/docs/run-maos.md` (and its locale counterparts) is amended in the same commit. ⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified): Story 16-1 §11 row 2 routes the home split-brain here — the memory root and spirit archives ignore `MAOS_HOME` (`crates/maos-audit/src/lib.rs:1456-1470`, `:1555-1575`) while the TL and journal honour it; `maos purge` must enumerate the roots that actually exist, so this story either unifies those resolvers or enumerates both, stating which. ⚠ R5 (Story 16-1 consistency check, HEAD `f72b557b`): `maos purge` deletes those store directories, so it takes Story 16-1's store lock set (D-16-1-D, `flock` on each store directory) exclusively before deleting, re-checks after each lock that the locked handle and the path are the same inode (`fstat` vs `stat`), holds the set until it exits, and refuses 69 `StoreInUse` while a daemon or an offline operation holds any of it.
- **AC3.** `MAOS_SECRETS_BACKEND` registered in `crates/maos-bin/src/env_contract.rs` (the `check_env_contract` scan covers maos-bin `src` only, `xtask/src/check_env_contract.rs:121`); `maosctl uninstall <spirit>` (FR65) keeps its semantics over the 16-1 surface (it shares `lifecycle_verb`, `subcommands.rs:31`; the cascade at `main.rs:5359` needs the live registry).

### 16-5-audit-drop-legal-hold-and-a2a-deny-vocabulary — Debt slot: audit drop, legal hold, A2A deny vocabulary ◆ FLAG-Winston

*Closes · Δ:* D3/D5/D7/D12/D18 · kernel-Δ +5–25 (15-2 D-F ledger; measured +3–25: `capability/mod.rs:349-351` +1–3, memory serialization +10–20 incl. the exemption attribute; D3 measured "+1 kernel-core, +3 maos-domain" for the audit-drop half, `epic-14-preflight-decisions.md:173`) · maos-a2a-core +20–40 · maos-domain +3–25 (`CapError` +3, `IacBusError` +10–20; covered by 15-2)

- **AC1 (command).** A fault-injected audit sink makes `record_invocation` (`crates/maos-kernel-core/src/capability/mod.rs:332-352`; today `cap_audit::record_drop()` at `:349` then `Ok(())` at `:351`) return `Err` and the invocation is refused; new `CapError` variant in `maos-domain` (`crates/maos-domain/src/ports/capability.rs:113`). The two production callers that swallow the result — `crates/maos-shell/src/lib.rs:265,278`, `let _ =` (⚠ R6, HEAD `bb9f0657`: those cites are now `:304,317`, and after 16-2 there is ONE — the `shell.turn` call in `maos_shell::run_shell` — Story 16-2 deletes the `shell.halt` call; the halt is an `invoke_halt` frame; after 16-2 that call reads `let _ = host.record_turn(..)` and `ShellHost::record_turn -> Result<(), CapError>` passes `record_invocation`'s error through unchanged, so this AC's propagation stays a change at that one site) — propagate it (`grep -rn '\.record_invocation(' crates/*/src` finds no other production caller — ⚠ R6: after Story 16-2 it finds ONLY the pass-through in `crates/maos-bin/src/shell_host.rs` (`run_shell` calls `host.record_turn(` instead), which returns the `Result` and swallows nothing; the only swallowing caller is `run_shell`'s `let _ = host.record_turn(..)`). The verify path at `capability/mod.rs:236-245` returns `()` and is **left as-is**: its `record_drop()` at `:243` stays a counter; re-typing the verify signature is outside D3 and outside the grant. ⚠ R6 (Story 16-2 §11 row 1; round-table §15 R3 — DECIDED in this story's preflight with a recorded reason, never silently skipped): the scheduler writes its eight `lifecycle.*` TL rows as `FrameKind::CapabilityInvocation`, tokenless, `FrameOrigin::SpiritAuto` (`crates/maos-kernel-core/src/scheduler/scheduler_loop.rs` `load`/`start`/`pause`/`resume`/`unload`/`journal_lifecycle`, `supervision/crash_detector.rs`) — rows that claim a mediated call. 16-2 routes around it with an exact FR4 classifier; re-kinding them is a kernel byte and could ride this story's FLAG-Winston re-pin. **The cost of declining, recorded so the decision is priced:** every reader outside `maos-audit` — a SIEM, `verify.py`, a sealed-export consumer — sees a capability invocation without a token on every boot. 16-2's writer-shape table stays either way (TLs written before a re-kind keep their rows forever); the decision and its reason are recorded here.
- **AC2.** The legal-hold check-then-act in `forget_with_reason` (`crates/maos-kernel-core/src/memory/mod.rs:424`; check `:481-485` `is_under_legal_hold`, act `:530-531` `scrub_distillate_body`) is serialized under one lock — an `#[i9_exempt]`-documented field on `MemoryManagerAdapter` (`memory/mod.rs:79`) or a TL-level transaction, since `Mutex` is I9-denylisted (`xtask/i9-denylist.toml:12`); under hold the outcome **stays** `ForgetOutcome::Suspended{hold}` (`:475,:505`; `crates/maos-domain/src/memory.rs:423`, no `MemoryError` variant) and the erasure proof carries `completed:false` — the corpus contract (`crates/maos-audit/src/bin/gen_gdpr_cascade.rs:122-126`, `legal_hold → held`) and `run_uninstall_cascade` (`main.rs:5359`) are unchanged.
- **AC3.** `map_a2a_error_to_iac_bus` (`crates/maos-a2a-core/src/router.rs:1933`) preserves the deny vocabulary: the five "no new `IacBusError` variant" notes at `router.rs:1979-1983, 2011-2013, 2031-2033, 2037-2043, 2048-2051` are retired with typed variants `CrossHostConsentDenied{reason, direction}`, `CrossHostTeamIdentityRefused`, `CrossHostConsentUnclassified{reason}` on `IacBusError` (`crates/maos-domain/src/iac_bus_types.rs:14`; today the only typed deny is `CrossHostIntentDenied` `:36`), replacing the `CrossHostRouteFailure(String)` collapse at `:2015-2019, 2021-2030, 2034-2036, 2044-2047, 2052-2061`; `CrossWallRecallRefusal` (`crates/maos-domain/src/log_recall.rs:291`) variants surface typed (D7). The note at `main.rs:10037` concerns `TcpA2ATransport` pins, not deny vocabulary, and is not touched.
- **AC4.** The kernel re-pin (`xtask/kernel-core-baseline.toml:481` `src_lines = 24474` → N, counted after 15-1's +2) and the measured grant on `xtask/kloc.toml:247` (`maos-kernel-core = 18935`) land in the same commit; 16-5 lands last in the epic.
- **AC5 (⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified), routed from Story 16-1 §11 row 1).** `JournalAdapter::open` (`crates/maos-kernel-core/src/journal/mod.rs:98-102`) opens the Lifecycle Journal with `.write(true)` and no `.append(true)`, and `append_transition` writes at the handle's own cursor (`:178-181`): two processes appending overwrite each other's lines and leave a torn fragment (replayed by 16-1's scout). The open gains `O_APPEND`, **and** each transition is written by ONE `write_all` of the complete line including its newline — ⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): `write!(file, "{line}\n")` on a raw `File` (`:180`) issues two or more `write(2)` calls, so `O_APPEND` alone still interleaves; a two-process × N-line append vector proves every line parses and none is lost. Line-neutral, but a kernel-core byte ⇒ it rides this story's content-hash re-pin and FLAG-Winston grant. The comment at `crates/maos-bin/src/main.rs:2863-2864` ("file-append is atomic… on POSIX") is corrected in the same commit.

### 16-6-maosctl-load-over-the-door — `maosctl load` over the door

*Closes · Δ:* FR9's `load` half · kernel-Δ **0** · maos-bin +250–450, maos-cli +40–80, maos-control +20–40 (estimates, ×1.3 applied at dev; maos-bin re-estimated by ⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`) for the admission extraction in AC1). ⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified): created because Story 16-1 measured that **no `maosctl load` verb exists anywhere in the tree** while this epic's *Closes* line claims FR9 (*"load, start, pause, resume, and unload Spirits at runtime via authenticated control plane"*, `prd/functional-requirements.md:37`). Kept OUT of 16-1, which is already 1.5–2× its sketch; sequenced after it inside W1 so the FR9 promise stays in this epic.

- **AC1 (command).** With a `maos run` root up and its door bound (16-1), `maosctl load <manifest>` loads a second Spirit into the RUNNING root and `maosctl spirit inspect <id>` reports it from `GET /v1/spirits/{id}`. The admission path is **the one `maos run` already uses** — manifest parse, `security.admit_spirit`, the rust-inproc class construction, `SpiritSchedulerAdapter::load` (`crates/maos-kernel-core/src/scheduler/scheduler_loop.rs:243`) — reached through 16-1's `BinPrivateOps` seam; no second admission implementation. Every gate `maos run` applies applies identically, proven by one refusal vector per gate. ⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): that path is **not a callable function** today — it is inline in the ~1,650-line `if let Some(run)` block (`crates/maos-bin/src/main.rs:3490-5146`), dispatched per class by `classify_spirit` (`:453`) with `load::<T: Spirit>` at a dozen sites — so this story EXTRACTS one admission function that `maos run` and the door both call; and the vetting precondition is NOT a `maos run` gate (`enforce_vetted_upgrade_precondition` is called only by the upgrade arms, `:6452,:6734`) — the gate list is re-derived at T0 from the extracted function.
- **AC2.** Loading an already-loaded id ⇒ typed `409 AlreadyLoaded` (`LifecycleError::AlreadyLoaded`, `scheduler_loop.rs:268-272`); a door-loaded Spirit unloads through 16-1's `unload`; the daemon reads the manifest path itself (same host, same uid), canonicalised, and refuses a non-regular file.
- **AC3 (threat model, stated).** A bearer holder can already exec `maos run`, so `load` adds no privilege — but until **17-1** isolates Workers, every bare Worker runs as the operator uid and can read `<home>/control.json`; this story's docs say so while 17-1 is not `done`; its route adds no environment variable and no file a Worker could read beyond 16-1's, so 17-1's AC3 `MAOS_OPERATOR_` prefix probe covers it (⚠ R5 (Story 16-1 validation round 2, HEAD `f72b557b`): REPLACED a ship gate that was already satisfied when written).

## Dependencies

Epic 15 (15-2 allowances incl. maos-cli **+1100**, maos-journey-test +400, maos-domain +300 and the lane-wide maos-bin grant; 15-5 ADR-062 (D-A) superseding `crates/maos-control/src/lib.rs:68-76` and ADR-064 (D-D); 15-6 as amended for the shell consumer at `main.rs:3378` (⚠ re-pinned 2026-09-09; `:3247-3250` was stale) **and for the `MAOS_ONE_SHOT=hello-spirit` consumer at `:8092`**; 15-1 AC5 registering `MAOS_OPERATOR_BEARER_TOKEN`/`MAOS_OPERATOR_HTTP_BIND`). **16-0 first** (it opens the epic and BLOCKS 16-5 — the pin instrument is rebuilt before the story that moves the pin); 16-1 before 16-2 and 16-4; 16-3 after 16-1; 16-6 after 16-1 (⚠ R5 (Story 16-1 round-table 2026-09-13, operator-ratified)); 16-5 last.
