# Epic 18 — Spirits That Think (W3)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 18 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (15 of 15 applied; the `sprint-status.yaml` row creation inside #6 was done lane-wide, not by this file) and the decisions D-D (one replay selector, ADR-064) and D-E (Epic 18 is not a founder-loop dependency) are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`. §K #9's new `--deterministic` flag was not added: it contradicts D-D (unset `MAOS_INFERENCE_MODE` is the deterministic path).

**Wave / duration:** 5–8.3 weeks serial (4–6.5 on two tracks) — measured (scout §G; the prior 4–6 was stated) · **Makes true:** v0.3 Butler and v0.5 Researcher: cognition through the Inference Port, measured against a model, replayable in CI

**Closes:** S3 (Butler third; Observer/hook callers → 17-4); FR17; FR32 (floors 0.85/0.85 per `functional-requirements.md:77`); NFR-Test-4, NFR-Aud-7, NFR-Perf-6 with coverage-matrix rows (`tests/coverage-matrix.yaml:1879,973,1359` — all `gates: [] corpora: []` today); J-Butler, J-Researcher. FR30, FR55 and FR56 dropped from this epic: no AC cites them (FR55's 11-hook set is 17-4; 18-1 touches only `on_idle`).

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green):**

```
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_STRICT=1 MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json maos run spirits/butler/manifest.toml --once   # 1 — Butler's proposal is parsed from the recorded completion into the manifest's required output fields (pattern/confidence/evidence/options, spirits/butler/manifest.toml:57-60); an output_shape violation or cassette drift/exhaustion exits non-zero (today main.rs:4895 only logs); the belief_variance halt fires as at HEAD (jb3)
maos eval halt --class butler   # 2 — replays the 30 rows through the 18-1 seam with user_preference_drift taken from the recorded comms-triage completion; prints recall/precision; asserts ≥0.90 / ≥0.85 (the floors CI already holds, spirits/butler/tests/corpus_halt.rs:298,303); writes the signed benchmark sidecar the registry serves
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_STRICT=1 MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-researcher/survey-distill.json maos run spirits/researcher/manifest.toml --once   # 3 — an absent, exhausted or drifted cassette exits non-zero (the fallback at spirits/researcher/src/lib.rs:915-932 removed by 18-3 AC1)
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_STRICT=1 MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json maos run spirits/butler/manifest.toml --clock compressed:21d --acceptance tests/fixtures/j-butler/acceptance-21d.jsonl   # 4 — J-Butler's machine-checked leg: 21 compressed on_idle days from the acceptance fixture; the acceptance ledger declines and the self-tuning halt fires (18-4 AC1). Provenance: `--clock`, `--acceptance` and the fixture are created by 18-4 AC1 (parser worker_spawn.rs:53-66 accepts only --live/--once @9f920180); red at HEAD: unknown flag
```

**Exit-block provenance (dry-parsed @9f920180; `bash -n` clean; one replay selector per D-D/ADR-064):**

- Line 1 — `MAOS_INFERENCE_MODE` created by 15-6 AC1 / ADR-064 (`grep -rn MAOS_INFERENCE_MODE crates spirits xtask .github` = 0 @9f920180); `MAOS_REPLAY_STRICT` exists (`crates/maos-bin/src/main.rs:4658-4660`, `env_contract.rs:265`; ⚠ re-pinned 2026-09-09 by 15-6's preflight); `MAOS_REPLAY_CASSETTE` exists (`env_contract.rs:255`; read only by the Researcher arm `main.rs:4643`/`:4657`, whose selector head is `:4470` — its Butler-arm consumer is created by 18-1 AC1); `maos run <manifest> --once` exists (`crates/maos-bin/src/worker_spawn.rs:44-72`, `--once` `:55`); the cassette exists (`ls`; hand-authored seed `on-idle-halt.json:4`, wildcard prompt hash `:10`, zero consumers at HEAD); the manifest exists. **HEAD: exit 0, vacuous** — the Butler arm (`main.rs:4188-4360`) reads neither variable, the halt renders from the hard-coded seed (`:4218-4242`) and a violation only `eprintln!`s (`:4895`). Red until 15-6 wires the port and 18-1 AC1 sources the proposal from the cassette: the job asserts the rendered proposal equals the entry's `response.text` (`on-idle-halt.json:13`) and that the planted-violation twin exits non-zero.
- Line 2 — `maos eval halt --class butler` created by 18-2 AC1 (`grep -n '"eval"' crates/maos-bin/src/main.rs` = 0; `grep -n Eval crates/maos-cli/src/cli.rs` = 0). **HEAD: not a clean red** — `eval` is not `run`, so `parse_run_args` returns `None` (`worker_spawn.rs:44-48`) and argv falls through to daemon boot (`main.rs:1653-1656`, the `maos --help` defect): the job reds only by timeout. A clean non-zero and `check-exit-commands` resolution both arrive with 15-3 fork (a) (the `maos --help` verb table). Never run at HEAD.
- Line 3 — selector tokens as line 1; `MAOS_REPLAY_CASSETTE` IS read by the Researcher arm (`main.rs:4643`, `:4657-4665`); the cassette exists (`crates/maos-journey-test/tests/journey_researcher.rs:69-70` consumes it; seed, wildcard hash — the prior path `tests/cassettes/…` did not exist); the manifest exists. **HEAD: exit 0** on the happy path (the arm already replays this cassette). Its proven-red twin — the same line with an empty cassette — must exit non-zero after 18-3 AC1; at HEAD it exits 0 because the error falls into `summarize(statement)` (`spirits/researcher/src/lib.rs:930`).
- Line 4 — selector tokens as line 1; `--clock compressed:<dur>`, `--acceptance <path>` and `tests/fixtures/j-butler/acceptance-21d.jsonl` are **created by 18-4 AC1/AC2** (`ls tests/fixtures/` = `anthropic-skill anthropic-skill-invalid dirty-network-fixture wasm`, no `j-butler/`; the parser accepts only `--live`/`--once`/`--replay-llm`, `worker_spawn.rs:53-66`). **HEAD: red** — `maos run: unknown argument '--clock'` (`worker_spawn.rs:63-68`), exit 1. The `Clock` port is 18-4's and is re-used by 21-1.
- Shape: lines 1, 3 and 4 are `ENV=… command args # comment`; line 2 is a bare `maos` verb. No secret, key, human, wall-clock or network. The job runs the four lines plus the two twins; `cargo test -p butler` / `-p maos-journey-test` (`discipline.yml:730`, `:2128`) are green today and are not this job.

**Operator-lane legs (never in the exit; tracker rows `sprint-status.yaml:288-289`):**
- `ops-nightly-cassette-rerecord` — nightly `MAOS_INFERENCE_MODE=record` with a provider key (`tier-2-rerecord`, `journey-nightly.yml:65-99`; env renamed by 15-6 AC3) refreshes the 18-1/18-2/18-3 cassettes with `provenance: live-record`; `cassette-age-gate` (`xtask/src/main.rs:663-671`) green; the judged five-metric evidence (18-3 AC2) and 18-1 AC1's `=live` completion are observed here, never gating.
- `ops-google-calendar-oauth-mcp` — Google Calendar MCP with real OAuth for Butler (18-1 AC2 live leg); CI uses the conformant fixture server only.

**Kernel-Δ:** ZERO kernel-Δ @**24920** (`xtask/kernel-core-baseline.toml:505`; set `crates/maos-kernel-core/src`, 98 files, `check_kernel_baseline.rs:31`). ⚠ **Re-pinned 2026-09-20 by the Epic-16 retrospective (confidence rule 9); the former `@24477` / `:488` cite was stale and `check-epic-close-coherence` was RED on it.** Epic 16 moved the inherited baseline **four** times — 24474 → 24477 (16-3's operator-authorized optional-token repair, 2026-09-16) → 24628 (16-5, FLAG-Winston) → 24796 → 24923 (16-6, FLAG-Winston over four kernel files) → 24922 (E16-A1) → **24920** (v0.1.0-alpha.1 macOS rehearsal, FLAG-Winston 2026-09-25: one dead Linux-leftover line in `security/sandbox/macos.rs`) — a net **+449** against an epic that planned `+5–25` in one story. Epic 18 itself still assumes zero delta. 18-3 AC3's spike decides paused-time (0) vs a dispatcher `Clock` (+15–30, FLAG-Winston; D-F is standing permission for that story's measured ceiling-and-pin grant, not pre-granted capacity) before the story leaves backlog — ⚠ and `maos-kernel-core` is now at **exact-measured ZERO headroom** (`kloc.toml:247` = 19213, `RATIFIED_AT_EASING` `recovery_lane_ceiling_rule.rs:268` = 19_213), so that grant must move the ceiling, the pin and `RATIFIED_AT_EASING` in one commit; 18-4 drives `on_idle` from `maos-bin` (0; `scheduler/idle_watchdog.rs` untouched); 18-1/18-2 call the kernel and never change it.

**Kloc asks (for the 15-2 table; tokei @9f920180, ceilings `xtask/kloc.toml`):** `maos-bin` +250 named for Epic 18 inside the lane-wide +3000 (`kloc.toml:343`, 17109/17109) against a §E estimate of +300–680 (18-1 arm +80–150 · 18-2 verb +80–200 · 18-3 typed exit +40–80 · 18-4 scene +100–250) — the gap closes by keeping Butler/Researcher logic in `spirits/*` (ungoverned, `kloc_check.rs:184`) and the 18-2 driver in `maos-eval`; re-measure per story (a grant is a global). `maos-mcp` +300 (new row; `:281` 999/1100; need +80–150). `maos-journey-test` +400 (new row; `:488` 493/593; fixture server +150–300 after 16-2's +50–120). `maos-eval` within +600 (`:485` 3661/3696; halt driver +150–300, string metrics +100–200; the nightly judge harness +300–600 lands last and re-measures). `maos-spirit-sdk` +100 for the `Clock` port (`:202` ceiling 3000, tokei 839 code lines — fits, no row needed). `maos-domain` 0 (the port does not land there). `maos-kernel-core` 0. `xtask` 0 (the CI job is a plain `cargo test`, not an xtask gate). Aggregate stays red until 15-2 (`_aggregate_hardfail`, `kloc.toml:501`).

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree · **10 route by charter, never by bucket** (Epic-15 retrospective 2026-09-12, operator-ratified): a deferral names one of three destinations and never a default one — (a) **fix at origin** when the item is inside the story's own blast radius (the eased CEILING RULE removes the budget reason this was declined); (b) **the story that already touches that file**, which is the first question asked, not the last; (c) **a theme story (20-3a/20-3b, 21-4) only when the item matches that story's charter.** Rationale: Epic 14 closed early because nine of its eleven remaining rows were gates, registries, ledgers and ceilings; the Epic-15 retrospective measured this lane repeating it, with two items filed to `21-4` that had nothing to do with one instrument and the env registry · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `18-1-butler-inference-seam-and-conformant-mcp` | Butler inference seam and a conformant MCP client | S3 (Butler third) · FR17 · kernel-Δ 0 · maos-bin +80–150, maos-mcp +80–150, maos-journey-test +150–300 |
| `18-2-eval-halt-verb-and-corpus-numbers` | `maos eval halt` and model-dependent per-class numbers | NFR-Test-4 · kernel-Δ 0 · maos-bin +80–200, maos-eval +150–300 |
| `18-3-researcher-replay-live-and-judged-five-metric` | Researcher typed replay/live exits, hermetic five-metric, nightly judge and the AC3 spike | NFR-Aud-7 · NFR-Perf-6 · kernel-Δ 0 (AC3 spike: 0, else +15–30 FLAG-Winston) · maos-bin +40–80, maos-eval +100–200 (+300–600 nightly judge) |
| `18-4-self-tuning-halt-with-injectable-clock` | Self-tuning halt with an injectable clock | J-Butler · kernel-Δ 0 · maos-bin +100–250, maos-spirit-sdk +100 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs. Citations are `@9f920180` unless stated.

### 18-1-butler-inference-seam-and-conformant-mcp — Butler inference seam and a conformant MCP client

*Closes · Δ:* S3 (Butler third; Observer/hook callers → 17-4) · FR17 · kernel-Δ 0

- **AC1 (command).** Exit line 1. Butler's `on_idle` calls `InferencePort::complete` (sync: `crates/maos-bin/src/cassette_replay.rs:116`; Researcher calls it at `spirits/researcher/src/lib.rs:915`) through a NEW `Butler::with_deferred_inference_port` mirroring Researcher's (`spirits/researcher/src/lib.rs:502-509`; daemon deferred-port builder `main.rs:4590-4622`, binding backfill `:4796`). Butler has no inference field today (struct `spirits/butler/src/lib.rs:344-380`; `grep -c -i inference spirits/butler/src/lib.rs` = 0; builders are `with_scenario/with_scalar_port/with_output_channel/with_mcp_port`, `:508-539`). The Butler arm (`main.rs:4188-4360`) gains the replay/record arm the Researcher arm has (`:4623-4641`) under the 15-6 selector (ADR-064). The completion is parsed into the manifest's required output fields (`spirits/butler/manifest.toml:57-60`); the scalar the model sets is `user_preference_drift`, taken from the comms-triage completion (today an input field, `preference_alignment`, `lib.rs:636-645`; §H-4 resolved), while `belief_variance` stays arithmetic (`assess()` `:623-667`; seed `main.rs:4218-4242`). Reds: an `output_shape` violation exits non-zero (today `main.rs:4895` only `eprintln!`s); strict drift or exhaustion (`MAOS_REPLAY_STRICT=1`, `main.rs:4624-4626`) exits non-zero; proven-red = a cassette whose completion lacks a required field. Capability `provider.complete` is already declared (`manifest.toml:20-21`); the story verifies admission mints it for Butler (scout §C: unchecked at HEAD). `MAOS_INFERENCE_MODE=live` with a key producing a real completion is observed in `ops-nightly-cassette-rerecord`.
- **AC2.** The `maos-mcp` streamable-HTTP client (`crates/maos-mcp/src/transport/streamable_http.rs:13-51`; stateless `&self`; headers only `Content-Type`/`Accept`, `:32-35`; `grep -rn '"initialize"\|Mcp-Session-Id\|Authorization' crates/maos-mcp` = 0; stdio spawns per call, `transport/stdio.rs:24-37`) implements `initialize`, `Mcp-Session-Id` persistence across calls (interior mutability) and `Authorization: Bearer`, proven in CI against a NEW fixture MCP server in `crates/maos-journey-test` (the `MockMcp` listener pattern, `src/lib.rs:175-300`; today it returns the fixture body regardless of method, `:236-295`) that (a) rejects `tools/call` before `initialize`, (b) issues and requires `Mcp-Session-Id`, (c) returns 401 without `Authorization: Bearer`; the three negatives are asserted (proven-red) — kernel-Δ 0 (the `ureq` adapter already passes headers, `crates/maos-kernel-core/src/io/mod.rs:103-105`); `maos-mcp` +80–150 (15-2 row +300), `maos-journey-test` +150–300 (15-2 row +400). Google OAuth: `ops-google-calendar-oauth-mcp`.
- **AC3.** FR17 morning digest produced on the live path: `morning_digest` (`spirits/butler/src/lib.rs:675-682`) gets a production caller in the Butler arm (callers today: `crates/maos-bench/src/harness/j0.rs:83`, `journey_butler.rs:153`, `spirits/butler/tests/{hallucination.rs:152,digest.rs:122}` — none in `maos-bin`); `audit_db`/`journal_path` come from the daemon, `now_ns` from the 18-4 `Clock` port once it lands (`SystemTime` until then). D-E: 19-4's daemon render is independent of this AC; no edge to Epic 19.
- **AC4.** The retired model pin `spirits/butler/manifest.toml:21` (`anthropic.claude-3-haiku-20240307`) is gone; depends on tracker row `model-pin-currency-gate-and-retired-pin-sweep` (`sprint-status.yaml:223`, backlog); the sweep also covers `spirits/researcher/manifest.toml:47`.

- **AC5 (received from Story 15-1's cut lines, Epic-15 retrospective 2026-09-12 — re-routed under the ratified routing rule).**
  `spawn_and_invoke`'s fatal write-side EPIPE (`crates/maos-mcp/src/transport/stdio.rs:30`) is handled: a one-shot NDJSON server that
  exits before the request is fully written must surface a typed transport error, not a fatal write. 15-1 filed this to **21-4**, which
  was wrong on charter — 21-4 is instrument unification and does not touch `maos-mcp`; **this story already cites `stdio.rs:24-37`** for
  the conformant-MCP work, so the file is open here anyway. 15-1 fixed only the *fixture race* that made the suite red; the underlying
  robustness question is this AC.

### 18-2-eval-halt-verb-and-corpus-numbers — `maos eval halt` and model-dependent per-class numbers

*Closes · Δ:* NFR-Test-4 · kernel-Δ 0

- **AC1 (command).** Exit line 2. `maos eval halt --class butler` is a NEW verb in `maos-bin`'s hand parser (`main.rs:1653`; `grep -n '"eval"' crates/maos-bin/src/main.rs` = 0; `grep -n Eval crates/maos-cli/src/cli.rs` = 0), hosted there because it must compose the Butler arm + `ButlerOrchestratorAdapter` (`main.rs:4198-4210`) that `spirits/butler/tests/corpus_halt.rs:73-139` today builds from `butler`'s dev-dependency on `maos-kernel-core` (`spirits/butler/Cargo.toml:35`) — §H-2 resolved; it is listed in the 15-3 fork (a) `maos --help` verb table so `check-exit-commands` resolves it. It replays the 30-row corpus (`spirits/butler/tests/fixtures/calendar-comms-v0.3.jsonl`, `wc -l` = 30; `BUTLER_CORPUS_REL` `crates/maos-eval/src/onboarding_gate_corpus.rs:79`; size lock `corpus_halt.rs:160`) through the 18-1 seam with `user_preference_drift` taken per row from the recorded comms-triage completion, prints recall/precision and asserts ≥0.90/≥0.85 (NFR-Test-4: the number CI already asserts at `corpus_halt.rs:296-305` through `HALT_RECALL_FLOOR`/`HALT_PRECISION_FLOOR` `onboarding_gate_corpus.rs:60,62` becomes model-dependent and published; the PRD's 0.7 `non-functional-requirements.md:81` is looser than the shipped floor, which binds). What reds: a cassette set whose completions drive precision below 0.85 (proven-red fixture), and a run in which no completion was consumed (the arithmetic-only path — today's number again) exits non-zero. The numbers are written to a signed benchmark sidecar in `maos-registry` storage (`crates/maos-registry/src/storage.rs`), readable by `maosctl` through a read verb named in the story (§H-3 resolved: sidecar — not ComplianceClaim, frozen at E1b, nor manifest `[benchmarks]`; `grep -rn halt_recall crates/maos-registry/src` = 0 today); the `tests/coverage-matrix.yaml:1879` NFR-Test-4 row gains this gate and `gate-registry.toml` the entry (`check-coverage-matrix-completeness`, `xtask/gate-registry.toml:58`).
- **AC2.** The corpus gains model I/O fields (prompt, completion reference = cassette path + `sequence`; rows carry none today — row `cc01`: `calendar_conflict, expected_halt, input{calendar,comms}, observed_halt, scenario_id`); 30 rows need 30 cassette entries (the port is sequence-keyed, `cassette_replay.rs:117-120`); each cassette's provenance is recorded (`model_id`, `recorded_at`; seeds carry `provenance: seed` per 15-6 AC2); the `corpus_pin` SHA-256 tests (`discipline.yml:727-729`, `cargo test -p butler`) are re-pinned in the same commit.
- **AC3.** The nightly re-record leg refreshes the cassettes: `ops-nightly-cassette-rerecord`.

### 18-3-researcher-replay-live-and-judged-five-metric — Researcher typed replay/live exits, hermetic five-metric, nightly judge and the AC3 spike

*Closes · Δ:* NFR-Aud-7 · NFR-Perf-6 · kernel-Δ 0 (AC3 spike decides 0 vs +15–30 FLAG-Winston)

- **AC1 (command).** Exit line 3 runs from a cassette under the one selector (D-D / ADR-064: `MAOS_INFERENCE_MODE={record,replay,live}` + `MAOS_REPLAY_CASSETTE`; no `--replay` and no `--deterministic` flag — unset `MAOS_INFERENCE_MODE` is today's deterministic path, `main.rs:4642-4645`; the `--replay-llm` no-op `worker_spawn.rs:56-59` is retired by 15-6). The runtime fallback `summarize(statement)` at `spirits/researcher/src/lib.rs:930` (`:915-932`: `Unconfigured`/`CapabilityDenied` are logged `:921-926`, every other error is silent `:927`, all arms fall back) is removed so that any `InferenceError` under `live` or `replay` is a typed non-zero exit (boot-time `Unconfigured` already exists at `main.rs:4590-4596`; `InferenceError::Unconfigured` `lib.rs:922`); the existing `--once` sites (`grep -rn -- '"--once"' crates spirits xtask | wc -l` = 36 @9f920180) stay on the deterministic default. Proven-red: the exit-line-3 twin with an empty cassette exits non-zero (it exits 0 at HEAD).
- **AC2.** Hermetic leg (CI): deterministic string metrics — recall of source-frame claims, traceability, secret-leakage — over a FROZEN distillate corpus replace the label means at `crates/maos-eval/tests/distillate_five_metrics_floor.rs:53-74` (asserts `:76-88`; job `discipline.yml:1131`); the corpus is regenerated only by the nightly leg, with a re-pin in the same commit. Judged faithfulness/hedge run only in `ops-nightly-cassette-rerecord` as reported evidence per 8.11 FORK A (`spirits/researcher/tests/five_metric_live_8_11.rs:20-26`; `grep -rn judge crates/maos-eval/src` = 0 — the judge harness belongs to the nightly lane and is never a CI floor, because a judge cassette keyed by the judge prompt's sha256 (`cassette_replay.rs:127-133`) is coherent only over a frozen distillate); the `tests/coverage-matrix.yaml:973` NFR-Aud-7 row gains the hermetic gate.
- **AC3 (spike, ≤1 d, rule 5).** Can `crates/maos-kernel-core/src/scheduler/hook_dispatch.rs:574-643` (`tokio::time::timeout` `:574`, `spawn_blocking` `:576`, the 80 % sleep `:592`, `BudgetWarning80` `:639`; cap lookup `effective_cap_seconds` `:360-367`) be driven by tokio paused time under `spawn_blocking`? If yes: add a paused-time variant of `crates/maos-kernel-core/tests/hook_dispatch_budget_envelope.rs:189-225` (today wall-clock, 900 ms hook vs 1 s cap) for the Researcher cap (`spirits/researcher/manifest.toml:90`, `time_cap_seconds = 60`; Butler `manifest.toml:65` = 30; cite the PRD line if the cap changes — NFR-Perf-6 `non-functional-requirements.md:14`). If no: cut this AC (the envelope is already proven) and file the dispatcher `Clock` trait as FLAG-Winston (+15–30 kernel lines, outside this epic). Either way the `tests/coverage-matrix.yaml:1359` NFR-Perf-6 row gains the envelope test. The prior `5400` had no source (`grep -rn 5400 crates spirits xtask docs _bmad-output/planning-artifacts/prd` = 0) and is deleted.

### 18-4-self-tuning-halt-with-injectable-clock — Self-tuning halt with an injectable clock

*Closes · Δ:* J-Butler · kernel-Δ 0

- **AC1 (command).** `MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j-butler/on-idle-halt.json maos run spirits/butler/manifest.toml --clock compressed:21d --acceptance tests/fixtures/j-butler/acceptance-21d.jsonl` — both flags and the fixture are NEW (the parser `worker_spawn.rs:53-66` accepts only `--live` (`:54`), `--once` (`:55`) and `--replay-llm` (`:59`, an accepted no-op that D-D/ADR-064 retires); `tests/fixtures/j-butler/` does not exist, the root `tests/fixtures/` does). `--clock` drives N `fire_on_idle` passes from `maos-bin` at the compressed cadence (one pass per simulated day, 21 days), then drains — kernel-Δ 0, `scheduler/idle_watchdog.rs:44-58` untouched (`MAOS_IDLE_FAST` `:46-49` is a ÷100 test knob, not a scenario clock; `--once` is a single pass + drain, `worker_spawn.rs:37-38`, `main.rs:3341`, and cannot show three weeks, so `--clock` replaces it on this line). It prints the acceptance ledger per simulated day and the tuning halt on the day the rule crosses. Tuning rule (stated): metric = rolling 7-simulated-day acceptance rate (option picks accepted ÷ notifications offered, sourced from `handle_option_pick`, `spirits/butler/src/lib.rs:551`); window = 7 days; threshold = rate < 0.6 — the rate feeds `preference_alignment` (`lib.rs:636-645`; the lower of the ledger rate and the 18-1 completion's value), so the existing `user_preference_drift` `on_value_below 0.6` rule (`spirits/butler/manifest.toml:92-94`) is the tuning halt and no new epistemic rule is added. Proven-red: a scripted fixture whose acceptance never declines must not halt (a halt reds the leg), and the declining fixture must halt by day 21 (no halt reds).
- **AC2.** NEW `AcceptanceLedger` written as Transparency-Log frames (§H-5 resolved: TL per the input package `correct-course-input-2026-09-04-confidence.md:51`; I2 log-before-deliver; queryable via `maos_audit::query`) from `handle_option_pick` (`spirits/butler/src/lib.rs:551`) — under replay the pick sequence is the `--acceptance` fixture; FR31 principal-namespace persistence is deferred until a Spirit-facing principal write port exists (only the kernel `memory/principal.rs:33` today; `grep -rn notification_acceptance_log crates spirits xtask` = 0). NEW `Clock` port in `maos-spirit-sdk` (§H-6 resolved; ceiling 3000 `kloc.toml:202`, tokei 839; deps `maos-spirit-abi`/`maos-spirit-derive` only, so no kernel edge; `CohortClock` `crates/maos-cohort/src/state.rs:25` is the only clock trait today and `maos-domain/src/ports/` has none) consumed by Butler and re-used by 21-1's scenario clock; `chrono_stub_snooze_time` (`lib.rs:831-843`; `SystemTime::now()` at `:834` — a display stub, not a cadence; Butler has no clock consumer: `grep -c -i clock spirits/butler/src/lib.rs` = 0) and 18-1 AC3's `now_ns` read it. Not deliverables of this story: `crates/maos-journey-test/tests/jb3_self_tuning_halt.rs:54` is NOT ignored (green in `journey-hermetic-tier-1`, `discipline.yml:2128`), and `journey_butler.rs:285` is `jb8_posture_shift_cognition` (`#[ignore = "RED: Epic 9 — …"]`, Epic 9 scope) — 21-2 AC4 must name another owner for it.

## Dependencies

15-2 (`maos-bin` +250 named for Epic 18, `maos-mcp` +300, `maos-journey-test` +400, `maos-eval` within +600), 15-3 fork (a) (the `maos --help` verb table, so `check-exit-commands` resolves `maos eval`), 15-6 as amended (ADR-064: `MAOS_INFERENCE_MODE` + `MAOS_REPLAY_CASSETTE`, `--replay-llm` retired), and the hotfix row `model-pin-currency-gate-and-retired-pin-sweep` (`sprint-status.yaml:223`; `provider-model-env-overrides` `:224` is its W3 companion, not a blocker). Order: 18-1 → 18-2 (seam, verb); 18-1 → 18-4 (builder and the `preference_alignment` source); 18-3 → 18-4 (the AC3 spike's outcome fixes how far the `Clock` port must reach); 18-3 ∥ 18-1 (Researcher-only). 21-1 consumes 18-4's `Clock`. Dropped: Epic 16 (no AC pauses the daemon; every exit line is `--once`); Epic 19 does not depend on this epic (D-E).
