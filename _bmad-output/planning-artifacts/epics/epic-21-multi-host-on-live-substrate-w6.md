# Epic 21 — Multi-Host on Live Substrate (W6)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 20 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (22 of 22 applied) and the decisions D-D (one replay selector, ADR-064), D-I (the env registry stays a kloc-free data file) and D-J (a named leaf crate for the orchestrator move, ordered after 21-4) are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`.

**Wave / duration:** 7.8–12.6 weeks serial (5.5–8.5 with 21-2 ∥ 21-3 ∥ 21-4; 21-5 adds 1.3–2.0) — measured from analogues (R2 note §G: 14-2a `ee3422d0`, 14-2b `4657cace`, 14-0 `38c52811`, 13-6c `c571a2b9`, 12-6 `5767cf0d`, 10-4b `3f65b6a8`; +30 %) · **Makes true:** v1.5 J4 and v2.2 J3 + Reza exercised by the journeys they were built for; identity durable across restarts; one kernel instrument

**Closes:** J3/J4/Reza fixture-by-default; FR23b (part), NFR-Sec-13 own-leaf (RELEASE-HOLDS 14-2 clause (c.2), `RELEASE-HOLDS.md:76-110`), RELEASE-HOLDS row 12 (`RELEASE-HOLDS.md:59`; NFR-Rel-6 semantics preserved — `crates/maos-a2a/tests/restart_invalidates_pin_nfr_rel_6.rs` stays green), NFR-Maint-1 instrument; D4a/b/c, D11 (E1/E2/E3), D13(b); D1 → 20-3 (`sprint-change-proposal-2026-09-04-round2.md:86`), D14 → 15-1/15-2 (the +51 is absorbed there); the unregistered `MAOS_*` env reads (54 in `src/`, 63 incl. tests, 15 of the 16 kernel-core names — grep in 21-4 AC2)

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green):**

```
cargo run -p xtask -- demo-reza                              # 1 — the 3-team/3-region Reza scene (xtask/src/main.rs:1012; demo_reza.rs:66): substrate probe, the 13.6b journey --include-ignored (demo_reza.rs:231-245), four gates, ledger verdict
cargo test -p maos-journey-test --test ignored_owner_audit   # 2 — every #[ignore = "..."] under crates/maos-journey-test/tests carries owner:<key> (21-2 AC4)
```

**Exit-block provenance (dry-parsed @9f920180; `bash -n` clean):**
- Line 1 — `demo-reza` exists @9f920180 (`xtask/src/main.rs:1012-1019`, clap; flags `--provision`, `--skip-gates`, `--journey-only`). **HEAD: red** — with no Postgres substrate `preflight` returns `false` (`psql` absent → `xtask/src/demo_reza.rs:139`; databases absent → `:71-81`) and the run exits non-zero with "substrate incomplete"; no workflow invokes it (`grep -rn demo-reza .github/workflows` = 0). The CI job (21-2 AC1) supplies the `services:` block + `provision-loom-substrate`; green ⇔ the journey and the four gates pass on that substrate.
- Line 2 — `ignored_owner_audit` created by 21-2 AC4 (`ls crates/maos-journey-test/tests` @9f920180 has no such target). **HEAD: red** — `cargo test` exits non-zero ("no test target named `ignored_owner_audit`"). After 21-2 it reds on any `#[ignore]` without an `owner:` token; today's two (`grep -n '#\[ignore' crates/maos-journey-test/tests/*.rs`): `journey_j4.rs:117` (→ 21-1 AC3) and `journey_butler.rs:285` (`jb8_posture_shift_cognition`, Epic-9 residual → `deferred-work.md` row filed by 21-2 AC4, NOT 18-4).
- Shape: two `cargo` lines with trailing `#` comments; no secret, key, human, wall-clock or network (Postgres is a `services:` container). `check-exit-commands` (15-3) resolves both tokens against `xtask --help` / `ls crates/<crate>/tests`. 21-1's `maos run` line lives in its AC1, not here: it is blocked on the operator row below and on 18-4.

**Operator-lane legs (never in the exit; tracker row `sprint-status.yaml:291`):**
- `ops-j4-live-incident-recording` — record the 50-incident cassette with a provider key (≥300 calls per run: 50 incidents × Mira diagnose + Nash architect + retries); PRODUCES `crates/maos-journey-test/cassettes/j4/mira-nash-50.json` (`provenance: live-record`) that 21-1 AC1 replays. 21-1 is blocked until the row is `done`; the hermetic leg never calls a provider.

**Kernel-Δ:** ZERO kernel-Δ lines for 21-1..21-4 (set `crates/maos-kernel-core/src`, `xtask/src/check_kernel_baseline.rs:31`; pin 24472 in `xtask/kernel-core-baseline.toml`, 24474 after 15-1's D-H re-pin); 21-4 AC1 rewrites the PIN VALUE without a kernel line (instrument change physical → tokei code, 18933 @9f920180); **21-5 is FLAG-Winston and negative** — −817 tokei code / −1028 physical (`tokei crates/maos-kernel-core/src/orchestrator/`) plus +2–6 port-type lines at `crates/maos-kernel-core/src/scheduler/scheduler_loop.rs:89,111`, re-pinned in tokei units because 21-4 lands first, with the `maos-kernel-core` ceiling (`xtask/kloc.toml:195`) LOWERED by the same amount (ADR-038).

**Kloc asks (for the 15-2 table; tokei @9f920180, ceilings `xtask/kloc.toml`):** `maos-a2a-core` +30–60 (`:319` 4856/4856, zero headroom; `SyncPinLookup` sub-trait +20–40, durable-store hooks +10–20; within 15-2's +150 after 16-5's +20–40). `maos-a2a-tcp` +50–140 (`:322` 1439/1500; `Arc<dyn …>` re-typing across the 9 concrete sites +20–60, listen-side SAN scoping +30–80 (ADR-063's binding choice); within +150). `maos-persistence` +300–600 (`:326` 1/2000; `FilePinStore`; no grant needed). `maos-control` +40–80 (`:329` 803/1500; `PinsSource` port + route; within headroom). `maos-cohort` +150–300 (`:482` 5857/5857; 14-2d port + deadline rule + accessor — EXCEEDS 15-2's +100; the remainder is 14-2d T8's own measured grant, taken at 21-3's commit). `maos-bin` gross +380–760 (`:343` 17109/17109; 21-1 flag/adapter/router/seams +250–500, 21-3 actuator + `PinsSource` impl +100–200, 21-4 boot check +30–60) less −479 when the registry leaves under D-I → net −100…+280 inside 15-2's lane-wide `maos-bin` row; Mira/Nash logic stays in ungoverned `spirits/*` (`kloc_check.rs:183-184`). `maos-domain` +48–78 (`:327` ceiling 9192 over the 8695 HEAD baseline, +497 free; 21-3 +3 variant, 21-4 +15 `EnvVar`/`EnvStability`, 21-5 +30–60 port; D14 is closed). `xtask` +180–410 (`:212` 41953/41953; 21-2 drift-control enrollment +30–60, 21-4 scanner root + per-crate proven-red +100–200 and instrument unification +50–150) against the +400 named row that Epic 19's +150 also draws — over-subscribed by up to ~190 at the top of the range; 21-4 states its measured figure before leaving backlog. `maos-journey-test` 0 (`ignored_owner_audit` and the fixtures sit under `tests/`, uncharged `kloc_check.rs:175-176`). `maos-spirit-sdk` 0 here (18-4's +100 builds the `Clock`). NEW row `maos-orchestrator-buffer = 917` (21-5: 817 measured + max(100, ceil 2 %)). `maos-kernel-core` 0 for 21-1..21-4; −817 with the ceiling lowered for 21-5. The aggregate (`_aggregate_hardfail`, RED 155498/147057 @HEAD) is 15-2's.

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree · **10 route by charter, never by bucket** (Epic-15 retrospective 2026-09-12, operator-ratified): a deferral names one of three destinations and never a default one — (a) **fix at origin** when the item is inside the story's own blast radius (the eased CEILING RULE removes the budget reason this was declined); (b) **the story that already touches that file**, which is the first question asked, not the last; (c) **a theme story (20-3a/20-3b, 21-4) only when the item matches that story's charter.** Rationale: Epic 14 closed early because nine of its eleven remaining rows were gates, registries, ledgers and ceilings; the Epic-15 retrospective measured this lane repeating it, with two items filed to `21-4` that had nothing to do with one instrument and the env registry · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `21-1-j4-incident-fixture-and-live-mira-nash` | J4 incident fixture and live Mira/Nash (after 18-4 and `ops-j4-live-incident-recording`) | J4 · NFR-Test-4 · kernel-Δ 0 · maos-bin +250–500 · spirits/* ungoverned |
| `21-2-nightly-postgres-and-ignored-journeys` | Nightly `demo-reza` scene, capture regenerate-and-diff, ignored-owner audit | J3/Reza · NFR-Scale-5 · kernel-Δ 0 · xtask +30–60 · maos-journey-test 0 |
| `21-3-durable-tofu-and-self-leaf-rotation` | Durable TOFU (`GET /v1/a2a/pins`) and self-leaf rotation (absorbs 14-2d) | RELEASE-HOLDS row 12, 14-2 (c.2) · kernel-Δ 0 · maos-a2a-core +30–60 · maos-a2a-tcp +50–140 · maos-persistence +300–600 · maos-control +40–80 · maos-cohort +150–300 · maos-bin +100–200 · maos-domain +3 |
| `21-4-one-instrument-and-env-registry` | One instrument and the env registry (D-I data file) | D4a/D4b/D4c/D11/D13(b) · kernel-Δ 0 lines (pin re-valued to tokei) · xtask +150–350 · maos-domain +15 · maos-bin −479 / +30–60 |
| `21-5-orchestrator-port-and-move` | Optional: orchestrator port and move to `maos-orchestrator-buffer` ◆ FLAG-Winston (negative; after 21-4) | §4.0.7 "does NOT embed an orchestration policy" · kernel-Δ −817 tokei (−1028 physical) · maos-domain +30–60 · new crate row 917 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 21-1-j4-incident-fixture-and-live-mira-nash — J4 incident fixture and live Mira/Nash

*Closes · Δ:* J4 · NFR-Test-4 · kernel-Δ 0 (all in `spirits/`, `maos-bin`, `maos-journey-test`; the scenario clock never touches `scheduler/idle_watchdog.rs`)

- **AC1 (command).** `MAOS_INFERENCE_MODE=replay MAOS_REPLAY_STRICT=1 MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j4/mira-nash-50.json maos run spirits/topologies/j4-mira-nash.toml --incidents crates/maos-journey-test/fixtures/j4/incidents-50.jsonl --clock compressed:90m` resolves the 50 incidents from recorded model turns under a compressed scenario clock and prints `closed=N/50 consent_upheld=M/50`. Provenance @9f920180: `MAOS_INFERENCE_MODE` is created by 15-6 AC1 / ADR-064 (D-D — the one replay selector; `crates/maos-bin/src/env_contract.rs` has only `MAOS_REPLAY_CASSETTE` `:255` and `MAOS_REPLAY_STRICT` `:265`); `--clock compressed:<dur>` and the `Clock` port in `maos-spirit-sdk` are created by 18-4 AC1/AC2 and RE-USED here (one mechanism; edge 18-4 → 21-1); `--incidents <path>` is NEW in this AC (`crates/maos-bin/src/worker_spawn.rs:53-66` accepts only `--live`/`--once`/`--replay-llm` and errors on anything else `:63-66`; rows feed `Mira::with_pending_signals`, `spirits/mira/src/lib.rs:241`, the only signal inlet — the daemon loads `Mira::default()` with no signals, `main.rs:3827`); the 50-row fixture `crates/maos-journey-test/fixtures/j4/incidents-50.jsonl` is NEW in this AC (`ls crates/maos-journey-test/fixtures` = `j3` only; one `AnomalySignal` per row — `subject, metric, observed, baseline, detail`, `lib.rs:87-98`); the cassette is PRODUCED by `ops-j4-live-incident-recording` (`ls crates/maos-journey-test/cassettes` = `j-butler j-researcher j0`; Mira and Nash declare NO inference today, `spirits/mira/manifest.toml:16-17`, `spirits/nash/manifest.toml:14-15`, so the turns must be recorded before they can be replayed); the scene loop — one `on_idle` pass per simulated minute for 90 simulated minutes, then drain — lives in `maos-bin`, never in `crates/maos-kernel-core/src/scheduler/idle_watchdog.rs` (`--once` is a single pass + drain, `worker_spawn.rs:37-38`, and cannot show 90 minutes, so `--clock` replaces it on this line as on 18-4's). closed ⇔ Nash's `ArchitectureProposal` for the incident has `subject == incident.subject` and a non-empty `proposed_fix` (`spirits/nash/src/lib.rs:53-59`, read via `last_proposals`, `:140`) within 90 simulated minutes (PRD close = incident-to-deployed-fix ≤ 90 min, `prd/product-scope.md:67`); consent_upheld ⇔ the advisory crossed under an intent in Nash's `accept_allowlist` with no kind-22 `ConsentRupture` row for that incident. Asserts N ≥ 45 and M ≥ 48 (`prd/project-scoping-phased-development.md:226`: "≥ 45/50 close in ≤ 90 min; ≥ 48/50 uphold typed-intent consent envelope"). Proven-red: a cassette whose Nash turns never propose → N = 0 → non-zero exit; `MAOS_REPLAY_STRICT=1` reds on cassette drift or exhaustion.
- **AC2.** A prompt→`AnomalySignal` adapter feeds Mira (`spirits/mira/src/lib.rs:274` `diagnose(&AnomalySignal)` takes a signal, not a prompt); Mira and Nash gain inference seams (`grep -cE 'Inference|provider' spirits/mira/src/lib.rs spirits/nash/src/lib.rs` = 0): manifests gain `[capabilities.required] provider.complete` (the section Butler already declares, `spirits/butler/manifest.toml:20-21`, so no manifest-schema change; the Decision-I smoke assertions `spirits/mira/tests/spirit_smoke.rs:99-102,128-131` — "declares no `[capabilities.required]`" — are inverted, and the manifest comments `spirits/mira/manifest.toml:16-17`, `spirits/nash/manifest.toml:14-15` updated); the daemon's `LoadedSpiritKind::Mira|Nash` arms (`main.rs:3797-3847`; today Mira gets only the `EpistemicScalarPort` adapter `:3827-3829`) wire the deferred inference port as 18-1 does for Butler (Researcher's deferred-port builder is the template, `main.rs:4590-4622`) under the 15-6 selector; provenance: `spirits/mira/manifest.toml:67-68` (`covered_model_id = "maos.mira.deterministic-diagnostic-v1"`) updated to the recorded model, and `spirits/nash/manifest.toml` gains a `[model_provenance]` section (it has none). Spirit code is uncharged (`kloc_check.rs:183-184` `-e spirits`).
- **AC3.** The `maos run` topology path routes Mira's advisory to Nash through an in-process `LoopbackA2ARouter` — NEW in the topology loader: today `main.rs:3797-3847` loads both Spirits into ONE scheduler with no router between them, and every `LoopbackA2ARouter`/`handle_intake` site in `main.rs` is a smoke function (`:10786,:11040,:11227,:11303`); the template is the fine-grained-consent smoke `main.rs:10969-11040` (`ADMIT_INTENT = "diagnosis-handoff:read-only-evidence"`, `DENY_INTENT = "code-mutation-directive"`; §H-4 resolved: loopback in-process, the J3 daemons already prove TCP). Peer configs come from a NEW `[[topology.a2a_peers]]` section of the topology file (the parser rejects unknown keys, `spirits/topologies/j4-mira-nash.toml:4-5`; allowlists are not manifest fields, `spirits/nash/manifest.toml:16-17`) carrying each Spirit's `accept_allowlist`. A second topology `spirits/topologies/j4-mira-nash-deny.toml`, whose Nash allowlist excludes the advisory intent, produces the kind-22 `RuptureReason::IntentAllowlistMismatch` row (`crates/maos-domain/src/frame.rs:386`) through `A2ARouterCore::handle_intake`; `journey_j4.rs:117` (`j4_earned_consent_rupture_typed_oracle_deferred`, `:119`) is un-ignored against it. Accept path first, deny path second.
- **AC4.** `safety_critical_corpus_8_5` (`crates/maos-eval/tests/safety_critical_corpus_8_5.rs` over `tests/corpora/safety-critical-mira-nash-v1.5.jsonl`, 300 rows, `tests/corpora/MANIFEST.toml:73-76`, κ = 0.83) stays green and is NOT the incident corpus; the 50-row fixture cites `MANIFEST.toml` provenance for its 50 subjects.

### 21-2-nightly-postgres-and-ignored-journeys — Nightly `demo-reza` scene, capture regenerate-and-diff, ignored-owner audit

*Closes · Δ:* J3/Reza · NFR-Scale-5 · kernel-Δ 0 (YAML, one xtask route-table entry, one in-crate test)

- **AC1 (command).** Exit line 1 in CI: `.github/workflows/journey-nightly.yml` (no `services:` today — `services:` exist only at `discipline.yml:2930,2995,3077,3130` and `rpo-rto-cadence.yml:46`) gains a `reza-nightly` job with a `services:` block byte-identical (modulo `POSTGRES_DB`) to `discipline.yml:2930-2943`, `./.github/actions/provision-loom-substrate` (composite, 13.6c; `action.yml:6-11`: it cannot define `services:`) with `extra-databases: maos_team_b maos_team_c` (the `check-multi-tenant-loom` shape, `discipline.yml:3104-3108`), `postgresql-client` installed (`demo_reza.rs:139` needs `psql`), and runs `cargo run -p xtask -- demo-reza --skip-gates` as the narrated nightly scene. What the nightly adds is the end-to-end narrated run, NOT coverage: the four `#[ignore]` tests in `crates/maos-bin/tests/cross_team_crossing_13_6b.rs` (`:1641,:1837,:2354,:2810`) and the five mechanism tests in `crates/maos-a2a-tcp/tests/t_12_4a_digest_read.rs` (`:298-526`) ALREADY run per commit as legs of `check-multi-tenant-loom` / `check-loom-substrate-drift` (`discipline.yml:3077-3120`, three team DBs, `TEAM_A/B/C` exported `:3120-3123`) and `check-cohort-mesh` (`discipline.yml:3054`; `xtask/src/check_cohort_mesh.rs:435-505`, `--ignored --exact`). No bare `--run-ignored` (nextest requires `only|all`; `demo-reza` uses cargo-test `--include-ignored`, `demo_reza.rs:231-245`). Proven-red: delete the `services:` block → `demo-reza` exits non-zero ("substrate incomplete", `demo_reza.rs:71-81`). 15-6 AC3 owns `journey-nightly.yml:94`'s invalid `--live`; this story must not re-introduce it.
- **AC2.** Regenerate-and-diff (§H-5 resolved): the nightly runs `t_12_4b_capture_j3_raw_inputs` (`cargo test -p maos-a2a-tcp --test t_12_4a_digest_read t_12_4b_capture_j3_raw_inputs -- --exact --ignored`) with `MAOS_CAPTURE_J3_FIXTURE=$RUNNER_TEMP/day-30-raw.json` — the capture stays `#[ignore]` (`t_12_4a_digest_read.rs:593-596`: it `expect`s the env var and WRITES a file; in-process N=8 mTLS mesh, no Postgres) — and `diff`s the output against the committed `crates/maos-journey-test/fixtures/j3/day-30-raw.json`; drift is a nightly red with the diff attached as an artifact. `journey_j3` keeps consuming the committed fixture (`crates/maos-journey-test/tests/journey_j3.rs:24,119,166` — 12.4b's fixture-by-default design; the capture is "the only producer of the committed J3 raw-input fixture", `t_12_4a_digest_read.rs:586-591`). Nothing is un-ignored.
- **AC3.** The nightly job's `services:` block is enrolled in the drift control: `xtask/src/check_loom_substrate_drift.rs` reads `discipline.yml` only (`WORKFLOW` `:66`; route table `:174-209` names four jobs), so it gains `journey-nightly.yml` as a second workflow and the `reza-nightly` route (xtask +30–60); proven-red = a one-byte change to the nightly block's `POSTGRES_PASSWORD`. (The former AC3 — "`tl-phase-b` leg Blocking with the Postgres job" — is DELETED: it is already binding in CI. `AdvisorySubstrate` hard-fails when `MAOS_TEST_POSTGRES_TEAM_A/B` are present, `xtask/src/gate_common.rs:216-233`; the leg is `check_reza_production_path.rs:456-457`; the job exports both, `discipline.yml:3182-3184`. Flipping the word would only red a laptop without Postgres.)
- **AC4.** Exit line 2: NEW `crates/maos-journey-test/tests/ignored_owner_audit.rs` (an in-crate `cargo test`, uncharged under `kloc_check.rs:175-176` `-e tests`; no xtask gate — §H-6 resolved) reads its own `tests/*.rs` for `#[ignore = "…"]` and reds any attribute without an `owner:<key>` token, where `<key>` is a `sprint-status.yaml` key or a `deferred-work.md` anchor. Today's two (`grep -n '#\[ignore' crates/maos-journey-test/tests/*.rs`): `journey_j4.rs:117` → `owner:21-1-j4-incident-fixture-and-live-mira-nash` (un-ignored by 21-1 AC3); `journey_butler.rs:285` = `jb8_posture_shift_cognition` (`#[ignore = "RED: Epic 9 — posture-shift cognition not yet wired"]`, `:284-289`) — an Epic-9 residual, NOT 18-4 (Epic-18's 18-4 AC2 disowns it): 21-2 files it as a `deferred-work.md` row `jb8-posture-shift-cognition` (no tracker key exists: `grep -n posture-shift sprint-status.yaml` = 0) and stamps `owner:deferred-work#jb8-posture-shift-cognition`; it stays ignored.

### 21-3-durable-tofu-and-self-leaf-rotation — Durable TOFU (`GET /v1/a2a/pins`) and self-leaf rotation (absorbs 14-2d)

*Closes · Δ:* RELEASE-HOLDS row 12 (`RELEASE-HOLDS.md:59`), 14-2 clause (c)/(c.2) (`:76-110`; NFR-Rel-6 semantics preserved) · kernel-Δ 0 (crates touched: `maos-persistence`, `maos-a2a-core`, `maos-a2a-tcp`, `maos-cohort`, `maos-bin`, `maos-control`, `maos-domain` — none under `crates/maos-kernel-core/src`)

- **AC1 (command).** NEW `GET /v1/a2a/pins` in `maos-control` (behind the same bearer check as `/v1/a2a/rotation-windows`, `crates/maos-control/src/lib.rs:285-297`, route `:299`; the previously cited `/v1/a2a/peer-versions` does NOT exist — `/v1/cohort/peer-versions` at `:347` returns manifest-version rows `peer, declared_version, declared_hash, observed_at_secs, valid, state` with NO fingerprint, and `/v1/cohort/self-identity` `:395` reports the local leaf; nothing reads the pin store) lists `{peer, fingerprint, generation, boot_nonce, invalidated}` through a NEW `PinsSource` port of plain owned scalars (`maos-control` links only `maos-domain` + `maos-kernel-core`, `lib.rs:32-35`; the impl lives in `crates/maos-bin/src/cert_rotation.rs` beside 14-2b's `CohortConvergenceSource` impl). After `SIGTERM` + restart the row for the rotated peer shows the promoted fingerprint: durable `FilePinStore: TofuPinStore` in `crates/maos-persistence` (today an 8-line placeholder with an empty `[dependencies]`, `src/lib.rs`, `Cargo.toml`; gains `maos-a2a-core` as a dep — acyclic, `maos-a2a-core` depends on `maos-domain`/`maos-spirit-abi`/`maos-capability` only; the trait's doc already names this crate, `crates/maos-a2a-core/src/tofu.rs:90-93`; sole impl today `InMemoryTofuPinStore` `:508`), a JSON file `$MAOS_HOME/a2a-pins.json` (`MAOS_HOME` registered, `env_contract.rs:270`; §H-7 resolved: JSON, not SQLite or TL frames). NFR-Rel-6 is preserved, not replaced: the persisted record survives the restart but is marked `Invalidated::SpiritRestarted` when the boot nonce changes, until the re-pin consent completes — the row is what the operator sees, trust still needs the re-pin — so `crates/maos-a2a/tests/restart_invalidates_pin_nfr_rel_6.rs` (6 scenarios) stays green. NEW `SyncPinLookup` sub-trait in `maos-a2a-core` (§H-8 resolved: the async `TofuPinStore` surface the 8.6 comment protects is unchanged) carrying `verify_pinned_sync` (`tofu.rs:454-457`, inherent today) and `find_active_pin_by_fingerprint` (`:250`, inherent; called `transport.rs:1188`), so that `TcpA2ATransport.pins` (`crates/maos-a2a-tcp/src/transport.rs:106`, `Arc<InMemoryTofuPinStore>`; the 9 concrete sites `:25,106,156,526,810,977,1183,1325,1353`) and `build_server_config`/`build_client_config` (`:1322-1334`, `:1350-1356`) take `Arc<dyn TofuPinStore + SyncPinLookup>` (`A2ARouterCore::try_new` already takes `Arc<dyn TofuPinStore>`, `:387`). Proven-red: delete `a2a-pins.json` between `SIGTERM` and restart → the route has no row for the peer and the next dial is refused `NotPinned` (`verify_pinned_sync` semantics, `tofu.rs:454-457`).
- **AC2.** Own-leaf rotation is live: `swap_serving_cert` (`crates/maos-a2a-tcp/src/transport.rs:541`, in-place `ResolvesServerCert`; doc `:536-540` "there is NO production caller"; callers today `t_14_2_rotation_scene.rs:211`, `t_10_4b_rotation_real_timing.rs:550,1252`) gains a production caller driven by the existing signed manifest reissue (14-2a), not a new message; `T_grace` raised to cold + I ≈ 65 s with both 14-2a legs re-derived (`crates/maos-bin/tests/cert_rotation_trigger_14_2a.rs:720-732` — `:720` sources the INSTALLED grace, `:725` the 45 s slop scales; `crates/maos-cohort/tests/cert_rotation_14_2a.rs:723-742` `t_grace_matches_the_shipped_formula…`, the Blocking equality leg) — 14-2d's design record (`_bmad-output/implementation-artifacts/14-2d-self-identity-rotation.md`; §3b `:119-152` ratified `G = cold_deployment_t_grace() + confirmation_interval()`, supersedes cold + 2I), i.e. ALL SIX 14-2d ACs (`:301-460`): AC1 `G` composed at the composition root with the shipped formula untouched; AC2 the `reconcile_local_identity(&PeerCertFingerprint)` port in `maos-cohort` and the actuator in `crates/maos-bin/src/cert_rotation.rs` (5th `install_cert_rotation` parameter, `main.rs:9824`) firing on `Applied`, `Confirmed` and the boot reconcile; AC3 the deadline rule (probe-free, feasibility-checked, NOT unanimity); AC4 the plane-C setter (F3: `std::sync::RwLock` at `router.rs:173,223,256,943`, `.clone()` never a held guard across the `.await` at `router.rs:1018`, net +4…+6 in `maos-a2a-core`, `14-2d…:404-410`) with the extended lock order; AC5 proven on a real mesh with both falsifiers fired; AC6 the installed-grace accessor consumed by the trigger test, `discipline.yml:402-404` `check-cert-rotation-trigger` `timeout-minutes` 20 → 25–30 (`14-2d…:438-442`; CI cost ≈ +60 s per §3b — the AC states the budget), and (c.2) closed. 14-2d's file (status `blocked` on 14-2b/14-2c, both `done`, `sprint-status.yaml:218-219`; no `14-2d` tracker row) moves to `_bmad-output/implementation-artifacts/.archive/` and its tracker row is 21-3.
- **AC3.** NEW `RuptureReason::CertPostGraceReject` (`crates/maos-domain/src/frame.rs:383-384`, `#[non_exhaustive]` — additive for the `consent_rupture_adr_034.rs`/`frame_bridge.rs` consumers; +3 lines under 15-2's `maos-domain` row; §H-9 resolved: a variant, not a `detail` field on `RuptureRejection` `frame.rs:359-377`) emitted on the promoted-generation mismatch path in `transport.rs:1032-1052` (sentinel string `:1034`; the `journal_peer_identity_refusal` call `:1048-1052` — the fn already takes `detail: &str`, `crates/maos-a2a-core/src/router.rs:502-507`; the FRAME had no token, `crates/maos-a2a-tcp/tests/t_14_2a_post_grace_token.rs:241-246`); `t_14_2a_post_grace_journal.rs` asserts the variant on the kind-22 row; ADR-063 (15-5 AC1; `docs/adr/` tops at ADR-059 @9f920180; edge 15-5 → 21-3) names the variant.
- **AC4.** ADR-063 unconditionally chooses SAN: listen-side scoping in `build_server_config` (`transport.rs:1322-1334`; current `None` at `:1333` performs a flat TOFU lookup, and `:1341` installs the resolver) is built here with the client leaf's SAN carrying `PeerId`. Every peer leaf is reissued through AC2's production `swap_serving_cert` rotation path under the ≈65 s grace window; no post-handshake binding alternative or residual RELEASE-HOLDS row remains.

- **AC6 (received from 21-4 AC3, Epic-15 retrospective 2026-09-12 — verbatim; D4a).** `MAOS_REGION_HOME` is reconciled against `TeamEntry.region` at daemon boot (D4a): the boot check lives in `maos-bin` at cohort-manifest load (the cohort bootstrap around `main.rs:9765-9824`) and compares `Region::resolve_home` (`crates/maos-domain/src/region.rs:97`, doc `:84`; `:293-324` are tests) with `TeamEntry.region` (`crates/maos-cohort/src/manifest.rs:154-159`); mismatch = typed boot refusal (§H-14 resolved: the maos-domain reader); the kernel's second reader `crates/maos-kernel-core/src/security/operator_config.rs:203` (`RegionSection::load`, doc `:177`; `:285-349` tests) is untouched.

### 21-4-one-instrument-and-env-registry — One instrument and the env registry (D-I data file)

*Closes · Δ:* ~~D4a~~ (⚠ moved to 21-3 AC6, Epic-15 retro 2026-09-12), D4b/D4c, D11 (E1/E2/E3), D13(b) · kernel-Δ 0 lines; the pin VALUE is re-based to the tokei instrument

- **AC1 (command).** `cargo run -p xtask -- check-kernel-baseline && cargo run -p xtask -- kloc-check` read ONE instrument: both count tokei code (`xtask/src/kloc_check.rs:163-190` filters — `-e target -e tests -e benches -e examples -e fuzz -e spirits -e crates/maos-kernel-core/src/memory/spill_test_faults.rs`; today `check_kernel_baseline.rs:99-110` counts physical lines) with tokei's version pinned in CI (§H-11 resolved; the D13 two-instrument disclosure is retired). `src_lines` in `xtask/kernel-core-baseline.toml` is re-pinned from 24472 (24474 after 15-1) to the tokei value — 18933 @9f920180 (`kloc-check` row `maos-kernel-core 18933/18933`; the story re-measures at its commit and states the number) — and every non-done epic file's `@24472` citation is updated (`xtask/src/check_epic_close_coherence.rs:187-197` reds a stale pin in a non-done epic file); `check_fkcs::validate_frozen_tag_src_lines` (`xtask/src/check_fkcs.rs:62-78`, counts `fkcs-baseline.toml:5` `src_lines = 23081` physically via `git show`) is re-derived by running tokei over a `git worktree` of the frozen tag `frozen-kernel-v2.0` and `fkcs-baseline.toml:5` re-pinned in the same unit; the baseline file's HISTORY block is converted to `#`-prefixed lines so `tomllib` parses it (invalid at line 29 today — "Expected '=' after a key"; `read_pinned` line-parses on purpose, `check_kernel_baseline.rs:73-80`, and stays single-sourced for `check-epic-close-coherence`); `check-cohort-mesh` re-runs the pin (`xtask/src/check_cohort_mesh.rs:674`) and is green. The stale ADR-057 citation is retired: ADR-057 is "enterprise governance is a daemon posture", while ADR-040's "≤25,000" is a J1 latency budget in microseconds. D13(b)'s successor instrument is authored here instead: a measured kernel-crate-set membership file, an ADR, a dedicated aggregate key, and an xtask leg, all using the unified tokei units. This scope belongs to 21-4 rather than 15-2 because it changes the instrument rather than re-basing its data.
- **AC2.** `MAOS_OPERATOR_HTTP_ENDPOINT` is read at `subcommands.rs:1938` but is absent from the registry; the new workspace-wide scan must prove this red and register it in this story rather than leave an ownerless failure. `xtask/src/check_env_contract.rs:119` (`Path::new(maos_bin_dir).join("src")`; registry path `:89`; `xtask/src/main.rs:1303-1305` passes `maos_bin_dir`) scans every workspace crate's `src/` with per-crate proven-red (the 12.6 design, `sprint-change-proposal-2026-07-13.md:61`); the registry becomes the kloc-free data file `xtask/env-contract.toml` read by the gate (D-I; §H-13 resolved): the 479 tokei-code lines of `crates/maos-bin/src/env_contract.rs` (507 physical; 87 `EnvVar {` entries, 86 names; `MAOS_ENV_REGISTRY` has zero consumers outside that file) leave `maos-bin`, and only `EnvVar`/`EnvStability` (+15) move to `maos-domain` as the shared type — NOT the registry (relocating it is ≈ +750 against 15-2's +300 already drawn by 15-1/16/18, and its rationale "avoid a kernel-crate-set member", `kloc.toml:278`, evaporates once registration is data). The unregistered `MAOS_*` reads are registered as data without a kernel edit — measured @9f920180: registered 86; distinct reads `grep -rhoE 'env::(var|var_os)\("MAOS_[A-Z0-9_]+"' --include='*.rs' $(find crates spirits xtask -type d -name src) | grep -oE 'MAOS_[A-Z0-9_]+' | sort -u` = 138 → **54 unregistered** in `src/` (63 incl. tests: 147 distinct; the R2 note's detector-aware count, which also matches the `duration_ms_from_env`/`any_env_with_prefix` shapes of `check_env_contract.rs:41-59`, is 55/64); kernel-core: 16 read, **15 unregistered** (`MAOS_REGISTRY_URI` is registered; the 15: `MAOS_AUTO_REVERT_FAST, MAOS_IDLE_FAST, MAOS_REGION_HOME, MAOS_REGISTRY_ALLOW_FORCE_TIER_AT_IMPORT, MAOS_REGISTRY_ALLOW_UNSIGNED_LOCAL, MAOS_REGISTRY_ORG_SIGNING_PUBKEY, MAOS_REGISTRY_REQUIRE_SERVER_TIER_SIGNATURE, MAOS_REGISTRY_T3_FOR_PUBLIC_UNTRUSTED, MAOS_REGISTRY_TIER_FLOOR, MAOS_REVOCATION_FAST, MAOS_SCHEDULE_FAST, MAOS_SUPERVISION_FAST, MAOS_T3_IMAGE_LOCK_PATH, MAOS_T3_IMAGE_TRUST_ANCHOR_PUB_HEX, MAOS_T3_RUNTIME`). NEW `EnvStability::Secret` (the enum is `HarnessOnly | UserFacing` today, `env_contract.rs:8-11`) classifies `ANTHROPIC_API_KEY`-class values, and `MAOS_REGION_HOME` is recorded as non-secret key-derivation input (D4c's open slot, `epic-14-preflight-decisions.md:282-300`).
- **AC3. ⚠ MOVED OUT 2026-09-12 (Epic-15 retrospective, operator-ratified) — the slot is kept, never deleted, so the D4a citation still resolves.** `MAOS_REGION_HOME` boot reconciliation now belongs to **`21-3-durable-tofu-and-self-leaf-rotation`**. **Why:** it is a *production boot refusal* — a runtime behaviour change, and the only AC in this story that can break a running deployment — sitting inside an instrument-unification story. Under the retrospective's ratified routing rule it goes to the story that already touches those files: 21-3 declares `maos-cohort`, `maos-bin` and `maos-domain` and owns cohort identity. Same wave (W6), so no schedule moves — only ownership. (Epic 14's own re-homing table had sent `14-d4a` to `20-5`, not here.) The AC text is carried verbatim into 21-3 as **AC6** below.
- **AC4.** `maos-persistence` is retained as 21-3's home with its 2000-line ceiling. Story 15-2 already zeroed the three phantom budgets — `maos-cap-registry`, `maos-wire`, and `maos-journal`: 0 LOC, no crate directory. Story 21-4 deletes those zero rows only after the unified instrument preserves fail-closed governance for a crate without a budget. This is the story-driven lowering authorized by architecture §15.5 Decision 2(a), replacing the stale ADR-038 rationale; D11-E1/E2/E3 (`epic-14-preflight-decisions.md:429-472`: `example_spirit_regen` budget-charged with no execution path; `clippy` installed and never invoked; in-`src` test modules budget-charged) are each closed or re-pointed with a citation in the register. (The "consolidate 19 canonical / 10 SigningKey" sentence is dropped: not a deliverable.)
- **AC5.** Grants become machine-readable in the unified instrument, then NEW `check-cited-kloc-grants` hard-fails when a story or grant ledger cites an absent grant. Story 15-2 cannot honestly implement that check: grants are comments today, so no parser can distinguish a grant from narrative history. The gate lands here only after 21-4 creates the data it can validate; this is D13(b) scope, not deferred effort.

### 21-5-orchestrator-port-and-move — Optional: orchestrator port and move to `maos-orchestrator-buffer` ◆ FLAG-Winston (negative; after 21-4)

*Closes · Δ:* §4.0.7 "the kernel does NOT embed an orchestration policy" (`architecture-maos-minimal-opus/4-kernel-design.md:185`) · kernel-Δ −817 tokei code (−1028 physical) · maos-domain +30–60 · NEW crate `maos-orchestrator-buffer`

- **AC1 (command).** `cargo run -p xtask -- check-kernel-baseline && cargo run -p xtask -- check-cohort-mesh` are green after `crates/maos-kernel-core/src/orchestrator/` (5 files — buffer 220, echo_gateway 90, gateway_dispatcher 531, mod 97, registry 90 physical = 1028; 817 tokei code) leaves kernel-core: the 817 lines land in the NEW leaf crate `crates/maos-orchestrator-buffer` (D-J; §H-15 resolved: no allowance exists in `maos-domain` or `maos-bin`; kloc row `maos-orchestrator-buffer = 917` = 817 measured + max(100, ceil 2 %); `check-workspace-count` (`xtask/src/check_workspace_count.rs:5-7`, `Cargo.toml` members vs the count declared in `4-kernel-design.md`) needs the declared count bumped; `Cargo.toml` members +1); the port trait for `OrchestratorBufferRegistry` — the only symbol kernel code uses outside the module (`crates/maos-kernel-core/src/scheduler/scheduler_loop.rs:89` field, `:111` ctor param) — in `maos-domain` (+30–60 under 15-2's row); the `maos-bin` users updated (`main.rs:1995` registry ctor; `:5696` `journal_orchestrator_queue`; `:5788` `journal_director_lifecycle_action`; `:5859` `journal_token_revocation`); the kernel tests `gateway_dispatcher_fr54.rs`, `gateway_uninstall_fr65_v05.rs`, `nfr_perf_4_pause_resume_latency.rs` and the `spirits/orchestrator/tests` imports of `maos_kernel_core::orchestrator` move with it; the pin is re-pinned NEGATIVE in tokei units (order 21-4 → 21-5, so the unit is fixed before the move) and the `maos-kernel-core` ceiling (`kloc.toml:195`, 18933) is LOWERED by the same amount (architecture §15.5 Decision 2(a) — the freed 817 lines are not growth capacity); `check-cohort-mesh` re-runs the pin at `xtask/src/check_cohort_mesh.rs:674` (not `:604`, a `migration_chain` leg) and is green. `check-loom` (`xtask/loom-blocklist.toml:14` `Orchestrator` over `kernel-crates.toml`) is not red at HEAD, so the move is a §4.0.7 alignment, not a measured violation — hence optional.

## Dependencies

15-2 (the allowances the asks above draw on: maos-a2a-core +150, maos-a2a-tcp +150, maos-cohort +100, maos-domain +300, the lane-wide maos-bin row, xtask +400), 15-3 (`check-exit-commands` resolving `maos` tokens via the `maos --help` verb table — 21-1 AC1's line), 15-5 (ADR-063 → 21-3 AC3/AC4), 15-6 (`MAOS_INFERENCE_MODE`/ADR-064 → 21-1), 18-4 (`Clock` port in `maos-spirit-sdk` + `--clock compressed:…` → 21-1), 20-3 (evidence states: the nightly's ledger publication uses them; the red-on-absence leg does not wait for it), `ops-j4-live-incident-recording` → 21-1. Dropped: `17-3b → 21-2` (vestigial — 21-2 touches no WASM; `dependency-dag.md:217`'s "J3 recall over the WASM wire" is the old E17→E20 mapping) and 20-2 (no runbook/package AC in this epic). Order: 21-2 ∥ 21-3 ∥ 21-4 (21-2 and 21-4 may run alongside Epic 20; they need only 15-2/15-3) → 21-5 after 21-4 (unit of the re-pin) → 21-1 last (after 18-4 and the operator row).
