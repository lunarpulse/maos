# Epic 19 — The Founder Loop (W4)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 19 file. `sprint-status.yaml` is authoritative for status.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (19 of 21 applied) and the decisions D-A, D-D, D-E, D-F are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`.

**Wave / duration:** 4.5–6.5 weeks (measured from the J1 lane 1a/1b/2a/2b/2c/2e + demo-j1 — 8 stories, 2026-08-14 → 2026-08-25, ≈10.5k non-test LOC, `git show --stat` per the note's §G — +30 %; 4–5.5 if 15-2/15-6/16-1 have landed) · **Makes true:** v0.8 · J1 wedge: Orchestrator decomposes, Workers execute under a profile, halt on ambiguity, digest at the end

**Closes:** FR20, FR21, FR25, FR52 (FR25/FR52 are live today at the CliWrapper admission/probe seam — this epic closes their effect-oracle residual); RELEASE-HOLDS row 16 (`RELEASE-HOLDS.md:63`); the two J1 beats no story owns today (`halt-resume-referential-identity`, and the halt/digest beats 19-4 adds); `tier2-live-agent-signed` stays operator-lane; `two-host-signed-run` is closed by the `j1-crosshost-2d` lane and renders INDETERMINATE by design.

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green):**

```
MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j1/founder-loop.json cargo run -p xtask -- demo-j1
# hermetic is demo-j1's DEFAULT (no --live-codex; there is NO --replay flag — D-D/ADR-064: the env pair is the one selector);
# unattended; pass = every EXECUTED beat ok; ABSENT owned by this epic = {halt-resume-referential-identity} until 19-4 AC5, then none;
# tier2-live-agent-signed renders ABSENT by construction (operator row ops-j1-live-agent-signed-take) and is NOT demanded;
# two-host-signed-run renders INDETERMINATE (capture present, run not claimed; demo_j1.rs:959-981) or PROVEN, never ABSENT;
# the digest event lists one source_log_ref per completion, each resolving to a TL row
```

Exit-block provenance (dry-parsed @9f920180; `bash -n` on a copy = OK; no secret, paid key, human, wall-clock or network):
- `MAOS_INFERENCE_MODE=replay` — NEITHER @9f920180 (`grep -rn MAOS_INFERENCE_MODE crates spirits xtask .github` = 0); created by 15-6 AC1/AC4 (ADR-064, authored by 15-5). At HEAD the variable is inert (no consumer); `demo-j1` forwards it to the child because the spawn has no `env_clear` (`xtask/src/demo_j1.rs:542-548`).
- `MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j1/founder-loop.json` — the variable exists @9f920180 (`crates/maos-bin/src/env_contract.rs:255`; read `main.rs:4609,4623`, Researcher-only today; the Orchestrator consumer is 19-1 AC3); the path does NOT exist (`ls crates/maos-journey-test/cassettes/` = `j-butler j-researcher j0`) — created by 19-4 AC4.
- `cargo run -p xtask -- demo-j1` — exists @9f920180 (`xtask/src/main.rs:993-1010`, dispatch `:1381-1387`; flags `--live-codex --codex-topology --keep-home --skip-build --skip-gate`; the hermetic run is the default path). Expected at HEAD: **red** — measured `demo-j1: 2 executed beat(s) did not hold` (`completion-vectors-enrolled`, `disallowed-intent-refused-blocking`): `check-j1-loopback-delegation` is Blocking (`xtask/gate-registry.toml:308-309`, `aggregate.needs` `discipline.yml:3410`) and EXIT=1 because `J1_TEST_SUFFIXES` (`xtask/src/check_j1_loopback_delegation.rs:114`) matches `crates/maos-bin/tests/cert_rotation_trigger_14_2a.rs` without the job (`discipline.yml:1904-1925`) invoking it. That fix is owned by 15-1 AC1 (narrow the suffix) — **green only after 15-1**, and then only after 19-1..19-4 land the epic topology, the cassette and the three new beats. The default path runs `cargo build --workspace` first (`demo_j1.rs:412-420`); the CI job may pass `--skip-build` after its own build step (flag exists).

**Operator-lane legs (never in the exit):**
- `ops-j1-live-agent-signed-take` — the paid `demo-j1 --live-codex` take (`xtask/src/main.rs:996-997`; `live_codex_take` `demo_j1.rs:1202`, spawns `maos run <codex topology> --live --once` with `MAOS_LIVE_AGENT=1`, `:1341-1349`), plus the `--live-claude` twin 19-3 AC1 adds; earns `tier2-live-agent-signed` under a named human signer; `PROVEN_LIVE_SIGNED` stays operator-only.
- `ops-live-worker-under-17-1-profile` — a real `claude`/`codex` Worker under the 17-1 egress-proxy profile (vendor key + signed T3 image); gates only 19-3's live leg.
- two-host signed run — owned by the closed `j1-crosshost-2d` lane (`sprint-status.yaml:209`, done 2026-08-25); permanent operator artifact (`_bmad-output/test-artifacts/j1-two-host-evidence/`, README `:11-18` "do not delete"); not a row.

**Kernel-Δ:** 19-1/19-2/19-4 ZERO @24472 (`xtask/kernel-core-baseline.toml:472`; 19-2 drains `OrchestratorBufferRegistry` from the composition root, `crates/maos-bin/src/main.rs:1993-1995`, through `Orchestrator::drain_next`, `spirits/orchestrator/src/lib.rs:219-227` — no kernel hook; `crates/maos-kernel-core/src/orchestrator/buffer.rs:58-60` forbids one); 19-3 +3–5 kernel lines (`BridgeSpawnSpec.cwd` + `cmd.current_dir`, `crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:240-269,461-469`), re-pin 24472→~24477 in the same commit, FLAG-Winston (D-F), drawn from the 15-2 `maos-kernel-core` grant.

**Kloc (15-2 named Epic-19 row; measured by `cargo run -p xtask -- kloc-check` @9f920180, EXIT=1):** maos-bin +500–1000 (19-1 +150–300 · 19-2 +60–120 · 19-3 +180–400 · 19-4 +80–160; crate at 17109/17109, `xtask/kloc.toml:343`) · xtask +150 (41953/41953, `:212`; cassette env forwarding + beat re-labelling) · maos-bench +200 (1531/1631; 19-3 real-spawn bench) · maos-kernel-core +3–5 (18933/18933, `:195`; 19-3 only) · maos-control +40–80 inside its 697 headroom (803/1500). `spirits/orchestrator`, `spirits/butler`, `spirits/worker` are ungoverned (`xtask/src/kloc_check.rs:184,270`) — the preferred home for the epic parser, the deterministic split, the halt tag and the digest composition.

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands`) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `19-1-orchestrator-dispatch-loop-and-epic-input` | Orchestrator dispatch loop and `--epic` input | FR21 · kernel-Δ 0 · maos-bin +150–300 |
| `19-2-fr20-enqueue-door-and-safe-point` | FR20 enqueue door and safe-point dequeue | FR20 · kernel-Δ 0 · maos-bin +60–120, maos-control +40–80 |
| `19-3-real-worker-default-with-effect-oracle` | Fixture Worker hermetically, real Worker on the operator lane, with an effect oracle ◆ FLAG-Winston | FR25/FR52 · kernel-Δ +3–5 · maos-bin +180–400, maos-bench +80–200 |
| `19-4-j1-beats-and-demo-replay` | J1 beats and the hermetic `demo-j1` replay | J1 · FR17 · kernel-Δ 0 · maos-bin +80–160, xtask +60–150 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs. Order: 19-1 → 19-2, 19-1 → 19-3, 19-1 → 19-4; 19-2 ∥ 19-3.

### 19-1-orchestrator-dispatch-loop-and-epic-input — Orchestrator dispatch loop and `--epic` input

*Closes · Δ:* FR21 · kernel-Δ 0 · maos-bin +150–300 (`spirits/orchestrator` ungoverned)

- **AC1 (command).** `maos run spirits/topologies/j1-founder-loop-epic.toml --epic tests/fixtures/j1/epic.md --once` — three things are NEW: `--epic <file>` in the hand parser (`crates/maos-bin/src/worker_spawn.rs:53-66` @9f920180; today `Err("unknown argument … expected: <manifest> [--live] [--once]")`, `:61-66`); the fixture `tests/fixtures/j1/epic.md` with three `### Story` sections (`tests/fixtures/` holds `anthropic-skill anthropic-skill-invalid dirty-network-fixture wasm` — no `j1/`); and the topology `spirits/topologies/j1-founder-loop-epic.toml` with three `host`-bearing Worker entries, because `j1-founder-loop.toml:28-30` is pinned to exactly one `host` entry by `check_j1_loopback_delegation.rs` leg 1 and `crates/maos-bin/tests/topology_delegation_1a.rs:232-238` (the `j1-founder-loop-crosshost.toml:1-8` precedent: extend by a separate file). A maos-bin dispatch loop — net-new: the root's `delegation_emitter` is scoped to one `for entry in topology_entries` pass (`main.rs:3395-3397`) and `:3434-3441` forbids a loop today — emits N = 3 `task.assign` frames via `assign_frame_remote` (`main.rs:3528`; gate needle `check_j1_loopback_delegation.rs:268` kept), one per section, split deterministically in `spirits/orchestrator` (Decision E kept for dispatch: the cassette supplies task text, never the count; a section without a `### Story` heading is the proven-red), and runs the three Workers in sequence, each closed at a safe point (`delegation_completed` ×3). Unset `MAOS_INFERENCE_MODE` is today's deterministic path (D-D); the replay selector is the env pair `MAOS_INFERENCE_MODE` + `MAOS_REPLAY_CASSETTE` (15-6, ADR-064) — `demo-j1` gets NO new flag, its hermetic default forwards the pair (`demo_j1.rs:542-548`, no `env_clear`).
- **AC2.** `MAOS_DELEGATED_GOAL` (read `main.rs:3497`; registered `crates/maos-bin/src/env_contract.rs:32`) keeps its cross-host REQUIRED semantics (`:3504-3511`); the loopback fallback string (`:3516-3519`) is replaced by the `--epic` sections; `smoke_cli_wrapper_8_12.rs:216-235` (sentinel goal) is re-keyed to a one-section epic; the registry row keeps its entry with its doc string naming the replacement. `check-j1-loopback-delegation` (Blocking, `xtask/gate-registry.toml:308-309`; job `discipline.yml:1904-1925`; `aggregate.needs` `:3410`) is RED at HEAD — RUN, EXIT=1, `"passed":false,"binding":"Blocking"`, leg `completion-vectors-enrolled`: `J1_TEST_SUFFIXES` (`check_j1_loopback_delegation.rs:114`, `["_1a.rs","_1b.rs","_2a.rs","_2b.rs"]`) derives `cert_rotation_trigger_14_2a.rs` into the enrolled set while the job never invokes it. That fix is OWNED by 15-1 AC1, which narrows the suffix; this story does not re-decide it. This story adds a gate leg for `j1-founder-loop-epic.toml` (three `host` entries, three `assign_frame_remote` emissions) and enrolls its tests in the narrowed derivation, so the gate is green after 15-1 narrows the suffix AND after this story lands (R2's "stays green" was false at HEAD).
- **AC3.** Orchestrator gains `with_deferred_inference_port` (the Researcher builder shape, `spirits/researcher/src/lib.rs:502-518`; grep `InferencePort` in `spirits/orchestrator/src/lib.rs` = 0 today) and `[capabilities.required] provider.complete` (`spirits/orchestrator/manifest.toml:33-36` today "provider.complete omitted"); the composition root wraps a THIRD `InferencePortAdapter` for it under the 15-6 selector (the Researcher-private wiring `main.rs:4590-4641` is the template; the shared adapter is `main.rs:3136`) — used ONLY for the 19-4 ambiguity judgement, never for dispatch; the Decision-E comment at `manifest.toml:33-36` is amended: dispatch deterministic, ambiguity judged. Stays `rust-inproc` (`manifest.toml:7`; ADR-060, authored by 15-5 — `docs/adr/` tops at `ADR-059` @9f920180).
- **AC4.** After each `worker_completion`, the dispatch loop calls `DistillateWriter::write_distillate` (writer built `main.rs:2379-2380`; production callers today = 0 — `main.rs:6041` is the smoke arm, `:8986` the test harness) over the run's `CliSubprocessOutput` rows on the Worker's behalf (producer pid = the root's Orchestrator pid, since the Worker journals under pid 0 — `wait_and_finalize(&transparency_log, 0, …)`, `worker_spawn.rs:732`), and dispatches section k+1 with `followup_dispatch` (`spirits/orchestrator/src/lib.rs:389-401`; production callers today = 0 — the root uses `build_task_assign` `main.rs:3522` + `assign_frame_remote` `:3528`) carrying that `PriorDistillateRef`; the FR21 60 s-window check (`crates/maos-bin/src/delegation.rs:276-292`) passes on the real rows; a follow-up built with `first_dispatch` (`lib.rs:372`) is the proven-red.

### 19-2-fr20-enqueue-door-and-safe-point — FR20 enqueue door and safe-point dequeue

*Closes · Δ:* FR20 · kernel-Δ 0 · maos-bin +60–120, maos-control +40–80

- **AC1 (command).** With `maos run spirits/topologies/j1-founder-loop-epic.toml --epic tests/fixtures/j1/epic.md` running WITHOUT `--once` (the continuous serving loop, `main.rs:4020-4031`; `--once` is one `on_idle` pass + drain, `:3341`), `maosctl orchestrator queue --spirit orchestrator "<instruction>"` (`crates/maos-cli/src/cli.rs:750-758`, `--spirit` required) POSTs to the daemon's loopback surface. The door is 16-1's, per D-A/ADR-062: the `maos run` daemon process binds the loopback POST surface, `maosctl` discovers endpoint + bearer from `MAOS_HOME/control.json` minted by `maos init`, and `maos-cli` gains an HTTP client without linking `maos-control` (today `maos-control` is GET-only — `crates/maos-control/src/lib.rs:69-76,299,347,392,428` — and `orchestrator queue` spawns a `MAOS_ONE_SHOT=orchestrator-queue` child, `subcommands.rs:1656-1676`, whose `hello-spirit`-only guard `main.rs:5667-5672` is deleted by 16-1's verb re-target). The daemon enqueues into `OrchestratorBufferRegistry` (`main.rs:1993-1995`) and the 19-1 loop drains it via `Orchestrator::drain_next(|| buffer.dequeue_at_safe_point())` (`spirits/orchestrator/src/lib.rs:219-227`; `crates/maos-kernel-core/src/orchestrator/buffer.rs:62`, first production caller — non-test callers today = 0) only after the in-flight `delegation_completed`; a `queue` issued mid-delegation is observed in the next `task.assign`, never before (observed earlier = the proven-red); `maosctl orchestrator status --spirit orchestrator` (`cli.rs:760-764`) reads the live count.
- **AC2.** `crates/maos-spirit-abi/src/ctx.rs:58` is unchanged (the doc at `spirits/orchestrator/src/lib.rs:143-147` stays true); the loop is a maos-bin tokio task, not an `on_idle` hook (`lib.rs:149-157` stays inert); kernel-Δ 0 — measured, not 0–10: the only kernel file a "safe-point hook" could touch is `crates/maos-kernel-core/src/orchestrator/buffer.rs`, whose contract at `:58-60` forbids kernel-internal dequeue ("NEVER from kernel-internal hooks").

### 19-3-real-worker-default-with-effect-oracle — Fixture Worker hermetically, real Worker on the operator lane, with an effect oracle ◆ FLAG-Winston

*Closes · Δ:* FR25/FR52 · kernel-Δ +3–5 (FLAG-Winston, re-pin in the same commit) · maos-bin +180–400, maos-bench +80–200

- **AC1 (command).** Hermetic: `cargo run -p xtask -- demo-j1` (hermetic is the default; `--live-codex` is the only live switch, `xtask/src/main.rs:994-1009`) runs `j1-founder-loop-epic.toml` with three fixture Workers (`worker-cli-fixture`, a real subprocess — `spirits/worker/src/bin/worker-cli-fixture.rs:20-45`, `spawn_and_bridge` `runtime.rs:449-476`, `child_pid` asserted `journey_j1.rs:138-146`; built-in host grant `worker_spawn.rs:157-163`, `:243`) — the fixture ALREADY runs with no flag today (`demo_j1.rs:48`, `:542-548`, `MAOS_LIVE_AGENT` removed on the child). Operator lane: `demo-j1 --live-codex` (exists, `main.rs:996-997`) and a NEW `--live-claude` select the live topologies (`spirits/topologies/j1-founder-loop-codex.toml`; a `-claude.toml` twin over `spirits/worker/manifest-claude.toml`) under `MAOS_LIVE_AGENT` + `MAOS_HOST_GRANTS` — the real Worker runs only when `MAOS_LIVE_AGENT` is set (rows `ops-j1-live-agent-signed-take`; `ops-live-worker-under-17-1-profile` once 17-1 lands). `spirits/worker/manifest.toml:8` (`command = "worker-cli-fixture"`) stays the hermetic baseline (11 consumers, note §C); `live_agent_gate` (`crates/maos-bin/src/worker_cli.rs:860`, called `worker_spawn.rs:505-507`: "CI never sets the flag, so CI cannot spawn a paid agent") and `ci_local_split_refuses_a_granted_real_agent_without_the_live_flag` (`crates/maos-bin/tests/smoke_cli_wrapper_8_12.rs:147`; doc `:139-142` "Removing the gate call from the run path reds THIS test") are UNCHANGED — they are the control. "Real Worker by default" is retired.
- **AC2 (FLAG-Winston).** `BridgeSpawnSpec` gains `cwd: Option<PathBuf>` applied via `cmd.current_dir` (`crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:240-269` has no cwd field; `spawn_and_bridge` `:461-469` never calls `current_dir` — grep `current_dir` in `cli_wrapper/` = 0; `spirits/worker/manifest-codex.toml:50-52` documents "MAOS does NOT set the child CWD"): +3–5 kernel lines, `kernel-core-baseline.toml` re-pinned 24472→~24477 in the same commit, under the 15-2 `maos-kernel-core` grant (D-F). Process-global `std::env::set_current_dir` is REFUSED (it would move the daemon's own relative paths). `run_cli_wrapper_manifest` (`worker_spawn.rs:384`) passes a per-run workspace dir as that cwd, snapshots it before spawn and diffs it after exit — the worktree-diff effect oracle, in maos-bin (RELEASE-HOLDS row 16's named successor, `RELEASE-HOLDS.md:63`); `claude_result_object_oracle` (`worker_cli.rs:498-537`; residual doc `:490-497`) becomes `Completed` only when the diff is non-empty, else `NotCompleted(NoEffectEvidence)` (the codex shape, `:465`); the fake-`claude` "I have written the file" vector (row 16, measured 2026-08-22) is the proven-red; row 16 closed and `_bmad-output/test-artifacts/runbook-j1-t8-two-host-paid-run.md` Appendix B abort condition 9 demoted from blocking to advisory.
- **AC3.** The three Worker entries run in SEQUENCE through the 19-1 loop: the topology loop stays a sequential `for` (`main.rs:3397`), `run_cli_wrapper_manifest` stays synchronous (`worker_spawn.rs:384`; `main.rs:3445-3446`), and 19-1 AC4's FR21 chain (section k+1 carries section k's distillate) makes concurrent execution incoherent — R2's "three concurrent fixture Workers" is dropped (no substrate: one `host` entry pinned, `wait_and_finalize(…, 0, …)` pid 0, no Worker SCB). A NEW `crates/maos-bin/tests/worker_sequence_19_3.rs` asserts three distinct `child_pid`s over one run, each `cli_wrapper_loaded`/`cli_wrapper_exit` pair closed before the next `task.assign`; it is enrolled in the `check-j1-loopback-delegation` job (`discipline.yml:1904-1925`) and in its `completion-vectors-enrolled` derivation as narrowed by 15-1. `crates/maos-bench/benches/orchestrator_fanout_nfr_perf_8.rs:22` (50 in-process task handles today) gains a `MAOS_BENCH_REAL_SPAWN=1` mode spawning fixture subprocesses (maos-bench +80–200 against 100 headroom → the 15-2 maos-bench +200 row); the job `nfr-perf-8-orchestrator-fanout` stays `continue-on-error: true` (`discipline.yml:1490-1493`; advisory until wall-clock stable).

### 19-4-j1-beats-and-demo-replay — J1 beats and the hermetic `demo-j1` replay

*Closes · Δ:* J1 · FR17 · kernel-Δ 0 · maos-bin +80–160, xtask +60–150

- **AC1 (command).** The exit command above: `demo-j1`'s hermetic default forwards `MAOS_INFERENCE_MODE`/`MAOS_REPLAY_CASSETTE` to the child (`demo_j1.rs:542-548`, no `env_clear`; no new flag — D-D/ADR-064), runs `j1-founder-loop-epic.toml` (`TOPOLOGY` `:48` re-pointed) and renders the REACHABLE state: every executed beat `ok`; `two-host-signed-run` INDETERMINATE (`apply_capture_judgement` `:959-981`, unchanged — the 2026-08-25 capture is present, `_bmad-output/test-artifacts/j1-two-host-evidence/README.md:11-18`; PROVEN only from an operator-verified run; never ABSENT again); `tier2-live-agent-signed` ABSENT by construction in every hermetic run (`SIGNABLE_WORKER_CLIS` excludes the fixture, `:392`; owner string `:890-894` → row `ops-j1-live-agent-signed-take`) — NOT demanded by the exit; `halt-resume-referential-identity` (`:900-904`) ABSENT until AC5 lands, then EXECUTED; three NEW executed beats `epic-decomposed-n-frames`, `ambiguity-halt-within-2-frames`, `digest-cites-source-refs`. `unlanded_beats()` (`:888-906`, three entries today) shrinks to one (the tier-2 beat). Measured @9f920180 the command exits `demo-j1: 2 executed beat(s) did not hold` (`completion-vectors-enrolled`, `disallowed-intent-refused-blocking` — the `check-j1-loopback-delegation` red) → green only after 15-1.
- **AC2.** The fixture epic's second section is ambiguous; the Orchestrator's inference judgement (19-1 AC3, replayed from the J1 cassette) writes the scalar `dispatch_ambiguity` through a NEW `Orchestrator::with_scalar_port` builder (grep `with_scalar_port` in `spirits/orchestrator/src/lib.rs` = 0; the Orchestrator is loaded bare today, `main.rs:3762-3768`) wired like Mira's (`main.rs:3796-3835`; `ButlerOrchestratorAdapter` `:570-591`); the daemon allowlist `requires_epistemic_halt_port` (`main.rs:553-567`, today `belief_variance | user_preference_drift | diagnostic_confidence`) admits `dispatch_ambiguity` and `delegation_confidence`; the existing manifest rule (`spirits/orchestrator/manifest.toml:40-43`, halt above 0.5) fires the kernel halt through `process_scalar_write`; the halt is journaled under ONE ambiguity tag repo-wide, `task.acceptance_criterion.ambiguous` — the spelling with a real emitter (`crates/maos-spirit-hello/src/lib.rs:148`, 16-2's exit token); the three `story.acceptance_criterion.ambiguous` seeds at `crates/maos-corpus-gen/src/lcas.rs:84,124,164` are re-spelled to it by THIS AC (`grep -rn story.acceptance_criterion.ambiguous crates spirits xtask` = 0 afterwards). The halt lands before the third `task.assign` (≤2 frames); a cassette whose judgement says "clear" is the proven-red (no halt, 3 frames).
- **AC3.** At topology drain the daemon composes the FR17 digest by calling `Butler::morning_digest` (`spirits/butler/src/lib.rs:675`; production callers today = 0 — `crates/maos-bench/src/harness/j0.rs:83`, `journey_butler.rs:153`, `spirits/butler/tests/{hallucination,digest}.rs`; `spirits/butler` is already a maos-bin dependency, `main.rs:4247`) over the run's TL window — D-E: no Butler class in the topology (`topology-spirits-loaded` keeps its exact-three assertion, `demo_j1.rs:578-586`, `journey_j1.rs:69-76`), no inference (Butler's `provider.complete`, `spirits/butler/manifest.toml:21-22`, is not exercised), no Epic-18 dependency — and prints a `digest` event; every `completions[].source_log_ref` (`butler/src/lib.rs:266,273`) resolves to a `TaskComplete`/`CliSubprocessOutput` row (the `spirits/butler/tests/hallucination.rs` check, run in-process); a planted extra completion in the event is the proven-red. The J3 `maos_digest::derive_team_digest` path (`main.rs:465-516`) is a different artifact and is not reused. FR17's 100-digest corpus floor (`prd/functional-requirements.md:50`) is NOT claimed by this run.
- **AC4.** `crates/maos-journey-test/tests/journey_j1.rs` (three tests `:38,:185,:225`; job `journey-hermetic-tier-1` `discipline.yml:2102-2128`, pre-builds `worker-cli-fixture` `:2125`) gains assertions on the `halt`, `digest` and three `delegation_completed` events (no wall-clock — `guards_meta.rs:24-25`); the J1 cassette `crates/maos-journey-test/cassettes/j1/founder-loop.json` (today `cassettes/` = `j-butler j-researcher j0`) is committed with `provenance: seed` (15-6 AC2), and the CI job sets `MAOS_REPLAY_STRICT=1` (`env_contract.rs:265`; read `main.rs:4624`) so a missing turn fails rather than falls back.
- **AC5 (NEW — the resume beat).** Halt → `maosctl halt resolve <halt_id> --spirit orchestrator --kind provided-context --text "<clarification>"` (`crates/maos-cli/src/cli.rs:711-727`; over the 16-1 surface against 16-2's daemon-held registry) → resume dispatches the third section; the post-resume digest cites the pre-halt `TrajectoryRef` (`journey_j1.rs:303-308` oracle un-deferred: capture the pre-halt ref from run 1's stdout, resume into the same `MAOS_HOME`, assert `post_resume_digest.cited_refs` contains it); `halt-resume-referential-identity` (`demo_j1.rs:900-904`, owner `FOLLOWUP-J1-RESUME-SEAM`) becomes an executed beat. Depends on 16-1 and 16-2.

## Dependencies

Epic 15 (15-2 named Epic-19 row; 15-6 selector; 15-5 ADR-062; 15-1 for the J1 gate), 16-1 (door) and 16-2 (halt resolve). 17-1 only for 19-3's operator leg. 19-1 before 19-4; 19-2 after 16-1. Epic 18 is not required (D-E: the digest is daemon-rendered without inference). Intra-epic: 19-1 → 19-2, 19-1 → 19-3, 19-1 → 19-4; 19-2 ∥ 19-3.
