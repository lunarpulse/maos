# Epic 13 — Reza Single-Org Cross-Team Cortex (v2.2)

**Status:** `in-progress — rescoped 2026-07-18 to ELEVEN stories` (was eight). 13.1 and 13.2 are **done**; 13.3 is **ready-for-dev**. Two evidence-driven rescopes landed the same day, both recorded in `sprint-change-proposal-2026-07-18.md`:
- **H1 split** — 13.3's provenance half (flattened I11 chain, cross-wall `log.recall`) has **no ratified §15 home**, cites **two ADRs that do not exist** (`ADR-013`, `ADR-018`), and requires **reversing Story 8.10's citer-auth control**. Split to **13.3b**. 13.3's headline was also reframed on evidence: *"the row is production-real; the crossing is not."*
- **13.5c scoping pass** — the old 13.5c owned **seven** concerns and was the only story with no ZERO-Δ claim; three consecutive mechanism stories had deferred every production wiring into it. Split to **13.5c / 13.5d / 13.5e**, with the consented-`log.recall` half handed to 13.3b and **NFR-Ops-11 explicitly served on the team axis only, not closed**.

**The critical-path change:** **13.5c is now the unblocker.** Until it lands, `MAOS_LOOM_HOME_TEAM` is a hard boot failure — so 13.1's physical wall and 13.2's cryptographic wall are **inert in every bootable configuration**.

⚠ **Bounded by the 13.5c preflight (2026-07-19) — do not overstate it.** 13.5c makes the tenant map **constructible and boot-reachable in production for the first time** (`TenantMapAdapter` has zero production constructors today). It does **not** make the wall serve: `register_spirit` has zero production callers, so every collective op then fails `TenantSpiritUnmapped` until **13.5d** wires it (fail-closed, correct scope). And because `tenant_map_for_store` sits inside the `MAOS_LOOM_POSTGRES` arm, the claim is measurable **only on the live Postgres leg** — absent substrate means **UNMEASURED, not green**. **"Boots" ≠ "serves."**

**Dev-gate:** external holds (pen-test NFR-Sec-7 + export counsel NFR-Comp-1) = **GA ledger only, non-gating for v2.2 dev** (E11 retro A3).

**Model/review discipline (E11 retro A1):** frontier-class dev allowlist + **§A6 full-layer review net is the binding control**. The physical and cryptographic tenant walls (13.1/13.2), cross-team consent/provenance (13.3), vetting promotion (13.4), collective erasure/hold (13.5b), production tenant-audit path (13.5c), and Reza journey (13.6) are adversarial boundaries → the full §A6 net (incl. Test-Infra + runtime) is **non-degradable**.

---

## Objective
Serve **Reza's single-org cross-team Cortex** (§10.7.2, committed v2.2) — a 400-person fintech running multiple teams as one governed Cortex on shared MAOS infrastructure. Epic 11 shipped the **enablers** (WASM form, multi-region Loom, PDP, identity/at-rest/SIEM); the journey itself is **unbuilt**. Epic 13 delivers the **tenant wall** (multi-tenant Loom, database-per-team, cryptographic per-team boundary), **cross-team asymmetric sharing with multi-hop distillation provenance**, the **FR37 vetting machinery** (the only unserved PRD FR — internal-vetter-first), the **Enterprise reference Spirit class**, collective-tier erasure/legal-hold closure, a real Spirit/daemon collective path with tenant audit isolation, and the **Reza Cortex scene E2E** on the 3-region substrate. The closer composes proven controls and refuses to translate absent or indeterminate evidence into a product claim.

## What Epic 13 stands on
| Substrate | From | Epic 13 use |
|---|---|---|
| Cross-region convergent replication, `region_guard` chokepoint, `canonical_kv_leaf`, per-region Merkle oracle, `CrossRegionReadmit` re-attestation (ADR-049) | 11.2a/b | `team_guard` reuses the guard-chokepoint + physical-absence pattern; `canonical_kv_leaf` **v2** adds `source_team`; cross-team re-attestation reuses the readmit path |
| Signed cohort/org manifest, per-(peer,role) consent tuples, full-pairwise mesh (ADR-054) | Epic 12 | the org manifest **owns** the team↔region↔datname mapping; cross-team A2A rides cohort links; consent tuples extend to the team axis |
| Enterprise PDP out-of-kernel policy port, fail-closed (ADR-050) | 11.4a (done) | Enterprise reference Spirit (13.4) + Reza cross-team policy |
| SSO/OIDC identity + org-KMS at-rest AEAD + SIEM export (ADR-051) | 11.4c (done) | Enterprise reference Spirit + at-rest for per-team Loom rows |
| Region signing-key HKDF weld, sealed-export root (9.4b, §7.3) | Epic 9 | per-team HKDF key-weld mirrors the 9.4b derivation exactly; vetter-key lifecycle rooted at the operator audit key |
| Registry publish/install/trust-tiers, ComplianceClaim (Epic 7) | Epic 7 | FR37 `public-vetted` is the 4th trust tier; attestation rides the CRL/yank path |

## Ratified architecture basis
- **ADR-055 — Multi-tenant Loom** (§15.3): database-per-team (distinct `datname`) + store-internal `team_guard` + per-team Merkle roots. **Fork-4 ratified = per-team HKDF key-weld** (same-region cross-team forgery cryptographically closed; `team_guard` upgrades to signature-verify, not presence-only). Mapping owned by the signed org manifest. Row-ownership = destination team's row. Multi-hop distillation provenance in the re-attested bundle.
- **ADR-056 — FR37 vetting machinery** (§15.4): out-of-kernel `VettingAttestation`, exact-hash + refuse-at-next-load, internal-vetter-first, vetter-key lifecycle rooted at the operator audit key, four distinguishable terminal causes.

---

## Story list (decompose ACs at each story's preflight; ≤6 ACs)

| # | Title | Scope (one-line) | ACs | Model | Kernel-Δ risk | Depends |
|---|-------|------------------|-----|-------|---------------|---------|
| **13.1** | Multi-tenant Loom — **physical tenant wall** (ADR-055) | Database-per-team (distinct `datname`) + store-internal `team_guard` chokepoint; manifest-owned team↔region↔datname mapping (`ETenantMapStale`); identity-keyed single-team Spirits (`ETenantConnectionMismatch`); physical row-ownership; `check-multi-tenant-loom` physical legs. | 6 | frontier + **full §A6** (tenant wall) | **ZERO expected** (maos-loom-lite + store-internal guard, 11.2b precedent); FLAG-Winston bounded only if cross-team readmit needs a kernel variant beyond `CrossRegionReadmit` (verify) | 11.2a/b, Epic 12 (12.1) |
| **13.2** | Multi-tenant Loom — **cryptographic tenant boundary** (ADR-055, Fork-4) | Per-team HKDF key-weld + `canonical_kv_leaf` v2 (`source_team` in pre-image, v1 byte-compat); `team_guard` upgrades to signature-verify (closes 11.2b D1); per-team Merkle independence; forged-team-stamp negative; team-identity source-reflex. | 6 | frontier + **full §A6** (crypto boundary) | ZERO expected (HKDF + crypto in maos-loom-lite; verify) | 13.1 |
| **13.3** | Cross-team asymmetric consent + multi-hop distillation provenance | Cross-team re-attested writes over cohort links; asymmetric consent envelopes (team axis); flattened I11 chain in the crossing bundle; consented cross-wall `log.recall`; refusal first-class. | 6 | frontier + **full §A6** (cross-team consent) | ZERO expected (reuses 11.2a re-attestation; team dimension in maos-loom-lite; verify) | 13.2, Epic 12 (12.2) |
| **13.4** | FR37 vetting machinery (ADR-056) | `VettingAttestation` issue→install→promote→revoke, internal vetter keys; exact-hash + refuse-at-next-load; vetter-key lifecycle at the operator audit key; four-cause distinguishability; `check-vetting-attestation`. | 6 | frontier + **full §A6** (trust-tier) | ZERO (out-of-kernel registry + maos-compliance) | Epic 7 registry, §7.3 audit key |
| **13.5a** | Enterprise reference Spirit class | The 11th reference Spirit, Spirit-side: PDP (11.4a) + identity/at-rest/SIEM (11.4c) composed into a reusable enterprise-governed Spirit; proven wired at the daemon seam (not just constructed). | 6 | frontier + §A6 | ZERO expected (Spirit-side) | 11.4a, 11.4c (both done) |
| **13.5b** | Collective-tier erasure + legal-hold cascade | Extend the shipped Epic-9 forget/hold spine to collective rows and cross-team destination copies using 13.2 `source_team`; reconcile success, hold, and partial-failure outcomes. | 6 | frontier + **full §A6** (lifecycle boundary) | **FLAG-Winston expected** — current `CollectiveMemoryPort` has no erase operation and kernel `forget*` never reaches `MemoryTier::Collective`; do not preserve a ZERO claim by routing around the owner | 13.2, Epic 9 |
| **13.3b** | Provenance crosses the wall (H1 split, 2026-07-18) | Leaf **v3** carrying the flattened I11 chain; the **citer-auth cross-team ratification** (Story 8.10 AC2b reversal — ADR-grade); consented cross-wall `log.recall` with first-class refusal; author **ADR-058**, which **ports and amends** ADR-013 (see ⚠ below) and ports ADR-018. | ~5 | frontier + **full §A6** (provenance boundary) | verify — leaf v3 byte-compat over two frozen predecessors; the `LogRecallPort` risk is the **generic cascade**, not a signature change | **13.3, 13.5c** |
| **13.5c** | Single composition root + bootable tenant mode | Thread the live `Arc`s into `run_cohort_a2a_daemon_from_env` (dispatched at `main.rs:6934`, **4 648 lines after the primary root already built everything**); **delete TL #2**; one TL, one boot nonce; move `CohortManifestState::load` above the store so the tenant map resolves; **`MAOS_LOOM_HOME_TEAM` boots for the first time**; **first-ever `cohort-a2a-daemon` smoke**. | 5 | frontier + **full §A6** (runtime boundary) | **ZERO kernel-core** (~180 LOC in `maos-bin` + ~200 LOC first-ever test) | 13.1 |
| **13.5d** | Production Spirit→collective route | `[capabilities.required.loom]` + `capabilities_required_to_scopes` + **manifest schema bump** (Loom scopes are **not manifest-declarable** today ⇒ `PolicyDecision::Deny`); Spirit-side port on the `LiveResearcherMcpPort` shape; token-at-call-time through cap-gated `collective_write`/`collective_read`; **dead-wire negative**; cross-team correlation ID. | 6 | frontier + **full §A6** (runtime boundary) | **kernel-core ZERO; `maos-manifest` ≈ 75 LOC + schema version bump** — do not scope this as "zero-Δ, wire the adapter" | **13.5c, 13.3** (13.5a is **advisory, not hard** — see 13.5d preflight D26) |
| **13.5e** | Tenant audit isolation — NFR-Ops-11 **team axis only** | Per-team TL shard (`maos-iac` has **zero occurrences of "team"**); close the `ranged_recall` filter-discard (`let _ = spirit_filter;`) and the path-addressed capability-free `maos_audit::query` hole; tenant-blind-audit negative. | 5 | frontier + **full §A6** (audit boundary) | **not ZERO** — `maos-iac` schema + `maos-audit` path resolution + the single-holder sanction (**doc comment at `transparency_log.rs:16`, not an actual attribute**) | 13.5c |
| **13.6a** | **Authenticated team identity** *(new 2026-07-28, second 13.6 split)* | The signed manifest declares which **team** each host speaks for (`COHORT_SCHEMA_V4`, v1/v2/v3 bytes frozen); both A2A seams refuse a peer that does not speak for the team it claims; eviction enforced on **both** endpoints; `Defer` becomes a **refusal** on the crossing intent only; `loom-threat-model.md` **T1 corrected**. Provable against synthetic frames — **inverts no dead-wire leg.** | 5 | frontier + **full §A6** (identity boundary) | **ZERO kernel-core** @23401; non-kernel Δ in `maos-cohort` + `maos-a2a-core` | 13.5g |
| **13.6b** | **Production cross-team crossing initiators** *(was 13.6a; read side split out 2026-07-29)* | **Write side only.** A production emitter **inside the cohort-a2a-daemon runtime** on team A + an applier at `handle_intake_verified` on team B, reaching `apply_replication_bundle` with a real `CrossTeamApplyContext`. ⚠ Adds **the weld the 13.6a/13.6b split left behind**: consent is decided from `bundle.source_team` (payload, unauthenticated) while 13.6a authenticates `request.cohort_source_team` (envelope) — nothing binds them, so D-10's impersonation survives 13.6a until the applier refuses a mismatch. Plus a boot-time reconcile of `MAOS_LOOM_HOME_TEAM` against the signed `CohortMember.team`. **Assigns the `(f-i)` inverter the epic deliberately left UNASSIGNED.** | 6 | frontier + **full §A6** (crossing boundary) | **ZERO kernel-core** @23401 (verified equal at `a414f922`) | **13.6a** (done), 13.3b, 13.5d |
| **13.6c** | **Three-team / three-region substrate** *(new 2026-07-28, Track 2 parallel enabler)* | Three databases (**not nine** — the manifest cannot express a team spanning regions), both env namespaces, all four substrate-bearing jobs. ⚠ `check-cross-region-consensus` and `check-multi-region-slo` currently run with **no Postgres service at all**, so `three-region-convergence` and its topology-fraud negative have **never executed** since 11.2b. | 4 | frontier + §A6 | **ZERO** — CI workflow + fixtures + env contract | none (parallel to 13.6a) |
| **13.6d** | **Cross-wall recall production initiator** *(new 2026-07-29, read side split out of 13.6b)* | The read side is unreachable **structurally, not by a missing call**: `LogRecallFilter` has no team field, so *the request cannot express the question*. Adds the team dimension behind the validating constructor, a production surface that supplies it, and **ADR-058 Decision 2** — a successful cross-wall recall journals today as a plain local `log.recall`, so a cross-team **disclosure** is indistinguishable from a local read. **Assigns the `cross-wall-recall-no-production-caller` inverter.** | 4→5 | frontier + §A6 | **ZERO** @23401 — `maos_kernel_core::iac` is a Story-6.5 re-export shim; verify with the gate | 13.3b (citer-auth pin); sequenced after **13.6b** on the shared gate file only |
| **13.6e** | **Judge machinery — the evidence ledger, and `ABSENT` that actually blocks** *(done + five-chunk adversarial review complete 2026-08-04)* | Four derived evidence states over a unified leg shape across all four substrate-bearing journey gates; fail-safe `required`; both Family-A and Family-B vacuity escapes retired; 30 harness guards (29 ordinary proofs + the jointly required mutation falsifier); operator-key verification with trusted gate/leg mappings; dirty-local and GitHub-run binding; four workflow artifact uploads consumed by `check-ship-gate-completeness`; three observational successor controls. | 5 | frontier + **full §A6** (the judge's own instrument) | **ZERO kernel-core** @23679; post-review FLAG-Winston formula grant: `xtask 35226/35931`, aggregate `141396/144224` | **none — UNBLOCKS 13.6** |
| **13.6** | Reza Cortex journey closer + NFR-Scale-5 envelope | 3 teams × 3 regions composing 13.1–13.6d; allowed collaboration, explainable refusal/recovery, minimum-disclosure provenance, collective erase reconciliation, live production wiring, and the measured—not executed—14-institution envelope. ⚠ **Re-grounded 2026-08-04 at `b568a052` (third reframe):** 13.6c **shipped** the substrate so AC1 is a close-out; the evidence ledger **split out to 13.6e**; AC4 is **2-of-4 buildable** (legal hold has zero `maos-loom-lite` reach, vetting has no path to a refused crossing — both become findings with owners); AC2's *"one run"* is structurally impossible → **one composed topology** extending the existing 2-daemon harness. | 6 | frontier + **full §A6** (journey) | No blanket ZERO claim; verify the final baseline — ⚠ **pin is 23679, not 23401** | 13.1–13.5j, **13.3b**, **13.6a–13.6e**, 11.2b |

**Sequencing:** 13.1 (physical wall) → 13.2 (crypto boundary) → 13.3 (governed cross-team row) → **13.5c** → **13.3b** (provenance crosses the wall). ⚠ **CORRECTED 2026-07-19 by the 13.3b preflight:** 13.3b's consent layer cannot be wired before 13.5c — `LogRecallAdapter::new` is at `main.rs:2417` and `CohortManifestState::load` at `main.rs:8031`, **5 614 lines apart**; 13.5c closes that to `:2268`. No cycle exists (13.5c depends only on 13.1). The leaf/store layer of 13.3b does *not* need 13.5c (13.1's live legs construct `LoomLiteStore` directly) — the dependency is **consent-layer-only**, which is why it was missed. 13.4 (FR37) and 13.5a (Enterprise Spirit) remain parallelizable. **13.5c is now the unblocker** — it makes tenant mode bootable, so 13.1's and 13.2's walls stop being inert; 13.5d then makes the collective tier Spirit-reachable, 13.5b closes collective lifecycle after `source_team`, and 13.5e closes the audit axis. **13.6 is last and only judges; it never invents a missing mechanism inside the journey harness.** ~~**Eleven stories total.**~~ ~~⚠ **CORRECTED 2026-07-29: twenty stories total**~~ ⚠ **CORRECTED AGAIN 2026-08-04: twenty-one stories total** (13.1, 13.2, 13.3, 13.3b, 13.4, 13.5a–13.5j, 13.6a, 13.6b, 13.6c, 13.6d, **13.6e**, 13.6). The "eleven" literal predated 13.5f–13.5j and the 13.6 split and had gone stale through five closes; "twenty" predated the sixth split. ⚠ **The Status line at the top of this file and the story table above are still stale in other respects** (the table has no rows for 13.5f–13.5j; the Status line says "ELEVEN" and reports 13.3 as `ready-for-dev`) — those are **Story 13.6's AC6 deliverable**, deliberately left for the closer to correct with the rest of the stale-claim surface.

⚠ **SIXTH SPLIT, 2026-08-04 (13.6 → 13.6e judge machinery + 13.6 closer), operator-ratified — and the boundary was drawn by 13.6c before 13.6e existed.** The 13.6 re-grounding pass at `b568a052` (five parallel scouts) found the closer still had **no instrument**: the four evidence states this file requires at §"Evidence state is separate from enforcement class" are prose (one grep hit, an unrelated doc comment), and *both* mechanisms that should stop an unproven claim return zero. Story 13.6c had already refused to build it and named the successor three times in its own shipped text (`13-6c…md:88`, `:163`, `:185` — *"that is judge machinery and belongs with 13.6/13.6e"*), so the slot was declared before it was filled. Pre-dev checklist item 5 applies for the sixth time. **The rationale is ORDERING, not deferral** — 13.6e is substrate-independent and buildable immediately, and a closer that builds its own instrument and then grades itself with it in the same commit violates the rule that makes 13.6 worth having.

**The same pass disproved six premises of the ratified 2026-07-30 judge-machinery design**, which is why the split is a re-scope and not a copy-paste: **`MAOS_AUDIT_KEY_SEED` does not exist** (it is a string literal inside an error message at `maos-bin/src/main.rs:8475`; the real var is `MAOS_AUDIT_KEY` and it holds a *path*), **`sign_bundle` is audit-bundle-shaped** (signing a transcript through it means fabricating `AuditEntry` rows — use `release_verify::sign_sha256sums`/`verify_release_signature`, already reachable from `xtask`), **`LegResult` is twelve structs in three incompatible families** so *"rides fields every leg already emits"* is true of 2 of 12 gates, **there are TWO vacuity mechanisms on two gate families** (the 13.6 draft named only Family A, 13.6c's preflight named only Family B — both were half right, and Family B's escape is *live*: `roundtrip-slo` is RED at HEAD and exits 0), **there is no artifact channel at all** (`upload-artifact` count in `discipline.yml` is zero), and **the kernel pin is 23679, not 23401** — with a phantom `23596` row in the HISTORY ledger that no gate can see, because `grep -rn "HISTORY" xtask/src/` returns zero.

**Sequencing after the sixth split:** … → 13.5j → 13.6a → 13.6b → 13.6d → **13.6e** → **13.6**, with **13.6c parallel** throughout (now `done`). A new failure shape for the epic's catalogue, distinct from *a claim standing in for a control* and from *a control standing next to the decision it does not govern*: **a control whose named owner has already shipped** — `consent_grant` names *"13.6a, done"*, the private-tier Markdown residue names *"13-5h"* (done), and fallible `record_invocation` names *"the next kernel-touching story"* while three have shipped since. 13.6's AC5 therefore requires the ownerless sweep to be **mechanical and to scan for stale owners, not just missing ones** (13.6d Residual 1).

⚠ **THIRD SPLIT, 2026-07-28 (13.6 → 13.6a + 13.6), operator-ratified.** The 13.6 grounding pass at `cb412348` disproved 13.6's own AC2/AC3: **the cross-team crossing has no production initiator.** `is_granted` has exactly one non-test consumer (`replication/bundle.rs:917`) inside `apply_replication_bundle`, which the Blocking leg `replication-crossing-has-no-production-initiator` proves has **zero production callers** — while the consent apparatus is nonetheless fully constructed at four sites in the composition root (`main.rs:2745,2753,2784,2958`). That is exactly the *"constructed-but-unwired controls fail"* shape 13.6·AC1 exists to catch, so the closer would have been judging itself. The read side is unreachable **structurally**: `recall_cross_wall` needs a `&TeamId` and `LogRecallFilter` has no team field — the request cannot express the question. **And the crossing cannot be done in one process:** there is exactly one `LoomLiteStore::new` in the composition root, pinned to `MAOS_LOOM_HOME_TEAM`, so a crossing is necessarily emitter-on-A + applier-on-B over the cohort A2A mesh. Per pre-dev checklist item 5 the mechanism was split out rather than hidden in the closer — the same rule that produced H1→13.3b and 13.5c→13.5c/d/e. **13.6b is now the `(f-i)` inverter.** Sequencing: … → 13.5j → **13.6a → 13.6b → 13.6d** → 13.6, with **13.6c parallel** throughout.

⚠ **FOURTH SPLIT, same day (13.6a → 13.6a identity + 13.6b crossing), operator-ratified — and the rationale is ORDERING, not deferral.** Party-mode preflight + an entailment pressure-test found the crossing story carried two separable mechanisms with a hard order between them. `bundle.source_team` is a **self-declaration whose signature any seed-holding emitter can forge** (`bundle.rs:414` → `derive_team_signing_seed` works for *any* team, and the crossing requires every emitter to hold `MAOS_CROSS_TEAM_BASE_SEED`), consent is decided from that field (`:917`), and **no host→team edge exists in the signed manifest**. Build the crossing first and *every commit containing it* has asymmetric cross-team consent bypassable by impersonation — the exact trade the review's option P2 offered and the room **rejected**; re-creating it in commit order would be the same mistake one layer down. 13.6a lands the binding (provable against synthetic frames, inverting nothing); 13.6b wires the crossing with the impersonation negative live from its first commit. **Both build inside Epic 13, back to back.**

⚠ **FIFTH SPLIT, 2026-07-29 (13.6b → 13.6b write side + 13.6d read side), operator-ratified — and the post-13.6a preflight that produced it found the weld the FOURTH split left behind.** 13.6a shipped the mTLS-axis team binding at `a414f922` (`COHORT_SCHEMA_V4`, both seams, 44/44 Blocking legs, T1 corrected in-commit, kernel-core verified **23401 == 23401** after a 4,225-line commit). But 13.6a authenticates the **envelope** (`request.cohort_source_team`, `router.rs:1429-1445`) while the crossing decides consent from the **payload** (`bundle.source_team` → `is_granted`, `bundle.rs:875-917`), and **nothing binds them** — 13.6b's AC6 moved into 13.6a and its AC4 is explicitly barred from claiming impersonation coverage, so **no AC in either story owned the weld**. A seed-holding emitter can stamp a truthful envelope and a lying payload and the row lands. *A control standing next to the decision it does not govern* — a new entry in the epic's failure-shape catalogue, distinct from a claim standing in for a control. The same pass measured two more: **the emitter cannot be a one-shot** (the sole production outbound path is `prepare_outbound`, whose transport is built only in `build_cohort_a2a_daemon_runtime` ← the `cohort-a2a-daemon` dispatch; a sibling arm at `main.rs:4883` returns ~4,700 lines earlier — the 13.5a shape on the send side), and **the five-cause matrix has never crossed a process boundary *or* a kernel boundary** (`TransportCause` has zero occurrences in either A2A crate, and `kernel-core/src/memory/mod.rs:204` discards the cause via `Transport(_)`). The split itself is the write/read fault line: different crates, different dead-wire leg, different failure mode, and the read side is blocked **structurally** (`LogRecallFilter` cannot express the question) rather than by a missing caller. It also retired a stale seam comment (`bundle.rs:441`) that had steered three stories toward a Spirit-initiated crossing: `CollectiveMemoryPort` has no `share` verb and `write` is what `team_guard` refuses foreign-team rows on, so the Spirit initiates **publication** and the host initiates the **crossing** — and keeping them apart is a security property, because *a Spirit that can name a destination team is a Spirit that can be prompt-injected into naming one.*

⚠ **Threat-model correction owed at 13.6a.** `docs/loom-threat-model.md` T1 currently states *"same-region cross-team forgery is cryptographically infeasible"* and **CLOSED on the team axis at ENTRY**, and its blast-radius note scopes the exposure to an attacker who **"recovers"** the seed — a breach. Wiring the crossing makes holding the seed a **job requirement**, so T1's close was only ever true while signing was test-only. **No artifact may say "Fork-4 proven" of the crossing path.**

### 13.5c scoping pass — 2026-07-18 (party-mode, Splinter's carry; 3 code scouts)

The original 13.5c owned **seven** concerns and was the only story in the epic with no ZERO-Δ claim. Three consecutive mechanism stories (13.1/13.2/13.3) had deferred every production wiring into it. The scoping pass decomposed it into **three stories, one hand-off, and two written-down deferrals** — per the epic's own pre-dev checklist item 5.

**Scout findings that drove the split:**
1. **The "two composition roots" premise was FALSE.** `run_cohort_a2a_daemon_from_env()` is dispatched from *inside* `async fn main` (`main.rs:6934`); no top-level `return` exists between `:1955` and `:4296`. A `cohort-a2a-daemon` process **already constructs** the TL, `LoomLiteStore`, `MemoryManagerAdapter` and `CapabilityRegistryAdapter`, then calls a **zero-argument function** that ignores them and builds a second TL. **The merge is "give an existing function its arguments," not "join two roots"** — and `CohortManifestState::load` has **no transport dependency**, so it can move above the store and satisfy the tenant map. **`"cohort-a2a-daemon"` appears twice in the whole repo, both in `main.rs`: the mode has never been executed** — budget the smoke test as the larger half.
2. **Loom scopes are not manifest-declarable.** `capabilities_required_to_scopes` emits only `ProviderInfer` and `McpCall`; `CapabilitiesRequired` has two fields. `Scope::LoomRead/LoomWrite/LoomScan` exist but no manifest can declare them ⇒ the first `issue_with_mediation(pid, Scope::LoomWrite, …)` is `PolicyDecision::Deny` (`cap_policy/mod.rs:143`). **kernel-core Δ = 0 but `maos-manifest` Δ ≈ 75 LOC + a schema version bump.** Anyone scoping 13.5d as zero-Δ discovers this mid-implementation.
3. **The audit spine has never heard of the tenant wall.** `grep "team\|tenant\|operator_id" crates/maos-iac/src/` → **zero matches**. `maos_audit::query` is path-addressed and capability-free; `ranged_recall` discards its filter (`log_composition.rs:125`). Shipping 13.5d while declaring the audit axis served would leave **a cryptographically walled store on a tenant-blind, path-addressable log** — a wall around a door with a window next to it.

**Hand-off:** the old 13.5c AC5 (*"another team's raw TL only through consented `log.recall`"*) **restates 13.3b's deliverable list** — cross-team-capable `recall`, read-path consent, first-class refusal. It moves to **13.3b**; 13.5e keeps only *"label-only team identity reds the gate"* as a leg, emitting **ABSENT** until 13.3b lands. **The old 13.5c dependency row (`13.1, 13.3, 13.5a`) was stale — it predates the H1 split and never listed 13.3b.**

**NFR-Ops-11 — served on the TEAM axis, NOT closed (Mary).** The requirement is titled *multi-**operator*** tenancy and names four sub-axes; **Reza is one operator with three teams**, and the PRD conflated the two. Disposition:
- **(ii) per-operator TL shard** → **13.5e, on the team axis.** The only sub-axis 13.5c legitimately owned.
- **(iv) per-operator GDPR-erasure scope** → **13.5b's**, always was. 13.5c never owned it.
- **(i) per-operator namespace** → **deferred past v2.2.** The v1.0 "reservation" is two string literals (`reserved_namespaces.rs:32`) with no non-test consumers, deliberately held *outside* the grammar-lock hash. Reza's journey does not exercise the operator axis.
- **(iii) per-operator capability-token signing key** → **deferred past v2.2, and it had NO owner in Epic 13.** ⚠ **Correct 13.1's F14**, which claims *"13.2 covers the signing key"* — a **misattribution**: 13.2 shipped `derive_team_signing_seed` in `maos-audit/src/sealed_export.rs` (the *replication-bundle* key). NFR-Ops-11(iii) means the **capability-token** signing key (`main.rs:2116`), still one global random-per-boot value carrying a `FIXME`.
- **Therefore: no artifact may claim NFR-Ops-11 complete at v2.2.** It is served on the team axis; the operator axis is deferred with this reason recorded.
- ⚠ **AMENDED 2026-07-19 (13.5e preflight).** Two corrections. **(a) Sub-axis (iii) is OPEN AND OWNERLESS, not "scheduled."** "Deferred past v2.2" has no successor story, no epic, and no owner — it must be recorded in the same register as the region `source_log_ref` residual (open since 11.2b D1, three sessions without a successor), or it will read as planned work that nobody planned. **(b) The stale-claim surface is FOUR sites across TWO files, not two:** `_bmad-output/planning-artifacts/epics/requirements-inventory.md:242` and `:514`, and `implementation-readiness-report-2026-07-10.md:43` and `:54`. Note the path — `requirements-inventory.md` lives under `epics/`, not at the planning-artifacts root, as an earlier note claimed. `:514` additionally names the **pre-split** 13.5c and conflates the two axes.

**Demo-ability:** the smallest real wall is a **CI-provisioned Postgres service with two distinct `datname`s** (13.1·AC5). Loom-lite has no SQLite collective store; a SQLite twin would prove code that does not ship. The full rung is the **3-team × 3-region Reza scene** (13.6). Absence of its live-substrate evidence may leave development lanes advisory, but it blocks the Reza/v2.2 product claim.

---

## Per-story AC sketch (finalize at preflight)

**13.1 — Multi-tenant Loom — physical tenant wall** (ADR-055)
1. Database-per-team: distinct `datname` per team on operator-assigned Postgres; private store-internal `team_guard` below `CollectiveMemoryPort`, invoked once at each Spirit-facing `read`/`scan`/`write` entry point. The static chokepoint leg proves each site and its unguarded negative twin.
2. Mapping ownership: schema-v2 `[[teams]]` is part of the operator-signed cohort manifest; a `TenantMapPort` implementation lives in a leaf `maos-tenant-map` crate and is injected at the composition root. Staleness uses the shipped `t_stale_secs` time lease—there is no announced-version channel. **F4 schema-v2 canonical-signature compatibility must be ratified before development starts.**
3. Membership is signed by stable `SpiritId`, then registered to runtime-local `spirit_pid` at composition. `team_guard` compares the manifest team with `StoreConfig.home_team`; connection construction verifies manifest `datname == current_database()`. Refusal is typed at `StoreError` and consciously lossy at the frozen collective port.
4. Physical row ownership only: the owning team is the database containing the row, proven by an unguarded physical-absence witness. **13.1 does not claim `source_team`, collective forget, or forge resistance**; 13.2 adds source/key identity and 13.5b closes erasure/hold.
5. `check-multi-tenant-loom`: hermetic Blocking chokepoint/tenant-map legs plus a CI-provisioned real-Postgres two-`datname` physical leg. A forged team stamp is deliberately documented as served at 13.1; crypto/provenance/lifecycle/runtime/journey legs emit **ABSENT**, never disappear or silently green.
6. ZERO kernel-Δ @23202 for this story only; ADR-055 and `docs/loom-threat-model.md` are AC deliverables. Any pressure to change kernel-core stops for FLAG-Winston rather than being absorbed.

**13.2 — Multi-tenant Loom — cryptographic tenant boundary** (ADR-055, Fork-4)
1. Per-team HKDF key-weld (Fork-4): second HKDF stage over the region seed with a **frozen versioned `TEAM_INFO_PREFIX`** grammar (mirrors 9.4b exactly); `verify_bundle` for cross-team bundles derives the pubkey from `(claimed_region, claimed_team)`, **never from bundle contents**.
2. `team_guard` upgrades to **signature verification** (not presence-only) — closes the **11.2b D1 presence-only residual** for the tenant wall: a foreign-team row without a valid re-attested signature under the derived team key is **refused, never served**.
3. `canonical_kv_leaf` v2: `source_team` enters the leaf pre-image under a **versioned domain tag**; 11.2a v1 leaves untouched (byte-compat by construction, the 9.2b idiom).
4. Per-team Merkle roots **independently re-derived** (mutating team B's store does not move team A's root); the payload-oracle + row-count-oracle catch what the SET-root is blind to (11.2a L3).
5. `check-multi-tenant-loom` (crypto legs): guard proven-red on a **live read** (foreign-team row without valid re-attestation refused, verifier **independently derived from the write codec**); per-team Merkle independence; **forged-team-stamp negative** (a same-region team forging another team's bundle is rejected — the Fork-4 payoff); **team-identity source-reflex** (source-team identity derived from the derived team key, not a label).
6. ZERO kernel-Δ expected (HKDF + crypto in maos-loom-lite; verify).

**13.3 — Cross-team asymmetric consent + multi-hop distillation provenance**
1. Cross-team sharing = an explicit, consented, **re-attested write into the other team's database**, never a shared table; the re-attested copy is the destination team's row (Merkle/capacity/GDPR-erasure) with `source_team` provenance for the forget-cascade (9.2 erasure spine must know whom to cascade to).
2. Asymmetric cross-team consent envelopes: governed by per-(peer,role) tuples (Epic 12) **extended to the team axis**; asymmetry (A shares X with B, B does not reciprocate) expressed in the manifest schema and enforced two-seam.
3. Multi-hop distillation provenance (the Reza "14 prior schema decisions cited in one consolidated proposal"): a cross-team distillate carries its **flattened I11 chain** (`source_log_ref` flattened-to-raw + `distillation_depth` + `intent_lineage`, per ADR-014/018) inside the re-attested crossing bundle — provenance lands with the row; ordinary traceback dereferences within the consumer team's own database.
4. Cross-wall raw traceback: dereferencing another team's TL is an **ADR-012-consented `log.recall`** to the source team, journaled on both sides; refusal is a **first-class surfaced outcome** (provenance-presence, ADR-049 §7 orphan discipline).
5. `check-multi-tenant-loom` (provenance legs): cross-team distillate provenance round-trip (flattened chain lands with the row; consented raw traceback works; unconsented refusal surfaced); **asymmetric-consent negative** (B→A share refused when only A→B consented). Proven-red on **real** cross-team writes.
6. ZERO kernel-Δ expected (reuses 11.2a re-attestation; cross-team dimension in maos-loom-lite; FLAG-Winston bounded only if a cross-team readmit seam is genuinely required — verify at preflight, recall the 11.2a +59 precedent).

**13.3b — Provenance crosses the wall** *(new 2026-07-18, H1 split)*
1. **Leaf v3** carries the flattened I11 chain across the wall: `source_log_ref` (flattened-to-raw) + `distillation_depth` + `intent_lineage` enter the crossing bundle under a **versioned v3 domain tag**, with **v1 AND v2 leaves byte-identical by construction** (the 9.2b idiom, now two frozen predecessors). Today `CollectiveKvLeaf`/`CrossRegionReplicationBundle` carry **none** of these fields — there is no wire representation at all.
2. **The citer-auth cross-team ratification — ADR-grade, never incidental.** `distillate.rs:330-359` (Story 8.10 AC2b) rejects `CiterUnauthorized` whenever `e.spirit_pid != spirit_pid`, **over the flattened refs**, precisely *"so a digest-of-digest cannot launder a cross-principal raw frame through an intermediate hop."* Cross-team citation is **exactly what that control was built to prevent**. Either it is widened under a ratified rule with its own negative control, or the Reza traceback scene does not reproduce. **Do not relax it as a side effect of making a test pass.**
3. **Consented cross-wall `log.recall` with first-class refusal.** `LogRecallPort` is two methods, `spirit_pid`-only; `recall` hard-pins emitter scope (`log_recall.rs:282`); the consent check is **commented out** (`:378-386`, *"a no-op pass-through at v0.3-β"*); `LogRecallError` has **no refusal variant**. All three are this story's. ⚠ **`ranged_recall` is forbidden** — path-addressed, capability-free, and compile-pinned out by `spirits/researcher`.
4. **Author ADR-058** for cross-wall read-consent, and **port `ADR-013` (log.recall) and `ADR-018` (intent provenance) into `docs/adr/`** — both are cited as binding by the PRD and §15.3 and **neither exists**; the index jumps 012 → 014. ADR-012 governs cross-**Host** write-side frame admission and cannot be stretched to cover reads.
5. **Provenance-presence, never provenance-promise (ADR-049 §7).** Origin ≠ authorization. A refused cross-wall recall is an **observable state**, never folded into a clean total, and **no AC, gate, or doc may assert the bundle establishes authorization provenance.**
6. Absorbs the old 13.5c AC5, which restated this deliverable list verbatim. **13.6 depends on this story.**

**13.4 — FR37 vetting machinery** (ADR-056)
1. `VettingAttestation` = Ed25519-signed envelope binding (manifest **exact-hash**, from-tier, to-tier, vetter-key-id, expiry, `revocation_semantics`, optional `successor_policy`); out-of-kernel (registry + `maos-compliance`); `public-vetted` = the 4th trust tier; promotion is an **attestation artifact, never a registry flag**; kernel admission unchanged (the strictest-of floor already reads the tier).
2. Full flow with **INTERNAL vetter keys**: issue → install → promote → revoke round-trip on a clean host, verifier independently derived; accredited external vetters (NFR-Comp-2) explicitly **v2.5**.
3. Upgrade semantics (ADV-056-1): exact-hash → upgrade-without-current-attestation = **admission refusal at the floor** (the flap is the feature); `successor_policy` (`exact-only` | `re-issue-required-with-expedited-review`); the target version's attestation is evaluated **before the chain starts** (folded into `maosctl swap --plan` precondition, ADR-036).
4. Expiry/revocation vs running Spirits (ADV-056-2): v2.2 ships **`refuse-at-next-load` only** + a **mandatory journaled observation event** when the compliance layer detects expiry/revocation while an affected Spirit runs; audit distinguishes **four terminal causes** (vetting-revocation / expiry-lapse / registry-yank / operator-local); `drain-and-refuse` named as the v2.5 slot (honest zero-kernel-Δ).
5. Vetter-key lifecycle (ADV-056-3): enrollment/rotation/revocation are Ed25519-signed events **signed by the operator audit key** (§7.3 root), journaled; `verify` walks attestation → vetter-key enrollment → operator root, refusing attestations whose vetter key lacks a journaled enrollment predating issuance.
6. `check-vetting-attestation`: issue→install→promote→revoke round-trip (independently-derived verifier); forged-signature, expired-attestation, **and forged-vetter-key (unenrolled key, valid signature)** negatives; upgrade-flap control (new version without attestation refused at the floor); running-Spirit lapse produces the journaled observation; four-cause distinguishability. ZERO kernel-Δ.

**13.5a — Enterprise reference Spirit class**
1. The 11th reference Spirit, composed **Spirit-side** (zero kernel): PDP (11.4a) + identity/at-rest/SIEM (11.4c) into a reusable enterprise-governed Spirit **class**.
2. Enterprise governance E2E through the Spirit lifecycle: SSO/OIDC principal → Enterprise PDP decision → at-rest AEAD → SIEM export — **reusing** the 11.4a/c subsystems, not re-implementing them.
3. Composition proven at the **daemon seam** (E11 retro lesson — real subsystems passed isolated tripwires while dead-wired in 11.4c): an **available-arm integration leg** proves the Spirit is actually wired end-to-end, not merely constructed.
4. Reference-Spirit **template + docs** so an operator can instantiate an enterprise-governed Spirit (registry/scaffold reuse, ADR-008).
5. Gate (enterprise-reference-spirit leg, folds into `check-multi-tenant-loom` or a sibling): real SSO→PDP→at-rest→SIEM round-trip **through the Spirit**; issuance-bypass-absence; **dead-wire negative control** (a constructed-but-unwired Spirit reds).
6. ZERO kernel-Δ (Spirit-side). Depends 11.4a + 11.4c (both `done`).

**13.5b — Collective-tier erasure + legal-hold cascade**
1. Add an explicit collective erase contract to `CollectiveMemoryPort`; kernel `forget`/`forget_with_reason` reaches the collective tier rather than stopping at local memory.
2. Resolve every destination-team copy from 13.2's cryptographically-bound `source_team` provenance; never infer ownership from a mutable label or audit text.
3. A valid erase reconciles the source row and every authorized cross-team copy with independently-derived row and audit evidence.
4. A destination copy under legal hold is not deleted; the source and destination audit surfaces distinguish `held` from `erased`, `not-found`, and `failed`.
5. Partial failure cannot report success: remaining copies, responsible team, retry safety, and correlation IDs are explicit; a planted one-sided erase reds the gate.
6. Preflight owns the port/kernel design and baseline budget. A kernel arm is currently required by evidence; any repin is named and FLAG-Winston-reviewed rather than hidden behind an out-of-kernel shim.

**13.5c — Single composition root + bootable tenant mode** *(rescoped 2026-07-18)*
1. `run_cohort_a2a_daemon_from_env` receives the live `Arc`s already constructed by the primary root (`transparency_log`, `boot_nonce`, capability/memory as needed) instead of being a zero-argument function; **TL #2 (`main.rs:7950-7958`) is deleted** along with `transparency_log_path` and `own_boot_nonce` from `CohortDaemonFileConfig`. One process, one Transparency Log, one boot nonce.
2. `CohortManifestState::load` moves above the store construction (it has **no transport dependency**; only refresh does), and its `Arc` reaches `tenant_map_for_store` — so **`MAOS_LOOM_HOME_TEAM` boots** instead of hard-failing at `main.rs:2286`. 13.1's and 13.2's walls stop being inert.
3. The refresh loop keeps working after the move: `pull_from`/`service_pending_pulls` still require the A2A transport, which stays in the tail dispatch and mutates the same `Arc`. Lease freshness is proven, not assumed.
4. **First-ever `cohort-a2a-daemon` smoke test.** The mode has never been executed — `"cohort-a2a-daemon"` occurs twice in the repo, both in `main.rs`. A boot→refresh→serve smoke is an AC deliverable, not a follow-up; it is expected to be the larger half of the story.
5. **ZERO kernel-core Δ** (~180 LOC in `maos-bin`, ~200 LOC of new test). Audit correlation is *unblocked* here but not *implemented* — two uncorrelated logs cannot carry a correlation ID, so collapsing to one TL is 13.5e's precondition, and 13.5e's claim.

**13.5d — Production Spirit→collective route** *(new 2026-07-18)*
1. **`[capabilities.required.loom]` is parseable and reaches the policy table.** Extend `CapabilitiesRequired` + `capabilities_required_to_scopes` (`maos-manifest/src/manifest.rs:512-539`) to emit `Scope::LoomRead`/`LoomWrite`/`LoomScan`, with the **manifest schema version bump** the `deny_unknown_fields` posture requires. Without this, every mediated collective call is `PolicyDecision::Deny` — this AC is the story's real cost, not an afterthought.
2. A Spirit performs collective read/write through a **mediated production path**: Spirit-side port trait in the Spirit crate, impl at the composition root holding the kernel handles, **token issued at call time** — the `LiveResearcherMcpPort` shape (`main.rs:1249-1357`) copied, not reinvented.
3. The route takes the **cap-gated `collective_write`/`collective_read`** (`memory/mod.rs:210`/`:274`) which enforce I1 verify + I2 journal + scope discrimination + the LoomWrite TTL ≤ 60 s cap. **`SpiritMemoryView` is a trap** — it forwards only to the trait path, which is a permanent `CapabilityDenied` in production by design (`memory/mod.rs:721-739`); do not "fix" `main.rs:2402`.
4. **Dead-wire negative**: a constructed-but-unwired collective port is proven red. Extend the existing `xtask/tests/story_10_4a_ac1_proven_red.rs` harness — the proven-red idiom, recording port, and available-arm gate all already exist.
5. Cross-team actions reconcile requester team, destination team, consent decision, store row, and both audit references under **one correlation ID**. ⚠ No such ID spans an A2A frame and a store write today, and `cost.rs:264-266` **asserts against** `correlation_id` in v1.0 cost frames — introducing one is a ratified change, and it collides with the `CollectiveMemoryPort` widening 13.5b is already doing. Coordinate the port change once.
6. **kernel-core Δ = ZERO; `maos-manifest` Δ ≈ 75 LOC + schema bump.** Say both sentences. Depends on 13.5c (bootable tenant mode) and 13.5a (a live Spirit to hang the port on — sequencing 13.5a first roughly halves this story's wiring cost).

**13.5e — Tenant audit isolation (NFR-Ops-11, team axis only)** *(new 2026-07-18)*
1. Per-**team** Transparency Log isolation. Today `grep "team\|tenant\|operator_id" crates/maos-iac/src/` returns **zero matches** — the audit spine has never heard of the tenant wall. A shared table plus a team predicate is **rejected by ADR-055's own precedent** (*"one omitted predicate defeats the wall"*), so isolation follows the shipped database-per-team shape.
2. Close the two tenant-blind read holes: `maos_audit::query` opens the log **path-addressed and capability-free** (`maos-audit/src/lib.rs:184`), and `ranged_recall` **discards its own filter** (`log_composition.rs:125`, `let _ = spirit_filter;`).
3. Stable task/team correlation across the single TL that 13.5c produced — two uncorrelated logs could not carry it.
4. **Tenant-blind-audit negative**: a cross-team raw-log read that succeeds without a team check reds the gate. *"Label-only team identity reds the gate"* is retained here; the consented cross-wall `log.recall` half moved to **13.3b** and this leg emits **ABSENT** until 13.3b lands.
5. **Not ZERO-Δ, and named as such:** `maos-iac` schema + `maos-audit` path resolution + the single-holder sanction — ⚠ **which is a doc comment, not a control**: `#[i9_exempt]` appears only in prose at `transparency_log.rs:16`; the attribute is **not on the struct**, and `check-empty-kernel` never scans `maos-iac` + NFR-Ops-9 backup/DR implications, because the same SQLite file also holds the memory stores, `legal_holds`, and the schema registry. **NFR-Ops-11 is served on the team axis and NOT closed** — sub-axes (i) namespace and (iii) capability-token signing key are operator-axis and deferred past v2.2; (iv) is 13.5b's.

**13.6 — Reza Cortex journey closer + NFR-Scale-5 envelope**
1. **Real composition:** one 3-team × 3-region run composes per-team Postgres, PDP/identity, physical+crypto tenant walls, asymmetric consent, vetting, the Enterprise Spirit, collective lifecycle, and the production collective/audit path from 13.1–13.5c. Constructed-but-unwired controls fail.
2. **Allowed collaboration + minimum disclosure:** Reza obtains the consolidated proposal through an allowed A→B asymmetric share. The crossing bundle contains only policy-allowed provenance; raw payload, secret-bearing fields, and unconsented TL references are negative controls.
3. **Explainable refusal and recovery:** reverse B→A share, stale tenant map, and vetting lapse/hold produce distinguishable operator outcomes naming the responsible authority and safe next action. Retry succeeds only after a valid manifest/consent/vetting repair.
4. **Lifecycle reconciliation:** source-team erase or legal hold exercises 13.5b against destination copies. Both audit sides reconcile `erased`/`held`/`failed`; a one-sided result or an unauthorized hold bypass is RED.
5. **Evidence-grade gate:** the Reza scene derives from real multi-team activity on the 3-region Postgres substrate. The 14-institution result remains a measured capacity envelope—never an assertion that 14 institutions executed. Each leg emits evidence state + artifact reference.
6. **Boundary preservation:** physical absence, team-key source-reflex, provenance minimum-disclosure, tenant TL isolation, duplicate/correlation reconciliation, and the post-13.5 baseline all hold. Any **ABSENT** or **INDETERMINATE** required leg blocks the Reza/v2.2 product claim.

---

## Gate discipline (§A7 reflexes named per gate — E11 retro carry-forward)
- **`check-multi-tenant-loom`** — derive-and-reconcile counts; a CI-provisioned real-Postgres two-`datname` physical leg; independently-derived verifier from the write codec; team-identity source-reflex after 13.2; physical-absence control a shared table cannot fake; explicit ABSENT declarations for unbuilt crypto/provenance/lifecycle/runtime/journey legs.
- **`check-collective-erasure`** — source→destination copy resolution from cryptographically-bound `source_team`; legal-hold negative; one-sided/partial-failure proven-red; independently reconciled store and TL evidence.
- **`check-reza-production-path`** *(rescoped 2026-07-18 — was one gate for the whole of old 13.5c)* — **13.5c legs:** single-TL/single-boot-nonce reconciliation, tenant-mode **boots** (the `MAOS_LOOM_HOME_TEAM` negative flips green), refresh-lease liveness after the load moves, and the **first-ever `cohort-a2a-daemon` smoke**. **13.5d legs:** real Spirit/daemon→collective-store call path through the **cap-gated** `collective_write`/`collective_read`, **dead-wire negative** (constructed-but-unwired port reds), manifest-declared `loom.*` scope actually reaching the policy table, and cross-team correlation. Consented `log.recall` and tenant TL isolation are **NOT** this gate's — see below.
- ~~**`check-tenant-audit-isolation`** *(new, 13.5e)*~~ — ⚠ **NOT CREATED. Corrected 2026-07-19 by the 13.5e preflight (M8 RE-SCOPED).** 13.5c's preflight already ratified **K5(b)** — "do not create the gate" — on four reasons that apply here verbatim: `check-multi-tenant-loom` already keys the exact `MAOS_TEST_POSTGRES_TEAM_A`/`_B` substrate, already models both `BindingClass` values with the non-vacuity guard, is ADR-055's gate, and already carries the `ABSENT_SUCCESSORS` entry that must be rewritten anyway. **13.5e's legs go onto `check-multi-tenant-loom`**, saving five registration surfaces. The legs themselves are unchanged: per-team TL isolation on a spine where `grep "team" crates/maos-iac/src/` currently returns **zero**; the two tenant-blind read holes proven closed (`maos_audit::query` path-addressed/capability-free; `ranged_recall`'s `let _ = spirit_filter;`); tenant-blind-audit negative; the *label-only team identity* leg emits **ABSENT until 13.3b lands**.
- **`check-vetting-attestation`** — forged-vetter-key negative; exact-hash upgrade-flap control; four-cause distinguishability derived, not asserted.
- **Dead-wire negatives are now standing discipline** (Epic-13 retro item, forward-only): every mechanism story's gate carries a leg proving what it does **not** wire, green while unwired and **inverted by the story that wires it**. 13.3's leg (f) `crossing-not-production-wired` is the first.
  - ⚠ **CORRECTION 2026-07-18 (found during 13.5c/13.5d story generation):** an earlier draft of this line said *"13.5d inverts it."* **That is false** — 13.5d's route is `DirectWrite` and never touches `apply_replication_bundle`. A dead-wire leg must therefore be written as **separately-named clauses, each with its own named inverter**, never as one composite assertion; a composite leg here would have been red by 13.5c while 13.3's own may-not-claim table asserted it stayed green. Split: **(f-i) `no-production-crossing-initiator` — inverter UNASSIGNED** (neither 13.5c nor 13.5d wires the crossing; named as an open gap, not given a fictional successor); **(f-ii) `tenant-mode-unbootable` — inverter 13.5c**.
  - **Generalized rule:** when authoring a dead-wire negative, enumerate one clause per thing-not-wired and name that clause's inverter. If a clause has no inverter, say so — an unsuccessored clause is honest; a mis-assigned one is a claim standing in for a control.
  - ✅ **CONFIRMED MECHANICALLY 2026-07-19 (13.5d preflight).** **(f-i) stays UNASSIGNED — 13.5d is definitively not its inverter**: `apply_replication_bundle` and `build_replication_bundle` have **zero non-test callers**. The split was the right call and the empty slot is real, not an artifact of incomplete search.
  - ✅ **(f-i) INVERTER ASSIGNED 2026-07-28 → Story 13.6b** *(named 13.6a when assigned; renamed by the fourth split).* Re-measured at `cb412348`: still zero production callers after 13.3, 13.3b, 13.5c **and** 13.5d — four stories, slot still empty, which is exactly what an honest unassigned clause is supposed to look like. The 13.6 grounding pass hit it head-on (13.6·AC2 cannot compose a mechanism with no entry point) and the operator ratified the split. The clause is now owned, and the same pass assigned **`cross-wall-recall-no-production-caller`** — a second unassigned clause on the read side — to the same story. ⚠ **CORRECTED 2026-07-29 by the fifth split:** the two clauses are **no longer inverted in one commit**. `(f-i)` is **13.6b's** (write side); `cross-wall-recall-no-production-caller` is **13.6d's** (read side) — they share a gate file and nothing else. Each is still inverted **in its own story's own commit**, replaced by a positive leg naming both endpoints plus a new negative that only becomes falsifiable because that path is live (an unconsented crossing must be refused **at the applier**, not merely un-initiated at the source).
  - ⚠ **New inverter edge found by the 13.5e preflight:** 13.5e's dead-wire negative names **11.4b** (escape-detector read wiring) as an inverter alongside 13.5d/13.5b. `Detector::detect` is a dead-wired fifth read surface with zero production callers, owned by 11.4b. The dependency DAG did not show this edge.

### Evidence state is separate from enforcement class
Every journey-relevant leg emits exactly one evidence state: **`PROVEN_BLOCKING`**, **`PROVEN_LIVE_SIGNED`**, **`ABSENT`**, or **`INDETERMINATE`**, plus its artifact reference when proven. This is orthogonal to `BindingClass::{Blocking, AdvisorySubstrate}`: an unavailable live substrate can remain advisory for a development lane while its evidence state is `ABSENT`, which prohibits the Reza completion claim. `ABSENT` never becomes green; `INDETERMINATE` is neither failure nor completion and requires reconciliation.

## Kernel-delta budget *(updated 2026-07-18)*
Baseline **23202**, pin == actual verified at HEAD `652347b8`. **There is no Epic-wide ZERO claim**, and — the 13.5c scoping pass added this — **"kernel-core ZERO" is not the same sentence as "zero delta."** State both.

| Story | kernel-core | other crates |
|---|---|---|
| 13.1, 13.2 | **ZERO** (shipped, verified) | — |
| 13.3 | **ZERO** | `maos-domain` port-error widening ⚠ **a new `CollectivePortError` variant is `E0004` in kernel-core** (`memory/mod.rs:189-197` is wildcard-free, enum not `#[non_exhaustive]`) — take the `Transport(TransportCause)` route; `maos-cohort` `COHORT_SCHEMA_V3` |
| 13.3b | verify | leaf **v3** byte-compat over two frozen predecessors. ⚠ **Reframed 2026-07-19:** the risk is **not** a `LogRecallPort` signature change. `check-kernel-baseline` is a **line count** (`count_rs_lines`), not content equality — **a rename does not red it.** The real exposure is the **generic cascade** through `KernelCtx`/`HookDispatcher` headers plus rustfmt reflow; coupling is **11 lines, not 6** |
| 13.4, 13.5a | **ZERO** | — |
| 13.5b | **FLAG-Winston expected** — collective port op + kernel forget arm | — |
| **13.5c** | **ZERO** | `maos-bin` ~180 LOC + ~200 LOC first-ever test |
| **13.5d** | ✅ **NO LONGER ZERO — FLAG-Winston GRANTED 2026-07-19 (bounded).** `collective_write`/`collective_read`/`collective_scan` never compare `spirit_pid` to `token.spirit_pid` (declared `memory/mod.rs:212/276/319`, forwarded `:268/311/355`, **never compared**; `verify()` takes no pid argument). A forged pid selects the tenant at `store.rs:517` while the audit logs the *token's* pid. Unreachable today only because `collective_*` has no callers **and** `spirit_bindings` is empty — **13.5d removes both in the same commit.** The only fix that holds for the next caller is kernel-core: **~+9 lines, re-pin 23202 → the MEASURED value** (`~23211` is a projection, not the grant). **Grant is fenced:** the pid-equality check on the three collective methods only; the L1(c) `Scope`-payload change stays **unauthorized**; re-escalate rather than spend the grant elsewhere; **do not move the pin before the code**; kloc headroom is **35 lines** (17865/17900) — verify. Full terms in the story's `### FLAG-Winston authorization (L10)` | **`maos-manifest` + a manifest schema version bump (3→4, not v3)** — Loom scopes are not manifest-declarable today, so `Scope::LoomWrite` is `PolicyDecision::Deny`. Anyone scoping this as "zero-Δ, wire the adapter" discovers it mid-implementation. |
| **13.5e** | verify | **not ZERO** — `maos-iac` schema + `maos-audit` path resolution + the single-holder sanction (**doc comment at `transparency_log.rs:16`, not an actual attribute**) + NFR-Ops-9 backup/DR (one SQLite file also holds memory stores, `legal_holds`, schema registry) |
| **13.6a** | **ZERO expected** @23401 — ⚠ `maos_kernel_core::iac` *looks* like kernel-core at `main.rs:2950` but `crates/maos-kernel-core/src/iac.rs:13` is `pub use maos_iac::*;`, a Story-6.5 back-compat shim. `LogRecallAdapter` lives in `maos-iac`, so adding to it does not move `count_rs_lines` over `maos-kernel-core/src`. **Verify with the gate; do not assume.** | `maos-bin` (emitter verb + applier arm), `maos-loom-lite`, `maos-iac`, `maos-domain` (target-team on the recall request). **Do NOT add a `FrameKind` variant** — `abi-diff` is a null control that cannot see it; ride the shipped `collective:share` intent |
| 13.6 | verify final baseline after 13.5b/13.5d/13.5e/**13.6a** | `xtask` (evidence ledger + non-vacuous `ABSENT`), `.github/workflows` (3×3 substrate), `maos-bench`, docs |

Every FLAG-Winston seam: name the minimal surface, count it, update HISTORY, prove no unrelated churn. **Preserving a ZERO claim by building a parallel out-of-kernel owner is forbidden.** Note `check_multi_tenant_loom.rs:275` carries a **duplicated `23202` literal** in violation of `kernel-core-baseline.toml`'s "no second literal" invariant — any re-pin must update it too.

## Cut / deferred (not Epic 13)
- 100-host churn scale-out, 10-host rotation chaos → **Epic 14**.
- Accredited external vetters (NFR-Comp-2), external FKCS authors, external N=12 → **v2.5** (non-gating).
- App-D.4 partner-org federation tier (deferred, trigger = first partner-org request).
- 30-day soak (NFR-Scale-1) + absolute geo-SLO → **release-gate artifacts**, not closable ACs.
- SAML, Vault/cloud-KMS backends → additive-per-port, **Epic 14 sweep** or deferred.

## Three preflights closed 2026-07-19 (13.3b, 13.5d, 13.5e) — outcomes

Dispatched in parallel, same adversarial brief as 13.5c's. **Verdicts: 13.5e `ready-for-dev`; 13.5d `needs-rework` → ✅ `ready-for-dev` same day (all three blockers cleared: L10 authorized, AC7 written, six evidence corrections folded); 13.3b `blocked`.** Across the three, **seven of the briefing agent's own premises were disproved** — the same rate as 13.5c's preflight. The pattern is now stable enough to name: *the brief that dispatches a scout is itself unscouted.*

**What changed at epic level:**
1. **A new hard edge:** 13.5c → 13.3b (consent layer only). Recorded in Sequencing above.
2. **A softened edge:** 13.5a → 13.5d is **advisory**, so the critical path is shorter than drawn. `researcher` is constructed at the composition root (`main.rs:3815`), supplies the trait shape and the enterprise runtimes, and its `provider.complete` block **discharges fork L2 outright**. 13.5a's own AC sketch never mentions memory — so it has no memory surface either, which makes "13.5a first" a non-sequitur.
3. **A kernel re-pin candidate:** 13.5d's `spirit_pid` binding hole (see the delta table). **Needs FLAG-Winston before 13.5d starts.**
4. **A gate that will not be created:** `check-tenant-audit-isolation`, on 13.5c's own K5(b) precedent.
5. **An ADR that exists after all:** ADR-013, in the archive, foreclosing 13.3b's AC3.

**Two gates found to be null controls** (instances #20 and #21 of *a claim standing in for a control*):
- **`abi-diff` cannot see a schema-version bump.** `cargo public-api` emits const signatures **value-erased** (`abi-baseline/v1-pre-bump.txt:148` is `pub const …ABI_VERSION: u32` with no value), the baseline does not contain `MANIFEST_SCHEMA_VERSION` at all, and `abi_diff.rs:48` fails only on **removed** lines. It is a registered, blocking job **with no sensor**. 13.3's claim that ABI gates are null *for kernel reasons* and 13.5d's claim that they invert *because the change is in that crate* are **both wrong** — the two stories are blind for different reasons.
- **`POST_V1_SCHEMA_SECTIONS`'s check is vacuous** — it greps the source it parsed from. `manifest-field-coverage` is blind parser→listed; **v3 already fell through it.**

**Meta-cause worth a retro item:** `check_ship_gate_completeness` never validates CI→registry, which is the shared upstream of both null controls above and of `check-abi-ratification` having no CI job at all.

**An ordering cycle nobody had found (13.5e):** `CohortManifestState::load` takes a TL-backed audit sink, so the dependency is **TL → manifest → tenant map → validated team → TL path**. Moving 13.5c's load earlier cannot break it. This is why H-ORDER survives 13.5c and why 13.5e needs a two-stage design rather than a reordering.

**One reversal that saved a wall from being built out of prose:** 13.5e's own recommended H-ORDER fix (a bare env-var read at `main.rs:2057`) is **not a control** — `home_team` at `:2282` is read *inside* the `MAOS_LOOM_POSTGRES` arm and `connection_assignment_guard` has exactly one non-test caller inside that same arm, so **without Postgres there is no later boot refusal at all**. The story would have shipped operator env-var discipline wearing a wall's clothes.

## Pre-dev checklist (per story, at preflight)
1. Ratify F4 before 13.1 development: schema-v2 `[[teams]]`, accepted versions `{1,2}`, canonical signature compatibility, and `teams.is_some() ⇒ v2`. Until then 13.1 remains `draft`.
2. Name each gate's §A7 source and evidence state: derive-and-reconcile numerator, real-Postgres proven-red, team-identity source-reflex, erasure/hold reconciliation, dead-wire negative, forged-vetter-key negative.
3. Confirm/bound the 13.2/13.3 `CrossRegionReadmit` seam and the 13.5b/13.5d/13.5e lifecycle/runtime/audit seams; count in-`src` test modules. ZERO is a per-story result, not an Epic premise — and **"kernel-core ZERO" ≠ "zero delta"; say both sentences.**
4. Record the frontier model + pre-book the complete §A6 net (incl. Test-Infra + runtime) for every adversarial boundary through 13.6.
5. Keep each story at ≤6 ACs. If grounding reveals another independently shippable mechanism, split it rather than hiding implementation in 13.6. **Applied twice on 2026-07-18** (H1 → 13.3b; 13.5c → 13.5c/d/e). ⚠ **One ratified exception, 2026-07-19: 13.5d ships with 7 ACs.** AC7 (`register_spirit`) is **not independently shippable** — without it, 13.5d's ACs 2/3/5 all terminate in `TenantSpiritUnmapped`, so a split would yield one story that cannot pass its own live leg and a second that exists only to make the first true. Worse, AC3(b) and AC7 must derive from **one `AtomicU32`** at the composition root (the whole point of the L10 grant), so splitting them **re-opens the confused-deputy window in the gap between the two stories**. The rule's purpose — no hidden second mechanism — is served, so the exception is written down rather than dressed as a seam. **If ever declined, the only correct split is AC7 + AC3(b) together; never AC7 alone.**
6. Author ADR-055/ADR-056/**ADR-058** and `docs/loom-threat-model.md` at their owning stories; ⚠ **CORRECTED 2026-07-19 by the 13.3b preflight: ADR-013 DOES exist, and its text constrains the story.** `_bmad-output/archives/architecture-maos.md:1506` carries a complete **"ADR-013 — `log.recall` as Kernel-Mediated Spirit ABI Capability"**; the newer 12-ADR registry **reassigned the number** to `task.assign`. This is an **ADR-number collision across architecture generations**, not a phantom citation, and the mis-citation surface is **≥8 files, not four**. Three consequences: ADR-058 **ports and amends** rather than invents; the archived text **explicitly forecloses 13.3b's AC3** (*"never grants cross-Spirit recall"*), making AC3 a **second** amendment to shipped design that nobody had noticed; and ADR-013's own revisit clause (*"would warrant a new ADR rather than extending this one"*) **pre-authorizes ADR-058**. `ADR-018` is still absent and still needs porting. The threat model includes same-region forgery, authority compromise, malicious member, prompt-injection disclosure, erase/hold partial failure, and tenant-audit leakage.
7. **Scout the story you are deferring INTO** *(new, 2026-07-18)*. 13.2's Option A was ratified on the promise that "13.3 builds the real cross-team write"; three scouts later that promise was unbuildable. **A deferral is a claim.** Before any story defers work to a successor, verify the successor's layer actually supports it.
8. **Every mechanism story's gate carries a dead-wire negative** for what it does not wire — green while unwired, inverted by the story that wires it. Forward-only; shipped stories are not reopened.
