# Epic 15 — Foundations (W0)

**Status:** `backlog` — Round 2 correct-course 2026-09-04 (`sprint-change-proposal-2026-09-04-round2.md`, Lunarpulse-ratified; Fork C). Replaces the morning's Epic 15 file. `sprint-status.yaml` is authoritative for status.

**Refinement round applied (2026-09-06, confidence rule 9 — mid-epic, because Story 15-2 disproved premises in its own section):** the 15-2 section's AC1/AC2/AC5 and its whole grant table are re-derived at HEAD `03ee0ad8`, and AC6/AC7 are added. Operator directive in force: **no drift in spec — the story and this file say the same thing, or this file is amended in the same commit.** Evidence and full derivation: `implementation-artifacts/15-2-kloc-ceiling-rebase.md`.

**Second refinement round applied (2026-09-06, confidence rule 9 — mid-epic, because Story 15-3's preflight disproved premises in its own section):** the 15-3 section is re-derived at HEAD `55ac884f` and grown from 4 ACs to 7. Five scouts disproved, among others: `check-cna-registration` is **live**, not vacuous, and retiring it drops two `SECURITY.md` controls; the phase duplication is **16 constants + 12 function copies + a repo-variable env source**, not 8 constants, and the partial consolidation **arms** the v2_2 decay it exists to remove; the new gate belongs in `v1-0-ship-gate.needs`, not `aggregate.needs`; AC3's tokeniser clause was a null control over `MAOS_ONE_SHOT`; and "xtask net ≤0" is arithmetically false. Evidence and full derivation: `implementation-artifacts/15-3-single-phase-source-and-exit-command-check.md`. **Round-table 2026-09-07 then re-ruled four of that story's own answers** (F13 placement REVERSED to a new `verbs.rs` with `SCANNED_SOURCE_FILES` 17→18; F14 resolve-or-be-named STRENGTHENED from a receipt to a control; F15 an exit block is a declared *template* and `epic-16` L3 is repaired by 15-3; F16 the name has a **third** occupant — 11 gate modules' JSON `"current_phase"` → `ship_phase`), and replaced AC1's oracle with a file count after finding the identifier count was moved by a test function's name.

**Third refinement round applied (2026-09-07/08, confidence rule 9 — mid-epic, because Story 15-1's preflight and two round-tables disproved premises in its own section):** the 15-1 section is re-derived at HEAD `2fd492c0` and grown from 6 ACs to 7. Four adversarial scouts and sixteen round-table voices disproved, among others: **the red inventory is EIGHT, not "4 red gates + 1 test red"**; **AC1's stated J1 fix is unimplementable and self-contradictory** (`j1_*` derives zero targets AND breaks the gate's own planted `xyz_1b.rs` vector); **`check-j1-loopback-delegation` is not in `aggregate.needs` at all**; **AC3's ~9/8 split is 18/1** and its two halves annihilate each other under a re-emit; **AC4 is discharged and vacuous** while the live kloc red is `xtask 43411 > 43095`; **AC6 can turn a currently-green Blocking gate red**; and **no CI job runs `cargo test --workspace`**. Evidence, seven decisions and full derivation: `implementation-artifacts/15-1-green-at-head.md`. ⚠ The baseline moved twice during that preflight — once from a concurrent agent, once from a rebase of 15-3 — which is the story's own thesis demonstrated on itself; every figure here is pinned to `2fd492c0`.

**Round 3 applied (2026-09-05):** the R2 preflight's §K edits (18 of 18 applied) and the decisions D-D, D-F, D-H, D-I (with D-A, D-B, D-C, D-E folded into 15-5 as ADR content) are folded in; residuals listed in `sprint-change-proposal-2026-09-05-round3.md`.

**Wave / duration:** 3–5 weeks (measured; 2.5–4.5 with 15-5 in parallel) · **Makes true:** the five blockers the preflight found, so Epics 16–21 can land a line and prove an exit

**Closes:** ⚠ **re-measured 2026-09-08 at `2fd492c0`: SIX red controls, FOUR workspace test failures, and the Blocking job `a2a-tcp-tests-8-6` which is unpassable by construction** (was stated as) 5 red gates + 1 test red (`check-empty-kernel`, `check-service-boundary`, `kloc-check`, `check-env-contract`, `check-j1-loopback-delegation`; `t_14_2a_post_grace_journal`); the phase sources (8 `CURRENT_PHASE` + 8 `PHASE_ORDER` + 12 duplicated fns + the `MAOS_SHIP_PHASE` repo variable); zero kloc headroom in 8 crates and `maos-domain` over by 51; `release.yml` first-run defect; the open design forks D-A..D-F, D-H (ADR-060..064); no hermetic inference seam outside Researcher (two inference consumers at HEAD)

**15-1 review amendment (2026-09-08):** the measured `onb-nfr2-timing` binary-size failure is
the seventh red control and the second unpassable Blocking job. Lunarpulse authorized its
in-story rebaseline from 24MiB to 28MiB against the 26,473,608-byte artifact, so AC1 retains
the full aggregate-green contract and 15-4 is not blocked on work filed from 15-1.

**Hermetic exit command (CI, no secrets; the epic's first CI job, red until green; dry-parsed at HEAD @9f920180 — `bash -n` clean, provenance below):**

```
cargo run -p xtask -- check-empty-kernel && cargo run -p xtask -- check-service-boundary && cargo run -p xtask -- kloc-check && cargo run -p xtask -- check-env-contract && cargo run -p xtask -- check-j1-loopback-delegation && cargo test --workspace --no-fail-fast   # green on tree content
cargo run -p xtask -- check-exit-commands                                    # every epic exit token resolves against maosctl/xtask --help and the maos verb table
cargo run -p xtask -- release-dry-run                                        # maos + maosctl built, artifacts merged, SHA256SUMS written; signing skipped, no secrets
```

**Exit-block provenance (@9f920180):**
- Line 1 — `check-empty-kernel` exists (`xtask/src/main.rs:175`), red: 6 violations (five structs + two undocumented-register rows, 15-1 AC2) · `check-service-boundary` exists (`:201`), red: 19 removed + 17 `other` + 4 rows (15-1 AC3) · `kloc-check` exists (`:161`), red: aggregate 155498 > `_aggregate_hardfail` 147057 and `maos-domain` 8695/8644 (15-2) · `check-env-contract` exists (`:673`), red: `MAOS_OPERATOR_BEARER_TOKEN`/`MAOS_OPERATOR_HTTP_BIND` unregistered (15-1 AC5) · `check-j1-loopback-delegation` exists (`:577-578`), red: `J1_TEST_SUFFIXES` `_2a.rs` (`check_j1_loopback_delegation.rs:114`) catches `crates/maos-bin/tests/cert_rotation_trigger_14_2a.rs` (15-1 AC1) · `cargo test --workspace --no-fail-fast` cargo built-in — ⚠ **MEASURED 2026-09-08 at `2fd492c0`: EXIT 101, 4168 passed / 4 FAILED / 118 ignored** (15-1 AC6 names all four; the *"other reds not measured"* obligation is closed). ⚠ **And NO CI JOB RUNS THIS COMMAND** — every `cargo test`/`nextest` in `discipline.yml` and `journey-nightly.yml` is `-p`/`--test`-scoped — so this clause is enforced by nobody until **15-1 AC7** enrolls it. ⚠ `kloc-check`'s red has MOVED CRATES since this line was written: `maos-domain` and the aggregate are green (15-2), and the live breach is **`xtask` 43411 > 43095, +316** (15-1 AC4).
- Line 2 — `check-exit-commands` created by 15-3 **AC5** (AC3 in the pre-refinement numbering); red at HEAD: unrecognized subcommand (`grep -rn check-exit-commands xtask .github` empty).
- Line 3 — `cargo run -p xtask -- release-dry-run` created by 15-4 AC1; red at HEAD: unrecognized subcommand (grep empty). Local run builds the host target; the three-target matrix runs in the discipline job of the same name.
- Nothing in the block needs a secret, a paid key, a human, wall-clock or the network beyond cargo's registry cache.

**Operator-lane legs (never in the exit):**
- `ops-provisioning-secrets-and-accounts` — `RELEASE_SIGNING_KEY`, `MAOS_RELEASE_PUBKEY`, tap repo, AUR account, ledger-branch permissions (checklist in 15-5)
- `ops-first-signed-tag` — `git tag v0.1.0-alpha.1 && git push --tags` after the row above records both secrets; acceptance: `release.yml` green on `v0.1.0-alpha.1` with both secrets present (moved out of the exit block, rule 2)
- `ops-nightly-cassette-rerecord` — the `tier-2-rerecord` leg under `MAOS_INFERENCE_MODE=record` with `MAOS_ANTHROPIC_API_KEY` (`journey-nightly.yml:88`); refreshes the 15-6 cassettes with `provenance: live-record`; never gating
- hotfix lane rows (model-pin gate/sweep, model env overrides, error surfacing) run in parallel, keys unchanged

**Kernel-Δ:** +2 attribute lines (15-1 AC2, D-H), re-pin 24472→24474 in the same commit; no whitelist edit. Every other story ZERO kernel-Δ @24474 (15-1 re-pinned 24472→24474).

**Confidence rules (ratified 2026-09-04 R2, binding):** 1 verb-first (exit verbs exist at HEAD or are AC1 in this epic; `check-exit-commands` (15-3 AC5); planning-time leg: an exit block is saved only after it was dry-parsed at HEAD with the reds recorded — the provenance list above) · 2 hermetic exit, live optional (paid/human/clock legs are operator-lane rows) · 3 foundations first · 4 cite-or-cut (every AC names file:line + kernel-Δ) · 5 spike before build · 6 size from measurement (+30%) · 7 one decision per fork · 8 inventory from the tree (every count carries its grep) · **9 refine in the retro** (operator directive 2026-09-06, binding): this epic's retrospective is convened as a refinement session, not a write-up — it re-preflights the NEXT epic against the tree at that day's HEAD (citations, exit-block dry-parse, foundations, budget) and files the OLD→NEW edits before that epic's first story is created; a refinement round may also be convened mid-epic when a story disproves a premise. `epic-N-retrospective` is therefore `backlog`, never `optional`.

**Dev-gate:** external holds (NFR-Sec-7, NFR-Comp-1) = GA ledger only. **Model/review:** frontier allowlist + §A6 net binding; non-degradable on stories marked ◆.

---

## Stories

| Key | Story | Closes · Δ |
|---|---|---|
| `15-1-green-at-head` | Green at HEAD | S4 (part), **six** red controls + **four** workspace test failures + the unpassable `a2a-tcp-tests-8-6` · kernel-Δ +2 (re-pin 24472→24474) · **D17 is 15-2's, already closed** · **7 ACs (was 6); refined 2026-09-07/08 by two mid-epic refinement rounds (rule 9) — this section was amended to match** |
| `15-2-kloc-ceiling-rebase` | One-time kloc ceiling re-base and CEILING RULE amendment ◆ | §2.2 of the preflight, D-F, **D14 (+51) and D17 (−8441)** · kernel-Δ 0 · **7 ACs; refined 2026-09-06 by a mid-epic refinement round (rule 9) — this section was amended to match** |
| `15-3-single-phase-source-and-exit-command-check` | Single phase source, `maos --help`/`--version`, and the exit-command check | S4 · rule 1 mechanical · **D20 (both gates)** · kernel-Δ 0 · maos-bin verb-table lines after 15-2 · **7 ACs; refined 2026-09-06 by a mid-epic refinement round (rule 9) — this section was amended to match** |
| `15-4-release-repair-and-first-signed-tag` | Release repair and the secret-free release dry-run | S5 · kernel-Δ 0 (the signed tag itself = `ops-first-signed-tag`) |
| `15-5-decision-adrs-and-provisioning-checklist` | Decision ADRs 060–064 and the provisioning checklist ◆ | rule 7, D-A..D-E · kernel-Δ 0 |
| `15-6-inference-replay-seam` | Inference record/replay seam for every inference consumer | preflight §2.4, D-D · kernel-Δ 0 |

AC1 is always the command and what the operator observes; every AC cites the code it changes; `bmad-create-story` may tighten but not add uncited ACs.

### 15-1-green-at-head — Green at HEAD

*Closes · Δ:* S4 (part), D17, the fifth red gate · kernel-Δ +2 (re-pin 24472→24474)

- **AC1 (command).** The first exit line above exits 0 on tree content and the discipline `aggregate` (job `aggregate:`, **`discipline.yml:3540`** — the `:3510` cite was wrong — with **123** `needs:` at `:3543-3665`, VERIFIED) is green with no gate demoted to advisory. The five reds it closes, each measured @9f920180: `check-empty-kernel` (6 violations = **4 struct-field rows + 2 undocumented `#[i9_exempt]`**, not "five structs + two rows" which sums to 7, AC2); `check-service-boundary` (19 removed + 17 `other` + 4 rows, AC3); `kloc-check` (aggregate 155498 vs 147057, `maos-domain` −51 — closed by 15-2, AC4); `check-env-contract` (2 unregistered vars, AC5); **`check-j1-loopback-delegation`** (Blocking: job **`discipline.yml:1939`**, and ⚠ **NOT in `aggregate.needs` — it is in `v1-0-ship-gate.needs` at `:3439`** (that job at `:3414`, 46 entries); the aggregate depends on it *transitively*. This is the same error this file already records against 15-3, re-committed; EXIT=1 because the suffix allowlist `J1_TEST_SUFFIXES = ["_1a.rs","_1b.rs","_2a.rs","_2b.rs"]` at `check_j1_loopback_delegation.rs:114` derives `crates/maos-bin/tests/cert_rotation_trigger_14_2a.rs` — a 14-2a cert-rotation test, not a J1 test — as an un-enrolled J1 target). ⚠ **THE STATED FIX WAS DISPROVED AND IS UNIMPLEMENTABLE (15-1 §3, D-2).** `j1_*` derives **ZERO** targets — none of the 8 genuine J1 files carries a `j1_` prefix — so the gate reds on its own `leg-vacuity` guard; **and both epic forms break `planting_an_unenrolled_j1_test_file_must_red`** (`xtask/tests/j1_crosshost_1b_proven_red.rs:621`, which plants `crates/maos-bin/tests/xyz_1b.rs`), the very vector this sentence promises to keep; and an explicit stem list resurrects the `ENROLLED_TEST_TARGETS` const the gate's own doc (`:101-108`) condemns by name. **NEW FIX (one decision, rule 7): in `derive_enrolled_targets` (`check_j1_loopback_delegation.rs:765`, filter `:788`) the suffix match additionally requires that the character preceding the suffix is NOT an ASCII digit** — an epic-numbered stem (`…_14_2a.rs`) is a foreign-lane test, a J1 stem (`…_2a.rs`, `xyz_1b.rs`) is not. Validated against all 9 matching files and the planted vector. And the test is **executed by `check-cert-rotation-trigger`'s own `invoke_cargo_test` (`check_cert_rotation_trigger.rs:595-613`), NOT by any `--test` line in `discipline.yml`** — zero such tokens exist in all 11 workflow files, so enrolling it here would buy zero coverage and double-run a real-daemon test on a job with no `timeout-minutes`. Plus the test red of AC6.

**15-1 review amendment to AC1:** `onb-nfr2-timing` is the seventh control closed here. Its
Blocking 24MiB limit was below the measured 26,473,608-byte release artifact; the authorized
28MiB rebaseline restores 2.75MiB of explicit headroom while keeping `aggregate` green.
D-2 now excludes an underscore-delimited decimal segment before the J1 suffix; a numeric J1
stem such as `j1_2a.rs` remains governed and has its own proven-red vector.

- **AC2.** I9 (D-H): `ValidatedRevocationRules` (`revocation/rules.rs:19`, attribute at `:16-18`) gets its entry in `docs/invariants/i9-exemptions.md` (§Entries format, `:12-16`); the 3-line `#[maos_attrs::i9_exempt]` at `security/mod.rs:120-122` moves onto `SecurityManagerAdapter` (`:130`, already documented at `i9-exemptions.md:91`, net-0; `T3ImageVerificationConfig` `:124` then carries no attribute and needs no entry — the register enumerates attribute sites only, `:8`); `ScbRuntimeSnapshot` (`scheduler/control_block.rs:244`) and `VerifiedImageLock` (`security/sandbox/t3/image_lock.rs:27`) get **single-line** `#[maos_attrs::i9_exempt(reason = "…")]` (precedent `maos-capability/src/cap_quota/mod.rs:37`, measured 95 chars/reason 58; `check-fmt` is Blocking — job **`check-fmt` at `discipline.yml:140`**, step `:151-152`, `aggregate.needs :3545` — so the reason string must not reflow: ⚠ **HARD BUDGET, omitted until now — there is no `rustfmt.toml`, so `max_width = 100`, attribute overhead is 37 chars, and the reason string must be ≤ 63 CHARACTERS.** At 64 it reflows to three lines, the delta becomes +6 not +2, and `check-fmt` reds) with entries in the register: **+2 kernel lines**, `xtask/kernel-core-baseline.toml` re-pinned 24472→24474 (`check_kernel_baseline.rs:7`, physical lines) and the `maos-kernel-core` kloc row +2 (its D13(a) row in `kloc.toml`, tokei code) in the same commit — both instruments. The whitelist path is NOT taken — path-scoped at **`check_empty_kernel.rs:136-141`** (`:243-246` is the consumption site), ADR-037 names no whitelist, and ⚠ **the stronger reason: `xtask/src/tests/check_empty_kernel_tests.rs::whitelist_has_exactly_three_paths` hard-asserts `paths.len() == 3`, so a fourth entry reds a unit test.** §Entries exemplar is **`i9-exemptions.md:13-17`**, not `:12-16`.
- **AC3.** NFR-Test-2: the **16** `other` symbols (17 rows, `SuccessorSpiritFactory` twice; `check_service_boundary.rs:236-241`) get exact-path rows in `xtask/kernel-api-classes.toml` (row format `:22-28`; header `:3` = invariant-lock review); ⚠ **THE ~9/8 SPLIT IS DISPROVED — MEASURED 18 SIGNATURE-CHANGED / 1 GENUINELY REMOVED** (15-1 §6). Seven of the eight symbols listed below as "genuinely removed" are **ALIVE at HEAD** and emit no `other` row only because they are **already classified** (`kernel-api-classes.toml:122,:541-546`) — the absence of a violation row was read as absence of the symbol. `SecurityManagerAdapter` is also signature-changed and was listed nowhere. **The single genuine removal is `security::sandbox::t3::image_lock::get_default_image`**, deleted by `af788c3e` and replaced in-place by `VerifiedImageLock::resolve_pin` — it was an *unverified fallback image URI*, so its deletion is a security repair. Root cause of the whole red: `af788c3e` touched **30 kernel-core files** and updated neither instrument, the same shape `kloc.toml:199` already indicts that commit for. Former (wrong) split, retained so the correction is legible — signature-changed (~9: `StateCodecError`, `SpiritControlBlock`, `SpiritSchedulerAdapter`, `HotSwapSaga`, `HotSwapCoordinator`, `build_runtime_argv`, `spawn_t3`, `load_and_verify_lock`) and "genuinely removed" (`SpawnError`, `UpgradeError`, `UpgradeOrchestrator`, `UpgradeOutcome`, `UpgradeReport`, `UpgradePolicy`, `RevocationApplier`, `get_default_image`); ⚠ **RE-RULED (15-1 D-4): the baseline is HAND-EDITED SURGICALLY and only the 8 genuinely-new paths are classified.** The AC's two halves annihilated each other: `removed` has exactly one input (the baseline), so **a full re-emit closes all 19 removed rows AND all 17 `other` rows at once** — proven by executing it with `kernel-api-classes.toml` untouched: **0 NFR-Test-2 rows remaining**, which makes the 16 classification rows unreachable by construction, i.e. 16 rows of invariant-lock judgement committed as decoration. So: **18 in-place `signature_hash` refreshes + 1 deletion, 370 → 369 items**, with the symmetric-diff proof in the commit message (`1dda03eb` precedent: *paths added 0 · paths removed 1 · hashes changed 18*). ⚠ **`--write-baseline` is NOT added**: measured **+15 charged xtask lines** onto a ceiling already **316 over**, and the named funding source — *"the xtask row of 15-2"* — **does not exist**: that row is a **15-3** grant dated 2026-09-07, declared ZERO HEADROOM. ⚠ **The review note goes in a NEW ADR (`ADR-066`), not in the JSON** — `KernelSurface` derives plain `Deserialize` so an added key parses, but `Serialize` emits only three fields, so any future re-emit silently destroys it; and `invariant-lock` maps `I1..I14 → docs/invariants/I*.md` only and **never fires on `kernel-api-classes.toml` or the baseline** (`ADR-065:87-92` already adjudicates this convention class; ADR-043 is the discharging precedent). Correct cite for the `--baseline` input-only claim: **`main.rs:203-222`** (`:206-211` is the arg), not `:204-208`.
- **AC4.** ⚠ **DISCHARGED AND REPLACED — as written this AC is a no-op a dev could claim credit for by doing nothing** (15-1 §7, the "claim standing in for a control" failure the Epic-12 retro named). Measured at `2fd492c0`: `maos-domain` **8695/9192 ✅**, `maos-bin` **17339/20109 ✅**, aggregate **157186/170884 ✅** (alarm 158608), and **D14 and D17 are already CLOSED against `15-2-kloc-ceiling-rebase`** by 15-2 AC7 — `check-decision-register` exits 0 at 23 rows / 12 open and neither names 15-1. **The kloc red did not go away; it moved crates: `xtask 43411 > 43095`, +316.** Four arms replace this AC: **(a)** close the breach — the reclaim is attempted and **its failure recorded as evidence** (14-0 already spent that pool; three of the 16 surviving `xtask/src` warnings are the exact functions 14-0 named un-deletable; yield ≈15–25 against 316), then **one named measured grant at the landing commit**, `xtask = final measured + 35`, restoring the PRICING shape `kloc.toml`'s 14-0 block ratifies for the gate crate, precedent `maos-a2a-core` 14-2a verbatim (*"RECLAIM WAS ATTEMPTED FIRST AND CANNOT FUND IT"*); **(b)** 15-3's booking corrected **in place, filed not overwritten** (D13(a)) — `check_exit_commands.rs` booked +1143, measured **1344** at `bce5ac68` and **1434** at `2fd492c0`; unbooked **+201/+23/+2 = 226**, plus **+90** from the amend = **316**; and `sprint-status.yaml`'s *"kloc-check PASS"*, *"17109→17309"* (**17339**) and *"Aggregate 156840"* (**157186**); **(c)** the falsifier — re-adding any deleted line must red `kloc-check`; **(d)** what 15-2 discharged is recorded as **measured-verified inheritance, not claimed**. ⚠ AC2's `maos-kernel-core` **18933 → 18935** lands here too, or `kloc-check` reds on AC2's `+2`. ⚠ **The grant is a FORMULA, never a literal** — the literal was wrong twice in 24 hours.

**15-1 review amendment to AC4(c):** the KLOC falsifier crosses the authorized 35-line pricing
reserve; the executed +43-line probe reds at `43452 > 43444`. A single added line is permitted
headroom, not a valid falsifier.
- **AC5.** `MAOS_OPERATOR_BEARER_TOKEN` and `MAOS_OPERATOR_HTTP_BIND` (read at **`main.rs:2703-2704`** — the `:2669-2670` cite drifted 34 lines; gate `check_env_contract.rs:107` parses `name: "MAOS_…"` textually) registered in `crates/maos-bin/src/env_contract.rs:13` format, **both `EnvStability::UserFacing`** — the surface is LIVE (`main.rs:2702-2725` validates and the bearer's presence ENABLES it; `:2824-2841` binds a real listener via `maos_control::OperatorHttpServer::bind`), `EnvStability` has exactly two variants (there is no `Secret`), and every secret-shaped var already in the registry (`MAOS_KMS_MASTER_KEY`, `MAOS_SSO_JWKS`, `MAOS_VETTER_KEYRING`, `MAOS_CROSS_TEAM_BASE_SEED`) is `UserFacing` while all 11 `HarnessOnly` rows are test knobs. ⚠ **SCOPE HONESTY: this registers the two reads the gate can SEE.** `crates/maos-cli/src/subcommands.rs:1940` reads the same token and `:1938` reads a **third** variable `MAOS_OPERATOR_HTTP_ENDPOINT`; both are permanently outside the gate's `crates/maos-bin/src` scope (`check_env_contract.rs:119`). ⚠ **The gate's own operator message names a DEAD KEY on both its PASS and FAIL paths** — *"workspace coverage tracked in Story 12.7"*, and 12.7, 14-7, 14-8 and 14-9 are all absent from `sprint-status.yaml`; repoint to the surviving home (D-I / 21-4 / `xtask/env-contract.toml`), 0 tokei lines. D-I: the registry stays where it is in W0; when 21-4 turns it into the data file `xtask/env-contract.toml`, these two rows migrate as data with the rest (no second home).
- **AC6.** ⚠ **GROWN FROM ONE TEST TO FOUR, AND THE ROOT CAUSE IS NOW PROVEN** (15-1 §1, §4). `cargo test --workspace --no-fail-fast` at `2fd492c0` is **EXIT 101 — 4168 passed / 4 failed / 118 ignored**, and the epic's *"other reds not measured"* is an unbounded obligation now closed: `t_14_2a_a_promoted_generation_…` (maos-a2a-tcp), `drr_backpressure_emitted_when_backlog_exceeds_threshold` (maos-kernel-core, **unlisted anywhere** — a fixed `sleep(50ms)` used as a synchronisation primitive), `stdio_transport_malformed_output_returns_transport_error` (maos-mcp, **unlisted** — **EPIPE, not parse**: the fixture `sh -c "echo …"` never reads stdin, so `spawn_and_invoke`'s write loses the race and yields `"write: Broken pipe"`; mechanism reproduced **300/300**), and `kloc_check::tests::kloc_check_runs_on_workspace` (downstream of AC4 — the test 15-2 un-ignored after ten epics, red on the first commit after it was armed). ⚠ **THREE OF THE FOUR ARE RACES, so a single green run is a null control** — every fix ships a repeat-count oracle with a measured before/after rate. ⚠ **ROOT CAUSE (a), proven by isolation, not by candidates: the process-global `tracing_core` callsite-interest cache.** Only `:267` installs a subscriber (`with_default`, thread-local) while the co-resident test at `:68` traverses the same `router.rs` refusal `warn!` callsite with **none**, so whichever thread registers it first fixes `Interest` for the process. Pairing: C+A **21/30 fail**, C+B **0/30**, C alone **0/30**; rates by threads 1→0/40, 2→0/40, 3→18/30, 4→23/30, 8→**24/30**, 32→29/40. **Fix shape ruled (15-1 D-7): process isolation — the capturing test moves to its own test binary, so the defect is gone by construction; the 3-line `LogCapture::register_callsite → Interest::sometimes()` alternative is permitted ONLY if measured to 0/40 at default threads and 0/50 at `--test-threads=8`.** ⚠ **ORDERING CONSTRAINT (D-6): `check-cert-rotation-trigger` is GREEN today *because of* the `--test-threads=1` this AC drops, and it EXECUTES this test via `invoke_cargo_test`. The flag comes out AFTER the fix is proven, in the same commit, with the gate re-run — never before, or a green Blocking gate goes red.** ⚠ **SIXTH RED, unlisted by this epic: `a2a-tcp-tests-8-6`** (`discipline.yml:1642`, **in `aggregate.needs` at `:3623`**, no `continue-on-error`, no `if:`) runs the suite **50× at `--test-threads=8`, failing on any flake** — at 24/30 per run it is **unpassable by construction** and has been red all along; its 50× loop is AC6(a)'s acceptance run. Original text: the `t_14_2a_post_grace_journal.rs:362` red is reproduced: FAIL under default threads with the 3-test binary (2/2, `captured tracing: []`), PASS alone, PASS at `--test-threads=1` and `=2` — i.e. inter-test interference, not the subscriber arrangement at `:267-268` (`with_default` on a `current_thread` runtime); the root cause is measured and recorded before the fix (candidates: tracing callsite-interest cache; handshake-arm timing `transport.rs:1002-1040`; no `static`/`OnceLock` in `maos-a2a-tcp/src`); `check-cert-rotation-trigger` drops `--test-threads=1` (`check_cert_rotation_trigger.rs:613`, xtask −1) so gate-green equals suite-green — **subject to D-6's ordering constraint above**.
- **AC7 (NEW, 2026-09-08 refinement round — 15-1 D-3/R-3/R-13).** `cargo test --workspace --no-fail-fast` is **enrolled as a Blocking CI job in `aggregate.needs`**, because at HEAD **no job runs it**: every `cargo test` in `discipline.yml` is `-p`/`--test`-scoped and both `cargo nextest run` invocations in `journey-nightly.yml` are `-p maos-journey-test`-scoped, and there is no composite action or script that runs it indirectly. **So AC1's exit line ends in a command CI never executes** — a claim, not a control. ⚠ **This is a GAP, not a regression**: the clause was never built; nothing was demoted. Enrolled at the sites 15-3 established (discipline job with an explicit `timeout-minutes` + `aggregate.needs` + `gate-registry.toml` + `check_ship_gate_completeness.rs` `EXPECTED_GATES`), with `check-ship-gate-completeness` re-run green and its count moved deliberately. Falsifier: planting a failing test anywhere in the workspace must red the aggregate. Cost, stated not hidden: one full workspace run per CI cycle (measured 497 test binaries, 4168 tests). ⚠ **Companion, downgraded on measurement:** making the suite Blocking makes `#[ignore]` the escape hatch — audited at **173 sites, 171 carrying a reason, 2 bare, and BOTH measured benign** (`maos-siem/tests/fault_inject.rs:67` is gate-driven via `check_enterprise_identity.rs:273`, the file is `#![cfg(feature = "siem-fault-inject")]`, and it **passes**; `cgroup_ceiling_smoke.rs:15` self-skips on its `MAOS_CGROUP_TEST` guard). So: **add the two reason strings (2 lines, in `crates/`, free) and FILE the enforcing gate to 20-3** — funding a new xtask gate, off the very grant AC4(a) asks for, to police a hatch with zero live defects is gold-plating.

### 15-2-kloc-ceiling-rebase — One-time kloc ceiling re-base and CEILING RULE amendment ◆

*Closes · Δ:* §2.2 of the preflight, D-F, D14 (+51) · kernel-Δ 0

- **AC1 (command).** `cargo run -p xtask -- kloc-check` is green and every crate named in Epics 16–21 shows the headroom its measured stories need, per the consolidated table below (tokei `code` @9f920180 from the seven R2 notes' §E; instrument `xtask/src/kloc_check.rs:163-213`, `spirits/*` and `examples/*` excluded at `:180,:184`). Rows whose HEAD headroom already covers the ask are recorded as `no change` in the table, not re-based — **`maos-secrets` 839 and `maos-providers` 748 only.** ⚠ **CORRECTED 2026-09-06 (Story 15-2 §6 row 10, review R-P11):** `maos-control` (697 vs an ask of 480–760) and `maos-shell` (100 vs 100–150) were listed here as `no change` and both claims are **FALSE**; the table below re-bases them to 1791 and 500, and `kloc.toml` ships those values. **D-F, in writing — a PERMISSION, not capacity (REFINED 2026-09-06):** the Epic-13 retro's refusal to grant `maos-kernel-core` headroom (the `_aggregate_hardfail` comment's "stay tight because Epic 14 is declared ZERO kernel-Δ"; the CEILING RULE's "Slack is operating capacity, NOT authorization") is overruled for the v2.2+ lane — Epic 14 is `done` and its ZERO-Δ declaration expired with it — because **five** (not three) FLAG-Winston stories cannot land under a blanket refusal. But the ceiling is **NOT pre-raised**: `maos-kernel-core` stays at **18933/18933, zero headroom**, because D13(a) bought that property deliberately over the formula's 19312 (the `maos-kernel-core` row's D13(a) comment), and pre-granting 250 lines to five stories that do not exist yet buys it back — a *reservation*, in the one crate where three epics of per-story FLAG-Winston discipline exist to prevent one. Each story raises **ceiling and pin together in its own commit**; the Σ forecast is **202**, funded in the aggregate and pre-granted nowhere, and at forecast close ≈19135 the crate is **865 under the NFR-Maint-1 20 KLOC letter**. Per story — 15-1 +2 (D-H) · 16-5 +5–25 · 17-1 +65–130 · 17-2 net ≤+10 · 19-3 +3–5, plus 18-3 AC3's conditional dispatcher `Clock` +15–30 = **Σ 202**; and `21-5` is **−817 tokei / −1028 physical** (CORRECTED 2026-09-06 — the earlier `≈ −1000` mixed the two instruments' units, and would over-lower the ceiling by ~183), the ceiling being LOWERED by the measured moved-line count when 21-5 lands. ⚠ The authority for lowering is **architecture §15.5 Decision clause 2(a)** (`15-full-spectrum-v2-2.md`) — *"downward at any time"*, with a crate minted by extraction taking its initial ceiling from measured LOC plus the CEILING RULE reserve — **NOT ADR-038**, which is 15 lines and says nothing about lowering, extraction, or lines moving out of a crate — `:17` governs *raising* via the ADR-037 process only. Each story still re-pins `kernel-core-baseline.toml` **and** its kloc row in its own commit (AC4).
- **AC2.** `xtask/kloc.toml` rows re-based to HEAD-measured values + allowance, each with a `MEASURED GRANT 2026-09-xx (operator-authorized)` comment in the existing format (inline `:212,:319,:343,:482`; comment-line `:203,:316,:334,:471`; whole-file precedent `2026-07-25 founder policy re-base` `:275,:278` and `2026-08-11 Epic-13 retrospective re-base` `:501`, commit `08ab6632`); `_aggregate_hardfail` re-derived as **every line this re-base authorizes anyone to write** (REFINED 2026-09-06 — the bare CEILING RULE formula yields 158608, which is spendable to only 23% of the grant and would deliver a table nobody can spend): **`155498 measured + 13774 funded + 1512 no-change asks + 100 rule floor = 170884`**. The `+100` is the rule's own floor, not a fitted constant — the aggregate compares `>=` (`kloc_check.rs:225`) while per-crate compares `>` (`:231`). Sum-of-ceilings after the re-base is 182113, so **11229** of independent anti-distributed-growth signal survives (D17's stated purpose) as a *named* reserve. ⚠ **Sizing the aggregate to Σgrants alone was rejected**: 20043 lines of headroom sit on rows this story does not grant, so the grants would compete with untracked growth — the reservation this story's own thesis forbids. The per-crate allowances in the table exceed that formula (`max(100, 2%)` → maos-bin 343); **Rule 6 applies to this table (REFINED 2026-09-06):** grants are `⌈Σupper × 1.3⌉` for the 18 rows whose ask is an *estimate* — the original table applied "size from measurement" and dropped "(+30%)", leaving 15 of 20 rows at zero margin over scout-note ranges. Rule 6 does **not** apply to `maos-orchestrator-buffer` (817 lines being *moved*: a move carries no estimation uncertainty, so the CEILING RULE formula governs and 917 stands) nor to `maos-bin`/`xtask`, which are **policy ceilings**, not grants. **Also in this pass:** the three phantom budgets `maos-cap-registry` (3000), `maos-wire` (2000) and `maos-journal` (2000) — 7000 lines of authorization for crates that do not exist — are **zeroed, not deleted** (a deleted key escapes the gate entirely, `kloc_check.rs:229-235`; a zero makes line 1 cost a grant). This supersedes `epic-21…:82` (21-4 AC4), which says *delete*, six epics early. AC2 therefore also amends the CEILING RULE text (the CEILING RULE block; ⚠ **CORRECTED 2026-09-06, review R-P5:** the amendment goes **inside** the rule block where it belongs — F2's citation-preservation arm was RETIRED because comments are not tokei `code`, so every line-number citation into `kloc.toml` was converted to quote-form at zero cost instead of the file's structure being bent around them) to name the **epic-scoped operator allowance** as a third sanctioned source beside the retro and the per-story measured grant, and names 15-1 AC2 (+2) among the kernel-core FLAG deltas.
- **AC3.** The CEILING RULE ("Recalculated ONLY at an epic retrospective, or under an explicitly authorized measured grant") and the `_aggregate_hardfail` comment are amended together: the aggregate is re-derived at epic close **or** by an operator-ratified re-base story, and that third source **expires when Story 21-4 lands**. ⚠ **CORRECTED 2026-09-06 (Story 15-2 §1 disproved the original claim, review R-P11):** *nothing* enforces any of it — `grep "MEASURED GRANT" xtask/src/*.rs` is empty and the rule is prose, and `check_epic_close_coherence.rs:38` does **NOT** keep `prior:` verbatim: `:38` is a doc-comment *analogy*, and that gate reads `sprint-status.yaml`, `epics/index.md`, `epics/` and `kernel-core-baseline.toml` (`:57-60`) and never opens `kloc.toml`. The `prior:` chain is a courtesy, not a control — the amendment is a promise to humans and must be written as one. (Cited by clause rather than line: Story 15-2 moves this file's line numbers.)
- **AC4.** The kernel pin rule is unchanged (equality at `check_kernel_baseline.rs:7-8`, "Hard-fails on ANY drift"); each FLAG-Winston story re-pins in its own commit with its figure from AC1. Documented in the story.
- **AC5.** ✅ **FUNDING RATIFIED 2026-09-06 (Lunarpulse)** — the grant increment approved *as the spec needs*, with the standing directive **"no drift in spec for this project; we do as per the spec — deferred ones are the last resort after all our effort first."** Recorded in the story file and in each `kloc.toml` row comment at landing (T0 precedent: a grant lands with the lines it authorizes; a grant is a global — whichever story measures first consumes it, so AC1's per-epic column is the reservation ledger).
- **AC6 (added 2026-09-06 under the no-drift directive).** The gate **stops failing open** — six defects, one class, one commit, and all six were re-classified from *deferred* to *effort* because `xtask/src/tests/` is measured at 2697 tokei code lines and is **UNCHARGED**, so every test is free: (a) `kloc_check.rs:53/:65` print `16/20 KLOC` from hardcoded literals describing the `unwrap_or` defaults, and NFR-Maint-1 bounds the *kernel core*, not a 155k workspace aggregate; (b) `:165` `let tokei_path = "tokei";` is a bare PATH lookup with **no version assertion** against a CI pin of 14.0.0 (`discipline.yml:11`); (c) `:229-235` never checks a crate that has no `kloc.toml` key — the hole that let `maos-cohort` grow +302 unmeasured (`docs/adr/ADR-055-multi-tenant-loom.md:183`); (d) `:137/:141` silently substitute defaults for an absent or non-integer aggregate key; (e) `:148-150`/`:149` silently drop a quoted budget and wrap a negative one to `u64::MAX`; (f) `kloc_check_runs_on_workspace` (`tests/kloc_check_tests.rs:29-36`) has been `#[ignore]`d since Epic 5 for an expired reason and **this re-base is what finally makes it pass** (`alarm` = `155498 >= 158608` = false). Each of (a)–(e) ships a proven-red test — every existing test on this gate is a null control. `xtask` cost ≈+32, from 15-2's own row.
- **AC7 (added 2026-09-06).** The decision register is re-pointed rather than left holding decisions this story discharges: **D14** (`epic-14-preflight-decisions.md:80`) targets `21-4` and **D17** (`:83`) targets `15-1`, but 15-2 does the work — both re-pointed to `15-2-kloc-ceiling-rebase` and CLOSED, with `check-decision-register` exiting 0 and the open count down by two, and the `:91` re-homing map corrected.

**15-2 AC1 table — REFINED 2026-09-06 (mid-epic refinement round, confidence rule 9).** Grants are
`⌈Σupper × 1.3⌉` (rule 6) except where noted. HEAD measured = tokei `code` @`03ee0ad8`, identical to
`9f920180` (no code changed between them); instrument `xtask/src/kloc_check.rs:163-213`, exclusions at
`:173-190` (⚠ `:180` is `examples`, `:184` is `spirits` — the earlier draft swapped the labels, and both
are bare directory-name patterns matching at any depth, not globs). **Stated in FINAL CEILINGS**, because
`measured + grant` destroys the existing headroom on nine rows and a `+N` column is ambiguous about it.

| Crate | HEAD | old ceiling | **NEW ceiling** | headroom | Σupper | Note |
|---|---|---|---|---|---|---|
| `maos-bin` | 17109 | 17109 | **20109** | 3000 | 4640 | **POLICY CEILING**, exempt from rule 6 — and it now exceeds `maos-kernel-core` by 1176. Uncovered at the top: Epics 17+19+21 combined (1.39× net). |
| `xtask` | 41953 | 41953 | **42353** | 400 | 800 | **POLICY CEILING**, exempt from rule 6, and a transient: 20-3 nets −4476…−5176. Uncovered: 21-2+21-4 at the top iff 20-3 slips past them. |
| `maos-kernel-core` | 18933 | 18933 | **18933** | 0 | 202 | **UNCHANGED — zero headroom.** D-F = standing permission (AC1). Σ forecast 202 funded in the aggregate, granted to nobody. |
| `maos-domain` | 8695 | 8644 | **9192** | 497 | 382 | ⌈382×1.3⌉. Absorbs D14 (+51 RED). **Epic 17 (+25–50) and Epic 19 (0–30) were omitted by the original table.** |
| `maos-cli` | 5270 | 5270 | **6856** | 1586 | 1220 | ⌈1220×1.3⌉. Σupper is 1220, not 1100 — the cell also books `18 maosctl eval` 0–120. |
| `maos-manifest` | 4214 | 4314 | **4474** | 260 | 200 | ⌈200×1.3⌉ |
| `maos-mcp` | 999 | 1100 | **1194** | 195 | 150 | ⌈150×1.3⌉ |
| `maos-journey-test` | 493 | 593 | **1104** | 611 | 470 | ⌈470×1.3⌉ |
| `maos-bench` | 1531 | 1631 | **1791** | 260 | 200 | ⌈200×1.3⌉ |
| `maos-eval` | 3661 | 3696 | **4831** | 1170 | 900 | ⌈900×1.3⌉ |
| `maos-wasm-host` | 1057 | 1129 | **1577** | 520 | 400 | ⌈400×1.3⌉ |
| `maos-egress` | — | — | **780** | 780 | 600 | NEW leaf crate (17-1). ⌈600×1.3⌉. |
| `maos-orchestrator-buffer` | — | — | **917** | 917 | 817 | **Measured move, NOT an estimate** — `917 = 817 tokei + max(100, ⌈2%⌉)`; rule 6 does not apply. |
| `maos-registry` | 3515 | 3615 | **4282** | 767 | 590 | ⌈590×1.3⌉. 20-1 re-priced +230–440 by `epic-20…:47`; the original +50–120 is superseded. |
| `maos-spirit-cli` | 753 | 853 | **1273** | 520 | 400 | ⌈400×1.3⌉ |
| `maos-cohort` | 5857 | 5857 | **6247** | 390 | 300 | ⌈300×1.3⌉. **The "14-2d T8 measured grant" DOES NOT EXIST** (14-2d is `blocked`, T8 unchecked, no amount) — clause struck. |
| `maos-a2a-core` | 4856 | 4856 | **4986** | 130 | 100 | ⌈100×1.3⌉ |
| `maos-a2a-tcp` | 1439 | 1500 | **1621** | 182 | 140 | ⌈140×1.3⌉ |
| `maos-iac` | 6960 | 6960 | **7060** | 100 | 30 | floor 100 (⌈30×1.3⌉=39 < the rule minimum). |
| `maos-audit` | 6847 | 6847 | **6951** | 104 | 80 | ⌈80×1.3⌉ |
| `maos-control` | 803 | 1500 | **1791** | 988 | 760 | ⌈760×1.3⌉. **Was `no change` — FALSE**: 480–760 vs 697. |
| `maos-shell` | 305 | 405 | **500** | 195 | 150 | ⌈150×1.3⌉. **Was `no change` — FALSE**: 100–150 vs 100. |
| `maos-cap-registry` · `maos-wire` · `maos-journal` | 0 · 0 · 0 | 3000 · 2000 · 2000 | **0 · 0 · 0** | −7000 | — | **RETIRED** — 7000 lines of authorization for crates that do not exist. Zeroed, **not deleted**. Supersedes `epic-21…:82` (21-4 AC4). |
| `maos-compliance` · `maos-host` · `maos-persistence` · `maos-spirit-abi` · `maos-secrets` · `maos-providers` · `maos-spirit-hello` · `maos-spirit-sdk` | — | — | **`no change`** | 85 · 100 · 1999 · 1962 · 839 · 748 · 635 · 2161 | 0 · 30 · 600 · 2 · 300 · 460 · 20 · 100 | headroom ≥ Σupper for each; their Σ asks **1512** is authorization and is funded in the aggregate. |
| `_aggregate_alarm` | — | 16000 | **158608** | — | — | the pre-grant formula figure; fires when the workspace begins drawing on the Epic-15 allowance (16000 has fired on every run since Epic 1). |
| `_aggregate_hardfail` | 155498 | 147057 **(−8441 RED)** | **170884** | — | — | `155498 + 13774 + 1512 + 100` (AC2). |

**Σ funded = 13774** (13572 per-crate grants + the 202 kernel-core forecast). **Σ per-crate ceilings after
the re-base and the 7000-line phantom retirement = 182113**, i.e. **11229 above** the aggregate.

> **Provenance.** This table replaces the 2026-09-05 R2/§E version, which was re-derived at the 15-2
> preflight and a two-round refinement (2026-09-06) against HEAD `03ee0ad8`. What changed and why:
> rule 6's `+30%` was restored (15 of 20 rows had zero margin); the R2 §E figures were reconciled against
> the Round-3 epic-file re-prices that superseded them (`maos-registry`, `maos-domain`/17+19, `maos-audit`,
> `maos-compliance`, Epic-19's `maos-bin`); the `maos-cohort` reservation against a non-existent 14-2d T8
> grant was struck; two `no change` rows (`maos-control`, `maos-shell`) were shown not to cover their asks;
> `maos-kernel-core` became a permission rather than +250; and the three phantom budgets were retired.
> Full derivation and evidence: `implementation-artifacts/15-2-kloc-ceiling-rebase.md`.
### 15-3-single-phase-source-and-exit-command-check — Single phase source, `maos --help`, and the exit-command check

*Closes · Δ:* S4 · rule 1 mechanical · D20 (both gates) · kernel-Δ 0 · maos-bin verb-table lines after 15-2 · **7 ACs; refined 2026-09-06 by a mid-epic refinement round (rule 9) — five scouts disproved premises in this section and it is amended to match `implementation-artifacts/15-3-single-phase-source-and-exit-command-check.md`, which carries the full derivation**

- **AC1 (command).** The phase source is single, and the oracle can see **every** source. The original one-line grep was a null control for this story's own title: at HEAD there are **16 phase constants and 12 duplicated functions**, not 8, plus a third source no instrument can see. ⚠ **Refined again 2026-09-07 (round-table): the oracle counts FILES, not identifiers** — an identifier grep is moved by a *test function's name* (`gate_common.rs` defines `fn phase_disposition_inherits_the_nearest_earlier_phase`), so the target number was right only by coincidence. Primary oracle: `grep -rlE "^(pub )?const (CURRENT_PHASE|PHASE_ORDER)|^(pub )?fn (is_blocking_at|phase_disposition)" xtask/src/*.rs | grep -v gate_common.rs | wc -l` → **0** (**7** at HEAD, and it **names the offending files** rather than printing a number); plus `grep -rlE "MAOS_SHIP_PHASE" xtask/src .github/workflows | wc -l` → **0** (2 at HEAD) and `grep -rn '"current_phase"' xtask/src/*.rs | wc -l` → **0** (22 at HEAD, 11 modules — see AC2). The original per-form counts, kept as derivation: `grep -rho "const CURRENT_PHASE" xtask/src | wc -l` → **1** (from 8: `gate_common.rs:166`, `check_cohort_mesh.rs:8`, `check_enterprise_identity.rs:15`, `check_enterprise_pdp.rs:51`, `check_escape_detector.rs:62`, `check_fkcs.rs:16`, `check_trial_attestation.rs:23`, `check_wasm_form_equiv.rs:50`); `grep -rho "const PHASE_ORDER" xtask/src | wc -l` → **1** (from 8: `gate_common.rs:161` plus the same seven gates at `:9,:14,:48,:59,:17,:24,:46`); `grep -rEo "fn (is_blocking_at|phase_disposition(_with_order)?)" xtask/src | wc -l` → **3** (from 15); `grep -rn "MAOS_SHIP_PHASE" xtask/src .github/workflows | wc -l` → **0** (from 2: `check_third_party_trial.rs:91-93` reads `std::env::var("MAOS_SHIP_PHASE")`, wired at `discipline.yml:2433` as a GitHub **repository variable** — a phase that moves with no code change, invisible to `check-env-contract`, which scans only `maos-bin/src` at `check_env_contract.rs:120`). Proven in both directions: re-introduce one local const, watch the counts red, revert.
- **AC2.** The consolidation is **complete**, because the partial form arms the decay it exists to remove. Six gates carry a **stale** local `PHASE_ORDER` (`["v1_0","v1_5","v2_0"]` against `gate_common`'s `["v1_0","v1_5","v2_0","v2_2"]`) **and** byte-identical private copies of `phase_disposition` (`check_fkcs.rs:825`, `check_wasm_form_equiv.rs:71`, `check_enterprise_pdp.rs:73`, `check_enterprise_identity.rs:34`, `check_escape_detector.rs:84`; `phase_disposition_with_order` `check_trial_attestation.rs:444`) and `is_blocking_at` (`:838,:83,:83,:44,:94`). Today the divergence is inert because each gate's own `CURRENT_PHASE` sits inside its own order; **deleting the const and leaving the order makes it live** — `phase_disposition` opens `PHASE_ORDER.iter().position(|p| *p == phase)?`, so at `v2_2` the local copies return `None` → `is_blocking_at` → `false` and **six gates go silently non-blocking at the phase they were written to block** (proven by compiling verbatim copies against `gate-registry.toml:397`). So all three are deleted together, in favour of `gate_common`. Consumers repaired in the same commit or the build breaks: `xtask/tests/fkcs_oracle.rs:1-4` imports both fns from `xtask::check_fkcs` (`pub` at `:825,:838`), `check_trial_attestation.rs:9` imports `is_blocking_at` from `check_fkcs`, `check_wasm_form_equiv.rs:361` has the same in-file `use super::{…}`. `check_cohort_mesh.rs:53-58`'s `assert_eq!(CURRENT_PHASE, "v1_5", "Story 12.1 must not advance global phase")` is **deleted, not repointed**: today it compares a local const to its own literal (a tautology); against the shared const it becomes a runtime **panic** fired by a sanctioned phase advance. **D20 is closed on BOTH gates, not one** — `grep -rln BindingClass xtask/src/*.rs` = 18, and of the eight `CURRENT_PHASE` holders exactly two never adopted it: `check_escape_detector.rs` and `check_cohort_mesh.rs` (the original Story-12.1 hand-rolled carve-out `gate_common.rs:151-153` exists to replace). Adoption is **mixed per-leg, not whole-gate**: measured, `check-escape-detector --json` is `oracle_green:false, passed:true, exit 0` — RED at HEAD and passing only because it is advisory, so whole-gate `Blocking` would red CI on every seccomp-less host. `gate_common.rs:216-221` names *"a seccomp-capable kernel"* as the canonical `AdvisorySubstrate` case; legs 1–7 → `Blocking`, the two seccomp legs → `AdvisorySubstrate` with a real substrate probe (today `invoke_cargo_test_marker` `:193-233` collapses "seccomp unavailable" and "the code regressed" into one `green=false` — that collapse is the defect). Template: `check_multi_region_slo.rs:74-101`. ⚠ **Added 2026-09-07 (round-table, F16) — the name has a THIRD occupant and it is the machine-readable one.** Eleven gate modules emit a JSON output field literally named `"current_phase"` carrying the **ship-ladder** value (⚠ **17 sites, not 22** — corrected 2026-09-07 by 15-3's own measurement, `grep -rn '"current_phase"' xtask/src` = 17; the ELEVEN-module list below is exact: `check_escape_detector`, `check_fkcs`, `check_cert_rotation_trigger`, `check_enterprise_identity`, `check_enterprise_pdp`, `check_rotation_real_timing`, `check_scale_churn`, `check_wasm_form_equiv` + 3). Renamed to **`ship_phase`** in place — net 0 lines, **zero in-repo consumers**, and the only moment the rename is free. The Rust symbol `CURRENT_PHASE` is deliberately **not** renamed: 95 sites in `xtask/src` and 30+ documents depend on it, including `epic-14-preflight-decisions.md`, whose ratified **D20** text quotes *"decouple blocking-disposition from `CURRENT_PHASE`"* — renaming it strands the decision this story closes.
- **AC3.** `tests/phase-config.toml` stays the **delivered**-phase source (`v0.1-alpha`, `:5`; loaded at `coverage_matrix.rs:81`, compared at `:98-99`, order-checked at `:119`; `main.rs:289` is only the `--phase-config` default); its key is renamed `delivered_phase` so the two phases can never be conflated again. ⚠ **The blast radius is ~27 files, not 2**: `tests/coverage-matrix.yaml:2`, **two non-`Option` serde fields with no `#[serde(default)]`** (`corpus_types.rs:34` `CoverageMatrixFile`, `:52` `PhaseConfig` — a missed file is a hard parse error), **5 fixture `phase-config.toml`**, **7 fixture `coverage-matrix.yaml`** (incl. `clean-staleness`/`violation-staleness`, which have no phase-config but are parsed by `CoverageMatrixFile`), ⚠ **8 inline literals, not 10** (corrected 2026-09-07: the figure contradicted its own citation list, which sums to 3+2+3=8; measured 8) (`xtask/src/tests/coverage_matrix_tests.rs:36,55,210`, `xtask/src/tests/corpus_staleness_tests.rs:21,111`, `xtask/tests/coverage_matrix_completeness_tests.rs:20,58,92`), plus `coverage_matrix.rs:98,99,101,105-106,145` and `corpus_staleness.rs:102`. The orphan fixture `xtask/tests/fixtures/clean-coverage-matrix-fr-complete/` (zero consumers) is renamed or deleted — stated, not left to rot; and no repo-wide `sed -i` (`.claude/worktrees/` holds copies). The two ladders are genuinely disjoint and never converted (`v1_5` vs `v0.1-alpha`), so this closes a *documentation* conflation. (Measured and reproduced exactly: moving the delivered phase to `v1.5` yields **145** coverage-matrix violations, advisory only because `mode: warning` at `coverage-matrix.yaml:3` / `coverage_matrix.rs:65`. Separately and unfiled until now: the gate is **already RED at HEAD**, 33 violations, advisory — Epic 20 AC5 owns the mode flip and the cleanup.)
- **AC4.** `maos --help`, `-h`, `help` **and `--version`** exit **0** to **stdout** in under a second. Measured at HEAD, `maos --help` is worse than "boots the daemon": **EXIT 124** (killed by SIGTERM, never returns), **0 bytes stdout**, 529 lines stderr, 34 threads, **four panicked tokio workers** (`monotonic_now_ns() called before init_monotonic_base()`, `maos-capability/src/cap_tokens/mod.rs:69`), two files written — so a CI gate shelling it without a timeout **hangs the runner**. An unknown verb exits non-zero with the usage table, so `maos eval …` reds cleanly instead of by timeout (`epic-18…:23`). ⚠ **There are TWO `main()`s with different verb sets** — air-gap `main.rs:1319` `{init, run, backup, audit, install, --version}` and network `main.rs:1645` `{init, run, shell, audit, traceback}`, overlap three — and `print_air_gap_usage()` (`main.rs:1345-1360`) is **already** the table this AC called "new", 300 lines above the broken arm; Epic 20 AC1 ships both builds. `--version` works in air-gap (`:1332-1335`) and hangs in network, while `epic-20…:80` asserts it passes: **`--version` is in scope here**, matching `epic-20…:81,:101,:113`. "Completeness asserted against the dispatcher's match arms" is **mechanically impossible** (a Rust test cannot enumerate match arms), so: **one `const VERBS`** with a per-row `builds` field, from which both `main()`s **render help and dispatch** (lookup-then-dispatch), making divergence unrepresentable rather than merely tested — `grep -c 'Some("' ` inside each `main()` must reach **0**; a source-text regex is rejected because ten `Some("…")` literals already live in unit-test modules (`main.rs:3123,:4634,:7121,:7134,:7141,:12438-12440,:13823,:13838`) and a regex matching nothing **passes**. ⚠ **REVERSED 2026-09-07 (round-table, F13): the table AND `verbs::dispatch(&args)` live in a NEW `crates/maos-bin/src/verbs.rs`, and `SCANNED_SOURCE_FILES` goes 17 → 18 deliberately.** The earlier answer — keep it in `main.rs` so `cohort_daemon_smoke_13_5c.rs:862`'s `[(&str,&str); 17]` assertion (checked at `:937-941`) does not red — routes around a tripwire whose own message states its purpose: *"every maos-bin src/\*.rs file must be listed so new files cannot evade this negative."* It is a **doorbell, not a wall**; the sanctioned move is to add the entry and state whether the negative applies to `verbs.rs`. The destination it was protecting is **14,120 lines / 640 KB**, already ~73% of the crate; 14-2b's precedent extended a *domain* file, not the composition root. Both `main()`s call `verbs::dispatch`, so `main.rs` shrinks for the first time in four epics. The check is inserted **before** the unconditional banner at `main.rs:1646`, or the banner moves behind dispatch. The `MAOS_ONE_SHOT` mode list (⚠ **48 modes, not 29** — corrected 2026-09-07 by 15-3's measurement after the list was moved into `crates/maos-bin/src/verbs.rs`; `main.rs:7993` enumerated only a subset, dispatched `:4969`) is exposed the same way because AC5 must resolve it. `clap` is free — v4.6.1 is already in maos-bin's normal closure. Citations repaired: `main.rs:1653` is a **comment**; the dispatcher `match` is `main.rs:1677-1746` with the fallthrough `Some(_)` at `:1738`; `parse_run_args` spans `worker_spawn.rs:44-85`. Completeness test in `crates/maos-bin/tests/`, **enrolled by name in `discipline.yml`** (below), equality both directions, non-empty, exact count, both feature sets; proven-red twice.
- **AC5.** `cargo run -p xtask -- check-exit-commands` parses the fenced exit block of every `epics/epic-*.md` whose sprint status is not `done`, and resolves `maos` / `maosctl` / `xtask` tokens. Corpus by **positive roster, not glob-and-skip**: every non-`done` `epic-N` key must have **exactly one** file carrying `Hermetic exit command` — zero or two is a finding (7 epics ↔ 7 files at HEAD; `epic-15-*` alone matches **three** files, two of them preflights with no block, and every fence is a bare ` ``` ` with no info string). Reuse `xtask::sprint_status::load_sprint_status` (`sprint_status.rs:16`), the `epic-N-` walk of `check_epic_close_coherence.rs:301-332`, `FenceTracker` (`gen_abi_docs.rs:591-623`), and the quote-aware split of `check_decision_register.rs:124-158`; the shell tokeniser is genuinely new (`grep -rn shlex xtask/` = 0). Tokens fall in **three buckets** — `Resolvable`, a **closed** `DeclaredNonCommand` list, and `Unrecognised` → **a finding, never a skip** (five in-repo precedents for failing loud, zero for silent-skip-as-policy: `check_decision_register.rs:585-601`, `:182-187`, `check_third_party_trial.rs:446`, `evidence_ledger.rs:1277`, `gate_common.rs:71-80`). ⚠ **`MAOS_ONE_SHOT=<mode>` is resolved, never stripped** — stripping it turns `MAOS_ONE_SHOT=registry-server maos` (`epic-20…` L5) into a bare `maos`, a legal verb-less invocation, so the gate would pass **vacuously** over one of 29 undeclared modes. The closed skip list additionally covers what the first draft missed: `break` (epic-20 L6), `mktemp` (L5), **whole-line** `#` comments (epic-19 L2–L6, five of them), `target/debug/maos run` (epic-17 L3), `cargo build -p … --bin …` (same line), and `cargo test --workspace --no-fail-fast` (epic-15 L1). Command substitution and quotes are matched **before** separator splitting (`epic-20…` L7's `$(od … | tr …)` re-heads the command otherwise). ⚠ **An exit block is a declared TEMPLATE, not a literal command (F15, 2026-09-07).** `epic-16…` L3's own comment says *"the harness substitutes `<halt_id>` from `halt list`"*, while rule 1's planning-time leg says a block is saved only after it was *dry-parsed*. Both are true of different artifacts. `bash -n` cleanliness is therefore **not** a sufficient oracle: `<halt_id>` parses as two **redirections** — run verbatim the line creates a file named `--spirit` and drops the flag the command requires, which is why `epic-15-21-preflight-r2-2026-09-05.md:36` caught it in prose and nobody applied it. So `<placeholder>` is a first-class token class the gate recognises, and **15-3 repairs `epic-16` L3** to `--spirit <s> --kind provided-context --text …`. ⚠ **The repair is deliberately NOT made at spec time**: 15-3 captures the gate's pre-repair red on that line first, because it is the story's only proof the gate is not green from birth — its first act on the live corpus is to catch a real defect a human preflight had already filed and dropped. ⚠ And once this block runs in CI, `>` / `<` in an exit block are **write primitives in the runner's workspace** — a change of category, stated once, not an exploit. Verbs resolve by **subprocess `--help` with a hard timeout**, binaries built in the gate's own job (precedent `check_cohort_mesh.rs:37-50`), xtask via `std::env::current_exe()` — because `struct Cli`/`enum Commands` are **private to the xtask binary** (`main.rs:143-152`) and unreachable from the lib the gate must live in; surfaces are 92+`help` (xtask) and 23+`help` (maosctl, `maos-cli/src/cli.rs:39-134`). **Resolve-or-be-named, as STRENGTHENED 2026-09-07 (round-table, F14):** an unresolvable token passes **iff** its own epic's provenance names it as created by `<story-key> AC<n>`, that key exists in `sprint-status.yaml`, its status is not `done`, **and the named story's own file or that epic's AC text actually mentions the token**. Without the last clause the rule is a *receipt*, not a control — `maos summon-dragon` plus a provenance bullet naming any open story would pass, and the gate would be checking that a string exists in a YAML file. Falsifier: a provenance line naming a story whose file never mentions the token must red. ⚠ **MEASURED 2026-09-07: 6 owed, not 8** — `init`, `shell`, `run` and `audit` all RESOLVE once AC4's verb table exists, which is what AC4 is for; the surviving six are `release-dry-run` (epic-15 and epic-20 → 15-4), `uninstall` (epic-16 → 16-4), `eval` (epic-18 → 18-2), and `maos-registry-server` + `maos-spirit` (epic-20 → 15-4), each claimed in its epic's AC text. The owed tokens are re-measured against the strengthened rule in **T0**, before code — 7 are named in an AC by verb and `maos-registry-server`/`maos-spirit` in `epic-20` AC1's text — this is what lets the gate land Blocking in 15-3 even though **8 owed tokens** exist at that moment (`release-dry-run` ×2 → 15-4; `maos uninstall` → 16-4; `maos eval halt` → 18-2; `maosctl spirit install` → 20-1; `--test ignored_owner_audit` → 21-2; `maos-registry-server`, `maos-spirit` → the 15-4/20-1 artifact set), and it is a **stronger** control than resolution alone. The gate carries `blocks_read` / `tokens_resolved` counters and **hard-fails at zero** (`gate_common::vacuous_legs:288-295`). Enrolled Blocking at **eight** sites: module · `main.rs` mod + `Commands` variant + dispatch arm · a `discipline.yml` job with `timeout-minutes` · **`v1-0-ship-gate.needs` (`discipline.yml:3386`, 46 entries) — NOT `aggregate.needs` (`:3512`), which `check_ship_gate_completeness::extract_ship_gate_needs` never reads and which would red the gate on its first run** · `gate-registry.toml` `gates` + `[[ship_gate]]` with `{v1_0=advisory, v1_5=blocking, v2_0=blocking, v2_2=blocking}` (the 14-2a precedent; architecture `:130`'s advisory-until-v2_2 idiom governs gates whose *subject* is a v2.2 capability, per its own "absent-result → BLOCK at the v2.2 ship gate" tail) · `check_ship_gate_completeness.rs:20` `EXPECTED_GATES` (40 → 41, then 40 once AC6 retires the CNA gate in the same commit) · ⚠ **a `tests/coverage-matrix.yaml` row was DISPROVED as a site (2026-09-07)**: that matrix maps {FR, NFR} → {corpora, gates} per NFR-Meta-3, this gate serves no FR/NFR, and measured at HEAD **zero** of its four sibling process gates (`check-decision-register`, `check-epic-close-coherence`, `check-dev-record-completeness`, `check-bare-review-findings`) carries a row. Attaching one to an unrelated requirement would be the "claim standing in for a control" failure this story attacks. Enrolled at **seven** real sites; the matrix violation count is unchanged at 33. Proven-red in `xtask/tests/` against a **GREEN control block** (`decision_register_gate.rs:76-89`) driving the real `audit()` via a `pub mod` in `lib.rs`.

**AC5 post-review hardening (2026-09-07; supersedes F14's weaker token-only ownership rule):**
an owed command is accepted only from the provenance entry for its exact exit-line label
(`Line N` or `Line Nb`), and that entry plus the owning story/epic AC must name the complete
ordered command path. This prevents the `install` leaf in `maosctl spirit install` from
inheriting the owner of `maosctl install` on another line. A shipped binary that is present
but whose `--help` exits non-zero is unavailable, not commandless-resolved; the live
`maos-registry-server --bind … --root …` stub is consequently owed to open Story 20-1.
Re-measured gate: **7 epics / 7 blocks / 31 resolved / 6 owed / 0 findings**; the six owed
commands are `release-dry-run` twice, `uninstall`, `eval`, `maos-registry-server`, and
`maosctl spirit install`.

- **AC6.** `check-cna-registration` is retired **without a control going dark**. ⚠ The prior premise was inverted: `docs/compliance/cna-registration.md` is **tracked and present** (92 lines, added `3806d9df`) and the gate runs its **live** arm — `cargo run -q -p xtask -- check-cna-registration` → `PASS (CNA doc + SECURITY.md valid)`, exit 0. The absent-PASS arm at `check_cna_registration.rs:54-60` is not the arm taken. It enforces three properties (`:64-85`), two of which are live and have **no other enforcer**: `SECURITY.md` carries no `<TO-BE-PUBLISHED>` GPG placeholder, and its supported-versions table has a real `1.0.x` **row** (`has_version_table_row` `:38-50`, hardened against the `11.0.x` substring false-pass). The only other reader of `SECURITY.md` is `check_security_md.rs`, whose `REQUIRED_SECTIONS` (`:16-21`) asserts **only four H2 headers** and is *"intentionally header-text-based"*. So, in order: **(1)** port both assertions into `check_security_md.rs` (≈ +25 lines) with proven-red tests; **(2)** mint the retirement ADR on the **ADR-043** pattern (`invariant-lock` **never fires on `gate-registry.toml`** — `xtask/invariants/lock.toml` maps `I1..In → docs/invariants/*.md` only, so `:2-3` is a prose convention with exactly one discharging precedent: ratified ADR + item-by-item successor table + explicit *"no control goes dark"* + `# RETIRED … per ADR-nnn` at each site); **(3)** delete, `EXPECTED_GATES` **last** (deleting it first leaves an orphan CI job invoking a removed subcommand). Complete site list — **six files, 13 code/config lines**, not "six sites": `check_cna_registration.rs` (whole, **209 tokei `code`**, not the `wc -l` 260 that `epic-20…:88,:94` quotes) · `main.rs:95` comment (it also covers export-control and fuzz-targets — rewrite), `:96`, **`:948-953`**, `:1360` · `gate-registry.toml:72`, `:215-217` · `check_ship_gate_completeness.rs:34` (leave `:32`'s comment, it covers four gates) · `discipline.yml:2593-2605`, `:3401`, `:3453`, `:3490` · `tests/coverage-matrix.yaml:1277` **and `:1282`**. Not a site: `coverage-matrix.yaml:844`. `docs/compliance/cna-registration.md` is **kept** (NFR-Ops-4 evidence, linked from `SECURITY.md:41,:77`); `:9`, `:17`, `:77` are edited. Removing the `gates` entry while leaving a coverage-matrix row fails **open** today (`mode: warning`) and becomes a red at Epic 20 AC5 — do both. ⚠ **"xtask net lines ≤0" is FALSE as previously stated** and is replaced by measured arithmetic: retirement frees −216, the **full** AC2 dedup frees −104 (the minimal form frees only −4…−7 and does not fund this story), `current_phase()` −3, against +25 re-home and a gate measured at **380–520** production lines against the four closest anchors (`check_decision_register.rs` 604, `check_epic_close_coherence.rs` 454, `check_ship_gate_completeness.rs` 411, `check_cna_registration.rs` 209) — **net +95 … +235** ⚠ **DISPROVED BY MEASUREMENT 2026-09-07 — the story's delivered net is +742** (the gate measured **1143** charged lines against this projection's 380–520, i.e. 2.2× the upper bound; the dedup freed −85 not −104, and the `SECURITY.md` re-home cost +134 not +25). Taken as an operator-authorized EXACT-MEASURED grant, `xtask` **42353 → 43095, ZERO headroom**, per §9's own instruction that the lawful move is a measured grant with the measurement attached rather than compressing the gate until it fits. Was: against `xtask` **42048/42353 = 305** headroom, which 15-1, 19 (+150), 21-2 (+150–300) and 21-4 (+150–350) also draw on. Tests are free in `xtask/tests/` and `crates/maos-bin/tests/` (`kloc_check.rs:300-302`'s bare `-e tests` is depth-agnostic; RUN: 546 Rust reports, 0 under any `tests/`). If the measured gate exceeds the projection the lawful move is a **measured grant**, not compressing a gate until it fits.
- **AC7.** The register and the ledgers are corrected rather than left holding this story's work. **D20** (`epic-14-preflight-decisions.md:86`, OPEN, target `20-3-gate-honesty-pass`) is re-pointed to `15-3` and **CLOSED** on both gates, `check-decision-register` exiting 0 with the open count down by one and the `:91` re-homing map updated. `xtask/kloc.toml`'s xtask ledger — which books *"15-3 +34 at top"* — takes this story's **measured** figure, and the row's provenance line (*"Final ceiling = 41897 measured + 35"* under a value of **42353**) is repaired to record 15-2's re-base. `epic-19…:22`'s provenance section is normalised to the `- Line N —` form the other six use, so AC5's resolve-or-be-named allowlist is derivable. Filed, not fixed, with the reason named: the `-e tests` glob is the load-bearing premise of every ceiling here and **nothing asserts it** (a tokei major bump would silently add ~11k charged lines in xtask alone; the pin at `kloc_check.rs:7`/`:293` is a pin, not a property test) → `21-4`/D13(b); the **systemic** `worker-cli-fixture` pre-build, fixed per-job twice (`discipline.yml:2119-2126`, and `cargo test -p maos-bin` is EXIT 101 on a clean checkout without it) → `20-3`; `/tmp/maos/crl/` escaping `MAOS_HOME`.

⚠ **Test-enrollment note binding on AC4 and AC5.** **No CI job runs `cargo test -p maos-bin` unscoped** — all ten invocations are `--test <name>`-scoped (`discipline.yml:1924,1925,1927,1928,1951,1969,1970,1985,2048,2809`), so **26 of the 38 files in `crates/maos-bin/tests/` execute nowhere**, and `:1902-1903` says so; `:1931` records the same for `xtask`. A new test file is not a control until it is named in a workflow step. (D16 is recorded CLOSED promising *"the lock AND the whole-package leg both ship here"*; the whole-package leg did not ship, and `check-decision-register` is green over it. This story enrolls **its own** tests and files the rest.)

### 15-4-release-repair-and-first-signed-tag — Release repair and the secret-free release dry-run

*Closes · Δ:* S5 · kernel-Δ 0 (the signed tag itself = `ops-first-signed-tag`)

- **AC1 (command).** `cargo run -p xtask -- release-dry-run` (NEW xtask verb; charged to 15-2's xtask row) builds `maos` **and** `maosctl` (`release.yml:44` builds only `-p maos-bin`) — the host target locally, the three matrix targets in a NEW Blocking discipline job `release-dry-run` (`aggregate.needs`) — lays the artifacts out as `release.yml:67-69` does with `download-artifact@v4` + `merge-multiple: true` (absent today, so each artifact lands in `dist/<name>/` and `sha256sum maos-*` at `:73` fails "Is a directory"; proven-red first by running the merge step against today's layout), writes `SHA256SUMS`, and skips signing. On a `v*` tag `release.yml` then produces both binaries for the three targets with `SHA256SUMS` + Ed25519 signature (`:74`) and its verify step (`:86`) passes.
- **AC2.** `aarch64-apple-darwin` and `aarch64-unknown-linux-gnu` (`release.yml:18-23` matrix, never compiled in any workflow) compile in the `release-dry-run` leg before the tag; the macOS runner is a cost line, not a secret.
- **AC3.** `sign-and-publish` (`release.yml:56-57`, `needs: build` only) refuses to publish unless the tagged SHA has a successful `discipline / aggregate` check-run (`gh api repos/:owner/:repo/commits/<sha>/check-runs` with the job's `GITHUB_TOKEN`, filtered on name `aggregate`, conclusion `success`), because a job cannot `needs:` across workflows (`release.yml:2-4` runs on `v*` tags, `discipline.yml:3-7` on push/PR; no `workflow_call`/`workflow_run` exists in `.github/workflows`).
- **AC4.** The `MAOS_RELEASE_PUBKEY` guardrail (`maos-audit/src/release_verify.rs:280-291`, `option_env!` at `:287`, "trivially true" when unset per `:283-285`) fails loudly in release context when unset.
- **AC5.** Workspace version `0.1.0-alpha.1` (`Cargo.toml:64`; the story greps the literal `0.1.0-alpha` — 25+ files: 12 Spirit manifests, `templates/spirit-rust/manifest.toml`, `manifest_n_minus_1_compat.rs` ×2, `worker/tests/cli_wrapper_admission.rs`, docs-site — and states which bind to the workspace version); `STABILITY.md` regenerated (`stability_matrix.rs:17,137`); `Cargo.lock` stays committed (tracked at HEAD, `git ls-files Cargo.lock`) and `--locked` builds pass on all three targets; `docs/release/tag-procedure.md` written (`docs/release/` has 3 files), linking — not duplicating — `docs/runbooks/release-signing.md`.
- **AC6.** The tag is cut by the operator row `ops-first-signed-tag` (acceptance: `release.yml` green on `v0.1.0-alpha.1` with both secrets present) only after `ops-provisioning-secrets-and-accounts` records both secrets; it is not in the exit block (rule 2).

### 15-5-decision-adrs-and-provisioning-checklist — Decision ADRs 060–064 and the provisioning checklist ◆

*Closes · Δ:* rule 7, D-A..D-E · kernel-Δ 0

- **AC1 (command).** `ls docs/adr/ADR-060-*.md docs/adr/ADR-061-*.md docs/adr/ADR-062-*.md docs/adr/ADR-063-*.md docs/adr/ADR-064-*.md` lists five accepted ADRs (numbers free: highest is `ADR-059`; frontmatter shape `Status/Gate/Decided/Accepted-in-PR/Amends/Reuses`, `ADR-059:1-8`; `docs/adr/index.md` updated), each carrying the ratified decision as content, not as an open question: **060** Spirit forms by trust tier (Fork C: `rust-inproc` first-party only, never admitted from a registry, runs unsandboxed in the daemon by design; WASM = third-party/polyglot form; `cli_wrapper` = agent CLIs; FR5 by form; FR33 via WASM toolchains) — carries **D-C** (WASM recall transport = a maos-bin side-channel service over a unix socket the runner connects to (the runner's stdio IS the kernel bridge and stays unchanged), kernel-Δ 0, priced by 17-3a; the WIT world imports nothing) and **D-E** (a first-party Spirit crate is a library the daemon may call directly: the digest is rendered by the daemon via `Butler::morning_digest` without Butler joining the topology, 19-4); **061** Worker egress + credential (**D-B**): T3 egress allowlist from the manifest enforced by a host-side proxy, `--network=none` stays, `env_clear`, kernel-minted scoped credential presented to the proxy, the raw vendor key exists only at the host-side proxy (never in `/proc/<pid>/environ`), out-of-port TL record; **062** mutating operator surface (**D-A**): the `maos run`/`maos shell` daemon process binds a loopback HTTP POST door with a bearer; `maos init` mints endpoint + bearer into `MAOS_HOME/control.json`; `maosctl` discovers both from that file and `maos-cli` gets an HTTP client and may not link `maos-control` (which depends on kernel-core); one trust path; supersedes the READ-ONLY decision at `maos-control/src/lib.rs:68-76`; **063** revocation model: chooses exactly one of reissue-is-revocation / CRL, and defines the machine-readable post-grace token as a `RuptureReason` variant (`crates/maos-domain/src/frame.rs:384`, `#[non_exhaustive]`; `CertPostGraceReject` emitted on the promoted-generation mismatch path at `transport.rs:1032-1052 (call `:1048-1052`)` via `journal_peer_identity_refusal`, which already takes `detail: &str`, `router.rs:506`; consumed by 21-3 AC3, +3 maos-domain lines under 15-2's row); **064** one replay selector (**D-D**): `MAOS_INFERENCE_MODE={record,replay,live}` + `MAOS_REPLAY_CASSETTE=<path>`; `--replay-llm` (`worker_spawn.rs:56-59`, accepted no-op) retired; no `--replay` flag on `maos`; unset = today's deterministic path; consumed by 15-6, 18-3/18-4, 19-1, 20-4, 21-1.
- **AC2.** `docs/runbooks/provisioning-checklist.md` (absent at HEAD) lists every item of preflight §2.6 with a state `present` / `waived-by <name> <date>`; the operator-lane row `ops-provisioning-secrets-and-accounts` (`sprint-status.yaml`) reports against it.
- **AC3.** `STABILITY.md` gains the WASM-off-until-Hold-2 statement (`RELEASE-HOLDS.md:29-33,:71` already carries it; `STABILITY.md` has no `wasm` mention today); `--features wasm-host` (`crates/maos-bin/Cargo.toml:30`) documented for self-builders.
- **AC4.** PRD `[DELTA-2026-09-04 R2]` (`prd/project-scoping-phased-development.md:18`) and architecture §13.1 note (`architecture-maos-minimal-opus/13-phased-roadmap.md:5`) cross-checked against the ADR text.

### 15-6-inference-replay-seam — Inference record/replay seam for every inference consumer

*Closes · Δ:* preflight §2.4, D-D · kernel-Δ 0

- **AC1 (command).** `MAOS_INFERENCE_MODE=replay MAOS_REPLAY_CASSETTE=<path> maos run spirits/researcher/manifest.toml --once` and `MAOS_INFERENCE_MODE=record MAOS_REPLAY_CASSETTE=<path> …` work for **every inference consumer**: the shared adapter at `main.rs:3136` (consumed by shell/hello-spirit via `main.rs:3247-3250` → `maos_shell::run_shell`, `maos-shell/src/lib.rs:15,166`, `maos-spirit-hello/src/lib.rs:79`) and the Researcher-private adapter at `main.rs:4599` (today's researcher-only sites `:4609` record / `:4623` replay) are both wrapped at the composition root by one `MAOS_INFERENCE_MODE` selector (ADR-064, D-D); `InferencePortAdapter` itself (`kernel-core/src/inference/mod.rs:38`) is untouched. The eight Spirits that declare no inference (`spirits/*/tests/spirit_smoke.rs` Decisions E/G/I; Butler is `McpClientPort`-only, `spirits/butler/src/lib.rs:812-825`) are unaffected and inherit the seam when 18-1 wires them. **Unset = today's deterministic path** (`main.rs:4642-4645`; the 10 existing `maos run spirits/researcher/manifest.toml --once` sites stay green — `cohort_daemon_smoke_13_5c.rs:732`, `researcher_8_14c.rs:151,182,238`, `inference_provider_polarity_8_11.rs:45`, `cross_team_crossing_13_6b.rs:2647`, … per R2 note §A #65); `replay` requires `MAOS_REPLAY_CASSETTE` else a typed non-zero exit; `live` requires a configured provider, else a typed `Unconfigured` halt with non-zero exit (`UnconfiguredProvider`, `main.rs:3121-3124`). `--replay-llm` (`worker_spawn.rs:56-59`) is retired and no `--replay` flag is added.
- **AC2.** Cassette `schema_version` stays `maos.journey.cassette/v1` (`cassette_replay.rs:28-33`); a `provenance: seed|live-record` field is added and the three `model_id: "hand-authored-seed"` cassettes (`cassettes/j-butler/on-idle-halt.json:4`, `j-researcher/survey-distill.json:4`, `j0/shell-intro.json:4`) are re-stamped `seed`; the journey harness labels them.
- **AC3.** `journey-nightly.yml:94` no longer passes `--live` to nextest (it sits after `--no-fail-fast`, not after `--`, so nextest rejects it); the `tier-2-rerecord` leg (`:65`) uses `MAOS_INFERENCE_MODE=record`; `MAOS_JOURNEY_MODE` (`env_contract.rs:260`, read at `main.rs:4608`, set at `journey-nightly.yml:87`) is retired in favour of `MAOS_INFERENCE_MODE=record`. The paid leg itself is the operator row `ops-nightly-cassette-rerecord`.
- **AC4.** `MAOS_INFERENCE_MODE` registered `UserFacing` in `env_contract.rs` (`:13` format); `MAOS_REPLAY_CASSETTE` kept as the cassette path (`:255`).

## Dependencies

None (opens the lane). 15-2 before 15-1, 15-3 (verb table, maos-bin) and 15-6; 15-3 before the exit's `check-exit-commands` line runs; 15-1 (aggregate green) before 15-4 AC3 can be true; 15-5 before Epics 16–17 open; 15-6 before Epic 18. Suggested order: 15-2 → 15-3 → 15-1 → {15-6, 15-5 in parallel} → 15-4 (the tag last, via `ops-first-signed-tag`).
