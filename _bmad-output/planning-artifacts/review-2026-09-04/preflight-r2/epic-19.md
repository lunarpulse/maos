# Preflight R2 — Epic 19 The Founder Loop (W4)

> Scout note, 2026-09-05. Epic file: `_bmad-output/planning-artifacts/epics/epic-19-founder-loop-w4.md`. Every citation pinned `@9f920180` (HEAD re-verified at start and end; `git status --porcelain` showed only `review-2026-09-04/preflight-r2/` untracked — nothing cited was dirty; the sibling worktree under `.claude/worktrees/` was excluded from every grep). Method: read-only. RUN, not inferred: `cargo run -p xtask -- kloc-check` (EXIT=1, table §E), `cargo run -p xtask -- check-j1-loopback-delegation` (**EXIT=1, Blocking**), `cargo build -p worker` (to produce `target/debug/worker-cli-fixture`) then `cargo run -p xtask -- demo-j1 --skip-build` (isolated `MAOS_HOME`; claim table in §A #4). No `maos` verb was executed directly (Epic-15 finding: `maos --help` boots the daemon); every `maos run` flag was verified in the hand parser `crates/maos-bin/src/worker_spawn.rs:44-72`. Builds on the Epic-15/16/17/18 notes (all `needs-rework`); their §K CONFIRMED-TRUE items are not re-checked here.

## Verdict block

| | |
|---|---|
| **Verdict** | **needs-rework** (not blocked: the Orchestrator, the FR20 buffer, the fixture Worker subprocess, the adapters, `demo-j1` and `journey_j1` are all real — but the exit block names a state that is unreachable on the current tree, the epic's own gate is RED at HEAD for a reason no story owns, one story silently needs kernel lines, one AC rewrites a fail-closed control, three ACs stand on mechanisms with zero production callers, and every story carries an undecided fork) |
| **Confidence** | **40 / 100** |
| **Measured duration** | **4.5–6.5 weeks** serial for one engineer + agents (epic says 4–6; the J1-lane tempo supports 4–5.5 only if 15-2, 15-6 and 16-1 have landed first — §G) |
| **False / partial premises** | **11 FALSE/NEITHER, 19 PARTIAL** out of 44 audited claims (§A); 14 CONFIRMED TRUE |

**Top-3 blockers**

1. **The exit block's target state cannot occur, and the exit command is red at HEAD for a reason outside this epic.** Measured (`demo-j1 --skip-build` @9f920180): the claim table renders **two** ABSENT beats (`tier2-live-agent-signed`, `halt-resume-referential-identity`), `two-host-signed-run` as **INDETERMINATE** (not ABSENT — the paid capture has been present since 2026-08-25, `_bmad-output/test-artifacts/j1-two-host-evidence/two-host-capture.json`, README `:11-18` "do not delete"; `apply_capture_judgement` sets `Indeterminate`, `xtask/src/demo_j1.rs:959-981`), and **two executed FAIL** beats, so the command exits with `demo-j1: 2 executed beat(s) did not hold`. "ABSENT = {two-host-signed-run} only" (exit line, 19-4 AC1) is therefore wrong twice: that beat will never be ABSENT again, and `tier2-live-agent-signed` is by construction earned only by `--live-codex` (owner string `demo_j1.rs:890-894`; `SIGNABLE_WORKER_CLIS` excludes the fixture `:392`) — an operator-lane beat that the hermetic exit demands be non-ABSENT. The third ABSENT beat, `halt-resume-referential-identity` (`FOLLOWUP-J1-RESUME-SEAM`, `:900-904`; `journey_j1.rs:303-308` "resume seam not yet implemented"), has no AC in 19-4. The FAILs come from `check-j1-loopback-delegation` (Blocking, `xtask/gate-registry.toml:308-309`, `needs:` of the aggregate `discipline.yml:3410`) which is **RED at HEAD**: leg `completion-vectors-enrolled` derives the enrolled set by suffix (`J1_TEST_SUFFIXES = ["_1a.rs","_1b.rs","_2a.rs","_2b.rs"]`, `check_j1_loopback_delegation.rs:114`) and the 14-2a test `crates/maos-bin/tests/cert_rotation_trigger_14_2a.rs` matches `_2a.rs` without being in the job (`discipline.yml:1904-1925`). A fifth red Blocking gate that 15-1 ("4 red gates", `epic-15-foundations-w0.md:7`) does not list; 19-1 AC2's "stays green" is false before the story starts.
2. **19-3 "kernel-Δ 0" is false, and "real Worker by default" rewrites a fail-closed control.** An "explicit child cwd" cannot be set from `maos-bin`: `BridgeSpawnSpec` has no cwd field (`crates/maos-kernel-core/src/lifecycle/cli_wrapper/runtime.rs:240-269`) and `spawn_and_bridge` never calls `current_dir` (`:449-476`; grep of `cli_wrapper/` = 0) — `manifest-codex.toml:50-52` documents "MAOS does NOT set the child CWD — it inherits `maos`'s". That is +3–5 kernel lines (field + `cmd.current_dir`) and a re-pin, or a process-global `set_current_dir` hack. "Default worker manifest is a real agent CLI" collides with `live_agent_gate` (`worker_cli.rs:860`, called `worker_spawn.rs:505-507`: "CI never sets the flag, so CI cannot spawn a paid agent"), with the built-in fixture-only grant (`worker_spawn.rs:243`), and with 11 files that consume `spirits/worker/manifest.toml` as the hermetic baseline (§C). The test the AC "rewrites" is itself the control: "Removing the gate call from the run path reds THIS test" (`smoke_cli_wrapper_8_12.rs:139-142`). And "three concurrent fixture Workers" has no substrate: the topology loop is sequential (`for entry in topology_entries` `main.rs:3397`; `run_cli_wrapper_manifest` is synchronous, `worker_spawn.rs:384`, "stays synchronous" `main.rs:3445`) and two Blocking controls pin exactly ONE `host` entry (`check_j1_loopback_delegation.rs` leg 1; `topology_delegation_1a.rs:236`).
3. **19-4 AC2/AC3 stand on mechanisms with zero production callers.** The halt: the Orchestrator has no `EpistemicScalarPort` builder (grep `with_scalar_port` in `spirits/orchestrator/src/lib.rs` = 0), is loaded bare (`main.rs:3762-3768`), its manifest rules are on `dispatch_ambiguity`/`delegation_confidence` (`spirits/orchestrator/manifest.toml:40-48`), and the daemon's port-wiring allowlist recognises only `belief_variance|user_preference_drift|diagnostic_confidence` (`main.rs:553-567`); the tag itself has two spellings — `story.acceptance_criterion.ambiguous` (three corpus seeds, `lcas.rs:84,124,164`, no emitter) and `task.acceptance_criterion.ambiguous` (a real emitter, `crates/maos-spirit-hello/src/lib.rs:148`, and 16-2's exit token). The digest: `Butler::morning_digest` (`spirits/butler/src/lib.rs:675`) has **zero** production callers (bench `j0.rs:83`, `journey_butler.rs:153`, two butler tests); Butler requires `provider.complete` (`butler/manifest.toml:21-22`) so "joins the topology" under replay needs 18-1; the daemon's only digest path is J3's `maos_digest::derive_team_digest` (`main.rs:465-516`), a different artifact. "Butler joins (or the daemon renders)" is an open fork (rule 7) hiding an absent foundation.

---

## A. Citation audit

Legend: TRUE / PARTIAL (exists, claim about it wrong) / FALSE / NEITHER (does not exist and no story creates it) / NOT CHECKED. All `@9f920180`.

| # | Claim | Where | Verdict | Evidence |
|---|---|---|---|---|
| 1 | "4–6 weeks (measured)" | header | PARTIAL | no measurement cited; J1-lane analogues give 4.5–6.5 (§G) |
| 2 | "Closes FR20, FR21, FR25, FR52" | header | PARTIAL | FR25/FR52 are already LIVE (`08-fr21-65-scout.md:2`: CliWrapper admission+probe; CLI subprocess capture); FR20 PARTIAL (`05-fr1-20-scout.md:22`: queue enqueues in a fresh process ⇒ lost); FR21 PARTIAL (`:3` 50-worker floor bench only). PRD text `prd/functional-requirements.md:53,58,63,65` |
| 3 | "RELEASE-HOLDS row 16" | header, 19-3 AC2 | TRUE | `RELEASE-HOLDS.md:63`; its successor column names "a MAOS-side worktree diff at the spawn seam" |
| 4 | "J1 ABSENT beats except the two-host signed run" / "today three (`:888-905`)" | header, 19-4 AC1 | **FALSE (measured)** | static list `unlanded_beats()` `demo_j1.rs:888-906` = 3; MEASURED claim table @HEAD: `tier2-live-agent-signed` ABSENT, `halt-resume-referential-identity` ABSENT, `two-host-signed-run` **INDETERMINATE** ("capture present and validated; the run is NOT claimed"), `completion-vectors-enrolled` **FAIL**, `disallowed-intent-refused-blocking` **FAIL** ("enrolled in CI = false"); exit `2 executed beat(s) did not hold` |
| 5 | `MAOS_INFERENCE_MODE=replay` | exit l.1 | NEITHER at HEAD | grep `crates spirits xtask .github` = 0; created by 15-6 (`sprint-status.yaml:238`, backlog). Under the Epic-15 amended contract "`replay` requires `MAOS_REPLAY_CASSETTE`" (epic-15 note §K #14) the line names no cassette |
| 6 | `cargo run -p xtask -- demo-j1` | exit l.1 | TRUE | `xtask/src/main.rs:993-1010` `DemoJ1 { live_codex, codex_topology, keep_home, skip_build, skip_gate }`; dispatch `:1381-1387` |
| 7 | `--replay` on `demo-j1` | exit l.1, 19-3 AC1, 19-4 AC1 | NEITHER at HEAD | not among the five args (`main.rs:995-1009`); declared as 19-4 AC1 — but 19-3 AC1 uses it first (§I) |
| 8 | "unattended" | exit l.1 | PARTIAL | the fixture run is unattended (`demo_j1.rs:542-548`, no prompt) but `preflight()` runs `cargo build --workspace` unless `--skip-build` (`:412-420`) and the default path runs the Blocking gate (`:260-262`) — red today (#4) |
| 9 | "digest cites a source ref per completion" | exit l.1, 19-4 AC3 | NEITHER (foundation) | no production digest on the J1 path: `morning_digest(` callers = `maos-bench/src/harness/j0.rs:83`, `journey_butler.rs:153`, `butler/tests/{hallucination,digest}.rs`; `main.rs` digest = `maos_digest::derive_team_digest` (`:465-516`, J3 `RawDigestInputs`) |
| 10 | `demo-j1 --live-codex` (paid) | operator legs | TRUE | `main.rs:996-997`; `live_codex_take` `demo_j1.rs:1202`, spawns `maos run <codex topology> --live --once` with `MAOS_LIVE_AGENT=1` (`:1341-1349`) |
| 11 | "two-host signed run owned by the closed `j1-crosshost-2d` lane" | operator legs | TRUE | `sprint-status.yaml:209` done 2026-08-25; evidence dir present (README `:11-18`) |
| 12 | "ZERO kernel-Δ @24472; 19-2 may need ≤10 for a safe-point hook" | Kernel-Δ | PARTIAL | pin TRUE (`kernel-core-baseline.toml`, `check-kernel-baseline` green per Epic-15 note). 19-2 needs **0** (registry + spirit-side gate, §D); the story that needs kernel lines is **19-3** (cwd, blocker 2) |
| 13 | `maos run spirits/topologies/j1-founder-loop.toml … --once` | 19-1 AC1 | TRUE | `worker_spawn.rs:44-72` (`--once` `:55`); topology file `spirits/topologies/j1-founder-loop.toml:1-30`; fallthrough `main.rs:1738` |
| 14 | `--epic tests/fixtures/j1/epic.md` | 19-1 AC1 | NEITHER ×2 | `--epic` → `Err("unknown argument … expected: <manifest> [--live] [--once]")` (`worker_spawn.rs:61-66`); `tests/fixtures/` = `anthropic-skill, anthropic-skill-invalid, dirty-network-fixture, wasm` — no `j1/`; no AC creates the fixture |
| 15 | "composition root currently forbids a loop, `main.rs:3434-3440`" | 19-1 AC1 | TRUE | `:3434-3441` "THIS STORY DOES NOT BUILD AN ORCHESTRATOR DISPATCH LOOP … do not 'fix' it by inventing a scheduler hook" |
| 16 | "calls `assign_frame_remote` once per host" | 19-1 AC1 | TRUE (+ pinned) | `:3528` inside `for entry in topology_entries` (`:3397`) on `match &entry.host` (`:3452`); gate leg 1 needle "must emit the delegation via `assign_frame_remote`" (`check_j1_loopback_delegation.rs:268`); exactly ONE host entry pinned (`topology_delegation_1a.rs:236`; `j1-founder-loop-crosshost.toml:3-8` explains the escape = a separate file) |
| 17 | `MAOS_DELEGATED_GOAL` "`main.rs:3495`" | 19-1 AC2 | PARTIAL | read at `:3497` (`:3495-3496` is the comment); registered `env_contract.rs:32`; on loopback it FALLS BACK to the rehearsal string (`:3516-3519`) — removing it "on the loopback arm" touches the cross-host REQUIRED branch text (`:3504-3511`) and the sentinel test `smoke_cli_wrapper_8_12.rs:216-235` |
| 18 | "`check_j1_loopback_delegation.rs` stays green" | 19-1 AC2 | **FALSE at HEAD** | RUN: `EXIT=1`, `"passed":false,"binding":"Blocking"`, finding `completion-vectors-enrolled`: `cert_rotation_trigger_14_2a` not invoked by the job (`discipline.yml:1904-1925`); cause `J1_TEST_SUFFIXES` `:114` matches `_2a.rs` |
| 19 | Orchestrator "gains `with_inference_port`" | 19-1 AC3 | NEITHER | grep `InferencePort` in `spirits/orchestrator/src/lib.rs` = 0; pattern exists on Researcher (`spirits/researcher/src/lib.rs:502-518`) with daemon wiring `main.rs:4590-4641` (Researcher-private adapter) |
| 20 | `spirits/orchestrator/manifest.toml:36` "provider.complete omitted" | 19-1 AC3 | TRUE | `:33-36` "dispatch is deterministic (Decision E; provider.complete omitted, the halt path needs no inference)" — AC3 reverses Decision E without saying so (§H-1) |
| 21 | "stays `rust-inproc` (ADR-060)" | 19-1 AC3 | PARTIAL | `forms = ["rust-inproc"]` `manifest.toml:7`; `docs/adr/` tops at `ADR-059` — ADR-060 is a 15-5 deliverable |
| 22 | "FR21 distillate rows are written for Worker output in production" | 19-1 AC4 | NEITHER (foundation) | `write_distillate` production callers = 0 (`main.rs:6041` smoke arm, `:8986` test harness); Worker completion is journaled as `TaskComplete` only (`delegation.rs:361-374`, `:792-794`); `DistillateWriter` built at `main.rs:2379` and unused on the topology path; FR21 gate = 60 s window over `TaskComplete` (`delegation.rs:276-292`) |
| 23 | `followup_dispatch` (`lib.rs:389-401`) | 19-1 AC4 | TRUE | `spirits/orchestrator/src/lib.rs:389-401`; zero production callers (the root uses `build_task_assign` `:3522` + `assign_frame_remote` `:3528`) |
| 24 | `maosctl orchestrator queue "…"` | 19-2 AC1 | PARTIAL (flag) | real shape `maosctl orchestrator queue --spirit <id> <instruction>` (`crates/maos-cli/src/cli.rs:750-758`, `--spirit` required); today spawns `MAOS_ONE_SHOT=orchestrator-queue` (`subcommands.rs:1656-1676`) which accepts only `hello-spirit` (`main.rs:5667-5672`) and enqueues into that fresh process's registry, drains, exits (`:5688-5718`) |
| 25 | "`dequeue_at_safe_point` gets its first caller" | 19-2 AC1 | TRUE | def `crates/maos-kernel-core/src/orchestrator/buffer.rs:62`; non-test callers = 0 (doc mentions `cli.rs:83`, `orchestrator/mod.rs:7-11`, `spirits/orchestrator/src/lib.rs:14,215`; tests `buffer.rs:125-217`, `spirits/orchestrator/tests/*.rs`) |
| 26 | "enqueue arrives over the 16-1 surface" | 19-2 AC1 | NEITHER at HEAD | `maos-control` is GET-only (`crates/maos-control/src/lib.rs:299,347,392,428`; `:69-76` refuses "a SECOND trust path"); 16-1 backlog (`sprint-status.yaml:241`) |
| 27 | "`Ctx` ABI unchanged (`lib.rs:143-147`)" | 19-2 AC2 | PARTIAL (cite) | `:143-147` is a doc comment in `spirits/orchestrator/src/lib.rs` ("the ABI `Ctx` exposes no kernel-buffer surface"); the ABI is `crates/maos-spirit-abi/src/ctx.rs:58` (cancellation, capability, mailbox handles; `pub fn` `:79,96,113`) |
| 28 | "the loop is daemon-side" / "during an in-flight delegation" | 19-2 | PARTIAL (foundation) | needs a long-running `maos run <topology>` (no `--once`: `--once` = one `on_idle` pass + drain, `main.rs:3341`; continuous loop `:4020-4031` per Epic-16 note) AND an async topology loop (`main.rs:3445-3446` "stays synchronous") |
| 29 | "Hermetic: `demo-j1 --replay` runs the fixture Worker" | 19-3 AC1 | PARTIAL | the fixture ALREADY runs with no flag (`demo_j1.rs:48` `TOPOLOGY`, `:542-548` `env_remove("MAOS_LIVE_AGENT")`); it is a real subprocess (`spirits/worker/src/bin/worker-cli-fixture.rs:20-45`; `spawn_and_bridge` `runtime.rs:449-476`; `child_pid` asserted `journey_j1.rs:138-146`) — the flag adds only the replay env |
| 30 | "the default worker manifest is a real agent CLI under the 17-1 profile" | 19-3 AC1 | PARTIAL / contradiction | `spirits/worker/manifest.toml:8` = `worker-cli-fixture`; live profiles already exist (`manifest-codex.toml`, `manifest-claude.toml`, `j1-founder-loop-codex.toml`); fail-closed `live_agent_gate` (`worker_cli.rs:860`; `worker_spawn.rs:505-507`) + fixture-only built-in grant (`:243`) mean "default" cannot be real without `MAOS_LIVE_AGENT` + `MAOS_HOST_GRANTS`; 17-1 backlog and needs an unpriced proxy (Epic-17 note blocker 1) |
| 31 | "worktree-diff effect oracle in `worker_spawn.rs` with an explicit child cwd" | 19-3 AC2 | PARTIAL | oracle placement OK (maos-bin, RELEASE-HOLDS row 16 successor); cwd NOT settable from maos-bin (`BridgeSpawnSpec` `runtime.rs:240-269` no cwd; `spawn_and_bridge` `:461-469` no `current_dir`); `manifest-codex.toml:50-52` |
| 32 | `worker_cli.rs:492-495` "effect-blind verdict" | 19-3 AC2 | PARTIAL (cite) | `:490-497` is the doc comment naming the residual; the verdict fn `claude_result_object_oracle` is `:498-537` (RELEASE-HOLDS row 16 cites both correctly) |
| 33 | test `ci_local_split_refuses_a_granted_real_agent_without_the_live_flag` | 19-3 AC3 | TRUE (control) | `smoke_cli_wrapper_8_12.rs:147` (`#[cfg(target_os = "linux")]` `:145`); asserts refusal names `MAOS_LIVE_AGENT` (`:202-205`); doc `:139-142` "Removing the gate call from the run path reds THIS test" |
| 34 | "three concurrent fixture Workers run hermetically" | 19-3 AC3 | NEITHER (foundation) | sequential `for entry` (`main.rs:3397`), synchronous `run_cli_wrapper_manifest` (`worker_spawn.rs:384`), one `host` entry pinned (#16), `wait_and_finalize(…, 0, …)` pid 0 (`worker_spawn.rs:732`), no SCB (Epic-16 note blocker 1) |
| 35 | "`nfr-perf-8` measures the real spawn path (advisory until wall-clock stable)" | 19-3 AC3 | PARTIAL | job `nfr-perf-8-orchestrator-fanout` `discipline.yml:1490-1505`, `continue-on-error: true` `:1493` (advisory since 2026-06-12); bench spawns 50 in-process task handles (`crates/maos-bench/benches/orchestrator_fanout_nfr_perf_8.rs:22`) — "real spawn path" is a bench rewrite |
| 36 | "`story.acceptance_criterion.ambiguous` exists only in `lcas.rs:84`; no emitter" | 19-4 AC2 | PARTIAL | at `crates/maos-corpus-gen/src/lcas.rs:84,124,164` (three seeds); no emitter TRUE; sibling `task.acceptance_criterion.ambiguous` HAS an emitter (`crates/maos-spirit-hello/src/lib.rs:148`) and is 16-2's exit token (Epic-16 note #5) |
| 37 | "halt within 2 frames" (Orchestrator) | 19-4 AC2 | NEITHER (foundation) | Orchestrator has no scalar port (grep = 0), loaded bare `main.rs:3762-3768`; manifest rules `dispatch_ambiguity`/`delegation_confidence` `manifest.toml:40-48`; port-wiring allowlist `main.rs:553-567` excludes both tags; Mira's wiring is the template (`:3796-3835`, `ButlerOrchestratorAdapter` `:570-591`) |
| 38 | "Butler joins the J1 topology (or the daemon renders)" | 19-4 AC3 | FORK + foundation | see #9; Butler `provider.complete` required (`spirits/butler/manifest.toml:21-22`); `source_log_ref` types exist (`butler/src/lib.rs:266,273`; `maos-domain/src/invariants/i11.rs:36`; `distillation.rs:18`); hallucination check pattern exists (`spirits/butler/tests/hallucination.rs`) |
| 39 | "0 hallucinated on the run" (FR17) | 19-4 AC3 | PARTIAL | FR17 floor is over a 100-digest corpus, digest within 30 s of first session (`functional-requirements.md:50`) — one digest on one run is not the FR |
| 40 | "`journey_j1` asserts the halt and digest beats" | 19-4 AC4 | TRUE (file) | `crates/maos-journey-test/tests/journey_j1.rs` 3 tests (`:38,:185,:225`), run by `journey-hermetic-tier-1` (`discipline.yml:2102-2128`, pre-builds `worker-cli-fixture` `:2125`); no-wallclock guard `guards_meta.rs:24-25`; resume oracle explicitly deferred `:303-308` |
| 41 | tracker rows `19-1..19-4` | header | TRUE | `sprint-status.yaml:260-265` |
| 42 | R2 proposal row / mapping | header | TRUE | `sprint-change-proposal-2026-09-04-round2.md:55,77-78` |
| 43 | "Epic 17 (17-1 profile) and Epic 18 (inference seams)" | Dependencies | PARTIAL (over-tight) | hermetic path needs 15-2 + 15-6 (+16-1 for 19-2, 16-2 for resolve); 17-1 only for 19-3's operator leg; Epic 18 only if Butler joins (§I) |
| 44 | "19-1 before 19-4" | Dependencies | PARTIAL | true but incomplete: 19-1 → 19-2, 19-1 → 19-3; `--replay` created in 19-4 but used in 19-3 AC1 |

## B. Verb-first

| Token | Status @9f920180 | Definition / creator |
|---|---|---|
| `cargo run -p xtask -- demo-j1` | exists | `xtask/src/main.rs:993-1010` (clap; `--help` resolvable) |
| `demo-j1 --replay` | NEITHER | declared 19-4 AC1; used by 19-3 AC1 and the exit — order defect (§I) |
| `demo-j1 --live-codex` | exists | `main.rs:996-997` |
| `MAOS_INFERENCE_MODE=replay` | NEITHER | 15-6 (backlog); `demo-j1` spawns `maos` without `env_clear` (`demo_j1.rs:542-548`; grep `env_clear` = 0) so the var WOULD propagate; nothing consumes it |
| `MAOS_REPLAY_CASSETTE` | exists, Researcher-only | `env_contract.rs:255`; read `main.rs:4609,4623`; no J1 cassette exists (`crates/maos-journey-test/cassettes/` = j-butler, j-researcher, j0) |
| `maos run <topology> --once` | exists | `worker_spawn.rs:44-72` |
| `--epic <file>` | NEITHER | 19-1 AC1 (rule 1 satisfied); fixture `tests/fixtures/j1/epic.md` uncreated by any AC |
| `maosctl orchestrator queue` | exists (shape wrong) | `cli.rs:750-758` requires `--spirit`; one-shot child today (`subcommands.rs:1656-1676`); retarget = 16-1 AC3 |
| `maosctl halt resolve` (implied by "halt … resume") | exists | `cli.rs:711-727` (`--spirit --kind --text`, Epic-16 note #7) |
| `cargo test … journey_j1` | exists | `discipline.yml:2102-2128` |
| `cargo run -p xtask -- check-j1-loopback-delegation` | exists, **RED** | Blocking, `gate-registry.toml:308-309`; EXIT=1 (#18) |
| `nfr-perf-8-orchestrator-fanout` | exists, advisory | `discipline.yml:1490-1505` |
| `check-exit-commands` (rule 1) | NEITHER | 15-3; can resolve `demo-j1` (xtask has clap) but not `maos run --epic` (no `maos --help`, Epic-15 blocker 1) |

## C. Foundation audit

**19-1** — assumes: (a) a place to loop: the root's own `delegation_emitter` (`main.rs:3395-3396`) is scoped to one `for entry` pass (`:3397`); the scheduler-loaded Orchestrator is "MOVED into `scheduler.load(...)` and unreachable" (`:3389-3392`), `on_idle` inert (`spirits/orchestrator/src/lib.rs:149-157`). VERIFIED absent — the loop is net-new in maos-bin. (b) N frames ⇒ N Workers: one `host` entry pinned by two Blocking controls (#16); N Workers require a new topology or N `[cli_wrapper]` entries and a change to the pins. (c) an inference port on the Orchestrator: absent (#19); the 15-6 amended contract wraps only the shared adapter (`main.rs:3136`) and the Researcher-private one (`:4599`) — the Orchestrator arm is a THIRD adapter 19-1 must build (fine, but say so and cost it). (d) FR21 rows: `write_distillate` has no production caller (#22); `DistillateWriter::write_distillate(spirit_pid, request)` needs a producer pid and the Worker journals under pid 0 (`worker_spawn.rs:732`) — the FR21 gate's prior-ref check (`delegation.rs:276-292`) must accept a distillate produced by the ROOT on the Worker's behalf, a design point no AC states. (e) `env_contract` update: registry format `env_contract.rs:13` + gate (Epic-15 #27) VERIFIED.

**19-2** — assumes: (a) `OrchestratorBufferRegistry` reachable from the topology path: built `main.rs:1994-1995`, passed at `:2656`, used by the one-shot arms `:5688,:5737,:5799` — VERIFIED (zero kernel lines to call it). (b) a long-running daemon holding it: `--once` exits (#28); the POST door is 16-1's (#26). (c) safe point = `Orchestrator::is_safe_point` (`lib.rs:188-190`) + `drain_next` (`:219-227`) — VERIFIED spirit-side. (d) `maosctl` reaching the running daemon: 16-1 (`operator.json` discovery per Epic-16 note #4). (e) the `hello-spirit`-only guard (`main.rs:5667-5672`) must go.

**19-3** — assumes: (a) fixture Worker is a real subprocess — VERIFIED (#29). (b) a cwd seam — ABSENT in kernel (#31). (c) claude adapter oracle to replace — VERIFIED (`worker_cli.rs:498-537`); codex already has an effect oracle (`codex_jsonl_oracle` `:415-465`, `NoEffectEvidence` `:465`). (d) a "real by default" that keeps CI hermetic — contradiction (#30, #33): the fixture is the ONLY worker granted by default (`builtin_fixture_grant` `worker_spawn.rs:157-163`, `:243`). (e) concurrency — ABSENT (#34). (f) 17-1 profile — backlog, unpriced proxy (Epic-17 note).

**19-4** — assumes: (a) `demo-j1` beat model — VERIFIED (`Beat`, `EvidenceState` `gate_common.rs:331-343`), but the target set is wrong (#4). (b) an ambiguity emitter on the Orchestrator — ABSENT (#37); the kernel halt evaluation path is real and already wired for Butler/Mira (`ButlerOrchestratorAdapter` `main.rs:570-591`, `:3814-3829`), so the emitter is maos-bin wiring + a manifest rule + a scalar write in `spirits/orchestrator`, zero kernel. (c) an FR17 digest on the J1 path — ABSENT (#9). (d) `journey_j1` — VERIFIED; the digest/halt events it would assert do not exist yet (events today: `spirit_loaded, delegation_routed, topology_worker_admit, cli_wrapper_loaded, cli_wrapper_exit, worker_completion, delegation_completed, on_idle_fired, drain` — `journey_j1.rs:63-182`). (e) closing the third ABSENT beat (`halt-resume-referential-identity`) — no AC, no seam (`journey_j1.rs:303-308`).

## D. Kernel-Δ audit

| Story | Zero achievable? | Notes |
|---|---|---|
| 19-1 | YES | loop + `--epic` parser + Orchestrator adapter arm live in `maos-bin`/`spirits/orchestrator`; `DistillateWriter` is kernel code called from maos-bin (`main.rs:2379`), no kernel edit |
| 19-2 | YES (0, not 0–10) | registry in the root (`main.rs:1994`), gate spirit-side (`lib.rs:219-227`), POST handler in `maos-control` (not in the kernel-core set); no scheduler hook needed — and `buffer.rs:58-60` forbids one ("NEVER from kernel-internal hooks") |
| 19-3 | **NO as written** | explicit child cwd = `BridgeSpawnSpec.cwd` + `cmd.current_dir` in `runtime.rs` (+3–5 lines, re-pin 24472→~24477, FLAG-Winston) — or a process-global `std::env::set_current_dir` in `worker_spawn.rs` (0 kernel lines; hazardous: the daemon's own relative paths change) — a fork the epic must decide (§H-4) |
| 19-4 | YES | halt via existing `EpistemicScalarPort` adapter wiring; digest via Butler library call or maos-bin; `--replay` in xtask |

## E. Kloc headroom (kloc-check @9f920180, EXIT=1; ceilings `xtask/kloc.toml`)

| Crate | measured / ceiling / headroom | 15-2 grant (Epic-15 §E) | already claimed (Epic 16/18 notes) | Epic 19 ask (estimate) | Verdict |
|---|---|---|---|---|---|
| maos-bin | 17109 / 17109 / **0** (`kloc.toml:343`) | +1200 | 15-6 +150–300; 16 +480–820; 18 +300–680 | 19-1 loop/`--epic`/adapter arm/distillate +150–300 · 19-2 door task +60–120 · 19-3 oracle/cwd/concurrency +180–400 · 19-4 halt wiring/digest render +80–160 → **+470–980** | **over-subscribed by ~0.5–1.5k** → 15-2 AC1 must name Epic 19 explicitly, or push logic into `spirits/orchestrator` (ungoverned) |
| xtask | 41953 / 41953 / **0** (`:212`) | net ≤ 0 | — | `--replay` + cassette env + beat re-labelling +60–150 | needs a named +150 or a reclaim inside 19-4 |
| maos-control | 803 / 1500 / 697 | — | 16-1 POST surface | 19-2 enqueue route +40–80 | fits |
| maos-cli | 5270 / 5270 / 0 (`:330`) | +400 | 16-1 +400–700 | 0 if 16-1 retargets `orchestrator queue`; else +40 | over-subscribed already |
| maos-kernel-core | 18933 / 18933 / 0 (`:195`) | +150 shared | 15-1, 16-5, 17-1, 17-2 | 19-3 cwd +3–5 | needs the grant + re-pin if §H-4 picks the field |
| maos-bench | 1531 / 1631 / 100 | — | — | 19-3 real-spawn bench +80–200 | tight |
| maos-journey-test | 493 / 593 / 100 | — (Epic-16/18 flagged) | 16-2, 18-1 | 0 (journey tests live under `tests/`, excluded) | OK |
| spirits/orchestrator, spirits/butler, spirits/worker | not governed (`kloc_check.rs:184,270`) | — | — | epic parser, decomposition, halt tag, digest composition | free — prefer this home |
| maos-domain | 8695 / 8644 / **−51** (red) | +300 | 16-4/16-5 | 0–30 (a `DigestEvent` type if added) | after 15-2 |

Aggregate 155498 vs `_aggregate_hardfail` (red until 15-2).

## F. Hermetic vs operator

- Exit line: hermetic (fixture worker, isolated home, no secrets) — but not green at HEAD (#4, #18) and not machine-complete without a cassette under 15-6-as-amended (#5).
- `demo-j1 --live-codex` (paid, `CODEX_API_KEY`, named human signer): correctly outside the exit, but it is a bullet, not an `ops-*` row (`sprint-status.yaml:281-284` has four ops rows, none for J1) — rule 2 requires a row (`ops-j1-live-agent-signed-take`).
- 19-3 AC1 "Live (operator lane): real agent CLI under the 17-1 profile": paid + vendor key; needs the row the Epic-17 note proposed (`ops-live-worker-under-17-1-profile`).
- 19-3 AC3 `nfr-perf-8` wall-clock: stays advisory — fine.
- 19-4 AC3 "0 hallucinated on the run": hermetic if the digest is composed from the TL (no model); a Butler-with-inference digest would need a cassette (H-2).
- Two-host signed run: done lane, permanent artifact — fine as a non-row.

## G. Sizing from measurement (J1 lane, `git show --stat`, story-file dates)

| Landed analogue | Commit(s) | Story created → done | Non-test LOC changed | All code LOC |
|---|---|---|---|---|
| j1-crosshost-1a (frame-borne delegation: pump, consumer, `assign_frame_remote`, gate skeleton) | `6827dc87` 08-15 | 08-15 → 08-15 | 1777 | 2940 |
| j1-demo-one-command-scene (`demo-j1`) | `f792f77d` 08-15 | 08-14 → 08-15 | 931 | 1140 |
| j1-crosshost-1b (consent proofs + gate legs) | `87eb6c37` 08-16 | 08-15 → 08-16 | 750 | 2337 |
| j1-crosshost-2a (adapters + oracles + signable worker) | `0769869d` 08-16 | 08-16 → 08-16 | 1882 | 4171 |
| j1-crosshost-2b (cross-host mechanism, two daemons) | `7aa07ee3` 08-17 | 08-16 → 08-17 | 3667 | 5465 |
| j1-crosshost-2c (two-host judge) | `dd4cf959` 08-18 | 08-16 → 08-18 | 646 | 1125 |
| j1-crosshost-2e/2d (six blockers + paid run) | `30a3794d`/`c549bbc0` 08-25 | 08-25 → 08-25 | 804 | 2262 |

Lane tempo: 8 stories, 2026-08-14 → 2026-08-25, ≈10.5k non-test LOC, each story 1–2 calendar days of dev+§A6 plus ~1 day of preflight/round-table (1b re-baseline, 2a round-table 08-16, 2d round-table 08-21).

| Epic 19 story | Analogue(s) | Est. non-test LOC | Dev days | +30 % |
|---|---|---|---|---|
| 19-1 | 1a (1777) + Researcher seam (8.14c `454dba44` +2323, 1 d) | 1.5–2.5k | 3–5 | 4–6.5 |
| 19-2 | 2c (646) + one POST route | 0.6–1.0k | 1.5–2.5 | 2–3 |
| 19-3 | 2a (1882) + 2e (804) | 1.8–2.6k | 3–5 | 4–6.5 |
| 19-4 | demo-j1 (931) + 2c (646) + halt/digest wiring | 1.5–2.2k | 3–4 | 4–5 |

Sum: 14–21 dev days (2.8–4.2 wk) + 4 preflight days + 2–3 governance days (two zero-headroom crates need measured grants each time; a kernel re-pin if §H-4 picks the field) ⇒ **3.9–5.6 wk**; add ~1 wk if 15-6/16-1 have not landed (the Orchestrator adapter and the door then carry their own foundations) ⇒ **4.5–6.5 weeks**. The epic's 4–6 is consistent at the top of the range only with 15-2/15-6/16-1 done.

## H. Fork audit (rule 7)

1. **19-1 AC1/AC3 — what decomposes the epic?** AC1 says "decomposed from the epic", AC3 adds an inference port, `manifest.toml:33-36` says dispatch is deterministic (Decision E). Under `replay` an LLM decomposition is a cassette echo: N = whatever the recorded completion lists; the hermetic exit then proves plumbing, not a decision. RECOMMEND: deterministic split on the fixture's `## Story` sections in `spirits/orchestrator` (N is a testable property; a malformed section is the proven-red), and reserve the inference port for 19-4's ambiguity judgement only (one model call, one cassette, one proven-red). Record as a Decision-E amendment in the manifest comment.
2. **19-4 AC3 — Butler in topology vs daemon renders.** RECOMMEND daemon renders: at topology drain, maos-bin calls `Butler::new().morning_digest(&db, &journal, now, &[], 0.0)` (`spirits/butler/src/lib.rs:675`, already a `maos-bin` dependency `main.rs:4247`) over the run's TL window and prints a `digest` event whose `completions[].source_log_ref` each resolve to a TL row (the `hallucination.rs` pattern). Zero inference, zero kernel, no 18-1 dependency, no Butler class in the topology (which would also change `topology-spirits-loaded`'s exact-three assertion `demo_j1.rs:578-586` and `journey_j1.rs:69-76`).
3. **19-2 — kernel 0–10.** Decide 0 (§D). Also decide the PROCESS: the door only means something against a long-running `maos run <topology>`; state that 19-2's AC1 runs the daemon without `--once` and that the dispatch loop is a tokio task in maos-bin draining `drain_next` after each `delegation_completed`.
4. **19-3 AC2 — cwd placement.** Kernel field (+3–5, FLAG-Winston, honest) vs process-global `set_current_dir` (0, hazardous) vs no cwd (the diff is taken over the inherited cwd, documented). RECOMMEND the kernel field with the grant, and say so in the Kernel-Δ line.
5. **19-3 AC1 — "real by default".** Decide what "default" is: the demo's default topology (`--replay` ⇒ `j1-founder-loop.toml`, `--live-codex|--live-claude` ⇒ the live files) — NOT `spirits/worker/manifest.toml`, which stays the hermetic baseline; the `MAOS_LIVE_AGENT` control and `ci_local_split_…` test stay as they are (not "rewritten"), and the new hermetic test is the three-Worker topology. Retire the slogan.
6. **19-4 AC2 — which tag.** `story.acceptance_criterion.ambiguous` (corpus, no emitter) vs `task.acceptance_criterion.ambiguous` (hello-spirit emitter, 16-2's exit). RECOMMEND one tag repo-wide, `task.acceptance_criterion.ambiguous`, and re-stamp the three `lcas.rs` seeds (`:84,124,164`), so 16-2's HaltRegistry path and 19-4's Orchestrator halt are the same mechanism.
7. **Exit line — two replay selectors.** `MAOS_INFERENCE_MODE=replay` on the xtask AND `--replay` on `demo-j1` (Epic-18 note names the four-way fork). RECOMMEND: the flag alone; `demo-j1 --replay` sets `MAOS_INFERENCE_MODE=replay` + `MAOS_REPLAY_CASSETTE=<j1 cassette>` on the child, so the exit line is `cargo run -p xtask -- demo-j1 --replay`.

## I. Dependency audit

- Stated: "Epic 17 (17-1 profile) and Epic 18 (inference seams). 19-1 before 19-4."
- Hermetic exit actually needs: **15-2** (maos-bin and xtask at 0 headroom, §E), **15-6** (the `MAOS_INFERENCE_MODE` selector; the Orchestrator arm is 19-1's own), **16-1** for 19-2 only (POST door; `maosctl` discovery), **16-2** only for "halt … resume" via `maosctl halt resolve` against the running daemon (the halt FIRING needs nothing from 16 — the kernel evaluation path is wired for Butler/Mira today, `main.rs:570-591,3814-3829`). **17-1 is needed only by 19-3's operator-lane leg** (a real CLI under the profile) and **Epic 18 is not needed at all** under H-2 (Butler's seam is required only if Butler joins the topology with inference). The line is over-tight by two epics for the hermetic exit: 19-1/19-2/19-4 and the hermetic half of 19-3 can run after Epic 16, in parallel with 17/18 — a gain of up to two waves for the founder loop.
- Intra-epic: 19-1 → 19-2 (the door needs the loop); 19-1 → 19-3 (three Workers need the loop and the N-entry topology); 19-1 → 19-4 (stated). `--replay` is used by 19-3 AC1 but created in 19-4 AC1 — move the flag to 19-1 AC1 (the exit's verb, first story).
- Later-epic mechanism used early: none, provided H-2 is taken (else 18-1).

## J. Did R2 fix the prior preflight? (old E18 `epic-18-w3-founder-loop-end-to-end-v08.md` @9f920180~1; preflight `epic-15-20-preflight-2026-09-04.md`)

| Prior false premise | R2 status | Evidence |
|---|---|---|
| `demo-j1 --live` is not a flag (§1 row 18, §2.5) | FIXED | `--live-codex` exists (`xtask/src/main.rs:996-997`); `--replay` declared as 19-4 AC1 |
| no Orchestrator dispatch loop (`main.rs:3434-3440`) | FIXED as premise → now the deliverable | 19-1 AC1 cites it (#15) |
| `MAOS_WORKER_TASK` already removed; shortcut is `MAOS_DELEGATED_GOAL` | FIXED | 19-1 AC2 (#17); mapping row `round2.md:77` |
| two of three ABSENT beats need paid/two-host/human runs | **STILL PRESENT (new form)** | exit demands `tier2-live-agent-signed` non-ABSENT hermetically and calls `two-host-signed-run` ABSENT when it is INDETERMINATE; the third beat has no AC (#4) |
| real agent CLIs cannot run under T3 `--network=none` (§2.3) | RELOCATED to 17-1 | 19-3 AC1 "under the 17-1 profile"; 17-1 itself needs an unpriced proxy (Epic-17 note) |
| `--epic`, `--replay` absent (§2.5) | FIXED (declared as AC1s) | but `--replay` ordering defect (§I); `tests/fixtures/j1/epic.md` uncreated (#14) |
| no hermetic inference seam outside Researcher (§2.4) | FIXED as dependency (15-6) | the Orchestrator arm is not in 15-6's amended scope — 19-1 AC3 must build it (it says so) |
| subprocess Orchestrator cannot take a builder-injected port (§2.1) | FIXED | ADR-060 / `rust-inproc` (#21) |
| "real by default" inverts `smoke_cli_wrapper_8_12` (§3) | **STILL PRESENT** | 19-3 AC3 "rewritten for the new default" — the test is the fail-closed control (#33) |
| old 18-2 AC3 "≤1/20 leak on the 20-story corpus" | FIXED (cut) | now "three concurrent fixture Workers" — but the concurrency substrate is absent (#34) |
| old 18-2 AC2 "kernel-side worktree diff" | RENAMED | now maos-bin + explicit cwd — cwd needs kernel lines (#31) |
| old 18-3 AC2 `task.acceptance_criterion.ambiguous` | RENAMED to `story.…` | created a second spelling; the one with an emitter was dropped (#36) |
| old 18-2 "nfr-perf-8 promoted to blocking" | FIXED | "advisory until wall-clock stable" |
| old 18-3 AC3 "overnight digest cites source_log_ref" | **STILL PRESENT** | no production FR17 digest path; fork hides it (#9, #38) |
| §2.2 kloc blocks product work | FIXED as dependency (15-2) | but 15-2's maos-bin allowance is already over-subscribed before Epic 19 (§E) |
| §2.8 latent defects | NEW ONE FOUND | `check-j1-loopback-delegation` RED since `cert_rotation_trigger_14_2a.rs` landed (14-2a, 2026-08-30): suffix collision `_2a.rs` (#18) — not in 15-1's list |

## K. Verdict, required changes, confirmed-true

**Confidence 40.** Every mechanism the epic names is real and locatable: the FR20 buffer and its spirit-side safe-point gate, the frame-borne delegation, the fixture subprocess Worker, both real-CLI adapters and their asymmetric oracles, `demo-j1`'s honest beat model, `journey_j1`, the Butler digest composer and the hallucination test pattern. What keeps it at 40: the exit block names a claim-table state that the tree cannot produce (an operator-only beat must be non-ABSENT; a beat that is INDETERMINATE by design is called ABSENT; the third ABSENT beat is unowned), the exit command is already red at HEAD through a gate defect nobody lists, 19-3 needs kernel lines it says it does not and proposes rewriting the control that keeps CI unpaid, 19-4's halt and digest rest on zero-caller mechanisms, and seven forks sit inside ACs. The dependency line delays the founder loop by two waves it does not need. All are edit-level; no new epic. **Verdict: needs-rework. Measured duration 4.5–6.5 weeks.**

### REQUIRED CHANGES (OLD → NEW)

1. **Exit block** — OLD: `MAOS_INFERENCE_MODE=replay cargo run -p xtask -- demo-j1 --replay # unattended; ABSENT = {two-host-signed-run} only; digest cites a source ref per completion` → NEW: `cargo run -p xtask -- demo-j1 --replay   # --replay sets MAOS_INFERENCE_MODE=replay + MAOS_REPLAY_CASSETTE=crates/maos-journey-test/cassettes/j1/founder-loop.json on the child (15-6 contract); unattended; every executed beat ok; ABSENT = {tier2-live-agent-signed} (operator row ops-j1-live-agent-signed-take); two-host-signed-run stays INDETERMINATE-by-design (capture present, run not claimed, demo_j1.rs:959-981); halt-resume-referential-identity flips to executed (19-4 AC5); the digest event lists one source_log_ref per completion, each resolving to a TL row`.
2. **Header "Closes"** — OLD: "J1 ABSENT beats except the two-host signed run" → NEW: "the two J1 beats no story owns today (`halt-resume-referential-identity`, and the halt/digest beats 19-4 adds); `tier2-live-agent-signed` stays operator-lane; `two-host-signed-run` is closed by the 2d lane and renders INDETERMINATE by design".
3. **Kernel-Δ line** — OLD: "ZERO kernel-Δ @24472 (19-2 may need ≤10 lines for a safe-point hook; FLAG-Winston if so)" → NEW: "19-1/19-2/19-4 ZERO @24472 (19-2 drains `OrchestratorBufferRegistry` from the composition root, `main.rs:1994`, through `Orchestrator::drain_next`, `lib.rs:219-227` — no kernel hook, `buffer.rs:58-60` forbids one); 19-3 +3–5 kernel lines (`BridgeSpawnSpec.cwd` + `cmd.current_dir`, `runtime.rs:240-269,461-469`), re-pin in the same commit, FLAG-Winston, with a `maos-kernel-core` kloc grant".
4. **19-1 AC1** — OLD: "`MAOS_INFERENCE_MODE=replay maos run spirits/topologies/j1-founder-loop.toml --epic tests/fixtures/j1/epic.md --once` makes the Orchestrator emit N `task.assign` frames decomposed from the epic (…)" → NEW: "`maos run spirits/topologies/j1-founder-loop-epic.toml --epic tests/fixtures/j1/epic.md --once` (both NEW: `--epic` in `worker_spawn.rs:53-66`; the fixture with three `## Story` sections; a NEW topology with three `[cli_wrapper]` entries, because `j1-founder-loop.toml` is pinned to exactly one `host` entry by `check_j1_loopback_delegation.rs` leg 1 and `topology_delegation_1a.rs:236` — the `-crosshost.toml` precedent) makes a maos-bin dispatch loop emit N = 3 `task.assign` frames via `assign_frame_remote` (`main.rs:3528`, needle kept) — one per section, split deterministically in `spirits/orchestrator` (Decision E kept for dispatch; a section without a `## Story` heading is the proven-red) — and run the three Workers in sequence, each closed at a safe point (`delegation_completed` ×3). `demo-j1 --replay` is ADDED here (the exit's verb) as a no-op alias until 19-4 wires the cassette."
5. **19-1 AC2** — OLD: "`MAOS_DELEGATED_GOAL` (`main.rs:3495`) is removed on the loopback arm and `env_contract` updated; `check_j1_loopback_delegation.rs` stays green." → NEW: "`MAOS_DELEGATED_GOAL` (read `main.rs:3497`, registered `env_contract.rs:32`) keeps its cross-host REQUIRED semantics (`:3504-3511`); the loopback fallback string (`:3516-3519`) is replaced by the `--epic` sections; `smoke_cli_wrapper_8_12.rs:216-235` (sentinel goal) is re-keyed to a one-section epic. `check_j1_loopback_delegation.rs` is made green FIRST: its suffix derivation (`J1_TEST_SUFFIXES`, `:114`) is narrowed to J1 files (or `cert_rotation_trigger_14_2a` is enrolled in `discipline.yml:1904-1925`) — measured RED at HEAD, EXIT=1 — and the new epic topology gets its own leg." (Also add this red to 15-1 AC1's list — Epic-15 required change.)
6. **19-1 AC3** — OLD: "Orchestrator gains `with_inference_port` and a capability scope (…today 'provider.complete omitted'); it stays `rust-inproc` (ADR-060)." → NEW: "Orchestrator gains `with_deferred_inference_port` (the Researcher builder shape, `spirits/researcher/src/lib.rs:502-518`) and `[capabilities.required] provider.complete`; the composition root wraps a THIRD `InferencePortAdapter` for it under the 15-6 selector (`main.rs:4590-4641` is the template) — used ONLY for the 19-4 ambiguity judgement; `manifest.toml:33-36` Decision-E comment amended: dispatch deterministic, ambiguity judged. Stays `rust-inproc` (ADR-060, authored by 15-5)."
7. **19-1 AC4** — OLD: "FR21 distillate rows are written for Worker output in production; `followup_dispatch` (`lib.rs:389-401`) consumes them." → NEW: "After each `worker_completion`, the dispatch loop calls `DistillateWriter::write_distillate` (`main.rs:2379`; production callers today = 0) over the run's `CliSubprocessOutput` rows on the Worker's behalf (producer pid = the root's Orchestrator pid, since the Worker journals under pid 0, `worker_spawn.rs:732`), and dispatches section k+1 with `followup_dispatch` (`lib.rs:389-401`, production callers today = 0) carrying that `PriorDistillateRef`; the FR21 60 s window check (`delegation.rs:276-292`) passes on the real rows; a follow-up built with `first_dispatch` is the proven-red."
8. **19-2 AC1** — OLD: "`maosctl orchestrator queue "…"` during an in-flight delegation is applied at the next safe point of the running daemon (`dequeue_at_safe_point` gets its first caller; enqueue arrives over the 16-1 surface)." → NEW: "With `maos run spirits/topologies/j1-founder-loop-epic.toml --epic … ` running WITHOUT `--once` (serving loop `main.rs:4020-4031`), `maosctl orchestrator queue --spirit orchestrator "<instruction>"` (`cli.rs:750-758`; retargeted by 16-1 AC3 from the `MAOS_ONE_SHOT=orchestrator-queue` child, `subcommands.rs:1656-1676`, whose `hello-spirit`-only guard `main.rs:5667-5672` is deleted) POSTs to the 16-1 surface; the daemon enqueues into `OrchestratorBufferRegistry` (`main.rs:1994`) and the 19-1 loop drains it via `Orchestrator::drain_next(|| buffer.dequeue_at_safe_point())` (`lib.rs:219-227`; `buffer.rs:62`, first production caller) only after the in-flight `delegation_completed`; a `queue` issued mid-delegation is observed in the next `task.assign`, never before; `maosctl orchestrator status` reads the live count."
9. **19-2 AC2** — OLD: "The `Ctx` ABI is unchanged (`lib.rs:143-147`); the loop is daemon-side." → NEW: "`crates/maos-spirit-abi/src/ctx.rs:58` is unchanged (the doc at `spirits/orchestrator/src/lib.rs:143-147` stays true); the loop is a maos-bin tokio task, not an `on_idle` hook (`lib.rs:149-157` stays inert); kernel-Δ 0."
10. **19-3 AC1** — OLD: "Hermetic: `demo-j1 --replay` runs the fixture Worker. Live (operator lane): the default worker manifest is a real agent CLI under the 17-1 profile." → NEW: "`demo-j1 --replay` runs `j1-founder-loop-epic.toml` with three fixture Workers (`worker-cli-fixture`, built-in grant `worker_spawn.rs:157-163`); `demo-j1 --live-codex|--live-claude` selects the existing live topologies (`j1-founder-loop-codex.toml`; a `-claude.toml` twin of `manifest-claude.toml`) under `MAOS_LIVE_AGENT` + `MAOS_HOST_GRANTS` — operator row `ops-live-worker-under-17-1-profile` once 17-1 lands. `spirits/worker/manifest.toml` stays the hermetic baseline (11 consumers); `live_agent_gate` (`worker_cli.rs:860`) and `ci_local_split_refuses_a_granted_real_agent_without_the_live_flag` (`smoke_cli_wrapper_8_12.rs:147`) are unchanged."
11. **19-3 AC2** — OLD: "A worktree-diff effect oracle in `worker_spawn.rs` with an explicit child cwd replaces the claude adapter's effect-blind verdict (`worker_cli.rs:492-495`); RELEASE-HOLDS row 16 closed." → NEW: "`BridgeSpawnSpec` gains `cwd: Option<PathBuf>` applied via `cmd.current_dir` (`runtime.rs:240-269,461-469`; +3–5 kernel lines, re-pin, FLAG-Winston); `run_cli_wrapper_manifest` passes a per-run workspace dir, snapshots it before spawn and diffs after exit; `claude_result_object_oracle` (`worker_cli.rs:498-537`) becomes `Completed` only when the diff is non-empty, else `NotCompleted(NoEffectEvidence)` (the codex shape, `:465`); the fake-`claude` 'I have written the file' vector (RELEASE-HOLDS row 16, measured 2026-08-22) is the proven-red; row 16 closed and `runbook-j1-t8-two-host-paid-run.md` abort condition 9 demoted."
12. **19-3 AC3** — OLD: "`ci_local_split_…` is rewritten for the new default; three concurrent fixture Workers run hermetically; `nfr-perf-8` measures the real spawn path (advisory until wall-clock stable)." → NEW: "The 19-1 loop runs the three `[cli_wrapper]` entries CONCURRENTLY (`run_cli_wrapper_manifest` `worker_spawn.rs:384` made async or run on `spawn_blocking`; three distinct `child_pid`s alive at once asserted by a new `crates/maos-bin/tests/worker_concurrency_19_3.rs`, enrolled in the `check-j1-loopback-delegation` job per its suffix rule); `orchestrator_fanout_nfr_perf_8.rs:22` gains a `MAOS_BENCH_REAL_SPAWN=1` mode spawning fixture subprocesses (maos-bench +80–200 of 100 headroom → 15-2 row); job stays `continue-on-error: true` (`discipline.yml:1493`)."
13. **19-4 AC1** — OLD: "The exit command runs unattended; `demo_j1.rs` renders only `two-host-signed-run` as ABSENT (today three: `:888-905`)." → NEW: "`demo-j1 --replay` sets the 15-6 env on the child (`demo_j1.rs:542-548`), runs the epic topology, and renders: every executed beat `ok`; `tier2-live-agent-signed` ABSENT (owner `ops-j1-live-agent-signed-take`); `two-host-signed-run` INDETERMINATE (`:959-981`, unchanged); `halt-resume-referential-identity` EXECUTED (AC5); three NEW executed beats `epic-decomposed-n-frames`, `ambiguity-halt-within-2-frames`, `digest-cites-source-refs`. `unlanded_beats()` (`:888-906`) shrinks to one."
14. **19-4 AC2** — OLD: "A planted ambiguous story makes the Orchestrator halt within 2 frames on `story.acceptance_criterion.ambiguous` (the tag exists only in `lcas.rs:84` today; no emitter)." → NEW: "The fixture epic's second section is ambiguous; the Orchestrator's inference judgement (19-1 AC3, replayed from the J1 cassette) writes the scalar `dispatch_ambiguity` through a NEW `Orchestrator::with_scalar_port` wired like Mira's (`main.rs:3796-3835`; `requires_epistemic_halt_port` `:553-567` gains `dispatch_ambiguity`), the existing manifest rule `manifest.toml:40-43` fires the kernel halt (`process_scalar_write`), and the halt frame carries tag `task.acceptance_criterion.ambiguous` — ONE spelling repo-wide: the hello-spirit emitter (`maos-spirit-hello/src/lib.rs:148`) and the three `lcas.rs` seeds (`:84,124,164`) are aligned to it; halt lands before the third `task.assign` (≤2 frames); a cassette whose judgement says 'clear' is the proven-red (no halt, 3 frames)."
15. **19-4 AC3** — OLD: "Butler joins the J1 topology (or the daemon renders) so the digest cites `source_log_ref` for every completion; 0 hallucinated on the run." → NEW: "At topology drain the daemon composes the FR17 digest by calling `Butler::morning_digest` (`spirits/butler/src/lib.rs:675`, production callers today = 0) over the run's TL window — no Butler class in the topology, no inference — and prints a `digest` event; every `completions[].source_log_ref` resolves to a `TaskComplete`/`CliSubprocessOutput` row (the `spirits/butler/tests/hallucination.rs` check, run in-process); a planted extra completion in the event is the proven-red. FR17's 100-digest corpus floor is NOT claimed by this run."
16. **19-4 NEW AC5** — "Halt → `maosctl halt resolve <id> --spirit orchestrator --kind provided-context --text …` (`cli.rs:711-727`, over the 16-1 surface) → resume dispatches the third section; the post-resume digest cites the pre-halt `TrajectoryRef` (`journey_j1.rs:303-308` oracle un-deferred); `halt-resume-referential-identity` (`demo_j1.rs:900-904`) becomes an executed beat."
17. **19-4 AC4** — OLD: "`journey_j1` asserts the halt and digest beats." → NEW: "`journey_j1.rs` gains assertions on the `halt`, `digest` and three `delegation_completed` events (no wall-clock, `guards_meta.rs:24-25`); the J1 cassette `crates/maos-journey-test/cassettes/j1/founder-loop.json` is committed with `provenance: seed` (15-6 AC2) and `MAOS_REPLAY_STRICT=1`."
18. **Tracker** — add `ops-j1-live-agent-signed-take: backlog` (paid `demo-j1 --live-codex|--live-claude`, named signer) and `ops-live-worker-under-17-1-profile` beside `sprint-status.yaml:281-284`; update the four 19-x row comments to the ACs above.
19. **Dependencies** — OLD: "Epic 17 (17-1 profile) and Epic 18 (inference seams). 19-1 before 19-4." → NEW: "Epic 15 (15-2 with a named Epic-19 row: maos-bin +500–1000, xtask +150, maos-bench +200, maos-kernel-core +5; 15-6 selector; 15-5 ADR-060). Epic 16: 16-1 before 19-2 and 19-4 AC5; 16-2 before 19-4 AC5. 17-1 gates ONLY the operator row `ops-live-worker-under-17-1-profile`. Epic 18 not required. Order: 19-1 → 19-2, 19-1 → 19-3, 19-1 → 19-4; 19-2 ∥ 19-3."
20. **Duration** — OLD: "4–6 weeks (measured)" → NEW: "4.5–6.5 weeks (measured from the J1 lane 1a/1b/2a/2b/2c/2e + demo-j1, +30 %; 4–5.5 if 15-2/15-6/16-1 have landed)".
21. **Epic 15 (cross-note)** — 15-1 AC1 "4 red gates" → 5: add `check-j1-loopback-delegation` (Blocking, EXIT=1 @9f920180, suffix collision with `cert_rotation_trigger_14_2a.rs`).

### CONFIRMED TRUE (do not re-check)

`demo-j1` exists with `--live-codex/--codex-topology/--keep-home/--skip-build/--skip-gate` (`xtask/src/main.rs:993-1010`) and spawns `maos run spirits/topologies/j1-founder-loop.toml --once` with an isolated `MAOS_HOME`, PATH-prepended `target/debug`, `MAOS_LIVE_AGENT` removed, no `env_clear` (`demo_j1.rs:542-548`) · the composition root forbids a loop and emits one `assign_frame_remote` per `host` entry (`main.rs:3434-3441,3528`) · `MAOS_DELEGATED_GOAL` read once at `main.rs:3497`, cross-host REQUIRED, loopback fallback (`:3504-3519`), registered `env_contract.rs:32` · `Orchestrator::{is_safe_point,drain_next,first_dispatch,followup_dispatch,assign_frame_remote}` (`spirits/orchestrator/src/lib.rs:188,219,372,389,332`); `on_idle` inert (`:149-157`) · `OrchestratorBuffer::dequeue_at_safe_point` (`buffer.rs:62`) has zero production callers; `OrchestratorBufferRegistry` built at `main.rs:1994` · `maosctl orchestrator queue --spirit <id> <instruction>` (`cli.rs:750-758`) → one-shot child, `hello-spirit` only (`main.rs:5667-5672`) · `Ctx` ABI `crates/maos-spirit-abi/src/ctx.rs:58` · fixture Worker is a real subprocess (`worker-cli-fixture.rs:20-45`, `runtime.rs:449-476`, `journey_j1.rs:138-146`); `SUPPORTED_WORKER_CLIS = [fixture, codex, claude]` (`worker_cli.rs:851`); `live_agent_gate` `:860`; codex effect oracle `:415-465`; claude effect-blind `:498-537` (doc `:490-497`) · RELEASE-HOLDS row 16 at `RELEASE-HOLDS.md:63` · `BridgeSpawnSpec` has no cwd (`runtime.rs:240-269`) · `run_cli_wrapper_manifest` synchronous (`worker_spawn.rs:384`; `main.rs:3445`); `wait_and_finalize(…, 0, …)` (`:732`) · `nfr-perf-8-orchestrator-fanout` advisory, in-process (`discipline.yml:1490-1505`; bench `:22`) · `unlanded_beats()` = 3 (`demo_j1.rs:888-906`); `apply_capture_judgement` → INDETERMINATE when the capture is present (`:959-981`); the capture IS present (`j1-two-host-evidence/README.md:11-18`) · `check-j1-loopback-delegation` Blocking (`gate-registry.toml:308-309`), job `discipline.yml:1904-1925`, RED at HEAD via `J1_TEST_SUFFIXES` (`:114`) · `journey_j1.rs` three tests, CI job `:2102-2128`, resume oracle deferred `:303-308` · `story.acceptance_criterion.ambiguous` at `lcas.rs:84,124,164`; `task.acceptance_criterion.ambiguous` emitter `maos-spirit-hello/src/lib.rs:148` · Orchestrator manifest: `rust-inproc`, no `[capabilities.required]`, rules on `dispatch_ambiguity`/`delegation_confidence` (`manifest.toml:7,33-48`) · `Butler::morning_digest` `spirits/butler/src/lib.rs:675`, zero production callers; `source_log_ref` fields `:266,273` · `write_distillate` zero production callers (`main.rs:6041` smoke, `:8986` harness); `DistillateWriter` built `:2379` · kloc: maos-bin 17109/17109, xtask 41953/41953, maos-kernel-core 18933/18933, maos-cli 5270/5270, maos-control 803/1500, maos-bench 1531/1631, maos-domain 8695/8644 red; `spirits/` ungoverned (`kloc_check.rs:184,270`) · `docs/adr` tops at ADR-059 · tracker rows `sprint-status.yaml:260-265`; ops rows `:281-284` (none for J1) · J1 lane: 8 stories 2026-08-14 → 2026-08-25 (§G table).
